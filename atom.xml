<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>杰尼龟的笔记</title>
  
  
  <link href="https://swagch.github.io/atom.xml" rel="self"/>
  
  <link href="https://swagch.github.io/"/>
  <updated>2026-07-16T16:00:00.000Z</updated>
  <id>https://swagch.github.io/</id>
  
  <author>
    <name>无敌杰尼龟</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>博客重新出发</title>
    <link href="https://swagch.github.io/2026/06/08/%E5%8D%9A%E5%AE%A2%E9%87%8D%E6%96%B0%E5%87%BA%E5%8F%91/"/>
    <id>https://swagch.github.io/2026/06/08/%E5%8D%9A%E5%AE%A2%E9%87%8D%E6%96%B0%E5%87%BA%E5%8F%91/</id>
    <published>2026-06-08T16:00:00.000Z</published>
    <updated>2026-07-16T16:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/posts/2026/blog-relaunch-cover.png" alt="博客重新出发封面图"></p><p>博客源码重新搭建完成，后续会继续记录技术学习、项目实践和问题排查过程。</p><span id="more"></span><h2 id="为什么重新搭建"><a href="#为什么重新搭建" class="headerlink" title="为什么重新搭建"></a>为什么重新搭建</h2><p>之前只保留了静态站点产物，源码目录已经丢失。为了后续能继续维护文章、主题和部署流程，这次重新生成了一个完整的 Hexo 源码项目。</p><h2 id="当前技术栈"><a href="#当前技术栈" class="headerlink" title="当前技术栈"></a>当前技术栈</h2><ul><li>Hexo：静态博客生成器</li><li>NexT：Hexo 主题</li><li>GitHub Pages：静态站点托管</li><li>GitHub Actions：后续可用于自动构建和部署</li></ul><h2 id="后续计划"><a href="#后续计划" class="headerlink" title="后续计划"></a>后续计划</h2><ol><li>整理 Java 后端开发笔记</li><li>梳理并发编程和 JVM 相关知识</li><li>记录 Linux、Nginx、部署排查经验</li><li>逐步完善博客的搜索、订阅和 SEO 配置</li></ol>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/posts/2026/blog-relaunch-cover.png&quot; alt=&quot;博客重新出发封面图&quot;&gt;&lt;/p&gt;
&lt;p&gt;博客源码重新搭建完成，后续会继续记录技术学习、项目实践和问题排查过程。&lt;/p&gt;</summary>
    
    
    
    <category term="随笔" scheme="https://swagch.github.io/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="Hexo" scheme="https://swagch.github.io/tags/Hexo/"/>
    
    <category term="GitHub Pages" scheme="https://swagch.github.io/tags/GitHub-Pages/"/>
    
  </entry>
  
  <entry>
    <title>2025 AI编程实践复盘：效率、安全与能力边界</title>
    <link href="https://swagch.github.io/2025/12/28/2025-12-28-2025%E5%B9%B4AI%E7%BC%96%E7%A8%8B%E5%AE%9E%E8%B7%B5%E5%A4%8D%E7%9B%98/"/>
    <id>https://swagch.github.io/2025/12/28/2025-12-28-2025%E5%B9%B4AI%E7%BC%96%E7%A8%8B%E5%AE%9E%E8%B7%B5%E5%A4%8D%E7%9B%98/</id>
    <published>2025-12-28T14:10:00.000Z</published>
    <updated>2026-07-16T16:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/posts/2025/ai-year-review-cover.png" alt="2025 AI 编程复盘封面图"></p><p>年初第一次认真使用 AI 编程时，我期待的是“少写一些代码”。到年底回头看，真正发生的变化更像是：代码出现得更快了，于是判断什么值得写、什么可以相信，变得更重要。</p><p>这一年我用 AI 解释陌生项目、补测试、改接口、做原型，也让它带我走过几次很完整的弯路。没有“一人顶一个团队”的故事，倒是积累了一些比工具榜单更稳定的认识。</p><span id="more"></span><h2 id="最明显的收益，不是生成速度"><a href="#最明显的收益，不是生成速度" class="headerlink" title="最明显的收益，不是生成速度"></a>最明显的收益，不是生成速度</h2><p>AI 当然能很快写出 DTO、测试夹具和重复转换代码，但我认为更大的收益是降低了“开始理解”的成本。</p><p>面对陌生模块，以前我要在 Controller、Service、Mapper 和配置之间跳很多次，脑中才能形成一张模糊地图。现在可以让模型先搜索引用、整理调用链、列出不确定点，我再沿着证据核对。它不是替我理解，而是帮我更快找到值得理解的位置。</p><p>另一个收益是候选方案变便宜了。过去想到一个实现，常常会因为试错成本而直接做下去；现在可以先让模型列出三种方案，比较改动范围、兼容性和失败方式，再选择最朴素的一种。</p><p>这一点悄悄改变了我的工作节奏：不再急着证明第一个想法正确，而是更愿意在写代码前暴露它的问题。</p><h2 id="也有一些“效率”后来被证明是借来的"><a href="#也有一些“效率”后来被证明是借来的" class="headerlink" title="也有一些“效率”后来被证明是借来的"></a>也有一些“效率”后来被证明是借来的</h2><p>有一次我让 AI 为旧接口补参数校验。十分钟后代码、测试和异常处理都齐了，看起来比手写快很多。Review 时才发现，它创建了第二套错误码，并把空字符串行为从“忽略”改成“报错”。为了恢复兼容行为，我花的时间比从头写还多。</p><p>还有一次生成前端页面，视觉效果几轮就接近预期，我便把“能操作”等同于“已完成”。到移动端测试时，弹窗按钮被键盘遮住；刷新页面后，刚编辑的数据也全部丢失。原型阶段的速度，被我错误地当成了交付阶段的进度。</p><p>这些经历让我把 AI 带来的时间分成两类：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">真正节省的时间：</span><br><span class="line">搜索、归纳、机械修改、生成测试骨架、整理文档。</span><br><span class="line"></span><br><span class="line">可能延期偿还的时间：</span><br><span class="line">未经验证的代码、范围过大的重构、重复抽象、隐藏的兼容变化。</span><br></pre></td></tr></table></figure><p>只有完成测试和 Review 后，才知道一次生成到底属于哪一类。</p><h2 id="我记录了三十个任务"><a href="#我记录了三十个任务" class="headerlink" title="我记录了三十个任务"></a>我记录了三十个任务</h2><p>下半年我抽样记录了三十个使用 AI 的开发任务，不做严谨统计，只想观察自己的使用模式。</p><p>结果大致是：</p><table><thead><tr><th>任务类型</th><th>主观效果</th><th>常见问题</th></tr></thead><tbody><tr><td>解释代码与调用链</td><td>很稳定</td><td>偶尔漏掉 XML、反射和动态配置</td></tr><tr><td>补边界测试</td><td>稳定</td><td>容易围绕当前实现写测试</td></tr><tr><td>局部重复重构</td><td>较稳定</td><td>喜欢引入新抽象</td></tr><tr><td>明确的小功能</td><td>有明显帮助</td><td>需求遗漏会被模型自然补全</td></tr><tr><td>跨模块功能</td><td>波动较大</td><td>上下文冲突、改动范围膨胀</td></tr><tr><td>性能与线上故障</td><td>适合辅助</td><td>没有现场指标时容易泛化猜测</td></tr></tbody></table><p>成功率与提示词是否“高级”关系不大，与任务是否可验证关系很大。输入、输出和完成条件越清楚，AI 越可靠；涉及隐含业务规则和多人决策时，它只能加速探索，无法代替沟通。</p><h2 id="安全问题终于从提醒变成了习惯"><a href="#安全问题终于从提醒变成了习惯" class="headerlink" title="安全问题终于从提醒变成了习惯"></a>安全问题终于从提醒变成了习惯</h2><p>年初我理解的安全，主要是“不把密码发给 AI”。实际使用后，边界远不止密码：</p><ul><li>日志里的手机号、邮箱和业务编号；</li><li>截图里的 Cookie、内网域名和浏览器插件；</li><li>数据库样本中的客户记录；</li><li>私有依赖名和未公开产品计划；</li><li>AI 自动执行的删除、发布和迁移动作；</li><li>生成依赖的许可证与供应链风险。</li></ul><p>现在我会在提交上下文前先做脱敏，生产操作保持人工确认，新增依赖单独检查来源与维护状态。高风险任务宁可少提供一点材料，也不突破团队的数据边界。</p><p>安全不是在提示词末尾加一句“请注意安全”，而是控制模型能看到什么、工具能执行什么、哪些动作必须有人批准。</p><h2 id="有些能力确实会退化"><a href="#有些能力确实会退化" class="headerlink" title="有些能力确实会退化"></a>有些能力确实会退化</h2><p>当补全随手可得，我有一段时间不再认真记常用 API；遇到构建错误，第一反应也从读日志变成把日志发给模型。短期感觉轻松，几个月后却发现自己对一些基础问题的判断变慢了。</p><p>我不认为必须拒绝工具来保持“纯手工能力”，但需要区分哪些能力可以外包，哪些是工程师的地基。</p><p>我愿意少记：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">不常用命令的完整参数</span><br><span class="line">框架样板代码</span><br><span class="line">格式转换和正则细节</span><br></pre></td></tr></table></figure><p>我仍然要练：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">读异常和指标</span><br><span class="line">理解事务、并发与数据一致性</span><br><span class="line">判断接口和安全边界</span><br><span class="line">设计能证明行为的测试</span><br><span class="line">估算变更影响与回滚成本</span><br></pre></td></tr></table></figure><p>模型可以在这些问题上给建议，但如果我没有自己的判断，就无法识别一份流畅答案中的错误前提。</p><h2 id="Vibe-Coding-留给我的，不只是争议"><a href="#Vibe-Coding-留给我的，不只是争议" class="headerlink" title="Vibe Coding 留给我的，不只是争议"></a>Vibe Coding 留给我的，不只是争议</h2><p>这一年“Vibe Coding”被反复讨论。有人把它当创造力释放，有人把它当不负责任的代名词。我更愿意把它看成一种新的交互层：人用目标和反馈驱动软件变化，不必亲手输入每一行。</p><p>它让我做了几个以前懒得开始的小工具，也让非前端出身的我更快验证页面想法。这种低门槛很有价值。</p><p>但只要原型开始保存真实数据、接入登录、交给别人使用，氛围就必须让位给工程。用户不会因为代码由 AI 生成，就接受数据丢失或权限泄露。</p><p>探索时大胆，交付时保守，这两种态度并不冲突。</p><h2 id="年底留下的六条判断"><a href="#年底留下的六条判断" class="headerlink" title="年底留下的六条判断"></a>年底留下的六条判断</h2><ol><li>AI 最适合加速有反馈的任务，不适合替代没有答案的业务决策。</li><li>项目上下文比华丽提示词更重要，测试反馈比模型自评更可靠。</li><li>生成代码的边际成本很低，长期维护代码的成本没有消失。</li><li>改动越快，越需要小步骤、清晰差异和可回退提交。</li><li>安全边界要由权限和流程保证，不能依赖模型“自觉”。</li><li>工程师的价值不会停留在敲代码，但理解代码仍是做判断的基础。</li></ol><h2 id="明年想做得更少一点"><a href="#明年想做得更少一点" class="headerlink" title="明年想做得更少一点"></a>明年想做得更少一点</h2><p>明年我不准备追每一个新模型和插件。更想把几件普通的事做扎实：给常见任务建立上下文模板，让测试和静态检查更早进入反馈循环，把有效规则留在仓库里，并定期删除 AI 制造的无用抽象。</p><p>我也会保留一些完全不用 AI 的练习时间，独立读源码、定位问题、写设计。不是为了证明人比模型强，而是确保工具失效或答案冲突时，我仍然能继续工作。</p><p>2025 年没有让我相信“程序员很快不需要编程”，但让我重新理解了编程：输入代码只是其中一部分，更多时间其实花在理解问题、组织上下文、验证结果和承担后果上。</p><p>AI 把这一点照得更清楚了。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/posts/2025/ai-year-review-cover.png&quot; alt=&quot;2025 AI 编程复盘封面图&quot;&gt;&lt;/p&gt;
&lt;p&gt;年初第一次认真使用 AI 编程时，我期待的是“少写一些代码”。到年底回头看，真正发生的变化更像是：代码出现得更快了，于是判断什么值得写、什么可以相信，变得更重要。&lt;/p&gt;
&lt;p&gt;这一年我用 AI 解释陌生项目、补测试、改接口、做原型，也让它带我走过几次很完整的弯路。没有“一人顶一个团队”的故事，倒是积累了一些比工具榜单更稳定的认识。&lt;/p&gt;</summary>
    
    
    
    <category term="随笔" scheme="https://swagch.github.io/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="年度总结" scheme="https://swagch.github.io/tags/%E5%B9%B4%E5%BA%A6%E6%80%BB%E7%BB%93/"/>
    
    <category term="AI编程" scheme="https://swagch.github.io/tags/AI%E7%BC%96%E7%A8%8B/"/>
    
    <category term="Vibe Coding" scheme="https://swagch.github.io/tags/Vibe-Coding/"/>
    
    <category term="程序员成长" scheme="https://swagch.github.io/tags/%E7%A8%8B%E5%BA%8F%E5%91%98%E6%88%90%E9%95%BF/"/>
    
  </entry>
  
  <entry>
    <title>我的AI编程工作流：让模型先读懂项目再动手</title>
    <link href="https://swagch.github.io/2025/09/14/2025-09-14-%E6%88%91%E7%9A%84AI%E7%BC%96%E7%A8%8B%E5%B7%A5%E4%BD%9C%E6%B5%81/"/>
    <id>https://swagch.github.io/2025/09/14/2025-09-14-%E6%88%91%E7%9A%84AI%E7%BC%96%E7%A8%8B%E5%B7%A5%E4%BD%9C%E6%B5%81/</id>
    <published>2025-09-14T02:20:00.000Z</published>
    <updated>2026-07-16T16:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/posts/2025/ai-workflow-cover.png" alt="AI 编程工作流封面图"></p><p>AI 编程最浪费时间的场景，不是模型写不出代码，而是它在错误的项目理解上连续工作了半小时：改错入口、重复已有工具类、使用不兼容版本，最后生成一大份很难审查的差异。</p><p>我逐渐把使用方式从“想到什么问什么”改成了一条固定工作流。它不绑定某个模型或编辑器，核心只有一句话：<strong>先让模型用证据还原项目，再让它在明确边界内修改。</strong></p><p>这篇按 Runbook 来写，可以直接拿一个小需求跟着走一遍。</p><span id="more"></span><h2 id="入口：先给任务分级"><a href="#入口：先给任务分级" class="headerlink" title="入口：先给任务分级"></a>入口：先给任务分级</h2><p>不是每个任务都值得启动完整流程。我先按影响范围分三档：</p><table><thead><tr><th>等级</th><th>例子</th><th>工作方式</th></tr></thead><tbody><tr><td>L1 可逆</td><td>改文案、补测试、调整局部样式</td><td>快速修改，执行局部验证</td></tr><tr><td>L2 模块内</td><td>新增接口字段、修改查询、重构 Service</td><td>先读调用链，写计划，小步提交</td></tr><tr><td>L3 高风险</td><td>权限、支付、迁移、生产运维</td><td>人主导设计，双人审查，演练与回滚</td></tr></tbody></table><p>风险不只看代码行数。把按钮颜色改错通常是 L1，删除脚本即使只有一行也可能是 L3。分级决定需要多少上下文、测试和人工确认。</p><p>下面的完整流程主要面向 L2。L1 可以裁剪，L3 还要叠加团队正式变更流程。</p><h2 id="检查点一：把请求改写成任务说明"><a href="#检查点一：把请求改写成任务说明" class="headerlink" title="检查点一：把请求改写成任务说明"></a>检查点一：把请求改写成任务说明</h2><p>原始需求经常是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">列表加一个状态筛选。</span><br></pre></td></tr></table></figure><p>我不会直接让 AI 改代码，而是先补一张短任务卡：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">问题：运营无法只查看“等待回访”的客户。</span><br><span class="line">目标：客户列表和导出增加 followStatus 筛选。</span><br><span class="line">输入：可选枚举 PENDING、DONE；不传保持原行为。</span><br><span class="line">范围：后端查询与现有前端筛选，不改数据库。</span><br><span class="line">非目标：不重构客户状态模型，不增加批量处理。</span><br><span class="line">验收：列表与导出结果一致；非法值返回现有参数错误码。</span><br></pre></td></tr></table></figure><p>“非目标”非常重要。模型擅长顺手完善周边，如果不写边界，一个筛选需求可能变成状态模型重构。</p><p>任务卡不需要一次写对。信息缺失时，让 AI 根据现有描述列出待确认问题，我再向产品或代码事实求证。需求澄清不能由模型自问自答。</p><h2 id="检查点二：只读探索仓库"><a href="#检查点二：只读探索仓库" class="headerlink" title="检查点二：只读探索仓库"></a>检查点二：只读探索仓库</h2><p>第一轮明确要求不修改文件，只回答项目现状。我通常让模型查这些位置：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">构建文件：Java、框架、依赖版本</span><br><span class="line">项目入口：模块边界和启动方式</span><br><span class="line">请求入口：Controller、路由或消息消费者</span><br><span class="line">数据流：Service、Mapper、外部客户端</span><br><span class="line">相邻实现：项目里已经完成的类似功能</span><br><span class="line">测试：当前使用的框架、夹具和断言风格</span><br><span class="line">规范：README、AGENTS.md、架构决策和 CI 配置</span><br></pre></td></tr></table></figure><p>探索结果必须带证据，格式可以很简单：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">结论：列表和导出共用 CustomerQueryCondition。</span><br><span class="line">证据：CustomerPageService.java:48、CustomerExportService.java:62。</span><br><span class="line"></span><br><span class="line">结论：项目要求 JDK 8。</span><br><span class="line">证据：pom.xml 的 maven.compiler.source=1.8。</span><br><span class="line"></span><br><span class="line">待确认：非法枚举由全局异常处理还是 Controller 转换。</span><br></pre></td></tr></table></figure><p>“证据”和“待确认”能阻止猜测混进结论。若模型找不到某个类，就继续搜索或明确不存在，不根据常见命名补一个出来。</p><h3 id="控制上下文预算"><a href="#控制上下文预算" class="headerlink" title="控制上下文预算"></a>控制上下文预算</h3><p>读仓库不是把所有文件一次塞给模型。我会沿调用链逐层加载：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">入口文件</span><br><span class="line">  -&gt; 它直接调用的接口</span><br><span class="line">  -&gt; 对应实现与数据对象</span><br><span class="line">  -&gt; 一个最接近的参考实现</span><br><span class="line">  -&gt; 相关测试和构建配置</span><br></pre></td></tr></table></figure><p>生成目录、压缩后的前端产物、重复日志和无关模块先排除。上下文足够大时，信息选择比信息数量更重要。</p><h2 id="检查点三：修改前先审计划"><a href="#检查点三：修改前先审计划" class="headerlink" title="检查点三：修改前先审计划"></a>检查点三：修改前先审计划</h2><p>项目地图清楚后，让 AI 给出计划，但暂时不写代码。一个可审查的计划至少包含：</p><ol><li>实际要修改的文件；</li><li>每个文件为什么要改；</li><li>数据和调用路径怎样变化；</li><li>空值、异常、并发与兼容边界；</li><li>要新增或运行哪些测试；</li><li>仍然无法确认的风险。</li></ol><p>计划阶段最容易发现范围膨胀。例如任务只需要复用已有查询条件，计划却新建 Repository、Assembler 和通用查询框架，这时删掉比写完后再删便宜得多。</p><p>我还会要求把步骤拆到可独立验证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">1. 先增加请求字段和参数校验，运行接口层测试；</span><br><span class="line">2. 再增加 Mapper 条件，运行数据库测试；</span><br><span class="line">3. 接入列表并验证原行为；</span><br><span class="line">4. 接入导出并做结果对照；</span><br><span class="line">5. 最后清理重复与更新文档。</span><br></pre></td></tr></table></figure><p>一条计划如果要同时修改二十个文件才可运行，通常还没有找到合适的切分点。</p><h2 id="检查点四：按小循环实施"><a href="#检查点四：按小循环实施" class="headerlink" title="检查点四：按小循环实施"></a>检查点四：按小循环实施</h2><p>实施阶段我使用同一条循环：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">修改一个职责</span><br><span class="line">  -&gt; 格式与静态检查</span><br><span class="line">  -&gt; 运行最小相关测试</span><br><span class="line">  -&gt; 阅读版本差异</span><br><span class="line">  -&gt; 记录结果</span><br><span class="line">  -&gt; 进入下一步</span><br></pre></td></tr></table></figure><p>这里“阅读差异”不能省。AI 有时会在目标代码之外格式化整个文件、调整错误文案或升级依赖。测试可能照样通过，但 Review 成本和回归范围已经扩大。</p><p>每轮我会检查：</p><ul><li>是否出现任务外文件；</li><li>是否复制了已有能力；</li><li>是否改变公共接口或默认值；</li><li>是否添加未经批准的依赖；</li><li>是否打印密钥、个人数据或完整请求；</li><li>是否使用项目版本不支持的语法；</li><li>注释是在解释原因，还是重复代码表面含义。</li></ul><p>发现方向错误时，优先回到最后一个小步骤，而不是继续提示模型在错误实现上打补丁。</p><h2 id="检查点五：建立验证阶梯"><a href="#检查点五：建立验证阶梯" class="headerlink" title="检查点五：建立验证阶梯"></a>检查点五：建立验证阶梯</h2><p>“已完成”需要对应证据。我把验证从便宜到昂贵排成阶梯：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">语法与格式</span><br><span class="line">  -&gt; 编译 / 类型检查</span><br><span class="line">  -&gt; 目标单元测试</span><br><span class="line">  -&gt; 模块测试</span><br><span class="line">  -&gt; 集成或端到端测试</span><br><span class="line">  -&gt; 构建生产产物</span><br><span class="line">  -&gt; 测试环境真实操作</span><br><span class="line">  -&gt; 监控与回滚演练</span><br></pre></td></tr></table></figure><p>简单改动可以停在较低层，高风险改动必须继续向上。关键是清楚说出停在哪里，而不是把“代码看起来没问题”写成“验证通过”。</p><p>Java 项目中，一组常见命令可能是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 先跑目标测试，快速得到反馈</span></span><br><span class="line">mvn -Dtest=CustomerQueryServiceTest <span class="built_in">test</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 再运行模块测试并打包，确认依赖与资源完整</span></span><br><span class="line">mvn clean package</span><br></pre></td></tr></table></figure><p>若仓库提供 Maven Wrapper，应优先使用仓库固定的 <code>./mvnw</code>。命令只是入口，真正的验收还要匹配业务，例如比较筛选前后数据、重复请求、权限用户和空记录。</p><h3 id="不要让模型代替测试宣布成功"><a href="#不要让模型代替测试宣布成功" class="headerlink" title="不要让模型代替测试宣布成功"></a>不要让模型代替测试宣布成功</h3><p>我要求执行结果分三类写：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">已验证：命令真实执行，附关键结果。</span><br><span class="line">未验证：当前环境无法执行，并说明原因。</span><br><span class="line">待观察：只能上线后从监控或真实流量确认。</span><br></pre></td></tr></table></figure><p>这三类比一句“应该没问题”诚实，也方便下一位接手的人继续。</p><h2 id="检查点六：提交前进行独立-Review"><a href="#检查点六：提交前进行独立-Review" class="headerlink" title="检查点六：提交前进行独立 Review"></a>检查点六：提交前进行独立 Review</h2><p>实现对话会让人产生路径依赖：模型已经解释了为什么这样改，我也一路看着它完成，很容易默认结论正确。</p><p>提交前，我会暂时不看对话，只从需求卡和最终差异重新审一遍：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">需求中的每条验收是否有对应代码和测试</span><br><span class="line">没有写进需求的行为是否被意外改变</span><br><span class="line">错误路径是否比正常路径更危险</span><br><span class="line">数据库和外部调用是否处在合理事务边界</span><br><span class="line">测试是否会在实现被删掉时失败</span><br><span class="line">回滚是否只需回退代码，还是涉及数据处理</span><br></pre></td></tr></table></figure><p>这相当于给自己换一个 Reviewer 视角。复杂任务还应由另一位工程师审查，不能因为代码由 AI 生成就降低门槛。</p><h2 id="检查点七：把有价值的上下文留在仓库"><a href="#检查点七：把有价值的上下文留在仓库" class="headerlink" title="检查点七：把有价值的上下文留在仓库"></a>检查点七：把有价值的上下文留在仓库</h2><p>一次任务中反复告诉模型的内容，如果下次仍然适用，应该沉淀成项目资产：</p><ul><li>构建和测试方式写进 README；</li><li>编码约束写进项目级说明；</li><li>关键架构取舍写成 ADR；</li><li>容易回归的规则写成测试；</li><li>发布步骤和回滚条件写进 Runbook。</li></ul><p>不要把对话记录当唯一文档。对话适合探索，仓库才是团队共同维护的长期上下文。</p><p>我还会在提交说明中保留三个信息：为什么改、行为怎样变、用什么验证。过几个月再看历史，不需要重新翻完整对话。</p><h2 id="三种常见失控方式"><a href="#三种常见失控方式" class="headerlink" title="三种常见失控方式"></a>三种常见失控方式</h2><h3 id="一直追加提示，不愿回到事实"><a href="#一直追加提示，不愿回到事实" class="headerlink" title="一直追加提示，不愿回到事实"></a>一直追加提示，不愿回到事实</h3><p>模型改错方向后，连续说“再修一下”会堆积补丁。先停止修改，重新确认入口和约束，通常更快。</p><h3 id="一次交给模型整个功能"><a href="#一次交给模型整个功能" class="headerlink" title="一次交给模型整个功能"></a>一次交给模型整个功能</h3><p>任务越大，错误越晚暴露，最终差异越难 Review。把垂直功能拆成可运行的小片段，让每一步都有反馈。</p><h3 id="只检查代码，不检查环境"><a href="#只检查代码，不检查环境" class="headerlink" title="只检查代码，不检查环境"></a>只检查代码，不检查环境</h3><p>代码正确不等于配置、数据库、权限和部署正确。尤其是版本、环境变量和外部接口，必须用真实构建与运行证据验证。</p><h2 id="一页版流程"><a href="#一页版流程" class="headerlink" title="一页版流程"></a>一页版流程</h2><p>需要快速回忆时，我只看这七行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">1. 说明问题、目标、范围和验收。</span><br><span class="line">2. 只读项目，用文件与日志建立事实。</span><br><span class="line">3. 先审计划，删除不必要的改动。</span><br><span class="line">4. 一次修改一个职责，小步看差异。</span><br><span class="line">5. 从编译到真实场景逐层验证。</span><br><span class="line">6. 脱离对话，按需求独立 Review。</span><br><span class="line">7. 把稳定知识沉淀进文档和测试。</span><br></pre></td></tr></table></figure><p>这套流程没有神奇提示词，甚至会让开始写代码的时间晚几分钟。但它减少了最昂贵的部分：在错误上下文上高速前进。</p><p>模型不需要一开始就“完全读懂项目”。它需要在每个决策点看到足够的真实信息，并及时收到构建、测试和人工判断的反馈。把这条循环设计好，AI 才会从偶尔惊艳的演示，变成可以稳定复用的开发工具。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/posts/2025/ai-workflow-cover.png&quot; alt=&quot;AI 编程工作流封面图&quot;&gt;&lt;/p&gt;
&lt;p&gt;AI 编程最浪费时间的场景，不是模型写不出代码，而是它在错误的项目理解上连续工作了半小时：改错入口、重复已有工具类、使用不兼容版本，最后生成一大份很难审查的差异。&lt;/p&gt;
&lt;p&gt;我逐渐把使用方式从“想到什么问什么”改成了一条固定工作流。它不绑定某个模型或编辑器，核心只有一句话：&lt;strong&gt;先让模型用证据还原项目，再让它在明确边界内修改。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;这篇按 Runbook 来写，可以直接拿一个小需求跟着走一遍。&lt;/p&gt;</summary>
    
    
    
    <category term="效率工具" scheme="https://swagch.github.io/categories/%E6%95%88%E7%8E%87%E5%B7%A5%E5%85%B7/"/>
    
    
    <category term="Git" scheme="https://swagch.github.io/tags/Git/"/>
    
    <category term="测试" scheme="https://swagch.github.io/tags/%E6%B5%8B%E8%AF%95/"/>
    
    <category term="Context Engineering" scheme="https://swagch.github.io/tags/Context-Engineering/"/>
    
    <category term="AI编程" scheme="https://swagch.github.io/tags/AI%E7%BC%96%E7%A8%8B/"/>
    
    <category term="工作流" scheme="https://swagch.github.io/tags/%E5%B7%A5%E4%BD%9C%E6%B5%81/"/>
    
  </entry>
  
  <entry>
    <title>AI辅助Java重构实战：拆解一个过长Service</title>
    <link href="https://swagch.github.io/2025/07/20/2025-07-20-AI%E8%BE%85%E5%8A%A9Java%E9%87%8D%E6%9E%84%E5%AE%9E%E6%88%98/"/>
    <id>https://swagch.github.io/2025/07/20/2025-07-20-AI%E8%BE%85%E5%8A%A9Java%E9%87%8D%E6%9E%84%E5%AE%9E%E6%88%98/</id>
    <published>2025-07-20T06:30:00.000Z</published>
    <updated>2026-07-16T16:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/posts/2025/java-refactor-cover.png" alt="AI 辅助 Java 重构封面图"></p><p>这次想记录一个更接近存量项目的 AI 使用场景：拆解一个 186 行的 <code>createOrder</code> 方法。</p><p>它同时做参数校验、商品查询、价格计算、库存扣减、订单落库、优惠券核销和通知发送。方法虽然长，却已经稳定运行两年。面对这种代码，最危险的需求不是“帮我重构”，而是“帮我一次重写得更优雅”。</p><p>重构的目标应该是改善内部结构而不改变外部行为。AI 可以加快识别重复和生成小函数的速度，但前提是我们先固定行为，再让每一步差异足够小。</p><span id="more"></span><h2 id="旧方法为什么不敢动"><a href="#旧方法为什么不敢动" class="headerlink" title="旧方法为什么不敢动"></a>旧方法为什么不敢动</h2><p>下面是原方法的压缩版。真实代码还有活动价、渠道价和赠品逻辑，这里只保留职责边界：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Transactional</span></span><br><span class="line"><span class="keyword">public</span> OrderResult <span class="title function_">createOrder</span><span class="params">(Long userId, Long productId,</span></span><br><span class="line"><span class="params">                               Integer quantity, String couponCode,</span></span><br><span class="line"><span class="params">                               String addressId, String requestId)</span> &#123;</span><br><span class="line">    <span class="comment">// 校验参数和幂等记录</span></span><br><span class="line">    validateRequest(userId, productId, quantity, requestId);</span><br><span class="line">    <span class="type">Product</span> <span class="variable">product</span> <span class="operator">=</span> productMapper.selectById(productId);</span><br><span class="line">    <span class="keyword">if</span> (product == <span class="literal">null</span> || !product.isOnSale()) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BusinessException</span>(<span class="string">&quot;PRODUCT_UNAVAILABLE&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 计算价格后依次修改多类业务数据</span></span><br><span class="line">    <span class="type">BigDecimal</span> <span class="variable">amount</span> <span class="operator">=</span> calculateAmount(product, quantity, couponCode);</span><br><span class="line">    stockService.deduct(productId, quantity);</span><br><span class="line">    <span class="type">Order</span> <span class="variable">order</span> <span class="operator">=</span> saveOrder(userId, product, quantity, amount, addressId);</span><br><span class="line">    couponService.consume(userId, couponCode, order.getId());</span><br><span class="line">    requestRepository.markSuccess(requestId, order.getId());</span><br><span class="line">    notificationClient.sendOrderCreated(order);</span><br><span class="line">    <span class="keyword">return</span> OrderResult.from(order);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>仅看这段就能发现几个问题：</p><ul><li>六个基础类型参数容易传错位置，调用含义不直观；</li><li>业务校验、计算和副作用混在同一抽象层；</li><li>外部通知在数据库事务中，超时会长期占用连接；</li><li>优惠券为空、库存扣减失败、重复请求等分支藏在内部方法；</li><li>测试只能从入口准备大量 Mock，失败后很难看出是哪条规则变化。</li></ul><p>不过“看见坏味道”和“知道怎样安全修改”是两件事。直接让 AI 按 SOLID 原则重写，它曾给出十一个新类、一套领域事件和一个当前项目根本没有的消息队列。结构更漂亮，风险也扩大了十倍。</p><p>我最后给这次改动设了三个约束：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">不改变接口、数据库和错误码</span><br><span class="line">不新增第三方依赖</span><br><span class="line">每一步都能单独编译、测试和回退</span><br></pre></td></tr></table></figure><h2 id="第零步：先建立行为护栏"><a href="#第零步：先建立行为护栏" class="headerlink" title="第零步：先建立行为护栏"></a>第零步：先建立行为护栏</h2><p>原方法只有两个正常流程测试。开始重构前，我让 AI 根据所有条件分支生成“行为清单”，再逐项与产品规则、线上日志和现有代码核对。</p><p>最后补了这些特征测试：</p><table><thead><tr><th>场景</th><th>要固定的行为</th></tr></thead><tbody><tr><td>重复 <code>requestId</code></td><td>返回第一次创建的订单，不重复扣库存</td></tr><tr><td>商品下架</td><td>返回 <code>PRODUCT_UNAVAILABLE</code></td></tr><tr><td>数量超过限购</td><td>返回 <code>PURCHASE_LIMIT_EXCEEDED</code></td></tr><tr><td>优惠券为空</td><td>按原价创建，不调用核销</td></tr><tr><td>优惠券失效</td><td>整笔订单不落库、不扣库存</td></tr><tr><td>库存不足</td><td>返回原错误码，不保存订单</td></tr><tr><td>通知超时</td><td>订单仍创建成功，产生可重试通知任务</td></tr></tbody></table><p>最后一条不是原代码行为，而是本次明确要修复的缺陷，因此单独作为需求变更，不伪装成纯重构。</p><p>测试先在旧代码上运行。除了“通知超时”之外全部通过，说明我们至少拥有一张当前行为快照。没有这一步，重构后测试全绿也可能只是因为测试跟着实现一起改错了。</p><h2 id="第一步：用参数对象给入口命名"><a href="#第一步：用参数对象给入口命名" class="headerlink" title="第一步：用参数对象给入口命名"></a>第一步：用参数对象给入口命名</h2><p>六个参数经常成组出现，先提取为命令对象。这一步不动业务逻辑：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">CreateOrderCommand</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Long userId;</span><br><span class="line">    <span class="keyword">private</span> Long productId;</span><br><span class="line">    <span class="keyword">private</span> Integer quantity;</span><br><span class="line">    <span class="keyword">private</span> String couponCode;</span><br><span class="line">    <span class="keyword">private</span> String addressId;</span><br><span class="line">    <span class="keyword">private</span> String requestId;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 省略标准 getter/setter，项目仍使用 JavaBean 方式绑定参数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Controller 负责将原请求转换成 <code>CreateOrderCommand</code>，Service 入口变成：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> OrderResult <span class="title function_">createOrder</span><span class="params">(CreateOrderCommand command)</span> &#123;</span><br><span class="line">    <span class="comment">// 暂时只转发字段，确保第一步不改变执行顺序</span></span><br><span class="line">    <span class="keyword">return</span> doCreateOrder(command);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>AI 最初使用了 Java <code>record</code>。它非常适合不可变数据载体，却不兼容这个 JDK 8 项目。模型知道现代 Java 的常见写法，并不代表它会始终记住当前仓库版本；编译是最直接的纠偏工具。</p><p>参数对象也不是越多越好。这里只有一组数据跨 Controller、校验器和应用服务反复传递，提取后能减少长参数列表。如果只是某个私有方法的两个值，没有必要为“面向对象”新建类型。</p><h2 id="第二步：把规则校验从流程中移开"><a href="#第二步：把规则校验从流程中移开" class="headerlink" title="第二步：把规则校验从流程中移开"></a>第二步：把规则校验从流程中移开</h2><p>旧方法中的校验散落在查询前后。我将不产生副作用的规则提取到 <code>OrderCreationValidator</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderCreationValidator</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">validate</span><span class="params">(CreateOrderCommand command, Product product)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (command == <span class="literal">null</span> || command.getUserId() == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BusinessException</span>(<span class="string">&quot;INVALID_ORDER_REQUEST&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (product == <span class="literal">null</span> || !product.isOnSale()) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BusinessException</span>(<span class="string">&quot;PRODUCT_UNAVAILABLE&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (command.getQuantity() == <span class="literal">null</span> || command.getQuantity() &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BusinessException</span>(<span class="string">&quot;INVALID_QUANTITY&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (command.getQuantity() &gt; product.getPurchaseLimit()) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BusinessException</span>(<span class="string">&quot;PURCHASE_LIMIT_EXCEEDED&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里选择提前抛出，避免多层 <code>if</code> 嵌套。校验器只依赖输入和已查询商品，没有数据库写入，因此可以直接覆盖边界测试。</p><p>不是所有校验都被搬走。优惠券有效性依赖实时状态，并且核销需要并发控制，它仍由优惠券服务负责。把“名字像校验”的代码全部塞进一个类，只会形成新的大类。</p><p>AI 在这一步帮我找出了三段重复的数量判断，也指出两个错误码文本不同。我没有让它自动统一错误码，因为对外错误码是接口契约；看似重复的字符串可能承载兼容行为。</p><h2 id="第三步：让价格计算成为纯计算"><a href="#第三步：让价格计算成为纯计算" class="headerlink" title="第三步：让价格计算成为纯计算"></a>第三步：让价格计算成为纯计算</h2><p>价格逻辑最适合先解耦，因为相同输入应该得到相同输出，不需要数据库或网络。</p><p>原逻辑直接返回一个金额，调用方无法知道优惠来自哪里。重构后用 <code>OrderPrice</code> 表达明细：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">OrderPrice</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> BigDecimal originalAmount;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> BigDecimal discountAmount;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> BigDecimal payableAmount;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">OrderPrice</span><span class="params">(BigDecimal originalAmount,</span></span><br><span class="line"><span class="params">                      BigDecimal discountAmount)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.originalAmount = originalAmount;</span><br><span class="line">        <span class="built_in">this</span>.discountAmount = discountAmount;</span><br><span class="line">        <span class="comment">// 金额在同一个对象内保证不小于零，避免规则散落到调用方</span></span><br><span class="line">        <span class="built_in">this</span>.payableAmount = originalAmount.subtract(discountAmount)</span><br><span class="line">                .max(BigDecimal.ZERO);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 省略只读 getter</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>计算服务只负责规则：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> OrderPrice <span class="title function_">calculate</span><span class="params">(Product product, <span class="type">int</span> quantity,</span></span><br><span class="line"><span class="params">                            CouponSnapshot coupon)</span> &#123;</span><br><span class="line">    <span class="type">BigDecimal</span> <span class="variable">original</span> <span class="operator">=</span> product.getSalePrice()</span><br><span class="line">            .multiply(BigDecimal.valueOf(quantity));</span><br><span class="line">    <span class="type">BigDecimal</span> <span class="variable">discount</span> <span class="operator">=</span> coupon == <span class="literal">null</span></span><br><span class="line">            ? BigDecimal.ZERO</span><br><span class="line">            : coupon.discountFor(original);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">OrderPrice</span>(original, discount);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>纯计算测试不再需要启动 Spring：输入单价、数量和优惠券快照，断言原价、优惠和应付金额即可。用 <code>BigDecimal.compareTo</code> 比较金额，避免 <code>equals</code> 因小数位不同产生误判。</p><p>这一步还暴露了旧代码的一个隐含行为：优惠后金额可以是负数，保存前才在另一个方法里修正为零。重构没有顺手“优化”规则，而是先用测试固定，再把修正逻辑移动到 <code>OrderPrice</code> 构造器。移动与修改必须分开，否则出了问题无法判断是哪一种变化导致。</p><h2 id="第四步：重新画事务边界"><a href="#第四步：重新画事务边界" class="headerlink" title="第四步：重新画事务边界"></a>第四步：重新画事务边界</h2><p>旧方法上的 <code>@Transactional</code> 包住了所有操作，包括通知 HTTP 请求。网络慢两秒，数据库连接就多占用两秒；通知失败还会回滚订单与库存，而对方可能已经收到请求，形成更难处理的不一致。</p><p>重构后把数据库内的原子操作放进独立组件：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Transactional</span></span><br><span class="line"><span class="keyword">public</span> Order <span class="title function_">persist</span><span class="params">(CreateOrderContext context)</span> &#123;</span><br><span class="line">    stockService.deduct(context.getProductId(), context.getQuantity());</span><br><span class="line">    <span class="type">Order</span> <span class="variable">order</span> <span class="operator">=</span> orderRepository.save(context.toOrder());</span><br><span class="line">    couponService.consume(context.getCoupon(), order.getId());</span><br><span class="line">    requestRepository.markSuccess(context.getRequestId(), order.getId());</span><br><span class="line">    <span class="comment">// 通知任务与订单在同一事务落库，提交后可由任务可靠重试</span></span><br><span class="line">    notificationTaskRepository.saveOrderCreatedTask(order.getId());</span><br><span class="line">    <span class="keyword">return</span> order;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>应用服务只编排步骤：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> OrderResult <span class="title function_">createOrder</span><span class="params">(CreateOrderCommand command)</span> &#123;</span><br><span class="line">    <span class="type">OrderResult</span> <span class="variable">duplicated</span> <span class="operator">=</span> duplicateRequestResult(command.getRequestId());</span><br><span class="line">    <span class="keyword">if</span> (duplicated != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> duplicated;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">Product</span> <span class="variable">product</span> <span class="operator">=</span> productRepository.findById(command.getProductId());</span><br><span class="line">    validator.validate(command, product);</span><br><span class="line">    <span class="type">CouponSnapshot</span> <span class="variable">coupon</span> <span class="operator">=</span> couponService.prepare(command);</span><br><span class="line">    <span class="type">OrderPrice</span> <span class="variable">price</span> <span class="operator">=</span> pricingService.calculate(</span><br><span class="line">            product, command.getQuantity(), coupon);</span><br><span class="line"></span><br><span class="line">    <span class="type">CreateOrderContext</span> <span class="variable">context</span> <span class="operator">=</span> contextFactory.create(</span><br><span class="line">            command, product, coupon, price);</span><br><span class="line">    <span class="keyword">return</span> OrderResult.from(transactionService.persist(context));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通知任务由现有定时任务发送，成功后更新状态，失败按退避策略重试。这里没有为了“解耦”突然引入 Kafka，因为项目已经有可靠任务表。架构选择应服从现有基础设施和故障处理能力。</p><p>还要注意 Spring 事务代理：如果把 <code>persist</code> 写成同一个类的 <code>private</code> 方法，或通过 <code>this.persist()</code> 自调用，<code>@Transactional</code> 不会按预期经过代理。将事务方法放进独立 Bean，既明确边界，也避开自调用失效。</p><h2 id="第五步：删除重复之前，再跑一次全量验证"><a href="#第五步：删除重复之前，再跑一次全量验证" class="headerlink" title="第五步：删除重复之前，再跑一次全量验证"></a>第五步：删除重复之前，再跑一次全量验证</h2><p>新结构稳定后，原来的私有方法有一部分不再被调用。AI 很适合搜索引用、列出候选删除项，但删除前我做了三层检查：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">IDE / 仓库全文引用搜索</span><br><span class="line">编译主代码与测试代码</span><br><span class="line">运行订单模块回归测试</span><br></pre></td></tr></table></figure><p>有一个 <code>calculateAmount</code> 看似无引用，实际被 SpEL 规则通过方法名调用，普通静态搜索没有找到。幸好集成测试启动规则时失败，避免了误删上线。</p><p>动态调用、反射、XML Mapper、序列化字段和框架约定都是 AI 重构的薄弱区。模型读取了 Java 引用，不代表它已经看到运行时所有关系。</p><h2 id="AI-在这次重构中犯过的错"><a href="#AI-在这次重构中犯过的错" class="headerlink" title="AI 在这次重构中犯过的错"></a>AI 在这次重构中犯过的错</h2><p>除了使用 <code>record</code>，还有几类建议被人工拒绝：</p><ol><li>用 <code>Stream.toList()</code> 收集结果，JDK 8 不支持；</li><li>把所有返回值改成 <code>Optional</code> 字段，增加序列化与调用复杂度；</li><li>统一替换历史错误码，破坏客户端兼容；</li><li>引入领域事件和新消息中间件，超出问题范围；</li><li>在没有并发测试的情况下，把重复请求判断改成先查后写；</li><li>为了减少类数量，把校验、价格和上下文工厂合并成新的 Helper 大类。</li></ol><p>这些建议并不荒唐，它们在另一个项目里可能成立。问题在于模型倾向输出“普遍合理”的设计，工程师要判断它是否适合当前版本、团队和风险预算。</p><h2 id="怎样证明重构没有把事情弄坏"><a href="#怎样证明重构没有把事情弄坏" class="headerlink" title="怎样证明重构没有把事情弄坏"></a>怎样证明重构没有把事情弄坏</h2><p>最终验证不只是一句“测试通过”：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">原有 42 个订单模块测试通过</span><br><span class="line">新增 18 个特征与边界测试通过</span><br><span class="line">接口契约快照无变化</span><br><span class="line">数据库迁移文件无变化</span><br><span class="line">正常订单与重复请求各完成一次联调</span><br><span class="line">通知超时演练中，订单提交成功且重试任务可见</span><br><span class="line">并发请求仍由 request_id 唯一约束兜底</span><br></pre></td></tr></table></figure><p>代码指标也有变化：入口方法从 186 行降到 24 行，六个参数变成一个命令对象，价格规则可以脱离 Spring 单测。类数量增加了四个，但每个类对应稳定职责，不是为了追求数字拆分。</p><p>我没有把“方法越短越好”当目标。真正的改善是：阅读入口时能看清业务顺序，修改价格不需要理解通知，调整通知也不会碰库存事务。</p><h2 id="这类任务最适合怎样使用-AI"><a href="#这类任务最适合怎样使用-AI" class="headerlink" title="这类任务最适合怎样使用 AI"></a>这类任务最适合怎样使用 AI</h2><p>我的顺序是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">先让 AI 画现状，不写代码</span><br><span class="line">再让 AI 枚举行为和风险</span><br><span class="line">人确定边界与优先级</span><br><span class="line">一次只做一种重构手法</span><br><span class="line">每步查看差异并运行测试</span><br><span class="line">最后让 AI 做遗漏检查，人做合并判断</span><br></pre></td></tr></table></figure><p>AI 在“快速读取、发现重复、生成测试骨架、执行机械移动”上很强；人在“哪些行为是契约、哪种抽象值得存在、风险是否可以接受”上不能缺席。</p><p>重构不是把旧代码替换成一份更现代的代码，而是在持续可工作的前提下，让下一次变化更容易。让 AI 参与后，这个过程可以更快，但“小步、测试、审查”反而比以前更重要，因为我们现在拥有了以极快速度制造大面积改动的能力。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/posts/2025/java-refactor-cover.png&quot; alt=&quot;AI 辅助 Java 重构封面图&quot;&gt;&lt;/p&gt;
&lt;p&gt;这次想记录一个更接近存量项目的 AI 使用场景：拆解一个 186 行的 &lt;code&gt;createOrder&lt;/code&gt; 方法。&lt;/p&gt;
&lt;p&gt;它同时做参数校验、商品查询、价格计算、库存扣减、订单落库、优惠券核销和通知发送。方法虽然长，却已经稳定运行两年。面对这种代码，最危险的需求不是“帮我重构”，而是“帮我一次重写得更优雅”。&lt;/p&gt;
&lt;p&gt;重构的目标应该是改善内部结构而不改变外部行为。AI 可以加快识别重复和生成小函数的速度，但前提是我们先固定行为，再让每一步差异足够小。&lt;/p&gt;</summary>
    
    
    
    <category term="Java进阶" scheme="https://swagch.github.io/categories/Java%E8%BF%9B%E9%98%B6/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="重构" scheme="https://swagch.github.io/tags/%E9%87%8D%E6%9E%84/"/>
    
    <category term="Spring Boot" scheme="https://swagch.github.io/tags/Spring-Boot/"/>
    
    <category term="AI编程" scheme="https://swagch.github.io/tags/AI%E7%BC%96%E7%A8%8B/"/>
    
    <category term="单元测试" scheme="https://swagch.github.io/tags/%E5%8D%95%E5%85%83%E6%B5%8B%E8%AF%95/"/>
    
  </entry>
  
  <entry>
    <title>Vibe Coding不是完全不看代码</title>
    <link href="https://swagch.github.io/2025/05/18/2025-05-18-Vibe-Coding%E4%B8%8D%E6%98%AF%E5%AE%8C%E5%85%A8%E4%B8%8D%E7%9C%8B%E4%BB%A3%E7%A0%81/"/>
    <id>https://swagch.github.io/2025/05/18/2025-05-18-Vibe-Coding%E4%B8%8D%E6%98%AF%E5%AE%8C%E5%85%A8%E4%B8%8D%E7%9C%8B%E4%BB%A3%E7%A0%81/</id>
    <published>2025-05-18T11:40:00.000Z</published>
    <updated>2026-07-16T16:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/posts/2025/vibe-coding-boundary-cover.png" alt="Vibe Coding 边界封面图"></p><p>Vibe Coding 最吸引人的画面，是只描述想法，AI 持续修改代码，人看着页面变化说“再来一点”，直到产品能用。这个过程真实存在，而且非常适合探索。但当它被简化成“以后不需要看代码”，问题就从一种高效交互方式变成了责任错位。</p><p>我的理解是：<strong>Vibe Coding 可以降低表达想法的门槛，不能取消软件运行后的后果。</strong></p><span id="more"></span><h2 id="两种场景，不应该使用同一套纪律"><a href="#两种场景，不应该使用同一套纪律" class="headerlink" title="两种场景，不应该使用同一套纪律"></a>两种场景，不应该使用同一套纪律</h2><p>周末做一个只在本机运行的小工具，最重要的是快速看到结果。数据丢了可以重来，页面崩了重新生成，没有用户依赖它。此时跟着感觉迭代很合理：先有可玩的东西，再理解实现。</p><p>但下面这些场景完全不同：</p><ul><li>修改支付、权限或数据删除逻辑；</li><li>接触生产密钥和客户数据；</li><li>在多人维护的仓库里引入新依赖；</li><li>改变数据库结构或公共接口；</li><li>让生成的服务长期对外运行。</li></ul><p>它们共同拥有一个特点：失败成本会落到别人身上。只要影响从“我自己的时间”扩展到数据、资金、隐私和团队维护成本，开发方式就必须升级。</p><p>所以我会把 AI 编程分为两个模式：</p><table><thead><tr><th>模式</th><th>目标</th><th>可以接受</th><th>不能省略</th></tr></thead><tbody><tr><td>探索模式</td><td>验证想法是否值得继续</td><td>临时代码、快速改写、少量手测</td><td>隔离环境、无真实数据、明确可丢弃</td></tr><tr><td>交付模式</td><td>让功能稳定进入真实系统</td><td>AI 生成大部分实现</td><td>Review、测试、安全检查、发布与回滚</td></tr></tbody></table><p>前者可以更有“氛围”，后者必须留下证据。</p><h2 id="不看每一行，不等于不理解系统"><a href="#不看每一行，不等于不理解系统" class="headerlink" title="不看每一行，不等于不理解系统"></a>不看每一行，不等于不理解系统</h2><p>传统开发也很少要求工程师阅读依赖库的每一行源码。我们依赖抽象、接口、测试和社区维护来控制复杂度。AI 生成代码后，人同样不一定要逐字符检查，但至少要能回答：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">改了哪些模块</span><br><span class="line">数据从哪里来，到哪里去</span><br><span class="line">失败后系统处于什么状态</span><br><span class="line">权限在哪一层校验</span><br><span class="line">哪些测试证明了关键行为</span><br><span class="line">出现问题怎样停止和回退</span><br></pre></td></tr></table></figure><p>如果这些问题都答不出来，所谓“不看代码”其实是“不知道自己交付了什么”。</p><p>尤其需要警惕一种假象：页面正常打开，主流程点得通，就认为功能完成。真实故障通常藏在重复提交、并发请求、空数据、超时重试、时区、权限绕过和升级兼容里。演示成功只能证明一条路径，不等于系统可靠。</p><h2 id="我给-Vibe-Coding-设的五条红线"><a href="#我给-Vibe-Coding-设的五条红线" class="headerlink" title="我给 Vibe Coding 设的五条红线"></a>我给 Vibe Coding 设的五条红线</h2><h3 id="1-密钥不进入对话"><a href="#1-密钥不进入对话" class="headerlink" title="1. 密钥不进入对话"></a>1. 密钥不进入对话</h3><p>配置文件、日志和截图里的 Token、密码、Cookie、客户信息先脱敏。模型能不能保密不是唯一问题，团队的数据合规要求才是边界。</p><h3 id="2-数据破坏操作必须人工确认"><a href="#2-数据破坏操作必须人工确认" class="headerlink" title="2. 数据破坏操作必须人工确认"></a>2. 数据破坏操作必须人工确认</h3><p>删除、覆盖、迁移、批量更新和生产环境操作，不允许因为模型“很确定”就自动执行。先备份、估算影响、准备回滚，再由有权限的人确认。</p><h3 id="3-权限代码必须读懂"><a href="#3-权限代码必须读懂" class="headerlink" title="3. 权限代码必须读懂"></a>3. 权限代码必须读懂</h3><p>登录、鉴权、租户隔离和资源归属校验不能只看测试绿灯。测试本身也可能只覆盖正常用户。至少要检查越权路径和默认拒绝策略。</p><h3 id="4-新依赖要说明代价"><a href="#4-新依赖要说明代价" class="headerlink" title="4. 新依赖要说明代价"></a>4. 新依赖要说明代价</h3><p>AI 很喜欢用一个新包解决十行代码的问题。依赖会带来许可证、漏洞、体积、升级和维护成本。能复用现有能力时，不为了生成方便扩大供应链。</p><h3 id="5-不接受“应该可以”作为验收"><a href="#5-不接受“应该可以”作为验收" class="headerlink" title="5. 不接受“应该可以”作为验收"></a>5. 不接受“应该可以”作为验收</h3><p>编译过、测试过、接口真实请求过，分别是不同证据。没有执行就写“未验证”，不能把推测包装成完成状态。</p><h2 id="真正的新能力是驾驭反馈循环"><a href="#真正的新能力是驾驭反馈循环" class="headerlink" title="真正的新能力是驾驭反馈循环"></a>真正的新能力是驾驭反馈循环</h2><p>Vibe Coding 并不是完全放弃工程，而是把编程界面从“逐行输入代码”扩展成“描述目标、观察结果、继续校正”。其中最重要的能力，也从记住 API 变成了设计反馈循环：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">想法</span><br><span class="line">  -&gt; 最小实现</span><br><span class="line">  -&gt; 可观察结果</span><br><span class="line">  -&gt; 发现偏差</span><br><span class="line">  -&gt; 缩小问题</span><br><span class="line">  -&gt; 再次修改</span><br></pre></td></tr></table></figure><p>一个反馈循环是否可靠，取决于“可观察结果”是什么。若反馈只有视觉效果，模型会优先把页面调得像；若反馈包含类型检查、单元测试、性能数据、安全扫描和真实用户行为，生成方向才会逐渐靠近工程目标。</p><p>这也是为什么优秀的 AI 编程并不轻松。人不再敲每段样板代码，却要更快识别错误假设、更清楚地表达约束，并构造能让错误尽早暴露的测试。</p><h2 id="初学者可以从小而可逆的任务开始"><a href="#初学者可以从小而可逆的任务开始" class="headerlink" title="初学者可以从小而可逆的任务开始"></a>初学者可以从小而可逆的任务开始</h2><p>我不赞成因为有风险就拒绝 Vibe Coding。相反，它非常适合做这些事：</p><ul><li>生成一次性数据转换脚本，并对比转换前后数量；</li><li>为已有函数补边界测试；</li><li>制作内部原型或个人效率工具；</li><li>解释陌生模块并画出调用关系；</li><li>把重复代码整理成候选重构方案；</li><li>根据明确规范调整样式和文案。</li></ul><p>共同点是范围清楚、结果可验证、失败可恢复。先在这样的任务上练习，能逐渐建立对模型能力和失误模式的真实认识。</p><h2 id="最后还是那句老话：谁提交，谁负责"><a href="#最后还是那句老话：谁提交，谁负责" class="headerlink" title="最后还是那句老话：谁提交，谁负责"></a>最后还是那句老话：谁提交，谁负责</h2><p>AI 不会参加故障复盘，也不会在凌晨判断是否回滚。生成速度可以属于模型，合并代码的决定仍然属于提交者。</p><p>我愿意拥抱 Vibe Coding，因为它让很多想法能更快变成可运行的东西；我也不愿把它解释成“代码已经不重要”，因为软件最终仍然由代码、配置、数据和运行环境组成。</p><p>最舒服的状态不是完全不看，而是把注意力从机械输入转向真正重要的地方：问题是否值得解决，边界是否清楚，证据是否足够，系统失败时能否安全回来。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/posts/2025/vibe-coding-boundary-cover.png&quot; alt=&quot;Vibe Coding 边界封面图&quot;&gt;&lt;/p&gt;
&lt;p&gt;Vibe Coding 最吸引人的画面，是只描述想法，AI 持续修改代码，人看着页面变化说“再来一点”，直到产品能用。这个过程真实存在，而且非常适合探索。但当它被简化成“以后不需要看代码”，问题就从一种高效交互方式变成了责任错位。&lt;/p&gt;
&lt;p&gt;我的理解是：&lt;strong&gt;Vibe Coding 可以降低表达想法的门槛，不能取消软件运行后的后果。&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    <category term="程序员随笔" scheme="https://swagch.github.io/categories/%E7%A8%8B%E5%BA%8F%E5%91%98%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="工程实践" scheme="https://swagch.github.io/tags/%E5%B7%A5%E7%A8%8B%E5%AE%9E%E8%B7%B5/"/>
    
    <category term="AI编程" scheme="https://swagch.github.io/tags/AI%E7%BC%96%E7%A8%8B/"/>
    
    <category term="Vibe Coding" scheme="https://swagch.github.io/tags/Vibe-Coding/"/>
    
    <category term="代码审查" scheme="https://swagch.github.io/tags/%E4%BB%A3%E7%A0%81%E5%AE%A1%E6%9F%A5/"/>
    
  </entry>
  
  <entry>
    <title>第一次用AI完成一个真实需求：从需求澄清到上线</title>
    <link href="https://swagch.github.io/2025/04/06/2025-04-06-%E7%AC%AC%E4%B8%80%E6%AC%A1%E7%94%A8AI%E5%AE%8C%E6%88%90%E4%B8%80%E4%B8%AA%E7%9C%9F%E5%AE%9E%E9%9C%80%E6%B1%82/"/>
    <id>https://swagch.github.io/2025/04/06/2025-04-06-%E7%AC%AC%E4%B8%80%E6%AC%A1%E7%94%A8AI%E5%AE%8C%E6%88%90%E4%B8%80%E4%B8%AA%E7%9C%9F%E5%AE%9E%E9%9C%80%E6%B1%82/</id>
    <published>2025-04-06T13:15:00.000Z</published>
    <updated>2026-07-16T16:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/posts/2025/ai-feature-log-cover.png" alt="AI 需求开发日志封面图"></p><p>这是我第一次有意识地让 AI 参与一个完整需求，而不是只让它解释报错或补一个工具方法。</p><p>需求不大：在工单列表增加“最近跟进时间”筛选，并让导出接口使用相同条件。真正做下来，AI 最有价值的部分不是写代码，而是帮助我快速梳理分散的调用链；最危险的部分也不是语法错误，而是它会非常自然地替需求补上一个没有确认过的定义。</p><p>下面按当天的开发时间线还原整个过程。项目和数据均做了简化，重点是我与 AI 怎样分工，以及哪些地方发生了返工。</p><span id="more"></span><h2 id="09-12，拿到一句看似明确的需求"><a href="#09-12，拿到一句看似明确的需求" class="headerlink" title="09:12，拿到一句看似明确的需求"></a>09:12，拿到一句看似明确的需求</h2><p>产品描述是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">工单列表增加最近跟进时间筛选，导出也要支持。</span><br></pre></td></tr></table></figure><p>我本来准备直接搜索列表接口，让 AI 帮我加两个时间参数。提交之前多问了一句：“最近跟进时间是最后一条跟进记录的创建时间，还是工单表上的 <code>last_follow_at</code>？”</p><p>答案是前者，而且还有两个规则：</p><ul><li>没有跟进记录的工单不命中任何时间范围；</li><li>开始时间包含，结束时间按次日零点之前处理。</li></ul><p>这两个规则如果没问，AI 大概率会根据字段名使用 <code>last_follow_at</code>，本地代码仍然能编译，查询结果却会悄悄出错。</p><p>我先把需求改写成状态明确的任务卡：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">目标：列表与导出支持按最后一条跟进记录的 created_at 筛选。</span><br><span class="line">输入：followStartDate、followEndDate，格式 yyyy-MM-dd，可单独传。</span><br><span class="line">边界：开始日 00:00:00 包含；结束日转换为下一日 00:00:00，使用小于比较。</span><br><span class="line">空记录：无跟进记录的工单不命中。</span><br><span class="line">兼容：不传参数时，SQL 与响应保持原行为。</span><br><span class="line">完成：列表和导出共用条件，Mapper 集成测试覆盖五个边界用例。</span><br></pre></td></tr></table></figure><p>这是当天最值得的一步，虽然一行代码都没有产生。</p><h2 id="09-35，让-AI-先找路，不要先写"><a href="#09-35，让-AI-先找路，不要先写" class="headerlink" title="09:35，让 AI 先找路，不要先写"></a>09:35，让 AI 先找路，不要先写</h2><p>我让 AI 在仓库中回答四个问题：</p><ol><li>列表接口与导出接口分别从哪里进入；</li><li>两者最终是否复用同一个查询对象；</li><li>工单和跟进记录通过什么字段关联；</li><li>当前项目的日期范围条件通常怎样写。</li></ol><p>它搜索后给出的调用图是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">TicketController#page</span><br><span class="line">  -&gt; TicketQueryService#page</span><br><span class="line">  -&gt; TicketMapper#selectPage</span><br><span class="line"></span><br><span class="line">TicketExportController#export</span><br><span class="line">  -&gt; TicketExportService#export</span><br><span class="line">  -&gt; TicketMapper#selectForExport</span><br><span class="line"></span><br><span class="line">两个 Mapper 方法都接收 TicketQueryCondition</span><br><span class="line">跟进表通过 ticket_id 关联</span><br></pre></td></tr></table></figure><p>同时找到另一个“完成时间”筛选，项目约定在 Java 层把日期转换成左闭右开的 <code>LocalDateTime</code> 区间，再传给 Mapper。</p><p>这一步节省了我在目录中来回跳转的时间。更重要的是，它用真实代码否定了我最初的猜测：列表和导出并没有共用 SQL，只共用条件对象，因此验收范围至少涉及两个 Mapper 查询。</p><h2 id="10-05，第一次计划里出现了过度设计"><a href="#10-05，第一次计划里出现了过度设计" class="headerlink" title="10:05，第一次计划里出现了过度设计"></a>10:05，第一次计划里出现了过度设计</h2><p>AI 建议：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">新增 FollowTimeRange 值对象</span><br><span class="line">新增 TicketFollowQueryDomainService</span><br><span class="line">新增 FollowRecordRepository</span><br><span class="line">统一重构列表与导出查询</span><br></pre></td></tr></table></figure><p>从结构上看很完整，但对于一个已有项目中的两个筛选条件，改动太大。新增领域层会让本次需求同时承担架构迁移，Review 和回归范围都被放大。</p><p>我把范围压回到四个文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">TicketQueryCondition.java</span><br><span class="line">TicketQueryService.java</span><br><span class="line">TicketMapper.xml</span><br><span class="line">TicketMapperIntegrationTest.java</span><br></pre></td></tr></table></figure><p>其中 XML 内有两个查询片段；导出服务继续复用同一个条件构造方法。原则是先完成可验证的业务变化，不借小需求清理整个项目。</p><h2 id="10-32，日期边界第一次写错"><a href="#10-32，日期边界第一次写错" class="headerlink" title="10:32，日期边界第一次写错"></a>10:32，日期边界第一次写错</h2><p>AI 生成的转换逻辑是：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">condition.setFollowEndTime(endDate.atTime(<span class="number">23</span>, <span class="number">59</span>, <span class="number">59</span>));</span><br></pre></td></tr></table></figure><p>这在秒精度数据库中看起来可用，但会漏掉带毫秒或微秒的记录。项目中已有的正确约定是左闭右开：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">applyFollowTimeRange</span><span class="params">(TicketQueryCondition condition,</span></span><br><span class="line"><span class="params">                                  LocalDate startDate,</span></span><br><span class="line"><span class="params">                                  LocalDate endDate)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (startDate != <span class="literal">null</span>) &#123;</span><br><span class="line">        condition.setFollowStartTime(startDate.atStartOfDay());</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (endDate != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="comment">// 使用次日零点作为排他上界，避免丢失结束日最后一秒的小数精度记录</span></span><br><span class="line">        condition.setFollowEndExclusive(endDate.plusDays(<span class="number">1</span>).atStartOfDay());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对应 SQL：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">&quot;followStartTime != null&quot;</span>&gt;</span></span><br><span class="line">  AND latest_follow.created_at &lt;![CDATA[&gt;=]]&gt; #&#123;followStartTime&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">&quot;followEndExclusive != null&quot;</span>&gt;</span></span><br><span class="line">  AND latest_follow.created_at &lt;![CDATA[&lt;]]&gt; #&#123;followEndExclusive&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br></pre></td></tr></table></figure><p>这次错误让我看到一个规律：AI 很容易给出“常见答案”，而项目已有约定才是更可靠的局部答案。让它搜索相邻实现，比在提示词中反复强调“考虑边界”有效。</p><h2 id="11-10，SQL-能运行，但语义不对"><a href="#11-10，SQL-能运行，但语义不对" class="headerlink" title="11:10，SQL 能运行，但语义不对"></a>11:10，SQL 能运行，但语义不对</h2><p>第一版 SQL 使用普通关联：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> ticket_follow_record follow_record</span><br><span class="line">  <span class="keyword">ON</span> follow_record.ticket_id <span class="operator">=</span> ticket.id</span><br></pre></td></tr></table></figure><p>再对 <code>follow_record.created_at</code> 加时间条件。问题是这会匹配“任意一条在区间内的跟进”，而需求要求“最后一条跟进在区间内”。</p><p>例如一张工单有两次跟进：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">2025-03-01  第一次跟进</span><br><span class="line">2025-04-03  最近一次跟进</span><br></pre></td></tr></table></figure><p>筛选三月份时，普通关联会返回它；按需求，最近跟进发生在四月，不应该返回。</p><p>最终 SQL 先聚合每张工单的最新时间：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> ticket_id, <span class="built_in">MAX</span>(created_at) <span class="keyword">AS</span> latest_follow_at</span><br><span class="line">    <span class="keyword">FROM</span> ticket_follow_record</span><br><span class="line">    <span class="keyword">WHERE</span> deleted <span class="operator">=</span> <span class="number">0</span></span><br><span class="line">    <span class="keyword">GROUP</span> <span class="keyword">BY</span> ticket_id</span><br><span class="line">) latest_follow <span class="keyword">ON</span> latest_follow.ticket_id <span class="operator">=</span> ticket.id</span><br></pre></td></tr></table></figure><p>筛选条件作用在 <code>latest_follow_at</code> 上。AI 随后提醒我检查执行计划，因为全表聚合可能成为新瓶颈。测试库数据量不大时，正确性会掩盖性能风险。</p><p>查看索引后，跟进表只有单列 <code>ticket_id</code> 索引。我们补充了 <code>(ticket_id, deleted, created_at)</code> 联合索引的数据库变更，并在接近生产分布的测试数据上对比执行计划。这个索引不是由 AI 一句话决定的，而是由查询模式、数据分布和执行计划共同决定。</p><h2 id="13-40，测试暴露了两个遗漏"><a href="#13-40，测试暴露了两个遗漏" class="headerlink" title="13:40，测试暴露了两个遗漏"></a>13:40，测试暴露了两个遗漏</h2><p>我先让 AI 根据任务卡列测试矩阵，再由它补测试数据。最终有六个用例：</p><table><thead><tr><th>用例</th><th>预期</th></tr></thead><tbody><tr><td>不传时间</td><td>保持原列表数量</td></tr><tr><td>只传开始日</td><td>包含开始日零点后的记录</td></tr><tr><td>只传结束日</td><td>不包含次日零点记录</td></tr><tr><td>同时传开始和结束</td><td>命中闭日期范围</td></tr><tr><td>一张工单多条跟进</td><td>只按最新一条判断</td></tr><tr><td>无跟进记录</td><td>不命中时间筛选</td></tr></tbody></table><p>第一次执行时，两个测试失败。</p><p>第一个失败来自导出。虽然 <code>TicketQueryCondition</code> 增加了字段，但导出 Controller 使用另一个请求对象，转换时漏传了两个参数。调用链分析做过一次，并不代表实现阶段不会漏掉转换边界。</p><p>第二个失败来自删除数据。最新时间子查询忘记过滤逻辑删除记录，导致一条已撤回的跟进成为“最新记录”。需求卡没有写这个规则，是项目的数据约定补全了它。</p><p>这两个失败都不是坏事。测试在上线前把“看起来完整”变成了具体反例。</p><h2 id="15-20，我自己做了一轮只看差异的-Review"><a href="#15-20，我自己做了一轮只看差异的-Review" class="headerlink" title="15:20，我自己做了一轮只看差异的 Review"></a>15:20，我自己做了一轮只看差异的 Review</h2><p>AI 已经解释过每个改动，但我关闭对话，只阅读版本差异。检查顺序是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">有没有超出需求的文件</span><br><span class="line">请求参数是否向后兼容</span><br><span class="line">日期转换是否只有一处</span><br><span class="line">列表和导出语义是否一致</span><br><span class="line">SQL 是否处理逻辑删除</span><br><span class="line">是否引入全表扫描或重复行</span><br><span class="line">测试是不是在验证结果，而不是复述实现</span><br><span class="line">日志里有没有敏感筛选条件</span><br></pre></td></tr></table></figure><p>这一轮删除了一段 AI 主动添加的调试日志。它会打印整个查询条件，其中包含创建人手机号。日志对排查有用，但不能把请求对象直接序列化后全部输出。</p><p>我还把模型生成的 <code>buildCondition</code> 复制代码合并回已有转换方法，避免列表和导出以后再次分叉。</p><h2 id="16-30，提测不是终点"><a href="#16-30，提测不是终点" class="headerlink" title="16:30，提测不是终点"></a>16:30，提测不是终点</h2><p>提测前完成了三类验证：</p><ol><li>Mapper 集成测试证明 SQL 边界；</li><li>Service 单元测试证明日期转换和参数传递；</li><li>测试环境手工比较列表与导出结果。</li></ol><p>发布清单增加了两项：观察慢查询中该 SQL 的扫描行数，以及确认新增索引构建不会阻塞高峰写入。数据库变更选择在低峰执行，并保留取消条件。</p><p>上线后用不传参数、单边界和完整区间各验证一次，再观察接口 P95、数据库 CPU 与慢查询。没有异常，才把任务标记完成。</p><h2 id="当天的投入到底有没有变少"><a href="#当天的投入到底有没有变少" class="headerlink" title="当天的投入到底有没有变少"></a>当天的投入到底有没有变少</h2><p>如果只看编码时间，AI 确实快：定位文件、生成测试骨架和整理 SQL 候选，大约节省了一到两个小时。但总交付时间没有缩短一半，因为需求确认、性能验证、数据安全和上线观察仍然必须由人完成。</p><p>我把当天的工作粗略拆开：</p><table><thead><tr><th>工作</th><th>AI 的作用</th><th>人的责任</th></tr></thead><tbody><tr><td>调用链梳理</td><td>搜索和摘要很快</td><td>判断摘要是否真实</td></tr><tr><td>方案设计</td><td>提供多个候选</td><td>控制改动范围</td></tr><tr><td>代码生成</td><td>完成机械修改</td><td>检查项目约定与边界</td></tr><tr><td>测试</td><td>生成矩阵和骨架</td><td>确认测试能证明需求</td></tr><tr><td>SQL 优化</td><td>提醒风险、辅助读计划</td><td>根据数据与生产约束决策</td></tr><tr><td>上线</td><td>整理清单</td><td>承担发布与回滚责任</td></tr></tbody></table><p>第一次完整使用 AI 交付需求后，我没有得到“以后可以少看代码”的结论。相反，我更清楚哪些工作不能被流畅的输出跳过。</p><p>AI 把从问题到候选方案的距离缩短了，但从候选方案到可信改动，仍然需要需求边界、项目事实、自动测试和人工判断。这四样东西在场时，它是效率很高的搭档；缺任何一项，速度都可能只是更快地走向返工。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/posts/2025/ai-feature-log-cover.png&quot; alt=&quot;AI 需求开发日志封面图&quot;&gt;&lt;/p&gt;
&lt;p&gt;这是我第一次有意识地让 AI 参与一个完整需求，而不是只让它解释报错或补一个工具方法。&lt;/p&gt;
&lt;p&gt;需求不大：在工单列表增加“最近跟进时间”筛选，并让导出接口使用相同条件。真正做下来，AI 最有价值的部分不是写代码，而是帮助我快速梳理分散的调用链；最危险的部分也不是语法错误，而是它会非常自然地替需求补上一个没有确认过的定义。&lt;/p&gt;
&lt;p&gt;下面按当天的开发时间线还原整个过程。项目和数据均做了简化，重点是我与 AI 怎样分工，以及哪些地方发生了返工。&lt;/p&gt;</summary>
    
    
    
    <category term="AI实践" scheme="https://swagch.github.io/categories/AI%E5%AE%9E%E8%B7%B5/"/>
    
    
    <category term="Code Review" scheme="https://swagch.github.io/tags/Code-Review/"/>
    
    <category term="Spring Boot" scheme="https://swagch.github.io/tags/Spring-Boot/"/>
    
    <category term="AI编程" scheme="https://swagch.github.io/tags/AI%E7%BC%96%E7%A8%8B/"/>
    
    <category term="开发日志" scheme="https://swagch.github.io/tags/%E5%BC%80%E5%8F%91%E6%97%A5%E5%BF%97/"/>
    
  </entry>
  
  <entry>
    <title>从Prompt Engineering到Context Engineering：一次提示词改造实验</title>
    <link href="https://swagch.github.io/2025/02/23/2025-02-23-%E4%BB%8EPrompt-Engineering%E5%88%B0Context-Engineering/"/>
    <id>https://swagch.github.io/2025/02/23/2025-02-23-%E4%BB%8EPrompt-Engineering%E5%88%B0Context-Engineering/</id>
    <published>2025-02-23T08:20:00.000Z</published>
    <updated>2026-07-16T16:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/posts/2025/context-engineering-cover.png" alt="Context Engineering 实验封面图"></p><p>刚开始用 AI 写代码时，我把主要精力放在“怎么写出更厉害的提示词”上：你是一名资深工程师、请深入思考、请给出最佳实践。它们偶尔有用，但面对真实项目，决定结果的往往不是形容词，而是模型到底看到了哪些事实。</p><p>为了确认这个判断，我拿一个相同的小需求做了三轮实验。任务是给 Spring Boot 项目的订单查询接口增加一个 <code>deliveryStatus</code> 字段。三轮使用同一个模型，只改变输入上下文，不手工修正中间结果。</p><span id="more"></span><h2 id="实验规则"><a href="#实验规则" class="headerlink" title="实验规则"></a>实验规则</h2><p>项目的真实约束如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">JDK 8 + Spring Boot 2.x</span><br><span class="line">Controller 返回统一的 ApiResult&lt;OrderDetailVO&gt;</span><br><span class="line">配送状态来自 delivery_record 表，不在 order_info 表</span><br><span class="line">一笔订单可能有多条配送记录，以 created_at 最新一条为准</span><br><span class="line">无配送记录时返回 NOT_STARTED</span><br><span class="line">不允许改变现有接口路径和字段含义</span><br></pre></td></tr></table></figure><p>评价结果时，我没有使用“看起来不错”这种标准，而是列了六项检查：</p><table><thead><tr><th>检查项</th><th align="right">分值</th></tr></thead><tbody><tr><td>找到正确的接口和返回对象</td><td align="right">2</td></tr><tr><td>找到配送状态的真实数据来源</td><td align="right">2</td></tr><tr><td>正确处理多条记录</td><td align="right">2</td></tr><tr><td>正确处理空记录</td><td align="right">1</td></tr><tr><td>遵守 JDK 8 与现有项目风格</td><td align="right">1</td></tr><tr><td>给出并通过对应测试</td><td align="right">2</td></tr></tbody></table><p>满分 10 分。这个分数不是严谨的模型评测，只用来比较上下文改变后的结果。</p><h2 id="第一轮：一句话需求"><a href="#第一轮：一句话需求" class="headerlink" title="第一轮：一句话需求"></a>第一轮：一句话需求</h2><p>我只输入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">给订单详情接口增加 deliveryStatus 字段，请直接修改代码。</span><br></pre></td></tr></table></figure><p>模型很快给出了一套完整方案：修改 Entity、DTO、Mapper 和数据库脚本，还建议在 <code>order_info</code> 表增加 <code>delivery_status</code> 字段。</p><p>代码组织得很像一个正常 Spring Boot 项目，甚至主动补充了 Swagger 注解。但项目根本没有它假设的 <code>OrderEntity</code>，状态也不应该存到订单表。</p><p>第一轮得分：<strong>2&#x2F;10</strong>。</p><p>得到的两分来自“字段确实加到了一个返回对象里”和“代码大致兼容 JDK 8”。其余内容主要由常见项目模式补全。模型不是故意忽略设计，它只是没有看到真实设计。</p><p>这轮让我意识到一个不太舒服的事实：输出越完整，越容易让人忘记检查前提。若我只浏览文件名，很可能会把这份错误方案当成高效率。</p><h2 id="第二轮：把提示词写得更专业"><a href="#第二轮：把提示词写得更专业" class="headerlink" title="第二轮：把提示词写得更专业"></a>第二轮：把提示词写得更专业</h2><p>第二轮仍然没有提供仓库，只把提示词改成结构化版本：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">角色：你是一名资深 Java 后端工程师。</span><br><span class="line"></span><br><span class="line">任务：为订单详情接口增加 deliveryStatus 字段。</span><br><span class="line"></span><br><span class="line">要求：</span><br><span class="line">1. 遵循分层架构和 SOLID 原则；</span><br><span class="line">2. 兼容 JDK 8；</span><br><span class="line">3. 考虑空值、异常和性能；</span><br><span class="line">4. 添加完整单元测试；</span><br><span class="line">5. 修改前先分析，修改后总结影响。</span><br></pre></td></tr></table></figure><p>第二轮的回答更谨慎。模型列出了待确认问题，提出可能从物流表获取状态，并给出 Service 聚合的伪代码。它没有直接要求改表，也提醒要检查 N+1 查询。</p><p>但只要进入具体代码，问题仍然存在：</p><ul><li>猜测项目使用 <code>ResponseEntity</code>；</li><li>创建了不存在的 <code>DeliveryStatusEnum</code>；</li><li>默认调用 <code>findTopByOrderIdOrderByCreatedAtDesc</code>；</li><li>测试使用 Mockito 5，而项目锁定的是 Mockito 2；</li><li>没有办法证明修改位置正确。</li></ul><p>第二轮得分：<strong>4&#x2F;10</strong>。</p><p>更清晰的提示词改善了任务纪律，却没有补上项目事实。它让模型更像一个会先问问题的工程师，但不能让工程师凭空看到仓库。</p><h2 id="第三轮：设计模型能够工作的上下文"><a href="#第三轮：设计模型能够工作的上下文" class="headerlink" title="第三轮：设计模型能够工作的上下文"></a>第三轮：设计模型能够工作的上下文</h2><p>第三轮没有继续润色角色，而是先准备上下文包。</p><h3 id="1-给出项目地图"><a href="#1-给出项目地图" class="headerlink" title="1. 给出项目地图"></a>1. 给出项目地图</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">请求入口：OrderController#getDetail</span><br><span class="line">业务编排：OrderQueryService#getDetail</span><br><span class="line">返回对象：OrderDetailVO</span><br><span class="line">订单查询：OrderMapper#selectDetail</span><br><span class="line">配送记录：DeliveryRecordMapper#selectLatestByOrderId</span><br><span class="line">同类实现：AfterSaleQueryService#getDetail</span><br><span class="line">测试入口：OrderQueryServiceTest</span><br></pre></td></tr></table></figure><p>这张地图不是凭感觉写的。我先在仓库里搜索接口路径、类型引用和 Mapper 方法，确保每个名字真实存在。</p><h3 id="2-提供必要代码，而不是整个仓库"><a href="#2-提供必要代码，而不是整个仓库" class="headerlink" title="2. 提供必要代码，而不是整个仓库"></a>2. 提供必要代码，而不是整个仓库</h3><p>我让模型读取上述六个文件，同时补充 <code>pom.xml</code> 中的 Java 与测试依赖版本。没有把无关 Controller、生成目录、历史日志全部塞进去。</p><h3 id="3-写清楚业务状态表"><a href="#3-写清楚业务状态表" class="headerlink" title="3. 写清楚业务状态表"></a>3. 写清楚业务状态表</h3><table><thead><tr><th>最新配送记录</th><th><code>deliveryStatus</code></th></tr></thead><tbody><tr><td>不存在</td><td><code>NOT_STARTED</code></td></tr><tr><td><code>CREATED</code></td><td><code>PREPARING</code></td></tr><tr><td><code>PICKED_UP</code></td><td><code>DELIVERING</code></td></tr><tr><td><code>SIGNED</code></td><td><code>COMPLETED</code></td></tr><tr><td>未知值</td><td><code>UNKNOWN</code>，并记录告警日志</td></tr></tbody></table><p>自然语言中的“按最新记录判断”很容易漏掉空值和未知值。状态表能让模型与人使用同一份边界定义。</p><h3 id="4-限定工作方式"><a href="#4-限定工作方式" class="headerlink" title="4. 限定工作方式"></a>4. 限定工作方式</h3><p>最终输入变成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">先阅读我列出的文件，说明当前调用链和最小修改范围。</span><br><span class="line">不要创建仓库中不存在的通用框架，也不要修改表结构。</span><br><span class="line">计划确认后再修改，每次只处理一个职责。</span><br><span class="line"></span><br><span class="line">验收条件：</span><br><span class="line">- 原有订单详情字段保持不变；</span><br><span class="line">- 四类配送记录映射符合状态表；</span><br><span class="line">- 无记录与未知状态有测试；</span><br><span class="line">- 执行项目现有测试命令；</span><br><span class="line">- 最后列出实际修改文件和仍未验证的风险。</span><br></pre></td></tr></table></figure><p>这里真正起作用的不是措辞更长，而是输入中包含可定位的文件、确定的业务规则和可执行的完成条件。</p><h2 id="第三轮发生了什么"><a href="#第三轮发生了什么" class="headerlink" title="第三轮发生了什么"></a>第三轮发生了什么</h2><p>模型先还原了实际调用链：Controller 只转发参数，<code>OrderQueryService</code> 负责组装 <code>OrderDetailVO</code>，同类的售后服务已经采用 Mapper 查询最新记录再映射状态的方式。</p><p>它提出只修改三个文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">OrderDetailVO.java</span><br><span class="line">OrderQueryService.java</span><br><span class="line">OrderQueryServiceTest.java</span><br></pre></td></tr></table></figure><p>由于 <code>DeliveryRecordMapper#selectLatestByOrderId</code> 已存在，不需要新建 Mapper 或改 SQL。映射逻辑也没有额外创造枚举，而是复用了项目中的 <code>DeliveryNodeEnum</code>。</p><p>测试第一次执行时，有一个未知状态用例失败。原因不是业务代码，而是测试夹具默认给 <code>createdAt</code> 留空，Mapper 模拟数据排序时触发空指针。模型根据真实失败信息修复了测试数据，再次执行通过。</p><p>第三轮得分：<strong>9&#x2F;10</strong>。</p><p>扣掉的一分来自它最初建议把映射方法放进 VO。我认为状态解释属于查询服务，不应让纯返回对象依赖配送领域枚举，因此人工审查后调整了位置。</p><h2 id="三轮结果放在一起"><a href="#三轮结果放在一起" class="headerlink" title="三轮结果放在一起"></a>三轮结果放在一起</h2><table><thead><tr><th>轮次</th><th>输入重点</th><th align="right">得分</th><th>主要问题</th></tr></thead><tbody><tr><td>一句话需求</td><td>目标</td><td align="right">2</td><td>大量猜测项目结构</td></tr><tr><td>结构化 Prompt</td><td>角色、原则、步骤</td><td align="right">4</td><td>行为更谨慎，事实仍缺失</td></tr><tr><td>Context Engineering</td><td>文件、规则、约束、验证</td><td align="right">9</td><td>基本可用，仍需架构判断</td></tr></tbody></table><p>这次实验并不能证明提示词不重要。清楚的任务表达仍然能减少歧义，但在代码仓库里，它只是上下文的一部分。</p><p>我对两者的区分是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Prompt Engineering</span><br><span class="line">关注“这一句话怎样表达得更有效”。</span><br><span class="line"></span><br><span class="line">Context Engineering</span><br><span class="line">关注“为了完成任务，模型在每一步应该看到什么事实、工具和反馈”。</span><br></pre></td></tr></table></figure><p>后者更像设计一条信息流水线。它不仅包含用户的第一段输入，还包括仓库检索、文件选择、构建日志、测试结果、团队规范和前一步产生的决策。</p><h2 id="上下文也会变质"><a href="#上下文也会变质" class="headerlink" title="上下文也会变质"></a>上下文也会变质</h2><p>上下文不是越多越好。我在后续任务中遇到过三个反例。</p><h3 id="过期上下文"><a href="#过期上下文" class="headerlink" title="过期上下文"></a>过期上下文</h3><p>需求文档写的是旧接口，代码已经迁移到新服务。模型严格遵循文档，反而改错位置。重要事实要标明来源与时间，冲突时以当前可执行代码和明确决策为准。</p><h3 id="相互冲突的上下文"><a href="#相互冲突的上下文" class="headerlink" title="相互冲突的上下文"></a>相互冲突的上下文</h3><p>全局规范要求所有异常转换成统一错误码，模块内却保留了旧的直接抛出方式。若不说明本次遵循哪一层，模型可能随机选择。冲突不能靠增加更多文本解决，需要人做决策。</p><h3 id="没有优先级的上下文"><a href="#没有优先级的上下文" class="headerlink" title="没有优先级的上下文"></a>没有优先级的上下文</h3><p>我曾一次提供十几个“参考实现”。模型混用了其中三种风格。后来只给一个最接近的主参考，其余资料明确标注“仅用于理解字段，不复制结构”，结果稳定很多。</p><p>所以，上下文设计还需要三条规则：</p><ol><li>保证来源真实且尽量新；</li><li>冲突信息写明优先级；</li><li>每份材料说明它在任务中的用途。</li></ol><h2 id="我现在使用的上下文清单"><a href="#我现在使用的上下文清单" class="headerlink" title="我现在使用的上下文清单"></a>我现在使用的上下文清单</h2><p>开始一个代码任务前，我通常检查下面这些信息是否齐全：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">[ ] 用户问题与业务目标</span><br><span class="line">[ ] 当前可复现的现象</span><br><span class="line">[ ] 入口、调用链和关键数据结构</span><br><span class="line">[ ] 语言、框架和依赖版本</span><br><span class="line">[ ] 可以复用的相邻实现</span><br><span class="line">[ ] 明确不能改变的行为</span><br><span class="line">[ ] 异常、空值、并发和权限边界</span><br><span class="line">[ ] 要执行的测试与构建命令</span><br><span class="line">[ ] 修改范围和回滚方式</span><br></pre></td></tr></table></figure><p>简单任务不必把每一项写成长文，但缺失项要心里有数。尤其是“怎样算完成”，如果人自己都没有答案，模型只能生成一个听起来合理的终点。</p><p>从 Prompt Engineering 走向 Context Engineering，对我最大的改变不是提示词变长了，而是开始把 AI 编程当成工程协作：先准备事实，控制信息流，再用真实反馈纠偏。模型负责快速探索和执行，人负责选择上下文、处理冲突并对结果签字。</p><p>这也许没有“一句话生成整个系统”那么有戏剧性，却更接近可以重复使用的生产力。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/posts/2025/context-engineering-cover.png&quot; alt=&quot;Context Engineering 实验封面图&quot;&gt;&lt;/p&gt;
&lt;p&gt;刚开始用 AI 写代码时，我把主要精力放在“怎么写出更厉害的提示词”上：你是一名资深工程师、请深入思考、请给出最佳实践。它们偶尔有用，但面对真实项目，决定结果的往往不是形容词，而是模型到底看到了哪些事实。&lt;/p&gt;
&lt;p&gt;为了确认这个判断，我拿一个相同的小需求做了三轮实验。任务是给 Spring Boot 项目的订单查询接口增加一个 &lt;code&gt;deliveryStatus&lt;/code&gt; 字段。三轮使用同一个模型，只改变输入上下文，不手工修正中间结果。&lt;/p&gt;</summary>
    
    
    
    <category term="AI实践" scheme="https://swagch.github.io/categories/AI%E5%AE%9E%E8%B7%B5/"/>
    
    
    <category term="AI" scheme="https://swagch.github.io/tags/AI/"/>
    
    <category term="Prompt Engineering" scheme="https://swagch.github.io/tags/Prompt-Engineering/"/>
    
    <category term="Context Engineering" scheme="https://swagch.github.io/tags/Context-Engineering/"/>
    
    <category term="AI编程" scheme="https://swagch.github.io/tags/AI%E7%BC%96%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>大模型入门：从Token、上下文到幻觉</title>
    <link href="https://swagch.github.io/2025/01/12/2025-01-12-%E5%A4%A7%E6%A8%A1%E5%9E%8B%E5%85%A5%E9%97%A8-%E4%BB%8EToken%E4%B8%8A%E4%B8%8B%E6%96%87%E5%88%B0%E5%B9%BB%E8%A7%89/"/>
    <id>https://swagch.github.io/2025/01/12/2025-01-12-%E5%A4%A7%E6%A8%A1%E5%9E%8B%E5%85%A5%E9%97%A8-%E4%BB%8EToken%E4%B8%8A%E4%B8%8B%E6%96%87%E5%88%B0%E5%B9%BB%E8%A7%89/</id>
    <published>2025-01-12T12:30:00.000Z</published>
    <updated>2026-07-16T16:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/posts/2025/llm-basics-cover.png" alt="大模型入门封面图"></p><p>第一次使用大模型时，我把它当成了一个更聪明的搜索框：输入问题，等待正确答案。用了一段时间后才发现，这个理解会制造很多误判。模型不是从知识库里查出一段标准答案，而是在已有上下文后面，持续预测更可能出现的下一个 Token。</p><p>这句话看起来抽象，却能解释日常使用中的大部分现象：为什么同一个问题会得到不同答案，为什么一段无关日志会让结果变差，为什么模型能写出语法正确但根本不存在的 API，也能解释为什么“把需求说清楚”比反复要求它聪明一点更有效。</p><span id="more"></span><h2 id="先画一张最小心智模型"><a href="#先画一张最小心智模型" class="headerlink" title="先画一张最小心智模型"></a>先画一张最小心智模型</h2><p>不讨论复杂公式，一个请求可以先理解成下面这条流水线：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">输入文本</span><br><span class="line">  ↓ 分词</span><br><span class="line">Token 序列</span><br><span class="line">  ↓ 结合已有上下文计算概率</span><br><span class="line">下一个 Token</span><br><span class="line">  ↓ 把新 Token 放回上下文继续计算</span><br><span class="line">一段完整回复</span><br></pre></td></tr></table></figure><p>这里有四个关键词：Token、上下文、概率生成和训练数据。它们不是模型的全部，但足够支撑入门阶段的判断。</p><p>如果把模型当数据库，就会问“它明明知道，为什么还答错”；把它当概率生成系统，问题会变成“我提供的上下文是否足以约束下一步输出”。后一个问题更接近可操作的工程问题。</p><h2 id="Token-不是汉字，也不等于单词"><a href="#Token-不是汉字，也不等于单词" class="headerlink" title="Token 不是汉字，也不等于单词"></a>Token 不是汉字，也不等于单词</h2><p>模型读取的基本单位不是“一个汉字”或“一个英文单词”，而是 Token。不同模型使用不同的分词方式，一段文本可能按完整词、词的一部分、标点或空格切分。</p><p>例如下面只是便于理解的示意，不代表某个具体模型的真实分词结果：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Spring Boot 启动失败</span><br><span class="line"></span><br><span class="line">可能被拆成：</span><br><span class="line">[Spring] [ Boot] [ 启动] [失败]</span><br><span class="line"></span><br><span class="line">orderService.getById(1001)</span><br><span class="line"></span><br><span class="line">可能被拆成：</span><br><span class="line">[order] [Service] [.] [get] [By] [Id] [(] [1001] [)]</span><br></pre></td></tr></table></figure><p>Token 直接影响三件事：</p><ol><li><strong>上下文容量</strong>：模型能同时处理多少输入和输出，通常以 Token 计量。</li><li><strong>调用成本</strong>：API 常按输入与输出 Token 数收费。</li><li><strong>响应时间</strong>：输入越长、输出越长，处理和生成通常越慢。</li></ol><p>所以“我只发了一个文件”并不代表输入很小。一个两千行的压缩 Java 类、完整依赖锁文件、重复堆栈和打包后的 HTML，都可能消耗大量上下文，却没有提供等比例的信息。</p><p>我后来会在提交问题前先做一次人工降噪：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">删除重复异常堆栈</span><br><span class="line">只保留相关配置段</span><br><span class="line">给代码附上文件路径</span><br><span class="line">说明入口方法和预期行为</span><br><span class="line">用摘要代替整份构建日志</span><br></pre></td></tr></table></figure><p>这不是为了替模型省字，而是提高有效信息密度。</p><h2 id="上下文窗口更像工作台，不像长期记忆"><a href="#上下文窗口更像工作台，不像长期记忆" class="headerlink" title="上下文窗口更像工作台，不像长期记忆"></a>上下文窗口更像工作台，不像长期记忆</h2><p>上下文窗口可以理解成模型当前这次任务能够看到的工作台。系统指令、历史对话、用户输入、工具读取到的代码和模型已经生成的内容，都要占用这张工作台。</p><p>工作台很大，不代表可以随意堆东西。一个常见误区是：既然模型支持很长上下文，就把整个项目一次性贴进去。结果往往是关键约束被埋在中间，模型抓住了相似但无关的实现。</p><p>我更愿意把上下文分成四层：</p><table><thead><tr><th>层次</th><th>应提供的内容</th><th>例子</th></tr></thead><tbody><tr><td>任务</td><td>这次到底要改变什么</td><td>“订单详情增加预计送达时间”</td></tr><tr><td>约束</td><td>哪些行为不能变</td><td>“JDK 8；不修改数据库；旧客户端兼容”</td></tr><tr><td>证据</td><td>当前系统真实是什么样</td><td>Controller、DTO、测试、报错日志</td></tr><tr><td>验收</td><td>怎样证明已经完成</td><td>指定测试、接口样例、构建命令</td></tr></tbody></table><p>这四层都比“你是一个资深 Java 专家，请认真完成”更有约束力。角色设定可以影响表达方式，却不能代替项目事实。</p><p>上下文也不等于长期记忆。新对话里，模型通常不会天然知道上一次讨论的决策。对于长期项目，真正可靠的记忆仍然应该落在仓库中，例如：</p><ul><li><code>README.md</code> 说明运行方式；</li><li><code>AGENTS.md</code> 记录编码约束；</li><li>ADR 记录架构决策；</li><li>测试固定行为边界；</li><li>Issue 或设计文档解释需求背景。</li></ul><p>能被版本管理的知识，比“希望模型下次还记得”可靠得多。</p><h2 id="推理过程的本质仍然是生成"><a href="#推理过程的本质仍然是生成" class="headerlink" title="推理过程的本质仍然是生成"></a>推理过程的本质仍然是生成</h2><p>模型回复并不是一次性吐出整段文字，而是一个 Token 接一个 Token 地生成。每一步都会根据当前上下文给候选 Token 分配概率，再按配置选择结果。</p><p>可以用一个不严谨但直观的例子理解：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">输入：Java 中 HashMap 在并发写入时可能出现 ____</span><br><span class="line"></span><br><span class="line">候选：</span><br><span class="line">数据覆盖       0.42</span><br><span class="line">结构异常       0.24</span><br><span class="line">可见性问题     0.19</span><br><span class="line">性能提升       0.01</span><br><span class="line">其他候选       0.14</span><br></pre></td></tr></table></figure><p>采样参数越偏确定，模型越倾向选择高概率候选；采样越开放，表达可能更有变化，也更容易偏离。写创意文案和修复支付代码显然不该使用同一种容错态度。</p><p>所谓“推理模型”能在回答前进行更充分的中间计算，复杂任务上的表现通常更好。但它仍然不会自动获得项目权限、真实数据库状态或团队没有写下来的业务规则。推理能力提升，不能弥补输入事实缺失。</p><h2 id="幻觉为什么看起来那么像真的"><a href="#幻觉为什么看起来那么像真的" class="headerlink" title="幻觉为什么看起来那么像真的"></a>幻觉为什么看起来那么像真的</h2><p>幻觉并不一定表现为胡言乱语。更危险的情况是：答案语法正确、解释流畅、命名符合生态习惯，但其中一个类、配置项或版本能力并不存在。</p><p>这与模型的目标有关。它擅长生成“像答案的内容”，不天然负责证明每句话来自可靠来源。</p><p>我遇到过几类典型幻觉：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">引用一个不存在的 Spring Boot 配置项</span><br><span class="line">把新版本 API 用在 JDK 8 项目中</span><br><span class="line">为内部 SDK 猜测一个风格很合理的方法名</span><br><span class="line">根据 Controller 名称臆测数据库字段</span><br><span class="line">声称测试已经通过，但实际上没有执行测试</span><br></pre></td></tr></table></figure><p>应对幻觉不能只靠一句“不要编造”。更有效的是让任务具备证据闭环：</p><ol><li><strong>要求引用来源</strong>：结论对应哪个文件、哪段日志或哪份官方文档。</li><li><strong>允许表达不确定</strong>：证据不足时明确写“无法从当前上下文确认”。</li><li><strong>让工具验证</strong>：搜索仓库、编译代码、执行测试，而不是凭记忆猜 API。</li><li><strong>缩小修改范围</strong>：先处理可验证的小步骤，避免一次生成几十个文件。</li><li><strong>人工阅读差异</strong>：模型写得越顺，越要检查被默认掉的前提。</li></ol><p>对代码任务，我现在会把“事实”和“建议”分开看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">事实：项目 pom.xml 的 Java 版本是 1.8</span><br><span class="line">事实：现有分页统一使用 PageInfo</span><br><span class="line">建议：复用 PageInfo，而不是引入新的分页对象</span><br><span class="line">待确认：空列表时 total 字段是否必须为 0</span><br></pre></td></tr></table></figure><p>只要这几种信息混在一起，猜测就容易披上事实的外衣。</p><h2 id="一个五分钟的小实验"><a href="#一个五分钟的小实验" class="headerlink" title="一个五分钟的小实验"></a>一个五分钟的小实验</h2><p>理解概念最简单的方式不是继续背术语，而是做一次对照。</p><p>先只问：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">帮我优化这段代码。</span><br></pre></td></tr></table></figure><p>然后补齐上下文：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">这是 JDK 8 的 Spring Boot 项目。</span><br><span class="line">目标：降低批量查询中的数据库访问次数。</span><br><span class="line">约束：接口响应结构不能变化，不新增依赖。</span><br><span class="line">现状：循环中调用 orderMapper.selectById，共 500 次。</span><br><span class="line">请先指出性能证据和修改范围，再给方案；没有看到的类不要猜。</span><br><span class="line">验收：原有单元测试通过，并新增“500 个重复订单只查询一次”的测试。</span><br></pre></td></tr></table></figure><p>比较两次结果时，不只看第二次是不是更长，而是看：</p><ul><li>是否识别到真正的 N+1 查询；</li><li>是否遵守 JDK 8 和不新增依赖；</li><li>是否区分重复 ID 与不存在的数据；</li><li>是否提供可以证明改进的测试；</li><li>是否仍然编造了仓库里没有的方法。</li></ul><p>这个实验会直观说明，输出质量并不只取决于模型大小，也取决于输入是否包含任务边界和验证方式。</p><h2 id="入门阶段，我认为最值得保留的四个习惯"><a href="#入门阶段，我认为最值得保留的四个习惯" class="headerlink" title="入门阶段，我认为最值得保留的四个习惯"></a>入门阶段，我认为最值得保留的四个习惯</h2><p><strong>第一，把模型当协作者，不当权威。</strong> 它可以快速提出候选方案，却不替开发者承担上线结果。</p><p><strong>第二，先给事实，再提要求。</strong> 文件、日志、版本、约束和验收条件，比堆叠形容词更有效。</p><p><strong>第三，生成之后必须验证。</strong> 能执行测试就执行测试，能查官方文档就查文档，不能验证就明确风险。</p><p><strong>第四，敏感信息默认不进入模型。</strong> Token、密码、客户数据、生产日志和内部地址要先脱敏，并遵守团队的数据使用规则。</p><p>学会几个提示词技巧并不难，真正需要时间的是建立正确预期：大模型擅长在上下文中生成高概率答案，而工程交付要求我们把这个答案变成可追溯、可测试、可回退的改动。</p><p>理解这个差距，才算真正开始使用 AI 编程。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/posts/2025/llm-basics-cover.png&quot; alt=&quot;大模型入门封面图&quot;&gt;&lt;/p&gt;
&lt;p&gt;第一次使用大模型时，我把它当成了一个更聪明的搜索框：输入问题，等待正确答案。用了一段时间后才发现，这个理解会制造很多误判。模型不是从知识库里查出一段标准答案，而是在已有上下文后面，持续预测更可能出现的下一个 Token。&lt;/p&gt;
&lt;p&gt;这句话看起来抽象，却能解释日常使用中的大部分现象：为什么同一个问题会得到不同答案，为什么一段无关日志会让结果变差，为什么模型能写出语法正确但根本不存在的 API，也能解释为什么“把需求说清楚”比反复要求它聪明一点更有效。&lt;/p&gt;</summary>
    
    
    
    <category term="AI实践" scheme="https://swagch.github.io/categories/AI%E5%AE%9E%E8%B7%B5/"/>
    
    
    <category term="AI" scheme="https://swagch.github.io/tags/AI/"/>
    
    <category term="大模型" scheme="https://swagch.github.io/tags/%E5%A4%A7%E6%A8%A1%E5%9E%8B/"/>
    
    <category term="Token" scheme="https://swagch.github.io/tags/Token/"/>
    
    <category term="上下文" scheme="https://swagch.github.io/tags/%E4%B8%8A%E4%B8%8B%E6%96%87/"/>
    
    <category term="幻觉" scheme="https://swagch.github.io/tags/%E5%B9%BB%E8%A7%89/"/>
    
  </entry>
  
  <entry>
    <title>无外网安装nginx</title>
    <link href="https://swagch.github.io/2024/05/22/%E6%97%A0%E5%A4%96%E7%BD%91%E5%AE%89%E8%A3%85nginx/"/>
    <id>https://swagch.github.io/2024/05/22/%E6%97%A0%E5%A4%96%E7%BD%91%E5%AE%89%E8%A3%85nginx/</id>
    <published>2024-05-22T05:04:02.000Z</published>
    <updated>2024-06-02T05:05:29.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/posts/2024/offline-nginx-cover.png" alt="无外网安装Nginx封面图"></p><blockquote><p>写在前面：客户给的服务器是银河麒麟，然后不给外网权限，要求安装nginx，网上教程基本没有这种情况的。也是踩了很多的坑，所以记录一下方便后面的bro。PS：重点在于如何安装上nginx，比较基础的操作可以同时参考其他教程一起看！！</p></blockquote><h2 id="1-服务器情况："><a href="#1-服务器情况：" class="headerlink" title="1. 服务器情况："></a>1. 服务器情况：</h2><figure class="highlight bash"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /proc/version</span><br><span class="line">Linux version 4.19.90-23.8.v2101.ky10.x86_64</span><br></pre></td></tr></tbody></table></figure><h2 id="2-准备工作"><a href="#2-准备工作" class="headerlink" title="2.准备工作"></a>2.准备工作</h2><h3 id="2-1-首先判断是否安装了nginx的三个依赖"><a href="#2-1-首先判断是否安装了nginx的三个依赖" class="headerlink" title="2.1.首先判断是否安装了nginx的三个依赖"></a>2.1.首先判断是否安装了nginx的三个依赖</h3><ol><li>gcc</li><li>pcre</li><li>zlib</li></ol><p>可以访问外网的情况下可以直接命令行一键安装</p><figure class="highlight bash"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">yum -y install gcc zlib zlib-devel pcre-devel openssl </span><br></pre></td></tr></tbody></table></figure><p>本篇就不涉及有外网了，重点在无外网。</p><p>没外网的情况下，需要先手动下载上面三个依赖包，方便大家我就把整合好的地址放下面，大家有需要就自取</p><p>链接：<a target="_blank" rel="noopener" href="https://pan.baidu.com/s/1r0c0o6ORbOdB9vM8RoWUrA?pwd=bpmy">https://pan.baidu.com/s/1r0c0o6ORbOdB9vM8RoWUrA?pwd=bpmy</a><br>提取码：bpmy </p><p>下载好了以后，分别把三个依赖包，和nginx，指定到自己指定的位置<br>我是把依赖包解压到了/usr/local/src下</p><figure class="highlight bash"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">这是解压命令</span><br><span class="line">tar -xvf openssl-3.0.13.tar.gz</span><br><span class="line"></span><br><span class="line">三个包手动替换的名称就好</span><br></pre></td></tr></tbody></table></figure><blockquote><p>重点！！！</p></blockquote><p>回到nginx的安装目录，执行下面的命令</p><figure class="highlight bash"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">./configure --prefix=/usr/local/nginx   --with-pcre=/usr/local/src/pcre-7.3 --with-zlib=/usr/local/src/zlib  --with-openssl=/usr/local/src/openssl</span><br><span class="line"></span><br><span class="line">解释</span><br><span class="line">--prefix 是指定nginx的安装路径，后续的我们操作的nginx都会在这里，类似于windows的选择安装路径</span><br><span class="line">--with-xxx 指定依赖的解压路径，用来找到我们刚刚解压缩的那三个依赖。</span><br></pre></td></tr></tbody></table></figure><p>没报错的情况下，继续执行</p><figure class="highlight bash"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">make</span><br><span class="line">make install</span><br><span class="line"></span><br><span class="line">就可以正常安装了</span><br></pre></td></tr></tbody></table></figure><p>到这里nginx应该就安装成功了，后续就是一些优化操作</p><ol><li>可以删除nginx压缩包和解压目录<br>2.设置软链，后续就不用在安装nginx的目录下操作nginx</li></ol><figure class="highlight bash"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ln</span> -s /usr/local/webserver/nginx/sbin/nginx /usr/bin/</span><br><span class="line"></span><br><span class="line">在其他目录使用</span><br><span class="line">nginx -v</span><br><span class="line">查看是否设置成功</span><br></pre></td></tr></tbody></table></figure><p>3.nginx常用命令</p><figure class="highlight bash"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 检查配置</span></span><br><span class="line">$ nginx -t（检查nginx.conf配置是否正确）</span><br><span class="line">$ nginx -s reload（重新载入配置文件，通常配合-t使用，在修改了nginx.conf且检查无误之后）</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动</span></span><br><span class="line">$ nginx</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看进程</span></span><br><span class="line">$ ps -ef | grep nginx</span><br></pre></td></tr></tbody></table></figure><blockquote><p>之前部署项目的时候没遇到过国产系统+无内网两种情况叠加，导致踩坑了两天才调通，希望这篇文章能对后面遇到这种情况的兄弟们有所帮助！</p></blockquote>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/posts/2024/offline-nginx-cover.png&quot; alt=&quot;无外网安装Nginx封面图&quot;&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;写在前面：客户给的服务器是银河麒麟，然后不给外网权限，要求安装nginx，网上教程基</summary>
      
    
    
    
    <category term="Linux运维" scheme="https://swagch.github.io/categories/Linux%E8%BF%90%E7%BB%B4/"/>
    
    
    <category term="Linux" scheme="https://swagch.github.io/tags/Linux/"/>
    
    <category term="运维" scheme="https://swagch.github.io/tags/%E8%BF%90%E7%BB%B4/"/>
    
  </entry>
  
  <entry>
    <title>JUC并发编程知识点梳理</title>
    <link href="https://swagch.github.io/2024/05/02/juc%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E4%B8%80%E4%BA%9B%E7%90%86%E8%A7%A3/"/>
    <id>https://swagch.github.io/2024/05/02/juc%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E4%B8%80%E4%BA%9B%E7%90%86%E8%A7%A3/</id>
    <published>2024-05-02T01:29:50.000Z</published>
    <updated>2024-06-02T05:03:03.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/posts/2024/juc-notes-cover.png" alt="JUC并发编程知识点封面图"></p><h1 id="JUC并发编程"><a href="#JUC并发编程" class="headerlink" title="JUC并发编程"></a>JUC并发编程</h1><blockquote><p>写在前面</p></blockquote><p>基础的查缺补漏问题，感觉工作上用的并发编程不是很多，都快忘得差不多了，通过看书+视频的方式，补充一些相关知识。</p><h2 id="1-什么是JUC"><a href="#1-什么是JUC" class="headerlink" title="1.什么是JUC"></a>1.什么是JUC</h2><p>JUC（Java Util Concurrency）是Java工具类库中的一部分，提供了在多线程环境下编写并发程序所需的工具和框架。JUC包含了一些并发编程的核心概念和工具，使得开发人员能够更轻松地编写高效、线程安全的并发程序。</p><h2 id="2-进程和线程"><a href="#2-进程和线程" class="headerlink" title="2.进程和线程"></a>2.进程和线程</h2><ol><li><p>进程：一个程序的集合。称为进程。</p></li><li><p>一个程序可以包含多个线程，至少包含一个线程！</p></li><li><p>Java默认包含两个线程，main ，GC</p></li><li><p>Java开启线程的方式，Thread，Runnable，Callable</p></li><li><p><strong>Java是无法自己开启线程的</strong>，本质上Java是通过操作c来开启线程</p><blockquote><p>​并发，并行</p></blockquote><p>并发：多个线程操作同一个资源</p><ul><li>cpu单核，模拟出多个线程，不停切换</li></ul><p>并行： 多个线程同时操作多个资源</p><ul><li>​多核cpu，多个线程同时执行</li></ul><p>并发编程的本质：充分利用cpu的资源</p><blockquote><p>线程状态</p></blockquote><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">State</span> {</span><br><span class="line"><span class="comment">//新生</span></span><br><span class="line">NEW,</span><br><span class="line">  <span class="comment">// 运行</span></span><br><span class="line">RUNNABLE,</span><br><span class="line"><span class="comment">// 阻塞</span></span><br><span class="line">BLOCKED,</span><br><span class="line"><span class="comment">// 等待，死死地等</span></span><br><span class="line">WAITING,</span><br><span class="line"><span class="comment">// 超时等待</span></span><br><span class="line">TIMED_WAITING,</span><br><span class="line"><span class="comment">// 终止</span></span><br><span class="line">TERMINATED;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><blockquote><p>​wait/sleep的区别</p></blockquote><ol><li><p>来自不同的类</p><p>Wait =&gt;object,sleep =&gt;Thread</p></li><li><p>锁的释放</p><p>wait会释放锁，sleep 不会释放锁！</p></li><li><p>使用的范围不同</p><p>wait：必须在同步代码块中。sleep没有限制</p></li><li><p>唤醒方式</p><p>wait：需要调用notify或者notifyAll唤醒，sleep自动唤醒，或者调用interrupt（）方法唤醒</p></li></ol></li></ol><h2 id="3-Lock锁（重点）"><a href="#3-Lock锁（重点）" class="headerlink" title="3.Lock锁（重点）"></a>3.Lock锁（重点）</h2><blockquote><p>传统Synchronized</p></blockquote><p>在代码块或者方法上加synchronized</p><blockquote><p>Lock接口</p></blockquote><ol><li>加锁，解锁方法</li><li>可重入锁，读写锁方法</li><li>公平锁，非公平锁</li></ol><blockquote><p>Synchronized 和 Lock 区别</p></blockquote><ol><li><p><code>synchronized</code>关键字和<code>Lock</code>接口是Java中用于实现线程同步的两种不同方式，它们之间有以下几点区别：</p><ol><li><p><strong>使用方式：</strong></p><ul><li><code>synchronized</code>是Java语言提供的关键字，可以直接用于方法或代码块中，不需要显式地创建锁对象。</li><li><code>Lock</code>是一个接口，需要显式地创建锁对象，通常使用<code>ReentrantLock</code>类来实现。</li></ul></li><li><p><strong>灵活性：</strong></p><ul><li><code>synchronized</code>是一种内置的锁机制，比较简单，但缺乏一些高级功能。</li><li><code>Lock</code>接口提供了更多的功能，如可中断锁、超时获取锁、尝试非阻塞获取锁等。</li></ul></li><li><p><strong>等待锁的方式：</strong></p><ul><li><code>synchronized</code>在获取不到锁时，线程会进入阻塞状态，直到获取到锁为止。</li><li><code>Lock</code>在获取不到锁时，可以选择等待、放弃等待或者做其他处理。</li></ul></li><li><p><strong>释放锁的方式：</strong></p><ul><li><code>synchronized</code>在方法执行完毕或代码块结束时会自动释放锁。</li><li><code>Lock</code>需要手动调用<code>unlock()</code>方法来释放锁。</li></ul></li><li><p><strong>性能：</strong></p><ul><li><code>synchronized</code>在JVM层面上对锁进行优化，因此在某些情况下性能可能比<code>Lock</code>更好。</li><li><code>Lock</code>提供了更多的灵活性和控制，但由于是通过Java代码实现的，性能上可能会稍差。</li></ul></li></ol><p>总的来说，<code>synchronized</code>适用于简单的线程同步需求，而<code>Lock</code>适用于对锁需要更精细控制和灵活性的场景。在选择使用时，需要根据具体情况来决定哪种方式更合适。</p></li></ol><h2 id="4-生产者消费者问题"><a href="#4-生产者消费者问题" class="headerlink" title="4.生产者消费者问题"></a>4.生产者消费者问题</h2><p>本质：等待唤醒，通知唤醒。</p><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">* 生产者消费者问题</span></span><br><span class="line"><span class="comment">* */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="type">data1</span> <span class="variable">d</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">data1</span>();</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt;{</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) {</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    d.inc();</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        },<span class="string">"T1"</span>).start();</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt;{</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) {</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    d.dec();</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        },<span class="string">"T2"</span>).start();</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">data1</span>{</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">number</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title function_">inc</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException {</span><br><span class="line">        <span class="keyword">if</span> (number != <span class="number">0</span>) {</span><br><span class="line">            <span class="built_in">this</span>.wait();</span><br><span class="line">        }</span><br><span class="line">        number++;</span><br><span class="line">        System.out.println(Thread.currentThread().getName()+<span class="string">"=&gt;"</span>+number);</span><br><span class="line">        <span class="built_in">this</span>.notifyAll();</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title function_">dec</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException {</span><br><span class="line">        <span class="keyword">if</span> (number == <span class="number">0</span>) {</span><br><span class="line">            <span class="built_in">this</span>.wait();</span><br><span class="line">        }</span><br><span class="line">        number--;</span><br><span class="line">        System.out.println(Thread.currentThread().getName()+<span class="string">"=&gt;"</span>+number);</span><br><span class="line">        <span class="built_in">this</span>.notifyAll();</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><blockquote><p>虚假唤醒问题</p></blockquote><p>虚假唤醒（Spurious Wakeup）是指在多线程编程中，一个线程在没有被通知、没有收到信号的情况下，从等待状态（通常是wait()函数）中被唤醒，但此时并没有符合其等待条件的事件发生。这种情况可能会在某些多线程环境中发生，尽管并没有其他线程调用notify()或notifyAll()来通知等待线程，等待线程仍然被唤醒。</p><p>虚假唤醒可能会导致程序逻辑错误，因为等待线程被唤醒后可能会继续执行，但实际上并没有真正的事件发生。为了防止虚假唤醒，通常在使用wait()方法时，应该始终在循环中检查条件，并在条件不满足时继续等待，直到条件满足为止。例如：</p><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">synchronized</span> (obj) {</span><br><span class="line">    <span class="keyword">while</span> (!condition) {</span><br><span class="line">        obj.wait();</span><br><span class="line">    }</span><br><span class="line">    <span class="comment">// 执行其他操作</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><p>这样可以确保即使发生虚假唤醒，等待线程也会重新检查条件并在条件满足时继续执行，避免了逻辑错误。</p><blockquote><p>使用juc实现</p></blockquote><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.jucstudy.customer;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Condition;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Lock;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.ReentrantLock;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">* 生产者消费者问题</span></span><br><span class="line"><span class="comment">* */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="type">data1</span> <span class="variable">d</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">data1</span>();</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt;{</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) {</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    d.inc();</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        },<span class="string">"T1"</span>).start();</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt;{</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) {</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    d.dec();</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        },<span class="string">"T2"</span>).start();</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">data1</span>{</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">number</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="type">Lock</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ReentrantLock</span>();</span><br><span class="line">    <span class="type">Condition</span> <span class="variable">condition</span> <span class="operator">=</span> lock.newCondition();</span><br><span class="line">    <span class="keyword">public</span>  <span class="keyword">void</span> <span class="title function_">inc</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException {</span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> {</span><br><span class="line">            <span class="keyword">if</span> (number != <span class="number">0</span>) {</span><br><span class="line"><span class="comment">//                等待</span></span><br><span class="line">                condition.await();</span><br><span class="line">            }</span><br><span class="line">            number++;</span><br><span class="line">            System.out.println(Thread.currentThread().getName()+<span class="string">"=&gt;"</span>+number);</span><br><span class="line"><span class="comment">//            通知</span></span><br><span class="line">            condition.signalAll();</span><br><span class="line">        } <span class="keyword">finally</span> {</span><br><span class="line">            lock.unlock();</span><br><span class="line">        }</span><br><span class="line"></span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">public</span>  <span class="keyword">void</span> <span class="title function_">dec</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException {</span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> {</span><br><span class="line">            <span class="keyword">if</span> (number == <span class="number">0</span>) {</span><br><span class="line">                condition.await();</span><br><span class="line">            }</span><br><span class="line">            number--;</span><br><span class="line">            System.out.println(Thread.currentThread().getName()+<span class="string">"=&gt;"</span>+number);</span><br><span class="line">            condition.signalAll();</span><br><span class="line">        } <span class="keyword">finally</span> {</span><br><span class="line">            lock.unlock();</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure><blockquote><p>使用JUC的优点，Condition可以精准的通知和唤醒线程   </p></blockquote><h2 id="5-集合类不安全"><a href="#5-集合类不安全" class="headerlink" title="5.集合类不安全"></a>5.集合类不安全</h2><blockquote><p>LIst，Set，Map，在并发情况下修改数据，都会产生 java.util.ConcurrentModificationException 并发修改异常！</p></blockquote><h4 id="List下的解决方案"><a href="#List下的解决方案" class="headerlink" title="List下的解决方案"></a>List下的解决方案</h4><ol><li>List list = new Vector&lt;&gt;();</li><li>List list = Collections.synchronizedList(new ArrayList&lt;&gt; ());</li><li><strong>List list = new CopyOnWriteArrayList&lt;&gt;()；</strong></li></ol><blockquote><p>Cow,读写分离</p></blockquote><p>Copy-on-write（写时复制）是一种用于处理共享数据的技术，它可以解决写入时覆盖的问题。当多个进程或线程要访问同一个资源时，如果每个进程都复制一份资源的副本，会占用大量内存。相反，可以让所有进程共享同一个资源的副本，但只有在有进程要修改资源时才会复制资源，这就是写时复制的概念。</p><p>具体来说，当一个进程要修改资源时，系统会首先检查资源的引用计数。如果这个资源只被一个进程引用，那么该进程可以直接修改资源而无需复制。但如果资源被多个进程引用，系统就会将资源复制一份，然后让要修改资源的进程修改新复制的资源，而其他进程继续共享原始资源。这样一来，每个进程都可以独立地修改自己的资源副本，而不会影响其他进程。</p><p>通过写时复制，可以在多个进程之间共享资源的同时，避免了写入时的覆盖问题，提高了系统的性能和效率。</p><h4 id="Set的解决方案"><a href="#Set的解决方案" class="headerlink" title="Set的解决方案"></a>Set的解决方案</h4><ol><li>Set set = Collections.synchronizedSet(new HashSet&lt;&gt;());</li><li>Set set = new CopyOnWriteArraySet&lt;&gt;();</li></ol><blockquote><p>HashSet的底层</p></blockquote><p>本质上就是HashMap，用set的值作为key，所以保证了唯一性。</p><h4 id="Map的解决方案"><a href="#Map的解决方案" class="headerlink" title="Map的解决方案"></a>Map的解决方案</h4><ol><li>Map map = new ConcurrentHashMap&lt;&gt;();</li><li>Collections.synchronizedMap；</li></ol><p>原理：</p><p>ConcurrentHashMap 是 Java 中线程安全的哈希表实现，它采用了一种不同于传统 HashMap 的机制来实现并发访问。主要原理如下：</p><ol><li><p><strong>分段锁（Segment）</strong>：ConcurrentHashMap 内部维护了一个 Segment 数组，每个 Segment 就是一个独立的哈希表，类似于将一个大的哈希表分成多个小的哈希表。每个 Segment 都相当于一个小的 HashMap，它们之间相互独立，可以独立进行并发操作。</p></li><li><p><strong>Hash 表的扩容</strong>：与 HashMap 类似，ConcurrentHashMap 也会在需要时对哈希表进行扩容。但不同的是，它只会对某个 Segment 进行扩容，而不是对整个哈希表进行扩容。这样可以减少扩容时的锁竞争，提高并发性能。</p></li><li><p><strong>put 操作的实现</strong>：当执行 put 操作时，ConcurrentHashMap 首先根据 key 的哈希值确定要放入哪个 Segment 中，然后在该 Segment 上加锁，进行插入操作。这样可以保证在放入元素时，其他线程可以继续访问其他 Segment 中的元素，不会受到阻塞。</p></li><li><p><strong>get 操作的实现</strong>：与 put 操作类似，get 操作也会根据 key 的哈希值确定要查找哪个 Segment，然后在该 Segment 上加锁，进行查找操作。这样可以保证在查找元素时，其他线程可以继续访问其他 Segment 中的元素，不会受到阻塞。</p></li><li><p><strong>性能优化</strong>：ConcurrentHashMap 在实现上进行了多项性能优化，如使用了 volatile 修饰的数组元素来保证可见性，使用了 CAS（Compare and Swap）操作来保证原子性，以及使用了分段锁来减少锁竞争等。</p><h2 id="6-Callable"><a href="#6-Callable" class="headerlink" title="6.Callable"></a>6.Callable</h2></li></ol><p>优势（对比Runnable）</p><ol><li>有返回值</li><li>可以抛出异常</li><li>方法不同run/call</li></ol><blockquote><p> 使用：需要借助FutureTask适配类来放入线程中</p></blockquote><h2 id="7-常用的辅助类"><a href="#7-常用的辅助类" class="headerlink" title="7.常用的辅助类"></a>7.常用的辅助类</h2><h3 id="8-1-CountDownLatch-减法计数器"><a href="#8-1-CountDownLatch-减法计数器" class="headerlink" title="8.1 CountDownLatch-减法计数器"></a>8.1 CountDownLatch-减法计数器</h3><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException {</span><br><span class="line">        <span class="type">CountDownLatch</span> <span class="variable">countDownLatch</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CountDownLatch</span>(<span class="number">5</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) {</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt;{</span><br><span class="line">                System.out.println(Thread.currentThread().getName()+<span class="string">"离开"</span>);</span><br><span class="line">            countDownLatch.countDown();<span class="comment">//数量-1</span></span><br><span class="line">                 },String.valueOf(i)).start();</span><br><span class="line">        }</span><br><span class="line"></span><br><span class="line">        countDownLatch.await();</span><br><span class="line">        System.out.println(<span class="string">"结束！！！"</span>);</span><br><span class="line">        }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><p>原理：</p><p><strong>countDownLatch.countDown();</strong> 数量-1</p><p><strong>countDownLatch.await();</strong> 等待计数器归零，再向下执行。</p><h3 id="8-2-CyclicBarrier-加法计数器"><a href="#8-2-CyclicBarrier-加法计数器" class="headerlink" title="8.2 CyclicBarrier-加法计数器"></a>8.2 CyclicBarrier-加法计数器</h3><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException, BrokenBarrierException {</span><br><span class="line">        <span class="type">CyclicBarrier</span> <span class="variable">cyclicBarrier</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CyclicBarrier</span>(<span class="number">7</span>,()-&gt;{</span><br><span class="line">            System.out.println(<span class="string">"总数到七了"</span>);</span><br><span class="line">        });</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">7</span>; i++) {</span><br><span class="line">            <span class="type">int</span> <span class="variable">finalI</span> <span class="operator">=</span> i;</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Thread</span>(()-&gt;{</span><br><span class="line">                System.out.println(<span class="string">"现在共有"</span>+ finalI +<span class="string">"个线程"</span>);</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    cyclicBarrier.await();</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException | BrokenBarrierException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                }</span><br><span class="line">            },i+<span class="string">""</span>).start();</span><br><span class="line">        }</span><br><span class="line">        }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><h3 id="8-3-Semaphore信号量"><a href="#8-3-Semaphore信号量" class="headerlink" title="8.3 Semaphore信号量"></a>8.3 Semaphore信号量</h3><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException, BrokenBarrierException {</span><br><span class="line">        <span class="type">Semaphore</span> <span class="variable">semaphore</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Semaphore</span>(<span class="number">3</span>);<span class="comment">//线程数量</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">6</span>; i++) {</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; {</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    semaphore.acquire();<span class="comment">//获得</span></span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">"创建了"</span>);</span><br><span class="line">                    TimeUnit.SECONDS.sleep(<span class="number">2</span>);</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">"销毁了"</span>);</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                } <span class="keyword">finally</span> {</span><br><span class="line">                    semaphore.release();<span class="comment">//释放</span></span><br><span class="line">                }</span><br><span class="line">            }, String.valueOf(i)).start();</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><p> <strong>semaphore.acquire()</strong>;//获得，如果已经到了设置的上限，则会等待，到释放为止</p><p><strong>semaphore.release();//释放</strong> 将当前的信号量释放，然后唤醒等待的线程。</p><blockquote><p>作用：多个共享资源互斥的使用，并发限流，控制最大的线程数！</p></blockquote><h2 id="8-读写锁"><a href="#8-读写锁" class="headerlink" title="8. 读写锁"></a>8. 读写锁</h2><p>ReadWriteLock</p><p>读可以多个线程读，写只能有一个线程</p><h2 id="9-阻塞队列"><a href="#9-阻塞队列" class="headerlink" title="9.阻塞队列"></a>9.阻塞队列</h2><p>BlockingQueue:Collection包的子类</p><p>用处：</p><ol><li>多线程并发处理</li><li>线程池</li><li>使用： ArrayBlockingQueue blockingQueue = new ArrayBlockingQueue&lt;&gt;(3);</li></ol><p>四组APi</p><table><thead><tr><th>方式</th><th>抛出异常</th><th>有返回值，不抛出</th><th>阻塞等待</th><th>超时等待</th></tr></thead><tbody><tr><td>添加</td><td>Add</td><td>Offer()</td><td>put</td><td>offer(,,,)</td></tr><tr><td>移除</td><td>Remove</td><td>Poll</td><td>Take</td><td>Poll(,)</td></tr><tr><td>检测队首元素</td><td>Element</td><td>Peek</td><td>-</td><td>-</td></tr></tbody></table><blockquote><p>SynchoronousQueue 同步队列</p></blockquote><p>没有容量：进去一个元素必须取出来以后，才能放入其他元素。put take来使用</p><h2 id="10-线程池（三大方法，七大参数，四种拒绝策略）"><a href="#10-线程池（三大方法，七大参数，四种拒绝策略）" class="headerlink" title="10.线程池（三大方法，七大参数，四种拒绝策略）"></a>10.线程池（三大方法，七大参数，四种拒绝策略）</h2><blockquote><p>池化技术</p></blockquote><p>本质：优化资源的使用，实现准备好一些资源，要用的时候直接来拿，使用完再返回给池</p><p>好处</p><ol><li>降低资源的消耗</li><li>提高响应速度</li><li>方便管理</li><li>线程复用，可以控制最大并发的数量，管理线程</li></ol><h3 id="三大方法"><a href="#三大方法" class="headerlink" title="三大方法"></a>三大方法</h3><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line"><span class="comment">//    线程池三大方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line"><span class="comment">//        ExecutorService threadPool = Executors.newSingleThreadExecutor();//单例模式</span></span><br><span class="line"><span class="comment">//        ExecutorService threadPool = Executors.newFixedThreadPool(5);//创建指定数量的线程池</span></span><br><span class="line">        <span class="type">ExecutorService</span> <span class="variable">threadPool</span> <span class="operator">=</span> Executors.newCachedThreadPool();<span class="comment">//可伸缩的线程池</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> {</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) {</span><br><span class="line"><span class="comment">//                使用线程池来创建线程</span></span><br><span class="line">                threadPool.execute(()-&gt;{</span><br><span class="line">                    System.out.println(Thread.currentThread().getName()+<span class="string">"okkk"</span>);</span><br><span class="line">                });</span><br><span class="line">            }</span><br><span class="line">        } <span class="keyword">finally</span> {</span><br><span class="line">            threadPool.shutdown();</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><h3 id="七大参数"><a href="#七大参数" class="headerlink" title="七大参数"></a>七大参数</h3><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">ThreadPoolExecutor</span><span class="params">(<span class="type">int</span> corePoolSize,//核心线程数</span></span><br><span class="line"><span class="params">                          <span class="type">int</span> maximumPoolSize,//最大线程数</span></span><br><span class="line"><span class="params">                          <span class="type">long</span> keepAliveTime,//超时释放时间</span></span><br><span class="line"><span class="params">                          TimeUnit unit,//超时单位</span></span><br><span class="line"><span class="params">                          BlockingQueue&lt;Runnable&gt; workQueue//阻塞队列</span></span><br><span class="line"><span class="params">                          ThreadFactory threadFactory, // 线程工厂：创建线程的，一般不用动</span></span><br><span class="line"><span class="params">RejectedExecutionHandler handle // 拒绝策略</span></span><br><span class="line"><span class="params">                         )</span> {</span><br><span class="line"> </span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><blockquote><p>为什么阿里巴巴编程规范说，不要使用Executors创建线程池</p></blockquote><ol><li>FixedThreadPool和SingleThreadPool：最大线程数设置为Interger.MAX_VALUE.可能会堆积大量的请求，导致OOM内存溢出。</li></ol><blockquote><p>线程池不拒绝的最大线程数</p></blockquote><p>最大线程数+阻塞队列长度</p><h3 id="四种拒绝策略"><a href="#四种拒绝策略" class="headerlink" title="四种拒绝策略"></a>四种拒绝策略</h3><p>在 Java 中，线程池中的任务队列满了，无法继续接受新任务时，线程池会根据配置的拒绝策略来处理这种情况。常见的四种拒绝策略如下：</p><ol><li><p><strong>AbortPolicy（默认策略）</strong>：直接抛出 RejectedExecutionException 异常，表示拒绝执行新任务。</p></li><li><p><strong>CallerRunsPolicy</strong>：将任务交给调用线程来执行，如果线程池已满，新任务将在调用线程中执行。这样可以降低新任务的提交速度，避免任务堆积，但可能会影响调用线程的性能。</p></li><li><p><strong>DiscardPolicy</strong>：直接丢弃无法处理的任务，不做任何处理。这种策略可能会导致任务丢失，慎用。</p></li><li><p><strong>DiscardOldestPolicy</strong>：丢弃最早加入任务队列的任务，然后尝试再次提交新任务。这种策略可以保证新任务能够得到处理，但会丢失一部分任务。</p></li></ol><h3 id="如何去设置线程池的大小"><a href="#如何去设置线程池的大小" class="headerlink" title="如何去设置线程池的大小"></a>如何去设置线程池的大小</h3><p>判断当前系统属于IO密集型还是CPU密集型</p><ol><li><strong>CPU 密集型任务</strong>：对于 CPU 密集型任务，线程池的大小应该设置为接近或等于处理器核心数的数量。这样可以充分利用 CPU 资源，避免线程过多导致频繁的上下文切换，提高任务执行效率。过多的线程数量可能会导致竞争，降低性能。</li><li><strong>I/O 密集型任务</strong>：对于 I/O 密集型任务，线程池的大小通常可以设置得更大一些，因为线程在等待 I/O 操作完成时并不会占用 CPU 资源，可以允许更多的线程同时执行。一般建议将线程池大小设置为 2 * CPU 核心数 或者更大，以确保能够充分利用 CPU 和 I/O 资源。</li></ol><h2 id="11-四大函数式接口"><a href="#11-四大函数式接口" class="headerlink" title="11.四大函数式接口"></a>11.四大函数式接口</h2><blockquote><p>函数式接口：只有一个方法的接口</p></blockquote><h3 id="1-Function函数式接口-有输入，有返回"><a href="#1-Function函数式接口-有输入，有返回" class="headerlink" title="1. Function函数式接口:有输入，有返回"></a>1. Function函数式接口:有输入，有返回</h3><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Function&lt;String,String&gt; function = (str)-&gt;{<span class="keyword">return</span> str;};</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure><h3 id="2-断定型接口：只有一个输入参数，返回值必须是布尔值"><a href="#2-断定型接口：只有一个输入参数，返回值必须是布尔值" class="headerlink" title="2. 断定型接口：只有一个输入参数，返回值必须是布尔值"></a>2. 断定型接口：只有一个输入参数，返回值必须是布尔值</h3><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Predicate&lt;String&gt; predicate = (str)-&gt;{<span class="keyword">return</span> str.isEmpty(); };</span><br></pre></td></tr></tbody></table></figure><h3 id="3-消费型接口：有输入，有返回"><a href="#3-消费型接口：有输入，有返回" class="headerlink" title="3. 消费型接口：有输入，有返回"></a>3. 消费型接口：有输入，有返回</h3><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Consumer&lt;String&gt; consumer = (str)-&gt;{System.out.println(str);};</span><br></pre></td></tr></tbody></table></figure><h3 id="4-Supper：供给型接口-没有输入，只有返回"><a href="#4-Supper：供给型接口-没有输入，只有返回" class="headerlink" title="4.Supper：供给型接口:没有输入，只有返回"></a>4.Supper：供给型接口:没有输入，只有返回</h3><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">Supplier</span> <span class="variable">supplier</span> <span class="operator">=</span> ()-&gt;{ <span class="keyword">return</span> <span class="number">1024</span>; };</span><br></pre></td></tr></tbody></table></figure><h2 id="12-Stream流式运算"><a href="#12-Stream流式运算" class="headerlink" title="12.Stream流式运算"></a>12.Stream流式运算</h2><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Test</span> {</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line"><span class="type">User</span> <span class="variable">u1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="number">1</span>,<span class="string">"a"</span>,<span class="number">21</span>);</span><br><span class="line"><span class="type">User</span> <span class="variable">u2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="number">2</span>,<span class="string">"b"</span>,<span class="number">22</span>);</span><br><span class="line"><span class="type">User</span> <span class="variable">u3</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="number">3</span>,<span class="string">"c"</span>,<span class="number">23</span>);</span><br><span class="line"><span class="type">User</span> <span class="variable">u4</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="number">4</span>,<span class="string">"d"</span>,<span class="number">24</span>);</span><br><span class="line"><span class="type">User</span> <span class="variable">u5</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="number">6</span>,<span class="string">"e"</span>,<span class="number">25</span>);</span><br><span class="line"><span class="comment">// 集合就是存储</span></span><br><span class="line">List&lt;User&gt; list = Arrays.asList(u1, u2, u3, u4, u5);</span><br><span class="line"><span class="comment">// 计算交给Stream流</span></span><br><span class="line"><span class="comment">// lambda表达式、链式编程、函数式接口、Stream流式计算</span></span><br><span class="line">list.stream()</span><br><span class="line">.filter(u-&gt;{<span class="keyword">return</span> u.getId()%<span class="number">2</span>==<span class="number">0</span>;})</span><br><span class="line">.filter(u-&gt;{<span class="keyword">return</span> u.getAge()&gt;<span class="number">23</span>;})</span><br><span class="line">.map(u-&gt;{<span class="keyword">return</span> u.getName().toUpperCase();})</span><br><span class="line">.sorted((uu1,uu2)-&gt;{<span class="keyword">return</span> uu2.compareTo(uu1);})</span><br><span class="line">.limit(<span class="number">1</span>)</span><br><span class="line">.forEach(System.out::println);</span><br><span class="line">}</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><p><strong>运用到的就是四大函数式接口的概念</strong></p><h2 id="13-ForkJoin"><a href="#13-ForkJoin" class="headerlink" title="13.ForkJoin"></a>13.ForkJoin</h2><p>ForkJoin 是 Java 中用于并行计算的框架，主要用于处理分治任务。它基于工作窃取（work-stealing）算法，可以将一个大任务拆分成多个小任务，并且在多个线程之间动态地调度这些任务，以实现并行计算的效果。</p><p>ForkJoin 框架的核心是 ForkJoinPool、ForkJoinTask 和 RecursiveTask（或 RecursiveAction）：</p><ol><li><p><strong>ForkJoinPool</strong>：ForkJoinPool 是一个特殊的线程池，它使用工作窃取算法来提高并行任务的执行效率。ForkJoinPool 中的线程通过工作队列来存放任务，每个线程都有自己的工作队列，当自己的队列中没有任务时，会去其他线程的队列中窃取任务执行。</p></li><li><p><strong>ForkJoinTask</strong>：ForkJoinTask 是一个抽象类，用于表示可以并行执行的任务。它有两个重要的子类：RecursiveTask 和 RecursiveAction。RecursiveTask 用于有返回值的任务，而 RecursiveAction 用于没有返回值的任务。</p></li><li><p><strong>RecursiveTask</strong>：RecursiveTask 是一个抽象类，表示可以返回结果的任务。当一个任务需要拆分成多个子任务进行并行计算，并且最终需要合并子任务的结果时，可以使用 RecursiveTask。</p></li><li><p><strong>RecursiveAction</strong>：RecursiveAction 是一个抽象类，表示不需要返回结果的任务。当一个任务只需要执行某些操作，而不需要返回结果时，可以使用 RecursiveAction。</p></li></ol><p>ForkJoin 框架适用于需要并行处理的分治任务，可以有效地利用多核处理器的优势，提高程序的性能。在使用 ForkJoin 框架时，需要注意合理设计任务的拆分和合并策略，以及避免出现死锁等并发问题。</p><h2 id="14-异步调用"><a href="#14-异步调用" class="headerlink" title="14.异步调用"></a>14.异步调用</h2><p>异步调用是一种编程模型，用于处理需要等待较长时间的操作，例如网络请求、文件读写、数据库查询等。在传统的同步调用中，调用者会等待操作完成后才能继续执行后续代码，而在异步调用中，调用者可以继续执行后续代码，而不必等待操作完成。</p><p>在异步调用中，通常会通过回调函数、Future/Promise 对象或者异步方法来处理操作完成后的结果。这样可以提高程序的响应性和并发性，避免阻塞主线程。异步调用常用于需要处理大量并发请求或者需要等待外部资源响应的场景，能够更有效地利用系统资源。</p><p>在 Java 中，可以使用 CompletableFuture、FutureTask、Callback 等机制来实现异步调用。在 JavaScript 中，可以使用 Promise、async/await 等机制来实现异步调用。异步调用在现代编程中非常常见，能够提高程序的性能和用户体验。</p><h2 id="15-JMM"><a href="#15-JMM" class="headerlink" title="15.JMM"></a>15.JMM</h2><blockquote><p>什么是JMM</p></blockquote><p>JMM 是 Java 内存模型（Java Memory Model）的缩写，是 Java 虚拟机规范中定义的一种抽象的计算机内存模型，用于屏蔽不同硬件和操作系统的内存访问差异，确保在不同平台上对 Java 程序的并发访问都能得到一致的结果。</p><p>Java 内存模型规定了所有线程之间共享的主内存以及每个线程私有的工作内存之间的关系，主要包括以下几个方面的内容：</p><ol><li><p><strong>原子性（Atomicity）</strong>：对基本数据类型的读取和赋值操作具有原子性，即这些操作不会被中断。</p></li><li><p><strong>可见性（Visibility）</strong>：一个线程修改的变量对其他线程是可见的，即一个线程修改的共享变量的值在其他线程中是可以看到的。</p></li><li><p><strong>有序性（Ordering）</strong>：在单个线程内，程序的执行顺序按照代码的顺序执行。在多线程情况下，由于指令重排序的存在，可能会导致不同线程观察到的执行顺序不同。Java 内存模型通过 happens-before 原则来规定多线程之间操作的执行顺序。</p></li></ol><p><strong>关于JMM的一些同步约定</strong></p><ol><li>线程解锁前，必须将共享变量立刻刷新回主存。</li><li>进程加锁前，必须读取主存的最新值到工作内存中</li><li>加锁解锁都必须是同一把锁。</li></ol><h2 id="16-Volatile"><a href="#16-Volatile" class="headerlink" title="16. Volatile"></a>16. Volatile</h2><ol><li><strong>可见性</strong>：被 <code>volatile</code> 修饰的变量在一个线程中被修改后，会立即更新到主内存中，其他线程读取该变量时会直接从主内存中获取最新值，保证了变量的可见性。</li><li><strong>禁止重排序</strong>：<code>volatile</code> 修饰的变量禁止了指令重排序，保证了变量的修改顺序与代码中的顺序一致。</li><li><strong>不保证原子性</strong>：<code>volatile</code> 修饰的变量对于单次读/写操作是原子的，但是对于多次操作（比如 i++）并不能保证原子性，如果需要保证原子性，可以使用 <code>synchronized</code> 关键字或者 <code>java.util.concurrent.atomic</code> 包中的原子类</li></ol><blockquote><p>如果不使用lock和synchronized，如何保证原子性？</p></blockquote><p>使用原子类 Atomic，底层直接根操作系统挂钩，在内存中修改值。</p><blockquote><p>指令重排</p></blockquote><p>代处理器为了提高执行效率而进行的一种优化手段。处理器在执行指令时，并不一定会按照代码中的顺序依次执行，而是可能会重新排列指令的执行顺序，以尽量减少指令之间的相关性，提高并行度和性能。</p><blockquote><p>volatile如何避免指令重排</p></blockquote><p><code>volatile</code>关键字实现禁止指令重排的机制是通过在编译器层面和运行时层面进行限制的。在编译器层面，编译器会禁止对<code>volatile</code>变量进行重排序优化，保证在执行顺序上不会将后面的操作排在前面。在运行时层面，JVM会在每次对<code>volatile</code>变量进行读写操作时，插入内存屏障指令（Memory Barrier），确保变量的写操作先于后续的读操作，从而保证了操作的顺序性。</p><h2 id="17-单例模式"><a href="#17-单例模式" class="headerlink" title="17.单例模式"></a>17.单例模式</h2><h3 id="饿汉式"><a href="#饿汉式" class="headerlink" title="饿汉式"></a>饿汉式</h3><p>直接创建对象，会浪费内存空间</p><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Test1</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">Test1</span> <span class="variable">instance</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Test1</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">Test1</span><span class="params">()</span> {}</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Test1 <span class="title function_">getInstance</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    }</span><br><span class="line">    </span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><h3 id="懒汉式"><a href="#懒汉式" class="headerlink" title="懒汉式"></a>懒汉式</h3><p>在使用时才会创建，会出现并发问题</p><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Singleton</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> Singleton instance;<span class="comment">//使用volatile，保证可见性</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">Singleton</span><span class="params">()</span> {</span><br><span class="line">        <span class="comment">// 私有化构造函数，避免外部实例化</span></span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title function_">getInstance</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">if</span> (instance == <span class="literal">null</span>) {</span><br><span class="line">            <span class="keyword">synchronized</span> (Singleton.class) {</span><br><span class="line">                <span class="keyword">if</span> (instance == <span class="literal">null</span>) {</span><br><span class="line">                    instance = <span class="keyword">new</span> <span class="title class_">Singleton</span>();</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">doSomething</span><span class="params">()</span> {</span><br><span class="line">        System.out.println(<span class="string">"Singleton is doing something."</span>);</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure><blockquote><p>反射可以破坏单例模式</p></blockquote><p><strong>最好的单例模式，是枚举，无法被破坏</strong></p><h2 id="18-CAS相关"><a href="#18-CAS相关" class="headerlink" title="18.CAS相关"></a>18.CAS相关</h2><blockquote><p>什么是CAS</p></blockquote><p>CAS（Compare and Swap，比较并交换）是一种用于实现多线程同步的原子操作。在CAS操作中，系统会先比较内存中某个位置的值与预期值是否相等，如果相等，则将该位置的值更新为新的值；如果不相等，则操作失败。整个操作是原子的，即在同一时刻只有一个线程能够成功执行该操作。</p><p>CAS操作通常用于实现非阻塞算法和并发数据结构，例如Java中的<code>AtomicInteger</code>、<code>AtomicLong</code>等类就是基于CAS操作实现的。CAS操作的优势在于它避免了传统的加锁操作所带来的性能损耗，因为它是基于硬件级别的原子操作，不需要像锁一样在用户态和内核态之间切换。</p><p>在Java中，CAS操作由<code>java.util.concurrent.atomic</code>包提供支持，通常使用<code>compareAndSet()</code>方法来实现。例如：</p><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">AtomicInteger</span> <span class="variable">atomicInteger</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AtomicInteger</span>(<span class="number">0</span>);</span><br><span class="line"><span class="type">int</span> <span class="variable">expectedValue</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> <span class="variable">newValue</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"><span class="keyword">while</span> (!atomicInteger.compareAndSet(expectedValue, newValue)) {</span><br><span class="line">    expectedValue = atomicInteger.get();</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><p>上面的代码中，<code>compareAndSet()</code>方法会比较<code>atomicInteger</code>的当前值是否等于<code>expectedValue</code>，如果相等，则将其更新为<code>newValue</code>，否则操作失败。通过循环不断尝试，直到操作成功为止。</p><blockquote><p>unsafe类</p></blockquote><p>Java是无法直接操作内存的，c++可以，CAS就是通过直接操作内存来实现的。</p><p>CAS ： 比较当前工作内存中的值和主内存中的值，如果这个值是期望的，那么则执行操作！如果不是就 一直循环！ </p><p>缺点</p><p> 1、 循环会耗时</p><p> 2、一次性只能保证一个共享变量的原子性 </p><p>3、ABA问题</p><h3 id="ABA问题（狸猫换太子）"><a href="#ABA问题（狸猫换太子）" class="headerlink" title="ABA问题（狸猫换太子）"></a>ABA问题（狸猫换太子）</h3><p>一个线程读取了值之后，另外一个线程修改了这个值两次，最终改为了原来的值，第一个线程发现值没有变化，则不会发现其他线程修改过。</p><h2 id="19-原子引用"><a href="#19-原子引用" class="headerlink" title="19.原子引用"></a>19.原子引用</h2><blockquote><p>解决ABA问题，引入原子引用，对应的是乐观锁思想</p></blockquote><p>解决ABA问题的常见方法是使用带有版本号的原子引用，例如<code>AtomicStampedReference</code>类。这个类在比较时除了比较值外，还会比较一个版本号（stamp），只有当值和版本号都匹配时才进行修改。这样一来，即使值在过程中发生了变化，只要版本号不同，就能够避免误判。</p><h2 id="20-各种锁的理解"><a href="#20-各种锁的理解" class="headerlink" title="20.各种锁的理解"></a>20.各种锁的理解</h2><h3 id="1-公平锁，非公平锁"><a href="#1-公平锁，非公平锁" class="headerlink" title="1.公平锁，非公平锁"></a>1.公平锁，非公平锁</h3><p>公平锁：先来后到。</p><p>非公平锁：可以插队（默认）</p><h3 id="2-可重入锁"><a href="#2-可重入锁" class="headerlink" title="2. 可重入锁"></a>2. 可重入锁</h3><p>可重入锁（Reentrant Lock）是一种支持在同一个线程中多次获取同一把锁的锁机制。这意味着线程可以在持有锁的情况下再次获取该锁，而不会造成死锁。可重入锁是一种比较常见的锁实现方式，在Java中通过<code>synchronized</code>关键字和<code>ReentrantLock</code>类来实现。</p><p>在使用可重入锁时，同一个线程可以多次获取锁而不会被阻塞。线程每次成功获取锁后，需要在最后释放相同次数的锁。这样可以确保锁的所有权不会被其他线程所获取，从而避免了死锁的发生。</p><p>可重入锁的一个典型应用场景是递归函数。如果一个函数是递归的，并且这个函数需要获取锁来访问临界区资源，那么使用可重入锁可以确保同一线程在递归调用过程中可以多次获取锁而不会产生死锁。</p><p>总的来说，可重入锁是一种非常实用的锁机制，能够简化并发编程中的锁管理，并提高程序的可靠性和性能。</p><h3 id="3-自旋锁（底层是CAS）"><a href="#3-自旋锁（底层是CAS）" class="headerlink" title="3.自旋锁（底层是CAS）"></a>3.自旋锁（底层是CAS）</h3><p>自旋锁是一种基于忙等待的锁机制，在尝试获取锁时，线程不会立即进入阻塞状态，而是循环检测锁的状态，直到获取到锁为止。自旋锁适用于锁被持有时间较短的情况，因为长时间的自旋会消耗CPU资源。</p><p>在Java中，<code>AtomicInteger</code>等原子类的<code>compareAndSet()</code>方法就是使用自旋锁实现的。当多个线程同时尝试修改同一个<code>AtomicInteger</code>的值时，如果没有竞争，<code>compareAndSet()</code>会立即成功；如果有竞争，线程会不断尝试，直到成功为止。</p><p>自旋锁的优点是避免了线程切换的开销，缺点是会消耗CPU资源，并且在高并发情况下可能会导致性能下降。因此，使用自旋锁时需要根据实际情况权衡。</p><h3 id="4-死锁"><a href="#4-死锁" class="headerlink" title="4.死锁"></a>4.死锁</h3><p>死锁是指两个或多个线程（或进程）在执行过程中，因争夺资源而造成的一种互相等待的现象，导致它们都无法继续执行下去。简单来说，就是多个线程在互相等待对方释放资源，导致它们都无法继续执行下去。</p><p>死锁发生的必要条件包括：</p><ol><li>互斥条件（资源只能被一个线程占用）、</li><li>请求与保持条件（一个线程持有了至少一个资源，并且正在请求另一个资源，但该资源被其他线程持有）、</li><li>不剥夺条件（线程不能强制占有其他线程的资源，只能自己释放）</li><li>循环等待条件（若干线程之间形成一种头尾相接的循环等待资源关系）。</li></ol><p>死锁在多线程编程中是一个比较常见的问题，因为线程在获取资源时可能会发生竞争，如果不合理地使用锁或资源分配策略，就容易造成死锁。预防死锁的方法包括使用适当的锁策略、避免循环等待、按照固定的顺序获取锁等。</p><blockquote><p>如何排查死锁</p></blockquote><ol><li>使用jps -l定位进程号</li><li>使用jstack进程号，找死锁问题</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/posts/2024/juc-notes-cover.png&quot; alt=&quot;JUC并发编程知识点封面图&quot;&gt;&lt;/p&gt;
&lt;h1 id=&quot;JUC并发编程&quot;&gt;&lt;a href=&quot;#JUC并发编程&quot; class=&quot;headerlink&quot; title=&quot;</summary>
      
    
    
    
    <category term="Java基础" scheme="https://swagch.github.io/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="并发编程" scheme="https://swagch.github.io/tags/%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>从零开始学习设计模式-第一篇</title>
    <link href="https://swagch.github.io/2024/04/24/%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AD%A6%E4%B9%A0%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%AC%AC%E4%B8%80%E7%AF%87/"/>
    <id>https://swagch.github.io/2024/04/24/%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AD%A6%E4%B9%A0%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%AC%AC%E4%B8%80%E7%AF%87/</id>
    <published>2024-04-24T06:32:01.000Z</published>
    <updated>2024-04-29T16:21:50.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/images/posts/2024/design-patterns-cover.png" alt="设计模式知识梳理封面图"></p><h1 id="设计模式"><a href="#设计模式" class="headerlink" title="设计模式"></a>设计模式</h1><blockquote><p>写在前面：</p></blockquote><p>找工作下来感觉自己基础知识确实很不牢靠，成了无情的接口调用工具人，所以恶补一下相关的知识。</p><h2 id="1-单例模式"><a href="#1-单例模式" class="headerlink" title="1. 单例模式"></a>1. 单例模式</h2><p>概念就是<strong>一个类负责创建自己的对象，同时确保只有单个对象被创建</strong>。</p><h3 id="1-1分类"><a href="#1-1分类" class="headerlink" title="1.1分类"></a>1.1分类</h3><h4 id="恶汉：加载就会导致该实例对象被创建"><a href="#恶汉：加载就会导致该实例对象被创建" class="headerlink" title="恶汉：加载就会导致该实例对象被创建"></a>恶汉：加载就会导致该实例对象被创建</h4><ol><li>创建方式<ol><li>静态变量方式 直接new Singleton（）；</li><li>静态代码块的方式 写一个 static{}</li></ol></li></ol><h4 id="懒汉：首次使用该对象时才会创建对象"><a href="#懒汉：首次使用该对象时才会创建对象" class="headerlink" title="懒汉：首次使用该对象时才会创建对象"></a>懒汉：首次使用该对象时才会创建对象</h4><blockquote><p>懒汉式会出现线程安全问题，需要优化判断条件</p></blockquote><p>可以通过对外提供静态方法的方式来获取，但是效率特别低。一般使用双重检查锁的方式进行处理，但是JVM可能会出现优化和指令重排的问题，所以可以使用volatile关键字保证代码的有序性。</p><figure class="highlight java"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Singleton</span>{</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">private</span> <span class="title function_">Singleton</span><span class="params">()</span>{}</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> Singleton instance;</span><br><span class="line">  </span><br><span class="line">  <span class="comment">//对外提供静态方法获取该对象</span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title function_">getInstance</span><span class="params">()</span>{</span><br><span class="line">      <span class="keyword">if</span>(instance == <span class="literal">null</span>){</span><br><span class="line">          <span class="keyword">synchronized</span>(Singleton.class){</span><br><span class="line">            <span class="keyword">if</span>(instance == <span class="literal">null</span>){</span><br><span class="line">              instance = <span class="keyword">new</span> <span class="title class_">Singleton</span>();</span><br><span class="line">            }</span><br><span class="line">          }</span><br><span class="line">        }</span><br><span class="line">      <span class="keyword">return</span> instance;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><h3 id="1-2-破解单例"><a href="#1-2-破解单例" class="headerlink" title="1.2 破解单例"></a>1.2 破解单例</h3><blockquote><p>可以通过序列化和反射的方式破解单例模式</p></blockquote><p><strong>解决办法</strong></p><ol><li>序列化：添加readResolve()方法，返回SingletonHolder.INSTANCE;</li><li>反射：在构造方法中，增加判断，如果私有化对象不为空，直接抛出异常</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;img src=&quot;/images/posts/2024/design-patterns-cover.png&quot; alt=&quot;设计模式知识梳理封面图&quot;&gt;&lt;/p&gt;
&lt;h1 id=&quot;设计模式&quot;&gt;&lt;a href=&quot;#设计模式&quot; class=&quot;headerlink&quot; title=&quot;设计</summary>
      
    
    
    
    <category term="Java基础" scheme="https://swagch.github.io/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
    <category term="设计模式" scheme="https://swagch.github.io/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>服务器磁盘空间与IO异常定位</title>
    <link href="https://swagch.github.io/2023/11/26/%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%A3%81%E7%9B%98%E7%A9%BA%E9%97%B4%E4%B8%8EIO%E5%BC%82%E5%B8%B8%E5%AE%9A%E4%BD%8D/"/>
    <id>https://swagch.github.io/2023/11/26/%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%A3%81%E7%9B%98%E7%A9%BA%E9%97%B4%E4%B8%8EIO%E5%BC%82%E5%B8%B8%E5%AE%9A%E4%BD%8D/</id>
    <published>2023-11-26T05:50:00.000Z</published>
    <updated>2023-11-27T14:05:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>写在前面：磁盘问题不只有“空间满了”。容量还有很多但文件创建失败，可能是 inode 用完；接口突然变慢但 CPU 不高，可能是 IO 等待；文件已经删除但空间不释放，可能是进程仍持有句柄；文件系统变成只读，则可能涉及底层错误。</p></blockquote><p>这篇整理成一份值班口袋手册。先分级和保留证据，再进入容量、inode、删除文件、IO 延迟或只读文件系统分支。涉及删除、重启、数据库和文件系统修复的操作必须由对应负责人授权。</p><h2 id="先按风险分级"><a href="#先按风险分级" class="headerlink" title="先按风险分级"></a>先按风险分级</h2><table><thead><tr><th>等级</th><th>典型现象</th><th>首要目标</th></tr></thead><tbody><tr><td>P0</td><td>文件系统只读、数据库写失败、核心服务不可用</td><td>隔离风险，保护数据</td></tr><tr><td>P1</td><td>使用率超过 95%、inode 耗尽、IO 延迟持续影响业务</td><td>停止增长，恢复余量</td></tr><tr><td>P2</td><td>使用率或延迟持续上升，预计 24 小时内越线</td><td>定位来源，计划处置</td></tr><tr><td>P3</td><td>单次波动，无业务影响</td><td>记录基线，观察趋势</td></tr></tbody></table><p>同样 90% 使用率，20G 根分区和 10TB 数据盘的剩余时间完全不同。分级要结合增长速度、剩余容量和业务用途，不只看百分比。</p><h2 id="前五分钟只做只读检查"><a href="#前五分钟只做只读检查" class="headerlink" title="前五分钟只做只读检查"></a>前五分钟只做只读检查</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">date</span> -Is</span><br><span class="line">hostname</span><br><span class="line"><span class="built_in">df</span> -hT</span><br><span class="line"><span class="built_in">df</span> -ih</span><br><span class="line">findmnt</span><br><span class="line">lsblk -f</span><br><span class="line">vmstat 1 5</span><br><span class="line">iostat -xz 1 5</span><br></pre></td></tr></table></figure><p>这组命令回答：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">哪个挂载点容量用尽</span><br><span class="line">哪个挂载点 inode 用尽</span><br><span class="line">设备与挂载点如何对应</span><br><span class="line">文件系统类型和挂载选项</span><br><span class="line">是否存在 IO 等待</span><br><span class="line">哪个块设备延迟异常</span><br></pre></td></tr></table></figure><p>先记录时间和输出，再做清理。恢复后才能与同一组基线比较。</p><p><code>iostat</code> 需要 <code>sysstat</code> 工具包，缺失时不要在故障现场临时安装未知软件；使用已有监控替代。</p><h2 id="分支-A：容量使用率高"><a href="#分支-A：容量使用率高" class="headerlink" title="分支 A：容量使用率高"></a>分支 A：容量使用率高</h2><p>从告警挂载点逐层向下，<code>-x</code> 避免跨文件系统：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">du</span> -xhd1 /var 2&gt;/dev/null | <span class="built_in">sort</span> -h</span><br><span class="line"><span class="built_in">du</span> -xhd1 /var/log 2&gt;/dev/null | <span class="built_in">sort</span> -h</span><br></pre></td></tr></table></figure><p>不要从根目录无差别执行高开销扫描。大目录 <code>du</code> 会产生大量 IO，业务已经抖动时应缩小路径、降低执行频率，必要时参考文件系统或监控平台已有统计。</p><p>定位大文件可以在明确目录中执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">find /var/log -xdev -<span class="built_in">type</span> f -size +500M -<span class="built_in">printf</span> <span class="string">&#x27;%s %p\n&#x27;</span> 2&gt;/dev/null | <span class="built_in">sort</span> -n</span><br></pre></td></tr></table></figure><p><code>find</code> 同样需要遍历目录。命中后先确认文件归属、是否正在写、保留要求和是否已采集，不能看到大文件就删除。</p><h2 id="分支-B：df-很高，du-却很小"><a href="#分支-B：df-很高，du-却很小" class="headerlink" title="分支 B：df 很高，du 却很小"></a>分支 B：df 很高，du 却很小</h2><p><code>df</code> 统计文件系统已占用块，<code>du</code> 遍历仍有目录项的文件。差值很大时检查已删除但仍被进程持有的文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lsof +L1</span><br></pre></td></tr></table></figure><p>典型输出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java  1842  app  17w  REG  253,1  18G  /var/log/order/app.log (deleted)</span><br></pre></td></tr></table></figure><p>处理优先级：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">让应用日志框架滚动并重开</span><br><span class="line">使用服务支持的平滑重载</span><br><span class="line">摘流后正常重启单个实例</span><br><span class="line">最后才评估是否操作具体文件描述符</span><br></pre></td></tr></table></figure><p>直接终止进程或截断描述符都有业务风险。执行前确认文件类型、进程职责、事务和剩余实例容量，由应用负责人操作。</p><p>空间释放后再次运行 <code>df</code> 和 <code>lsof +L1</code>，证明句柄已经消失，而不是只看命令返回成功。</p><h2 id="分支-C：inode-用尽"><a href="#分支-C：inode-用尽" class="headerlink" title="分支 C：inode 用尽"></a>分支 C：inode 用尽</h2><p><code>df -h</code> 可能还有大量空间，<code>df -i</code> 却达到 100%。一个空文件也需要 inode，海量小日志、缓存或临时文件会导致无法创建新文件。</p><p>先从已知高风险路径估算文件数量：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">find /var/tmp -xdev -<span class="built_in">type</span> f -<span class="built_in">printf</span> <span class="string">&#x27;%h\n&#x27;</span> 2&gt;/dev/null | <span class="built_in">sort</span> | <span class="built_in">uniq</span> -c | <span class="built_in">sort</span> -n | <span class="built_in">tail</span></span><br></pre></td></tr></table></figure><p>在百万级目录执行 <code>find</code> 可能很慢，应先结合应用配置、目录更新时间和监控缩小范围。</p><p>常见来源：</p><ul><li>每次请求创建独立临时文件但不清理。</li><li>日志按请求或任务拆成小文件。</li><li>Session、缓存落盘无过期。</li><li>邮件队列、爬虫或批处理产生大量碎片。</li><li>容器存储层残留。</li></ul><p>处理时优先暂停继续产生文件的任务，再按应用定义的生命周期清理。只删当前文件、不修复生产逻辑，inode 很快再次耗尽。</p><h2 id="分支-D：空间正常但-IO-延迟高"><a href="#分支-D：空间正常但-IO-延迟高" class="headerlink" title="分支 D：空间正常但 IO 延迟高"></a>分支 D：空间正常但 IO 延迟高</h2><p><code>iostat -xz</code> 常看：</p><table><thead><tr><th>指标</th><th>含义</th><th>注意</th></tr></thead><tbody><tr><td><code>r/s w/s</code></td><td>每秒读写请求</td><td>与业务基线比较</td></tr><tr><td><code>rkB/s wkB/s</code></td><td>吞吐</td><td>高吞吐不一定高延迟</td></tr><tr><td><code>await</code></td><td>请求平均等待时间</td><td>读写混合值可能掩盖差异</td></tr><tr><td><code>aqu-sz</code></td><td>平均队列长度</td><td>持续增长表示积压</td></tr><tr><td><code>%util</code></td><td>设备忙碌程度</td><td>SSD、RAID、云盘不能单独解读</td></tr></tbody></table><p>不要使用“<code>%util=100%</code> 就一定是磁盘极限”的单一规则。并行设备和虚拟块存储的含义不同，延迟、队列、吞吐和云盘额度要一起看。</p><p>定位进程：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">pidstat -d 1 5</span><br><span class="line">iotop -oPa</span><br></pre></td></tr></table></figure><p><code>iotop</code> 需要权限并可能未安装。使用前确认生产诊断规范。</p><p>把高 IO 进程对应到行为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Java：日志暴增、Heap Dump、文件导出</span><br><span class="line">MySQL：慢查询、刷脏页、备份、索引构建</span><br><span class="line">容器运行时：镜像拉取、日志、层清理</span><br><span class="line">系统任务：压缩、备份、病毒扫描</span><br><span class="line">未知进程：先确认来源，不直接终止</span><br></pre></td></tr></table></figure><p>还要看 <code>vmstat</code> 的 <code>si/so</code>。内存不足导致 swap 时，磁盘忙只是第二现场。</p><h2 id="分支-E：文件系统变成只读"><a href="#分支-E：文件系统变成只读" class="headerlink" title="分支 E：文件系统变成只读"></a>分支 E：文件系统变成只读</h2><p>应用日志可能出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Read-only file system</span><br><span class="line">Input/output error</span><br><span class="line">EXT4-fs error</span><br><span class="line">XFS metadata I/O error</span><br></pre></td></tr></table></figure><p>先保存：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">findmnt -o TARGET,SOURCE,FSTYPE,OPTIONS</span><br><span class="line">journalctl -k --since <span class="string">&#x27;30 minutes ago&#x27;</span></span><br><span class="line">dmesg -T | <span class="built_in">tail</span> -200</span><br></pre></td></tr></table></figure><p>这可能是文件系统为了保护数据在检测到错误后切换只读，也可能是挂载配置本来如此。不要直接 <code>mount -o remount,rw</code> 掩盖问题，更不能在已挂载生产文件系统上随意执行 <code>fsck</code>。</p><p>正确动作通常需要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">停止或迁移写入流量</span><br><span class="line">保存内核与存储事件</span><br><span class="line">联系系统和存储负责人</span><br><span class="line">确认备份与副本</span><br><span class="line">在维护窗口离线检查或修复</span><br><span class="line">恢复后做数据一致性验证</span><br></pre></td></tr></table></figure><p>P0 场景优先保护数据，不能为了让应用尽快写入而跳过文件系统判断。</p><h2 id="常见占用来源与正确负责人"><a href="#常见占用来源与正确负责人" class="headerlink" title="常见占用来源与正确负责人"></a>常见占用来源与正确负责人</h2><h3 id="应用日志"><a href="#应用日志" class="headerlink" title="应用日志"></a>应用日志</h3><p>由应用或统一日志策略滚动，设置单文件大小、保留天数和总量上限。确认采集成功后再删除归档。不能同时让 Logback 和 <code>logrotate</code> 无协调地管理同一文件。</p><h3 id="Docker-或容器数据"><a href="#Docker-或容器数据" class="headerlink" title="Docker 或容器数据"></a>Docker 或容器数据</h3><p>先区分容器日志、镜像、可写层和 Volume。清理由平台负责人通过容器运行时机制执行，不能手工删除 <code>/var/lib/docker</code> 下的文件。</p><h3 id="MySQL-Binlog、Redo、临时文件"><a href="#MySQL-Binlog、Redo、临时文件" class="headerlink" title="MySQL Binlog、Redo、临时文件"></a>MySQL Binlog、Redo、临时文件</h3><p>由 DBA 使用数据库命令、保留策略和复制状态处理。手工删除数据库目录中的 binlog 或表文件可能破坏恢复与复制。</p><h3 id="Heap-Dump-与-Core-Dump"><a href="#Heap-Dump-与-Core-Dump" class="headerlink" title="Heap Dump 与 Core Dump"></a>Heap Dump 与 Core Dump</h3><p>确认是否已安全转存并仍有排障价值，再按事件保留策略清理。Dump 可能包含敏感数据，不传到个人存储。</p><h3 id="临时文件"><a href="#临时文件" class="headerlink" title="临时文件"></a>临时文件</h3><p>确认创建进程、有效期和是否仍打开。修复清理任务与命名规则，避免只做一次人工删除。</p><h2 id="操作授权矩阵"><a href="#操作授权矩阵" class="headerlink" title="操作授权矩阵"></a>操作授权矩阵</h2><table><thead><tr><th>动作</th><th>风险</th><th>执行要求</th></tr></thead><tbody><tr><td><code>df</code>、<code>findmnt</code>、受控 <code>du</code></td><td>低到中</td><td>值班人员，注意 IO 开销</td></tr><tr><td>暂停日志或批任务</td><td>中</td><td>应用负责人确认影响</td></tr><tr><td>日志滚动、单实例摘流重启</td><td>中</td><td>发布&#x2F;值班流程与容量确认</td></tr><tr><td>数据库日志清理</td><td>高</td><td>DBA 审核复制与恢复策略</td></tr><tr><td>容器存储清理</td><td>高</td><td>平台负责人使用正式工具</td></tr><tr><td>文件系统修复、重新挂载</td><td>很高</td><td>系统&#x2F;存储负责人，维护窗口</td></tr><tr><td>盲删未知文件、底层数据库文件</td><td>不可接受</td><td>禁止</td></tr></tbody></table><p>高风险命令不应该出现在“通用一键清理脚本”里。脚本不知道当前目录属于数据库、容器、正在运行的任务，还是唯一一份故障证据。</p><h2 id="恢复验证"><a href="#恢复验证" class="headerlink" title="恢复验证"></a>恢复验证</h2><p>释放空间或降低 IO 后，至少验证：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">[ ] df 容量与 inode 回到安全线</span><br><span class="line">[ ] 增长来源已经停止或受控</span><br><span class="line">[ ] lsof 不再存在异常 deleted 大文件</span><br><span class="line">[ ] iostat 的 await 和队列回到基线</span><br><span class="line">[ ] 文件系统保持预期读写状态</span><br><span class="line">[ ] 应用日志、临时文件和数据库可以正常写</span><br><span class="line">[ ] 核心接口 P95/P99 与错误率恢复</span><br><span class="line">[ ] 消息、任务和复制积压开始下降</span><br><span class="line">[ ] 清理没有破坏审计、备份和回退能力</span><br></pre></td></tr></table></figure><p>业务恢复后继续观察一个完整高峰或任务周期。磁盘指标下降不代表上层数据已经自动补齐。</p><h2 id="预防比应急清理更便宜"><a href="#预防比应急清理更便宜" class="headerlink" title="预防比应急清理更便宜"></a>预防比应急清理更便宜</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">容量：使用率 + 剩余 GiB + 预计耗尽时间</span><br><span class="line">inode：使用率与文件创建速率</span><br><span class="line">IO：await、队列、吞吐和云盘额度</span><br><span class="line">日志：写入速率、滚动失败、采集延迟</span><br><span class="line">容器：镜像、日志和 Volume 配额</span><br><span class="line">数据库：binlog 保留、备份和临时空间</span><br><span class="line">Dump：独立目录、容量上限和保留流程</span><br></pre></td></tr></table></figure><p>告警应在还有选择时触发。剩余 15% 但预计两小时耗尽，比使用率 95% 且三个月不增长更紧急。</p><p>这份口袋手册的核心只有三句话：先确认是空间、inode、IO 还是文件系统错误；先阻止继续增长，再由资源所有者释放；恢复后同时验证底层指标和业务结果。磁盘故障真正危险的不是命令不会用，而是在压力下执行了一个无法撤销的错误动作。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;写在前面：磁盘问题不只有“空间满了”。容量还有很多但文件创建失败，可能是 inode 用完；接口突然变慢但 CPU 不高，可能是 IO 等待；文件已经删除但空间不释放，可能是进程仍持有句柄；文件系统变成只读，则可能涉及底层错误。&lt;/p&gt;
&lt;/blo</summary>
      
    
    
    
    <category term="故障排查" scheme="https://swagch.github.io/categories/%E6%95%85%E9%9A%9C%E6%8E%92%E6%9F%A5/"/>
    
    
    <category term="Linux" scheme="https://swagch.github.io/tags/Linux/"/>
    
    <category term="故障排查" scheme="https://swagch.github.io/tags/%E6%95%85%E9%9A%9C%E6%8E%92%E6%9F%A5/"/>
    
    <category term="磁盘IO" scheme="https://swagch.github.io/tags/%E7%A3%81%E7%9B%98IO/"/>
    
    <category term="服务器运维" scheme="https://swagch.github.io/tags/%E6%9C%8D%E5%8A%A1%E5%99%A8%E8%BF%90%E7%BB%B4/"/>
    
  </entry>
  
  <entry>
    <title>服务器内存不足与OOM排查</title>
    <link href="https://swagch.github.io/2023/09/10/%E6%9C%8D%E5%8A%A1%E5%99%A8%E5%86%85%E5%AD%98%E4%B8%8D%E8%B6%B3%E4%B8%8EOOM%E6%8E%92%E6%9F%A5/"/>
    <id>https://swagch.github.io/2023/09/10/%E6%9C%8D%E5%8A%A1%E5%99%A8%E5%86%85%E5%AD%98%E4%B8%8D%E8%B6%B3%E4%B8%8EOOM%E6%8E%92%E6%9F%A5/</id>
    <published>2023-09-10T08:40:00.000Z</published>
    <updated>2023-09-11T13:25:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>写在前面：服务器内存“看起来快满了”和真正发生 OOM 不是一回事。Linux 会使用空闲内存做页缓存，<code>free</code> 很低不一定危险；反过来，容器被 OOM Kill 时，宿主机可能还有大量空闲内存。</p></blockquote><p>这篇按一份内存取证报告展开。案例中容器限制为 4GiB，JVM <code>-Xmx3g</code>，退出码 137。监控显示堆只用了约 2.1GiB，因此最初“堆泄漏”的判断并不成立。</p><h2 id="事故摘要"><a href="#事故摘要" class="headerlink" title="事故摘要"></a>事故摘要</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">现象：order-service Pod 重启，退出码 137</span><br><span class="line">容器限制：4GiB</span><br><span class="line">JVM：JDK 8，-Xms3g -Xmx3g</span><br><span class="line">退出前 Heap used：约 2.1GiB</span><br><span class="line">退出前 RSS：约 3.95GiB</span><br><span class="line">宿主机 available：约 18GiB</span><br><span class="line">Java 日志：没有 java.lang.OutOfMemoryError</span><br></pre></td></tr></table></figure><p>这组事实更像容器边界触发的 OOM Kill，而不是 JVM 主动抛出堆 OOM。</p><h2 id="第一步：先确认是谁判定-OOM"><a href="#第一步：先确认是谁判定-OOM" class="headerlink" title="第一步：先确认是谁判定 OOM"></a>第一步：先确认是谁判定 OOM</h2><p>不同错误对应不同边界：</p><table><thead><tr><th>现象</th><th>决策者</th><th>典型方向</th></tr></thead><tbody><tr><td><code>Java heap space</code></td><td>JVM</td><td>堆对象过多或堆不足</td></tr><tr><td><code>GC overhead limit exceeded</code></td><td>JVM</td><td>GC 高耗但回收很少</td></tr><tr><td><code>Metaspace</code></td><td>JVM</td><td>类元数据或类加载器</td></tr><tr><td><code>Direct buffer memory</code></td><td>JVM&#x2F;NIO</td><td>直接内存</td></tr><tr><td><code>unable to create new native thread</code></td><td>JVM&#x2F;OS</td><td>线程、地址空间、系统限制</td></tr><tr><td>退出码 137 &#x2F; OOMKilled</td><td>cgroup 或内核</td><td>进程总内存超过边界</td></tr><tr><td>宿主机多个进程被杀</td><td>Linux OOM Killer</td><td>整机内存压力</td></tr></tbody></table><p>容器平台先看事件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">kubectl describe pod &lt;pod-name&gt; -n &lt;namespace&gt;</span><br><span class="line">kubectl get pod &lt;pod-name&gt; -n &lt;namespace&gt; -o yaml</span><br></pre></td></tr></table></figure><p>节点侧根据权限检查内核记录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">journalctl -k --since <span class="string">&#x27;2023-09-10 16:00:00&#x27;</span></span><br><span class="line">dmesg -T | rg -i <span class="string">&#x27;out of memory|killed process|oom&#x27;</span></span><br></pre></td></tr></table></figure><p>内核日志可能包含进程和主机信息，只由有权限的人员读取。部分集群不会向应用账号开放节点日志，此时以 Pod 事件和监控平台为准。</p><p>cgroup v2 可以查看：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /sys/fs/cgroup/memory.current</span><br><span class="line"><span class="built_in">cat</span> /sys/fs/cgroup/memory.max</span><br><span class="line"><span class="built_in">cat</span> /sys/fs/cgroup/memory.events</span><br></pre></td></tr></table></figure><p>路径会随运行时和 cgroup 版本变化。当前案例 <code>oom_kill</code> 计数增加，确认是容器超限。</p><h2 id="第二步：整机、容器、进程、JVM-四层不要混看"><a href="#第二步：整机、容器、进程、JVM-四层不要混看" class="headerlink" title="第二步：整机、容器、进程、JVM 四层不要混看"></a>第二步：整机、容器、进程、JVM 四层不要混看</h2><h3 id="整机"><a href="#整机" class="headerlink" title="整机"></a>整机</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">free -h</span><br><span class="line">vmstat 1 5</span><br><span class="line">ps -eo pid,user,%mem,rss,vsz,etime,cmd --<span class="built_in">sort</span>=-rss | <span class="built_in">head</span> -20</span><br></pre></td></tr></table></figure><p><code>free -h</code> 优先看 <code>available</code>，不要只看 <code>free</code>。<code>vmstat</code> 关注 <code>si/so</code>，持续交换通常意味着真实压力。</p><h3 id="容器"><a href="#容器" class="headerlink" title="容器"></a>容器</h3><p>看工作集、RSS、Cache、limit 和 OOM 事件。宿主机有空闲，不能阻止单个容器越过自己的 4GiB 限制。</p><h3 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /proc/&lt;pid&gt;/status</span><br><span class="line"><span class="built_in">cat</span> /proc/&lt;pid&gt;/smaps_rollup</span><br></pre></td></tr></table></figure><p>重点包括：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">VmRSS：当前驻留物理内存</span><br><span class="line">VmSize：虚拟地址空间，不等于实际占用</span><br><span class="line">RssAnon：匿名页，常包含 JVM 堆和本地内存</span><br><span class="line">RssFile：文件映射</span><br><span class="line">Threads：线程数量</span><br></pre></td></tr></table></figure><h3 id="JVM"><a href="#JVM" class="headerlink" title="JVM"></a>JVM</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">jstat -gcutil &lt;pid&gt; 1000 10</span><br><span class="line">jcmd &lt;pid&gt; GC.heap_info</span><br><span class="line">jcmd &lt;pid&gt; VM.flags</span><br></pre></td></tr></table></figure><p>JVM 堆只是进程内存的一部分。容器限制针对进程总量，而不是只看 <code>-Xmx</code>。</p><h2 id="第三步：画一张-JVM-内存预算"><a href="#第三步：画一张-JVM-内存预算" class="headerlink" title="第三步：画一张 JVM 内存预算"></a>第三步：画一张 JVM 内存预算</h2><p>Java 进程常见组成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Java Heap</span><br><span class="line">Metaspace 与 Compressed Class Space</span><br><span class="line">Code Cache</span><br><span class="line">Direct Buffer</span><br><span class="line">线程栈</span><br><span class="line">GC 与 JVM 内部结构</span><br><span class="line">JNI / 本地库</span><br><span class="line">内存映射文件</span><br><span class="line">页缓存与容器统计差异</span><br></pre></td></tr></table></figure><p>本次退出前的估算：</p><table><thead><tr><th>组成</th><th align="right">观察或预算</th></tr></thead><tbody><tr><td>Heap used</td><td align="right">2.1GiB</td></tr><tr><td>Heap committed</td><td align="right">3.0GiB</td></tr><tr><td>Metaspace + Class</td><td align="right">230MiB</td></tr><tr><td>Code Cache</td><td align="right">110MiB</td></tr><tr><td>Direct Buffer</td><td align="right">430MiB</td></tr><tr><td>线程约 620 个</td><td align="right">栈上限约 620MiB</td></tr><tr><td>JVM&#x2F;GC&#x2F;本地库</td><td align="right">约 180MiB</td></tr></tbody></table><p>这些数字不能简单相加当精确 RSS：堆 committed 不一定全部驻留，线程栈也会按需提交。但它足以说明 <code>-Xmx3g</code> 放进 4GiB 容器几乎没有安全余量。</p><p>配置预算至少要满足：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">容器 limit</span><br><span class="line">  &gt; Xmx</span><br><span class="line">  + 可预期的 Metaspace 和 Code Cache</span><br><span class="line">  + Direct Memory 上界</span><br><span class="line">  + 最大线程数 × 栈预算</span><br><span class="line">  + JVM 与本地库</span><br><span class="line">  + 安全余量</span><br></pre></td></tr></table></figure><p>不能使用“容器 4G，所以 Xmx 也配 4G”的方式。</p><h2 id="第四步：用-NMT-观察堆外组成"><a href="#第四步：用-NMT-观察堆外组成" class="headerlink" title="第四步：用 NMT 观察堆外组成"></a>第四步：用 NMT 观察堆外组成</h2><p>JDK 8 可在启动时启用 Native Memory Tracking：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-XX:NativeMemoryTracking=summary</span><br></pre></td></tr></table></figure><p>运行时查看：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jcmd &lt;pid&gt; VM.native_memory summary scale=MB</span><br></pre></td></tr></table></figure><p>NMT 必须在 JVM 启动时开启，不能在事故发生后临时补开。它能分类 JVM 自身原生内存，但不保证覆盖所有第三方本地库和内核页缓存，也有一定性能开销，需要在压测后启用。</p><p>输出重点：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">Java Heap</span><br><span class="line">Class</span><br><span class="line">Thread</span><br><span class="line">Code</span><br><span class="line">GC</span><br><span class="line">Compiler</span><br><span class="line">Internal</span><br><span class="line">Arena Chunk</span><br></pre></td></tr></table></figure><p>NIO 直接缓冲还可以通过应用指标、BufferPoolMXBean 或 <code>jcmd VM.native_memory</code> 辅助判断。只看 NMT 一项仍不足以解释进程全部 RSS。</p><h2 id="第五步：为什么线程数突然到-620"><a href="#第五步：为什么线程数突然到-620" class="headerlink" title="第五步：为什么线程数突然到 620"></a>第五步：为什么线程数突然到 620</h2><p>故障前下游库存接口延迟从 100ms 上升到 4s。应用中有一个无边界伸缩的异步池：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="type">ExecutorService</span> <span class="variable">executor</span> <span class="operator">=</span></span><br><span class="line">        Executors.newCachedThreadPool();</span><br></pre></td></tr></table></figure><p><code>newCachedThreadPool</code> 在任务堆积时可以持续创建线程。下游变慢后，旧任务没有完成，新任务继续创建线程：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">正常线程数：约 180</span><br><span class="line">故障前 1 分钟：320</span><br><span class="line">故障前 20 秒：510</span><br><span class="line">退出前：620</span><br></pre></td></tr></table></figure><p>同时 HTTP 客户端使用直接缓冲，等待请求增加后 Direct Memory 也上涨。堆使用率并没有明显异常，但进程 RSS 触碰容器限制。</p><p>这次根因可以分成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">触发条件：下游响应显著变慢</span><br><span class="line">放大机制：无界线程池持续创建线程</span><br><span class="line">容量缺口：Xmx 占容器限制 75%，堆外余量不足</span><br><span class="line">观测缺口：只告警 Heap，没有线程、RSS 和 cgroup 指标</span><br></pre></td></tr></table></figure><h2 id="第六步：如果真的是-Java-Heap-Space"><a href="#第六步：如果真的是-Java-Heap-Space" class="headerlink" title="第六步：如果真的是 Java Heap Space"></a>第六步：如果真的是 Java Heap Space</h2><p>堆 OOM 的取证路径不同。建议提前配置：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">-XX:+HeapDumpOnOutOfMemoryError</span><br><span class="line">-XX:HeapDumpPath=/var/lib/order/dumps</span><br><span class="line">-XX:+ExitOnOutOfMemoryError</span><br></pre></td></tr></table></figure><p><code>ExitOnOutOfMemoryError</code> 是否使用要结合 JDK 版本和服务恢复策略评估。Dump 目录必须有足够独立空间、正确权限和保留策略；否则 OOM 时可能因为磁盘不足再次失败。</p><p>临时主动导出：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jcmd &lt;pid&gt; GC.heap_dump /secure/path/order-heap.hprof</span><br></pre></td></tr></table></figure><p>Heap Dump 可能触发停顿、产生接近堆大小的文件并包含敏感业务数据。生产执行前必须评估磁盘、停顿和数据安全，由负责人授权。已经严重抖动的进程不一定承受得住主动 Dump。</p><p>分析时先看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">对象数量和浅堆大小</span><br><span class="line">Retained Heap</span><br><span class="line">Dominator Tree</span><br><span class="line">从大对象到 GC Root 的引用链</span><br><span class="line">类加载器数量</span><br><span class="line">集合和缓存是否无界</span><br></pre></td></tr></table></figure><p>“某个 DTO 数量最多”不等于 DTO 泄漏。要找谁长期持有它，以及这些对象是否超过业务合理数量。</p><h2 id="常见-OOM-分支"><a href="#常见-OOM-分支" class="headerlink" title="常见 OOM 分支"></a>常见 OOM 分支</h2><h3 id="Java-heap-space"><a href="#Java-heap-space" class="headerlink" title="Java heap space"></a>Java heap space</h3><p>可能是无界缓存、一次加载大结果集、消息积压对象化或真实容量不足。对照 GC 后 Old 区是否下降、对象增长曲线和引用链。</p><h3 id="GC-overhead-limit-exceeded"><a href="#GC-overhead-limit-exceeded" class="headerlink" title="GC overhead limit exceeded"></a>GC overhead limit exceeded</h3><p>JVM 花大量时间 GC，却只回收很少内存。通常已经接近堆耗尽，不能只关闭这个保护开关。继续查长期存活对象和分配速率。</p><h3 id="Metaspace"><a href="#Metaspace" class="headerlink" title="Metaspace"></a>Metaspace</h3><p>检查动态代理、脚本引擎、热部署和重复创建类加载器。若类数量持续上涨，单纯增加 <code>MaxMetaspaceSize</code> 只会延迟暴露。</p><h3 id="Direct-buffer-memory"><a href="#Direct-buffer-memory" class="headerlink" title="Direct buffer memory"></a>Direct buffer memory</h3><p>关注 Netty&#x2F;NIO、文件传输、HTTP 客户端和未及时释放的直接缓冲。可以设置 <code>MaxDirectMemorySize</code> 形成边界，但值过小也会影响正常 IO。</p><h3 id="Unable-to-create-new-native-thread"><a href="#Unable-to-create-new-native-thread" class="headerlink" title="Unable to create new native thread"></a>Unable to create new native thread</h3><p>同时看线程数、<code>ulimit -u</code>、地址空间、容器 PID limit 和剩余内存。线程池无界、阻塞调用和线程泄漏是常见原因。</p><h3 id="Linux-或-cgroup-OOM-Kill"><a href="#Linux-或-cgroup-OOM-Kill" class="headerlink" title="Linux 或 cgroup OOM Kill"></a>Linux 或 cgroup OOM Kill</h3><p>可能没有 Java 异常和 Heap Dump，因为 JVM 没有机会处理。证据主要来自容器事件、内核日志、RSS、cgroup 和退出前指标。</p><h2 id="本次修复"><a href="#本次修复" class="headerlink" title="本次修复"></a>本次修复</h2><h3 id="1-重新划分内存预算"><a href="#1-重新划分内存预算" class="headerlink" title="1. 重新划分内存预算"></a>1. 重新划分内存预算</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">容器 limit：4GiB -&gt; 5GiB</span><br><span class="line">Xms/Xmx：3GiB -&gt; 2.5GiB</span><br><span class="line">MaxDirectMemorySize：512MiB</span><br><span class="line">Xss：在测试后从 1MiB 调整为 512KiB</span><br><span class="line">安全余量：至少保留约 25%</span><br></pre></td></tr></table></figure><p>调整 <code>Xss</code> 可能让深调用更早 <code>StackOverflowError</code>，必须通过测试验证，不能为了省内存盲目减小。容器扩容和下调 Xmx 同时执行，是为了先恢复余量，再根据堆真实峰值逐步优化。</p><h3 id="2-无界线程池改为有界"><a href="#2-无界线程池改为有界" class="headerlink" title="2. 无界线程池改为有界"></a>2. 无界线程池改为有界</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ThreadPoolExecutor</span> <span class="variable">executor</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>(</span><br><span class="line">        <span class="number">32</span>,</span><br><span class="line">        <span class="number">64</span>,</span><br><span class="line">        <span class="number">60</span>,</span><br><span class="line">        TimeUnit.SECONDS,</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">ArrayBlockingQueue</span>&lt;&gt;(<span class="number">200</span>),</span><br><span class="line">        namedThreadFactory(<span class="string">&quot;inventory-call&quot;</span>),</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>.AbortPolicy()</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>线程数还要受下游并发和连接池约束。拒绝后进入可追踪补偿，不静默丢弃。</p><h3 id="3-下游调用快速失败"><a href="#3-下游调用快速失败" class="headerlink" title="3. 下游调用快速失败"></a>3. 下游调用快速失败</h3><p>设置连接池获取、建连和读取超时，取消叠加重试；当下游变慢时对入口限流，避免请求在内存中无限累积。</p><h3 id="4-补齐四层指标"><a href="#4-补齐四层指标" class="headerlink" title="4. 补齐四层指标"></a>4. 补齐四层指标</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">主机 available / swap / OOM</span><br><span class="line">容器 working set / RSS / limit / oom_kill</span><br><span class="line">进程 RSS / thread count / open files</span><br><span class="line">JVM heap / metaspace / direct buffer / GC</span><br></pre></td></tr></table></figure><p>任何一层达到 80% 都不直接等于故障，但趋势和剩余时间应触发提前调查。</p><h2 id="复现与容量验证"><a href="#复现与容量验证" class="headerlink" title="复现与容量验证"></a>复现与容量验证</h2><p>修复后在隔离环境注入 4 秒下游延迟：</p><table><thead><tr><th>指标</th><th align="right">修复前</th><th align="right">修复后</th></tr></thead><tbody><tr><td>最大线程数</td><td align="right">620+</td><td align="right">248</td></tr><tr><td>进程 RSS</td><td align="right">3.95GiB 后被杀</td><td align="right">峰值 3.42GiB</td></tr><tr><td>Heap used</td><td align="right">2.1GiB</td><td align="right">2.0GiB</td></tr><tr><td>Direct Buffer</td><td align="right">430MiB</td><td align="right">280MiB</td></tr><tr><td>拒绝&#x2F;补偿</td><td align="right">无边界堆积</td><td align="right">有明确计数</td></tr><tr><td>OOM Kill</td><td align="right">发生</td><td align="right">未发生</td></tr></tbody></table><p>还需要确认业务结果：超载请求是否得到明确响应，补偿任务能否追赶，下游恢复后内存和线程是否回落。</p><h2 id="取证清单"><a href="#取证清单" class="headerlink" title="取证清单"></a>取证清单</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">[ ] 判断 JVM OOM、容器 OOM 还是整机 OOM</span><br><span class="line">[ ] 保存退出时间、版本、事件和内核记录</span><br><span class="line">[ ] 对齐主机、容器、进程、JVM 四层指标</span><br><span class="line">[ ] 比较 Heap used 与进程 RSS</span><br><span class="line">[ ] 检查线程数、Direct Buffer、Metaspace 和本地库</span><br><span class="line">[ ] Heap Dump 前评估停顿、磁盘和数据安全</span><br><span class="line">[ ] 用引用链证明泄漏，不只看对象数量</span><br><span class="line">[ ] 建立 Xmx 之外的完整内存预算</span><br><span class="line">[ ] 在下游变慢和任务积压场景重新压测</span><br><span class="line">[ ] 验证恢复后 RSS、线程和业务积压都会回落</span><br></pre></td></tr></table></figure><p>内存问题最关键的第一问不是“哪个对象最大”，而是“哪一层的边界被突破”。只有先分清整机、容器、进程和 JVM，Heap Dump、NMT、线程数和 cgroup 指标才会落到正确的位置。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;写在前面：服务器内存“看起来快满了”和真正发生 OOM 不是一回事。Linux 会使用空闲内存做页缓存，&lt;code&gt;free&lt;/code&gt; 很低不一定危险；反过来，容器被 OOM Kill 时，宿主机可能还有大量空闲内存。&lt;/p&gt;
&lt;/blockq</summary>
      
    
    
    
    <category term="故障排查" scheme="https://swagch.github.io/categories/%E6%95%85%E9%9A%9C%E6%8E%92%E6%9F%A5/"/>
    
    
    <category term="Linux" scheme="https://swagch.github.io/tags/Linux/"/>
    
    <category term="OOM" scheme="https://swagch.github.io/tags/OOM/"/>
    
    <category term="JVM" scheme="https://swagch.github.io/tags/JVM/"/>
    
    <category term="内存排查" scheme="https://swagch.github.io/tags/%E5%86%85%E5%AD%98%E6%8E%92%E6%9F%A5/"/>
    
  </entry>
  
  <entry>
    <title>Linux服务器CPU飙高问题定位</title>
    <link href="https://swagch.github.io/2023/06/18/Linux%E6%9C%8D%E5%8A%A1%E5%99%A8CPU%E9%A3%99%E9%AB%98%E9%97%AE%E9%A2%98%E5%AE%9A%E4%BD%8D/"/>
    <id>https://swagch.github.io/2023/06/18/Linux%E6%9C%8D%E5%8A%A1%E5%99%A8CPU%E9%A3%99%E9%AB%98%E9%97%AE%E9%A2%98%E5%AE%9A%E4%BD%8D/</id>
    <published>2023-06-18T06:10:00.000Z</published>
    <updated>2023-06-19T12:45:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>写在前面：看到 CPU 100% 后，最直接的反应往往是重启服务。重启可能恢复业务，也会清掉最有价值的线程现场。只要系统还允许取证，先用几分钟回答“哪一种 CPU、哪个进程、哪个线程、哪段调用栈”，再决定限流、回滚还是重启。</p></blockquote><p>这篇按分支决策树组织。目标不是把所有 Linux 命令跑一遍，而是每一步都根据上一步证据选择下一条路。</p><h2 id="总决策树"><a href="#总决策树" class="headerlink" title="总决策树"></a>总决策树</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">CPU 告警</span><br><span class="line">  |</span><br><span class="line">  +-- 告警仍然存在吗？实例和时间对得上吗？</span><br><span class="line">  |</span><br><span class="line">  +-- us 高 -&gt; 用户态计算</span><br><span class="line">  |            -&gt; 进程 -&gt; 线程 -&gt; JVM 栈 / 火焰图</span><br><span class="line">  |</span><br><span class="line">  +-- sy 高 -&gt; 内核态消耗</span><br><span class="line">  |            -&gt; 系统调用、网络、中断、上下文切换</span><br><span class="line">  |</span><br><span class="line">  +-- wa 高 -&gt; CPU 在等 IO</span><br><span class="line">  |            -&gt; 磁盘、网络存储、不可中断任务</span><br><span class="line">  |</span><br><span class="line">  +-- st 高 -&gt; 虚拟机 CPU 被宿主机抢占</span><br><span class="line">  |            -&gt; 宿主机或云平台</span><br><span class="line">  |</span><br><span class="line">  +-- load 高但 CPU 不高</span><br><span class="line">  |            -&gt; 运行队列或 D 状态任务</span><br><span class="line">  |</span><br><span class="line">  +-- 容器慢但宿主机不高</span><br><span class="line">               -&gt; CPU quota 与 throttling</span><br></pre></td></tr></table></figure><p>“负载高”不等于“CPU 正在计算”。先分类，后面的命令才有意义。</p><h2 id="前三分钟：保存一份系统快照"><a href="#前三分钟：保存一份系统快照" class="headerlink" title="前三分钟：保存一份系统快照"></a>前三分钟：保存一份系统快照</h2><p>先记录时间、主机、负载和 CPU 分类：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">date</span> -Is</span><br><span class="line">hostname</span><br><span class="line"><span class="built_in">uptime</span></span><br><span class="line">top -b -n 1</span><br><span class="line">vmstat 1 5</span><br><span class="line">mpstat -P ALL 1 5</span><br></pre></td></tr></table></figure><p>生产输出可能包含命令行参数和内部信息，应由授权人员保存到受控位置。不要把完整 <code>top -c</code> 结果直接贴到公开渠道。</p><p><code>vmstat</code> 与 <code>mpstat</code> 重点字段：</p><table><thead><tr><th>字段</th><th>含义</th><th>常见方向</th></tr></thead><tbody><tr><td><code>us</code></td><td>用户态 CPU</td><td>Java 业务、序列化、压缩、计算</td></tr><tr><td><code>sy</code></td><td>内核态 CPU</td><td>系统调用、网络、中断、调度</td></tr><tr><td><code>wa</code></td><td>IO 等待</td><td>磁盘或网络存储延迟</td></tr><tr><td><code>st</code></td><td>虚拟化偷取</td><td>宿主机资源竞争</td></tr><tr><td><code>r</code></td><td>运行或等待 CPU 的任务</td><td>持续高于核数表示排队</td></tr><tr><td><code>b</code></td><td>不可中断睡眠任务</td><td>常见于 IO 等待</td></tr></tbody></table><p>还要看每个逻辑 CPU。32 核机器总体只有 4%，可能是一个单线程把一核打满；总体平均值会把热点稀释。</p><h2 id="分支-A：us-高，找到真正消耗-CPU-的进程"><a href="#分支-A：us-高，找到真正消耗-CPU-的进程" class="headerlink" title="分支 A：us 高，找到真正消耗 CPU 的进程"></a>分支 A：us 高，找到真正消耗 CPU 的进程</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">pidstat -u 1 5</span><br><span class="line">ps -eo pid,ppid,%cpu,%mem,etime,cmd --<span class="built_in">sort</span>=-%cpu | <span class="built_in">head</span> -20</span><br></pre></td></tr></table></figure><p>确认 PID 后先记录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">readlink</span> -f /proc/&lt;pid&gt;/exe</span><br><span class="line"><span class="built_in">tr</span> <span class="string">&#x27;\0&#x27;</span> <span class="string">&#x27; &#x27;</span> &lt; /proc/&lt;pid&gt;/cmdline</span><br><span class="line"><span class="built_in">cat</span> /proc/&lt;pid&gt;/status</span><br></pre></td></tr></table></figure><p>命令行可能包含密码或 Token，查看和保存时要脱敏。</p><p>容器环境有两层限制。宿主机 PID 与容器 PID 可能不同，先通过容器平台确认 Pod、容器和节点：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">kubectl top pod -n &lt;namespace&gt;</span><br><span class="line">kubectl describe pod &lt;pod-name&gt; -n &lt;namespace&gt;</span><br></pre></td></tr></table></figure><p>这些命令依赖集群权限。不要为了排查临时扩大账号权限，也不要仅凭 Pod 名猜宿主机进程。</p><h2 id="从-Java-进程缩小到线程"><a href="#从-Java-进程缩小到线程" class="headerlink" title="从 Java 进程缩小到线程"></a>从 Java 进程缩小到线程</h2><p>查看进程内线程：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">top -H -p &lt;pid&gt;</span><br><span class="line">pidstat -t -p &lt;pid&gt; 1 5</span><br></pre></td></tr></table></figure><p>记录连续多个采样中都高 CPU 的线程 ID，例如十进制 TID <code>23741</code>。JVM dump 中的 <code>nid</code> 常使用十六进制：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">printf</span> <span class="string">&#x27;%x\n&#x27;</span> 23741</span><br></pre></td></tr></table></figure><p>得到 <code>5cbd</code> 后抓三次线程现场：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">jstack -l &lt;pid&gt; &gt; /tmp/cpu-1.<span class="built_in">log</span></span><br><span class="line">jstack -l &lt;pid&gt; &gt; /tmp/cpu-2.<span class="built_in">log</span></span><br><span class="line">jstack -l &lt;pid&gt; &gt; /tmp/cpu-3.<span class="built_in">log</span></span><br><span class="line">rg -n -i <span class="string">&#x27;nid=0x5cbd&#x27;</span> /tmp/cpu-*.<span class="built_in">log</span></span><br></pre></td></tr></table></figure><p>三次采样间隔数秒。如果同一高 CPU 线程始终停在相同业务栈，更像死循环或热点计算；如果调用栈持续变化，可能只是正常繁忙。</p><p>一个典型热点：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&quot;http-nio-8080-exec-42&quot; RUNNABLE</span><br><span class="line">  at java.util.regex.Pattern$Curly.match(Pattern.java:...)</span><br><span class="line">  at java.util.regex.Matcher.search(Matcher.java:...)</span><br><span class="line">  at com.example.rule.RuleMatcher.match(RuleMatcher.java:87)</span><br><span class="line">  at com.example.order.OrderRuleService.calculate(OrderRuleService.java:132)</span><br></pre></td></tr></table></figure><p>这只能说明采样时线程在正则匹配。还要对照请求量、traceId、最近变更和输入大小，确认是否由灾难性回溯或异常大文本触发。</p><h2 id="如果高-CPU-线程是-GC"><a href="#如果高-CPU-线程是-GC" class="headerlink" title="如果高 CPU 线程是 GC"></a>如果高 CPU 线程是 GC</h2><p>线程名和栈指向 GC 时，继续收集：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">jstat -gcutil &lt;pid&gt; 1000 10</span><br><span class="line">jcmd &lt;pid&gt; GC.heap_info</span><br><span class="line">jcmd &lt;pid&gt; VM.flags</span><br></pre></td></tr></table></figure><p>观察：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Young GC 是否过于频繁</span><br><span class="line">Full GC 次数是否持续增加</span><br><span class="line">Old 区在 GC 后是否明显下降</span><br><span class="line">分配速率是否突然变化</span><br><span class="line">GC 日志中的停顿和原因</span><br></pre></td></tr></table></figure><p>常见分支：</p><ul><li>Old 区回收后不下降：怀疑对象长期持有或泄漏。</li><li>Young GC 极频繁：分配速率过高、堆或新生代不匹配。</li><li>Full GC 与元空间相关：动态类加载或类加载器泄漏。</li><li>显式 GC：检查代码或组件是否调用 <code>System.gc()</code>。</li></ul><p>不要在故障现场先调大堆。更大的堆可能延后 OOM，也可能增加 Full GC 停顿。先保存 GC 日志、对象分布和容量边界。</p><h2 id="如果栈一直变化，使用采样剖析"><a href="#如果栈一直变化，使用采样剖析" class="headerlink" title="如果栈一直变化，使用采样剖析"></a>如果栈一直变化，使用采样剖析</h2><p>线程 dump 是离散快照，热点分散时不够。服务器已安装并经过评估时，可以使用 <code>async-profiler</code> 或 <code>perf</code> 做短时间采样：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">perf top -p &lt;pid&gt;</span><br><span class="line">perf record -F 99 -p &lt;pid&gt; -g -- <span class="built_in">sleep</span> 30</span><br></pre></td></tr></table></figure><p><code>perf</code> 需要内核支持和相应权限，采样也有性能开销。生产使用前应走诊断授权，限制频率和时间，并在异常时立即停止。</p><p>火焰图回答一段时间内 CPU 样本集中在哪里，比单次 <code>jstack</code> 更适合：</p><ul><li>JSON 序列化。</li><li>压缩和加密。</li><li>正则表达式。</li><li>大集合排序。</li><li>频繁对象分配。</li><li>第三方库内部计算。</li></ul><p>“栈顶出现某方法”不等于它就是根因，要看累计样本宽度和调用路径。</p><h2 id="分支-B：sy-高，检查调度和系统调用"><a href="#分支-B：sy-高，检查调度和系统调用" class="headerlink" title="分支 B：sy 高，检查调度和系统调用"></a>分支 B：sy 高，检查调度和系统调用</h2><p><code>sy</code> 高时先看上下文切换：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">pidstat -w -p &lt;pid&gt; 1 5</span><br><span class="line">vmstat 1 5</span><br></pre></td></tr></table></figure><p>如果每秒上下文切换异常高，可能是：</p><ul><li>线程数过多。</li><li>大量短任务频繁唤醒。</li><li>锁竞争。</li><li>网络包和中断激增。</li><li>高频文件或系统调用。</li></ul><p>查看线程数量：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ls</span> /proc/&lt;pid&gt;/task | <span class="built_in">wc</span> -l</span><br><span class="line"><span class="built_in">cat</span> /proc/&lt;pid&gt;/limits</span><br></pre></td></tr></table></figure><p>线程多不一定是问题，但几千线程加上高切换、低业务吞吐就是强信号。</p><p>系统调用明细可以用 <code>strace</code> 短时采样，但附加到生产进程可能影响性能并输出敏感参数。只有在其他证据不足、经过授权时才使用，且限制时间和输出范围。</p><p>网络侧还可以检查软中断是否集中在单核、连接数是否异常、是否发生重传。此时通常需要系统或网络负责人共同判断，不应只在 Java 代码中找答案。</p><h2 id="分支-C：wa-高，不要继续调-JVM-CPU"><a href="#分支-C：wa-高，不要继续调-JVM-CPU" class="headerlink" title="分支 C：wa 高，不要继续调 JVM CPU"></a>分支 C：wa 高，不要继续调 JVM CPU</h2><p><code>wa</code> 高表示 CPU 时间花在等待 IO。继续看：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">iostat -xz 1 5</span><br><span class="line">pidstat -d 1 5</span><br><span class="line">ps -eo state,pid,ppid,wchan:32,cmd | awk <span class="string">&#x27;$1 ~ /^D/&#x27;</span></span><br></pre></td></tr></table></figure><p>关注设备延迟、队列、利用率和哪个进程在读写。常见来源：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">日志突增</span><br><span class="line">数据库刷盘或慢查询</span><br><span class="line">容器镜像和文件层</span><br><span class="line">网络块存储延迟</span><br><span class="line">大文件压缩或备份</span><br><span class="line">内存不足后的频繁 swap</span><br></pre></td></tr></table></figure><p><code>load average</code> 会计入不可中断睡眠任务，因此 IO 卡住时负载可能很高，CPU 却并不忙。应转入磁盘和 IO 分支，而不是增加业务线程。</p><h2 id="分支-D：st-高，问题可能不在虚拟机内"><a href="#分支-D：st-高，问题可能不在虚拟机内" class="headerlink" title="分支 D：st 高，问题可能不在虚拟机内"></a>分支 D：st 高，问题可能不在虚拟机内</h2><p><code>st</code> 表示虚拟 CPU 想运行，但宿主机没有分配时间。应用进程看起来繁忙、响应变慢，虚拟机内部却找不到对应计算。</p><p>需要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">确认 steal 是否持续且跨实例</span><br><span class="line">对比同宿主机其他虚拟机</span><br><span class="line">查看云平台 CPU credit 或宿主机事件</span><br><span class="line">联系基础设施团队迁移或扩容</span><br></pre></td></tr></table></figure><p>在虚拟机内重启 Java 通常不会解决宿主机争抢，反而会增加恢复成本。</p><h2 id="分支-E：容器被-CPU-限流"><a href="#分支-E：容器被-CPU-限流" class="headerlink" title="分支 E：容器被 CPU 限流"></a>分支 E：容器被 CPU 限流</h2><p>容器设置 1 核额度时，即使宿主机有 32 核空闲，容器也可能被 CFS 限流。检查容器指标和 cgroup：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /sys/fs/cgroup/cpu.stat</span><br><span class="line"><span class="built_in">cat</span> /sys/fs/cgroup/cpu.max</span><br></pre></td></tr></table></figure><p>cgroup v1 路径和字段不同，需要按运行环境确认。关键指标是限制周期、被限流次数和限流时间。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">usage 高 + throttled 高：额度不足或突发计算过强</span><br><span class="line">usage 不高 + 应用慢：继续查 IO、锁、下游</span><br><span class="line">宿主机不高 + 容器长期打满：不能用宿主机平均值否定问题</span><br></pre></td></tr></table></figure><p>提高 CPU limit 前仍要确认代码是否有异常热点。扩容能缓解容量不足，不能合理化死循环。</p><h2 id="如何选择应急动作"><a href="#如何选择应急动作" class="headerlink" title="如何选择应急动作"></a>如何选择应急动作</h2><table><thead><tr><th>证据</th><th>优先动作</th><th>不建议</th></tr></thead><tbody><tr><td>新版本单一热点方法</td><td>限流、回滚、保存剖析</td><td>直接长期扩容</td></tr><tr><td>流量符合预期但容量不足</td><td>扩实例并排队优化</td><td>只增单机线程</td></tr><tr><td>Full GC 持续</td><td>降低流量、保留堆证据</td><td>盲目调大 Xmx</td></tr><tr><td>sy 与切换异常高</td><td>控制线程、查锁与系统调用</td><td>只看业务栈</td></tr><tr><td>wa 高</td><td>转查磁盘与存储</td><td>调 JVM CPU 参数</td></tr><tr><td>st 高</td><td>联系宿主机&#x2F;云平台</td><td>反复重启应用</td></tr><tr><td>容器 throttling</td><td>优化热点或调整 quota</td><td>看宿主机空闲就结束</td></tr></tbody></table><p>当核心业务已不可用，可以在保存最小证据后执行摘流、回滚或重启。最小证据通常包括时间、系统快照、PID&#x2F;TID、三份线程 dump、GC 摘要、版本和最近变更。</p><p>恢复动作必须由当班负责人授权，并先确认单实例退出不会让剩余节点过载。</p><h2 id="修复后的验证"><a href="#修复后的验证" class="headerlink" title="修复后的验证"></a>修复后的验证</h2><p>不能只看 CPU 从 100% 降下来：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">[ ] QPS 和输入规模与故障时可比</span><br><span class="line">[ ] us/sy/wa/st 已回到各自基线</span><br><span class="line">[ ] P95/P99 和错误率恢复</span><br><span class="line">[ ] 热点线程或方法样本明显下降</span><br><span class="line">[ ] GC、线程数和上下文切换正常</span><br><span class="line">[ ] 容器 throttling 没有继续增长</span><br><span class="line">[ ] 下游、数据库和 IO 没被转移压力</span><br><span class="line">[ ] 限流、回滚或临时扩容有退出计划</span><br></pre></td></tr></table></figure><p>CPU 排查的核心路径可以压缩成：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">确认告警 -&gt; 区分 us/sy/wa/st -&gt; 找进程</span><br><span class="line">-&gt; 找线程 -&gt; 对应调用栈 -&gt; 用时间样本验证</span><br><span class="line">-&gt; 选择与证据匹配的恢复动作 -&gt; 复测整条链路</span><br></pre></td></tr></table></figure><p>只要第一步分类正确，CPU 100% 就不再是一个笼统现象。它会逐渐变成一段热点代码、一组 GC、一项 IO 等待、一次容器限流，或者一个需要基础设施团队处理的宿主机问题。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;写在前面：看到 CPU 100% 后，最直接的反应往往是重启服务。重启可能恢复业务，也会清掉最有价值的线程现场。只要系统还允许取证，先用几分钟回答“哪一种 CPU、哪个进程、哪个线程、哪段调用栈”，再决定限流、回滚还是重启。&lt;/p&gt;
&lt;/block</summary>
      
    
    
    
    <category term="故障排查" scheme="https://swagch.github.io/categories/%E6%95%85%E9%9A%9C%E6%8E%92%E6%9F%A5/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="Linux" scheme="https://swagch.github.io/tags/Linux/"/>
    
    <category term="故障排查" scheme="https://swagch.github.io/tags/%E6%95%85%E9%9A%9C%E6%8E%92%E6%9F%A5/"/>
    
    <category term="CPU" scheme="https://swagch.github.io/tags/CPU/"/>
    
  </entry>
  
  <entry>
    <title>MySQL联合索引与索引失效排查</title>
    <link href="https://swagch.github.io/2023/04/23/MySQL%E8%81%94%E5%90%88%E7%B4%A2%E5%BC%95%E4%B8%8E%E7%B4%A2%E5%BC%95%E5%A4%B1%E6%95%88%E6%8E%92%E6%9F%A5/"/>
    <id>https://swagch.github.io/2023/04/23/MySQL%E8%81%94%E5%90%88%E7%B4%A2%E5%BC%95%E4%B8%8E%E7%B4%A2%E5%BC%95%E5%A4%B1%E6%95%88%E6%8E%92%E6%9F%A5/</id>
    <published>2023-04-23T02:35:00.000Z</published>
    <updated>2023-04-24T14:15:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>写在前面：索引建了不等于一定会用，执行计划里出现索引也不等于查询足够快。与其背“最左匹配”“范围后失效”这些短句，不如准备一张测试表，把每个判断放进执行计划里验证。</p></blockquote><p>这篇使用同一个订单表回答十个常见问题。实验基于 MySQL 8.0，具体计划会随版本、数据分布和统计信息变化，结论不能脱离现场照搬。</p><h2 id="实验台"><a href="#实验台" class="headerlink" title="实验台"></a>实验台</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> order_info (</span><br><span class="line">    id <span class="type">BIGINT</span> <span class="keyword">NOT NULL</span> AUTO_INCREMENT,</span><br><span class="line">    tenant_id <span class="type">BIGINT</span> <span class="keyword">NOT NULL</span>,</span><br><span class="line">    order_no <span class="type">VARCHAR</span>(<span class="number">32</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    buyer_phone <span class="type">VARCHAR</span>(<span class="number">20</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    status <span class="type">VARCHAR</span>(<span class="number">20</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    amount <span class="type">DECIMAL</span>(<span class="number">12</span>, <span class="number">2</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">    created_at DATETIME <span class="keyword">NOT NULL</span>,</span><br><span class="line">    <span class="keyword">PRIMARY KEY</span> (id),</span><br><span class="line">    <span class="keyword">UNIQUE</span> KEY uk_order_no (order_no),</span><br><span class="line">    KEY idx_tenant_status_created (tenant_id, status, created_at)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB;</span><br></pre></td></tr></table></figure><p>联合索引可以近似理解为先按 <code>tenant_id</code> 排序，在相同租户内按 <code>status</code> 排序，再在相同状态内按 <code>created_at</code> 排序：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tenant_id -&gt; status -&gt; created_at -&gt; primary key</span><br></pre></td></tr></table></figure><p>测试数据约 500 万行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">tenant_id：1 万个，分布不完全均匀</span><br><span class="line">status：PAID 58%，CREATED 22%，其他 20%</span><br><span class="line">created_at：覆盖 24 个月</span><br><span class="line">buyer_phone：高区分度字符串</span><br></pre></td></tr></table></figure><p>数据分布很重要。只有 5 行的空表上，优化器选择全表扫描并不能证明索引失效。</p><p>每个问题至少执行：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN FORMAT<span class="operator">=</span>TREE</span><br><span class="line"><span class="keyword">SELECT</span> ...;</span><br><span class="line"></span><br><span class="line">EXPLAIN ANALYZE</span><br><span class="line"><span class="keyword">SELECT</span> ...;</span><br></pre></td></tr></table></figure><p><code>EXPLAIN ANALYZE</code> 会真正执行 SQL，只在可控环境或经过评估的查询上使用。对更新和删除语句更不能直接在生产试验。</p><h2 id="问题一：最左匹配是不是必须写全三列"><a href="#问题一：最左匹配是不是必须写全三列" class="headerlink" title="问题一：最左匹配是不是必须写全三列"></a>问题一：最左匹配是不是必须写全三列</h2><p>不是。只使用连续的左侧列也可以：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 可以按 tenant_id 定位</span></span><br><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 可以继续按 tenant_id、status 定位</span></span><br><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span>;</span><br></pre></td></tr></table></figure><p>跳过中间的 <code>status</code>：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line">  <span class="keyword">AND</span> created_at <span class="operator">&gt;=</span> <span class="string">&#x27;2023-04-01&#x27;</span>;</span><br></pre></td></tr></table></figure><p>执行计划仍可能使用 <code>idx_tenant_status_created</code> 按 <code>tenant_id</code> 缩小范围，但 <code>created_at</code> 无法与前导列组成连续边界，需要扫描该租户的多个状态后再过滤。</p><p>所以“索引使用了”和“索引的每一列都用于定位”是两件事。看 <code>key_len</code>、树形计划和实际扫描行数，不能只看 <code>key</code>。</p><h2 id="问题二：跳过第一列一定全表扫描吗"><a href="#问题二：跳过第一列一定全表扫描吗" class="headerlink" title="问题二：跳过第一列一定全表扫描吗"></a>问题二：跳过第一列一定全表扫描吗</h2><p>查询：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> created_at <span class="operator">&gt;=</span> <span class="string">&#x27;2023-04-01&#x27;</span>;</span><br></pre></td></tr></table></figure><p>普通 B-Tree 无法直接从 <code>status</code> 找到一个连续区间，因为相同状态散布在每个租户段中。当前数据里 <code>tenant_id</code> 有一万个不同值，优化器选择全表扫描。</p><p>MySQL 8.0 在特定条件下可能使用 Skip Scan，把少量前导列值分别探测。但它依赖版本、前导列基数和成本估算。前导列一万个值时，跳跃一万次通常没有优势。</p><p>结论不是“永远不可能使用”，而是不能把 Skip Scan 当成核心查询的稳定设计。如果按状态和时间查询是高频场景，应建立与它匹配的索引或改变查询入口。</p><h2 id="问题三：范围条件后的列是否完全失效"><a href="#问题三：范围条件后的列是否完全失效" class="headerlink" title="问题三：范围条件后的列是否完全失效"></a>问题三：范围条件后的列是否完全失效</h2><p>索引顺序改成：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_tenant_created_status</span><br><span class="line"><span class="keyword">ON</span> order_info (tenant_id, created_at, status);</span><br></pre></td></tr></table></figure><p>查询：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line">  <span class="keyword">AND</span> created_at <span class="operator">&gt;=</span> <span class="string">&#x27;2023-04-01&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span>;</span><br></pre></td></tr></table></figure><p><code>tenant_id</code> 和 <code>created_at</code> 可以形成索引范围。到达 <code>created_at &gt;=</code> 后，<code>status</code> 通常不能继续缩小连续范围，但它仍可能：</p><ul><li>在索引条件下推中提前过滤。</li><li>作为覆盖列避免回表。</li><li>减少传递到 Server 层的记录。</li></ul><p>因此“范围之后全部失效”太绝对。更准确的说法是：后续列通常不能继续参与构造本次索引查找的连续边界，但仍可能降低其他成本。</p><p>如果 <code>status</code> 是等值且选择性有价值，原索引 <code>(tenant_id, status, created_at)</code> 往往更适合当前查询。最终仍要比较真实分布。</p><h2 id="问题四：字段上使用函数一定没救吗"><a href="#问题四：字段上使用函数一定没救吗" class="headerlink" title="问题四：字段上使用函数一定没救吗"></a>问题四：字段上使用函数一定没救吗</h2><p>普通写法：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> <span class="type">DATE</span>(created_at) <span class="operator">=</span> <span class="string">&#x27;2023-04-23&#x27;</span>;</span><br></pre></td></tr></table></figure><p>对每行计算 <code>DATE(created_at)</code>，普通 <code>created_at</code> 索引无法直接定位原值区间。优先改写为：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> created_at <span class="operator">&gt;=</span> <span class="string">&#x27;2023-04-23 00:00:00&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> created_at <span class="operator">&lt;</span>  <span class="string">&#x27;2023-04-24 00:00:00&#x27;</span>;</span><br></pre></td></tr></table></figure><p>半开区间避免 <code>23:59:59</code> 漏掉更高精度时间。</p><p>如果业务只能按表达式查询，MySQL 8.0.13 以上可以评估函数索引：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_created_date</span><br><span class="line"><span class="keyword">ON</span> order_info ((<span class="type">DATE</span>(created_at)));</span><br></pre></td></tr></table></figure><p>函数索引增加存储与写入成本，也要求查询表达式与索引表达式匹配。能用原字段范围表达时，通常先选择范围查询。</p><h2 id="问题五：隐式类型转换为什么有时影响索引"><a href="#问题五：隐式类型转换为什么有时影响索引" class="headerlink" title="问题五：隐式类型转换为什么有时影响索引"></a>问题五：隐式类型转换为什么有时影响索引</h2><p><code>buyer_phone</code> 是 <code>VARCHAR</code>：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_buyer_phone</span><br><span class="line"><span class="keyword">ON</span> order_info (buyer_phone);</span><br></pre></td></tr></table></figure><p>错误参数类型：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> buyer_phone <span class="operator">=</span> <span class="number">13800138000</span>;</span><br></pre></td></tr></table></figure><p>数字与字符串比较可能让 MySQL 对列做数值转换，普通字符串索引难以按原顺序定位。正确方式：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> buyer_phone <span class="operator">=</span> <span class="string">&#x27;13800138000&#x27;</span>;</span><br></pre></td></tr></table></figure><p>反过来，数字列与可转换的字符串常量比较时，优化器可能转换常量后继续使用索引。不能简化成“类型不同一定失效”，但应用层仍应使用与字段一致的 JDBC 参数类型，避免计划、精度和异常值差异。</p><p>排查时同时看表定义和实际绑定参数。日志里的 <code>user_id = &#39;1001&#39;</code> 不一定等于驱动实际以字符串发送，需要在 SQL 采样或代码中确认。</p><h2 id="问题六：LIKE-是否都会失去索引"><a href="#问题六：LIKE-是否都会失去索引" class="headerlink" title="问题六：LIKE 是否都会失去索引"></a>问题六：LIKE 是否都会失去索引</h2><p>前缀匹配通常能转成范围：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> order_no <span class="keyword">LIKE</span> <span class="string">&#x27;O20230423%&#x27;</span>;</span><br></pre></td></tr></table></figure><p>前置通配符无法利用普通 B-Tree 的有序前缀：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> order_no <span class="keyword">LIKE</span> <span class="string">&#x27;%0423&#x27;</span>;</span><br></pre></td></tr></table></figure><p>如果后缀查询是核心需求，可以考虑冗余反转字段并按前缀查询、全文索引或搜索系统，但要同步写入和校验。不要为一个低频后台搜索立刻增加复杂链路。</p><p>字符集、排序规则和参数长度也会影响范围与选择性，仍以计划和扫描量为准。</p><h2 id="问题七：不等于、NOT-IN、IS-NOT-NULL-一定全表扫描吗"><a href="#问题七：不等于、NOT-IN、IS-NOT-NULL-一定全表扫描吗" class="headerlink" title="问题七：不等于、NOT IN、IS NOT NULL 一定全表扫描吗"></a>问题七：不等于、NOT IN、IS NOT NULL 一定全表扫描吗</h2><p>这类条件经常匹配大部分数据：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> status <span class="operator">&lt;&gt;</span> <span class="string">&#x27;CANCELLED&#x27;</span>;</span><br></pre></td></tr></table></figure><p>如果 95% 行都符合，走二级索引再回表可能比顺序扫描更贵，优化器选择全表扫描是合理成本决策，不是索引坏了。</p><p>当查询只返回索引中的 <code>id</code> 和 <code>status</code> 时，覆盖索引成本可能又低于全表扫描。结果取决于：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">匹配比例</span><br><span class="line">返回字段</span><br><span class="line">表行宽</span><br><span class="line">缓存命中</span><br><span class="line">随机 IO 成本</span><br><span class="line">统计信息</span><br></pre></td></tr></table></figure><p>优化目标是总成本，不是强迫每条 SQL 都显示 <code>type=ref</code>。</p><h2 id="问题八：OR-一定让索引失效吗"><a href="#问题八：OR-一定让索引失效吗" class="headerlink" title="问题八：OR 一定让索引失效吗"></a>问题八：OR 一定让索引失效吗</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> order_no <span class="operator">=</span> <span class="string">&#x27;O202304230001&#x27;</span></span><br><span class="line">   <span class="keyword">OR</span> buyer_phone <span class="operator">=</span> <span class="string">&#x27;13800138000&#x27;</span>;</span><br></pre></td></tr></table></figure><p>两列都有合适索引时，MySQL 可能使用 <code>index_merge</code>；数据量和成本不合适时，也可能全表扫描。</p><p>可以对照改写：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> order_no <span class="operator">=</span> <span class="string">&#x27;O202304230001&#x27;</span></span><br><span class="line"><span class="keyword">UNION</span> <span class="keyword">DISTINCT</span></span><br><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> buyer_phone <span class="operator">=</span> <span class="string">&#x27;13800138000&#x27;</span>;</span><br></pre></td></tr></table></figure><p><code>UNION DISTINCT</code> 有去重成本；如果业务保证两边不重叠，才可以使用 <code>UNION ALL</code>。改写后还要验证排序、分页和重复语义，不能只比较执行计划。</p><p>如果 OR 的一侧完全没有可用条件，常常会拖累整体。此时要评估拆查询、补索引，还是限制搜索能力。</p><h2 id="问题九：IN-很短就一定等同于等值吗"><a href="#问题九：IN-很短就一定等同于等值吗" class="headerlink" title="问题九：IN 很短就一定等同于等值吗"></a>问题九：IN 很短就一定等同于等值吗</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, created_at</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="keyword">IN</span> (<span class="string">&#x27;PAID&#x27;</span>, <span class="string">&#x27;REFUNDED&#x27;</span>)</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure><p>在 <code>(tenant_id, status, created_at)</code> 上，两个 status 对应两个索引区间。每个区间内部按时间有序，但合并后的全局时间顺序可能需要额外排序。</p><p>当 <code>IN</code> 值很多时，范围数量、优化器内存和排序成本继续增加。不要仅凭“IN 也是等值”判断能否同时满足 <code>ORDER BY</code>。</p><p>如果“全部有效状态按时间取最近”是核心入口，可能需要：</p><ul><li>冗余一个 <code>is_active</code> 分类字段并建立匹配索引。</li><li>分状态各取一部分后在应用合并。</li><li>调整业务查询模型。</li></ul><p>每个方案都增加写入或实现成本，应通过数据验证。</p><h2 id="问题十：索引完全匹配，优化器为什么仍不用"><a href="#问题十：索引完全匹配，优化器为什么仍不用" class="headerlink" title="问题十：索引完全匹配，优化器为什么仍不用"></a>问题十：索引完全匹配，优化器为什么仍不用</h2><p>常见原因不止“统计信息过期”：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">条件匹配比例太高</span><br><span class="line">返回列很多，回表成本高</span><br><span class="line">表很小，全表扫描更便宜</span><br><span class="line">数据倾斜，平均统计无法描述某个热点值</span><br><span class="line">组合条件存在相关性，估算偏差</span><br><span class="line">索引和表缓存状态不同</span><br><span class="line">优化器参数或版本行为变化</span><br></pre></td></tr></table></figure><p>先核对：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> INDEX <span class="keyword">FROM</span> order_info;</span><br><span class="line">ANALYZE <span class="keyword">TABLE</span> order_info;</span><br></pre></td></tr></table></figure><p><code>ANALYZE TABLE</code> 会更新统计信息，并可能带来资源和锁影响，应按数据库变更规范执行。不要把它当成每次慢查询的固定按钮。</p><p>MySQL 8.0 可以用直方图描述非索引列或倾斜分布，但直方图也需要维护，且不是联合相关性的万能解法。</p><h2 id="FORCE-INDEX-能不能作为最终修复"><a href="#FORCE-INDEX-能不能作为最终修复" class="headerlink" title="FORCE INDEX 能不能作为最终修复"></a>FORCE INDEX 能不能作为最终修复</h2><p><code>FORCE INDEX</code> 适合在隔离环境比较候选计划，生产长期使用要谨慎：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id</span><br><span class="line"><span class="keyword">FROM</span> order_info FORCE INDEX (idx_tenant_status_created)</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span>;</span><br></pre></td></tr></table></figure><p>它把今天的判断固定进 SQL。数据增长、状态比例和索引变化后，被强制的计划可能变差，优化器也失去调整空间。</p><p>如果必须临时使用，应记录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">为什么优化器当前误判</span><br><span class="line">哪些参数和数据已验证</span><br><span class="line">观察指标</span><br><span class="line">移除条件</span><br><span class="line">复核日期</span><br></pre></td></tr></table></figure><p>更长期的方向通常是修正统计、查询模型或索引设计。</p><h2 id="联合索引怎么从业务查询推导"><a href="#联合索引怎么从业务查询推导" class="headerlink" title="联合索引怎么从业务查询推导"></a>联合索引怎么从业务查询推导</h2><p>我现在按四步设计。</p><h3 id="1-收集查询族"><a href="#1-收集查询族" class="headerlink" title="1. 收集查询族"></a>1. 收集查询族</h3><p>不是只看一条慢 SQL，而是整理同一张表的高频读写：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">租户 + 状态 + 时间列表</span><br><span class="line">订单号精确查询</span><br><span class="line">买家手机号搜索</span><br><span class="line">按用户游标分页</span><br><span class="line">状态更新</span><br><span class="line">批量归档</span><br></pre></td></tr></table></figure><h3 id="2-区分等值、范围、排序和返回"><a href="#2-区分等值、范围、排序和返回" class="headerlink" title="2. 区分等值、范围、排序和返回"></a>2. 区分等值、范围、排序和返回</h3><p>通常先考虑高频等值条件，再考虑范围和排序；但数据选择性、是否总是出现、是否支持多种查询同样重要。不存在一个脱离业务的固定顺序口诀。</p><h3 id="3-用最少索引覆盖主要路径"><a href="#3-用最少索引覆盖主要路径" class="headerlink" title="3. 用最少索引覆盖主要路径"></a>3. 用最少索引覆盖主要路径</h3><p>相似索引会重复占用空间并拖慢写入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">idx_tenant</span><br><span class="line">idx_tenant_status</span><br><span class="line">idx_tenant_status_created</span><br></pre></td></tr></table></figure><p>长索引可能覆盖短索引的部分查询，但不一定能完全替代：索引宽度、排序、覆盖和其他 SQL 都要检查。确认替代关系后再分阶段删除。</p><h3 id="4-对读写一起压测"><a href="#4-对读写一起压测" class="headerlink" title="4. 对读写一起压测"></a>4. 对读写一起压测</h3><p>索引验收至少看：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">目标查询 P95 / P99</span><br><span class="line">Rows_examined / Rows_sent</span><br><span class="line">并发下 CPU 与 IO</span><br><span class="line">INSERT / UPDATE 延迟</span><br><span class="line">索引体积与 Buffer Pool</span><br><span class="line">主从复制延迟</span><br></pre></td></tr></table></figure><p>查询快了 20ms、写入慢了 30%，可能不是一次成功优化。</p><h2 id="一张不靠口诀的排查单"><a href="#一张不靠口诀的排查单" class="headerlink" title="一张不靠口诀的排查单"></a>一张不靠口诀的排查单</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">[ ] SQL、参数类型和表结构来自真实现场</span><br><span class="line">[ ] 数据量与分布足以代表生产</span><br><span class="line">[ ] 分清“使用索引”和“各列参与定位”</span><br><span class="line">[ ] 查看估算行数，也查看实际扫描行数</span><br><span class="line">[ ] 函数和类型转换是否作用在列上</span><br><span class="line">[ ] 范围、IN、OR 与 ORDER BY 的组合是否改变计划</span><br><span class="line">[ ] 优化器选择全表扫描是否本来就更便宜</span><br><span class="line">[ ] 统计信息、倾斜和版本差异是否影响估算</span><br><span class="line">[ ] 候选索引是否重复或过宽</span><br><span class="line">[ ] 读取收益与写入、存储成本一起验证</span><br></pre></td></tr></table></figure><p>所谓“索引失效”经常把很多不同现象揉成一句话：有时根本没用索引，有时只用了联合索引前几列，有时使用了索引但扫描仍然很大，还有时全表扫描就是更好的计划。</p><p>把问题还原成“优化器选择了哪条访问路径、估算依据是什么、实际付出了多少成本”，比背下更多绝对规则更可靠。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;写在前面：索引建了不等于一定会用，执行计划里出现索引也不等于查询足够快。与其背“最左匹配”“范围后失效”这些短句，不如准备一张测试表，把每个判断放进执行计划里验证。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;这篇使用同一个订单表回答十个常见问题。实</summary>
      
    
    
    
    <category term="数据库" scheme="https://swagch.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
    
    <category term="MySQL" scheme="https://swagch.github.io/tags/MySQL/"/>
    
    <category term="SQL优化" scheme="https://swagch.github.io/tags/SQL%E4%BC%98%E5%8C%96/"/>
    
    <category term="联合索引" scheme="https://swagch.github.io/tags/%E8%81%94%E5%90%88%E7%B4%A2%E5%BC%95/"/>
    
  </entry>
  
  <entry>
    <title>一次MySQL慢SQL调优记录</title>
    <link href="https://swagch.github.io/2023/02/19/%E4%B8%80%E6%AC%A1MySQL%E6%85%A2SQL%E8%B0%83%E4%BC%98%E8%AE%B0%E5%BD%95/"/>
    <id>https://swagch.github.io/2023/02/19/%E4%B8%80%E6%AC%A1MySQL%E6%85%A2SQL%E8%B0%83%E4%BC%98%E8%AE%B0%E5%BD%95/</id>
    <published>2023-02-19T07:20:00.000Z</published>
    <updated>2023-02-20T13:10:00.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>写在前面：SQL 调优最怕一上来就加索引。索引可能暂时把耗时压下去，也可能只是把成本从查询转移到写入和存储。下面按一次完整实验记录来写：先保存基线，再提出候选方案，用执行计划和对照数据筛选，最后明确上线与回滚条件。</p></blockquote><p>问题接口查询某个用户最近的已支付订单。数据增长后，P99 从 300ms 左右升到 5 秒，数据库 CPU 没有打满，但慢查询中的扫描行数非常夸张。</p><h2 id="13-50，固定问题样本"><a href="#13-50，固定问题样本" class="headerlink" title="13:50，固定问题样本"></a>13:50，固定问题样本</h2><p>原始 SQL：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, order_no, user_id, status, amount, created_at</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">10001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure><p>慢查询摘要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># Query_time: 4.782  Lock_time: 0.000</span><br><span class="line"># Rows_sent: 20  Rows_examined: 2864317</span><br></pre></td></tr></table></figure><p>只返回 20 行，却扫描 286 万行。为了避免优化错对象，还需要确认：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">SQL digest：0x7A82...</span><br><span class="line">过去 1 小时调用：18,420 次</span><br><span class="line">参数分布：高频用户与普通用户都慢</span><br><span class="line">调用入口：订单列表、支付结果页</span><br><span class="line">数据库实例：只读副本</span><br><span class="line">表数据量：约 1,860 万行</span><br></pre></td></tr></table></figure><p>应用日志中的 SQL 可能经过 ORM 动态拼装，真正执行的字段、条件和排序应以数据库采样为准。样本中的用户和订单数据均做脱敏。</p><h2 id="14-05，建立可比较的基线"><a href="#14-05，建立可比较的基线" class="headerlink" title="14:05，建立可比较的基线"></a>14:05，建立可比较的基线</h2><p>测试环境使用一份接近生产分布的快照，数据库版本与参数保持一致。每个候选方案执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">冷缓存：重启隔离测试实例后执行 3 次</span><br><span class="line">暖缓存：预热后执行 20 次</span><br><span class="line">并发：1、20、50 三档</span><br><span class="line">写入：保持相同订单写入压测</span><br><span class="line">记录：P50、P95、P99、Rows_examined、CPU、IO</span><br></pre></td></tr></table></figure><p>这里不通过生产执行 <code>RESET QUERY CACHE</code> 或重启数据库制造冷缓存。冷缓存实验只在隔离实例进行。</p><p>基线结果：</p><table><thead><tr><th>场景</th><th align="right">P50</th><th align="right">P99</th><th align="right">Rows_examined</th><th align="right">数据库 CPU</th></tr></thead><tbody><tr><td>单线程暖缓存</td><td align="right">4.12s</td><td align="right">4.91s</td><td align="right">2,864,317</td><td align="right">18%</td></tr><tr><td>20 并发</td><td align="right">5.86s</td><td align="right">8.42s</td><td align="right">2,864,317</td><td align="right">63%</td></tr><tr><td>50 并发</td><td align="right">9.74s</td><td align="right">超时</td><td align="right">2,864,317</td><td align="right">91%</td></tr></tbody></table><p>单次查询时 CPU 看起来不高，并发放大后问题才接近生产影响。只测一次很容易低估扫描成本。</p><h2 id="14-18，读执行计划而不是只看-key"><a href="#14-18，读执行计划而不是只看-key" class="headerlink" title="14:18，读执行计划而不是只看 key"></a>14:18，读执行计划而不是只看 key</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN</span><br><span class="line"><span class="keyword">SELECT</span> id, order_no, user_id, status, amount, created_at</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">10001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure><p>关键结果：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">type: index</span><br><span class="line">possible_keys: idx_user_id,idx_status</span><br><span class="line">key: idx_created_at</span><br><span class="line">rows: 2840000</span><br><span class="line">filtered: 0.1</span><br><span class="line">Extra: Using where</span><br></pre></td></tr></table></figure><p>优化器选择 <code>idx_created_at</code>，是因为它能按创建时间倒序扫描并尽早满足 <code>LIMIT</code>。但用户 10001 的已支付订单很稀疏，为找到 20 条匹配记录，它沿时间索引跳过了大量其他用户订单。</p><p><code>key</code> 不为空只说明使用了某个索引，不说明这个访问路径足够便宜。<code>rows</code> 和实际 <code>Rows_examined</code> 才暴露扫描规模。</p><p>在 MySQL 8.0.18 以上的隔离环境，还可以使用：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">EXPLAIN ANALYZE</span><br><span class="line"><span class="keyword">SELECT</span> id, order_no, user_id, status, amount, created_at</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">10001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure><p><code>EXPLAIN ANALYZE</code> 会真实执行查询，可能产生负载和读取开销，不能把它当成生产环境的无成本命令。它显示大部分时间消耗在按 <code>created_at</code> 扫描后过滤。</p><h2 id="14-32，先看选择性和数据分布"><a href="#14-32，先看选择性和数据分布" class="headerlink" title="14:32，先看选择性和数据分布"></a>14:32，先看选择性和数据分布</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> total,</span><br><span class="line">    <span class="built_in">COUNT</span>(<span class="keyword">DISTINCT</span> user_id) <span class="keyword">AS</span> users,</span><br><span class="line">    <span class="built_in">SUM</span>(status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span>) <span class="keyword">AS</span> paid_rows</span><br><span class="line"><span class="keyword">FROM</span> order_info;</span><br></pre></td></tr></table></figure><p>结果概要：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">总行数：18,600,000</span><br><span class="line">用户数：1,240,000</span><br><span class="line">PAID 占比：约 61%</span><br><span class="line">普通用户订单中位数：9</span><br><span class="line">高频用户最大订单数：84,000</span><br></pre></td></tr></table></figure><p><code>status</code> 单列区分度很低，<code>user_id</code> 更适合作为联合索引前导列。查询还有按时间排序，因此候选索引应同时考虑过滤和顺序。</p><h2 id="14-45，候选-A：user-id-status"><a href="#14-45，候选-A：user-id-status" class="headerlink" title="14:45，候选 A：user_id + status"></a>14:45，候选 A：user_id + status</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_user_status</span><br><span class="line"><span class="keyword">ON</span> order_info (user_id, status);</span><br></pre></td></tr></table></figure><p>执行计划改为按用户和状态定位，但仍有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">rows: 84216</span><br><span class="line">Extra: Using index condition; Using filesort</span><br></pre></td></tr></table></figure><p>普通用户查询约 28ms，高频用户约 310ms。相比原方案已经明显改善，但热门用户需要读取并排序 8 万多行，P99 仍不稳定。</p><p>这个实验验证了前两列的过滤价值，也说明排序字段不能忽略。</p><h2 id="15-10，候选-B：加入-created-at"><a href="#15-10，候选-B：加入-created-at" class="headerlink" title="15:10，候选 B：加入 created_at"></a>15:10，候选 B：加入 created_at</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_user_status_created</span><br><span class="line"><span class="keyword">ON</span> order_info (user_id, status, created_at <span class="keyword">DESC</span>);</span><br></pre></td></tr></table></figure><p>MySQL 8.0 支持降序索引；在不支持的版本中，InnoDB 也可以反向扫描普通 B-Tree，需要以实际执行计划验证。</p><p>新计划：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">type: ref</span><br><span class="line">key: idx_user_status_created</span><br><span class="line">rows: 84216</span><br><span class="line">Extra: Backward index scan</span><br></pre></td></tr></table></figure><p><code>rows</code> 仍是统计估算，但实际执行在按顺序取到 20 行后停止：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Rows_sent: 20</span><br><span class="line">Rows_examined: 20</span><br></pre></td></tr></table></figure><p>关键不是计划里某个字段看起来漂亮，而是实际扫描量已经与返回量接近。</p><h2 id="15-35，候选-C：做成宽覆盖索引"><a href="#15-35，候选-C：做成宽覆盖索引" class="headerlink" title="15:35，候选 C：做成宽覆盖索引"></a>15:35，候选 C：做成宽覆盖索引</h2><p>为了验证回表成本，又测试：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_user_status_created_cover</span><br><span class="line"><span class="keyword">ON</span> order_info (</span><br><span class="line">    user_id,</span><br><span class="line">    status,</span><br><span class="line">    created_at <span class="keyword">DESC</span>,</span><br><span class="line">    order_no,</span><br><span class="line">    amount</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>因为 InnoDB 二级索引叶子节点已经包含主键，查询所需字段基本都在索引中。单查询 P99 从 14ms 下降到约 10ms，但索引体积增加明显，订单写入 P99 上升约 13%。</p><p>一次只返回 20 行，方案 B 的回表次数很少。为了 4ms 收益承担更宽索引和更高写放大不划算，因此候选 C 被淘汰。</p><h2 id="三个方案的对照结果"><a href="#三个方案的对照结果" class="headerlink" title="三个方案的对照结果"></a>三个方案的对照结果</h2><table><thead><tr><th>方案</th><th align="right">暖缓存 P99</th><th align="right">50 并发 P99</th><th align="right">实际扫描行</th><th align="right">写入 P99 变化</th></tr></thead><tbody><tr><td>原始索引</td><td align="right">4.91s</td><td align="right">超时</td><td align="right">2,864,317</td><td align="right">基线</td></tr><tr><td>A：user,status</td><td align="right">310ms</td><td align="right">1.12s</td><td align="right">84,216</td><td align="right">+5%</td></tr><tr><td>B：user,status,time</td><td align="right">14ms</td><td align="right">68ms</td><td align="right">20</td><td align="right">+7%</td></tr><tr><td>C：宽覆盖索引</td><td align="right">10ms</td><td align="right">55ms</td><td align="right">20</td><td align="right">+13%</td></tr></tbody></table><p>最终选择 B。它不是查询绝对最快的方案，但在读取收益、写入成本和索引体积之间更平衡。</p><h2 id="16-10，上线前先写回滚门槛"><a href="#16-10，上线前先写回滚门槛" class="headerlink" title="16:10，上线前先写回滚门槛"></a>16:10，上线前先写回滚门槛</h2><p>大表加索引不是普通代码发布。执行前记录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">预计索引大小</span><br><span class="line">构建临时空间</span><br><span class="line">DDL 预计时间</span><br><span class="line">元数据锁等待</span><br><span class="line">主从复制延迟</span><br><span class="line">写入延迟变化</span><br><span class="line">中止方式</span><br><span class="line">旧索引保留时间</span><br></pre></td></tr></table></figure><p>本次先在只读副本创建并验证，再通过数据库变更平台执行。是否支持在线 DDL 与 <code>LOCK=NONE</code> 取决于 MySQL 版本、存储引擎和具体操作，不能只看语法：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> order_info</span><br><span class="line"><span class="keyword">ADD</span> INDEX idx_user_status_created (user_id, status, created_at <span class="keyword">DESC</span>),</span><br><span class="line">ALGORITHM<span class="operator">=</span>INPLACE,</span><br><span class="line">LOCK<span class="operator">=</span><span class="keyword">NONE</span>;</span><br></pre></td></tr></table></figure><p>执行由数据库负责人操作，并持续观察。预先约定暂停或回滚条件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">元数据锁阻塞核心写入</span><br><span class="line">复制延迟持续超过 30 秒</span><br><span class="line">磁盘剩余空间低于安全线</span><br><span class="line">订单写入 P99 比基线上升超过 15%</span><br><span class="line">新索引上线后仍未被目标 SQL 使用</span><br></pre></td></tr></table></figure><p>删除索引同样是生产 DDL，不在发现异常后凭情绪立即执行。先确认异常与索引相关，再按变更流程回退。</p><h2 id="17-20，灰度验证真实流量"><a href="#17-20，灰度验证真实流量" class="headerlink" title="17:20，灰度验证真实流量"></a>17:20，灰度验证真实流量</h2><p>索引完成后先让一个只读实例承接少量订单查询，核对：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">目标 SQL 的 key 是否为新索引</span><br><span class="line">Rows_examined / Rows_sent 是否接近 1</span><br><span class="line">普通用户与高频用户 P99</span><br><span class="line">Buffer Pool、磁盘读取与 CPU</span><br><span class="line">订单写入和复制延迟</span><br><span class="line">错误率与结果一致性</span><br></pre></td></tr></table></figure><p>灰度一小时后：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">接口 P99：4.9s -&gt; 72ms</span><br><span class="line">Rows_examined P95：280 万 -&gt; 20</span><br><span class="line">只读实例 CPU 峰值：76% -&gt; 31%</span><br><span class="line">订单写入 P99：增加约 6%</span><br><span class="line">复制延迟：无明显变化</span><br></pre></td></tr></table></figure><p>结果符合实验，才逐步扩大流量。</p><h2 id="索引解决不了深分页"><a href="#索引解决不了深分页" class="headerlink" title="索引解决不了深分页"></a>索引解决不了深分页</h2><p>首页查询改善后，运营后台仍有：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">LIMIT <span class="number">200000</span>, <span class="number">20</span>;</span><br></pre></td></tr></table></figure><p>即使使用联合索引，数据库仍要跳过大量记录。用户侧列表改为游标分页：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, order_no, user_id, status, amount, created_at</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> user_id <span class="operator">=</span> :userId</span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> (</span><br><span class="line">      created_at <span class="operator">&lt;</span> :lastCreatedAt</span><br><span class="line">      <span class="keyword">OR</span> (created_at <span class="operator">=</span> :lastCreatedAt <span class="keyword">AND</span> id <span class="operator">&lt;</span> :lastId)</span><br><span class="line">  )</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span>, id <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure><p>正式索引相应增加 <code>id</code> 作为稳定排序键。只按时间游标时，相同时间的订单可能重复或遗漏。</p><p>运营导出不再通过深分页同步拉取，而是走异步批任务，避免一个请求长期占用数据库连接。</p><h2 id="一周后的复核"><a href="#一周后的复核" class="headerlink" title="一周后的复核"></a>一周后的复核</h2><p>索引上线不是实验结束。我们在一周后再次检查：</p><ul><li>慢查询 digest 的调用量和 P99。</li><li>新索引实际使用次数。</li><li>单表索引体积和 Buffer Pool 命中。</li><li>写入、更新和删除成本。</li><li>旧单列索引是否与新索引重复。</li><li>数据分布变化后计划是否稳定。</li></ul><p>确认新联合索引覆盖了 <code>idx_user_id</code> 的用途后，才单独发起旧索引下线评审。删除前还要检索其他 SQL，不能只围绕本次接口判断。</p><p>这次调优最后留下的不是一句“加联合索引”，而是一条可以复查的决策链：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">真实慢日志建立基线</span><br><span class="line">执行计划解释扫描路径</span><br><span class="line">数据分布决定候选顺序</span><br><span class="line">多个索引方案对照读写成本</span><br><span class="line">提前定义上线和回滚门槛</span><br><span class="line">灰度验证真实流量</span><br><span class="line">持续复核索引价值</span><br></pre></td></tr></table></figure><p>性能优化不是把某一次耗时压到最低，而是在真实负载下，让读取、写入、容量和可回退性达到更稳定的平衡。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;写在前面：SQL 调优最怕一上来就加索引。索引可能暂时把耗时压下去，也可能只是把成本从查询转移到写入和存储。下面按一次完整实验记录来写：先保存基线，再提出候选方案，用执行计划和对照数据筛选，最后明确上线与回滚条件。&lt;/p&gt;
&lt;/blockquote</summary>
      
    
    
    
    <category term="数据库" scheme="https://swagch.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
    
    <category term="MySQL" scheme="https://swagch.github.io/tags/MySQL/"/>
    
    <category term="慢查询" scheme="https://swagch.github.io/tags/%E6%85%A2%E6%9F%A5%E8%AF%A2/"/>
    
    <category term="SQL优化" scheme="https://swagch.github.io/tags/SQL%E4%BC%98%E5%8C%96/"/>
    
  </entry>
  
  <entry>
    <title>从单体到微服务总结</title>
    <link href="https://swagch.github.io/2022/12/25/2022-12-25-%E4%BB%8E%E5%8D%95%E4%BD%93%E5%88%B0%E5%BE%AE%E6%9C%8D%E5%8A%A1%E6%80%BB%E7%BB%93/"/>
    <id>https://swagch.github.io/2022/12/25/2022-12-25-%E4%BB%8E%E5%8D%95%E4%BD%93%E5%88%B0%E5%BE%AE%E6%9C%8D%E5%8A%A1%E6%80%BB%E7%BB%93/</id>
    <published>2022-12-25T15:31:18.000Z</published>
    <updated>2026-08-26T03:54:37.317Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>2022 年最后一篇不再写组件配置，想回头看一看这一年的技术路线。年初我关心的是“一个系统怎么拆成多个服务”，到年末更关心“团队能不能长期维护拆完以后的复杂度”。这个变化比多会几个框架更重要。</p></blockquote><h2 id="年初的理解：服务越独立越先进"><a href="#年初的理解：服务越独立越先进" class="headerlink" title="年初的理解：服务越独立越先进"></a>年初的理解：服务越独立越先进</h2><p>最开始看到单体应用的问题很直接：工程越来越大，发布越来越慢，模块之间相互影响。微服务看起来给出了一个明确答案：把用户、订单、库存、支付拆开，每个服务独立数据库、独立部署、独立扩容。</p><p>这个方向没有错，但太容易忽略前提。服务从一个进程拆成四个进程以后，原来隐藏的成本全部显性化：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">本地调用变成网络调用</span><br><span class="line">本地事务变成跨服务状态协调</span><br><span class="line">一个配置文件变成多环境配置管理</span><br><span class="line">一份日志变成跨实例检索</span><br><span class="line">一次发布变成多服务版本兼容</span><br></pre></td></tr></table></figure><p>系统不是被拆小了，而是把代码复杂度的一部分换成了分布式系统和组织协作复杂度。</p><h2 id="这一年的实际路线"><a href="#这一年的实际路线" class="headerlink" title="这一年的实际路线"></a>这一年的实际路线</h2><p>回看文章顺序，基本就是我对微服务认识逐渐完整的过程：</p><h3 id="一月：先讨论边界"><a href="#一月：先讨论边界" class="headerlink" title="一月：先讨论边界"></a>一月：先讨论边界</h3><p>第一步不是引入注册中心，而是判断用户、订单、库存和支付的数据归属。边界不清楚时，服务拆得越快，后面跨服务修改越多。</p><p>这一阶段最大的收获是“服务按业务能力拆，不按 Controller 或数据库表数量拆”。如果订单服务仍然直接更新库存表，两个仓库并没有形成两个边界。</p><h3 id="二月至三月：补服务治理"><a href="#二月至三月：补服务治理" class="headerlink" title="二月至三月：补服务治理"></a>二月至三月：补服务治理</h3><p>服务数量增加以后，固定 IP 调用很快失效，于是接触 Nacos、Gateway 和 OpenFeign。它们分别解决服务位置、统一入口和声明式调用，但也引入 namespace、路由、超时和版本兼容问题。</p><p>这让我意识到组件只有放在故障链路里才有意义。注册中心不是为了控制台里出现服务名，而是让调用方能在实例变化后继续找到可用地址；网关不是为了统一加一个前缀，而是为了入口治理和流量控制。</p><h3 id="六月至七月：开始面对异步和高可用"><a href="#六月至七月：开始面对异步和高可用" class="headerlink" title="六月至七月：开始面对异步和高可用"></a>六月至七月：开始面对异步和高可用</h3><p>RabbitMQ 把非核心动作从订单主流程拆出去，响应更快了，但消息丢失、重复消费和最终一致性成为新的责任。Redis 主从和 Sentinel 提升了可用性，却仍然存在异步复制窗口、客户端重连和数据库回源风险。</p><p>从这一阶段开始，我不再把“中间件集群已部署”当成完成，而会继续追问：</p><ul><li>数据有没有可能丢。</li><li>故障以后多久恢复。</li><li>重试会不会制造重复结果。</li><li>整个组件不可用时业务怎么降级。</li></ul><h3 id="八月：让环境可以重复"><a href="#八月：让环境可以重复" class="headerlink" title="八月：让环境可以重复"></a>八月：让环境可以重复</h3><p>Docker 和 Compose 解决了本地、测试环境反复安装依赖的问题。镜像把运行环境固化，Compose 把服务关系写成配置。</p><p>但容器化不是把 Jar 放进镜像就结束。数据卷、容器网络、健康检查、日志轮转、资源边界和密钥管理都需要重新设计。环境一致性提高了，部署规范的重要性反而更高了。</p><h3 id="十月至十一月：补上可观测性"><a href="#十月至十一月：补上可观测性" class="headerlink" title="十月至十一月：补上可观测性"></a>十月至十一月：补上可观测性</h3><p>服务拆开以后，排查问题最明显的痛点出现了：不知道请求经过了哪些实例，也不知道系统是在偶发报错还是整体变慢。</p><p>ELK 让分散日志可以通过 traceId 关联，Prometheus 让请求速率、错误率、延迟和资源饱和度形成趋势。直到这一步，我才真正感受到“可观测性应该先于大规模拆分”。没有日志和指标，微服务的故障隔离只是架构图上的描述。</p><h2 id="单体和微服务不是二选一"><a href="#单体和微服务不是二选一" class="headerlink" title="单体和微服务不是二选一"></a>单体和微服务不是二选一</h2><p>现在再比较两者，我不会简单列优缺点，而会看系统正处于什么阶段。</p><table><thead><tr><th>判断维度</th><th>更适合模块化单体</th><th>可以考虑微服务</th></tr></thead><tbody><tr><td>团队</td><td>单一小团队</td><td>多团队需要独立交付</td></tr><tr><td>业务边界</td><td>仍在频繁变化</td><td>能形成稳定业务能力</td></tr><tr><td>发布</td><td>整体发布仍可接受</td><td>不同模块发布相互阻塞</td></tr><tr><td>资源</td><td>各模块负载接近</td><td>部分能力需要独立扩容</td></tr><tr><td>故障</td><td>影响范围可控</td><td>需要明确隔离关键链路</td></tr><tr><td>工程能力</td><td>自动化和监控较弱</td><td>CI&#x2F;CD、监控、治理已具备</td></tr></tbody></table><p>模块化单体不是失败的微服务。模块边界、数据所有权和依赖方向做得好，未来要拆时成本更低；如果业务一直不需要拆，它本身也可以是长期架构。</p><h2 id="这一年踩过的几个认知坑"><a href="#这一年踩过的几个认知坑" class="headerlink" title="这一年踩过的几个认知坑"></a>这一年踩过的几个认知坑</h2><h3 id="1-把“服务数量”当作架构成熟度"><a href="#1-把“服务数量”当作架构成熟度" class="headerlink" title="1. 把“服务数量”当作架构成熟度"></a>1. 把“服务数量”当作架构成熟度</h3><p>服务很多不代表边界合理。拆得过细会增加接口数量、部署单元和跨服务事务。成熟度更应该看发布成功率、恢复时间、故障影响范围和团队交付效率。</p><h3 id="2-先引入组件，再寻找问题"><a href="#2-先引入组件，再寻找问题" class="headerlink" title="2. 先引入组件，再寻找问题"></a>2. 先引入组件，再寻找问题</h3><p>注册中心、消息队列、缓存和监控都有适用场景。为了技术栈完整而引入，会让团队维护一组没有明确收益的基础设施。</p><p>更合理的顺序是：先描述当前痛点和目标，再选择最小能力解决它，并定义如何验证收益。</p><h3 id="3-认为重试可以提升稳定性"><a href="#3-认为重试可以提升稳定性" class="headerlink" title="3. 认为重试可以提升稳定性"></a>3. 认为重试可以提升稳定性</h3><p>没有超时预算、退避和幂等的重试，会把一次失败放大成多次流量。下游已经过载时，上游同时重试只会让恢复更慢。</p><p>重试是一种有成本的恢复手段，不是异常处理的默认模板。</p><h3 id="4-只设计正常流程"><a href="#4-只设计正常流程" class="headerlink" title="4. 只设计正常流程"></a>4. 只设计正常流程</h3><p>架构图通常只画成功箭头，但线上问题都发生在箭头中间：网络超时、部分成功、实例重启、消息重复、配置未刷新。</p><p>现在设计接口时，我会同时写失败状态、幂等键、补偿方式和可观测字段。能解释失败后的状态，比成功时少几毫秒更重要。</p><h3 id="5-低估团队协作成本"><a href="#5-低估团队协作成本" class="headerlink" title="5. 低估团队协作成本"></a>5. 低估团队协作成本</h3><p>服务边界也是团队边界。接口变更、错误码、数据库迁移和发布顺序都需要约定。没有明确负责人时，独立服务很容易变成“所有人都能改，但没人真正维护”。</p><h2 id="我现在理解的演进顺序"><a href="#我现在理解的演进顺序" class="headerlink" title="我现在理解的演进顺序"></a>我现在理解的演进顺序</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">整理单体模块边界</span><br><span class="line">  -&gt; 明确数据所有权和依赖方向</span><br><span class="line">  -&gt; 建立自动化测试、部署和回滚</span><br><span class="line">  -&gt; 补齐日志、指标和接口契约</span><br><span class="line">  -&gt; 拆出边界稳定且收益明确的服务</span><br><span class="line">  -&gt; 对真实故障做限流、降级、幂等和补偿</span><br><span class="line">  -&gt; 根据运行数据继续调整边界</span><br></pre></td></tr></table></figure><p>这不是所有项目必须照走的路线，但顺序背后有一个原则：先获得控制复杂度的能力，再增加分布式复杂度。</p><h2 id="还有哪些没有解决"><a href="#还有哪些没有解决" class="headerlink" title="还有哪些没有解决"></a>还有哪些没有解决</h2><p>到 2022 年底，系统能运行不等于治理已经成熟。下一阶段仍然要补：</p><ol><li>接口版本和兼容性管理，避免服务独立发布后相互破坏。</li><li>数据一致性对账，不能只依赖消息“应该会到”。</li><li>容量评估和压测，让限流阈值有数据依据。</li><li>告警分级和 Runbook，让监控真正进入故障处理。</li><li>交付指标，验证微服务是否真的缩短了发布时间。</li><li>定期故障演练，验证文档中的恢复方案是否可执行。</li></ol><h2 id="年末结论"><a href="#年末结论" class="headerlink" title="年末结论"></a>年末结论</h2><p>这一年最大的变化，不是系统从单体变成了微服务，而是看待架构的角度从“用了什么技术”转向“解决了什么约束”。技术组件可以快速学习，边界、数据和失败语义需要在实践中反复调整。</p><p>微服务不是一个终点。它只是当业务、团队和交付方式发展到一定阶段后，用来重新分配复杂度的手段。真正值得追求的仍然是可理解、可交付、可观察、可恢复的系统。只要这四件事在变好，架构就在向正确方向演进。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;2022 年最后一篇不再写组件配置，想回头看一看这一年的技术路线。年初我关心的是“一个系统怎么拆成多个服务”，到年末更关心“团队能不能长期维护拆完以后的复杂度”。这个变化比多会几个框架更重要。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;年</summary>
      
    
    
    
    <category term="架构成长" scheme="https://swagch.github.io/categories/%E6%9E%B6%E6%9E%84%E6%88%90%E9%95%BF/"/>
    
    
    <category term="年度总结" scheme="https://swagch.github.io/tags/%E5%B9%B4%E5%BA%A6%E6%80%BB%E7%BB%93/"/>
    
    <category term="系统设计" scheme="https://swagch.github.io/tags/%E7%B3%BB%E7%BB%9F%E8%AE%BE%E8%AE%A1/"/>
    
    <category term="微服务" scheme="https://swagch.github.io/tags/%E5%BE%AE%E6%9C%8D%E5%8A%A1/"/>
    
    <category term="架构演进" scheme="https://swagch.github.io/tags/%E6%9E%B6%E6%9E%84%E6%BC%94%E8%BF%9B/"/>
    
  </entry>
  
  <entry>
    <title>Prometheus监控部署</title>
    <link href="https://swagch.github.io/2022/11/20/2022-11-20-Prometheus%E7%9B%91%E6%8E%A7%E9%83%A8%E7%BD%B2/"/>
    <id>https://swagch.github.io/2022/11/20/2022-11-20-Prometheus%E7%9B%91%E6%8E%A7%E9%83%A8%E7%BD%B2/</id>
    <published>2022-11-20T12:29:47.000Z</published>
    <updated>2026-08-26T03:54:37.317Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>日志适合解释一次具体失败，指标适合回答系统是否正在变坏。Prometheus 建起来不难，难的是决定采集什么、标签怎么设计，以及什么情况值得在半夜叫醒值班人员。</p></blockquote><p>这篇按“指标、查询、面板、告警、运行”五个层次整理，不把部署成功等同于监控完成。</p><h2 id="1-先确定要回答的问题"><a href="#1-先确定要回答的问题" class="headerlink" title="1. 先确定要回答的问题"></a>1. 先确定要回答的问题</h2><p>对于一个订单服务，我希望监控能快速回答：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">现在有没有流量</span><br><span class="line">成功率是否下降</span><br><span class="line">用户等待时间是否变长</span><br><span class="line">哪个 URI 或下游导致异常</span><br><span class="line">线程池、连接池和 JVM 是否接近容量上限</span><br><span class="line">订单创建量与支付量是否出现业务偏差</span><br></pre></td></tr></table></figure><p>这些问题可以分成两套方法：</p><h3 id="RED：面向请求"><a href="#RED：面向请求" class="headerlink" title="RED：面向请求"></a>RED：面向请求</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Rate：请求速率</span><br><span class="line">Errors：错误比例</span><br><span class="line">Duration：耗时分布</span><br></pre></td></tr></table></figure><h3 id="USE：面向资源"><a href="#USE：面向资源" class="headerlink" title="USE：面向资源"></a>USE：面向资源</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Utilization：资源使用率</span><br><span class="line">Saturation：资源排队或饱和程度</span><br><span class="line">Errors：资源错误</span><br></pre></td></tr></table></figure><p>接口使用 RED，CPU、磁盘、连接池和线程池使用 USE。两者结合，才能区分“请求变慢是业务代码问题”还是“资源已经饱和”。</p><h2 id="2-Prometheus-链路"><a href="#2-Prometheus-链路" class="headerlink" title="2. Prometheus 链路"></a>2. Prometheus 链路</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Spring Boot + Micrometer</span><br><span class="line">  -&gt; /actuator/prometheus</span><br><span class="line">  -&gt; Prometheus 定时拉取</span><br><span class="line">  -&gt; PromQL 查询与规则计算</span><br><span class="line">  -&gt; Grafana 展示</span><br><span class="line">  -&gt; Alertmanager 分组、抑制和通知</span><br></pre></td></tr></table></figure><p>Prometheus 使用 Pull 模型。目标服务暴露当前累计指标，Prometheus 定期抓取并保存时间序列。应用不需要知道 Prometheus 地址，也不会因为监控端短暂不可用而阻塞业务请求。</p><p>批处理等短生命周期任务不适合机械套用 Pull，可以评估 Pushgateway，但 Pushgateway 中指标生命周期需要额外管理，不能把它当成所有业务指标的通用上报入口。</p><h2 id="3-Spring-Boot-接入"><a href="#3-Spring-Boot-接入" class="headerlink" title="3. Spring Boot 接入"></a>3. Spring Boot 接入</h2><p>JDK 8、Spring Boot 2.x 项目增加依赖：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-actuator<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>io.micrometer<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>micrometer-registry-prometheus<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p>配置：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">management:</span></span><br><span class="line">  <span class="attr">endpoints:</span></span><br><span class="line">    <span class="attr">web:</span></span><br><span class="line">      <span class="attr">exposure:</span></span><br><span class="line">        <span class="attr">include:</span> <span class="string">health,prometheus</span></span><br><span class="line">  <span class="attr">endpoint:</span></span><br><span class="line">    <span class="attr">health:</span></span><br><span class="line">      <span class="attr">show-details:</span> <span class="string">never</span></span><br><span class="line">  <span class="attr">metrics:</span></span><br><span class="line">    <span class="attr">tags:</span></span><br><span class="line">      <span class="attr">application:</span> <span class="string">order-service</span></span><br><span class="line">    <span class="attr">distribution:</span></span><br><span class="line">      <span class="attr">percentiles-histogram:</span></span><br><span class="line">        <span class="attr">http.server.requests:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure><p>不要为了方便暴露 <code>*</code>。Actuator 可能包含环境、配置和线程信息，应该通过独立管理端口、认证或网络策略限制访问。Prometheus 抓取端点不应直接暴露到公网。</p><p>验证：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">curl -s http://127.0.0.1:8080/actuator/health</span><br><span class="line">curl -s http://127.0.0.1:8080/actuator/prometheus | <span class="built_in">head</span> -30</span><br></pre></td></tr></table></figure><p>如果 Prometheus 端点不存在，先检查依赖和 exposure 配置；如果返回 401&#x2F;403，再检查安全规则，不要直接关闭所有鉴权。</p><h2 id="4-Prometheus-抓取配置"><a href="#4-Prometheus-抓取配置" class="headerlink" title="4. Prometheus 抓取配置"></a>4. Prometheus 抓取配置</h2><p>最小静态配置：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">global:</span></span><br><span class="line">  <span class="attr">scrape_interval:</span> <span class="string">15s</span></span><br><span class="line">  <span class="attr">evaluation_interval:</span> <span class="string">15s</span></span><br><span class="line"></span><br><span class="line"><span class="attr">scrape_configs:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">job_name:</span> <span class="string">order-service</span></span><br><span class="line">    <span class="attr">metrics_path:</span> <span class="string">/actuator/prometheus</span></span><br><span class="line">    <span class="attr">static_configs:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">targets:</span></span><br><span class="line">          <span class="bullet">-</span> <span class="string">order-service-1:8080</span></span><br><span class="line">          <span class="bullet">-</span> <span class="string">order-service-2:8080</span></span><br><span class="line">        <span class="attr">labels:</span></span><br><span class="line">          <span class="attr">env:</span> <span class="string">prod</span></span><br></pre></td></tr></table></figure><p>检查配置并热加载前先验证：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">promtool check config /etc/prometheus/prometheus.yml</span><br><span class="line">promtool check rules /etc/prometheus/rules/*.yml</span><br></pre></td></tr></table></figure><p>实例经常变化时，不应该长期手工维护静态地址。可以接入 Kubernetes、Consul、云平台或文件服务发现。服务发现标签很多，进入存储前要通过 relabel 只保留稳定且有查询价值的标签。</p><h2 id="5-指标名称和标签"><a href="#5-指标名称和标签" class="headerlink" title="5. 指标名称和标签"></a>5. 指标名称和标签</h2><p>一条时间序列由指标名和完整标签集合唯一确定：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">http_server_requests_seconds_count&#123;</span><br><span class="line">  application=&quot;order-service&quot;,</span><br><span class="line">  method=&quot;POST&quot;,</span><br><span class="line">  status=&quot;200&quot;,</span><br><span class="line">  uri=&quot;/orders&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>标签最危险的问题是高基数。下面这些值不能直接做 label：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">userId</span><br><span class="line">orderId</span><br><span class="line">traceId</span><br><span class="line">完整 URL</span><br><span class="line">异常 message</span><br><span class="line">时间戳</span><br></pre></td></tr></table></figure><p>每个不同值都会创建新时间序列。订单量上百万，就可能产生百万级序列，显著增加应用、Prometheus 和查询压力。</p><p>适合做标签的是有限枚举：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">service、env、method、status、规范化 uri、result、reason_type</span><br></pre></td></tr></table></figure><p>具体 orderId 和 traceId 留在日志中，指标负责发现趋势，日志负责定位单次事件。</p><h2 id="6-自定义业务指标"><a href="#6-自定义业务指标" class="headerlink" title="6. 自定义业务指标"></a>6. 自定义业务指标</h2><p>订单创建结果可以使用 Counter：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderMetrics</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> MeterRegistry registry;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">OrderMetrics</span><span class="params">(MeterRegistry registry)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.registry = registry;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">recordCreate</span><span class="params">(String result, String reasonType)</span> &#123;</span><br><span class="line">        Counter.builder(<span class="string">&quot;order_create_total&quot;</span>)</span><br><span class="line">            .tag(<span class="string">&quot;result&quot;</span>, result)</span><br><span class="line">            .tag(<span class="string">&quot;reason&quot;</span>, reasonType)</span><br><span class="line">            .register(registry)</span><br><span class="line">            .increment();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>reasonType</code> 必须是有限枚举，例如 <code>stock_shortage</code>、<code>payment_timeout</code>、<code>validation</code>，不能直接放异常文本。</p><p>频繁在业务方法里动态创建 Meter 会增加阅读成本。生产代码可以封装固定 Counter，或者通过统一埋点组件管理指标定义。指标名称、标签和语义变化都要经过评审，因为 Grafana 和告警规则依赖它们。</p><h2 id="7-四条基础-PromQL"><a href="#7-四条基础-PromQL" class="headerlink" title="7. 四条基础 PromQL"></a>7. 四条基础 PromQL</h2><h3 id="7-1-总-QPS"><a href="#7-1-总-QPS" class="headerlink" title="7.1 总 QPS"></a>7.1 总 QPS</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">sum(</span><br><span class="line">  rate(http_server_requests_seconds_count&#123;</span><br><span class="line">    application=&quot;order-service&quot;</span><br><span class="line">  &#125;[5m])</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>Counter 必须使用 <code>rate</code> 或 <code>increase</code> 计算变化，不要直接对累计值做告警。</p><h3 id="7-2-5xx-错误比例"><a href="#7-2-5xx-错误比例" class="headerlink" title="7.2 5xx 错误比例"></a>7.2 5xx 错误比例</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">sum(rate(http_server_requests_seconds_count&#123;</span><br><span class="line">  application=&quot;order-service&quot;,</span><br><span class="line">  status=~&quot;5..&quot;</span><br><span class="line">&#125;[5m]))</span><br><span class="line">/</span><br><span class="line">clamp_min(</span><br><span class="line">  sum(rate(http_server_requests_seconds_count&#123;</span><br><span class="line">    application=&quot;order-service&quot;</span><br><span class="line">  &#125;[5m])),</span><br><span class="line">  0.001</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>使用比例比固定错误数更合理。低流量服务还要设置最小请求量条件，否则偶发一个错误就会显示 100%。</p><h3 id="7-3-按-URI-计算-P95"><a href="#7-3-按-URI-计算-P95" class="headerlink" title="7.3 按 URI 计算 P95"></a>7.3 按 URI 计算 P95</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">histogram_quantile(</span><br><span class="line">  0.95,</span><br><span class="line">  sum by (le, uri) (</span><br><span class="line">    rate(http_server_requests_seconds_bucket&#123;</span><br><span class="line">      application=&quot;order-service&quot;</span><br><span class="line">    &#125;[5m])</span><br><span class="line">  )</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>只有启用了 Histogram Bucket，才能正确计算分位数。<code>uri</code> 应该是框架归一化后的路由模板，例如 <code>/orders/&#123;id&#125;</code>，不能是带真实订单号的 URL。</p><h3 id="7-4-订单创建失败速率"><a href="#7-4-订单创建失败速率" class="headerlink" title="7.4 订单创建失败速率"></a>7.4 订单创建失败速率</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">sum by (reason) (</span><br><span class="line">  rate(order_create_total&#123;result=&quot;failed&quot;&#125;[5m])</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>这条业务指标可以直接看出失败原因趋势，比只统计 HTTP 500 更接近用户感知。</p><h2 id="8-Grafana-面板怎么排"><a href="#8-Grafana-面板怎么排" class="headerlink" title="8. Grafana 面板怎么排"></a>8. Grafana 面板怎么排</h2><p>一个服务首页我会按排查顺序摆放：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">第一行：QPS、成功率、P95/P99</span><br><span class="line">第二行：各 URI 错误和耗时</span><br><span class="line">第三行：下游 HTTP、数据库、Redis、MQ</span><br><span class="line">第四行：JVM Heap、GC、线程、CPU</span><br><span class="line">第五行：连接池、线程池队列和拒绝数</span><br><span class="line">第六行：核心业务量和业务失败原因</span><br></pre></td></tr></table></figure><p>面板变量只提供有边界的选项，例如 <code>env</code>、<code>application</code> 和 <code>instance</code>。默认时间范围、单位和阈值颜色要统一，避免每个面板用不同口径。</p><p>图表标题应该表达问题，例如“订单接口 P95”比“HTTP Duration”更容易使用。Dashboard 需要存入版本库或通过平台 API 备份，不能只存在 Grafana 数据库里。</p><h2 id="9-告警规则要包含持续时间"><a href="#9-告警规则要包含持续时间" class="headerlink" title="9. 告警规则要包含持续时间"></a>9. 告警规则要包含持续时间</h2><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">groups:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">order-service</span></span><br><span class="line">    <span class="attr">rules:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">alert:</span> <span class="string">OrderServiceHighErrorRate</span></span><br><span class="line">        <span class="attr">expr:</span> <span class="string">|</span></span><br><span class="line"><span class="string">          sum(rate(http_server_requests_seconds_count&#123;</span></span><br><span class="line"><span class="string">            application=&quot;order-service&quot;,status=~&quot;5..&quot;</span></span><br><span class="line"><span class="string">          &#125;[5m]))</span></span><br><span class="line"><span class="string">          /</span></span><br><span class="line"><span class="string">          clamp_min(sum(rate(http_server_requests_seconds_count&#123;</span></span><br><span class="line"><span class="string">            application=&quot;order-service&quot;</span></span><br><span class="line"><span class="string">          &#125;[5m])), 0.001) &gt; 0.05</span></span><br><span class="line"><span class="string"></span>        <span class="attr">for:</span> <span class="string">10m</span></span><br><span class="line">        <span class="attr">labels:</span></span><br><span class="line">          <span class="attr">severity:</span> <span class="string">warning</span></span><br><span class="line">          <span class="attr">service:</span> <span class="string">order-service</span></span><br><span class="line">        <span class="attr">annotations:</span></span><br><span class="line">          <span class="attr">summary:</span> <span class="string">&quot;订单服务 5xx 比例持续超过 5%&quot;</span></span><br><span class="line">          <span class="attr">runbook:</span> <span class="string">&quot;https://runbook.example.com/order/high-error-rate&quot;</span></span><br></pre></td></tr></table></figure><p><code>for: 10m</code> 能过滤短暂毛刺。阈值只是示例，真实值要结合 SLO、历史基线和业务时段调整。</p><p>一条可执行的告警至少包含：</p><ul><li>哪个服务、环境和症状。</li><li>当前值与阈值。</li><li>相关 Dashboard 和日志入口。</li><li>Runbook 与升级联系人。</li><li>恢复通知和抑制规则。</li></ul><h2 id="10-告警为什么会失效"><a href="#10-告警为什么会失效" class="headerlink" title="10. 告警为什么会失效"></a>10. 告警为什么会失效</h2><h3 id="告警太多"><a href="#告警太多" class="headerlink" title="告警太多"></a>告警太多</h3><p>同一个根因触发 CPU、延迟、错误率和实例存活几十条通知，值班人员很快会忽略。Alertmanager 应按服务和故障域分组，并用抑制规则让根因告警覆盖症状告警。</p><h3 id="没有用户影响"><a href="#没有用户影响" class="headerlink" title="没有用户影响"></a>没有用户影响</h3><p>JVM Old 区 80% 不一定是故障，GC 后能回落就可能正常。相比单个资源阈值，持续的错误率和延迟更接近用户影响。资源告警适合作为容量预警，不一定都需要最高级别通知。</p><h3 id="标签变化导致规则静默"><a href="#标签变化导致规则静默" class="headerlink" title="标签变化导致规则静默"></a>标签变化导致规则静默</h3><p>框架升级后 <code>uri</code>、<code>status</code> 或指标名改变，查询可能返回空结果。规则也要测试，并对关键指标使用 <code>absent()</code> 检测缺失。</p><h3 id="没有最小流量条件"><a href="#没有最小流量条件" class="headerlink" title="没有最小流量条件"></a>没有最小流量条件</h3><p>凌晨只有一次请求且失败，错误率是 100%，但影响和高峰期完全不同。告警表达式需要结合请求量或使用燃烧率思路。</p><h2 id="11-监控系统本身也要监控"><a href="#11-监控系统本身也要监控" class="headerlink" title="11. 监控系统本身也要监控"></a>11. 监控系统本身也要监控</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Prometheus target 是否持续 up</span><br><span class="line">抓取耗时是否接近 scrape_interval</span><br><span class="line">规则计算是否失败</span><br><span class="line">本地磁盘和 TSDB 压缩是否正常</span><br><span class="line">时间序列数量和新增速度</span><br><span class="line">Alertmanager 通知是否成功</span><br><span class="line">Grafana 数据源查询是否异常</span><br></pre></td></tr></table></figure><p>Prometheus 本地存储适合一定规模和保留周期。需要长期保存、跨集群查询或更高可用时，可以评估远程存储方案，但先解决标签治理，否则只是把高基数问题搬到更大的集群。</p><h2 id="12-上线检查"><a href="#12-上线检查" class="headerlink" title="12. 上线检查"></a>12. 上线检查</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">[ ] Actuator 端点不暴露公网</span><br><span class="line">[ ] target 标签能区分服务、环境和实例</span><br><span class="line">[ ] HTTP Histogram 已按需开启</span><br><span class="line">[ ] 没有 userId、orderId、traceId 等高基数标签</span><br><span class="line">[ ] Dashboard 覆盖 RED、资源和业务指标</span><br><span class="line">[ ] 告警有 for、分级、Runbook 和恢复通知</span><br><span class="line">[ ] 规则通过 promtool 检查</span><br><span class="line">[ ] Prometheus 和 Alertmanager 自身有监控</span><br></pre></td></tr></table></figure><h2 id="最后的理解"><a href="#最后的理解" class="headerlink" title="最后的理解"></a>最后的理解</h2><p>Prometheus 最有价值的地方不是收集了多少指标，而是把“系统健康”变成了可以讨论和验证的数字。QPS、错误率、延迟和饱和度形成趋势以后，很多问题会在用户反馈前出现信号。</p><p>日志、指标和链路各有职责：指标发现异常，链路缩小范围，日志解释细节。把三者通过服务名、时间和 traceId 关联起来，才是一套真正可用于故障处理的可观测体系。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;日志适合解释一次具体失败，指标适合回答系统是否正在变坏。Prometheus 建起来不难，难的是决定采集什么、标签怎么设计，以及什么情况值得在半夜叫醒值班人员。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;这篇按“指标、查询、面板、告警、运行”五个层</summary>
      
    
    
    
    <category term="架构成长" scheme="https://swagch.github.io/categories/%E6%9E%B6%E6%9E%84%E6%88%90%E9%95%BF/"/>
    
    
    <category term="可观测性" scheme="https://swagch.github.io/tags/%E5%8F%AF%E8%A7%82%E6%B5%8B%E6%80%A7/"/>
    
    <category term="Prometheus" scheme="https://swagch.github.io/tags/Prometheus/"/>
    
    <category term="监控" scheme="https://swagch.github.io/tags/%E7%9B%91%E6%8E%A7/"/>
    
    <category term="Grafana" scheme="https://swagch.github.io/tags/Grafana/"/>
    
  </entry>
  
  <entry>
    <title>ELK日志平台搭建</title>
    <link href="https://swagch.github.io/2022/10/23/2022-10-23-ELK%E6%97%A5%E5%BF%97%E5%B9%B3%E5%8F%B0%E6%90%AD%E5%BB%BA/"/>
    <id>https://swagch.github.io/2022/10/23/2022-10-23-ELK%E6%97%A5%E5%BF%97%E5%B9%B3%E5%8F%B0%E6%90%AD%E5%BB%BA/</id>
    <published>2022-10-23T13:55:04.000Z</published>
    <updated>2026-08-26T03:54:37.317Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>这篇记录一次日志平台从“能收集”到“能排查”的建设过程。严格来说，加入 Filebeat 后常被称为 Elastic Stack。组件启动只是基础，真正决定平台质量的是日志字段、索引映射、保留策略和采集链路自身的可靠性。</p></blockquote><h2 id="1-为什么不继续登录服务器-grep"><a href="#1-为什么不继续登录服务器-grep" class="headerlink" title="1. 为什么不继续登录服务器 grep"></a>1. 为什么不继续登录服务器 grep</h2><p>单体阶段，一台机器上的 <code>app.log</code> 基本能还原请求。服务拆分后，一次订单请求可能经过：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Gateway</span><br><span class="line">  -&gt; order-service</span><br><span class="line">      -&gt; stock-service</span><br><span class="line">      -&gt; payment-service</span><br><span class="line">      -&gt; RabbitMQ consumer</span><br></pre></td></tr></table></figure><p>如果每个服务有三个实例，就要在十几份文件里按时间猜测。服务器滚动发布后，旧容器日志还可能已经被删除。</p><p>日志平台希望解决四件事：</p><ol><li>集中检索，不依赖登录生产服务器。</li><li>通过 traceId 串起跨服务请求。</li><li>按服务、级别、业务编号和异常类型过滤。</li><li>对错误趋势、采集延迟和存储容量建立监控。</li></ol><p>它不应该成为无限期保存全部文本的仓库。日志有成本，也可能包含敏感数据，必须从一开始设计范围和生命周期。</p><h2 id="2-组件职责"><a href="#2-组件职责" class="headerlink" title="2. 组件职责"></a>2. 组件职责</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Application：生成结构化日志</span><br><span class="line">Filebeat：读取文件、记录采集位置、轻量转发</span><br><span class="line">Logstash：解析、清洗、补充和路由</span><br><span class="line">Elasticsearch：索引和检索</span><br><span class="line">Kibana：查询、可视化和管理</span><br></pre></td></tr></table></figure><p>小规模场景可以让 Filebeat 直接写 Elasticsearch，减少 Logstash 成本。需要复杂字段清洗、多目标路由或统一入口时，再加入 Logstash。组件不是越齐全越专业，每增加一层都要处理它的容量和故障。</p><p>Elastic Stack 各组件尽量保持同一大版本，并核对官方兼容说明。Elasticsearch、Kibana、Logstash 和 Filebeat 跨大版本混用，常见问题包括协议、模板和安全默认值不一致。</p><h2 id="3-先统一应用日志"><a href="#3-先统一应用日志" class="headerlink" title="3. 先统一应用日志"></a>3. 先统一应用日志</h2><p>目标事件：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;timestamp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2022-10-23T21:00:00.123+08:00&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;level&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ERROR&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;service&quot;</span><span class="punctuation">:</span> <span class="string">&quot;order-service&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;env&quot;</span><span class="punctuation">:</span> <span class="string">&quot;prod&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;traceId&quot;</span><span class="punctuation">:</span> <span class="string">&quot;4d61c188f9d24b76&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;spanId&quot;</span><span class="punctuation">:</span> <span class="string">&quot;5a8b2c11&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;thread&quot;</span><span class="punctuation">:</span> <span class="string">&quot;http-nio-8080-exec-7&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;logger&quot;</span><span class="punctuation">:</span> <span class="string">&quot;com.example.order.OrderService&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;bizId&quot;</span><span class="punctuation">:</span> <span class="string">&quot;order-20221023001&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;message&quot;</span><span class="punctuation">:</span> <span class="string">&quot;create order failed&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;exception&quot;</span><span class="punctuation">:</span> <span class="string">&quot;java.sql.SQLTimeoutException: query timeout&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>建议字段分为三类：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">固定技术字段：timestamp、level、service、env、traceId、thread</span><br><span class="line">业务检索字段：bizId、userType、operation，不记录敏感原值</span><br><span class="line">文本字段：message、exception</span><br></pre></td></tr></table></figure><p>不要把用户手机号、身份证、Token、密码和完整请求体直接写入日志。需要定位用户时，可以记录脱敏值或内部不可逆标识。</p><p>Java 服务通过 MDC 放入 traceId：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">doFilter</span><span class="params">(ServletRequest request, ServletResponse response,</span></span><br><span class="line"><span class="params">                     FilterChain chain)</span> <span class="keyword">throws</span> IOException, ServletException &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">traceId</span> <span class="operator">=</span> UUID.randomUUID().toString().replace(<span class="string">&quot;-&quot;</span>, <span class="string">&quot;&quot;</span>);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        MDC.put(<span class="string">&quot;traceId&quot;</span>, traceId);</span><br><span class="line">        chain.doFilter(request, response);</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="comment">// 线程池会复用线程，必须清理 MDC</span></span><br><span class="line">        MDC.remove(<span class="string">&quot;traceId&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>实际项目应优先复用网关或链路追踪组件传入的 traceId，而不是每个服务重新生成。异步线程和消息消费还要显式传递上下文。</p><h2 id="4-一行一个-JSON-事件"><a href="#4-一行一个-JSON-事件" class="headerlink" title="4. 一行一个 JSON 事件"></a>4. 一行一个 JSON 事件</h2><p>Java 异常栈天然多行，普通文本采集需要复杂的 multiline 规则。更稳定的方式是由日志框架输出单行 JSON，把异常栈作为字符串字段。</p><p>这样 Filebeat 不负责猜测一条事件从哪里开始，能减少错拼和漏拼。代价是单行会很长，因此日志框架、Filebeat 和 Elasticsearch 都要设置合理的单事件大小限制。</p><p>应用应保留本地滚动策略，Filebeat 不是日志轮转工具：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">按大小或时间切割</span><br><span class="line">限制保留天数和文件数量</span><br><span class="line">压缩历史文件</span><br><span class="line">确保 Filebeat 读取完成后再删除</span><br></pre></td></tr></table></figure><h2 id="5-Filebeat-采集配置"><a href="#5-Filebeat-采集配置" class="headerlink" title="5. Filebeat 采集配置"></a>5. Filebeat 采集配置</h2><p>以 Filebeat 7.x 常见的 <code>log</code> input 为例：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">filebeat.inputs:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">type:</span> <span class="string">log</span></span><br><span class="line">    <span class="attr">enabled:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">paths:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/data/logs/order-service/*.json</span></span><br><span class="line">    <span class="attr">json.keys_under_root:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">json.add_error_key:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">fields:</span></span><br><span class="line">      <span class="attr">source_type:</span> <span class="string">spring-boot</span></span><br><span class="line">    <span class="attr">fields_under_root:</span> <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="attr">output.logstash:</span></span><br><span class="line">  <span class="attr">hosts:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">logstash-1:5044</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">logstash-2:5044</span></span><br><span class="line">  <span class="attr">loadbalance:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">compression_level:</span> <span class="number">3</span></span><br></pre></td></tr></table></figure><p>Filebeat 8.x 更推荐 <code>filestream</code> input，JSON parser 写法也会变化。升级时需要按目标版本迁移，不能只把 <code>type</code> 改名。</p><p>检查配置并前台调试：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">filebeat <span class="built_in">test</span> config -e</span><br><span class="line">filebeat <span class="built_in">test</span> output -e</span><br><span class="line">filebeat -e -d <span class="string">&#x27;input,publish&#x27;</span></span><br></pre></td></tr></table></figure><p>调试日志量很大，只在受控时间短暂开启。Filebeat registry 保存文件读取位置，不能随意删除，否则可能导致历史日志重新采集。</p><h2 id="6-Logstash-只做必要加工"><a href="#6-Logstash-只做必要加工" class="headerlink" title="6. Logstash 只做必要加工"></a>6. Logstash 只做必要加工</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">input &#123;</span><br><span class="line">  beats &#123;</span><br><span class="line">    port =&gt; 5044</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">filter &#123;</span><br><span class="line">  date &#123;</span><br><span class="line">    match =&gt; [&quot;timestamp&quot;, &quot;ISO8601&quot;]</span><br><span class="line">    target =&gt; &quot;@timestamp&quot;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  mutate &#123;</span><br><span class="line">    remove_field =&gt; [&quot;agent&quot;, &quot;ecs&quot;, &quot;host&quot;, &quot;input&quot;]</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">output &#123;</span><br><span class="line">  elasticsearch &#123;</span><br><span class="line">    hosts =&gt; [&quot;https://es-1:9200&quot;, &quot;https://es-2:9200&quot;]</span><br><span class="line">    index =&gt; &quot;app-log-%&#123;+YYYY.MM.dd&#125;&quot;</span><br><span class="line">    user =&gt; &quot;$&#123;ES_USER&#125;&quot;</span><br><span class="line">    password =&gt; &quot;$&#123;ES_PASSWORD&#125;&quot;</span><br><span class="line">    ssl =&gt; true</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>示例省略了证书路径，生产环境必须正确校验证书，不能为了连通直接关闭 TLS 校验。</p><p>应用已经输出 JSON 时，Filebeat 可以把字段展开，Logstash 不需要再对 <code>message</code> 做一次 JSON 解析。重复解析会增加 CPU，也容易产生字段覆盖。</p><p>复杂 Grok 正则要谨慎。一个低效表达式在日志量升高时可能打满 Logstash CPU。能在应用端结构化的字段，不要留到采集端猜。</p><h2 id="7-映射决定日志是否可用"><a href="#7-映射决定日志是否可用" class="headerlink" title="7. 映射决定日志是否可用"></a>7. 映射决定日志是否可用</h2><p>Elasticsearch 动态映射很方便，也很危险。不同服务把同一个字段 <code>cost</code> 分别写成字符串和数字，会造成映射冲突；动态业务键会制造大量字段。</p><p>一个简化模板：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;index_patterns&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;app-log-*&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;template&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;mappings&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;properties&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;@timestamp&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;date&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;level&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;keyword&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;service&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;keyword&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;env&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;keyword&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;traceId&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;keyword&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;bizId&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;keyword&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;message&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;text&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;exception&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;text&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;index&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;costMs&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;long&quot;</span> <span class="punctuation">&#125;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p><code>keyword</code> 适合精确过滤和聚合，<code>text</code> 适合全文检索。异常栈通常只需要展示，可以不建立倒排索引，节省存储。是否关闭索引要根据真实查询需求决定。</p><h2 id="8-不要只按天创建索引"><a href="#8-不要只按天创建索引" class="headerlink" title="8. 不要只按天创建索引"></a>8. 不要只按天创建索引</h2><p>按天索引容易理解，但日志量变化后会出现两个问题：小服务产生大量小索引，大服务单日索引又可能过大。</p><p>更长期的方案是使用 ILM 或 Data Stream：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Hot：持续写入和高频查询</span><br><span class="line">Warm：只读，降低资源规格</span><br><span class="line">Delete：超过业务保留期自动删除</span><br></pre></td></tr></table></figure><p>滚动条件可以综合主分片大小、文档数和时间，而不是固定每天零点。不同 Elastic 版本的数据流、模板和 ILM 配置方式不同，需要按实际版本设计。</p><p>容量估算至少包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">每日原始日志量</span><br><span class="line">JSON 和索引膨胀系数</span><br><span class="line">副本数量</span><br><span class="line">保留天数</span><br><span class="line">磁盘高水位预留</span><br><span class="line">故障恢复空间</span><br></pre></td></tr></table></figure><p>不能等磁盘达到 90% 才第一次讨论保留周期。</p><h2 id="9-Kibana-中怎么查"><a href="#9-Kibana-中怎么查" class="headerlink" title="9. Kibana 中怎么查"></a>9. Kibana 中怎么查</h2><p>常用 KQL 示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">traceId : &quot;4d61c188f9d24b76&quot;</span><br><span class="line">service : &quot;order-service&quot; and level : &quot;ERROR&quot;</span><br><span class="line">bizId : &quot;order-20221023001&quot;</span><br><span class="line">service : &quot;payment-service&quot; and message : &quot;timeout&quot;</span><br></pre></td></tr></table></figure><p>一个有效的排查流程是：先按业务时间和 bizId 找入口日志，获得 traceId；再按 traceId 展开所有服务；最后比较每段耗时、错误码和异常栈。</p><p>Kibana Saved Search 和 Dashboard 也需要版本管理。只存在某个人浏览器收藏里的查询，不算团队能力。</p><h2 id="10-采集链路也会丢日志"><a href="#10-采集链路也会丢日志" class="headerlink" title="10. 采集链路也会丢日志"></a>10. 采集链路也会丢日志</h2><p>需要监控：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Filebeat：harvester、publish 失败、输出阻塞、registry 异常</span><br><span class="line">Logstash：事件吞吐、filter 耗时、persistent queue、失败事件</span><br><span class="line">Elasticsearch：写入拒绝、分片、磁盘水位、索引延迟</span><br><span class="line">Kibana：查询错误和慢查询</span><br></pre></td></tr></table></figure><p>Logstash 可以开启 Persistent Queue 缓冲 Elasticsearch 短暂不可用，但它不是无限队列。Filebeat 本地文件保留时间也要覆盖最长故障窗口，否则输出恢复前日志文件已经被应用删除。</p><p>对于解析失败事件，单独路由到错误索引：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">output &#123;</span><br><span class="line">  if &quot;_dateparsefailure&quot; in [tags] or [error] &#123;</span><br><span class="line">    elasticsearch &#123;</span><br><span class="line">      hosts =&gt; [&quot;https://es-1:9200&quot;]</span><br><span class="line">      index =&gt; &quot;app-log-parse-failed-%&#123;+YYYY.MM.dd&#125;&quot;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    elasticsearch &#123;</span><br><span class="line">      hosts =&gt; [&quot;https://es-1:9200&quot;]</span><br><span class="line">      index =&gt; &quot;app-log-%&#123;+YYYY.MM.dd&#125;&quot;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>错误索引要有告警和短保留期，不能让失败数据静默堆积。</p><h2 id="11-上线前的日志规范"><a href="#11-上线前的日志规范" class="headerlink" title="11. 上线前的日志规范"></a>11. 上线前的日志规范</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">[ ] 所有服务输出统一时间格式和时区</span><br><span class="line">[ ] traceId 能跨网关、Feign 和消息队列传递</span><br><span class="line">[ ] ERROR 包含异常栈，不只打印 getMessage()</span><br><span class="line">[ ] 敏感字段已脱敏或禁止记录</span><br><span class="line">[ ] 字段类型写入索引模板</span><br><span class="line">[ ] 应用日志和索引都有保留策略</span><br><span class="line">[ ] 采集延迟、解析失败和磁盘水位有告警</span><br><span class="line">[ ] 有按 traceId 和业务 ID 的共享查询模板</span><br></pre></td></tr></table></figure><h2 id="建设后的变化"><a href="#建设后的变化" class="headerlink" title="建设后的变化"></a>建设后的变化</h2><p>ELK 搭建完成后，最大的变化不是“搜索速度更快”，而是排查过程开始有统一入口。开发不再先问日志在哪台机器，而是先拿到时间、业务编号和 traceId。</p><p>但平台不会自动产生好日志。字段随意、异常栈缺失、敏感信息失控时，集中存储只会把问题放大。应用日志规范、采集可靠性和生命周期管理三者一起落地，日志平台才从一个大搜索框变成真正的故障分析工具。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;这篇记录一次日志平台从“能收集”到“能排查”的建设过程。严格来说，加入 Filebeat 后常被称为 Elastic Stack。组件启动只是基础，真正决定平台质量的是日志字段、索引映射、保留策略和采集链路自身的可靠性。&lt;/p&gt;
&lt;/blockqu</summary>
      
    
    
    
    <category term="架构成长" scheme="https://swagch.github.io/categories/%E6%9E%B6%E6%9E%84%E6%88%90%E9%95%BF/"/>
    
    
    <category term="ELK" scheme="https://swagch.github.io/tags/ELK/"/>
    
    <category term="日志平台" scheme="https://swagch.github.io/tags/%E6%97%A5%E5%BF%97%E5%B9%B3%E5%8F%B0/"/>
    
    <category term="Elasticsearch" scheme="https://swagch.github.io/tags/Elasticsearch/"/>
    
    <category term="Logstash" scheme="https://swagch.github.io/tags/Logstash/"/>
    
  </entry>
  
  <entry>
    <title>Docker基础部署，Docker Compose实践</title>
    <link href="https://swagch.github.io/2022/08/14/2022-08-14-Docker%E5%9F%BA%E7%A1%80%E9%83%A8%E7%BD%B2%EF%BC%8CDocker%20Compose%E5%AE%9E%E8%B7%B5/"/>
    <id>https://swagch.github.io/2022/08/14/2022-08-14-Docker%E5%9F%BA%E7%A1%80%E9%83%A8%E7%BD%B2%EF%BC%8CDocker%20Compose%E5%AE%9E%E8%B7%B5/</id>
    <published>2022-08-14T09:42:26.000Z</published>
    <updated>2026-08-26T03:54:37.317Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>这篇做一个可以实际运行的部署练习：把 JDK 8 的 Spring Boot 应用构建成镜像，再使用 Docker Compose 启动应用、MySQL 和 Redis。重点不只是 <code>up -d</code>，还包括网络、健康检查、数据卷、日志和排障。</p></blockquote><h2 id="0-最终目录"><a href="#0-最终目录" class="headerlink" title="0. 最终目录"></a>0. 最终目录</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">demo-app</span><br><span class="line">├── Dockerfile</span><br><span class="line">├── .dockerignore</span><br><span class="line">├── compose.yaml</span><br><span class="line">├── .env.example</span><br><span class="line">├── pom.xml</span><br><span class="line">└── src</span><br></pre></td></tr></table></figure><p>Docker Compose V2 推荐使用 <code>docker compose</code> 命令。旧环境可能仍然使用独立的 <code>docker-compose</code>。新版 Compose 文件可以省略顶层 <code>version</code>，旧项目中的 <code>version: &quot;3.8&quot;</code> 仍然常见，但不要把它和 Docker Engine 版本混为一谈。</p><h2 id="1-先理解四个对象"><a href="#1-先理解四个对象" class="headerlink" title="1. 先理解四个对象"></a>1. 先理解四个对象</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Image：只读构建产物</span><br><span class="line">Container：镜像运行后的进程和隔离环境</span><br><span class="line">Network：容器之间的通信空间和 DNS</span><br><span class="line">Volume：独立于容器生命周期的数据存储</span><br></pre></td></tr></table></figure><p>容器不是轻量虚拟机。一个容器通常运行一个主要前台进程，进程退出，容器也会停止。容器内写入层可以保存临时文件，但删除容器后数据会消失，因此 MySQL 数据必须放在 Volume。</p><h2 id="2-为-Spring-Boot-编写-Dockerfile"><a href="#2-为-Spring-Boot-编写-Dockerfile" class="headerlink" title="2. 为 Spring Boot 编写 Dockerfile"></a>2. 为 Spring Boot 编写 Dockerfile</h2><p>使用多阶段构建，第一阶段通过 Maven 打包，第二阶段只保留 JRE 和 Jar：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> maven:<span class="number">3.8</span>.<span class="number">8</span>-eclipse-temurin-<span class="number">8</span> AS build</span><br><span class="line"></span><br><span class="line"><span class="keyword">WORKDIR</span><span class="language-bash"> /workspace</span></span><br><span class="line"><span class="keyword">COPY</span><span class="language-bash"> pom.xml ./</span></span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> mvn -B dependency:go-offline</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">COPY</span><span class="language-bash"> src ./src</span></span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> mvn -B -DskipTests package</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">FROM</span> eclipse-temurin:<span class="number">8</span>-jre</span><br><span class="line"></span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> groupadd --system app \</span></span><br><span class="line"><span class="language-bash">    &amp;&amp; useradd --system --gid app --home-dir /app app</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">WORKDIR</span><span class="language-bash"> /app</span></span><br><span class="line"><span class="keyword">COPY</span><span class="language-bash"> --from=build /workspace/target/*.jar /app/app.jar</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">USER</span> app</span><br><span class="line"><span class="keyword">EXPOSE</span> <span class="number">8080</span></span><br><span class="line"><span class="keyword">ENTRYPOINT</span><span class="language-bash"> [<span class="string">&quot;java&quot;</span>, <span class="string">&quot;-jar&quot;</span>, <span class="string">&quot;/app/app.jar&quot;</span>]</span></span><br></pre></td></tr></table></figure><p>这里有几个目的：</p><ol><li>构建环境和运行环境分开，运行镜像不包含 Maven 缓存和源代码。</li><li>先复制 <code>pom.xml</code> 下载依赖，依赖不变时可以复用构建缓存。</li><li>应用使用非 root 用户运行。</li><li>Jar 名称在镜像中固定为 <code>app.jar</code>，启动命令不依赖版本号。</li></ol><p>基础镜像标签应该固定到经过验证的版本或 digest。示例标签用于说明结构，正式项目需要经过漏洞扫描和升级流程。</p><p>如果项目打包目录里可能有多个 Jar，<code>target/*.jar</code> 会匹配多个文件并导致构建失败。可以在 Maven 中固定 <code>finalName</code>，或者在构建脚本中明确选择可执行 Jar。</p><h2 id="3-减少无关构建上下文"><a href="#3-减少无关构建上下文" class="headerlink" title="3. 减少无关构建上下文"></a>3. 减少无关构建上下文</h2><p><code>.dockerignore</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">.git</span><br><span class="line">.idea</span><br><span class="line">target</span><br><span class="line">logs</span><br><span class="line">*.iml</span><br><span class="line">README.md</span><br><span class="line">compose.yaml</span><br><span class="line">.env</span><br></pre></td></tr></table></figure><p>Docker 构建会把上下文发送给 daemon。忽略 <code>target</code>、日志和本地环境文件可以提升速度，也能避免把密钥误打进镜像层。</p><p>构建并检查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">docker build -t demo-app:1.0.0 .</span><br><span class="line">docker image inspect demo-app:1.0.0</span><br><span class="line">docker <span class="built_in">history</span> demo-app:1.0.0</span><br></pre></td></tr></table></figure><p>不要只使用永久不变的 <code>latest</code>。至少同时保留不可变版本标签，方便回滚和追踪实际部署内容。</p><h2 id="4-编写-Compose"><a href="#4-编写-Compose" class="headerlink" title="4. 编写 Compose"></a>4. 编写 Compose</h2><p><code>.env.example</code> 只保留变量名和示例值，真实 <code>.env</code> 不提交：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">MYSQL_ROOT_PASSWORD=change-me</span><br><span class="line">MYSQL_APP_PASSWORD=change-me</span><br></pre></td></tr></table></figure><p><code>compose.yaml</code>：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">mysql:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">mysql:8.0.36</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">MYSQL_ROOT_PASSWORD:</span> <span class="string">$&#123;MYSQL_ROOT_PASSWORD&#125;</span></span><br><span class="line">      <span class="attr">MYSQL_DATABASE:</span> <span class="string">demo</span></span><br><span class="line">      <span class="attr">MYSQL_USER:</span> <span class="string">demo</span></span><br><span class="line">      <span class="attr">MYSQL_PASSWORD:</span> <span class="string">$&#123;MYSQL_APP_PASSWORD&#125;</span></span><br><span class="line">      <span class="attr">TZ:</span> <span class="string">Asia/Shanghai</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">mysql-data:/var/lib/mysql</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD-SHELL&quot;</span>, <span class="string">&quot;mysqladmin ping -h 127.0.0.1 -p$$&#123;MYSQL_ROOT_PASSWORD&#125;&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">10s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">10</span></span><br><span class="line">      <span class="attr">start_period:</span> <span class="string">30s</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">redis:6.2.14</span></span><br><span class="line">    <span class="attr">command:</span> [<span class="string">&quot;redis-server&quot;</span>, <span class="string">&quot;--appendonly&quot;</span>, <span class="string">&quot;yes&quot;</span>]</span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">redis-data:/data</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD&quot;</span>, <span class="string">&quot;redis-cli&quot;</span>, <span class="string">&quot;ping&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">10s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">3s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">5</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">app:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">demo-app:1.0.0</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">SPRING_DATASOURCE_URL:</span> <span class="string">jdbc:mysql://mysql:3306/demo?useUnicode=true&amp;characterEncoding=utf8&amp;serverTimezone=Asia/Shanghai</span></span><br><span class="line">      <span class="attr">SPRING_DATASOURCE_USERNAME:</span> <span class="string">demo</span></span><br><span class="line">      <span class="attr">SPRING_DATASOURCE_PASSWORD:</span> <span class="string">$&#123;MYSQL_APP_PASSWORD&#125;</span></span><br><span class="line">      <span class="attr">SPRING_REDIS_HOST:</span> <span class="string">redis</span></span><br><span class="line">      <span class="attr">SPRING_REDIS_PORT:</span> <span class="number">6379</span></span><br><span class="line">      <span class="attr">JAVA_TOOL_OPTIONS:</span> <span class="string">-Xms256m</span> <span class="string">-Xmx512m</span> <span class="string">-XX:+ExitOnOutOfMemoryError</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="attr">mysql:</span></span><br><span class="line">        <span class="attr">condition:</span> <span class="string">service_healthy</span></span><br><span class="line">      <span class="attr">redis:</span></span><br><span class="line">        <span class="attr">condition:</span> <span class="string">service_healthy</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;8080:8080&quot;</span></span><br><span class="line">    <span class="attr">logging:</span></span><br><span class="line">      <span class="attr">driver:</span> <span class="string">json-file</span></span><br><span class="line">      <span class="attr">options:</span></span><br><span class="line">        <span class="attr">max-size:</span> <span class="string">100m</span></span><br><span class="line">        <span class="attr">max-file:</span> <span class="string">&quot;5&quot;</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">mysql-data:</span></span><br><span class="line">  <span class="attr">redis-data:</span></span><br></pre></td></tr></table></figure><p>示例没有把 MySQL 和 Redis 端口映射到宿主机，因为只有应用容器需要访问它们。减少端口暴露可以降低误访问风险。本地确实需要客户端连接时，再显式增加 <code>127.0.0.1:3306:3306</code>，避免监听所有网卡。</p><p><code>depends_on</code> 的健康条件需要较新的 Compose 实现。旧版本可能只保证启动顺序，不等待服务真正可用，因此应用自身仍应有有限重试和明确的启动失败日志。</p><p><code>-XX:+ExitOnOutOfMemoryError</code> 需要较新的 JDK 8 update，使用前要通过实际基础镜像验证。JVM 堆也不能等于容器全部内存，还要给 Metaspace、线程栈和堆外内存留空间。</p><h2 id="5-为什么容器里不能用-localhost"><a href="#5-为什么容器里不能用-localhost" class="headerlink" title="5. 为什么容器里不能用 localhost"></a>5. 为什么容器里不能用 localhost</h2><p>Compose 默认会创建一个项目网络，服务名自动成为 DNS 名称：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">app -&gt; mysql:3306</span><br><span class="line">app -&gt; redis:6379</span><br></pre></td></tr></table></figure><p>应用容器里的 <code>localhost</code> 只指向应用容器自己，不是宿主机，也不是 MySQL 容器。因此：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">SPRING_DATASOURCE_URL:</span> <span class="string">jdbc:mysql://mysql:3306/demo</span></span><br><span class="line"><span class="attr">SPRING_REDIS_HOST:</span> <span class="string">redis</span></span><br></pre></td></tr></table></figure><p>从应用容器检查 DNS 和端口：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker compose <span class="built_in">exec</span> app getent hosts mysql</span><br><span class="line">docker run --<span class="built_in">rm</span> --network demo-app_default busybox:1.36.1 nc -zv mysql 3306</span><br></pre></td></tr></table></figure><p>基础镜像不一定包含 <code>curl</code>、<code>ping</code> 或 <code>nc</code>。调试时不要为了一个命令永久把大量工具装进运行镜像，可以临时启动同网络的诊断容器。示例网络名会随 Compose 项目名变化，应先通过 <code>docker network ls</code> 确认。</p><h2 id="6-启动与验证"><a href="#6-启动与验证" class="headerlink" title="6. 启动与验证"></a>6. 启动与验证</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">docker compose config</span><br><span class="line">docker compose up -d</span><br><span class="line">docker compose ps</span><br><span class="line">docker compose logs --<span class="built_in">tail</span>=200 app</span><br></pre></td></tr></table></figure><p><code>docker compose config</code> 会解析变量并校验最终配置。输出可能包含展开后的密码，不要把完整结果贴到公开工单或日志平台。</p><p>验证健康接口：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">curl http://127.0.0.1:8080/actuator/health</span><br></pre></td></tr></table></figure><p>再查看容器资源：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker stats --no-stream</span><br><span class="line">docker compose top</span><br></pre></td></tr></table></figure><p>启动成功的标准不是三个容器都显示 <code>Up</code>，而是应用健康检查正常，数据库连接可用，Redis 读写成功，并且日志没有持续重连。</p><h2 id="7-验证数据卷"><a href="#7-验证数据卷" class="headerlink" title="7. 验证数据卷"></a>7. 验证数据卷</h2><p>先写入一条测试数据，再重建容器：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker compose down</span><br><span class="line">docker compose up -d</span><br></pre></td></tr></table></figure><p>不带 <code>-v</code> 时，命名 Volume 会保留。确认数据仍存在以后，再检查卷位置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker volume <span class="built_in">ls</span></span><br><span class="line">docker volume inspect demo-app_mysql-data</span><br></pre></td></tr></table></figure><p>下面的命令会同时删除容器和声明的 Volume，数据不可恢复：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker compose down -v</span><br></pre></td></tr></table></figure><p>只能在确认数据可删除的开发环境使用。生产数据还需要独立备份，Volume 本身不是备份。</p><h2 id="8-五个常见故障"><a href="#8-五个常见故障" class="headerlink" title="8. 五个常见故障"></a>8. 五个常见故障</h2><h3 id="8-1-容器反复重启"><a href="#8-1-容器反复重启" class="headerlink" title="8.1 容器反复重启"></a>8.1 容器反复重启</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">docker compose ps</span><br><span class="line">APP_CONTAINER=$(docker compose ps -q app)</span><br><span class="line">docker inspect --format <span class="string">&#x27;&#123;&#123;.State.ExitCode&#125;&#125; &#123;&#123;.State.Error&#125;&#125;&#x27;</span> <span class="string">&quot;<span class="variable">$APP_CONTAINER</span>&quot;</span></span><br><span class="line">docker compose logs --<span class="built_in">tail</span>=300 app</span><br></pre></td></tr></table></figure><p>先看退出码和最后一段日志。<code>restart: unless-stopped</code> 会不断拉起失败进程，容易让真正的首次异常滚出日志窗口。</p><h3 id="8-2-MySQL-一直不健康"><a href="#8-2-MySQL-一直不健康" class="headerlink" title="8.2 MySQL 一直不健康"></a>8.2 MySQL 一直不健康</h3><p>检查密码变量是否正确展开、数据目录权限、旧数据卷与新版本是否兼容。MySQL 初始化只在空数据目录执行，修改环境变量不会自动修改已有数据库用户密码。</p><h3 id="8-3-应用能解析服务名但连不上"><a href="#8-3-应用能解析服务名但连不上" class="headerlink" title="8.3 应用能解析服务名但连不上"></a>8.3 应用能解析服务名但连不上</h3><p>检查服务是否在同一个网络、目标进程是否监听 <code>0.0.0.0</code>，以及端口使用的是容器端口还是宿主机映射端口。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker network <span class="built_in">ls</span></span><br><span class="line">docker network inspect demo-app_default</span><br></pre></td></tr></table></figure><h3 id="8-4-磁盘被日志写满"><a href="#8-4-磁盘被日志写满" class="headerlink" title="8.4 磁盘被日志写满"></a>8.4 磁盘被日志写满</h3><p>Docker <code>json-file</code> 默认可能持续增长。Compose 中已经限制单文件和数量，仍要监控 Docker 根目录和应用自身文件日志，避免两套日志重复写盘。</p><h3 id="8-5-镜像在另一台机器无法运行"><a href="#8-5-镜像在另一台机器无法运行" class="headerlink" title="8.5 镜像在另一台机器无法运行"></a>8.5 镜像在另一台机器无法运行</h3><p>确认 CPU 架构、镜像是否成功推送、私有仓库认证和配置文件来源。Apple Silicon 本地构建的镜像部署到 x86 服务器时，尤其要检查目标平台。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker image inspect demo-app:1.0.0 --format <span class="string">&#x27;&#123;&#123;.Architecture&#125;&#125;/&#123;&#123;.Os&#125;&#125;&#x27;</span></span><br></pre></td></tr></table></figure><h2 id="9-配置和密钥"><a href="#9-配置和密钥" class="headerlink" title="9. 配置和密钥"></a>9. 配置和密钥</h2><p>Compose 环境变量适合本地练习，但生产环境不能把密码直接写进仓库。至少做到：</p><ul><li><code>.env</code> 加入 <code>.gitignore</code>。</li><li>使用独立的应用数据库账号，不使用 root。</li><li>密钥通过受控平台、挂载文件或 Secrets 管理。</li><li>日志禁止打印数据库 URL 中的密码和 Token。</li><li>配置变更有审计和回滚版本。</li></ul><p>Docker 镜像应当只包含通用应用，不把某个环境的 <code>application-prod.yml</code> 和密钥烘焙进去。</p><h2 id="10-Compose-的适用边界"><a href="#10-Compose-的适用边界" class="headerlink" title="10. Compose 的适用边界"></a>10. Compose 的适用边界</h2><p>Compose 很适合本地开发、自动化测试和单机多容器部署。它能把依赖关系固化成代码，也方便复现环境。</p><p>生产环境还需要考虑：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">多机调度和节点故障</span><br><span class="line">滚动升级与自动回滚</span><br><span class="line">集中式 Secrets</span><br><span class="line">服务发现和负载均衡</span><br><span class="line">资源限制与配额</span><br><span class="line">日志、指标和告警</span><br><span class="line">数据备份与恢复</span><br></pre></td></tr></table></figure><p>这些问题不是再写几行 Compose 就能全部解决的。是否进入 Kubernetes 等平台，要根据服务规模和运维能力决定。</p><h2 id="收尾命令"><a href="#收尾命令" class="headerlink" title="收尾命令"></a>收尾命令</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看最终状态</span></span><br><span class="line">docker compose ps</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看最近日志</span></span><br><span class="line">docker compose logs --<span class="built_in">tail</span>=100 app mysql redis</span><br><span class="line"></span><br><span class="line"><span class="comment"># 停止但保留数据卷</span></span><br><span class="line">docker compose down</span><br></pre></td></tr></table></figure><p>这次实践真正解决的不是“会用 Docker 命令”，而是把构建、网络、配置、依赖和数据生命周期写成了一个可以重复执行的环境。能够在另一台干净机器上用同一份配置启动并验证，才是容器化带来的环境一致性。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;这篇做一个可以实际运行的部署练习：把 JDK 8 的 Spring Boot 应用构建成镜像，再使用 Docker Compose 启动应用、MySQL 和 Redis。重点不只是 &lt;code&gt;up -d&lt;/code&gt;，还包括网络、健康检查、数据卷</summary>
      
    
    
    
    <category term="架构成长" scheme="https://swagch.github.io/categories/%E6%9E%B6%E6%9E%84%E6%88%90%E9%95%BF/"/>
    
    
    <category term="部署" scheme="https://swagch.github.io/tags/%E9%83%A8%E7%BD%B2/"/>
    
    <category term="Docker" scheme="https://swagch.github.io/tags/Docker/"/>
    
    <category term="Docker Compose" scheme="https://swagch.github.io/tags/Docker-Compose/"/>
    
    <category term="容器化" scheme="https://swagch.github.io/tags/%E5%AE%B9%E5%99%A8%E5%8C%96/"/>
    
  </entry>
  
</feed>
