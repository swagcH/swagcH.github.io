<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>杰尼龟的笔记</title>
  
  
  <link href="https://swagch.github.io/atom.xml" rel="self"/>
  
  <link href="https://swagch.github.io/"/>
  <updated>2026-06-09T06:22:08.822Z</updated>
  <id>https://swagch.github.io/</id>
  
  <author>
    <name>无敌杰尼龟</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>博客重新出发</title>
    <link href="https://swagch.github.io/2026/06/09/%E5%8D%9A%E5%AE%A2%E9%87%8D%E6%96%B0%E5%87%BA%E5%8F%91/"/>
    <id>https://swagch.github.io/2026/06/09/%E5%8D%9A%E5%AE%A2%E9%87%8D%E6%96%B0%E5%87%BA%E5%8F%91/</id>
    <published>2026-06-08T16:00:00.000Z</published>
    <updated>2026-06-09T06:22:08.822Z</updated>
    
    <content type="html"><![CDATA[<p>博客源码重新搭建完成，后续会继续记录技术学习、项目实践和问题排查过程。</p><span id="more"></span><h2 id="为什么重新搭建"><a href="#为什么重新搭建" class="headerlink" title="为什么重新搭建"></a>为什么重新搭建</h2><p>之前只保留了静态站点产物，源码目录已经丢失。为了后续能继续维护文章、主题和部署流程，这次重新生成了一个完整的 Hexo 源码项目。</p><h2 id="当前技术栈"><a href="#当前技术栈" class="headerlink" title="当前技术栈"></a>当前技术栈</h2><ul><li>Hexo：静态博客生成器</li><li>NexT：Hexo 主题</li><li>GitHub Pages：静态站点托管</li><li>GitHub Actions：后续可用于自动构建和部署</li></ul><h2 id="后续计划"><a href="#后续计划" class="headerlink" title="后续计划"></a>后续计划</h2><ol><li>整理 Java 后端开发笔记</li><li>梳理并发编程和 JVM 相关知识</li><li>记录 Linux、Nginx、部署排查经验</li><li>逐步完善博客的搜索、订阅和 SEO 配置</li></ol>]]></content>
    
    
    <summary type="html">&lt;p&gt;博客源码重新搭建完成，后续会继续记录技术学习、项目实践和问题排查过程。&lt;/p&gt;</summary>
    
    
    
    <category term="随笔" scheme="https://swagch.github.io/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="Hexo" scheme="https://swagch.github.io/tags/Hexo/"/>
    
    <category term="GitHub Pages" scheme="https://swagch.github.io/tags/GitHub-Pages/"/>
    
  </entry>
  
  <entry>
    <title>2018年度学习总结</title>
    <link href="https://swagch.github.io/2018/12/30/2018-12-30-2018%E5%B9%B4%E5%BA%A6%E5%AD%A6%E4%B9%A0%E6%80%BB%E7%BB%93/"/>
    <id>https://swagch.github.io/2018/12/30/2018-12-30-2018%E5%B9%B4%E5%BA%A6%E5%AD%A6%E4%B9%A0%E6%80%BB%E7%BB%93/</id>
    <published>2018-12-30T15:47:08.000Z</published>
    <updated>2026-06-09T07:57:18.232Z</updated>
    
    <content type="html"><![CDATA[<p>现在是 2018 年 12 月 30 日深夜，窗外很安静，桌面上摊着几本书、一杯凉掉的咖啡、开着 IDE 的屏幕。刚刚跑通了一个 Spring 的小 Demo，不算什么了不起的东西，但我盯着控制台里那几行日志，愣是坐了好一会儿。</p><p>总觉得该写点什么，为这一年。</p><p>回头看，2018 年是我真正踏进编程世界的第一年。从连编辑器都不会配，到能用 Java 写点能跑的东西，这三百多天走得七拐八拐，中间有好几次差点放弃。趁着跨年前这个安静的夜晚，把这一年的路好好捋一捋。</p><h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>2018 年 1 月，我坐在电脑前打开了一本 C 语言的电子书，那是我和编程的第一次正式见面。</p><p>在此之前，我对编程的全部认知大概就是——很酷、很复杂、不适合我。高考报志愿时阴差阳错进了计算机相关专业，大一上学期懵懵懂懂地过了，真正开始认真对待，就是从 2018 年开始的。</p><p>最开始学的是 C 语言，指针、内存管理、结构体——说实话，学得挺痛苦的。尤其是指针，绕来绕去，debug 的时候经常对着一个段错误发呆半小时。但也正因为 C 的硬核，让我对内存、对程序的底层运行有了一点感觉。</p><p>到了三四月份，我开始纠结一个问题：C 语言当然好，但它适合作为长期深耕的方向吗？当时打听了不少学长，也刷了很多帖子，结论是——Java 在后端领域生态成熟、岗位多、学习路径清晰。于是我做了一个重要的决定：转学 Java。</p><p>这个决定，基本定义了我整个 2018 年剩下的路。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20back-end%20learning%20roadmap%202018,%20from%20Java%20SE%20to%20Spring%20framework,%20road%20map%20style%20timeline%20infographic,%20developer%20education%20path&image_size=landscape_16_9" alt="2018 年 Java 后端学习路线图"></p><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>目标定下来之后，真正走起来才发现，问题一个接一个。</p><h2 id="1-学习路线不清晰，走了不少弯路"><a href="#1-学习路线不清晰，走了不少弯路" class="headerlink" title="1. 学习路线不清晰，走了不少弯路"></a>1. 学习路线不清晰，走了不少弯路</h2><p>网上关于 Java 学习的资料和路线图很多，但信息多到让人不知道该信谁。今天看到一个博客说先学 Java SE，明天又有人说直接上手 Spring Boot。我是那种喜欢把”所有前置知识”都学完再做事情的人，结果就是在基础语法阶段兜兜转转了太久。</p><h2 id="2-光看不练，眼高手低"><a href="#2-光看不练，眼高手低" class="headerlink" title="2. 光看不练，眼高手低"></a>2. 光看不练，眼高手低</h2><p>这是我这一年最大的毛病。</p><p>看书的时候觉得自己都懂了——不就是面向对象吗，不就是集合框架吗，有什么难的。但是合上书，打开 IDE，写一个简单的增删改查都要查半天 API。印象最深的是学完 HashMap 原理之后，自己试着实现一个简化版本，写到一半发现根本不知道从哪里下手。这才意识到，”看懂”和”会写”之间隔着十万行代码的距离。</p><h2 id="3-遇到问题不敢问"><a href="#3-遇到问题不敢问" class="headerlink" title="3. 遇到问题不敢问"></a>3. 遇到问题不敢问</h2><p>当时加了一些技术群，也在论坛上潜水。遇到问题第一反应不是去问，而是反复怀疑自己是不是太笨了——这么基础的东西都不懂。于是经常一个人卡在一个小 bug 上几个小时，浪费大量时间。后来才明白，每个经历过自学的人都是从这些坑里爬出来的，问问题不是丢人的事。</p><h2 id="4-博客看得多，写得少"><a href="#4-博客看得多，写得少" class="headerlink" title="4. 博客看得多，写得少"></a>4. 博客看得多，写得少</h2><p>我的浏览器书签里收藏了上百篇文章，从 HashMap 源码分析到 JVM 内存模型，应有尽有。每天刷好几篇，刷完觉得充实。但一个月后回头看，脑子里能留下的印象很模糊。因为光看不写，知识的留存率太低了。</p><h2 id="5-时间管理不好，经常拖延"><a href="#5-时间管理不好，经常拖延" class="headerlink" title="5. 时间管理不好，经常拖延"></a>5. 时间管理不好，经常拖延</h2><p>“今天太累了，明天再看吧”——这句话几乎是我上半年的口头禅。没有固定的学习时间，没有计划，全凭热情驱动。热情高的时候一天学八个小时，没状态的时候一周都不碰代码。这种节奏非常低效。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>后来我慢慢想明白了这些问题的根因，也在下半年做了一些调整。</p><h2 id="学习规划的重要性"><a href="#学习规划的重要性" class="headerlink" title="学习规划的重要性"></a>学习规划的重要性</h2><p>没有规划的学习就像没有地图的旅行。知识海洋是无限的，但个人的时间和精力是有限的。后来我给自己定了一条原则：选定一个方向，找到一条被验证过的路线，先踏踏实实走完，不要中途反复切换赛道。</p><h2 id="编程是手艺活"><a href="#编程是手艺活" class="headerlink" title="编程是手艺活"></a>编程是手艺活</h2><p>我越来越理解一个道理：编程像学游泳，你看再多教学视频，不下水就永远学不会。代码必须亲手敲，bug 必须亲自查，项目必须亲手做。任何一个你”觉得”懂了但写不出来的知识点，都说明你还没真正掌握它。</p><p>后来我强迫自己每学一个新知识点，就写一段代码验证它，哪怕只是一个十行的小 demo。</p><h2 id="社区和交流的价值"><a href="#社区和交流的价值" class="headerlink" title="社区和交流的价值"></a>社区和交流的价值</h2><p>下半年我开始尝试在群里发言，偶尔回答一些别人提的问题。我发现教是最好的学——为了回答一个问题，你往往需要把那个知识点重新梳理一遍，这个过程本身就是一次高质量复习。而且当你发现别人也会犯和你一样的错误时，那种”只有我不行”的焦虑感会少很多。</p><h2 id="写作是深度学习的加速器"><a href="#写作是深度学习的加速器" class="headerlink" title="写作是深度学习的加速器"></a>写作是深度学习的加速器</h2><p>十月份开始写博客之后，我明显感觉到自己对知识的理解深了一层。因为要把一个东西讲清楚，你必须先把它真正搞懂。写作强迫你梳理逻辑、查漏补缺、用自己的语言重新表达。这个过程比单纯地读十篇文章都有效。</p><h2 id="固定学习节奏"><a href="#固定学习节奏" class="headerlink" title="固定学习节奏"></a>固定学习节奏</h2><p>下半年我开始用番茄工作法，每 25 分钟专注学习，然后休息 5 分钟。同时给自己定了规矩：每天晚上 8 点到 10 点是固定学习时间，雷打不动。效果比”等我有时间了再学”好太多。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Time%20management%20and%20learning%20methods%20for%20programmers,%20Pomodoro%20technique,%20fixed%20study%20schedule,%20knowledge%20retention%20pyramid,%20tech%20blog%20illustration&image_size=landscape_16_9" alt="时间管理与学习方法示意图"></p><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>说是解决方案，其实没有那么多方法论。就是一个普通人在跌跌撞撞中，按季度一步步走过来的真实记录。</p><h2 id="Q1（1-3-月）：C-语言入门，决定转-Java"><a href="#Q1（1-3-月）：C-语言入门，决定转-Java" class="headerlink" title="Q1（1-3 月）：C 语言入门，决定转 Java"></a>Q1（1-3 月）：C 语言入门，决定转 Java</h2><p>年初从 C 语言开始，把指针、数组、结构体这些基础概念过了一遍。过程中有过一点小小的成就感——当第一个指针程序运行正确的时候，那种感觉确实很奇妙。但也逐渐意识到，C 语言在应用层面的天花板比较低，对我来说更适合作为理解计算机底层的基础，而不是长期主攻方向。</p><p>三月份做了全年最重要的决定：转向 Java。</p><p>这个决定不是一时冲动。当时花了一周时间调研，看了很多自学路线帖和招聘要求，确认 Java 在服务端开发中的地位和前景。确定了方向之后，整个人的心态反而安定下来了——不再东张西望，就朝这一个方向走。</p><p>接着就是搭环境。JDK 安装、环境变量配置、第一个 IDE（Eclipse）的安装和使用。这些现在看来基础得不能再基础的操作，当时每一步都可能卡住。JDK 版本选哪个？环境变量配不对怎么办？Eclipse 为什么打不开？每一个问题都要查好久。</p><p>当 Hello World 终于出现在控制台的时候，我截图发了个朋友圈。虽然现在想来有点好笑，但那一刻真的觉得——这个大门，我算是挤进来了。</p><h2 id="Q2（4-6-月）：Java-语法基础"><a href="#Q2（4-6-月）：Java-语法基础" class="headerlink" title="Q2（4-6 月）：Java 语法基础"></a>Q2（4-6 月）：Java 语法基础</h2><p>第二季度是大量输入基础知识的时候。</p><p>从基本数据类型、运算符、流程控制开始，然后是面向对象三大特性——封装、继承、多态。这些东西第一次接触时确实觉得抽象，尤其是多态，学完之后做题会做，但问自己”多态在实际项目中有什么用”就答不上来。后来才明白，有些东西的理解是分阶段的——先记住，然后在使用中慢慢感受。</p><p>五月份学到集合框架。ArrayList、LinkedList、HashSet、HashMap——每个实现类都过了一遍，大概理解了接口和实现分离的设计思想。</p><p>这三个月基本是靠看书 + 看视频 + 敲 demo 过来的。节奏不算快，但比较扎实。每学完一个章节，就把书上的代码亲手敲一遍，改一改，看输出有什么变化。</p><h2 id="Q3（7-9-月）：深入-Java，完成第一个小项目"><a href="#Q3（7-9-月）：深入-Java，完成第一个小项目" class="headerlink" title="Q3（7-9 月）：深入 Java，完成第一个小项目"></a>Q3（7-9 月）：深入 Java，完成第一个小项目</h2><p>暑假是全年学习效率最高的一段时间。</p><p>七月啃 HashMap。那时候看了好几篇源码分析的文章，自己也把源码拉下来断点跟踪。虽然当时对红黑树的理解还很浅，但至少搞清楚了哈希冲突怎么解决、扩容机制是怎么回事。也在这个月第一次写博客——<a href="https://chenzixin.github.io/2018/07/22/HashMap%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0/">HashMap 基础学习</a>，写得很浅，但迈出了第一步。</p><p>八月学异常处理机制，写了 <a href="https://chenzixin.github.io/2018/08/19/Java%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86%E6%9C%BA%E5%88%B6/">Java 异常处理机制</a>。然后开始学 JDBC，这是第一次把 Java 和数据库联系起来，写了篇文章 <a href="https://chenzixin.github.io/2018/09/15/JDBC%E8%BF%9E%E6%8E%A5MySQL%E5%AE%9E%E8%B7%B5/">JDBC 连接 MySQL 实践</a>。</p><p>九月最重要的事情是完成了第一个完整的 Java 小项目——学生管理系统。</p><p>说是系统，其实就是一个控制台程序：从 MySQL 里读取学生数据，展示在控制台上，支持增删改查。技术上没什么难度，但对我来说意义很大——我第一次经历了”从零开始分析需求、设计数据结构、写代码、调试、最终跑通”的完整流程。也正是在这个项目里，我初次感受到了写代码的快感。</p><h2 id="Q4（10-12-月）：数据库、Linux、Spring-入门"><a href="#Q4（10-12-月）：数据库、Linux、Spring-入门" class="headerlink" title="Q4（10-12 月）：数据库、Linux、Spring 入门"></a>Q4（10-12 月）：数据库、Linux、Spring 入门</h2><p>第四季度开始进一步拓展广度。</p><p>MySQL 部分不再满足于基本的 CRUD，开始深入学习表设计、索引、事务隔离级别。也正是在这个过程中，我才理解了为什么三范式、为什么加索引、什么时候用 InnoDB 什么时候用 MyISAM。</p><p>十一月开始接触 Linux。装了一个虚拟机，装 CentOS，学着用命令行操作文件、配置网络、装软件。一开始觉得没有图形界面很不习惯，但后来慢慢体会到了命令行的高效。这期间也学会了搭 Nginx 和部署简单项目。</p><p>十二月的关键词是 Spring。终于在年底正式接触到了这个如雷贯耳的框架。从 IoC（控制反转）和 DI（依赖注入）的概念开始，慢慢理解为什么 Spring 能让代码变得松耦合。十二月中旬跑通了第一个 Spring 项目，虽然只是配置了几个 Bean，做了最简单的依赖注入，但那种”原来框架是这样工作的”的感觉让我兴奋了好几天。</p><h2 id="2019-年计划"><a href="#2019-年计划" class="headerlink" title="2019 年计划"></a>2019 年计划</h2><p>坐在 2018 年的尾巴上，对明年有一些想法：</p><ul><li><strong>框架学习</strong>：把 SSM（Spring + SpringMVC + MyBatis）这个组合啃下来，搭建一个完整的 Web 项目</li><li><strong>Redis</strong>：学习缓存，理解 NoSQL 的适用场景</li><li><strong>项目实战</strong>：不再满足于 demo 和小工具，至少做一个拿得出手的完整项目</li><li><strong>坚持写博客</strong>：把学到的东西沉淀下来，不管写得好不好，先写</li><li><strong>更多交流</strong>：尝试去回答别人提出的问题，在社区中参与讨论</li></ul><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>2018 年结束了。</p><p>这一年走过的路，用代码来表达大概是这样的：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">public class Year2018 &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        int totalDays = 365;</span><br><span class="line">        int learningDays = 250;     // 翻书 + 看视频</span><br><span class="line">        int codingDays = 150;       // 亲手写代码</span><br><span class="line">        int projectCount = 1;       // 第一个完整项目</span><br><span class="line">        int blogCount = 5;          // 这一年写的博客</span><br><span class="line">        int debugErrors = 999;      // 记不清的错误次数，上不封顶</span><br><span class="line"></span><br><span class="line">        String result = (codingDays &gt; 0 &amp;&amp; projectCount &gt; 0)</span><br><span class="line">            ? &quot;入门成功&quot; : &quot;仍需努力&quot;;</span><br><span class="line">        System.out.println(&quot;2018年结果：&quot; + result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>代码不会骗人。这一年的输入和输出清清楚楚地写在上面。</p><p>关于心态的变化可能比知识的增长更重要。年初的时候我总觉得编程是一件需要天赋的事情，那些写框架的人、做开源项目的人，一定天生就比普通人聪明。现在我不这么认为了。我看到的所有”厉害的人”，不过是在你看不到的地方，花了你想象不到的时间。</p><p>编程是一种技能，技能是可以靠持续练习获得的。这一年教会我最大的道理就是：<strong>不要高估一天能学到的东西，不要低估一年能积累的东西。</strong></p><p>当然也有遗憾。有些知识点学得不够深入，很多 bug 挡了一下就绕过去了，没有彻底搞清楚根源。有些计划中的书没看完，有些想写的博客没来得及写。</p><p>但没关系。2019 年还会继续。</p><p>最后的最后，用一句话总结这个深夜的感慨吧：</p><blockquote><p>这一年，从 Hello World 写到了 Spring 容器；从对着控制台发呆，到享受 Debug 通过那一刻的畅快。路还很长，但至少，我开始享受写代码这件事了。</p></blockquote><p>2019，继续写下去。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;现在是 2018 年 12 月 30 日深夜，窗外很安静，桌面上摊着几本书、一杯凉掉的咖啡、开着 IDE 的屏幕。刚刚跑通了一个 Spring 的小 Demo，不算什么了不起的东西，但我盯着控制台里那几行日志，愣是坐了好一会儿。&lt;/p&gt;
&lt;p&gt;总觉得该写点什么，为这一年。&lt;</summary>
      
    
    
    
    <category term="随笔" scheme="https://swagch.github.io/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="年度总结" scheme="https://swagch.github.io/tags/%E5%B9%B4%E5%BA%A6%E6%80%BB%E7%BB%93/"/>
    
    <category term="学习路线" scheme="https://swagch.github.io/tags/%E5%AD%A6%E4%B9%A0%E8%B7%AF%E7%BA%BF/"/>
    
    <category term="回顾" scheme="https://swagch.github.io/tags/%E5%9B%9E%E9%A1%BE/"/>
    
    <category term="展望" scheme="https://swagch.github.io/tags/%E5%B1%95%E6%9C%9B/"/>
    
  </entry>
  
  <entry>
    <title>Linux常用命令整理</title>
    <link href="https://swagch.github.io/2018/11/18/2018-11-18-Linux%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4%E6%95%B4%E7%90%86/"/>
    <id>https://swagch.github.io/2018/11/18/2018-11-18-Linux%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4%E6%95%B4%E7%90%86/</id>
    <published>2018-11-18T07:52:31.000Z</published>
    <updated>2026-06-09T08:05:22.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>2018年双十一刚过，项目终于要部署上线了。公司买了两台阿里云ECS服务器，操作系统是CentOS 7.4。作为一个从大学到现在一直在Windows环境下写代码的.NET程序员，听到”部署到Linux服务器”这几个字时，内心是拒绝的。</p><p>之前对Linux的印象停留在”黑乎乎的命令行窗口，一堆看不懂的英文字母”。但项目不等人，硬着头皮上吧。花了大概两周时间边查边练，把日常运维常用的Linux命令过了一遍，这篇文章就是这段时间的学习笔记。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Linux%20terminal%20with%20multiple%20windows%20showing%20different%20commands%20and%20output&image_size=landscape_16_9" alt="Linux终端界面"></p><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚接手Linux服务器时，遇到了一堆”小白问题”，说来有点丢人但确实很真实：</p><p><strong>1. 文件权限看不懂</strong></p><p><code>ls -l</code> 输出了一堆 <code>drwxr-xr-x</code> 这样的东西，完全不知道什么意思。想给文件加执行权限，百度了半天才找到 <code>chmod 777</code>，但到底什么是”777”也说不清楚。</p><p><strong>2. vim编辑器不会用</strong></p><p>用 <code>vi</code> 打开文件后，发现打不了字，想退出却退不出来，最后直接关了终端。后来才知道 vim 有三种模式，退出要用 <code>:q</code>。</p><p><strong>3. 进程查不到</strong></p><p>程序跑起来后不知道在哪看，Windows 有任务管理器，Linux 呢？程序挂了也不知道为什么挂的。</p><p><strong>4. 磁盘满了不知道清哪里</strong></p><p>有一天数据库写不进去了，排查半天发现是磁盘满了。但日志文件、临时文件散落在各个目录，不知道从哪里开始清理。</p><p><strong>5. 日志文件太大查看困难</strong></p><p>Java 应用的日志文件动辄几百兆甚至几个G，用记事本打开直接卡死。想查最新的报错信息，总不能把整个文件拖下来吧。</p><p><strong>6. 端口被占用不知道怎么查</strong></p><p>Tomcat 启动报错 <code>Address already in use</code>，知道是端口被占了，但不知道被哪个程序占了，也不知道怎么杀掉它。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>遇到这些问题后，我花了一些时间理解了 Linux 的几个核心概念，这些概念是后续学习命令的基础。</p><h3 id="Linux-一切皆文件"><a href="#Linux-一切皆文件" class="headerlink" title="Linux 一切皆文件"></a>Linux 一切皆文件</h3><p>这是 Linux 最核心的设计哲学。普通文件是文件，目录是文件，设备是文件，甚至是进程信息、网络连接都被抽象成了文件。理解了这一点，就能理解为什么 Linux 有那么多文件操作命令了。</p><h3 id="权限的-rwx-表示法"><a href="#权限的-rwx-表示法" class="headerlink" title="权限的 rwx 表示法"></a>权限的 rwx 表示法</h3><p><code>rwx</code> 分别代表：</p><ul><li><code>r</code>（read）：读取权限，值为 4</li><li><code>w</code>（write）：写入权限，值为 2</li><li><code>x</code>（execute）：执行权限，值为 1</li></ul><p>权限分三组，每组三个字符：<strong>所有者&#x2F;所属组&#x2F;其他人</strong>。所以 <code>rwxr-xr-x</code> 表示：所有者可读写执行，所属组可读执行，其他人可读执行。</p><p>而 <code>chmod 755</code> 中的 755，就是用数字表示的权限：</p><ul><li>7 &#x3D; 4+2+1 &#x3D; rwx（所有者）</li><li>5 &#x3D; 4+0+1 &#x3D; r-x（所属组）</li><li>5 &#x3D; 4+0+1 &#x3D; r-x（其他人）</li></ul><h3 id="vim-的三种模式"><a href="#vim-的三种模式" class="headerlink" title="vim 的三种模式"></a>vim 的三种模式</h3><p>刚接触 vim 时觉得这编辑器反人类，理解了三种模式后就豁然开朗了：</p><ul><li><strong>命令模式</strong>：刚进入 vim 时的模式，可以复制粘贴删除，但<strong>不能输入文字</strong></li><li><strong>插入模式</strong>：按 <code>i</code> 进入，这个模式才能正常打字</li><li><strong>末行模式</strong>：按 <code>:</code> 进入，用来保存退出、搜索替换</li></ul><p>三种模式切换搞明白后，vim 就好用多了。</p><h3 id="进程管理思路"><a href="#进程管理思路" class="headerlink" title="进程管理思路"></a>进程管理思路</h3><p>Linux 的进程管理不像 Windows 那样可视化，但命令行的方式其实更灵活：</p><ul><li><code>ps</code> 查看静态进程快照</li><li><code>top</code> 查看动态进程信息（相当于任务管理器）</li><li><code>kill</code> 发送信号给进程</li></ul><h3 id="磁盘查看思路"><a href="#磁盘查看思路" class="headerlink" title="磁盘查看思路"></a>磁盘查看思路</h3><ul><li><code>df</code> 从宏观角度查看磁盘整体使用情况</li><li><code>du</code> 从微观角度查看具体目录&#x2F;文件占用空间</li></ul><h3 id="日志查看三板斧"><a href="#日志查看三板斧" class="headerlink" title="日志查看三板斧"></a>日志查看三板斧</h3><p>大文件用编辑器打开是不现实的，Linux 提供了三个利器：</p><ul><li><code>tail</code>：从尾部看，适合看最新的日志</li><li><code>grep</code>：按关键字过滤，适合搜索特定错误</li><li><code>less</code>：分页查看，适合在文件中上下翻阅</li></ul><h3 id="端口查看思路"><a href="#端口查看思路" class="headerlink" title="端口查看思路"></a>端口查看思路</h3><ul><li><code>netstat</code>：查看网络连接状态</li><li><code>lsof</code>：查看打开的文件（一切皆文件，网络连接也是文件）</li></ul><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>下面按场景分类整理常用命令，每个分类给出常用命令和实际运维示例。</p><h3 id="一、文件目录操作"><a href="#一、文件目录操作" class="headerlink" title="一、文件目录操作"></a>一、文件目录操作</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看目录内容</span></span><br><span class="line"><span class="built_in">ls</span>                    <span class="comment"># 列出当前目录文件</span></span><br><span class="line"><span class="built_in">ls</span> -l                 <span class="comment"># 详细列表（显示权限、大小、修改时间）</span></span><br><span class="line"><span class="built_in">ls</span> -a                 <span class="comment"># 显示隐藏文件（以.开头的文件）</span></span><br><span class="line"><span class="built_in">ls</span> -lh                <span class="comment"># 以人类可读方式显示大小（K、M、G）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 目录切换</span></span><br><span class="line"><span class="built_in">cd</span> /usr/local         <span class="comment"># 进入指定目录</span></span><br><span class="line"><span class="built_in">cd</span> ~                  <span class="comment"># 进入home目录</span></span><br><span class="line"><span class="built_in">cd</span> -                  <span class="comment"># 返回上一个目录</span></span><br><span class="line"><span class="built_in">cd</span> ..                 <span class="comment"># 返回上级目录</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建和删除</span></span><br><span class="line"><span class="built_in">mkdir</span> myapp           <span class="comment"># 创建目录</span></span><br><span class="line"><span class="built_in">mkdir</span> -p a/b/c        <span class="comment"># 递归创建多级目录</span></span><br><span class="line"><span class="built_in">touch</span> README.md       <span class="comment"># 创建空文件</span></span><br><span class="line"><span class="built_in">cp</span> app.conf app.conf.bak    <span class="comment"># 复制文件</span></span><br><span class="line"><span class="built_in">cp</span> -r webapp/ webapp_bak/   <span class="comment"># 递归复制目录</span></span><br><span class="line"><span class="built_in">mv</span> app.conf /tmp/     <span class="comment"># 移动文件</span></span><br><span class="line"><span class="built_in">mv</span> oldname newname    <span class="comment"># 重命名文件</span></span><br><span class="line"><span class="built_in">rm</span> temp.txt           <span class="comment"># 删除文件（不会进回收站，直接删）</span></span><br><span class="line"><span class="built_in">rm</span> -rf logs/          <span class="comment"># 递归强制删除目录（慎用！）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查找文件</span></span><br><span class="line">find / -name <span class="string">&quot;*.log&quot;</span>                   <span class="comment"># 从根目录找所有.log文件</span></span><br><span class="line">find /var/log -mtime -7                <span class="comment"># 查找7天内修改过的文件</span></span><br><span class="line">find . -size +100M                     <span class="comment"># 查找当前目录下大于100M的文件</span></span><br><span class="line">find . -name <span class="string">&quot;*.tmp&quot;</span> -<span class="built_in">exec</span> <span class="built_in">rm</span> &#123;&#125; \;    <span class="comment"># 查找并删除临时文件</span></span><br></pre></td></tr></table></figure><p><strong>运维场景：查找并清理大文件</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查找根目录下大于500M的文件，按大小排序</span></span><br><span class="line">find / -<span class="built_in">type</span> f -size +500M -<span class="built_in">exec</span> <span class="built_in">ls</span> -lh &#123;&#125; \; 2&gt;/dev/null | awk <span class="string">&#x27;&#123;print $5, $NF&#125;&#x27;</span> | <span class="built_in">sort</span> -rh</span><br><span class="line"></span><br><span class="line"><span class="comment"># 清理7天前的日志文件</span></span><br><span class="line">find /var/log/myapp/ -name <span class="string">&quot;*.log&quot;</span> -mtime +7 -<span class="built_in">exec</span> <span class="built_in">rm</span> &#123;&#125; \;</span><br></pre></td></tr></table></figure><h3 id="二、文件查看与搜索"><a href="#二、文件查看与搜索" class="headerlink" title="二、文件查看与搜索"></a>二、文件查看与搜索</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看文件内容</span></span><br><span class="line"><span class="built_in">cat</span> README.md               <span class="comment"># 显示整个文件（适合小文件）</span></span><br><span class="line"><span class="built_in">cat</span> -n app.log              <span class="comment"># 显示行号</span></span><br><span class="line">less app.log                <span class="comment"># 分页查看（按空格翻页，按q退出）</span></span><br><span class="line"><span class="built_in">head</span> -20 app.log            <span class="comment"># 查看前20行</span></span><br><span class="line"><span class="built_in">tail</span> -100 app.log           <span class="comment"># 查看后100行</span></span><br><span class="line"><span class="built_in">tail</span> -f app.log             <span class="comment"># 实时跟踪文件新增内容（看日志神器！）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索文件内容</span></span><br><span class="line">grep <span class="string">&quot;ERROR&quot;</span> app.log                      <span class="comment"># 在文件中搜索关键字</span></span><br><span class="line">grep -i <span class="string">&quot;error&quot;</span> app.log                   <span class="comment"># 忽略大小写搜索</span></span><br><span class="line">grep -r <span class="string">&quot;NullPointerException&quot;</span> /logs/     <span class="comment"># 递归搜索目录下所有文件</span></span><br><span class="line">grep -n <span class="string">&quot;ERROR&quot;</span> app.log                   <span class="comment"># 显示匹配行及其行号</span></span><br><span class="line">grep -c <span class="string">&quot;ERROR&quot;</span> app.log                   <span class="comment"># 统计匹配行数</span></span><br><span class="line">grep -A5 <span class="string">&quot;ERROR&quot;</span> app.log                  <span class="comment"># 显示匹配行及其后5行</span></span><br><span class="line">grep -B5 <span class="string">&quot;ERROR&quot;</span> app.log                  <span class="comment"># 显示匹配行及其前5行</span></span><br><span class="line">grep -C5 <span class="string">&quot;ERROR&quot;</span> app.log                  <span class="comment"># 显示匹配行及其前后各5行</span></span><br></pre></td></tr></table></figure><p><strong>运维场景：查看日志中的错误信息</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看今天出现的所有ERROR及上下文</span></span><br><span class="line"><span class="built_in">tail</span> -10000 app.log | grep -C5 <span class="string">&quot;ERROR&quot;</span> | less</span><br><span class="line"></span><br><span class="line"><span class="comment"># 统计各错误类型出现次数</span></span><br><span class="line">grep -o <span class="string">&quot;ERROR \[.*\]&quot;</span> app.log | <span class="built_in">sort</span> | <span class="built_in">uniq</span> -c | <span class="built_in">sort</span> -rn</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看某个时间段内的日志</span></span><br><span class="line">grep <span class="string">&quot;2018-11-18 10:&quot;</span> app.log | less</span><br></pre></td></tr></table></figure><h3 id="三、权限管理"><a href="#三、权限管理" class="headerlink" title="三、权限管理"></a>三、权限管理</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 修改权限</span></span><br><span class="line"><span class="built_in">chmod</span> 755 script.sh          <span class="comment"># 设置权限为rwxr-xr-x</span></span><br><span class="line"><span class="built_in">chmod</span> +x script.sh           <span class="comment"># 给文件加执行权限</span></span><br><span class="line"><span class="built_in">chmod</span> -R 644 html/           <span class="comment"># 递归设置目录下所有文件权限</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 修改所有者</span></span><br><span class="line"><span class="built_in">chown</span> admin app.log          <span class="comment"># 修改文件所有者为admin</span></span><br><span class="line"><span class="built_in">chown</span> admin:admin app.log    <span class="comment"># 同时修改所有者和所属组</span></span><br><span class="line"><span class="built_in">chown</span> -R admin:admin /app/   <span class="comment"># 递归修改目录下所有文件所有者</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看文件权限</span></span><br><span class="line"><span class="built_in">ls</span> -l                        <span class="comment"># 查看文件的权限信息</span></span><br><span class="line"><span class="built_in">stat</span> app.conf                <span class="comment"># 查看文件的详细状态（包括权限）</span></span><br></pre></td></tr></table></figure><p><strong>运维场景：部署项目后修复权限</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 项目部署后的权限设置</span></span><br><span class="line"><span class="built_in">chown</span> -R www:www /var/www/myapp/          <span class="comment"># 设置为www用户</span></span><br><span class="line">find /var/www/myapp/ -<span class="built_in">type</span> f -<span class="built_in">exec</span> <span class="built_in">chmod</span> 644 &#123;&#125; \;   <span class="comment"># 文件设置为644</span></span><br><span class="line">find /var/www/myapp/ -<span class="built_in">type</span> d -<span class="built_in">exec</span> <span class="built_in">chmod</span> 755 &#123;&#125; \;   <span class="comment"># 目录设置为755</span></span><br></pre></td></tr></table></figure><h3 id="四、用户管理"><a href="#四、用户管理" class="headerlink" title="四、用户管理"></a>四、用户管理</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 创建用户</span></span><br><span class="line">useradd tom                    <span class="comment"># 创建用户tom</span></span><br><span class="line">passwd tom                     <span class="comment"># 设置用户tom的密码</span></span><br><span class="line">useradd -m -g www dev          <span class="comment"># 创建用户dev，加入www组并创建home目录</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看用户</span></span><br><span class="line"><span class="built_in">who</span>                            <span class="comment"># 查看当前登录用户</span></span><br><span class="line">w                              <span class="comment"># 查看更详细的登录信息</span></span><br><span class="line"><span class="built_in">id</span> tom                         <span class="comment"># 查看用户tom的UID和GID</span></span><br><span class="line"><span class="built_in">groups</span>                         <span class="comment"># 查看当前用户所属组</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 切换用户</span></span><br><span class="line">su - tom                       <span class="comment"># 切换到tom用户</span></span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">command</span>                   <span class="comment"># 以root身份执行命令</span></span><br></pre></td></tr></table></figure><h3 id="五、进程管理"><a href="#五、进程管理" class="headerlink" title="五、进程管理"></a>五、进程管理</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看进程</span></span><br><span class="line">ps -ef                         <span class="comment"># 查看所有进程的详细信息</span></span><br><span class="line">ps aux                         <span class="comment"># 查看所有进程（CPU/内存占用）</span></span><br><span class="line">ps -ef | grep java             <span class="comment"># 查看Java相关进程</span></span><br><span class="line">top                            <span class="comment"># 实时查看进程状态（按q退出）</span></span><br><span class="line">top -bn1                       <span class="comment"># 一次性输出top结果（适合脚本采集）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 终止进程</span></span><br><span class="line"><span class="built_in">kill</span> 12345                     <span class="comment"># 优雅终止PID为12345的进程</span></span><br><span class="line"><span class="built_in">kill</span> -9 12345                  <span class="comment"># 强制杀掉PID为12345的进程</span></span><br><span class="line">killall java                   <span class="comment"># 杀掉所有Java进程</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 后台运行</span></span><br><span class="line"><span class="built_in">nohup</span> java -jar app.jar &amp;      <span class="comment"># 后台运行Java应用，退出终端不停止</span></span><br><span class="line"><span class="built_in">jobs</span>                           <span class="comment"># 查看后台运行的任务列表</span></span><br><span class="line"><span class="built_in">fg</span> %1                          <span class="comment"># 将后台任务1调回前台</span></span><br></pre></td></tr></table></figure><p><strong>运维场景：重启 Java 应用</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 完整的应用重启流程</span></span><br><span class="line">ps -ef | grep app.jar | grep -v grep  <span class="comment"># 先找到进程</span></span><br><span class="line"><span class="built_in">kill</span> 12345                             <span class="comment"># 优雅停止</span></span><br><span class="line"><span class="built_in">sleep</span> 3                                <span class="comment"># 等待3秒</span></span><br><span class="line"><span class="built_in">nohup</span> java -jar app.jar &gt; app.log 2&gt;&amp;1 &amp;   <span class="comment"># 后台启动，日志重定向</span></span><br><span class="line"><span class="built_in">tail</span> -f app.log                       <span class="comment"># 实时查看启动日志</span></span><br></pre></td></tr></table></figure><h3 id="六、磁盘管理"><a href="#六、磁盘管理" class="headerlink" title="六、磁盘管理"></a>六、磁盘管理</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看磁盘整体情况</span></span><br><span class="line"><span class="built_in">df</span> -h                          <span class="comment"># 以人类可读格式查看磁盘使用情况</span></span><br><span class="line"><span class="built_in">df</span> -h /dev/sda1                <span class="comment"># 查看指定分区</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看目录大小</span></span><br><span class="line"><span class="built_in">du</span> -sh /var/log/               <span class="comment"># 查看目录总大小</span></span><br><span class="line"><span class="built_in">du</span> -sh *                       <span class="comment"># 查看当前目录下每个子目录的大小</span></span><br><span class="line"><span class="built_in">du</span> -h --max-depth=1 /home/     <span class="comment"># 只看一层目录的大小</span></span><br><span class="line"><span class="built_in">du</span> -sh /* | <span class="built_in">sort</span> -rh           <span class="comment"># 查看根目录下各目录大小并排序</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 磁盘空间排查</span></span><br><span class="line"><span class="built_in">du</span> -sh /var/log/               <span class="comment"># 检查日志目录大小</span></span><br><span class="line"><span class="built_in">du</span> -sh /tmp/                   <span class="comment"># 检查临时目录大小</span></span><br><span class="line"><span class="built_in">du</span> -sh /opt/                   <span class="comment"># 检查应用目录大小</span></span><br></pre></td></tr></table></figure><p><strong>运维场景：磁盘告警排查</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 第一步：查看整体使用情况</span></span><br><span class="line"><span class="built_in">df</span> -h</span><br><span class="line"></span><br><span class="line"><span class="comment"># 第二步：找到最大的目录</span></span><br><span class="line"><span class="built_in">du</span> -sh /* | <span class="built_in">sort</span> -rh | <span class="built_in">head</span> -10</span><br><span class="line"></span><br><span class="line"><span class="comment"># 第三步：逐层深入找到大文件</span></span><br><span class="line"><span class="built_in">du</span> -sh /var/* | <span class="built_in">sort</span> -rh | <span class="built_in">head</span> -5</span><br><span class="line"><span class="built_in">du</span> -sh /var/log/* | <span class="built_in">sort</span> -rh | <span class="built_in">head</span> -5</span><br><span class="line"></span><br><span class="line"><span class="comment"># 第四步：清理日志</span></span><br><span class="line">find /var/log/ -name <span class="string">&quot;*.log&quot;</span> -mtime +30 -<span class="built_in">exec</span> <span class="built_in">rm</span> &#123;&#125; \;</span><br></pre></td></tr></table></figure><h3 id="七、网络管理"><a href="#七、网络管理" class="headerlink" title="七、网络管理"></a>七、网络管理</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看端口</span></span><br><span class="line">netstat -tlnp                  <span class="comment"># 查看所有TCP监听端口及对应进程</span></span><br><span class="line">netstat -tlnp | grep 8080      <span class="comment"># 查看8080端口被谁占用</span></span><br><span class="line">netstat -an | grep ESTABLISHED <span class="comment"># 查看已建立的连接</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># lsof 查看端口（更直观）</span></span><br><span class="line">lsof -i :8080                  <span class="comment"># 查看8080端口被哪个进程占用</span></span><br><span class="line">lsof -i tcp                    <span class="comment"># 查看所有TCP连接</span></span><br><span class="line">lsof -p 12345                  <span class="comment"># 查看进程12345打开了哪些文件</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 网络连通性</span></span><br><span class="line">ping -c 4 baidu.com            <span class="comment"># 测试网络连通性，发4个包</span></span><br><span class="line">curl -I http://localhost:8080   <span class="comment"># 查看HTTP响应头</span></span><br><span class="line">telnet 192.168.1.1 3306        <span class="comment"># 测试端口是否可达</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># DNS相关</span></span><br><span class="line">nslookup www.baidu.com         <span class="comment"># 域名解析查询</span></span><br><span class="line">host www.baidu.com             <span class="comment"># 简化的域名查询</span></span><br></pre></td></tr></table></figure><p><strong>运维场景：端口被占用了怎么办</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 场景：Tomcat启动报错 Address already in use</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 步骤1：查看端口被谁占了</span></span><br><span class="line">netstat -tlnp | grep 8080</span><br><span class="line"><span class="comment"># 输出：tcp6  0  0 :::8080  :::*  LISTEN  12345/java</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 步骤2：确认是这个进程，可以选择：</span></span><br><span class="line"><span class="comment"># 方案A：杀掉占用进程</span></span><br><span class="line"><span class="built_in">kill</span> 12345</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方案B：或者换个端口启动，如果杀不掉的话</span></span><br></pre></td></tr></table></figure><h3 id="八、压缩解压"><a href="#八、压缩解压" class="headerlink" title="八、压缩解压"></a>八、压缩解压</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># tar 打包/解包</span></span><br><span class="line">tar -czf myapp.tar.gz /var/www/myapp/      <span class="comment"># 打包并压缩为.tar.gz</span></span><br><span class="line">tar -xzf myapp.tar.gz                      <span class="comment"># 解压.tar.gz包</span></span><br><span class="line">tar -tzf myapp.tar.gz                      <span class="comment"># 查看包内容列表（不实际解压）</span></span><br><span class="line">tar -xjf myapp.tar.bz2                     <span class="comment"># 解压.tar.bz2包</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># gzip压缩</span></span><br><span class="line">gzip app.log                               <span class="comment"># 压缩文件（生成app.log.gz）</span></span><br><span class="line">gzip -d app.log.gz                         <span class="comment"># 解压缩</span></span><br><span class="line">gunzip app.log.gz                          <span class="comment"># 同上，解压缩</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># zip（跨平台常用）</span></span><br><span class="line">zip -r myapp.zip /var/www/myapp/           <span class="comment"># 压缩目录</span></span><br><span class="line">unzip myapp.zip -d /tmp/                   <span class="comment"># 解压到指定目录</span></span><br></pre></td></tr></table></figure><p><strong>运维场景：备份项目并压缩</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 备份项目目录，按日期命名</span></span><br><span class="line">tar -czf myapp_$(<span class="built_in">date</span> +%Y%m%d_%H%M%S).tar.gz /var/www/myapp/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 解压到指定目录</span></span><br><span class="line">tar -xzf myapp_20181118.tar.gz -C /tmp/restore/</span><br></pre></td></tr></table></figure><h3 id="九、vim-基本操作"><a href="#九、vim-基本操作" class="headerlink" title="九、vim 基本操作"></a>九、vim 基本操作</h3><p>vim 刚接触时确实让人抓狂，记住下面这些就够日常用了：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 打开文件</span></span><br><span class="line">vim app.conf               <span class="comment"># 打开文件</span></span><br><span class="line">vim +100 app.conf          <span class="comment"># 打开文件并定位到第100行</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 命令模式（刚进入vim时的状态）</span></span><br><span class="line">i                          <span class="comment"># 在光标前插入（进入插入模式）</span></span><br><span class="line">a                          <span class="comment"># 在光标后插入</span></span><br><span class="line"><span class="built_in">dd</span>                         <span class="comment"># 删除当前行</span></span><br><span class="line">5dd                        <span class="comment"># 删除从当前行开始的5行</span></span><br><span class="line">yy                         <span class="comment"># 复制当前行</span></span><br><span class="line">p                          <span class="comment"># 粘贴</span></span><br><span class="line">u                          <span class="comment"># 撤销</span></span><br><span class="line">Ctrl + r                   <span class="comment"># 重做（取消撤销）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 末行模式（按:进入）</span></span><br><span class="line">:w                         <span class="comment"># 保存</span></span><br><span class="line">:q                         <span class="comment"># 退出</span></span><br><span class="line">:wq                        <span class="comment"># 保存并退出</span></span><br><span class="line">:q!                        <span class="comment"># 不保存强制退出</span></span><br><span class="line">:%s/旧文本/新文本/g        <span class="comment"># 全文替换</span></span><br><span class="line">:<span class="built_in">set</span> nu                    <span class="comment"># 显示行号</span></span><br></pre></td></tr></table></figure><h3 id="十、组合命令与管道"><a href="#十、组合命令与管道" class="headerlink" title="十、组合命令与管道"></a>十、组合命令与管道</h3><p>管道 <code>|</code> 是 Linux 命令的精髓，它把前一个命令的输出作为后一个命令的输入，像流水线一样：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 管道的基本使用</span></span><br><span class="line">ps -ef | grep java              <span class="comment"># 在进程列表中搜索Java进程</span></span><br><span class="line">netstat -tlnp | grep 80         <span class="comment"># 在网络连接中搜索80端口</span></span><br><span class="line"><span class="built_in">cat</span> app.log | grep ERROR | <span class="built_in">head</span> -20    <span class="comment"># 在日志中找错误，只看前20条</span></span><br><span class="line"><span class="built_in">ls</span> -l | grep <span class="string">&quot;^d&quot;</span>               <span class="comment"># 只列出目录（行首为d的是目录）</span></span><br><span class="line"><span class="built_in">ls</span> -l | awk <span class="string">&#x27;&#123;print $5, $NF&#125;&#x27;</span>   <span class="comment"># 只显示文件大小和名称</span></span><br><span class="line"><span class="built_in">ls</span> -l | <span class="built_in">sort</span> -k5 -rn | <span class="built_in">head</span> -5  <span class="comment"># 列出当前目录最大的5个文件</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出重定向</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;Hello&quot;</span> &gt; README.md        <span class="comment"># 覆盖写入文件</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;World&quot;</span> &gt;&gt; README.md       <span class="comment"># 追加写入文件</span></span><br><span class="line"><span class="built_in">ls</span> /nonexist 2&gt; error.log       <span class="comment"># 将错误信息重定向到文件</span></span><br><span class="line">java -jar app.jar &gt; app.log 2&gt;&amp;1 &amp;    <span class="comment"># 标准输出和错误都输出到日志文件</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 实际运维组合命令示例</span></span><br><span class="line"><span class="comment"># 找出占用CPU最高的进程</span></span><br><span class="line">top -bn1 | <span class="built_in">head</span> -20</span><br><span class="line"></span><br><span class="line"><span class="comment"># 统计日志中各IP的访问次数</span></span><br><span class="line"><span class="built_in">cat</span> access.log | awk <span class="string">&#x27;&#123;print $1&#125;&#x27;</span> | <span class="built_in">sort</span> | <span class="built_in">uniq</span> -c | <span class="built_in">sort</span> -rn | <span class="built_in">head</span> -10</span><br><span class="line"></span><br><span class="line"><span class="comment"># 批量替换配置文件中的IP</span></span><br><span class="line">find /app/config/ -name <span class="string">&quot;*.properties&quot;</span> | xargs sed -i <span class="string">&#x27;s/192.168.1.100/192.168.2.100/g&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 压缩30天前的日志</span></span><br><span class="line">find /var/log/myapp/ -name <span class="string">&quot;*.log&quot;</span> -mtime +30 | xargs gzip</span><br></pre></td></tr></table></figure><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Linux%20pipeline%20concept%20diagram%20showing%20commands%20connected%20by%20pipes%20data%20flow&image_size=landscape_16_9" alt="Linux命令管道工作流程"></p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>两周的密集使用下来，从最初在终端面前手足无措，到能比较顺畅地完成日常运维操作，有一些心得想分享给和我一样从 Windows 转 Linux 的同学：</p><p><strong>1. Linux 命令不需要死记硬背</strong></p><p>命令太多了，没有人能记住所有命令的所有参数。关键是要知道<strong>解决某个问题可以用什么命令</strong>，具体的参数用法用 <code>man</code> 或 <code>--help</code> 现查就行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">man <span class="built_in">ls</span>                 <span class="comment"># 查看ls的完整手册（按q退出）</span></span><br><span class="line"><span class="built_in">ls</span> --<span class="built_in">help</span>              <span class="comment"># 查看ls的快速帮助（推荐，更直观）</span></span><br></pre></td></tr></table></figure><p>我基本上每天都要查几次 <code>tar</code> 的参数——“压缩是 <code>-czf</code> 还是 <code>-xzf</code> 来着”——这很正常，查多了自然就记住了。</p><p><strong>2. 管道是 Linux 的精髓</strong></p><p>管道 <code>|</code> 让我真正感受到了 Linux 命令行的威力。单个命令功能有限，但组合起来能做很复杂的事情。就像搭积木一样，把一个个小命令拼起来解决问题。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 学会了管道之后，就能写出这样的命令来解决复杂问题</span></span><br><span class="line">ps -ef | grep java | grep -v grep | awk <span class="string">&#x27;&#123;print $2&#125;&#x27;</span> | xargs <span class="built_in">kill</span> -9</span><br></pre></td></tr></table></figure><p><strong>3. 多用 alias 简化常用操作</strong></p><p>把常用的长命令设成别名，能省不少事：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">alias</span> ll=<span class="string">&#x27;ls -alh&#x27;</span></span><br><span class="line"><span class="built_in">alias</span> g=<span class="string">&#x27;grep --color=auto&#x27;</span></span><br><span class="line"><span class="built_in">alias</span> ..=<span class="string">&#x27;cd ..&#x27;</span></span><br><span class="line"><span class="comment"># 写入 .bashrc 后永久生效</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;alias ll=&#x27;ls -alh&#x27;&quot;</span> &gt;&gt; ~/.bashrc</span><br><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br></pre></td></tr></table></figure><p><strong>4. 小心 rm -rf</strong></p><p><code>rm -rf</code> 是 Linux 上最危险的命令之一。没有回收站，没有二次确认，文件直接就没了。使用前一定要确认路径是否正确，尤其是在 root 用户下。建议养成习惯：</p><ul><li>执行 <code>rm</code> 前先 <code>ls</code> 确认一下</li><li>重要的文件先备份</li><li>用 <code>mv</code> 代替 <code>rm</code>，把文件移到 <code>/tmp/</code> 目录（系统定期会清理）</li></ul><p><strong>5. 多敲多练是最好的学习方式</strong></p><p>读十遍这篇文章不如自己在服务器上敲一遍。我在自己的电脑上装了虚拟机，把上面的命令都练了一遍。遇到实际运维问题时，先百度&#x2F;Google，解决了就记下来，慢慢就形成了自己的命令速查手册。</p><p>现在回头看，Linux 命令行没有想象的那么可怕。它只是和 Windows 不同的另一种工作方式，适应了之后甚至会喜欢上这种高效的操作。希望这篇文章对同样刚接触 Linux 的你有所帮助。</p><hr><p><em>这篇文章发布于 2018 年 11 月，是我刚接触 Linux 时的学习笔记。命令在不同发行版上可能略有差异，文中以 CentOS 7.4 为准。</em></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;2018年双十一刚过，项目终于要部署上线了。公司买了两台阿里云ECS服务器，操作系统是CentOS 7.4。作为一个从大学到现在一直在Win</summary>
      
    
    
    
    <category term="Linux运维" scheme="https://swagch.github.io/categories/Linux%E8%BF%90%E7%BB%B4/"/>
    
    
    <category term="Linux" scheme="https://swagch.github.io/tags/Linux/"/>
    
    <category term="Shell" scheme="https://swagch.github.io/tags/Shell/"/>
    
    <category term="运维" scheme="https://swagch.github.io/tags/%E8%BF%90%E7%BB%B4/"/>
    
    <category term="命令行" scheme="https://swagch.github.io/tags/%E5%91%BD%E4%BB%A4%E8%A1%8C/"/>
    
    <category term="服务器" scheme="https://swagch.github.io/tags/%E6%9C%8D%E5%8A%A1%E5%99%A8/"/>
    
  </entry>
  
  <entry>
    <title>MySQL基础增删改查</title>
    <link href="https://swagch.github.io/2018/10/28/2018-10-28-MySQL%E5%9F%BA%E7%A1%80%E5%A2%9E%E5%88%A0%E6%94%B9%E6%9F%A5/"/>
    <id>https://swagch.github.io/2018/10/28/2018-10-28-MySQL%E5%9F%BA%E7%A1%80%E5%A2%9E%E5%88%A0%E6%94%B9%E6%9F%A5/</id>
    <published>2018-10-28T13:05:16.000Z</published>
    <updated>2026-06-09T08:03:53.849Z</updated>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>学完JDBC之后，我意识到一个很严重的问题——我一直在“照着写”SQL，却从来没有真正理解过每条SQL背后的含义。JDBC教会了我怎么用Java连接数据库、怎么执行SQL、怎么处理ResultSet，但SQL本身呢？我只会照猫画虎地写几句SELECT和INSERT，稍微复杂一点的查询就抓瞎。</p><p>这段时间我决定从头系统地学习SQL，从最基础的建表开始，到增删改查，再到多表关联、聚合函数、索引优化，一步一步把每个知识点搞清楚。这篇文章就是我在2018年10月学习MySQL基础CRUD的完整记录。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=SQL%20query%20execution%20flowchart%20showing%20FROM%20WHERE%20GROUP%20BY%20HAVING%20SELECT%20ORDER%20BY%20steps,%20technical%20diagram%20on%20dark%20blue%20background&image_size=landscape_4_3"></p><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>在实际写SQL的过程中，我遇到了好几个一直没搞明白的问题：</p><ol><li><strong>DELETE和TRUNCATE到底有什么区别？</strong> 之前一直以为都是删数据，随便用哪个都行，结果有一次用TRUNCATE删完数据发现回不去了。</li><li><strong>GROUP BY和HAVING总是写错</strong>，经常把过滤条件写在WHERE里，结果报错，搞不清什么时候该用WHERE、什么时候该用HAVING。</li><li><strong>多表JOIN搞不清关系</strong>，INNER JOIN、LEFT JOIN、RIGHT JOIN到底返回什么数据，每次都要画图才能想明白。</li><li><strong>索引加了但查询还是很慢</strong>，以为建了索引就万事大吉，结果有些查询根本没走索引。</li><li><strong>不知道什么时候该建索引</strong>，哪些列该建、哪些不该建，完全没有概念。</li></ol><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><h2 id="DELETE和TRUNCATE的区别"><a href="#DELETE和TRUNCATE的区别" class="headerlink" title="DELETE和TRUNCATE的区别"></a>DELETE和TRUNCATE的区别</h2><p>这两个看起来都是“删数据”，但底层机制完全不同：</p><table><thead><tr><th>对比点</th><th>DELETE</th><th>TRUNCATE</th></tr></thead><tbody><tr><td>删除方式</td><td>逐行删除</td><td>清空并重建表结构</td></tr><tr><td>SQL类型</td><td>DML</td><td>DDL</td></tr><tr><td>是否可回滚</td><td>在事务中可回滚</td><td>通常不可回滚</td></tr><tr><td>是否支持WHERE</td><td>支持</td><td>不支持</td></tr><tr><td>自增ID</td><td>一般不重置</td><td>通常会重置</td></tr><tr><td>执行速度</td><td>数据量大时较慢</td><td>通常更快</td></tr></tbody></table><p>简单理解：DELETE是一行一行删，可以配合WHERE精确删除，也可以在事务中回滚；TRUNCATE更像是把表清空后重新整理，速度快，但风险也更高。</p><h2 id="GROUP-BY和HAVING"><a href="#GROUP-BY和HAVING" class="headerlink" title="GROUP BY和HAVING"></a>GROUP BY和HAVING</h2><p>我之前经常把聚合条件写到WHERE里，例如想查平均分大于80的学生，却写成：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> student_id, <span class="built_in">AVG</span>(score)</span><br><span class="line"><span class="keyword">FROM</span> score</span><br><span class="line"><span class="keyword">WHERE</span> <span class="built_in">AVG</span>(score) <span class="operator">&gt;</span> <span class="number">80</span></span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> student_id;</span><br></pre></td></tr></table></figure><p>这肯定是不对的，因为WHERE执行时还没有分组，自然也没有AVG(score)这个结果。正确写法应该是：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> student_id, <span class="built_in">AVG</span>(score)</span><br><span class="line"><span class="keyword">FROM</span> score</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> student_id</span><br><span class="line"><span class="keyword">HAVING</span> <span class="built_in">AVG</span>(score) <span class="operator">&gt;</span> <span class="number">80</span>;</span><br></pre></td></tr></table></figure><p>WHERE是在分组前过滤原始行，HAVING是在分组后过滤聚合结果。</p><h2 id="JOIN类型的区别"><a href="#JOIN类型的区别" class="headerlink" title="JOIN类型的区别"></a>JOIN类型的区别</h2><p>JOIN最核心的不是语法，而是先搞清楚两张表之间的关系：</p><ul><li><strong>INNER JOIN</strong>：只返回两边都匹配的数据。</li><li><strong>LEFT JOIN</strong>：左表全部保留，右表匹配不到的地方用NULL补齐。</li><li><strong>RIGHT JOIN</strong>：右表全部保留，左表匹配不到的地方用NULL补齐。</li></ul><p>实际开发中我感觉LEFT JOIN用得更多，因为很多时候我们会以一张主表为中心，再去关联其它明细表。</p><h2 id="索引不是万能的"><a href="#索引不是万能的" class="headerlink" title="索引不是万能的"></a>索引不是万能的</h2><p>索引像书的目录，可以帮助数据库快速定位数据。但它不是万能药，加了索引并不代表一定快：</p><ul><li>表数据量很小时，全表扫描可能更快。</li><li>查询返回的数据太多时，走索引反而不划算。</li><li>在索引列上使用函数，可能导致索引失效。</li><li>LIKE以<code>%</code>开头时，通常无法利用普通索引。</li><li>字段发生隐式类型转换时，也可能导致索引失效。</li></ul><h2 id="索引应该建在什么列上"><a href="#索引应该建在什么列上" class="headerlink" title="索引应该建在什么列上"></a>索引应该建在什么列上</h2><p>比较适合建索引的列：</p><ul><li>区分度高的列，比如学生ID、手机号、身份证号。</li><li>经常出现在WHERE条件里的列。</li><li>经常用于JOIN关联的外键列。</li><li>经常用于ORDER BY或GROUP BY的列。</li></ul><p>不太适合建索引的列：</p><ul><li>区分度很低的列，比如性别。</li><li>经常更新的列，因为维护索引也有成本。</li><li>很少参与查询条件的列。</li></ul><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=MySQL%20index%20B-tree%20structure%20diagram%20showing%20how%20index%20speeds%20up%20query,%20versus%20full%20table%20scan,%20technical%20illustration%20on%20dark%20background&image_size=landscape_4_3"></p><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="准备三张表"><a href="#准备三张表" class="headerlink" title="准备三张表"></a>准备三张表</h2><p>我用学生、课程、成绩三张表来练习，关系比较清晰：一个学生可以选择多门课程，一门课程也可以被多个学生选择，成绩表负责连接学生和课程。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 学生表：保存学生基本信息</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> student (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT COMMENT <span class="string">&#x27;学生ID&#x27;</span>,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;姓名&#x27;</span>,</span><br><span class="line">    age <span class="type">INT</span> COMMENT <span class="string">&#x27;年龄&#x27;</span>,</span><br><span class="line">    gender <span class="type">VARCHAR</span>(<span class="number">10</span>) COMMENT <span class="string">&#x27;性别&#x27;</span>,</span><br><span class="line">    major <span class="type">VARCHAR</span>(<span class="number">50</span>) COMMENT <span class="string">&#x27;专业&#x27;</span>,</span><br><span class="line">    create_time DATETIME <span class="keyword">DEFAULT</span> <span class="built_in">CURRENT_TIMESTAMP</span> COMMENT <span class="string">&#x27;创建时间&#x27;</span></span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8 COMMENT<span class="operator">=</span><span class="string">&#x27;学生表&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 课程表：保存课程基本信息</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> course (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT COMMENT <span class="string">&#x27;课程ID&#x27;</span>,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;课程名称&#x27;</span>,</span><br><span class="line">    credit <span class="type">INT</span> <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;学分&#x27;</span></span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8 COMMENT<span class="operator">=</span><span class="string">&#x27;课程表&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 成绩表：保存学生和课程的对应关系以及成绩</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> score (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT COMMENT <span class="string">&#x27;成绩ID&#x27;</span>,</span><br><span class="line">    student_id <span class="type">INT</span> <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;学生ID&#x27;</span>,</span><br><span class="line">    course_id <span class="type">INT</span> <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;课程ID&#x27;</span>,</span><br><span class="line">    score <span class="type">DECIMAL</span>(<span class="number">5</span>,<span class="number">2</span>) COMMENT <span class="string">&#x27;成绩&#x27;</span>,</span><br><span class="line">    exam_time <span class="type">DATE</span> COMMENT <span class="string">&#x27;考试日期&#x27;</span>,</span><br><span class="line">    <span class="keyword">CONSTRAINT</span> fk_score_student <span class="keyword">FOREIGN KEY</span> (student_id) <span class="keyword">REFERENCES</span> student(id),</span><br><span class="line">    <span class="keyword">CONSTRAINT</span> fk_score_course <span class="keyword">FOREIGN KEY</span> (course_id) <span class="keyword">REFERENCES</span> course(id)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8 COMMENT<span class="operator">=</span><span class="string">&#x27;成绩表&#x27;</span>;</span><br></pre></td></tr></table></figure><h2 id="INSERT：新增数据"><a href="#INSERT：新增数据" class="headerlink" title="INSERT：新增数据"></a>INSERT：新增数据</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 新增学生数据</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> student (name, age, gender, major) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="string">&#x27;张三&#x27;</span>, <span class="number">20</span>, <span class="string">&#x27;男&#x27;</span>, <span class="string">&#x27;计算机科学&#x27;</span>),</span><br><span class="line">(<span class="string">&#x27;李四&#x27;</span>, <span class="number">21</span>, <span class="string">&#x27;女&#x27;</span>, <span class="string">&#x27;软件工程&#x27;</span>),</span><br><span class="line">(<span class="string">&#x27;王五&#x27;</span>, <span class="number">19</span>, <span class="string">&#x27;男&#x27;</span>, <span class="string">&#x27;数据科学&#x27;</span>),</span><br><span class="line">(<span class="string">&#x27;赵六&#x27;</span>, <span class="number">22</span>, <span class="string">&#x27;女&#x27;</span>, <span class="string">&#x27;计算机科学&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 新增课程数据</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> course (name, credit) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="string">&#x27;数据库原理&#x27;</span>, <span class="number">4</span>),</span><br><span class="line">(<span class="string">&#x27;数据结构&#x27;</span>, <span class="number">3</span>),</span><br><span class="line">(<span class="string">&#x27;操作系统&#x27;</span>, <span class="number">4</span>),</span><br><span class="line">(<span class="string">&#x27;计算机网络&#x27;</span>, <span class="number">3</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 新增成绩数据</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> score (student_id, course_id, score, exam_time) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="number">1</span>, <span class="number">1</span>, <span class="number">92.50</span>, <span class="string">&#x27;2018-10-20&#x27;</span>),</span><br><span class="line">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">88.00</span>, <span class="string">&#x27;2018-10-21&#x27;</span>),</span><br><span class="line">(<span class="number">2</span>, <span class="number">1</span>, <span class="number">78.50</span>, <span class="string">&#x27;2018-10-20&#x27;</span>),</span><br><span class="line">(<span class="number">2</span>, <span class="number">3</span>, <span class="number">95.00</span>, <span class="string">&#x27;2018-10-22&#x27;</span>),</span><br><span class="line">(<span class="number">3</span>, <span class="number">2</span>, <span class="number">66.00</span>, <span class="string">&#x27;2018-10-21&#x27;</span>),</span><br><span class="line">(<span class="number">3</span>, <span class="number">4</span>, <span class="number">72.50</span>, <span class="string">&#x27;2018-10-23&#x27;</span>),</span><br><span class="line">(<span class="number">4</span>, <span class="number">1</span>, <span class="number">85.00</span>, <span class="string">&#x27;2018-10-20&#x27;</span>),</span><br><span class="line">(<span class="number">4</span>, <span class="number">4</span>, <span class="number">90.00</span>, <span class="string">&#x27;2018-10-23&#x27;</span>);</span><br></pre></td></tr></table></figure><h2 id="UPDATE：修改数据"><a href="#UPDATE：修改数据" class="headerlink" title="UPDATE：修改数据"></a>UPDATE：修改数据</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 修改张三的年龄</span></span><br><span class="line"><span class="keyword">UPDATE</span> student</span><br><span class="line"><span class="keyword">SET</span> age <span class="operator">=</span> <span class="number">21</span></span><br><span class="line"><span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;张三&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 修改数据库原理课程的学分</span></span><br><span class="line"><span class="keyword">UPDATE</span> course</span><br><span class="line"><span class="keyword">SET</span> credit <span class="operator">=</span> <span class="number">5</span></span><br><span class="line"><span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;数据库原理&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 给王五的所有成绩加5分</span></span><br><span class="line"><span class="keyword">UPDATE</span> score</span><br><span class="line"><span class="keyword">SET</span> score <span class="operator">=</span> score <span class="operator">+</span> <span class="number">5</span></span><br><span class="line"><span class="keyword">WHERE</span> student_id <span class="operator">=</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> id <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;王五&#x27;</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><h2 id="DELETE：删除数据"><a href="#DELETE：删除数据" class="headerlink" title="DELETE：删除数据"></a>DELETE：删除数据</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 删除低于60分的成绩记录</span></span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> score</span><br><span class="line"><span class="keyword">WHERE</span> score <span class="operator">&lt;</span> <span class="number">60</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除某个学生的成绩记录</span></span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> score</span><br><span class="line"><span class="keyword">WHERE</span> student_id <span class="operator">=</span> <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除成绩表全部数据，可配合事务回滚</span></span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> score;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 清空成绩表，不支持WHERE，风险更高</span></span><br><span class="line"><span class="keyword">TRUNCATE</span> <span class="keyword">TABLE</span> score;</span><br></pre></td></tr></table></figure><h2 id="SELECT：基础查询"><a href="#SELECT：基础查询" class="headerlink" title="SELECT：基础查询"></a>SELECT：基础查询</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询所有学生</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 只查询指定列</span></span><br><span class="line"><span class="keyword">SELECT</span> name, age, major <span class="keyword">FROM</span> student;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 条件查询</span></span><br><span class="line"><span class="keyword">SELECT</span> name, age</span><br><span class="line"><span class="keyword">FROM</span> student</span><br><span class="line"><span class="keyword">WHERE</span> major <span class="operator">=</span> <span class="string">&#x27;计算机科学&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 模糊查询</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student</span><br><span class="line"><span class="keyword">WHERE</span> name <span class="keyword">LIKE</span> <span class="string">&#x27;张%&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 排序查询</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> age <span class="keyword">DESC</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 分页查询：从第0条开始取10条</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student</span><br><span class="line">LIMIT <span class="number">0</span>, <span class="number">10</span>;</span><br></pre></td></tr></table></figure><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Relational%20database%20tables%20student%20course%20score%20with%20primary%20keys%20foreign%20keys%20and%20CRUD%20SQL%20examples,%20clean%20technical%20infographic&image_size=landscape_4_3"></p><h2 id="多表JOIN查询"><a href="#多表JOIN查询" class="headerlink" title="多表JOIN查询"></a>多表JOIN查询</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- INNER JOIN：只查询有成绩的学生和课程</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    c.name <span class="keyword">AS</span> course_name,</span><br><span class="line">    sc.score <span class="keyword">AS</span> exam_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> course c <span class="keyword">ON</span> sc.course_id <span class="operator">=</span> c.id;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- LEFT JOIN：查询所有学生，即使学生还没有成绩也会显示</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    c.name <span class="keyword">AS</span> course_name,</span><br><span class="line">    sc.score <span class="keyword">AS</span> exam_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> course c <span class="keyword">ON</span> sc.course_id <span class="operator">=</span> c.id;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- RIGHT JOIN：以课程表为主，查询所有课程的选课成绩</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    c.name <span class="keyword">AS</span> course_name,</span><br><span class="line">    sc.score <span class="keyword">AS</span> exam_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">RIGHT</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">RIGHT</span> <span class="keyword">JOIN</span> course c <span class="keyword">ON</span> sc.course_id <span class="operator">=</span> c.id;</span><br></pre></td></tr></table></figure><h2 id="聚合函数和GROUP-BY"><a href="#聚合函数和GROUP-BY" class="headerlink" title="聚合函数和GROUP BY"></a>聚合函数和GROUP BY</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询每个学生的平均成绩</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    <span class="built_in">AVG</span>(sc.score) <span class="keyword">AS</span> avg_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> s.id, s.name;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询平均成绩大于80分的学生</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    <span class="built_in">AVG</span>(sc.score) <span class="keyword">AS</span> avg_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> s.id, s.name</span><br><span class="line"><span class="keyword">HAVING</span> <span class="built_in">AVG</span>(sc.score) <span class="operator">&gt;</span> <span class="number">80</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询每门课程的选课人数、最高分和最低分</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    c.name <span class="keyword">AS</span> course_name,</span><br><span class="line">    <span class="built_in">COUNT</span>(sc.id) <span class="keyword">AS</span> student_count,</span><br><span class="line">    <span class="built_in">MAX</span>(sc.score) <span class="keyword">AS</span> max_score,</span><br><span class="line">    <span class="built_in">MIN</span>(sc.score) <span class="keyword">AS</span> min_score</span><br><span class="line"><span class="keyword">FROM</span> course c</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> c.id <span class="operator">=</span> sc.course_id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> c.id, c.name;</span><br></pre></td></tr></table></figure><h2 id="子查询"><a href="#子查询" class="headerlink" title="子查询"></a>子查询</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查询成绩高于全班平均分的记录</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    sc.score <span class="keyword">AS</span> exam_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">WHERE</span> sc.score <span class="operator">&gt;</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> <span class="built_in">AVG</span>(score) <span class="keyword">FROM</span> score</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询选择了数据库原理课程的学生</span></span><br><span class="line"><span class="keyword">SELECT</span> name</span><br><span class="line"><span class="keyword">FROM</span> student</span><br><span class="line"><span class="keyword">WHERE</span> id <span class="keyword">IN</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> sc.student_id</span><br><span class="line">    <span class="keyword">FROM</span> score sc</span><br><span class="line">    <span class="keyword">INNER</span> <span class="keyword">JOIN</span> course c <span class="keyword">ON</span> sc.course_id <span class="operator">=</span> c.id</span><br><span class="line">    <span class="keyword">WHERE</span> c.name <span class="operator">=</span> <span class="string">&#x27;数据库原理&#x27;</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询至少有一门成绩大于90分的学生</span></span><br><span class="line"><span class="keyword">SELECT</span> s.name</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">EXISTS</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> <span class="number">1</span></span><br><span class="line">    <span class="keyword">FROM</span> score sc</span><br><span class="line">    <span class="keyword">WHERE</span> sc.student_id <span class="operator">=</span> s.id</span><br><span class="line">      <span class="keyword">AND</span> sc.score <span class="operator">&gt;</span> <span class="number">90</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><h2 id="索引创建和使用建议"><a href="#索引创建和使用建议" class="headerlink" title="索引创建和使用建议"></a>索引创建和使用建议</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 给成绩表的学生ID建立索引，提升按学生查询成绩的速度</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_score_student_id <span class="keyword">ON</span> score(student_id);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 给成绩表的课程ID建立索引，提升按课程查询成绩的速度</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_score_course_id <span class="keyword">ON</span> score(course_id);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 给学生姓名建立索引，提升按姓名精确查询的速度</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_student_name <span class="keyword">ON</span> student(name);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 联合索引：适合同时按学生和课程查询成绩的场景</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_score_student_course <span class="keyword">ON</span> score(student_id, course_id);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除索引</span></span><br><span class="line"><span class="keyword">DROP</span> INDEX idx_student_name <span class="keyword">ON</span> student;</span><br></pre></td></tr></table></figure><p>联合索引要注意最左前缀原则。假设建立了<code>(student_id, course_id)</code>这个联合索引：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 可以使用联合索引</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> score <span class="keyword">WHERE</span> student_id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 可以使用联合索引</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> score <span class="keyword">WHERE</span> student_id <span class="operator">=</span> <span class="number">1</span> <span class="keyword">AND</span> course_id <span class="operator">=</span> <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 通常不能完整使用这个联合索引，因为跳过了最左边的student_id</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> score <span class="keyword">WHERE</span> course_id <span class="operator">=</span> <span class="number">2</span>;</span><br></pre></td></tr></table></figure><h2 id="EXPLAIN查看执行计划"><a href="#EXPLAIN查看执行计划" class="headerlink" title="EXPLAIN查看执行计划"></a>EXPLAIN查看执行计划</h2><p>EXPLAIN可以帮助我判断SQL到底有没有走索引，不能只靠感觉猜。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 查看按学生ID查询成绩的执行计划</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> score <span class="keyword">WHERE</span> student_id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看JOIN查询的执行计划</span></span><br><span class="line">EXPLAIN</span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    c.name <span class="keyword">AS</span> course_name,</span><br><span class="line">    sc.score <span class="keyword">AS</span> exam_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> course c <span class="keyword">ON</span> sc.course_id <span class="operator">=</span> c.id;</span><br></pre></td></tr></table></figure><p>EXPLAIN里我现在重点看这几个字段：</p><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td>type</td><td>访问类型，ALL通常表示全表扫描，ref、range一般比ALL好</td></tr><tr><td>possible_keys</td><td>可能用到的索引</td></tr><tr><td>key</td><td>实际使用的索引</td></tr><tr><td>rows</td><td>MySQL预估要扫描的行数</td></tr><tr><td>Extra</td><td>额外信息，比如Using index、Using where、Using filesort</td></tr></tbody></table><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 如果name没有索引，可能会全表扫描</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;张三&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 建立索引后，再观察key和rows的变化</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_student_name <span class="keyword">ON</span> student(name);</span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;张三&#x27;</span>;</span><br></pre></td></tr></table></figure><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次系统整理MySQL基础增删改查之后，我最大的感受是：SQL真的是后端开发的基本功。以前写JDBC时，我只关心Java代码能不能跑通，现在才发现SQL本身才是和数据库沟通的核心。</p><p>我需要牢记的几个点：</p><ol><li><strong>理解SQL执行顺序</strong>：FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY。只有知道SQL先做什么、后做什么，才能写出正确的分组查询。</li><li><strong>DELETE和TRUNCATE不能混用</strong>：DELETE适合按条件删除，也更容易配合事务；TRUNCATE适合清空整张表，但风险更高。</li><li><strong>JOIN要先想清楚主表和关系</strong>：INNER JOIN看匹配，LEFT JOIN保留左表，RIGHT JOIN保留右表。</li><li><strong>索引要合理使用</strong>：索引应该建在区分度高、经常查询、经常关联的列上，不是越多越好。</li><li><strong>用EXPLAIN验证SQL</strong>：查询慢的时候，不要靠猜，要看执行计划。</li></ol><p>2018年10月的这个阶段，我觉得自己终于从“会照着写SQL”，开始慢慢变成“知道为什么这样写SQL”了。下一步准备继续学习事务、锁和隔离级别，这些应该会更接近真实项目里的数据库问题。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;学完JDBC之后，我意识到一个很严重的问题——我一直在“照着写”SQL，却从来没有真正理解过每条SQL背后的含义。JDBC教会了我怎么用Ja</summary>
      
    
    
    
    <category term="数据库" scheme="https://swagch.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
    
    <category term="MySQL" scheme="https://swagch.github.io/tags/MySQL/"/>
    
    <category term="SQL" scheme="https://swagch.github.io/tags/SQL/"/>
    
    <category term="数据库" scheme="https://swagch.github.io/tags/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
    <category term="CRUD" scheme="https://swagch.github.io/tags/CRUD/"/>
    
    <category term="索引" scheme="https://swagch.github.io/tags/%E7%B4%A2%E5%BC%95/"/>
    
  </entry>
  
  <entry>
    <title>JDBC连接MySQL实践</title>
    <link href="https://swagch.github.io/2018/09/15/2018-09-15-JDBC%E8%BF%9E%E6%8E%A5MySQL%E5%AE%9E%E8%B7%B5/"/>
    <id>https://swagch.github.io/2018/09/15/2018-09-15-JDBC%E8%BF%9E%E6%8E%A5MySQL%E5%AE%9E%E8%B7%B5/</id>
    <published>2018-09-15T02:27:44.000Z</published>
    <updated>2026-06-09T07:52:42.670Z</updated>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>2018 年 9 月，我刚把 Java 基础和 MySQL 的常用语法系统学完。前面写 Java 时，大多是在控制台里打印结果；学 MySQL 时，也主要是在命令行或者图形化工具里执行 SQL。到了这个阶段，一个很自然的问题出现了：能不能用 Java 代码直接操作 MySQL 数据库？</p><p>答案就是 JDBC。</p><p>JDBC，全称是 Java Database Connectivity，它是 Java 提供的一套数据库访问规范。简单理解，JDBC 就像 Java 程序和数据库之间的一座桥：Java 代码通过 JDBC API 发送 SQL，数据库执行后再把结果返回给 Java 程序。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20program%20connects%20to%20MySQL%20through%20JDBC%20driver,%20flow%20diagram%20with%20Java%20Application,%20JDBC%20API,%20MySQL%20Connector%20J,%20MySQL%20Database,%20clean%20technical%20illustration&image_size=landscape_16_9" alt="Java 程序通过 JDBC 连接 MySQL 的流程图"></p><p>我当时的目标很明确：</p><ul><li>用 Java 连接 MySQL；</li><li>用 Java 执行增删改查；</li><li>理解 <code>Connection</code>、<code>PreparedStatement</code>、<code>ResultSet</code> 这些核心对象；</li><li>避免连接泄漏、SQL 注入、中文乱码这些常见坑。</li></ul><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>第一次真正写 JDBC 代码时，问题比想象中多。很多错误不是 SQL 写错，而是连接、驱动、资源释放这些基础细节没有处理好。</p><h2 id="1-ClassNotFoundException-找不到驱动"><a href="#1-ClassNotFoundException-找不到驱动" class="headerlink" title="1. ClassNotFoundException 找不到驱动"></a>1. ClassNotFoundException 找不到驱动</h2><p>最常见的报错是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java.lang.ClassNotFoundException: com.mysql.jdbc.Driver</span><br></pre></td></tr></table></figure><p>这个异常的意思很直接：Java 程序找不到 MySQL 的 JDBC 驱动类。</p><p>当时我以为 JDK 自带了所有数据库驱动，后来才知道 JDBC 只是规范，真正连接 MySQL 还需要 MySQL 官方提供的 Connector&#x2F;J 驱动包。如果没有把 <code>mysql-connector-java</code> 加入 classpath，<code>Class.forName(&quot;com.mysql.jdbc.Driver&quot;)</code> 就会失败。</p><h2 id="2-连接字符串写错"><a href="#2-连接字符串写错" class="headerlink" title="2. 连接字符串写错"></a>2. 连接字符串写错</h2><p>第二类问题是 JDBC URL 写错，比如数据库名漏写、端口写错、参数拼错。</p><p>JDBC 连接 MySQL 的基本格式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jdbc:mysql://host:port/database</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jdbc:mysql://localhost:3306/test</span><br></pre></td></tr></table></figure><p>如果还要处理中文编码，通常需要加上参数：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jdbc:mysql://localhost:3306/test?useUnicode=true&amp;characterEncoding=UTF-8</span><br></pre></td></tr></table></figure><h2 id="3-SQL-注入风险"><a href="#3-SQL-注入风险" class="headerlink" title="3. SQL 注入风险"></a>3. SQL 注入风险</h2><p>一开始我会用字符串拼接 SQL：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;select * from user where username = &#x27;&quot;</span> + username + <span class="string">&quot;&#x27; and password = &#x27;&quot;</span> + password + <span class="string">&quot;&#x27;&quot;</span>;</span><br></pre></td></tr></table></figure><p>这种写法看起来简单，但非常危险。如果用户输入中带有特殊 SQL 片段，就可能绕过登录校验，甚至破坏数据。</p><h2 id="4-ResultSet-取值顺序混乱"><a href="#4-ResultSet-取值顺序混乱" class="headerlink" title="4. ResultSet 取值顺序混乱"></a>4. ResultSet 取值顺序混乱</h2><p><code>ResultSet</code> 不是一次性把所有数据都变成 Java 对象，而是一个游标。刚拿到 <code>ResultSet</code> 时，游标在第一行之前，必须先调用 <code>next()</code>，再读取当前行的数据。</p><p>如果忘记 <code>next()</code>，或者列名、列下标混着用，很容易出现取值混乱。</p><h2 id="5-资源忘记关闭导致连接泄漏"><a href="#5-资源忘记关闭导致连接泄漏" class="headerlink" title="5. 资源忘记关闭导致连接泄漏"></a>5. 资源忘记关闭导致连接泄漏</h2><p>JDBC 里最容易忽略的一点是资源释放。<code>Connection</code>、<code>Statement</code>、<code>ResultSet</code> 都持有数据库或网络资源，用完不关闭，就可能导致连接越来越多，最后数据库拒绝新的连接。</p><h2 id="6-中文数据存入乱码"><a href="#6-中文数据存入乱码" class="headerlink" title="6. 中文数据存入乱码"></a>6. 中文数据存入乱码</h2><p>如果数据库、表、连接 URL 的编码不一致，中文数据就可能变成问号或者乱码。尤其是早期 MySQL 项目里，连接参数里没有指定 <code>characterEncoding=UTF-8</code> 时很容易遇到。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>把这些问题拆开看，其实它们都对应 JDBC 的几个基础机制。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=JDBC%20debugging%20checklist%20diagram,%20driver%20classpath,%20JDBC%20URL,%20PreparedStatement,%20ResultSet%20cursor,%20resource%20close,%20UTF-8%20encoding,%20technical%20blog%20illustration&image_size=landscape_16_9" alt="JDBC 常见问题定位示意图"></p><h2 id="1-驱动必须加入-classpath"><a href="#1-驱动必须加入-classpath" class="headerlink" title="1. 驱动必须加入 classpath"></a>1. 驱动必须加入 classpath</h2><p>JDBC API 在 JDK 中提供，但 MySQL 的具体驱动实现不在 JDK 里。要连接 MySQL，必须引入 Connector&#x2F;J。</p><p>如果是普通 Java 工程，需要把 jar 包加入 classpath；如果是 Maven 工程，可以加入依赖：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-java<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.1.47<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p>2018 年我使用较多的是 MySQL Connector&#x2F;J 5.x，对应驱动类通常写：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">com.mysql.jdbc.Driver</span><br></pre></td></tr></table></figure><h2 id="2-JDBC-URL-要写完整"><a href="#2-JDBC-URL-要写完整" class="headerlink" title="2. JDBC URL 要写完整"></a>2. JDBC URL 要写完整</h2><p>JDBC URL 不是普通网址，它会被驱动解析。MySQL 的基础格式是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jdbc:mysql://主机名:端口号/数据库名?参数1=值1&amp;参数2=值2</span><br></pre></td></tr></table></figure><p>常用示例：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jdbc:mysql://localhost:3306/jdbc_demo?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>localhost</code> 表示本机数据库；</li><li><code>3306</code> 是 MySQL 默认端口；</li><li><code>jdbc_demo</code> 是数据库名；</li><li><code>useUnicode=true</code> 和 <code>characterEncoding=UTF-8</code> 用于处理中文；</li><li><code>useSSL=false</code> 可以避免部分版本驱动的 SSL 警告。</li></ul><h2 id="3-PreparedStatement-比-Statement-更安全"><a href="#3-PreparedStatement-比-Statement-更安全" class="headerlink" title="3. PreparedStatement 比 Statement 更安全"></a>3. PreparedStatement 比 Statement 更安全</h2><p><code>Statement</code> 通常需要拼接 SQL 字符串，这会带来 SQL 注入风险。<code>PreparedStatement</code> 使用占位符 <code>?</code>，再通过 <code>setXxx</code> 方法传参，参数会被驱动正确处理。</p><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;select * from user where username = ? and password = ?&quot;</span>;</span><br><span class="line"><span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> conn.prepareStatement(sql);</span><br><span class="line">ps.setString(<span class="number">1</span>, username);</span><br><span class="line">ps.setString(<span class="number">2</span>, password);</span><br></pre></td></tr></table></figure><p>这样 SQL 结构和参数值是分离的，可读性和安全性都更好。</p><h2 id="4-ResultSet-是游标模式"><a href="#4-ResultSet-是游标模式" class="headerlink" title="4. ResultSet 是游标模式"></a>4. ResultSet 是游标模式</h2><p><code>ResultSet</code> 可以理解为结果集游标。读取数据时通常这样写：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> (rs.next()) &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">id</span> <span class="operator">=</span> rs.getInt(<span class="string">&quot;id&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">username</span> <span class="operator">=</span> rs.getString(<span class="string">&quot;username&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我更推荐使用列名取值，而不是列下标。列名更直观，SQL 字段顺序调整时也不容易出错。</p><h2 id="5-资源必须关闭"><a href="#5-资源必须关闭" class="headerlink" title="5. 资源必须关闭"></a>5. 资源必须关闭</h2><p>JDBC 资源关闭顺序一般和创建顺序相反：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ResultSet -&gt; Statement/PreparedStatement -&gt; Connection</span><br></pre></td></tr></table></figure><p>JDK 1.7 以后可以使用 try-with-resources 自动关闭资源。不过为了理解原理，先掌握 <code>finally</code> 中手动关闭也很重要。</p><h2 id="6-编码要统一"><a href="#6-编码要统一" class="headerlink" title="6. 编码要统一"></a>6. 编码要统一</h2><p>中文乱码通常不是单点问题，要同时检查：</p><ul><li>数据库编码；</li><li>表编码；</li><li>字段编码；</li><li>JDBC URL 编码参数；</li><li>Java 字符串本身。</li></ul><p>JDBC URL 中至少应明确指定：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">useUnicode=true&amp;characterEncoding=UTF-8</span><br></pre></td></tr></table></figure><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>下面记录一套完整的 JDBC 连接 MySQL 实践代码。示例基于 JDK 1.8、MySQL Connector&#x2F;J 5.x，表结构使用一个简单的 <code>user</code> 表。</p><h2 id="准备表结构"><a href="#准备表结构" class="headerlink" title="准备表结构"></a>准备表结构</h2><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">create</span> database if <span class="keyword">not</span> <span class="keyword">exists</span> jdbc_demo <span class="keyword">default</span> <span class="keyword">character set</span> utf8;</span><br><span class="line"></span><br><span class="line">use jdbc_demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">create table</span> if <span class="keyword">not</span> <span class="keyword">exists</span> <span class="keyword">user</span> (</span><br><span class="line">    id <span class="type">int</span> <span class="keyword">primary key</span> auto_increment,</span><br><span class="line">    username <span class="type">varchar</span>(<span class="number">50</span>) <span class="keyword">not null</span>,</span><br><span class="line">    password <span class="type">varchar</span>(<span class="number">50</span>) <span class="keyword">not null</span>,</span><br><span class="line">    age <span class="type">int</span>,</span><br><span class="line">    create_time datetime</span><br><span class="line">);</span><br></pre></td></tr></table></figure><h2 id="1-JDBC-连接-MySQL-的-6-步代码"><a href="#1-JDBC-连接-MySQL-的-6-步代码" class="headerlink" title="1. JDBC 连接 MySQL 的 6 步代码"></a>1. JDBC 连接 MySQL 的 6 步代码</h2><p>JDBC 最基础的流程可以概括为 6 步：</p><ol><li>加载数据库驱动；</li><li>获取数据库连接；</li><li>编写 SQL；</li><li>创建执行 SQL 的对象；</li><li>执行 SQL 并处理结果；</li><li>关闭资源。</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.DriverManager;</span><br><span class="line"><span class="keyword">import</span> java.sql.PreparedStatement;</span><br><span class="line"><span class="keyword">import</span> java.sql.ResultSet;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">JdbcSixStepDemo</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">url</span> <span class="operator">=</span> <span class="string">&quot;jdbc:mysql://localhost:3306/jdbc_demo?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false&quot;</span>;</span><br><span class="line">        <span class="type">String</span> <span class="variable">username</span> <span class="operator">=</span> <span class="string">&quot;root&quot;</span>;</span><br><span class="line">        <span class="type">String</span> <span class="variable">password</span> <span class="operator">=</span> <span class="string">&quot;root&quot;</span>;</span><br><span class="line"></span><br><span class="line">        <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="type">ResultSet</span> <span class="variable">rs</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class.forName(<span class="string">&quot;com.mysql.jdbc.Driver&quot;</span>);</span><br><span class="line">            conn = DriverManager.getConnection(url, username, password);</span><br><span class="line"></span><br><span class="line">            <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;select id, username, age from user where age &gt; ?&quot;</span>;</span><br><span class="line">            ps = conn.prepareStatement(sql);</span><br><span class="line">            ps.setInt(<span class="number">1</span>, <span class="number">18</span>);</span><br><span class="line"></span><br><span class="line">            rs = ps.executeQuery();</span><br><span class="line">            <span class="keyword">while</span> (rs.next()) &#123;</span><br><span class="line">                <span class="type">int</span> <span class="variable">id</span> <span class="operator">=</span> rs.getInt(<span class="string">&quot;id&quot;</span>);</span><br><span class="line">                <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> rs.getString(<span class="string">&quot;username&quot;</span>);</span><br><span class="line">                <span class="type">int</span> <span class="variable">age</span> <span class="operator">=</span> rs.getInt(<span class="string">&quot;age&quot;</span>);</span><br><span class="line">                System.out.println(id + <span class="string">&quot; - &quot;</span> + name + <span class="string">&quot; - &quot;</span> + age);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            closeQuietly(rs, ps, conn);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">closeQuietly</span><span class="params">(ResultSet rs, PreparedStatement ps, Connection conn)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (rs != <span class="literal">null</span>) &#123;</span><br><span class="line">                rs.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (ps != <span class="literal">null</span>) &#123;</span><br><span class="line">                ps.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (conn != <span class="literal">null</span>) &#123;</span><br><span class="line">                conn.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码看起来比较长，但它把 JDBC 的基本动作都展示出来了。实际开发中，关闭资源、获取连接这些重复代码应该封装到工具类里。</p><h2 id="2-PreparedStatement-的-CRUD-完整示例"><a href="#2-PreparedStatement-的-CRUD-完整示例" class="headerlink" title="2. PreparedStatement 的 CRUD 完整示例"></a>2. PreparedStatement 的 CRUD 完整示例</h2><p>下面用 <code>PreparedStatement</code> 完成增删改查。为了让代码更清晰，我先定义一个简单的实体类。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Integer id;</span><br><span class="line">    <span class="keyword">private</span> String username;</span><br><span class="line">    <span class="keyword">private</span> String password;</span><br><span class="line">    <span class="keyword">private</span> Integer age;</span><br><span class="line">    <span class="keyword">private</span> Date createTime;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Integer <span class="title function_">getId</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> id;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setId</span><span class="params">(Integer id)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.id = id;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getUsername</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> username;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setUsername</span><span class="params">(String username)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.username = username;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getPassword</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> password;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setPassword</span><span class="params">(String password)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.password = password;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Integer <span class="title function_">getAge</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setAge</span><span class="params">(Integer age)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Date <span class="title function_">getCreateTime</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> createTime;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setCreateTime</span><span class="params">(Date createTime)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.createTime = createTime;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="新增数据"><a href="#新增数据" class="headerlink" title="新增数据"></a>新增数据</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">insertUser</span><span class="params">(User user)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;insert into user(username, password, age, create_time) values(?, ?, ?, ?)&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        ps = conn.prepareStatement(sql);</span><br><span class="line">        ps.setString(<span class="number">1</span>, user.getUsername());</span><br><span class="line">        ps.setString(<span class="number">2</span>, user.getPassword());</span><br><span class="line">        ps.setInt(<span class="number">3</span>, user.getAge());</span><br><span class="line">        ps.setTimestamp(<span class="number">4</span>, <span class="keyword">new</span> <span class="title class_">java</span>.sql.Timestamp(user.getCreateTime().getTime()));</span><br><span class="line">        <span class="keyword">return</span> ps.executeUpdate();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, ps, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="查询数据"><a href="#查询数据" class="headerlink" title="查询数据"></a>查询数据</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> User <span class="title function_">findUserById</span><span class="params">(Integer id)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;select id, username, password, age, create_time from user where id = ?&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">ResultSet</span> <span class="variable">rs</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        ps = conn.prepareStatement(sql);</span><br><span class="line">        ps.setInt(<span class="number">1</span>, id);</span><br><span class="line">        rs = ps.executeQuery();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!rs.next()) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user.setId(rs.getInt(<span class="string">&quot;id&quot;</span>));</span><br><span class="line">        user.setUsername(rs.getString(<span class="string">&quot;username&quot;</span>));</span><br><span class="line">        user.setPassword(rs.getString(<span class="string">&quot;password&quot;</span>));</span><br><span class="line">        user.setAge(rs.getInt(<span class="string">&quot;age&quot;</span>));</span><br><span class="line">        user.setCreateTime(rs.getTimestamp(<span class="string">&quot;create_time&quot;</span>));</span><br><span class="line">        <span class="keyword">return</span> user;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(rs, ps, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="修改数据"><a href="#修改数据" class="headerlink" title="修改数据"></a>修改数据</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">updateUserAge</span><span class="params">(Integer id, Integer age)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;update user set age = ? where id = ?&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        ps = conn.prepareStatement(sql);</span><br><span class="line">        ps.setInt(<span class="number">1</span>, age);</span><br><span class="line">        ps.setInt(<span class="number">2</span>, id);</span><br><span class="line">        <span class="keyword">return</span> ps.executeUpdate();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, ps, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">deleteUserById</span><span class="params">(Integer id)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;delete from user where id = ?&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        ps = conn.prepareStatement(sql);</span><br><span class="line">        ps.setInt(<span class="number">1</span>, id);</span><br><span class="line">        <span class="keyword">return</span> ps.executeUpdate();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, ps, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里有一个小经验：查询使用 <code>executeQuery()</code>，增删改使用 <code>executeUpdate()</code>。后者返回受影响的行数，可以用来判断操作是否成功。</p><h2 id="3-事务管理代码"><a href="#3-事务管理代码" class="headerlink" title="3. 事务管理代码"></a>3. 事务管理代码</h2><p>事务适合处理一组必须同时成功或同时失败的操作，比如转账：A 扣钱成功，B 加钱也必须成功。</p><p>JDBC 默认是自动提交事务的，也就是每执行一条 SQL 就提交一次。要手动控制事务，需要关闭自动提交：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">transfer</span><span class="params">(Integer fromId, Integer toId, Integer amount)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">decreaseSql</span> <span class="operator">=</span> <span class="string">&quot;update account set balance = balance - ? where id = ?&quot;</span>;</span><br><span class="line">    <span class="type">String</span> <span class="variable">increaseSql</span> <span class="operator">=</span> <span class="string">&quot;update account set balance = balance + ? where id = ?&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">decreasePs</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">increasePs</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        conn.setAutoCommit(<span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">        decreasePs = conn.prepareStatement(decreaseSql);</span><br><span class="line">        decreasePs.setInt(<span class="number">1</span>, amount);</span><br><span class="line">        decreasePs.setInt(<span class="number">2</span>, fromId);</span><br><span class="line">        decreasePs.executeUpdate();</span><br><span class="line"></span><br><span class="line">        increasePs = conn.prepareStatement(increaseSql);</span><br><span class="line">        increasePs.setInt(<span class="number">1</span>, amount);</span><br><span class="line">        increasePs.setInt(<span class="number">2</span>, toId);</span><br><span class="line">        increasePs.executeUpdate();</span><br><span class="line"></span><br><span class="line">        conn.commit();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        rollback(conn);</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, increasePs, <span class="literal">null</span>);</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, decreasePs, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">rollback</span><span class="params">(Connection conn)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (conn == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn.rollback();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码的关键点是：</p><ul><li><code>conn.setAutoCommit(false)</code>：关闭自动提交；</li><li><code>conn.commit()</code>：所有 SQL 成功后提交；</li><li><code>conn.rollback()</code>：出现异常时回滚；</li><li>同一个事务中的多条 SQL 必须使用同一个 <code>Connection</code>。</li></ul><h2 id="4-JDBC-工具类封装"><a href="#4-JDBC-工具类封装" class="headerlink" title="4. JDBC 工具类封装"></a>4. JDBC 工具类封装</h2><p>如果每个方法都写加载驱动、创建连接、关闭资源，代码会非常重复。所以我把这些逻辑封装成一个工具类。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.DriverManager;</span><br><span class="line"><span class="keyword">import</span> java.sql.ResultSet;</span><br><span class="line"><span class="keyword">import</span> java.sql.Statement;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">JdbcUtils</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DRIVER_CLASS</span> <span class="operator">=</span> <span class="string">&quot;com.mysql.jdbc.Driver&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">URL</span> <span class="operator">=</span> <span class="string">&quot;jdbc:mysql://localhost:3306/jdbc_demo?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">USERNAME</span> <span class="operator">=</span> <span class="string">&quot;root&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">PASSWORD</span> <span class="operator">=</span> <span class="string">&quot;root&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class.forName(DRIVER_CLASS);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ExceptionInInitializerError</span>(e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">JdbcUtils</span><span class="params">()</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Connection <span class="title function_">getConnection</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="keyword">return</span> DriverManager.getConnection(URL, USERNAME, PASSWORD);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">close</span><span class="params">(ResultSet rs, Statement stmt, Connection conn)</span> &#123;</span><br><span class="line">        closeResultSet(rs);</span><br><span class="line">        closeStatement(stmt);</span><br><span class="line">        closeConnection(conn);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">closeResultSet</span><span class="params">(ResultSet rs)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (rs == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            rs.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">closeStatement</span><span class="params">(Statement stmt)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (stmt == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            stmt.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">closeConnection</span><span class="params">(Connection conn)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (conn == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            conn.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个工具类做了三件事：</p><ul><li>静态代码块加载驱动；</li><li><code>getConnection()</code> 统一获取连接；</li><li><code>close()</code> 统一关闭资源。</li></ul><p>学习阶段这样写足够清晰，但生产环境不建议每次都通过 <code>DriverManager</code> 新建物理连接，因为创建连接成本比较高。</p><h2 id="5-try-with-resources-写法"><a href="#5-try-with-resources-写法" class="headerlink" title="5. try-with-resources 写法"></a>5. try-with-resources 写法</h2><p>JDK 1.7 之后可以用 try-with-resources 自动关闭资源。JDK 1.8 中也可以使用这种写法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> User <span class="title function_">findUserByUsername</span><span class="params">(String username)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;select id, username, password, age, create_time from user where username = ?&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> (<span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> JdbcUtils.getConnection();</span><br><span class="line">         <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> conn.prepareStatement(sql)) &#123;</span><br><span class="line"></span><br><span class="line">        ps.setString(<span class="number">1</span>, username);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> (<span class="type">ResultSet</span> <span class="variable">rs</span> <span class="operator">=</span> ps.executeQuery()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!rs.next()) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">            user.setId(rs.getInt(<span class="string">&quot;id&quot;</span>));</span><br><span class="line">            user.setUsername(rs.getString(<span class="string">&quot;username&quot;</span>));</span><br><span class="line">            user.setPassword(rs.getString(<span class="string">&quot;password&quot;</span>));</span><br><span class="line">            user.setAge(rs.getInt(<span class="string">&quot;age&quot;</span>));</span><br><span class="line">            user.setCreateTime(rs.getTimestamp(<span class="string">&quot;create_time&quot;</span>));</span><br><span class="line">            <span class="keyword">return</span> user;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这种写法的好处是资源关闭更可靠，也减少了大量 <code>finally</code> 模板代码。</p><h2 id="6-Druid-连接池简介"><a href="#6-Druid-连接池简介" class="headerlink" title="6. Druid 连接池简介"></a>6. Druid 连接池简介</h2><p>实际项目中，不应该每次操作数据库都创建一个新的物理连接。连接池会提前维护一批连接，程序需要时从池中取，用完后归还给连接池。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Druid%20database%20connection%20pool%20architecture,%20Java%20application%20borrows%20and%20returns%20connections%20to%20pool,%20MySQL%20database,%20clean%20technical%20illustration&image_size=landscape_16_9" alt="Druid 连接池工作方式示意图"></p><p>Druid 是阿里开源的数据库连接池，在国内 Java 项目里很常见。Maven 依赖示例：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>druid<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.1.10<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p><code>druid.properties</code> 配置示例：</p><figure class="highlight properties"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">driverClassName</span>=<span class="string">com.mysql.jdbc.Driver</span></span><br><span class="line"><span class="attr">url</span>=<span class="string">jdbc:mysql://localhost:3306/jdbc_demo?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false</span></span><br><span class="line"><span class="attr">username</span>=<span class="string">root</span></span><br><span class="line"><span class="attr">password</span>=<span class="string">root</span></span><br><span class="line"><span class="attr">initialSize</span>=<span class="string">5</span></span><br><span class="line"><span class="attr">maxActive</span>=<span class="string">20</span></span><br><span class="line"><span class="attr">minIdle</span>=<span class="string">5</span></span><br><span class="line"><span class="attr">maxWait</span>=<span class="string">3000</span></span><br><span class="line"><span class="attr">validationQuery</span>=<span class="string">select 1</span></span><br><span class="line"><span class="attr">testWhileIdle</span>=<span class="string">true</span></span><br><span class="line"><span class="attr">testOnBorrow</span>=<span class="string">false</span></span><br><span class="line"><span class="attr">testOnReturn</span>=<span class="string">false</span></span><br></pre></td></tr></table></figure><p>使用 Druid 获取连接：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> com.alibaba.druid.pool.DruidDataSourceFactory;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.sql.DataSource;</span><br><span class="line"><span class="keyword">import</span> java.io.InputStream;</span><br><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.util.Properties;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DruidUtils</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> DataSource dataSource;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">Properties</span> <span class="variable">properties</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Properties</span>();</span><br><span class="line">            <span class="type">InputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> DruidUtils.class.getClassLoader().getResourceAsStream(<span class="string">&quot;druid.properties&quot;</span>);</span><br><span class="line">            properties.load(inputStream);</span><br><span class="line">            dataSource = DruidDataSourceFactory.createDataSource(properties);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ExceptionInInitializerError</span>(e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">DruidUtils</span><span class="params">()</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Connection <span class="title function_">getConnection</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="keyword">return</span> dataSource.getConnection();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> DataSource <span class="title function_">getDataSource</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> dataSource;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用连接池后，调用 <code>conn.close()</code> 并不是真的关闭物理连接，而是把连接归还给连接池。这一点很重要，所以即使用了连接池，也仍然必须关闭连接。</p><h2 id="7-批处理操作"><a href="#7-批处理操作" class="headerlink" title="7. 批处理操作"></a>7. 批处理操作</h2><p>当需要一次插入很多条数据时，如果一条一条执行 SQL，效率会比较低。JDBC 提供了批处理能力，可以把多条操作合并提交。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">batchInsertUsers</span><span class="params">(List&lt;User&gt; users)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;insert into user(username, password, age, create_time) values(?, ?, ?, ?)&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        ps = conn.prepareStatement(sql);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; users.size(); i++) &#123;</span><br><span class="line">            <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> users.get(i);</span><br><span class="line">            ps.setString(<span class="number">1</span>, user.getUsername());</span><br><span class="line">            ps.setString(<span class="number">2</span>, user.getPassword());</span><br><span class="line">            ps.setInt(<span class="number">3</span>, user.getAge());</span><br><span class="line">            ps.setTimestamp(<span class="number">4</span>, <span class="keyword">new</span> <span class="title class_">java</span>.sql.Timestamp(user.getCreateTime().getTime()));</span><br><span class="line">            ps.addBatch();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> ((i + <span class="number">1</span>) % <span class="number">500</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                ps.executeBatch();</span><br><span class="line">                ps.clearBatch();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ps.executeBatch();</span><br><span class="line">        ps.clearBatch();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, ps, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>批处理适合大量新增或更新数据。这里每 500 条执行一次，是为了避免一次性积累太多 SQL 占用内存。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次 JDBC 连接 MySQL 的实践，让我把 Java 基础、SQL 语法和数据库连接机制串了起来。以前只是在数据库工具里执行 SQL，现在可以通过 Java 代码完成完整的 CRUD 操作，感觉 Java 程序终于可以真正和数据打交道了。</p><p>我给自己总结了几条规则：</p><ol><li>永远优先使用 <code>PreparedStatement</code>，不要用字符串拼接 SQL；</li><li><code>ResultSet</code> 是游标模式，读取前先 <code>next()</code>；</li><li><code>Connection</code>、<code>Statement</code>、<code>ResultSet</code> 用完必须关闭；</li><li>资源关闭要放在 <code>finally</code> 中，或者使用 try-with-resources；</li><li>中文数据要注意数据库编码和 JDBC URL 中的 <code>characterEncoding=UTF-8</code>；</li><li>涉及多条 SQL 的一致性操作时，要明确事务边界；</li><li>生产环境不要直接频繁创建连接，应该使用连接池，比如 Druid。</li></ol><p>JDBC 的代码确实比较模板化，但它非常适合用来理解数据库访问的底层过程。后面再学习 DBUtils、JdbcTemplate、MyBatis 这些框架时，就会更容易明白它们到底帮我们封装了什么。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;2018 年 9 月，我刚把 Java 基础和 MySQL 的常用语法系统学完。前面写 Java 时，大多是在控制台里打印结果；学 MySQ</summary>
      
    
    
    
    <category term="数据库" scheme="https://swagch.github.io/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="MySQL" scheme="https://swagch.github.io/tags/MySQL/"/>
    
    <category term="CRUD" scheme="https://swagch.github.io/tags/CRUD/"/>
    
    <category term="JDBC" scheme="https://swagch.github.io/tags/JDBC/"/>
    
    <category term="数据库连接" scheme="https://swagch.github.io/tags/%E6%95%B0%E6%8D%AE%E5%BA%93%E8%BF%9E%E6%8E%A5/"/>
    
  </entry>
  
  <entry>
    <title>Java异常处理机制</title>
    <link href="https://swagch.github.io/2018/08/19/2018-08-19-Java%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86%E6%9C%BA%E5%88%B6/"/>
    <id>https://swagch.github.io/2018/08/19/2018-08-19-Java%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86%E6%9C%BA%E5%88%B6/</id>
    <published>2018-08-19T12:33:17.000Z</published>
    <updated>2026-06-09T07:52:22.472Z</updated>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>最近写 Java 小项目的时候，我经常会遇到各种报错。有些错误是编译阶段就提示的，比如文件找不到、方法必须抛出异常；有些错误是程序运行到一半才出现的，比如空指针、数组越界、类型转换失败。</p><p>一开始我对异常的理解比较简单：报错了就 <code>try-catch</code> 一下，能运行就行。但是写得多了之后发现，异常处理并不是把代码包起来这么简单。如果处理方式不对，程序表面上不报错了，真正的问题反而被藏起来了。</p><p>尤其是这段时间，我经常看到代码里有 <code>catch (Exception e)</code>，但 catch 里面只打印一句话，甚至什么都不写。这样虽然编译能通过，但以后排查问题会非常痛苦。</p><p>所以我打算在 2018 年 8 月这个阶段，把 Java 异常处理机制系统梳理一遍，弄清楚异常体系、<code>try-catch-finally</code> 执行顺序、什么时候应该抛异常、什么时候应该捕获异常。</p><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>我目前遇到的困惑主要有这几个：</p><ul><li>Checked异常和Unchecked异常分不清</li><li><code>finally</code> 中的 <code>return</code> 会覆盖 <code>try</code> 中的返回值</li><li>习惯性 <code>catch (Exception e)</code>，但 catch 之后不知道怎么处理</li><li>空的 catch 块会吞掉异常，导致排查问题很困难</li><li>不知道什么时候应该向上抛异常，什么时候应该自己 catch</li></ul><p>其中最让我印象深的是 <code>finally</code> 里的 <code>return</code>。以前我以为 <code>try</code> 里面已经 <code>return</code> 了，方法就结束了。后来才发现，只要有 <code>finally</code>，它仍然会执行；如果 <code>finally</code> 里面也写了 <code>return</code>，还会把 <code>try</code> 或 <code>catch</code> 里的返回值覆盖掉。</p><p>另外，空 catch 块也很危险。异常发生了，但是代码没有任何日志、没有任何提示，就像什么都没发生一样。这种写法短期看起来“安静”，长期看就是在给自己埋雷。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>Java 的异常可以先从 <code>Throwable</code> 这个顶层父类看起。它下面主要有两个分支：<code>Error</code> 和 <code>Exception</code>。</p><p><code>Error</code> 通常表示比较严重的问题，比如 JVM 内存不足、栈溢出等，一般不是业务代码应该捕获处理的重点。我们平时写业务代码更多关注的是 <code>Exception</code>。</p><p><code>Exception</code> 又可以分成两类：Checked异常和Unchecked异常。</p><p>Checked异常是编译期检查异常。也就是说，如果方法可能抛出这类异常，编译器会要求我们必须处理：要么使用 <code>try-catch</code> 捕获，要么在方法上使用 <code>throws</code> 声明继续向上抛。</p><p>Unchecked异常主要指 <code>RuntimeException</code> 以及它的子类，比如 <code>NullPointerException</code>、<code>IllegalArgumentException</code>、<code>IndexOutOfBoundsException</code> 等。这类异常编译器不会强制要求处理，但它们仍然会在运行时导致程序出错。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20exception%20hierarchy%20diagram%20Throwable%20Error%20Exception%20RuntimeException%20checked%20unchecked%20exceptions,%20clean%20technical%20infographic&image_size=landscape_4_3" alt="Java异常体系结构图"><br><em>配图说明：Java异常体系中 Throwable、Error、Exception 和 RuntimeException 的关系</em></p><p>简单理解可以这样记：</p><table><thead><tr><th>类型</th><th>是否编译期强制处理</th><th>常见例子</th><th>处理思路</th></tr></thead><tbody><tr><td>Checked异常</td><td>是</td><td>IOException、SQLException</td><td>捕获后处理，或向上抛出</td></tr><tr><td>Unchecked异常</td><td>否</td><td>NullPointerException、IllegalArgumentException</td><td>优先通过代码逻辑避免</td></tr><tr><td>Error</td><td>否</td><td>OutOfMemoryError、StackOverflowError</td><td>通常不在业务代码中捕获</td></tr></tbody></table><p><code>finally</code> 的规则也要特别注意：无论 <code>try</code> 中是否发生异常，只要 JVM 没有直接退出，<code>finally</code> 通常都会执行。它适合用来释放资源，比如关闭文件流、数据库连接等。</p><p>但是不要在 <code>finally</code> 中写 <code>return</code>。因为 <code>finally</code> 的 <code>return</code> 会覆盖 <code>try</code> 或 <code>catch</code> 中已经准备返回的结果，也可能覆盖正在抛出的异常，让问题变得非常隐蔽。</p><p>从代码结构上看，比较合理的异常处理思路是：底层代码只负责发现问题并抛出带上下文的异常，上层代码统一捕获并处理，比如返回错误提示、记录日志、回滚事务等。这样职责会更清晰，也更容易排查问题。</p><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="Java异常体系结构图"><a href="#Java异常体系结构图" class="headerlink" title="Java异常体系结构图"></a>Java异常体系结构图</h2><p>先把异常体系记清楚，对后面理解 Checked 和 Unchecked 很有帮助。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">Throwable</span><br><span class="line">├── Error</span><br><span class="line">│   ├── OutOfMemoryError</span><br><span class="line">│   └── StackOverflowError</span><br><span class="line">└── Exception</span><br><span class="line">    ├── IOException</span><br><span class="line">    ├── SQLException</span><br><span class="line">    └── RuntimeException</span><br><span class="line">        ├── NullPointerException</span><br><span class="line">        ├── IllegalArgumentException</span><br><span class="line">        └── IndexOutOfBoundsException</span><br></pre></td></tr></table></figure><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Checked%20exception%20vs%20unchecked%20exception%20comparison%20table%20in%20Java,%20educational%20infographic,%20blue%20and%20orange&image_size=landscape_4_3" alt="Checked和Unchecked异常对比"><br><em>配图说明：Checked异常和Unchecked异常的区别</em></p><p>Checked异常的典型例子是读取文件：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.FileNotFoundException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CheckedExceptionDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">FileInputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">FileInputStream</span>(<span class="string">&quot;test.txt&quot;</span>);</span><br><span class="line">            System.out.println(inputStream);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (FileNotFoundException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;文件不存在：&quot;</span> + e.getMessage());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>FileNotFoundException</code> 是 Checked异常，如果不处理，代码不能通过编译。</p><p>Unchecked异常的典型例子是空指针：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UncheckedExceptionDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">text</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        System.out.println(text.length());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码编译可以通过，但运行时会抛出 <code>NullPointerException</code>。所以 Unchecked异常不是“不重要”，而是编译器不强制我们写 <code>try-catch</code>。</p><h2 id="try-catch-finally执行顺序详解"><a href="#try-catch-finally执行顺序详解" class="headerlink" title="try-catch-finally执行顺序详解"></a>try-catch-finally执行顺序详解</h2><p>先看最普通的情况：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TryCatchFinallyDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;1. 执行try代码块&quot;</span>);</span><br><span class="line">            <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> <span class="number">10</span> / <span class="number">0</span>;</span><br><span class="line">            System.out.println(result);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ArithmeticException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;2. 执行catch代码块：&quot;</span> + e.getMessage());</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;3. 执行finally代码块&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;4. 程序继续向下执行&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行顺序是：先进入 <code>try</code>，遇到异常后进入匹配的 <code>catch</code>，最后一定执行 <code>finally</code>，然后继续执行后面的代码。</p><p>再看有 <code>return</code> 的情况：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">FinallyReturnDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(getNumber());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">getNumber</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">3</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个方法最终返回的是 <code>3</code>，不是 <code>1</code>。原因是 <code>try</code> 中虽然已经准备返回 <code>1</code>，但是在方法真正返回之前，<code>finally</code> 仍然会执行，而 <code>finally</code> 里的 <code>return 3</code> 覆盖了原来的返回值。</p><p>更推荐的写法是：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BetterFinallyDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(getNumber());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">getNumber</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;释放资源或记录必要信息&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>finally</code> 只做资源释放，不做返回值控制，这样逻辑更清楚。</p><h2 id="自定义业务异常类"><a href="#自定义业务异常类" class="headerlink" title="自定义业务异常类"></a>自定义业务异常类</h2><p>实际项目里，很多异常不是 Java 自带异常能准确表达的。比如用户余额不足、订单状态不正确、参数不合法等，都可以定义成业务异常。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BusinessException</span> <span class="keyword">extends</span> <span class="title class_">RuntimeException</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String errorCode;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">BusinessException</span><span class="params">(String errorCode, String message)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(message);</span><br><span class="line">        <span class="built_in">this</span>.errorCode = errorCode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">BusinessException</span><span class="params">(String errorCode, String message, Throwable cause)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(message, cause);</span><br><span class="line">        <span class="built_in">this</span>.errorCode = errorCode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getErrorCode</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> errorCode;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用业务异常时，异常信息要写清楚，最好带上业务上下文：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderService</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">pay</span><span class="params">(String orderId, <span class="type">int</span> balance, <span class="type">int</span> amount)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (balance &lt; amount) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BusinessException</span>(<span class="string">&quot;BALANCE_NOT_ENOUGH&quot;</span>, <span class="string">&quot;订单&quot;</span> + orderId + <span class="string">&quot;支付失败，余额不足&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;订单支付成功：&quot;</span> + orderId);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里我选择让 <code>BusinessException</code> 继承 <code>RuntimeException</code>，是因为业务异常很多时候希望由上层统一处理，而不是在每一层都被迫 <code>try-catch</code>。</p><h2 id="异常链cause的使用"><a href="#异常链cause的使用" class="headerlink" title="异常链cause的使用"></a>异常链cause的使用</h2><p>有时候底层异常不能直接暴露给上层，但又不能丢失原始原因。这时就应该使用异常链，也就是把原始异常作为 <code>cause</code> 传进去。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.FileNotFoundException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ExceptionCauseDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            loadConfig(<span class="string">&quot;app.properties&quot;</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (BusinessException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;错误码：&quot;</span> + e.getErrorCode());</span><br><span class="line">            System.out.println(<span class="string">&quot;错误信息：&quot;</span> + e.getMessage());</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">loadConfig</span><span class="params">(String fileName)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">FileInputStream</span>(fileName);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (FileNotFoundException e) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BusinessException</span>(<span class="string">&quot;CONFIG_NOT_FOUND&quot;</span>, <span class="string">&quot;配置文件不存在：&quot;</span> + fileName, e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样做的好处是：上层看到的是业务含义明确的异常，排查问题时又可以通过 <code>cause</code> 找到最原始的 <code>FileNotFoundException</code>。</p><h2 id="多catch的使用"><a href="#多catch的使用" class="headerlink" title="多catch的使用"></a>多catch的使用</h2><p>Java 7 之后可以在一个 catch 中捕获多种异常，适合处理方式相同的场景。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MultiCatchDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">String</span> <span class="variable">numberText</span> <span class="operator">=</span> args[<span class="number">0</span>];</span><br><span class="line">            <span class="type">int</span> <span class="variable">number</span> <span class="operator">=</span> Integer.parseInt(numberText);</span><br><span class="line">            System.out.println(<span class="string">&quot;数字是：&quot;</span> + number);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ArrayIndexOutOfBoundsException | NumberFormatException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;请输入一个合法的整数参数：&quot;</span> + e.getMessage());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果不同异常的处理方式不同，就不要强行合并，应该分开 catch。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SeparateCatchDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">String</span> <span class="variable">numberText</span> <span class="operator">=</span> args[<span class="number">0</span>];</span><br><span class="line">            <span class="type">int</span> <span class="variable">number</span> <span class="operator">=</span> Integer.parseInt(numberText);</span><br><span class="line">            System.out.println(<span class="string">&quot;数字是：&quot;</span> + number);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ArrayIndexOutOfBoundsException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;缺少启动参数&quot;</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (NumberFormatException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;参数不是合法整数：&quot;</span> + args[<span class="number">0</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>需要注意的是，catch 的顺序要从具体异常到宽泛异常。如果先 catch <code>Exception</code>，后面的具体异常就没有机会被捕获了。</p><h2 id="try-with-resources释放资源"><a href="#try-with-resources释放资源" class="headerlink" title="try-with-resources释放资源"></a>try-with-resources释放资源</h2><p>以前释放资源通常写在 <code>finally</code> 里：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.BufferedReader;</span><br><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OldResourceCloseDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">        <span class="type">BufferedReader</span> <span class="variable">reader</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            reader = <span class="keyword">new</span> <span class="title class_">BufferedReader</span>(<span class="keyword">new</span> <span class="title class_">FileReader</span>(<span class="string">&quot;test.txt&quot;</span>));</span><br><span class="line">            System.out.println(reader.readLine());</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (reader != <span class="literal">null</span>) &#123;</span><br><span class="line">                reader.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Java 7 之后更推荐使用 <code>try-with-resources</code>，它会自动关闭实现了 <code>AutoCloseable</code> 接口的资源。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.BufferedReader;</span><br><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TryWithResourcesDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> (<span class="type">BufferedReader</span> <span class="variable">reader</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BufferedReader</span>(<span class="keyword">new</span> <span class="title class_">FileReader</span>(<span class="string">&quot;test.txt&quot;</span>))) &#123;</span><br><span class="line">            System.out.println(reader.readLine());</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;读取文件失败：&quot;</span> + e.getMessage());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20try%20with%20resources%20automatic%20resource%20closing%20flowchart,%20file%20reader%20close%20process,%20clean%20infographic&image_size=landscape_4_3" alt="try-with-resources资源释放流程"><br><em>配图说明：try-with-resources 自动关闭资源的流程</em></p><p>相比手写 <code>finally</code>，<code>try-with-resources</code> 更简洁，也更不容易忘记关闭资源。</p><h2 id="异常处理最佳实践清单"><a href="#异常处理最佳实践清单" class="headerlink" title="异常处理最佳实践清单"></a>异常处理最佳实践清单</h2><p>经过这次整理，我觉得写异常处理时可以按下面这份清单检查：</p><ul><li>不要写空 catch 块，至少要记录日志或给出明确处理</li><li>不要只写 <code>catch (Exception e)</code>，除非这一层确实是统一异常处理入口</li><li>不要在 <code>finally</code> 中写 <code>return</code></li><li>异常信息要带上下文，比如订单号、用户ID、文件名</li><li>能用代码判断避免的 RuntimeException，就不要依赖 try-catch</li><li>底层异常如果无法处理，就向上抛出，不要假装处理了</li><li>上层统一处理异常，负责日志、提示、事务回滚等</li><li>包装异常时保留原始 cause，避免丢失根因</li><li>关闭资源优先使用 try-with-resources</li><li>自定义异常命名要清晰，比如 <code>BusinessException</code>、<code>OrderStatusException</code></li></ul><p>例如下面这种空 catch 就是反模式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> <span class="number">10</span> / <span class="number">0</span>;</span><br><span class="line">    System.out.println(result);</span><br><span class="line">&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>更好的方式是根据当前层的职责处理异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> <span class="number">10</span> / <span class="number">0</span>;</span><br><span class="line">    System.out.println(result);</span><br><span class="line">&#125; <span class="keyword">catch</span> (ArithmeticException e) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BusinessException</span>(<span class="string">&quot;CALCULATE_ERROR&quot;</span>, <span class="string">&quot;计算失败，除数不能为0&quot;</span>, e);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里没有把异常吞掉，而是把底层异常包装成更有业务意义的异常，并保留原始原因。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次系统梳理 Java 异常处理机制之后，我最大的感受是：异常处理不是为了“让程序不报错”，而是为了让错误在合适的位置被发现、被记录、被处理。</p><p>我的阶段性总结是：</p><ul><li>不要吞异常，空 catch 块是非常危险的写法</li><li>异常信息要带上下文，不然排查问题会很困难</li><li>底层无法处理的异常应该向上抛，上层再统一 catch</li><li><code>finally</code> 适合释放资源，但不要在里面写 <code>return</code></li><li>关闭文件流、网络连接等资源时，优先使用 <code>try-with-resources</code></li></ul><p>以后写代码时，我会尽量避免为了“消除红线”而随便 <code>try-catch</code>。真正好的异常处理，应该能帮助我更快定位问题，而不是把问题藏起来。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;最近写 Java 小项目的时候，我经常会遇到各种报错。有些错误是编译阶段就提示的，比如文件找不到、方法必须抛出异常；有些错误是程序运行到一半</summary>
      
    
    
    
    <category term="Java基础" scheme="https://swagch.github.io/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="异常处理" scheme="https://swagch.github.io/tags/%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86/"/>
    
    <category term="try-catch" scheme="https://swagch.github.io/tags/try-catch/"/>
    
    <category term="Exception" scheme="https://swagch.github.io/tags/Exception/"/>
    
    <category term="最佳实践" scheme="https://swagch.github.io/tags/%E6%9C%80%E4%BD%B3%E5%AE%9E%E8%B7%B5/"/>
    
  </entry>
  
  <entry>
    <title>HashMap基础学习</title>
    <link href="https://swagch.github.io/2018/07/22/2018-07-22-HashMap%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0/"/>
    <id>https://swagch.github.io/2018/07/22/2018-07-22-HashMap%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0/</id>
    <published>2018-07-22T06:18:39.000Z</published>
    <updated>2026-06-09T07:52:03.649Z</updated>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>2018 年 7 月，最近把 <code>List</code> 相关的内容过了一遍，接下来开始学习 <code>Map</code>。和 <code>List</code> 按顺序存储元素不同，<code>Map</code> 更像是一张“键值对表”，通过 <code>key</code> 可以快速找到对应的 <code>value</code>。</p><p>在 Java 集合框架里，<code>HashMap</code> 应该是工作中最常用的数据结构之一。比如根据用户 id 查用户信息、根据配置名查配置值、统计某个字段出现次数等，都很容易想到用 <code>HashMap</code>。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20HashMap%20bucket%20array%20linked%20list%20collision%20diagram,%20clean%20technical%20blog%20illustration&image_size=landscape_16_9" alt="HashMap桶数组与链表结构"></p><p>刚开始看 <code>HashMap</code> 的时候，我觉得它只是“根据 key 存取 value”的工具类。但继续往下学才发现，里面涉及哈希算法、数组、链表、扩容、线程安全、<code>hashCode</code> 和 <code>equals</code> 等一系列知识点。也难怪它经常出现在面试题里。</p><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>学习过程中，我主要遇到了下面几个疑问：</p><ol><li><code>key</code> 为 <code>null</code> 时，<code>HashMap</code> 到底怎么处理？</li><li>以前听说 <code>HashMap</code> 不是线程安全的，实际会出什么问题？</li><li>如果两个不同的 <code>key</code> 算出来的 <code>hashCode</code> 一样，数据会不会被覆盖？</li><li>遍历 <code>HashMap</code> 的方式太多，到底该用哪一种？</li><li><code>HashMap</code> 和 <code>Hashtable</code> 有什么区别？</li></ol><p>这些问题如果只停留在“会用 API”的层面，其实很容易混淆。所以我打算先从常用操作入手，再慢慢分析它的底层原理。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><h2 id="1-HashMap-可以存-null-吗？"><a href="#1-HashMap-可以存-null-吗？" class="headerlink" title="1. HashMap 可以存 null 吗？"></a>1. HashMap 可以存 null 吗？</h2><p><code>HashMap</code> 允许一个 <code>null key</code>，也允许多个 <code>null value</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;String, String&gt; map = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;String, String&gt;();</span><br><span class="line">map.put(<span class="literal">null</span>, <span class="string">&quot;空key&quot;</span>);</span><br><span class="line">map.put(<span class="string">&quot;name&quot;</span>, <span class="literal">null</span>);</span><br><span class="line">map.put(<span class="string">&quot;address&quot;</span>, <span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line">System.out.println(map.get(<span class="literal">null</span>));</span><br></pre></td></tr></table></figure><p>需要注意的是，<code>key</code> 是唯一的，所以 <code>null key</code> 只能有一个。如果重复放入 <code>null key</code>，后面的值会覆盖前面的值。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;String, String&gt; map = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;String, String&gt;();</span><br><span class="line">map.put(<span class="literal">null</span>, <span class="string">&quot;第一次放入&quot;</span>);</span><br><span class="line">map.put(<span class="literal">null</span>, <span class="string">&quot;第二次放入&quot;</span>);</span><br><span class="line"></span><br><span class="line">System.out.println(map.get(<span class="literal">null</span>));</span><br></pre></td></tr></table></figure><p>输出结果是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">第二次放入</span><br></pre></td></tr></table></figure><h2 id="2-HashMap-为什么不是线程安全的？"><a href="#2-HashMap-为什么不是线程安全的？" class="headerlink" title="2. HashMap 为什么不是线程安全的？"></a>2. HashMap 为什么不是线程安全的？</h2><p><code>HashMap</code> 的大多数操作都没有加锁。如果多个线程同时修改同一个 <code>HashMap</code>，就可能出现问题。</p><p>在 JDK7 中，<code>HashMap</code> 扩容时采用头插法迁移链表节点，在并发 <code>put</code> 的情况下，链表可能形成环，最终导致查询时死循环。</p><p>在 JDK8 中，虽然扩容迁移逻辑有所调整，降低了死循环风险，但 <code>HashMap</code> 依然不是线程安全的。多个线程同时 <code>put</code> 时，仍然可能出现数据覆盖、数据丢失、扩容状态不一致等问题。</p><p>所以结论很明确：如果是多线程写入场景，不要直接使用普通 <code>HashMap</code>。</p><h2 id="3-hashCode-相同怎么办？"><a href="#3-hashCode-相同怎么办？" class="headerlink" title="3. hashCode 相同怎么办？"></a>3. hashCode 相同怎么办？</h2><p><code>HashMap</code> 根据 key 的哈希值计算数组下标。不同的 key 可能被计算到同一个数组位置，这种情况叫哈希冲突。</p><p>JDK7 中，哈希冲突主要通过“数组 + 链表”解决：多个元素落到同一个桶时，会挂在同一条链表上。</p><p>JDK8 中，底层结构变成“数组 + 链表 + 红黑树”。当链表长度达到一定阈值，并且数组容量也达到要求后，链表会转成红黑树，减少极端情况下的查询时间。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=HashMap%20hash%20collision%20linked%20list%20and%20red%20black%20tree%20conversion,%20Java%20technical%20diagram&image_size=landscape_16_9" alt="HashMap哈希冲突处理"></p><p>因此，两个 key 的 <code>hashCode</code> 相同，并不代表一定会互相覆盖。<code>HashMap</code> 还会继续通过 <code>equals</code> 判断 key 是否真正相等。</p><h2 id="4-遍历方式应该选哪个？"><a href="#4-遍历方式应该选哪个？" class="headerlink" title="4. 遍历方式应该选哪个？"></a>4. 遍历方式应该选哪个？</h2><p><code>HashMap</code> 的遍历方式很多，比如遍历 <code>keySet</code>、遍历 <code>entrySet</code>、只遍历 value，或者使用 <code>Iterator</code>。</p><p>日常开发里，如果同时需要 key 和 value，我更推荐使用 <code>entrySet</code>。因为 <code>entrySet</code> 可以一次拿到 key 和 value，避免通过 key 再执行一次 <code>get</code> 查询。</p><h2 id="5-HashMap-和-Hashtable-区别"><a href="#5-HashMap-和-Hashtable-区别" class="headerlink" title="5. HashMap 和 Hashtable 区别"></a>5. HashMap 和 Hashtable 区别</h2><p><code>Hashtable</code> 是比较早期的集合类，<code>HashMap</code> 是后来集合框架中的常用实现。两者主要区别如下：</p><table><thead><tr><th>对比项</th><th>HashMap</th><th>Hashtable</th></tr></thead><tbody><tr><td>线程安全</td><td>非线程安全</td><td>方法使用 synchronized，线程安全</td></tr><tr><td>null 支持</td><td>允许一个 null key 和多个 null value</td><td>不允许 null key 和 null value</td></tr><tr><td>性能</td><td>单线程下通常更好</td><td>加锁开销较大</td></tr><tr><td>推荐程度</td><td>日常开发常用</td><td>现在较少使用</td></tr><tr><td>替代方案</td><td>多线程场景用 ConcurrentHashMap</td><td>通常不推荐新代码使用</td></tr></tbody></table><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="1-HashMap-基本-CRUD-操作"><a href="#1-HashMap-基本-CRUD-操作" class="headerlink" title="1. HashMap 基本 CRUD 操作"></a>1. HashMap 基本 CRUD 操作</h2><p>先从最基础的增删改查开始。这里我用 <code>String</code> 作为 key，<code>Integer</code> 作为 value，模拟记录学生成绩。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;String, Integer&gt; scoreMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;String, Integer&gt;();</span><br><span class="line"></span><br><span class="line">scoreMap.put(<span class="string">&quot;张三&quot;</span>, <span class="number">90</span>);</span><br><span class="line">scoreMap.put(<span class="string">&quot;李四&quot;</span>, <span class="number">85</span>);</span><br><span class="line">scoreMap.put(<span class="string">&quot;王五&quot;</span>, <span class="number">92</span>);</span><br><span class="line"></span><br><span class="line"><span class="type">Integer</span> <span class="variable">zhangSanScore</span> <span class="operator">=</span> scoreMap.get(<span class="string">&quot;张三&quot;</span>);</span><br><span class="line">System.out.println(<span class="string">&quot;张三成绩：&quot;</span> + zhangSanScore);</span><br></pre></td></tr></table></figure><p>修改和删除也很简单：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">scoreMap.put(<span class="string">&quot;李四&quot;</span>, <span class="number">88</span>);</span><br><span class="line">scoreMap.remove(<span class="string">&quot;王五&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (scoreMap.containsKey(<span class="string">&quot;李四&quot;</span>)) &#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;李四成绩：&quot;</span> + scoreMap.get(<span class="string">&quot;李四&quot;</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>常见判断操作：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">boolean</span> <span class="variable">hasZhangSan</span> <span class="operator">=</span> scoreMap.containsKey(<span class="string">&quot;张三&quot;</span>);</span><br><span class="line"><span class="type">boolean</span> <span class="variable">hasFullScore</span> <span class="operator">=</span> scoreMap.containsValue(<span class="number">100</span>);</span><br><span class="line"><span class="type">int</span> <span class="variable">size</span> <span class="operator">=</span> scoreMap.size();</span><br><span class="line"><span class="type">boolean</span> <span class="variable">empty</span> <span class="operator">=</span> scoreMap.isEmpty();</span><br><span class="line"></span><br><span class="line">System.out.println(hasZhangSan + <span class="string">&quot;, &quot;</span> + hasFullScore);</span><br><span class="line">System.out.println(<span class="string">&quot;数量：&quot;</span> + size + <span class="string">&quot;, 是否为空：&quot;</span> + empty);</span><br></pre></td></tr></table></figure><h2 id="2-用-Student-作为-key-时重写-hashCode-和-equals"><a href="#2-用-Student-作为-key-时重写-hashCode-和-equals" class="headerlink" title="2. 用 Student 作为 key 时重写 hashCode 和 equals"></a>2. 用 Student 作为 key 时重写 hashCode 和 equals</h2><p>如果自定义对象要作为 <code>HashMap</code> 的 key，必须特别注意 <code>hashCode</code> 和 <code>equals</code>。</p><p>比如下面这个 <code>Student</code> 类，我认为只要 <code>id</code> 相同，就是同一个学生。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Student</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Integer id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Student</span><span class="params">(Integer id, String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.id = id;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这时应该重写 <code>equals</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">equals</span><span class="params">(Object obj)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span> == obj) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!(obj <span class="keyword">instanceof</span> Student)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">Student</span> <span class="variable">other</span> <span class="operator">=</span> (Student) obj;</span><br><span class="line">    <span class="keyword">return</span> id != <span class="literal">null</span> ? id.equals(other.id) : other.id == <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>同时必须重写 <code>hashCode</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">hashCode</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> id != <span class="literal">null</span> ? id.hashCode() : <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>完整使用示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;Student, String&gt; studentMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;Student, String&gt;();</span><br><span class="line"></span><br><span class="line"><span class="type">Student</span> <span class="variable">s1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Student</span>(<span class="number">1</span>, <span class="string">&quot;张三&quot;</span>);</span><br><span class="line"><span class="type">Student</span> <span class="variable">s2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Student</span>(<span class="number">1</span>, <span class="string">&quot;张三同学&quot;</span>);</span><br><span class="line"></span><br><span class="line">studentMap.put(s1, <span class="string">&quot;第一次放入&quot;</span>);</span><br><span class="line">studentMap.put(s2, <span class="string">&quot;第二次放入&quot;</span>);</span><br><span class="line"></span><br><span class="line">System.out.println(studentMap.size());</span><br></pre></td></tr></table></figure><p>如果 <code>equals</code> 和 <code>hashCode</code> 都按 <code>id</code> 判断，那么 <code>s1</code> 和 <code>s2</code> 会被认为是同一个 key，最后 <code>size</code> 是 <code>1</code>。</p><p>这里最重要的规则是：重写 <code>equals</code> 必须重写 <code>hashCode</code>。否则两个对象即使 <code>equals</code> 返回 <code>true</code>，也可能因为哈希值不同而被放到不同桶里，导致 <code>HashMap</code> 行为异常。</p><h2 id="3-HashMap-的-4-种遍历方式"><a href="#3-HashMap-的-4-种遍历方式" class="headerlink" title="3. HashMap 的 4 种遍历方式"></a>3. HashMap 的 4 种遍历方式</h2><h3 id="方式一：遍历-keySet，再通过-key-获取-value"><a href="#方式一：遍历-keySet，再通过-key-获取-value" class="headerlink" title="方式一：遍历 keySet，再通过 key 获取 value"></a>方式一：遍历 keySet，再通过 key 获取 value</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (String key : scoreMap.keySet()) &#123;</span><br><span class="line">    <span class="type">Integer</span> <span class="variable">value</span> <span class="operator">=</span> scoreMap.get(key);</span><br><span class="line">    System.out.println(key + <span class="string">&quot; = &quot;</span> + value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这种写法直观，但如果同时需要 key 和 value，会多一次 <code>get</code> 查询，不是最推荐的方式。</p><h3 id="方式二：遍历-entrySet"><a href="#方式二：遍历-entrySet" class="headerlink" title="方式二：遍历 entrySet"></a>方式二：遍历 entrySet</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (Map.Entry&lt;String, Integer&gt; entry : scoreMap.entrySet()) &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> entry.getKey();</span><br><span class="line">    <span class="type">Integer</span> <span class="variable">value</span> <span class="operator">=</span> entry.getValue();</span><br><span class="line">    System.out.println(key + <span class="string">&quot; = &quot;</span> + value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这种方式性能更好，也更适合需要同时访问 key 和 value 的场景。</p><h3 id="方式三：只遍历-values"><a href="#方式三：只遍历-values" class="headerlink" title="方式三：只遍历 values"></a>方式三：只遍历 values</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (Integer value : scoreMap.values()) &#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;成绩：&quot;</span> + value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果只关心 value，不关心 key，这种方式最直接。</p><h3 id="方式四：使用-Iterator-遍历"><a href="#方式四：使用-Iterator-遍历" class="headerlink" title="方式四：使用 Iterator 遍历"></a>方式四：使用 Iterator 遍历</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Iterator&lt;Map.Entry&lt;String, Integer&gt;&gt; iterator = scoreMap.entrySet().iterator();</span><br><span class="line"><span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">    Map.Entry&lt;String, Integer&gt; entry = iterator.next();</span><br><span class="line">    System.out.println(entry.getKey() + <span class="string">&quot; = &quot;</span> + entry.getValue());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果遍历过程中需要删除元素，推荐使用 <code>Iterator</code> 的 <code>remove</code> 方法，避免 <code>ConcurrentModificationException</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Iterator&lt;Map.Entry&lt;String, Integer&gt;&gt; iterator = scoreMap.entrySet().iterator();</span><br><span class="line"><span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">    Map.Entry&lt;String, Integer&gt; entry = iterator.next();</span><br><span class="line">    <span class="keyword">if</span> (entry.getValue() &lt; <span class="number">60</span>) &#123;</span><br><span class="line">        iterator.remove();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>简单对比一下：</p><table><thead><tr><th>遍历方式</th><th>适用场景</th><th>推荐程度</th></tr></thead><tbody><tr><td>keySet + get</td><td>只想看 key，或代码很简单</td><td>一般</td></tr><tr><td>entrySet</td><td>同时需要 key 和 value</td><td>推荐</td></tr><tr><td>values</td><td>只需要 value</td><td>推荐</td></tr><tr><td>Iterator</td><td>遍历时需要删除元素</td><td>推荐</td></tr></tbody></table><h2 id="4-初始容量和负载因子怎么设置？"><a href="#4-初始容量和负载因子怎么设置？" class="headerlink" title="4. 初始容量和负载因子怎么设置？"></a>4. 初始容量和负载因子怎么设置？</h2><p><code>HashMap</code> 有两个很重要的参数：初始容量和负载因子。</p><p>默认情况下，初始容量是 <code>16</code>，负载因子是 <code>0.75</code>。当元素数量超过 <code>容量 * 负载因子</code> 时，<code>HashMap</code> 会触发扩容。</p><p>如果提前知道大概要存多少数据，可以合理设置初始容量，减少扩容次数。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">expectedSize</span> <span class="operator">=</span> <span class="number">1000</span>;</span><br><span class="line"><span class="type">float</span> <span class="variable">loadFactor</span> <span class="operator">=</span> <span class="number">0.75f</span>;</span><br><span class="line"><span class="type">int</span> <span class="variable">initialCapacity</span> <span class="operator">=</span> (<span class="type">int</span>) (expectedSize / loadFactor) + <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">Map&lt;String, String&gt; map = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;String, String&gt;(initialCapacity);</span><br><span class="line">System.out.println(<span class="string">&quot;初始容量建议值：&quot;</span> + initialCapacity);</span><br></pre></td></tr></table></figure><p>不过，<code>HashMap</code> 内部容量会调整为 2 的幂。实际开发里可以用一个简单方法计算建议容量：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">calculateHashMapCapacity</span><span class="params">(<span class="type">int</span> expectedSize)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (expectedSize &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;expectedSize不能小于0&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> (<span class="type">int</span>) (expectedSize / <span class="number">0.75f</span>) + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">capacity</span> <span class="operator">=</span> calculateHashMapCapacity(<span class="number">500</span>);</span><br><span class="line">Map&lt;Integer, String&gt; userMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;Integer, String&gt;(capacity);</span><br><span class="line"></span><br><span class="line">userMap.put(<span class="number">1</span>, <span class="string">&quot;用户A&quot;</span>);</span><br><span class="line">userMap.put(<span class="number">2</span>, <span class="string">&quot;用户B&quot;</span>);</span><br></pre></td></tr></table></figure><p>这里的思路不是追求绝对精确，而是尽量避免明知道要放很多数据，却还使用默认容量导致频繁扩容。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20HashMap%20resize%20rehash%20capacity%20load%20factor%20technical%20illustration&image_size=landscape_16_9" alt="HashMap扩容机制"></p><h2 id="5-多线程场景使用-ConcurrentHashMap"><a href="#5-多线程场景使用-ConcurrentHashMap" class="headerlink" title="5. 多线程场景使用 ConcurrentHashMap"></a>5. 多线程场景使用 ConcurrentHashMap</h2><p>如果多个线程会同时读写 Map，可以考虑使用 <code>ConcurrentHashMap</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;String, Integer&gt; countMap = <span class="keyword">new</span> <span class="title class_">ConcurrentHashMap</span>&lt;String, Integer&gt;();</span><br><span class="line"></span><br><span class="line">countMap.put(<span class="string">&quot;Java&quot;</span>, <span class="number">1</span>);</span><br><span class="line">countMap.put(<span class="string">&quot;HashMap&quot;</span>, <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line"><span class="type">Integer</span> <span class="variable">count</span> <span class="operator">=</span> countMap.get(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">System.out.println(<span class="string">&quot;Java出现次数：&quot;</span> + count);</span><br></pre></td></tr></table></figure><p>需要注意的是，<code>ConcurrentHashMap</code> 不是简单地给所有方法都加一把大锁。它内部做了更细粒度的并发控制，所以在并发场景下通常比 <code>Hashtable</code> 更合适。</p><p>不过，复合操作仍然要小心。例如“先判断再放入”不是天然原子的，可以优先使用 <code>putIfAbsent</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">ConcurrentHashMap&lt;String, Integer&gt; map = <span class="keyword">new</span> <span class="title class_">ConcurrentHashMap</span>&lt;String, Integer&gt;();</span><br><span class="line"></span><br><span class="line"><span class="type">Integer</span> <span class="variable">oldValue</span> <span class="operator">=</span> map.putIfAbsent(<span class="string">&quot;Java&quot;</span>, <span class="number">1</span>);</span><br><span class="line"><span class="keyword">if</span> (oldValue == <span class="literal">null</span>) &#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;第一次放入Java&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习 <code>HashMap</code>，我最大的感受是：它看起来简单，但底层并不简单。</p><p><code>HashMap</code> 是面试高频题，也是实际开发中非常常用的集合类。只会 <code>put</code> 和 <code>get</code> 当然可以完成很多需求，但如果不了解底层原理，就容易在哈希冲突、扩容、线程安全、自定义 key 等问题上踩坑。</p><p>我给自己总结了几条使用原则：</p><ol><li><code>HashMap</code> 允许一个 <code>null key</code> 和多个 <code>null value</code>。</li><li>两个 key 的 <code>hashCode</code> 相同不会直接覆盖，还要继续比较 <code>equals</code>。</li><li>同时需要 key 和 value 时，优先使用 <code>entrySet</code> 遍历。</li><li>能预估数据量时，可以设置合适的初始容量，减少扩容。</li><li>多线程写入场景不要直接使用 <code>HashMap</code>，可以考虑 <code>ConcurrentHashMap</code>。</li><li>自定义对象作为 key 时，重写 <code>equals</code> 必须重写 <code>hashCode</code>。</li></ol><p>学完这些内容后，我再看 <code>HashMap</code> 就不只是一个“键值对容器”了，而是能联想到数组、链表、红黑树、哈希冲突和并发安全等知识点。理解底层原理，才能真正把这个常用工具用好。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;2018 年 7 月，最近把 &lt;code&gt;List&lt;/code&gt; 相关的内容过了一遍，接下来开始学习 &lt;code&gt;Map&lt;/code&gt;。和 </summary>
      
    
    
    
    <category term="Java集合" scheme="https://swagch.github.io/categories/Java%E9%9B%86%E5%90%88/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="集合框架" scheme="https://swagch.github.io/tags/%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6/"/>
    
    <category term="HashMap" scheme="https://swagch.github.io/tags/HashMap/"/>
    
    <category term="哈希表" scheme="https://swagch.github.io/tags/%E5%93%88%E5%B8%8C%E8%A1%A8/"/>
    
    <category term="Map" scheme="https://swagch.github.io/tags/Map/"/>
    
  </entry>
  
  <entry>
    <title>ArrayList与LinkedList区别</title>
    <link href="https://swagch.github.io/2018/06/10/2018-06-10-ArrayList%E4%B8%8ELinkedList%E5%8C%BA%E5%88%AB/"/>
    <id>https://swagch.github.io/2018/06/10/2018-06-10-ArrayList%E4%B8%8ELinkedList%E5%8C%BA%E5%88%AB/</id>
    <published>2018-06-10T08:42:55.000Z</published>
    <updated>2026-06-09T07:53:40.334Z</updated>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>学完 Java 面向对象之后，终于进入了集合框架这一章。说实话，刚接触 <code>List</code> 接口的时候我是懵的——<code>ArrayList</code> 和 <code>LinkedList</code> 都实现了 <code>List</code>，方法几乎一模一样，那到底该用哪个？</p><p>当时做课程作业，随手就 <code>new ArrayList&lt;&gt;()</code>，也没多想。直到有一次写一个模拟队列的功能，频繁在头部插入元素，程序跑得巨慢，我才意识到：<strong>选错 List 实现，性能差距可以差到几十倍</strong>。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=ArrayList%20array%20based%20data%20structure%20vs%20LinkedList%20doubly%20linked%20list%20structure,%20side%20by%20side%20comparison,%20memory%20layout%20diagram,%20technical%20illustration&image_size=landscape_4_3" alt="ArrayList与LinkedList底层数据结构对比"></p><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>踩坑过程中，我遇到了三个核心问题：</p><p><strong>1. 随机访问和头插性能差异巨大</strong></p><p>同样的 10 万次操作，<code>ArrayList</code> 随机访问几乎瞬间完成，但头部插入慢到怀疑人生；而 <code>LinkedList</code> 头部插入飞快，随机访问却慢得离谱。我当时完全不理解为什么。</p><p><strong>2. 不知道什么场景该用哪个</strong></p><p>网上文章都说“随机访问多用 ArrayList，增删多用 LinkedList”，但这个“增删”到底指哪个位置的增删？尾部增删两者差不多啊！这种笼统的说法反而让我更困惑。</p><p><strong>3. 遍历删除元素时 ConcurrentModificationException</strong></p><p>这是最坑的。我想遍历 <code>List</code> 删除符合条件的元素，写了下面这种代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (String item : list) &#123;</span><br><span class="line">    <span class="keyword">if</span> (item.equals(<span class="string">&quot;deleteMe&quot;</span>)) &#123;</span><br><span class="line">        list.remove(item);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>结果直接抛了 <code>ConcurrentModificationException</code>，当时完全不知道为什么，也不知道怎么改。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>要真正搞懂这两个类的区别，必须从底层源码入手。</p><h2 id="ArrayList：底层是数组"><a href="#ArrayList：底层是数组" class="headerlink" title="ArrayList：底层是数组"></a>ArrayList：底层是数组</h2><p>翻开 <code>ArrayList</code> 的 JDK 源码，核心就是一个数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">transient</span> Object[] elementData;</span><br><span class="line"><span class="keyword">private</span> <span class="type">int</span> size;</span><br></pre></td></tr></table></figure><p><strong>随机访问为什么是 O(1)？</strong></p><p>因为数组在内存中是连续存储的，通过下标可以直接定位：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">get</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">    rangeCheck(index);</span><br><span class="line">    <span class="keyword">return</span> elementData(index);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">E <span class="title function_">elementData</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> (E) elementData[index];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段源码说明，<code>get(index)</code> 的核心操作就是 <code>elementData[index]</code>，所以随机访问时间复杂度是 O(1)。</p><p><strong>头部插入为什么是 O(n)？</strong></p><p>因为要在头部插入元素，需要把后面所有元素都往后挪一位：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">add</span><span class="params">(<span class="type">int</span> index, E element)</span> &#123;</span><br><span class="line">    rangeCheckForAdd(index);</span><br><span class="line">    ensureCapacityInternal(size + <span class="number">1</span>);</span><br><span class="line">    System.arraycopy(elementData, index, elementData, index + <span class="number">1</span>, size - index);</span><br><span class="line">    elementData[index] = element;</span><br><span class="line">    size++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>关键就在 <code>System.arraycopy</code>。如果在索引 0 的位置插入，原来所有元素都要向后移动，元素越多越慢，所以头插是 O(n)。</p><p><strong>扩容机制也会带来额外成本</strong></p><p><code>ArrayList</code> 默认初始容量是 10，容量不够时会扩容为原来的 1.5 倍：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">grow</span><span class="params">(<span class="type">int</span> minCapacity)</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">oldCapacity</span> <span class="operator">=</span> elementData.length;</span><br><span class="line">    <span class="type">int</span> <span class="variable">newCapacity</span> <span class="operator">=</span> oldCapacity + (oldCapacity &gt;&gt; <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (newCapacity - minCapacity &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        newCapacity = minCapacity;</span><br><span class="line">    &#125;</span><br><span class="line">    elementData = Arrays.copyOf(elementData, newCapacity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>扩容时会复制旧数组，因此如果一开始就知道大概容量，最好使用 <code>new ArrayList&lt;&gt;(capacity)</code>，避免多次扩容。</p><h2 id="LinkedList：底层是双向链表"><a href="#LinkedList：底层是双向链表" class="headerlink" title="LinkedList：底层是双向链表"></a>LinkedList：底层是双向链表</h2><p><code>LinkedList</code> 的核心是 <code>Node</code> 节点，每个节点保存当前元素、前驱节点和后继节点：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Node</span>&lt;E&gt; &#123;</span><br><span class="line">    E item;</span><br><span class="line">    Node&lt;E&gt; next;</span><br><span class="line">    Node&lt;E&gt; prev;</span><br><span class="line"></span><br><span class="line">    Node(Node&lt;E&gt; prev, E element, Node&lt;E&gt; next) &#123;</span><br><span class="line">        <span class="built_in">this</span>.item = element;</span><br><span class="line">        <span class="built_in">this</span>.next = next;</span><br><span class="line">        <span class="built_in">this</span>.prev = prev;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>头部插入为什么是 O(1)？</strong></p><p>因为链表头插只需要修改几个引用，不需要移动已有元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addFirst</span><span class="params">(E e)</span> &#123;</span><br><span class="line">    linkFirst(e);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">linkFirst</span><span class="params">(E e)</span> &#123;</span><br><span class="line">    <span class="keyword">final</span> Node&lt;E&gt; f = first;</span><br><span class="line">    <span class="keyword">final</span> Node&lt;E&gt; newNode = <span class="keyword">new</span> <span class="title class_">Node</span>&lt;&gt;(<span class="literal">null</span>, e, f);</span><br><span class="line">    first = newNode;</span><br><span class="line">    <span class="keyword">if</span> (f == <span class="literal">null</span>) &#123;</span><br><span class="line">        last = newNode;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        f.prev = newNode;</span><br><span class="line">    &#125;</span><br><span class="line">    size++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个过程只调整 <code>first</code>、<code>prev</code> 等引用，和集合大小没有直接关系，所以是 O(1)。</p><p><strong>随机访问为什么是 O(n)？</strong></p><p>链表不像数组那样可以通过下标直接定位。即使知道 index，也必须从头或从尾一步一步走过去：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">get</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">    checkElementIndex(index);</span><br><span class="line">    <span class="keyword">return</span> node(index).item;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Node&lt;E&gt; <span class="title function_">node</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (index &lt; (size &gt;&gt; <span class="number">1</span>)) &#123;</span><br><span class="line">        Node&lt;E&gt; x = first;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; index; i++) &#123;</span><br><span class="line">            x = x.next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        Node&lt;E&gt; x = last;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> size - <span class="number">1</span>; i &gt; index; i--) &#123;</span><br><span class="line">            x = x.prev;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>JDK 做了一个优化：如果 index 在前半部分，就从头开始找；如果在后半部分，就从尾开始找。但本质仍然是遍历，复杂度还是 O(n)。</p><h2 id="fail-fast-机制与-ConcurrentModificationException"><a href="#fail-fast-机制与-ConcurrentModificationException" class="headerlink" title="fail-fast 机制与 ConcurrentModificationException"></a>fail-fast 机制与 ConcurrentModificationException</h2><p>这个问题和 <code>ArrayList</code>、<code>LinkedList</code> 都有关，因为它们都使用了 <code>AbstractList</code> 中的 <code>modCount</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">protected</span> <span class="keyword">transient</span> <span class="type">int</span> <span class="variable">modCount</span> <span class="operator">=</span> <span class="number">0</span>;</span><br></pre></td></tr></table></figure><p>每次对集合做结构性修改，比如 <code>add</code>、<code>remove</code>，<code>modCount</code> 都会变化。迭代器创建时会记录一个 <code>expectedModCount</code>，遍历过程中会检查两者是否一致：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">checkForComodification</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (modCount != expectedModCount) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ConcurrentModificationException</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>所以用 for-each 遍历时调用 <code>list.remove()</code>，集合自己的 <code>modCount</code> 变了，但迭代器里的 <code>expectedModCount</code> 没有同步更新，就会触发 fail-fast 机制，抛出 <code>ConcurrentModificationException</code>。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20fail-fast%20mechanism%20diagram,%20modCount%20expectedModCount%20comparison,%20ConcurrentModificationException%20flow%20chart,%20technical%20illustration&image_size=landscape_4_3" alt="fail-fast机制与ConcurrentModificationException原理"></p><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="完整对比表格"><a href="#完整对比表格" class="headerlink" title="完整对比表格"></a>完整对比表格</h2><table><thead><tr><th>对比项</th><th>ArrayList</th><th>LinkedList</th></tr></thead><tbody><tr><td>底层数据结构</td><td>Object[] 数组</td><td>双向链表</td></tr><tr><td>随机访问 get(i)</td><td><strong>O(1)</strong></td><td>O(n)</td></tr><tr><td>头部插入 add(0, e)</td><td>O(n)</td><td><strong>O(1)</strong></td></tr><tr><td>尾部插入 add(e)</td><td>均摊 O(1)</td><td>O(1)</td></tr><tr><td>中间插入 add(i, e)</td><td>O(n)</td><td>O(n)</td></tr><tr><td>删除 remove(i)</td><td>O(n)</td><td>O(n)</td></tr><tr><td>内存占用</td><td>比较紧凑，但可能有预留空间</td><td>每个节点额外保存 prev、next 引用</td></tr><tr><td>是否支持 RandomAccess</td><td>支持</td><td>不支持</td></tr><tr><td>额外能力</td><td>主要作为 List 使用</td><td>同时可作为 Deque、Queue 使用</td></tr><tr><td>适用场景</td><td>读多写少、随机访问多、尾部追加多</td><td>频繁头部插入删除、队列或双端队列场景</td></tr></tbody></table><p>注意：<code>LinkedList</code> 的中间插入和删除，修改节点引用本身是 O(1)，但定位到目标节点需要 O(n)，所以整体仍然是 O(n)。</p><h2 id="ArrayList-LinkedList-基本操作"><a href="#ArrayList-LinkedList-基本操作" class="headerlink" title="ArrayList&#x2F;LinkedList 基本操作"></a>ArrayList&#x2F;LinkedList 基本操作</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ListBasicDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;String&gt; arrayList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        arrayList.add(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">        arrayList.add(<span class="string">&quot;Python&quot;</span>);</span><br><span class="line">        arrayList.add(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line">        arrayList.add(<span class="number">1</span>, <span class="string">&quot;Go&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;ArrayList: &quot;</span> + arrayList);</span><br><span class="line">        System.out.println(<span class="string">&quot;获取索引2: &quot;</span> + arrayList.get(<span class="number">2</span>));</span><br><span class="line">        arrayList.remove(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;删除C++后: &quot;</span> + arrayList);</span><br><span class="line"></span><br><span class="line">        LinkedList&lt;String&gt; linkedList = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line">        linkedList.add(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">        linkedList.addFirst(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line">        linkedList.addLast(<span class="string">&quot;Python&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;LinkedList: &quot;</span> + linkedList);</span><br><span class="line">        System.out.println(<span class="string">&quot;第一个: &quot;</span> + linkedList.getFirst());</span><br><span class="line">        System.out.println(<span class="string">&quot;最后一个: &quot;</span> + linkedList.getLast());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="性能对比测试代码"><a href="#性能对比测试代码" class="headerlink" title="性能对比测试代码"></a>性能对比测试代码</h2><p>这是我当时写的测试代码，能够比较直观地感受到两者差距：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ListPerformanceTest</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">COUNT</span> <span class="operator">=</span> <span class="number">100000</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        testHeadInsert();</span><br><span class="line">        testRandomAccess();</span><br><span class="line">        testTailInsert();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">testHeadInsert</span><span class="params">()</span> &#123;</span><br><span class="line">        List&lt;String&gt; arrayList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        LinkedList&lt;String&gt; linkedList = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">        <span class="type">long</span> <span class="variable">start</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            arrayList.add(<span class="number">0</span>, <span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">long</span> <span class="variable">end</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;ArrayList 头部插入 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line"></span><br><span class="line">        start = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            linkedList.addFirst(<span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line">        end = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;LinkedList 头部插入 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">testRandomAccess</span><span class="params">()</span> &#123;</span><br><span class="line">        List&lt;String&gt; arrayList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        List&lt;String&gt; linkedList = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            arrayList.add(<span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">            linkedList.add(<span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">long</span> <span class="variable">start</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            arrayList.get(i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">long</span> <span class="variable">end</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;ArrayList 随机访问 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line"></span><br><span class="line">        start = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            linkedList.get(i);</span><br><span class="line">        &#125;</span><br><span class="line">        end = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;LinkedList 随机访问 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">testTailInsert</span><span class="params">()</span> &#123;</span><br><span class="line">        List&lt;String&gt; arrayList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        List&lt;String&gt; linkedList = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">        <span class="type">long</span> <span class="variable">start</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            arrayList.add(<span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">long</span> <span class="variable">end</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;ArrayList 尾部插入 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line"></span><br><span class="line">        start = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            linkedList.add(<span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line">        end = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;LinkedList 尾部插入 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我本机跑出来的结果大致如下，不同机器会有差异：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">ArrayList 头部插入 100000 次: 2835ms</span><br><span class="line">LinkedList 头部插入 100000 次: 8ms</span><br><span class="line">ArrayList 随机访问 100000 次: 3ms</span><br><span class="line">LinkedList 随机访问 100000 次: 5421ms</span><br><span class="line">ArrayList 尾部插入 100000 次: 5ms</span><br><span class="line">LinkedList 尾部插入 100000 次: 9ms</span><br></pre></td></tr></table></figure><p>从结果看，头部插入 <code>ArrayList</code> 明显慢，随机访问则是 <code>LinkedList</code> 明显慢，尾部插入两者差距不大。</p><h2 id="正确遍历删除的方式"><a href="#正确遍历删除的方式" class="headerlink" title="正确遍历删除的方式"></a>正确遍历删除的方式</h2><p><strong>方式一：使用 Iterator.remove()，推荐这种写法</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.Iterator;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IteratorRemoveDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        list.add(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;Python&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line"></span><br><span class="line">        Iterator&lt;String&gt; iterator = list.iterator();</span><br><span class="line">        <span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">            <span class="type">String</span> <span class="variable">item</span> <span class="operator">=</span> iterator.next();</span><br><span class="line">            <span class="keyword">if</span> (<span class="string">&quot;deleteMe&quot;</span>.equals(item)) &#123;</span><br><span class="line">                iterator.remove();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;删除后: &quot;</span> + list);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>Iterator.remove()</code> 安全的原因是：它删除元素后会同步更新迭代器内部的 <code>expectedModCount</code>，因此不会触发 fail-fast。</p><p><strong>方式二：倒序遍历删除，适合 ArrayList</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ReverseRemoveDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        list.add(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;Python&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> list.size() - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="string">&quot;deleteMe&quot;</span>.equals(list.get(i))) &#123;</span><br><span class="line">                list.remove(i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;删除后: &quot;</span> + list);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>倒序删除不会影响还没有遍历到的索引，所以也能避免漏删问题。不过如果已经使用迭代器遍历，还是优先使用 <code>Iterator.remove()</code>。</p><p><strong>方式三：JDK 8 的 removeIf，最简洁</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RemoveIfDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        list.add(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;Python&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line"></span><br><span class="line">        list.removeIf(<span class="string">&quot;deleteMe&quot;</span>::equals);</span><br><span class="line">        System.out.println(<span class="string">&quot;删除后: &quot;</span> + list);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=ArrayList%20vs%20LinkedList%20decision%20tree%20flowchart,%20when%20to%20use%20which%20Java%20List%20implementation,%20scenario%20based%20selection,%20clean%20technical%20diagram&image_size=landscape_4_3" alt="ArrayList与LinkedList场景选择决策图"></p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>经过这次踩坑，我总结出三条实用原则：</p><p><strong>1. 90% 的场景用 ArrayList 就对了</strong></p><p>日常开发里，大部分场景都是读多写少、随机访问、尾部追加。<code>ArrayList</code> 底层数组内存连续，访问速度快，CPU 缓存命中率也更友好，所以它应该作为默认选择。</p><p><strong>2. 频繁头插再考虑 LinkedList</strong></p><p>如果确实需要频繁在头部插入或删除，比如实现队列、双端队列，<code>LinkedList</code> 的 O(1) 头部操作才有优势。但如果还需要大量随机访问，就不要选它。</p><p><strong>3. 遍历删除用 Iterator</strong></p><p>遍历过程中不要直接 <code>list.remove()</code>，否则很容易遇到 <code>ConcurrentModificationException</code>。稳妥做法是使用 <code>Iterator.remove()</code>，JDK 8 以后也可以使用 <code>removeIf</code>。</p><p>最后的感受是：不要只背“ArrayList 查询快，LinkedList 增删快”这种结论。真正重要的是理解数组和链表的底层结构：数组访问快但插入可能搬移元素，链表头插快但定位元素慢。理解了这些，再遇到 List 选择问题就不会慌了。<br>EOF; __tr_native_ec&#x3D;$?; pwd -P &gt;| ‘&#x2F;var&#x2F;folders&#x2F;sb&#x2F;zyyfx17n2q13ky7hfwtl_8dc0000gn&#x2F;T&#x2F;agent-toolhost&#x2F;jobs&#x2F;job-9de6a9094cdf4f9787cdf8a27b5814ac&#x2F;cwd.txt’; exit “$__tr_native_ec”</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;学完 Java 面向对象之后，终于进入了集合框架这一章。说实话，刚接触 &lt;code&gt;List&lt;/code&gt; 接口的时候我是懵的——&lt;code</summary>
      
    
    
    
    <category term="Java集合" scheme="https://swagch.github.io/categories/Java%E9%9B%86%E5%90%88/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="集合框架" scheme="https://swagch.github.io/tags/%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6/"/>
    
    <category term="ArrayList" scheme="https://swagch.github.io/tags/ArrayList/"/>
    
    <category term="LinkedList" scheme="https://swagch.github.io/tags/LinkedList/"/>
    
    <category term="数据结构" scheme="https://swagch.github.io/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
  </entry>
  
  <entry>
    <title>面向对象编程理解</title>
    <link href="https://swagch.github.io/2018/05/14/2018-05-14-%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%E7%BC%96%E7%A8%8B%E7%90%86%E8%A7%A3/"/>
    <id>https://swagch.github.io/2018/05/14/2018-05-14-%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%E7%BC%96%E7%A8%8B%E7%90%86%E8%A7%A3/</id>
    <published>2018-05-14T11:48:27.000Z</published>
    <updated>2026-06-09T08:05:21.999Z</updated>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>学完了Java的基础语法，终于进入了面向对象编程这块硬骨头。在此之前我一直用的是C语言写一些小项目，习惯了面向过程的思维方式——写一个函数，调用它，再写一个函数，再调用它，整个程序就是一系列函数的调用链。突然切换到面向对象，感觉脑子有点转不过弯来。</p><p>我记得《Head First Java》里面有一个很形象的类比：说类就像是饼干的模具，而对象就是用这个模具做出来的饼干。模具本身是一个概念上的模板，真正的饼干才是实在的东西。这个类比让我开始慢慢理解”对象”到底是个什么东西。</p><p>还特意去图书馆借了本《Java编程思想》，翻了几章感觉有点深，还是先老老实实把基础搞懂再说。</p><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始学的时候，遇到了好多困惑：</p><p><strong>第一，类和对象的关系总是搞不清楚。</strong> 我知道类是用class关键字定义的，对象是用new创建出来的，但实际写代码的时候，完全不知道什么时候该定义类，一个类应该包含哪些东西。经常写着写着就把所有代码塞到一个类里面，跟写面向过程没什么区别。</p><p><strong>第二，封装的意义不理解。</strong> 刚接触private、public这些访问修饰符的时候，我就想：为什么要把字段设为private，然后还要写getter和setter去访问？这不脱裤子放屁吗？直接public不是更省事？</p><p><strong>第三，继承和组合不知道怎么选。</strong> 看了书上的例子说”猫是一种动物”可以用继承，”车有轮子”可以用组合。但实际写代码的时候，总是倾向于用继承，觉得extends一下什么都有了，方便。</p><p><strong>第四，多态太抽象了。</strong> 书上说”父类引用指向子类对象”，这句话我背都能背下来，但就是不知道这玩意儿到底有什么用，写代码的时候也完全想不起来用。</p><p><strong>第五，接口和抽象类看着差不多，不知道怎么选。</strong> 两个都不能实例化，都包含抽象方法，到底有啥区别？</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>随着写了一些小例子，慢慢对这些概念有了自己的理解。</p><p><strong>类和对象：</strong> 类就是一个模板&#x2F;蓝图，比如说”学生”这个概念，它有一些属性（姓名、学号、年龄）和行为（学习、考试）。而对象就是根据这个模板创建出来的具体实例，比如”张三”这个具体的学生。写代码的时候，应该先分析业务需求中有哪些实体，每个实体有什么属性和行为，再来定义类。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=OOP%20three%20pillars%20encapsulation%20inheritance%20polymorphism%20diagram,%20clean%20modern%20infographic%20style&image_size=landscape_4_3" alt="OOP三大特性"><br><em>配图说明：面向对象三大特性关系图</em></p><p><strong>封装：</strong> 现在理解了，封装的意义在于保护内部状态。如果不封装，外部代码可以随意修改对象的内部数据，可能会导致数据不一致。比如一个Student的age字段，如果直接暴露为public，外部可以给它赋一个-1这种不合理的值。通过setter方法，可以在赋值前做校验。</p><p><strong>继承与组合：</strong> “IS-A”关系适合用继承，”HAS-A”关系适合用组合。但继承的问题在于，父类的任何变化都会影响到所有子类，耦合度太高。现在很多书都推荐优先使用组合，组合更灵活，耦合度更低。</p><p><strong>多态：</strong> 多态的本质就在于父类引用指向子类对象，调用的方法却是子类重写后的版本。这样做的最大好处是：可以写通用的代码来处理不同的具体类型。比如定义一个统一的pay()方法，不管是学生交学费，还是员工发工资，都调用同一个方法名，具体怎么实现由子类自己决定。</p><p><strong>接口与抽象类：</strong> 接口定义的是行为契约——“你能做什么”；抽象类提供的部分实现——“你是什么”。接口强调的是能力，抽象类强调的是血缘关系。接口弥补了Java单继承的不足，一个类可以实现多个接口。</p><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>光说不练假把式，写几个例子来巩固一下。</p><h3 id="封装示例：Student类"><a href="#封装示例：Student类" class="headerlink" title="封装示例：Student类"></a>封装示例：Student类</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Student</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> String studentId;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> age;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> score;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Student</span><span class="params">(String name, String studentId, <span class="type">int</span> age)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        <span class="built_in">this</span>.studentId = studentId;</span><br><span class="line">        setAge(age); <span class="comment">// 通过setter方法赋值，享受校验逻辑</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getName</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getStudentId</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> studentId;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getAge</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setAge</span><span class="params">(<span class="type">int</span> age)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (age &lt; <span class="number">0</span> || age &gt; <span class="number">150</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;年龄不合法：&quot;</span> + age);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getScore</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> score;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setScore</span><span class="params">(<span class="type">double</span> score)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (score &lt; <span class="number">0</span> || score &gt; <span class="number">100</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;分数不合法：&quot;</span> + score);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">this</span>.score = score;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">study</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot;正在学习&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">takeExam</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot;正在考试&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>把字段设为private，通过public的getter&#x2F;setter访问，在setter里做合法性校验，这就是封装最直观的用法。</p><h3 id="继承示例：Person-Student-Teacher"><a href="#继承示例：Person-Student-Teacher" class="headerlink" title="继承示例：Person -&gt; Student &#x2F; Teacher"></a>继承示例：Person -&gt; Student &#x2F; Teacher</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="keyword">protected</span> String name;</span><br><span class="line">    <span class="keyword">protected</span> <span class="type">int</span> age;</span><br><span class="line">    <span class="keyword">protected</span> String gender;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Person</span><span class="params">(String name, <span class="type">int</span> age, String gender)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">        <span class="built_in">this</span>.gender = gender;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">introduce</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;我叫&quot;</span> + name + <span class="string">&quot;，今年&quot;</span> + age + <span class="string">&quot;岁&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 统一的行为接口，子类可以重写</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">pay</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot;支付了费用&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Student</span> <span class="keyword">extends</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String studentId;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> score;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Student</span><span class="params">(String name, <span class="type">int</span> age, String gender, String studentId)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(name, age, gender);</span><br><span class="line">        <span class="built_in">this</span>.studentId = studentId;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">pay</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;学生&quot;</span> + name + <span class="string">&quot;支付了学费5000元&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">study</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot;正在学习&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Teacher</span> <span class="keyword">extends</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String employeeId;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> salary;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Teacher</span><span class="params">(String name, <span class="type">int</span> age, String gender, String employeeId, <span class="type">double</span> salary)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(name, age, gender);</span><br><span class="line">        <span class="built_in">this</span>.employeeId = employeeId;</span><br><span class="line">        <span class="built_in">this</span>.salary = salary;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">pay</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;教师&quot;</span> + name + <span class="string">&quot;支付了工会费200元&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">teach</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot;正在上课&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Student和Teacher都是Person，所以用继承。它们都重写了pay()方法，这就是多态的基础。</p><h3 id="多态示例：用统一的pay-方法"><a href="#多态示例：用统一的pay-方法" class="headerlink" title="多态示例：用统一的pay()方法"></a>多态示例：用统一的pay()方法</h3><p>多态的好处，看下面这段代码就明白了：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 创建一个Student和一个Teacher</span></span><br><span class="line">        <span class="type">Person</span> <span class="variable">stu</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Student</span>(<span class="string">&quot;张三&quot;</span>, <span class="number">18</span>, <span class="string">&quot;男&quot;</span>, <span class="string">&quot;S001&quot;</span>);</span><br><span class="line">        <span class="type">Person</span> <span class="variable">tch</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Teacher</span>(<span class="string">&quot;李老师&quot;</span>, <span class="number">35</span>, <span class="string">&quot;女&quot;</span>, <span class="string">&quot;T001&quot;</span>, <span class="number">8000</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 把它们放到同一个数组中</span></span><br><span class="line">        Person[] people = &#123;stu, tch&#125;;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 统一调用pay()方法，实际执行的是各自子类的版本</span></span><br><span class="line">        <span class="keyword">for</span> (Person p : people) &#123;</span><br><span class="line">            p.pay(); <span class="comment">// 多态！不需要判断具体类型</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行结果：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">学生张三支付了学费5000元</span><br><span class="line">教师李老师支付了工会费200元</span><br></pre></td></tr></table></figure><p>看到没有？循环里写的是<code>p.pay()</code>，但实际执行的效果取决于p到底指向的是Student还是Teacher。如果不用多态，就得写一堆if-else来判断类型，扩展性差多了。</p><h3 id="接口示例：实现Comparable接口"><a href="#接口示例：实现Comparable接口" class="headerlink" title="接口示例：实现Comparable接口"></a>接口示例：实现Comparable接口</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Student</span> <span class="keyword">extends</span> <span class="title class_">Person</span> <span class="keyword">implements</span> <span class="title class_">Comparable</span>&lt;Student&gt; &#123;</span><br><span class="line">    <span class="comment">// ...其他代码同上...</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">compareTo</span><span class="params">(Student other)</span> &#123;</span><br><span class="line">        <span class="comment">// 按分数排序，分数高的排前面</span></span><br><span class="line">        <span class="keyword">return</span> Double.compare(other.getScore(), <span class="built_in">this</span>.getScore());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 排序用法</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;Student&gt; students = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        students.add(<span class="keyword">new</span> <span class="title class_">Student</span>(<span class="string">&quot;张三&quot;</span>, <span class="number">18</span>, <span class="string">&quot;男&quot;</span>, <span class="string">&quot;S001&quot;</span>));</span><br><span class="line">        students.add(<span class="keyword">new</span> <span class="title class_">Student</span>(<span class="string">&quot;李四&quot;</span>, <span class="number">19</span>, <span class="string">&quot;男&quot;</span>, <span class="string">&quot;S002&quot;</span>));</span><br><span class="line">        students.add(<span class="keyword">new</span> <span class="title class_">Student</span>(<span class="string">&quot;王五&quot;</span>, <span class="number">18</span>, <span class="string">&quot;女&quot;</span>, <span class="string">&quot;S003&quot;</span>));</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 给学生们设置分数</span></span><br><span class="line">        students.get(<span class="number">0</span>).setScore(<span class="number">85</span>);</span><br><span class="line">        students.get(<span class="number">1</span>).setScore(<span class="number">92</span>);</span><br><span class="line">        students.get(<span class="number">2</span>).setScore(<span class="number">78</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 调用Collections.sort()排序，底层会调用compareTo()方法</span></span><br><span class="line">        Collections.sort(students);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (Student s : students) &#123;</span><br><span class="line">            System.out.println(s.getName() + <span class="string">&quot;：&quot;</span> + s.getScore() + <span class="string">&quot;分&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>实现Comparable接口，就是告诉别人：我这个类的对象是可以比较大小的。这意味着你可以把这个类的对象放进TreeSet、用Collections.sort()排序等等。这就是面向接口编程的思想——依赖接口而不是具体实现。</p><h3 id="抽象类示例：Shape-Circle-Rectangle"><a href="#抽象类示例：Shape-Circle-Rectangle" class="headerlink" title="抽象类示例：Shape -&gt; Circle &#x2F; Rectangle"></a>抽象类示例：Shape -&gt; Circle &#x2F; Rectangle</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="keyword">protected</span> String color;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Shape</span><span class="params">(String color)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.color = color;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getColor</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> color;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 抽象方法：子类必须实现</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="type">double</span> <span class="title function_">getArea</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 抽象方法：子类必须实现</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="type">double</span> <span class="title function_">getPerimeter</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 具体方法：所有子类共享</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">display</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;颜色：&quot;</span> + color + <span class="string">&quot;，面积：&quot;</span> + getArea() + <span class="string">&quot;，周长：&quot;</span> + getPerimeter());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Circle</span> <span class="keyword">extends</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> radius;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Circle</span><span class="params">(String color, <span class="type">double</span> radius)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(color);</span><br><span class="line">        <span class="built_in">this</span>.radius = radius;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getArea</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> Math.PI * radius * radius;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getPerimeter</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">2</span> * Math.PI * radius;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Rectangle</span> <span class="keyword">extends</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> width;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> height;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Rectangle</span><span class="params">(String color, <span class="type">double</span> width, <span class="type">double</span> height)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(color);</span><br><span class="line">        <span class="built_in">this</span>.width = width;</span><br><span class="line">        <span class="built_in">this</span>.height = height;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getArea</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> width * height;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getPerimeter</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">2</span> * (width + height);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>抽象类Shape定义了形状该有的行为（求面积、求周长），但具体的计算方式交给子类去实现。display()方法是通用的，在抽象类里直接实现就好，子类不用重复写。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>折腾了两周，对面向对象总算有了个整体的认识。总结一下这几个星期的收获：</p><ol><li><p><strong>三大特性</strong>：封装、继承、多态。封装保护内部数据，继承实现代码复用，多态让代码更灵活。这三个东西是面向对象的基石，缺一不可。</p></li><li><p><strong>设计思路要转变</strong>：从面向过程的”先想步骤”变成面向对象的”先找对象”。写代码之前先分析业务中有哪些实体（类），它们有什么属性和行为，它们之间有什么关系。这个思维转换确实需要时间，但慢慢练习就会习惯。</p></li><li><p><strong>优先用组合</strong>：虽然继承用起来方便，但继承会让父类和子类耦合太紧。如果只是想要复用代码，组合往往是更好的选择。只有当确实是”IS-A”关系，并且行为需要多态时，才考虑继承。</p></li><li><p><strong>面向接口编程</strong>：接口定义契约，实现类去遵守契约。这样写出来的代码耦合度低，扩展性好。比如排序功能依赖Comparable接口而不是具体的类，以后想换排序规则，只要改compareTo()的实现就好了。</p></li></ol><p>面向对象的路才刚刚开始，后面还有设计模式等着我。不过有了这些基础，至少看代码的时候不会再一头雾水了。继续加油！</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20programmer%20coding%20with%20OOP%20concepts%20in%20mind,%20simple%20flat%20style&image_size=landscape_4_3" alt="继续前行"><br><em>配图说明：初学者的面向对象学习之路</em></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;学完了Java的基础语法，终于进入了面向对象编程这块硬骨头。在此之前我一直用的是C语言写一些小项目，习惯了面向过程的思维方式——写一个函数，</summary>
      
    
    
    
    <category term="Java基础" scheme="https://swagch.github.io/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="面向对象" scheme="https://swagch.github.io/tags/%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1/"/>
    
    <category term="OOP" scheme="https://swagch.github.io/tags/OOP/"/>
    
    <category term="封装" scheme="https://swagch.github.io/tags/%E5%B0%81%E8%A3%85/"/>
    
    <category term="继承" scheme="https://swagch.github.io/tags/%E7%BB%A7%E6%89%BF/"/>
    
    <category term="多态" scheme="https://swagch.github.io/tags/%E5%A4%9A%E6%80%81/"/>
    
  </entry>
  
  <entry>
    <title>Java数据类型学习笔记</title>
    <link href="https://swagch.github.io/2018/04/08/2018-04-08-Java%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
    <id>https://swagch.github.io/2018/04/08/2018-04-08-Java%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</id>
    <published>2018-04-08T12:15:33.000Z</published>
    <updated>2026-06-09T07:47:48.119Z</updated>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>学完第一个 <code>Hello World</code> 之后，我开始系统学习 Java 的基础语法。刚开始觉得数据类型应该不难，因为之前学 C 语言的时候也接触过 <code>int</code>、<code>char</code>、<code>float</code> 这些类型。</p><p>但是真正写 Java 代码时，我发现 Java 的类型系统比 C 语言更严格。变量必须先声明类型，类型之间不能随便混用，很多转换都需要明确写出来。</p><p>这几天练习的时候，我还遇到了两个很容易忽略的问题：一个是 <code>int</code> 运算溢出，另一个是 <code>float</code> 和 <code>double</code> 做小数计算时出现精度丢失。看起来只是基础知识，但如果不弄清楚，以后写业务代码肯定会踩坑。</p><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>我目前遇到的问题主要有这几个：</p><ul><li>整数运算结果和预期不符（int溢出）</li><li>float和double计算金额时精度丢失</li><li>int和Integer傻傻分不清</li><li>自动类型转换和强制类型转换的规则记不住</li></ul><p>其中最让我困惑的是：明明代码没有报错，运行结果却是错的。比如两个很大的 <code>int</code> 相加，结果可能变成负数；两个小数相加，结果也可能不是我以为的 <code>0.3</code>。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>Java 里常用的整数类型是 <code>int</code>，但是 <code>int</code> 的范围并不是无限大，它的范围大约是 ±21 亿，也就是 <code>-2147483648</code> 到 <code>2147483647</code>。如果超过这个范围，就会发生溢出。大数运算应该使用 <code>long</code>，如果数字特别大，还需要使用 <code>BigInteger</code>。</p><p>浮点数的问题也比较典型。<code>float</code> 和 <code>double</code> 采用 IEEE 754 标准，它们在底层是用二进制表示小数的，所以无法精确表示某些十进制小数，比如 <code>0.1</code>、<code>0.2</code>。因此金额计算不能使用 <code>float</code> 或 <code>double</code>，应该使用 <code>BigDecimal</code>。</p><p>另外，<code>int</code> 是基本类型，而 <code>Integer</code> 是包装类。基本类型保存的是具体的数值，包装类是对象。Java 5 以后支持自动装箱和自动拆箱，所以平时写代码时它们看起来可以互相转换，但本质上不是一回事。</p><p>类型转换的规则可以简单理解为：小类型自动转大类型，大类型转小类型需要强制转换，而且可能丢失精度。</p><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="8种基本类型的完整表格"><a href="#8种基本类型的完整表格" class="headerlink" title="8种基本类型的完整表格"></a>8种基本类型的完整表格</h2><p>Java 一共有 8 种基本类型，可以分成整数、浮点数、字符和布尔四类。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20data%20type%20classification%20tree%20diagram%20showing%208%20primitive%20types%20and%20reference%20types,%20clean%20educational%20infographic&image_size=landscape_4_3" alt="Java数据类型分类"><br><em>配图说明：Java数据类型分类体系</em></p><table><thead><tr><th>类型</th><th align="right">字节数</th><th>范围</th><th>默认值</th></tr></thead><tbody><tr><td>byte</td><td align="right">1</td><td>-128 ~ 127</td><td>0</td></tr><tr><td>short</td><td align="right">2</td><td>-32768 ~ 32767</td><td>0</td></tr><tr><td>int</td><td align="right">4</td><td>-2147483648 ~ 2147483647</td><td>0</td></tr><tr><td>long</td><td align="right">8</td><td>-9223372036854775808 ~ 9223372036854775807</td><td>0L</td></tr><tr><td>float</td><td align="right">4</td><td>约 ±3.4E38，有效位约 6~7 位</td><td>0.0f</td></tr><tr><td>double</td><td align="right">8</td><td>约 ±1.7E308，有效位约 15 位</td><td>0.0d</td></tr><tr><td>char</td><td align="right">2</td><td>0 ~ 65535</td><td>‘\u0000’</td></tr><tr><td>boolean</td><td align="right">1位或不确定，JVM未明确规定</td><td>true &#x2F; false</td><td>false</td></tr></tbody></table><p>下面是 8 种基本类型的声明方式和取值范围演示：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">PrimitiveTypeDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">byte</span> <span class="variable">byteValue</span> <span class="operator">=</span> <span class="number">127</span>;</span><br><span class="line">        <span class="type">short</span> <span class="variable">shortValue</span> <span class="operator">=</span> <span class="number">32767</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">intValue</span> <span class="operator">=</span> <span class="number">2147483647</span>;</span><br><span class="line">        <span class="type">long</span> <span class="variable">longValue</span> <span class="operator">=</span> <span class="number">9223372036854775807L</span>;</span><br><span class="line">        <span class="type">float</span> <span class="variable">floatValue</span> <span class="operator">=</span> <span class="number">3.14F</span>;</span><br><span class="line">        <span class="type">double</span> <span class="variable">doubleValue</span> <span class="operator">=</span> <span class="number">3.141592653589793</span>;</span><br><span class="line">        <span class="type">char</span> <span class="variable">charValue</span> <span class="operator">=</span> <span class="string">&#x27;A&#x27;</span>;</span><br><span class="line">        <span class="type">boolean</span> <span class="variable">booleanValue</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;byte范围：&quot;</span> + Byte.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Byte.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;short范围：&quot;</span> + Short.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Short.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;int范围：&quot;</span> + Integer.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Integer.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;long范围：&quot;</span> + Long.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Long.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;float范围：&quot;</span> + Float.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Float.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;double范围：&quot;</span> + Double.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Double.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;char示例：&quot;</span> + charValue);</span><br><span class="line">        System.out.println(<span class="string">&quot;boolean示例：&quot;</span> + booleanValue);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里要注意，<code>long</code> 类型的字面量后面最好加 <code>L</code>，<code>float</code> 类型的字面量后面要加 <code>F</code>，否则 Java 默认会把小数当成 <code>double</code>。</p><h2 id="int溢出演示代码"><a href="#int溢出演示代码" class="headerlink" title="int溢出演示代码"></a>int溢出演示代码</h2><p><code>int</code> 超过最大值后不会自动报错，而是会绕回负数，这一点刚开始看很反直觉。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IntOverflowDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">max</span> <span class="operator">=</span> Integer.MAX_VALUE;</span><br><span class="line">        <span class="type">int</span> <span class="variable">overflow</span> <span class="operator">=</span> max + <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;int最大值：&quot;</span> + max);</span><br><span class="line">        System.out.println(<span class="string">&quot;int最大值 + 1：&quot;</span> + overflow);</span><br><span class="line"></span><br><span class="line">        <span class="type">long</span> <span class="variable">correctResult</span> <span class="operator">=</span> (<span class="type">long</span>) max + <span class="number">1</span>;</span><br><span class="line">        System.out.println(<span class="string">&quot;转成long后再计算：&quot;</span> + correctResult);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出结果中，<code>Integer.MAX_VALUE + 1</code> 会变成 <code>Integer.MIN_VALUE</code>。所以如果参与运算的数字可能超过 <code>int</code> 范围，就要提前使用 <code>long</code>。</p><p>如果数字比 <code>long</code> 还大，就要考虑 <code>BigInteger</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.math.BigInteger;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BigIntegerDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">BigInteger</span> <span class="variable">a</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BigInteger</span>(<span class="string">&quot;999999999999999999999999999999&quot;</span>);</span><br><span class="line">        <span class="type">BigInteger</span> <span class="variable">b</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BigInteger</span>(<span class="string">&quot;888888888888888888888888888888&quot;</span>);</span><br><span class="line">        <span class="type">BigInteger</span> <span class="variable">sum</span> <span class="operator">=</span> a.add(b);</span><br><span class="line"></span><br><span class="line">        System.out.println(sum);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="float精度丢失代码"><a href="#float精度丢失代码" class="headerlink" title="float精度丢失代码"></a>float精度丢失代码</h2><p>下面这个例子是我印象很深的一个坑：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">FloatPrecisionDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">float</span> <span class="variable">a</span> <span class="operator">=</span> <span class="number">0.1F</span>;</span><br><span class="line">        <span class="type">float</span> <span class="variable">b</span> <span class="operator">=</span> <span class="number">0.2F</span>;</span><br><span class="line">        <span class="type">double</span> <span class="variable">c</span> <span class="operator">=</span> <span class="number">0.1</span>;</span><br><span class="line">        <span class="type">double</span> <span class="variable">d</span> <span class="operator">=</span> <span class="number">0.2</span>;</span><br><span class="line"></span><br><span class="line">        System.out.println(a + b);</span><br><span class="line">        System.out.println(c + d);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>结果不一定是准确的 <code>0.3</code>，因为二进制浮点数不能精确表示某些十进制小数。</p><h2 id="BigDecimal解决金额计算的完整示例"><a href="#BigDecimal解决金额计算的完整示例" class="headerlink" title="BigDecimal解决金额计算的完整示例"></a>BigDecimal解决金额计算的完整示例</h2><p>金额计算应该使用 <code>BigDecimal</code>，而且创建 <code>BigDecimal</code> 时尽量使用字符串，不要直接传入 <code>double</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.math.BigDecimal;</span><br><span class="line"><span class="keyword">import</span> java.math.RoundingMode;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BigDecimalMoneyDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">BigDecimal</span> <span class="variable">price</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BigDecimal</span>(<span class="string">&quot;19.90&quot;</span>);</span><br><span class="line">        <span class="type">BigDecimal</span> <span class="variable">count</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BigDecimal</span>(<span class="string">&quot;3&quot;</span>);</span><br><span class="line">        <span class="type">BigDecimal</span> <span class="variable">discount</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BigDecimal</span>(<span class="string">&quot;5.00&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="type">BigDecimal</span> <span class="variable">total</span> <span class="operator">=</span> price.multiply(count).subtract(discount);</span><br><span class="line">        <span class="type">BigDecimal</span> <span class="variable">average</span> <span class="operator">=</span> total.divide(count, <span class="number">2</span>, RoundingMode.HALF_UP);</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;总金额：&quot;</span> + total);</span><br><span class="line">        System.out.println(<span class="string">&quot;平均金额：&quot;</span> + average);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个例子里，<code>multiply</code> 是乘法，<code>subtract</code> 是减法，<code>divide</code> 是除法。除法一定要注意指定保留位数和舍入方式，否则遇到除不尽的情况可能会抛异常。</p><h2 id="引用类型和基本类型的区别对比"><a href="#引用类型和基本类型的区别对比" class="headerlink" title="引用类型和基本类型的区别对比"></a>引用类型和基本类型的区别对比</h2><p>Java 的数据类型可以分成基本类型和引用类型。</p><table><thead><tr><th>对比项</th><th>基本类型</th><th>引用类型</th></tr></thead><tbody><tr><td>保存内容</td><td>直接保存值</td><td>保存对象的引用地址</td></tr><tr><td>是否为对象</td><td>不是对象</td><td>是对象</td></tr><tr><td>默认值</td><td>例如 int 是 0，boolean 是 false</td><td>null</td></tr><tr><td>常见类型</td><td>byte、short、int、long、float、double、char、boolean</td><td>类、接口、数组、枚举、注解</td></tr><tr><td>方法调用</td><td>不能直接调用方法</td><td>可以调用对象方法</td></tr><tr><td>泛型使用</td><td>不能直接作为泛型类型</td><td>可以作为泛型类型</td></tr></tbody></table><p>比如 <code>int</code> 和 <code>Integer</code> 就是最常见的一组对比：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IntIntegerDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">primitiveValue</span> <span class="operator">=</span> <span class="number">10</span>;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">wrapperValue</span> <span class="operator">=</span> Integer.valueOf(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">        System.out.println(primitiveValue);</span><br><span class="line">        System.out.println(wrapperValue.toString());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>int</code> 只是一个数值，<code>Integer</code> 是一个对象，所以 <code>Integer</code> 可以调用方法，也可以放进集合里。</p><h2 id="自动类型转换规则图示"><a href="#自动类型转换规则图示" class="headerlink" title="自动类型转换规则图示"></a>自动类型转换规则图示</h2><p>Java 中常见的自动类型转换顺序可以记成下面这样：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">byte -&gt; short -&gt; int -&gt; long -&gt; float -&gt; double</span><br><span class="line">char -&gt; int -&gt; long -&gt; float -&gt; double</span><br></pre></td></tr></table></figure><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20numeric%20type%20conversion%20flow%20chart%20byte%20short%20char%20int%20long%20float%20double,%20clean%20study%20note%20style&image_size=landscape_4_3" alt="Java自动类型转换规则"><br><em>配图说明：Java数值类型自动转换方向</em></p><p>小范围类型赋值给大范围类型时，可以自动转换：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AutoConvertDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">byte</span> <span class="variable">byteValue</span> <span class="operator">=</span> <span class="number">10</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">intValue</span> <span class="operator">=</span> byteValue;</span><br><span class="line">        <span class="type">long</span> <span class="variable">longValue</span> <span class="operator">=</span> intValue;</span><br><span class="line">        <span class="type">double</span> <span class="variable">doubleValue</span> <span class="operator">=</span> longValue;</span><br><span class="line"></span><br><span class="line">        <span class="type">char</span> <span class="variable">charValue</span> <span class="operator">=</span> <span class="string">&#x27;A&#x27;</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">charCode</span> <span class="operator">=</span> charValue;</span><br><span class="line"></span><br><span class="line">        System.out.println(doubleValue);</span><br><span class="line">        System.out.println(charCode);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里 <code>byte</code> 可以自动转成 <code>int</code>，<code>int</code> 可以自动转成 <code>long</code>，<code>long</code> 也可以自动转成 <code>double</code>。</p><h2 id="强制类型转换示例及注意事项"><a href="#强制类型转换示例及注意事项" class="headerlink" title="强制类型转换示例及注意事项"></a>强制类型转换示例及注意事项</h2><p>大范围类型转成小范围类型时，需要强制转换：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ForceConvertDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">double</span> <span class="variable">doubleValue</span> <span class="operator">=</span> <span class="number">3.99</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">intValue</span> <span class="operator">=</span> (<span class="type">int</span>) doubleValue;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">largeValue</span> <span class="operator">=</span> <span class="number">130</span>;</span><br><span class="line">        <span class="type">byte</span> <span class="variable">byteValue</span> <span class="operator">=</span> (<span class="type">byte</span>) largeValue;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;double转int：&quot;</span> + intValue);</span><br><span class="line">        System.out.println(<span class="string">&quot;int转byte：&quot;</span> + byteValue);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>double</code> 转成 <code>int</code> 后，小数部分会被直接截断，不会四舍五入。<code>130</code> 转成 <code>byte</code> 后也不是 <code>130</code>，因为 <code>byte</code> 的范围只有 <code>-128 ~ 127</code>，超出范围会发生溢出。</p><h2 id="自动装箱拆箱的示例及IntegerCache陷阱"><a href="#自动装箱拆箱的示例及IntegerCache陷阱" class="headerlink" title="自动装箱拆箱的示例及IntegerCache陷阱"></a>自动装箱拆箱的示例及IntegerCache陷阱</h2><p>自动装箱就是基本类型自动转成包装类，自动拆箱就是包装类自动转成基本类型。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BoxingDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">primitiveValue</span> <span class="operator">=</span> <span class="number">100</span>;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">boxedValue</span> <span class="operator">=</span> primitiveValue;</span><br><span class="line">        <span class="type">int</span> <span class="variable">unboxedValue</span> <span class="operator">=</span> boxedValue;</span><br><span class="line"></span><br><span class="line">        System.out.println(boxedValue);</span><br><span class="line">        System.out.println(unboxedValue);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但是 <code>Integer</code> 有一个缓存范围，默认是 <code>-128 ~ 127</code>。这个地方很容易让人误以为 <code>==</code> 比较的是数值，其实对象之间的 <code>==</code> 比较的是引用地址。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IntegerCacheDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">a</span> <span class="operator">=</span> <span class="number">127</span>;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">b</span> <span class="operator">=</span> <span class="number">127</span>;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">c</span> <span class="operator">=</span> <span class="number">128</span>;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">d</span> <span class="operator">=</span> <span class="number">128</span>;</span><br><span class="line"></span><br><span class="line">        System.out.println(a == b);</span><br><span class="line">        System.out.println(c == d);</span><br><span class="line">        System.out.println(c.equals(d));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>a == b</code> 是 <code>true</code>，因为 <code>127</code> 在缓存范围内。<code>c == d</code> 是 <code>false</code>，因为 <code>128</code> 默认不在缓存范围内，会创建不同的对象。比较包装类的数值时，应该使用 <code>equals</code>，不要使用 <code>==</code>。</p><p>另外，自动拆箱时如果包装类是 <code>null</code>，会抛出空指针异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UnboxingNullDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">value</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> value;</span><br><span class="line"></span><br><span class="line">        System.out.println(result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码编译能通过，但运行时会报 <code>NullPointerException</code>。所以包装类拆箱前要特别注意是否为 <code>null</code>。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>学完这部分之后，我感觉 Java 的数据类型虽然看起来基础，但细节很多。尤其是类型范围、精度问题、包装类和类型转换，如果只靠直觉写代码，很容易出现隐藏 bug。</p><p>我的阶段性总结是：</p><ul><li>Java是强类型语言，类型安全比C更严格</li><li>金额计算永远不要用float&#x2F;double</li><li>注意基本类型和包装类的区别</li><li>类型转换要小心精度丢失</li></ul><p>以后写代码时，我会尽量先想清楚变量的范围和用途，再决定使用哪种类型。对于金额这种对精度要求很高的数据，一开始就应该使用 <code>BigDecimal</code>，不要等出问题后再改。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;学完第一个 &lt;code&gt;Hello World&lt;/code&gt; 之后，我开始系统学习 Java 的基础语法。刚开始觉得数据类型应该不难，因为之</summary>
      
    
    
    
    <category term="Java基础" scheme="https://swagch.github.io/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="数据类型" scheme="https://swagch.github.io/tags/%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/"/>
    
    <category term="基本类型" scheme="https://swagch.github.io/tags/%E5%9F%BA%E6%9C%AC%E7%B1%BB%E5%9E%8B/"/>
    
    <category term="引用类型" scheme="https://swagch.github.io/tags/%E5%BC%95%E7%94%A8%E7%B1%BB%E5%9E%8B/"/>
    
    <category term="类型转换" scheme="https://swagch.github.io/tags/%E7%B1%BB%E5%9E%8B%E8%BD%AC%E6%8D%A2/"/>
    
  </entry>
  
  <entry>
    <title>第一个Hello World程序详解</title>
    <link href="https://swagch.github.io/2018/02/25/2018-02-25-%E7%AC%AC%E4%B8%80%E4%B8%AAHello-World%E7%A8%8B%E5%BA%8F%E8%AF%A6%E8%A7%A3/"/>
    <id>https://swagch.github.io/2018/02/25/2018-02-25-%E7%AC%AC%E4%B8%80%E4%B8%AAHello-World%E7%A8%8B%E5%BA%8F%E8%AF%A6%E8%A7%A3/</id>
    <published>2018-02-25T07:22:08.000Z</published>
    <updated>2026-06-09T07:47:40.384Z</updated>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>今天把 JDK 和开发工具都装好以后，终于开始写第一个 Java 程序了。虽然只是最简单的 <code>Hello World</code>，但是实际动手以后发现，这里面并不是只有一行输出语句那么简单。</p><p>我之前学 C 语言的时候，也是从 <code>Hello World</code> 开始的。当时大概知道写完 <code>.c</code> 文件以后，用编译器编译，再运行生成的可执行文件就可以了。所以刚开始学 Java 的时候，我也下意识以为流程差不多：写代码、编译、运行。</p><p>但是 Java 这里多了一个 <code>.class</code> 文件，还要用 <code>javac</code> 和 <code>java</code> 两个命令分别处理，一开始确实有点懵。于是我想把 Java 程序从编写到运行的完整过程搞清楚，至少以后再遇到编译错误、运行错误时，知道问题大概出在哪里。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20compile%20and%20run%20flowchart%20showing%20java%20source%20to%20class%20file%20to%20JVM%20execution,%20clean%20diagram%20style&image_size=landscape_4_3" alt="Java编译运行流程"><br><em>配图说明：Java程序编译运行流程</em></p><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>第一次写 Java 的 <code>Hello World</code>，我主要遇到了下面几个问题。</p><h2 id="文件名和类名不一致导致编译报错"><a href="#文件名和类名不一致导致编译报错" class="headerlink" title="文件名和类名不一致导致编译报错"></a>文件名和类名不一致导致编译报错</h2><p>我一开始把文件保存成了 <code>hello.java</code>，里面写的是：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Hello World&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">javac hello.java</span><br></pre></td></tr></table></figure><p>结果编译器报错，大概意思是公共类 <code>HelloWorld</code> 应该放在 <code>HelloWorld.java</code> 文件中。这个错误很典型，因为 Java 对 <code>public class</code> 的类名和文件名要求非常严格。</p><h2 id="缺少main方法不知道程序入口在哪"><a href="#缺少main方法不知道程序入口在哪" class="headerlink" title="缺少main方法不知道程序入口在哪"></a>缺少main方法不知道程序入口在哪</h2><p>我还尝试过只写一个普通方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sayHello</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Hello World&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码能编译通过，但是运行时会提示找不到 <code>main</code> 方法。也就是说，Java 程序不是只要有类就能直接运行，JVM 需要知道从哪里开始执行。</p><h2 id="不理解为什么javac编译后生成-class文件"><a href="#不理解为什么javac编译后生成-class文件" class="headerlink" title="不理解为什么javac编译后生成.class文件"></a>不理解为什么javac编译后生成.class文件</h2><p>执行 <code>javac HelloWorld.java</code> 后，目录里生成了 <code>HelloWorld.class</code>。我一开始以为编译后应该生成类似 Windows 下的 <code>.exe</code> 文件，但是 Java 生成的是字节码文件，这和 C 语言直接编译成机器码不同。</p><h2 id="中文输出乱码问题"><a href="#中文输出乱码问题" class="headerlink" title="中文输出乱码问题"></a>中文输出乱码问题</h2><p>我在程序里输出中文时，也遇到过乱码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">System.out.println(<span class="string">&quot;你好，Java&quot;</span>);</span><br></pre></td></tr></table></figure><p>有时编译没问题，但是运行后控制台显示的中文不正常。后来才知道，这和源文件编码、命令行窗口编码、<code>javac</code> 读取源码时使用的编码都有关系。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><h2 id="public类的类名必须和文件名完全一致"><a href="#public类的类名必须和文件名完全一致" class="headerlink" title="public类的类名必须和文件名完全一致"></a>public类的类名必须和文件名完全一致</h2><p>Java 要求一个 <code>.java</code> 文件中，如果存在 <code>public</code> 修饰的类，那么这个 <code>public</code> 类的类名必须和文件名完全一致，包括大小写。</p><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>文件名就必须是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">HelloWorld.java</span><br></pre></td></tr></table></figure><p>不能写成 <code>helloworld.java</code>、<code>hello.java</code> 或 <code>Hello.java</code>。虽然 Windows 文件名大小写不敏感，但 Java 编译器对类名是区分大小写的，初学的时候最好严格保持一致。</p><h2 id="JVM执行时寻找main方法作为程序入口"><a href="#JVM执行时寻找main方法作为程序入口" class="headerlink" title="JVM执行时寻找main方法作为程序入口"></a>JVM执行时寻找main方法作为程序入口</h2><p>Java 程序运行时，JVM 会寻找固定格式的 <code>main</code> 方法作为入口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span></span><br></pre></td></tr></table></figure><p>这里的几个关键点是：</p><ul><li><code>public</code>：表示 JVM 可以从类外部访问这个方法</li><li><code>static</code>：表示不需要创建对象，就可以直接调用这个方法</li><li><code>void</code>：表示这个方法没有返回值</li><li><code>main</code>：固定的方法名，是程序入口</li><li><code>String[] args</code>：命令行参数，运行程序时可以从外部传入字符串数组</li></ul><p>如果少写了 <code>static</code>，或者把 <code>main</code> 写成 <code>Main</code>，都可能导致 JVM 找不到程序入口。</p><h2 id="Java先编译成字节码再由JVM执行"><a href="#Java先编译成字节码再由JVM执行" class="headerlink" title="Java先编译成字节码再由JVM执行"></a>Java先编译成字节码再由JVM执行</h2><p>Java 的执行流程大致是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">HelloWorld.java  --javac编译--&gt;  HelloWorld.class  --JVM执行--&gt;  程序输出结果</span><br></pre></td></tr></table></figure><p>这里的 <code>.class</code> 文件不是普通文本文件，而是 Java 字节码文件。JVM 可以读取字节码，然后在不同操作系统上解释执行或即时编译执行。</p><p>这也是 Java 经常说的“一次编译，到处运行”的基础：只要目标机器安装了合适的 JVM，同一份 <code>.class</code> 字节码理论上就可以在不同平台运行。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20class%20file%20bytecode%20executed%20by%20JVM%20on%20Windows%20Linux%20and%20Mac,%20educational%20diagram&image_size=landscape_4_3" alt="class文件和JVM关系"><br><em>配图说明：class字节码文件通过JVM在不同平台运行</em></p><h2 id="Windows默认GBK编码可能导致中文乱码"><a href="#Windows默认GBK编码可能导致中文乱码" class="headerlink" title="Windows默认GBK编码可能导致中文乱码"></a>Windows默认GBK编码可能导致中文乱码</h2><p>我现在主要在 Windows 环境下学习 Java。Windows 命令行默认编码经常是 GBK，而很多编辑器保存源码时默认是 UTF-8。</p><p>如果源文件实际是 UTF-8，但 <code>javac</code> 按系统默认编码去读取，或者控制台用另一种编码显示，就可能出现中文乱码。最稳妥的方式是明确告诉 <code>javac</code> 源文件编码：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">javac -encoding UTF-8 HelloWorld.java</span><br></pre></td></tr></table></figure><p>这样至少编译阶段不会因为编码识别错误导致中文出问题。</p><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="完整的HelloWorld-java代码及逐行解释"><a href="#完整的HelloWorld-java代码及逐行解释" class="headerlink" title="完整的HelloWorld.java代码及逐行解释"></a>完整的HelloWorld.java代码及逐行解释</h2><p>下面是我整理后的第一个 Java 程序，文件名必须保存为 <code>HelloWorld.java</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123; <span class="comment">// 定义一个公共类，类名必须和文件名 HelloWorld.java 完全一致</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123; <span class="comment">// Java程序入口，JVM运行程序时会从这个方法开始执行</span></span><br><span class="line">        System.out.println(<span class="string">&quot;Hello World&quot;</span>); <span class="comment">// 向控制台输出一行文本，输出结束后自动换行</span></span><br><span class="line">        System.out.println(<span class="string">&quot;你好，Java&quot;</span>); <span class="comment">// 输出中文内容，用来测试当前编码是否正常</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>逐行理解：</p><ol><li><code>public class HelloWorld</code>：声明一个公共类，类名是 <code>HelloWorld</code>。</li><li><code>&#123;</code> 和 <code>&#125;</code>：表示代码块的开始和结束，类和方法都需要用大括号包起来。</li><li><code>public static void main(String[] args)</code>：固定格式的主方法，是 Java 程序入口。</li><li><code>System.out.println(&quot;Hello World&quot;)</code>：在控制台打印 <code>Hello World</code>。</li><li>每条语句末尾的分号不能省略，这是 Java 的语法要求。</li></ol><h2 id="javac编译和java运行的完整命令"><a href="#javac编译和java运行的完整命令" class="headerlink" title="javac编译和java运行的完整命令"></a>javac编译和java运行的完整命令</h2><p>假设当前目录下有 <code>HelloWorld.java</code> 文件，先打开命令行进入这个目录。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> D:\java-study</span><br></pre></td></tr></table></figure><p>编译 Java 源文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">javac HelloWorld.java</span><br></pre></td></tr></table></figure><p>如果编译成功，命令行通常不会输出任何内容，但是目录里会多出一个文件：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">HelloWorld.class</span><br></pre></td></tr></table></figure><p>然后运行程序：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java HelloWorld</span><br></pre></td></tr></table></figure><p>注意这里运行时写的是类名 <code>HelloWorld</code>，不要写成 <code>HelloWorld.class</code>，也不要写成 <code>HelloWorld.java</code>。</p><p>正常输出如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Hello World</span><br><span class="line">你好，Java</span><br></pre></td></tr></table></figure><p>如果担心中文编码问题，可以这样编译：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">javac -encoding UTF-8 HelloWorld.java</span><br><span class="line">java HelloWorld</span><br></pre></td></tr></table></figure><p>对应输出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Hello World</span><br><span class="line">你好，Java</span><br></pre></td></tr></table></figure><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Windows%20command%20prompt%20running%20javac%20HelloWorld.java%20and%20java%20HelloWorld%20with%20Hello%20World%20output,%202018%20beginner%20study%20note%20style&image_size=landscape_4_3" alt="命令行编译运行HelloWorld"><br><em>配图说明：在命令行中编译并运行HelloWorld程序</em></p><h2 id="带命令行参数的版本"><a href="#带命令行参数的版本" class="headerlink" title="带命令行参数的版本"></a>带命令行参数的版本</h2><p><code>main</code> 方法里的 <code>String[] args</code> 可以接收命令行参数。为了理解它，我又写了一个版本：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123; <span class="comment">// 定义HelloWorld类，文件名仍然必须是HelloWorld.java</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123; <span class="comment">// args用于接收运行程序时传入的命令行参数</span></span><br><span class="line">        <span class="keyword">if</span> (args.length == <span class="number">0</span>) &#123; <span class="comment">// 判断是否没有传入任何参数，避免直接访问数组导致异常</span></span><br><span class="line">            System.out.println(<span class="string">&quot;Hello World&quot;</span>); <span class="comment">// 没有参数时输出默认内容</span></span><br><span class="line">            <span class="keyword">return</span>; <span class="comment">// 提前结束main方法，让后面的参数输出逻辑不再执行</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;收到的第一个参数是：&quot;</span> + args[<span class="number">0</span>]); <span class="comment">// 输出命令行传入的第一个参数</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">javac -encoding UTF-8 HelloWorld.java</span><br></pre></td></tr></table></figure><p>不带参数运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java HelloWorld</span><br></pre></td></tr></table></figure><p>输出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Hello World</span><br></pre></td></tr></table></figure><p>带参数运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java HelloWorld Java入门</span><br></pre></td></tr></table></figure><p>输出：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">收到的第一个参数是：Java入门</span><br></pre></td></tr></table></figure><p>如果传入多个参数，<code>args[0]</code> 是第一个，<code>args[1]</code> 是第二个。这个地方让我想到 C 语言里的 <code>argc</code> 和 <code>argv</code>，只是 Java 把参数封装成了字符串数组。</p><h2 id="使用Eclipse创建Java项目的步骤"><a href="#使用Eclipse创建Java项目的步骤" class="headerlink" title="使用Eclipse创建Java项目的步骤"></a>使用Eclipse创建Java项目的步骤</h2><p>手动编译运行理解清楚以后，也可以用 Eclipse 创建项目。Eclipse 会自动帮我们调用编译器，很多目录结构和 classpath 细节也会帮忙处理。</p><p>大致步骤如下：</p><ol><li>打开 Eclipse。</li><li>选择 <code>File</code> -&gt; <code>New</code> -&gt; <code>Java Project</code>。</li><li>在 <code>Project name</code> 中输入项目名，例如 <code>HelloWorldDemo</code>。</li><li>选择已经安装好的 JDK，初学阶段默认配置即可。</li><li>点击 <code>Finish</code> 创建项目。</li><li>在 <code>src</code> 目录上右键，选择 <code>New</code> -&gt; <code>Class</code>。</li><li>在 <code>Name</code> 中输入 <code>HelloWorld</code>。</li><li>勾选 <code>public static void main(String[] args)</code>，让 Eclipse 自动生成主方法。</li><li>在 <code>main</code> 方法中写入输出语句。</li><li>右键代码区域，选择 <code>Run As</code> -&gt; <code>Java Application</code> 运行程序。</li></ol><p>Eclipse 里生成的代码大概是：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123; <span class="comment">// Eclipse创建的公共类，类名和文件名保持一致</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123; <span class="comment">// 勾选主方法选项后，Eclipse会自动生成程序入口</span></span><br><span class="line">        System.out.println(<span class="string">&quot;Hello World&quot;</span>); <span class="comment">// 在控制台输出Hello World</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Eclipse%20IDE%20creating%20a%20Java%20project%20HelloWorldDemo%20with%20src%20folder%20and%20HelloWorld.java,%20beginner%20tutorial%20screenshot%20style&image_size=landscape_4_3" alt="Eclipse创建Java项目"><br><em>配图说明：使用Eclipse创建并运行Java项目</em></p><h2 id="classpath的概念"><a href="#classpath的概念" class="headerlink" title="classpath的概念"></a>classpath的概念</h2><p><code>classpath</code> 可以简单理解为 JVM 查找 <code>.class</code> 文件的路径。</p><p>我们执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java HelloWorld</span><br></pre></td></tr></table></figure><p>JVM 会根据 classpath 去找 <code>HelloWorld.class</code>。如果当前目录不在 classpath 中，就可能出现找不到类的错误。</p><p>在简单练习时，可以在 <code>.class</code> 文件所在目录执行命令，或者显式指定 classpath：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java -classpath . HelloWorld</span><br></pre></td></tr></table></figure><p>这里的 <code>.</code> 表示当前目录。也可以写成简写形式：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java -<span class="built_in">cp</span> . HelloWorld</span><br></pre></td></tr></table></figure><p>如果 class 文件放在 <code>bin</code> 目录下，可以这样运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java -<span class="built_in">cp</span> bin HelloWorld</span><br></pre></td></tr></table></figure><p>初学阶段我觉得没必要一开始就背很多 classpath 的复杂规则，但至少要知道：运行 Java 程序时，JVM 必须能在 classpath 指定的位置找到对应的 <code>.class</code> 文件。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Java%20classpath%20concept%20diagram%20showing%20JVM%20searching%20current%20directory%20and%20bin%20folder%20for%20HelloWorld.class,%20clean%20educational%20style&image_size=landscape_4_3" alt="classpath查找class文件"><br><em>配图说明：JVM根据classpath查找class文件</em></p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>今天这个 <code>Hello World</code> 程序虽然很小，但实际把 Java 的几个基础概念都串起来了。</p><p>首先，Java 的编译运行机制和 C 语言有本质区别。C 语言通常直接编译成本机平台的机器码，而 Java 是先通过 <code>javac</code> 编译成 <code>.class</code> 字节码，再交给 JVM 运行。</p><p>其次，写 Java 程序时要注意文件名、类名、编码的一致性。尤其是 <code>public class HelloWorld</code> 必须放在 <code>HelloWorld.java</code> 中，中文源码最好统一使用 UTF-8，并在命令行编译时加上：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">javac -encoding UTF-8 HelloWorld.java</span><br></pre></td></tr></table></figure><p>再次，<code>main</code> 方法是 Java 程序入口，签名最好先按固定格式记住：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span></span><br></pre></td></tr></table></figure><p>最后，我对“一次编译，到处运行”有了更具体的理解。它不是说 Java 源文件可以直接在任何地方运行，而是说 Java 源文件编译成字节码后，可以交给不同平台上的 JVM 执行。</p><p>IDE 确实能帮我们屏蔽很多细节，比如自动编译、自动设置 classpath、自动生成 main 方法。但是我觉得初学者还是应该先用命令行手动编译运行几次。这样以后 Eclipse 或其他工具报错时，不至于完全不知道底层发生了什么。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;今天把 JDK 和开发工具都装好以后，终于开始写第一个 Java 程序了。虽然只是最简单的 &lt;code&gt;Hello World&lt;/code&gt;</summary>
      
    
    
    
    <category term="Java基础" scheme="https://swagch.github.io/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="HelloWorld" scheme="https://swagch.github.io/tags/HelloWorld/"/>
    
    <category term="入门" scheme="https://swagch.github.io/tags/%E5%85%A5%E9%97%A8/"/>
    
    <category term="编译运行" scheme="https://swagch.github.io/tags/%E7%BC%96%E8%AF%91%E8%BF%90%E8%A1%8C/"/>
    
  </entry>
  
  <entry>
    <title>Java开发环境搭建记录</title>
    <link href="https://swagch.github.io/2018/02/13/2018-02-13-Java%E5%BC%80%E5%8F%91%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA%E8%AE%B0%E5%BD%95/"/>
    <id>https://swagch.github.io/2018/02/13/2018-02-13-Java%E5%BC%80%E5%8F%91%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA%E8%AE%B0%E5%BD%95/</id>
    <published>2018-02-13T13:36:12.000Z</published>
    <updated>2026-06-09T08:05:21.998Z</updated>
    
    <content type="html"><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>2018 年春节回来，我下定决心从 C 语言转向 Java 后端开发。上学时学过一点 C 语言，顶多写过一些控制台排序算法，对”企业级开发”完全没概念。周围同学有搞 PHP 的，有搞 Python 的，选 Java 主要是看中了它的就业面广、生态成熟。</p><p>当时手头参考资料主要是两样：<strong>廖雪峰的 Java 教程</strong>（在线版）和 <strong>《Head First Java》</strong> 这本经典入门书。廖雪峰的教程偏实战，直接讲 Eclipse 怎么配、Maven 怎么用，而 Head First 用大量插图讲面向对象思想，两本搭配着看效果不错。</p><p>因为之前只用过 Code::Blocks 写 C 语言，对 JDK、JRE、IDE 这些概念完全分不清，所以第一步搭建环境就折腾了好几天。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Oracle%20JDK%20download%20page%20showing%20JDK%208%20installation%20packages,%20browser%20screenshot%20style&image_size=landscape_4_3" alt="JDK下载页面"><br><em>Oracle 官网 JDK 8 下载页面</em></p><h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><h3 id="1-JDK-版本选择困难"><a href="#1-JDK-版本选择困难" class="headerlink" title="1. JDK 版本选择困难"></a>1. JDK 版本选择困难</h3><p>当时 Oracle 在 2017 年 9 月刚发布了 JDK 9，官网上最显眼的位置就是 JDK 9 的下载链接。JDK 8 要往下翻半天才能找到。作为新手真的纠结了很久——装新不装旧？但是网上搜了一圈，几乎所有教程都在说 JDK 8 是 LTS（长期支持版本），更稳定，企业里用的最多。廖雪峰的教程里也明确写了建议用 JDK 8。最后还是选了 JDK 8u161。</p><h3 id="2-环境变量配置后还是找不到-java-命令"><a href="#2-环境变量配置后还是找不到-java-命令" class="headerlink" title="2. 环境变量配置后还是找不到 java 命令"></a>2. 环境变量配置后还是找不到 java 命令</h3><p>这是我踩的第一个坑。按照教程配置了 <code>JAVA_HOME</code>、<code>Path</code>、<code>CLASSPATH</code>，打开新命令行窗口输入 <code>java -version</code>，结果报：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&#x27;java&#x27; 不是内部或外部命令，也不是可运行的程序或批处理文件。</span><br></pre></td></tr></table></figure><p>当时心态有点崩，明明按照步骤一步一步来的啊。来来回回检查了好几遍才发现问题。</p><h3 id="3-Eclipse-启动报错"><a href="#3-Eclipse-启动报错" class="headerlink" title="3. Eclipse 启动报错"></a>3. Eclipse 启动报错</h3><p>装好 JDK 后下载了 Eclipse Oxygen（当时的最新版），结果双击启动图标直接弹出一个错误对话框：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Failed to create the Java Virtual Machine</span><br></pre></td></tr></table></figure><p>一脸懵，刚装好的环境怎么就起不来？</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Eclipse%20IDE%20error%20dialog%20showing%20Failed%20to%20create%20the%20Java%20Virtual%20Machine%20on%20Windows%2010%20desktop&image_size=landscape_4_3" alt="Eclipse JVM 错误弹窗"><br><em>Eclipse 启动时提示 JVM 创建失败</em></p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>回过头来分析这几个问题，其实都不复杂，但对新手来说每个都是拦路虎。</p><h3 id="JDK-版本"><a href="#JDK-版本" class="headerlink" title="JDK 版本"></a>JDK 版本</h3><p>JDK 9 引入了模块化系统（Jigsaw），算是一次比较大的架构变动。虽然新特性很吸引人，但 LTS 版本更稳妥这个道理现在懂了。JDK 8 一直到 2018 年初都还是绝对的主流，公司项目基本都在用，遇到问题网上也更容易找到解决方案。</p><h3 id="环境变量问题"><a href="#环境变量问题" class="headerlink" title="环境变量问题"></a>环境变量问题</h3><p>仔细排查后发现是犯了两个低级错误：</p><ol><li><p><strong>路径多了尾部斜杠</strong>：配置 <code>JAVA_HOME</code> 时写成了 <code>C:\Program Files\Java\jdk1.8.0_161\</code>（多了反斜杠），导致 <code>%JAVA_HOME%\bin</code> 解析出来变成了 <code>C:\Program Files\Java\jdk1.8.0_161\\bin</code>，双斜杠导致路径无效。</p></li><li><p><strong>配置后没重新打开命令行</strong>：环境变量改了之后，已经打开的命令行窗口不会自动刷新，需要关掉重新打开。</p></li></ol><h3 id="Eclipse-JVM-参数"><a href="#Eclipse-JVM-参数" class="headerlink" title="Eclipse JVM 参数"></a>Eclipse JVM 参数</h3><p>打开 Eclipse 安装目录下的 <code>eclipse.ini</code> 看了下，默认配置是这样的：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-Xms256m</span><br><span class="line">-Xmx1024m</span><br></pre></td></tr></table></figure><p>我的笔记本当时只有 4GB 内存，系统和其他软件占掉一大半，Eclipse 默认的 <code>-Xmx1024m</code>（最大堆内存 1024MB）对这台机器来说太大了，JVM 启动时分配不到足够的连续内存空间，所以直接崩了。</p><h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h3 id="第一步：安装-JDK-8u161"><a href="#第一步：安装-JDK-8u161" class="headerlink" title="第一步：安装 JDK 8u161"></a>第一步：安装 JDK 8u161</h3><p>从 Oracle 官网下载了 <code>jdk-8u161-windows-x64.exe</code>，双击安装，安装路径选择默认的 <code>C:\Program Files\Java\jdk1.8.0_161</code>。安装过程中会同时安装 JRE，一路下一步就好。</p><h3 id="第二步：配置环境变量"><a href="#第二步：配置环境变量" class="headerlink" title="第二步：配置环境变量"></a>第二步：配置环境变量</h3><p>在 Windows 10 中按 <code>Win + R</code> 输入 <code>sysdm.cpl</code>，在”高级”选项卡里点击”环境变量”，分别配置以下三个变量：</p><h4 id="新建系统变量：JAVA-HOME"><a href="#新建系统变量：JAVA-HOME" class="headerlink" title="新建系统变量：JAVA_HOME"></a>新建系统变量：JAVA_HOME</h4><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">变量名：JAVA_HOME</span><br><span class="line">变量值：C:\Program Files\Java\jdk1.8.0_161</span><br></pre></td></tr></table></figure><h4 id="编辑系统变量：Path"><a href="#编辑系统变量：Path" class="headerlink" title="编辑系统变量：Path"></a>编辑系统变量：Path</h4><p>在 Path 变量的值末尾追加（注意分号分隔）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">%JAVA_HOME%\bin</span><br></pre></td></tr></table></figure><h4 id="新建系统变量：CLASSPATH（可选）"><a href="#新建系统变量：CLASSPATH（可选）" class="headerlink" title="新建系统变量：CLASSPATH（可选）"></a>新建系统变量：CLASSPATH（可选）</h4><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">变量名：CLASSPATH</span><br><span class="line">变量值：.;%JAVA_HOME%\lib\dt.jar;%JAVA_HOME%\lib\tools.jar</span><br></pre></td></tr></table></figure><blockquote><p>注意前面那个点号 <code>.</code> 代表当前目录，必须要加。</p></blockquote><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Windows%2010%20environment%20variables%20settings%20window%20showing%20JAVA_HOME%20and%20Path%20configuration,%20system%20properties%20dialog&image_size=landscape_4_3" alt="环境变量配置界面"><br><em>Windows 10 环境变量配置窗口</em></p><h3 id="第三步：验证安装"><a href="#第三步：验证安装" class="headerlink" title="第三步：验证安装"></a>第三步：验证安装</h3><p>配置完成后，重新打开一个命令行窗口，依次输入以下命令验证：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">C:\Users\chenzixin&gt; java -version</span><br><span class="line">java version <span class="string">&quot;1.8.0_161&quot;</span></span><br><span class="line">Java(TM) SE Runtime Environment (build 1.8.0_161-b12)</span><br><span class="line">Java HotSpot(TM) 64-Bit Server VM (build 25.161-b12, mixed mode)</span><br><span class="line"></span><br><span class="line">C:\Users\chenzixin&gt; javac -version</span><br><span class="line">javac 1.8.0_161</span><br></pre></td></tr></table></figure><p>看到这个输出时心里踏实了不少，说明 JDK 安装和环境变量都生效了。</p><h3 id="第四步：编写第一个-Java-程序"><a href="#第四步：编写第一个-Java-程序" class="headerlink" title="第四步：编写第一个 Java 程序"></a>第四步：编写第一个 Java 程序</h3><p>在 <code>D:\JavaWorkspace</code> 目录下新建 <code>Test.java</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Test</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Hello Java! 环境搭建成功！&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;JDK 版本: &quot;</span> + System.getProperty(<span class="string">&quot;java.version&quot;</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在命令行中编译和运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">D:\JavaWorkspace&gt; javac Test.java</span><br><span class="line"></span><br><span class="line">D:\JavaWorkspace&gt; java Test</span><br><span class="line">Hello Java! 环境搭建成功！</span><br><span class="line">JDK 版本: 1.8.0_161</span><br></pre></td></tr></table></figure><p>第一个 Java 程序跑起来的那一刻，感觉之前折腾的这几天的辛苦都值了。</p><h3 id="第五步：安装-Eclipse-并调整-JVM-参数"><a href="#第五步：安装-Eclipse-并调整-JVM-参数" class="headerlink" title="第五步：安装 Eclipse 并调整 JVM 参数"></a>第五步：安装 Eclipse 并调整 JVM 参数</h3><p>下载 Eclipse IDE for Java Developers（Oxygen 版本），解压到 <code>D:\eclipse</code>。</p><p>打开 <code>D:\eclipse\eclipse.ini</code>，找到 JVM 参数相关配置，修改前：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-Xms256m</span><br><span class="line">-Xmx1024m</span><br></pre></td></tr></table></figure><p>修改后：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">-Xms256m</span><br><span class="line">-Xmx512m</span><br></pre></td></tr></table></figure><p>只改了一处——把 <code>-Xmx</code> 从 <code>1024m</code> 降到了 <code>512m</code>。改完后重启 Eclipse，顺利启动成功。</p><p><img src="https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Eclipse%20Oxygen%20IDE%20workspace%20welcome%20screen%20on%20Windows%2010,%20Java%20perspective&image_size=landscape_4_3" alt="Eclipse Oxygen 工作台界面"><br><em>Eclipse Oxygen 启动后的欢迎界面</em></p><h3 id="第六步：配置-Eclipse-的-Workspace"><a href="#第六步：配置-Eclipse-的-Workspace" class="headerlink" title="第六步：配置 Eclipse 的 Workspace"></a>第六步：配置 Eclipse 的 Workspace</h3><p>我选择了 <code>D:\JavaWorkspace</code> 作为工作空间目录，然后在 Eclipse 中配置已安装的 JDK：</p><ol><li>点击菜单 <code>Window</code> -&gt; <code>Preferences</code></li><li>展开 <code>Java</code> -&gt; <code>Installed JREs</code></li><li>确保指向的是 <code>C:\Program Files\Java\jdk1.8.0_161</code></li></ol><p>这样配置后，Eclipse 就自动识别了 JDK 的类库和编译选项，可以在 IDE 里直接创建和运行 Java 项目了。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次搭建环境的经历让我这个 Java 新手学到了不少东西：</p><ol><li><p><strong>JDK 选择 LTS 版本最稳妥</strong>。JDK 9 虽然新，但 JDK 8 作为 LTS 版本在企业中广泛使用，遇到问题更容易找到解决方案。对于初学者来说，稳定压倒一切。</p></li><li><p><strong>环境变量配置要仔细检查路径</strong>。多一个斜杠、少一个分号都可能导致配置不生效。配置完一定要重新打开命令行窗口再测试，这个坑后来再也没踩过。</p></li><li><p><strong>IDE 的 JVM 参数要根据机器配置调整</strong>。Eclipse 默认的 JVM 参数是为配置较高的机器准备的，2018 年主流笔记本还是 4GB~8GB 内存，默认的 1024m 对低配机器来说偏大。<code>eclipse.ini</code> 里的 <code>-Xmx</code> 参数控制最大堆内存，根据自己电脑的内存大小酌情调整就好。后来的 IntelliJ IDEA 也有类似问题，<code>idea64.exe.vmoptions</code> 里面的参数同样需要根据机器配置手动调整。</p></li><li><p><strong>建议新手先熟悉命令行编译运行再依赖 IDE</strong>。虽然 Eclipse 很方便，但用命令行编译运行能让人更好理解 Java 的编译原理——Java 源代码通过 <code>javac</code> 编译成字节码（<code>.class</code> 文件），再由 JVM 解释执行。理解了这套流程，后面学 Maven、Gradle 这些构建工具也会轻松很多。</p></li></ol><p>最后的最后，记录一下当时的主要软件版本，方便以后回顾：</p><table><thead><tr><th>软件</th><th>版本</th></tr></thead><tbody><tr><td>JDK</td><td>1.8.0_161 (build 1.8.0_161-b12)</td></tr><tr><td>Eclipse</td><td>Oxygen (4.7)</td></tr><tr><td>操作系统</td><td>Windows 10 专业版 64位</td></tr><tr><td>笔记本内存</td><td>4GB</td></tr></tbody></table><p>到现在我写这篇博客时，已经能用 Java 写一些简单的 Web 应用了。回头看看搭建环境这件事，其实没那么难，只是对一个新领域的未知让人感到畏惧而已。动手去做，遇到问题就查、就问、就试，总能解决。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;背景&quot;&gt;&lt;a href=&quot;#背景&quot; class=&quot;headerlink&quot; title=&quot;背景&quot;&gt;&lt;/a&gt;背景&lt;/h1&gt;&lt;p&gt;2018 年春节回来，我下定决心从 C 语言转向 Java 后端开发。上学时学过一点 C 语言，顶多写过一些控制台排序算法，对”企业级开发”</summary>
      
    
    
    
    <category term="Java基础" scheme="https://swagch.github.io/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
    <category term="Java" scheme="https://swagch.github.io/tags/Java/"/>
    
    <category term="JDK" scheme="https://swagch.github.io/tags/JDK/"/>
    
    <category term="环境配置" scheme="https://swagch.github.io/tags/%E7%8E%AF%E5%A2%83%E9%85%8D%E7%BD%AE/"/>
    
    <category term="Eclipse" scheme="https://swagch.github.io/tags/Eclipse/"/>
    
  </entry>
  
</feed>
