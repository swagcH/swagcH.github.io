<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Java开发环境搭建记录</title>
    <url>/2018/02/13/2018-02-13-Java%E5%BC%80%E5%8F%91%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>2018 年春节回来，我下定决心从 C 语言转向 Java 后端开发。上学时学过一点 C 语言，顶多写过一些控制台排序算法，对”企业级开发”完全没概念。周围同学有搞 PHP 的，有搞 Python 的，选 Java 主要是看中了它的就业面广、生态成熟。</p>
<p>当时手头参考资料主要是两样：<strong>廖雪峰的 Java 教程</strong>（在线版）和 <strong>《Head First Java》</strong> 这本经典入门书。廖雪峰的教程偏实战，直接讲 Eclipse 怎么配、Maven 怎么用，而 Head First 用大量插图讲面向对象思想，两本搭配着看效果不错。</p>
<p>因为之前只用过 Code::Blocks 写 C 语言，对 JDK、JRE、IDE 这些概念完全分不清，所以第一步搭建环境就折腾了好几天。</p>
<p><img src="/images/posts/2018/java-setup-1.svg" alt="JDK下载页面"><br><em>Oracle 官网 JDK 8 下载页面</em></p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><h3 id="1-JDK-版本选择困难"><a href="#1-JDK-版本选择困难" class="headerlink" title="1. JDK 版本选择困难"></a>1. JDK 版本选择困难</h3><p>当时 Oracle 在 2017 年 9 月刚发布了 JDK 9，官网上最显眼的位置就是 JDK 9 的下载链接。JDK 8 要往下翻半天才能找到。作为新手真的纠结了很久——装新不装旧？但是网上搜了一圈，几乎所有教程都在说 JDK 8 是 LTS（长期支持版本），更稳定，企业里用的最多。廖雪峰的教程里也明确写了建议用 JDK 8。最后还是选了 JDK 8u161。</p>
<h3 id="2-环境变量配置后还是找不到-java-命令"><a href="#2-环境变量配置后还是找不到-java-命令" class="headerlink" title="2. 环境变量配置后还是找不到 java 命令"></a>2. 环境变量配置后还是找不到 java 命令</h3><p>这是我踩的第一个坑。按照教程配置了 <code>JAVA_HOME</code>、<code>Path</code>、<code>CLASSPATH</code>，打开新命令行窗口输入 <code>java -version</code>，结果报：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#x27;java&#x27; 不是内部或外部命令，也不是可运行的程序或批处理文件。</span><br></pre></td></tr></table></figure>

<p>当时心态有点崩，明明按照步骤一步一步来的啊。来来回回检查了好几遍才发现问题。</p>
<h3 id="3-Eclipse-启动报错"><a href="#3-Eclipse-启动报错" class="headerlink" title="3. Eclipse 启动报错"></a>3. Eclipse 启动报错</h3><p>装好 JDK 后下载了 Eclipse Oxygen（当时的最新版），结果双击启动图标直接弹出一个错误对话框：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Failed to create the Java Virtual Machine</span><br></pre></td></tr></table></figure>

<p>一脸懵，刚装好的环境怎么就起不来？</p>
<p><em>Eclipse 启动时提示 JVM 创建失败</em></p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>回过头来分析这几个问题，其实都不复杂，但对新手来说每个都是拦路虎。</p>
<h3 id="JDK-版本"><a href="#JDK-版本" class="headerlink" title="JDK 版本"></a>JDK 版本</h3><p>JDK 9 引入了模块化系统（Jigsaw），算是一次比较大的架构变动。虽然新特性很吸引人，但 LTS 版本更稳妥这个道理现在懂了。JDK 8 一直到 2018 年初都还是绝对的主流，公司项目基本都在用，遇到问题网上也更容易找到解决方案。</p>
<h3 id="环境变量问题"><a href="#环境变量问题" class="headerlink" title="环境变量问题"></a>环境变量问题</h3><p>仔细排查后发现是犯了两个低级错误：</p>
<ol>
<li><p><strong>路径多了尾部斜杠</strong>：配置 <code>JAVA_HOME</code> 时写成了 <code>C:\Program Files\Java\jdk1.8.0_161\</code>（多了反斜杠），导致 <code>%JAVA_HOME%\bin</code> 解析出来变成了 <code>C:\Program Files\Java\jdk1.8.0_161\\bin</code>，双斜杠导致路径无效。</p>
</li>
<li><p><strong>配置后没重新打开命令行</strong>：环境变量改了之后，已经打开的命令行窗口不会自动刷新，需要关掉重新打开。</p>
</li>
</ol>
<h3 id="Eclipse-JVM-参数"><a href="#Eclipse-JVM-参数" class="headerlink" title="Eclipse JVM 参数"></a>Eclipse JVM 参数</h3><p>打开 Eclipse 安装目录下的 <code>eclipse.ini</code> 看了下，默认配置是这样的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-Xms256m</span><br><span class="line">-Xmx1024m</span><br></pre></td></tr></table></figure>

<p>我的笔记本当时只有 4GB 内存，系统和其他软件占掉一大半，Eclipse 默认的 <code>-Xmx1024m</code>（最大堆内存 1024MB）对这台机器来说太大了，JVM 启动时分配不到足够的连续内存空间，所以直接崩了。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h3 id="第一步：安装-JDK-8u161"><a href="#第一步：安装-JDK-8u161" class="headerlink" title="第一步：安装 JDK 8u161"></a>第一步：安装 JDK 8u161</h3><p>从 Oracle 官网下载了 <code>jdk-8u161-windows-x64.exe</code>，双击安装，安装路径选择默认的 <code>C:\Program Files\Java\jdk1.8.0_161</code>。安装过程中会同时安装 JRE，一路下一步就好。</p>
<h3 id="第二步：配置环境变量"><a href="#第二步：配置环境变量" class="headerlink" title="第二步：配置环境变量"></a>第二步：配置环境变量</h3><p>在 Windows 10 中按 <code>Win + R</code> 输入 <code>sysdm.cpl</code>，在”高级”选项卡里点击”环境变量”，分别配置以下三个变量：</p>
<h4 id="新建系统变量：JAVA-HOME"><a href="#新建系统变量：JAVA-HOME" class="headerlink" title="新建系统变量：JAVA_HOME"></a>新建系统变量：JAVA_HOME</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">变量名：JAVA_HOME</span><br><span class="line">变量值：C:\Program Files\Java\jdk1.8.0_161</span><br></pre></td></tr></table></figure>

<h4 id="编辑系统变量：Path"><a href="#编辑系统变量：Path" class="headerlink" title="编辑系统变量：Path"></a>编辑系统变量：Path</h4><p>在 Path 变量的值末尾追加（注意分号分隔）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">%JAVA_HOME%\bin</span><br></pre></td></tr></table></figure>

<h4 id="新建系统变量：CLASSPATH（可选）"><a href="#新建系统变量：CLASSPATH（可选）" class="headerlink" title="新建系统变量：CLASSPATH（可选）"></a>新建系统变量：CLASSPATH（可选）</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">变量名：CLASSPATH</span><br><span class="line">变量值：.;%JAVA_HOME%\lib\dt.jar;%JAVA_HOME%\lib\tools.jar</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注意前面那个点号 <code>.</code> 代表当前目录，必须要加。</p>
</blockquote>
<p><em>Windows 10 环境变量配置窗口</em></p>
<h3 id="第三步：验证安装"><a href="#第三步：验证安装" class="headerlink" title="第三步：验证安装"></a>第三步：验证安装</h3><p>配置完成后，重新打开一个命令行窗口，依次输入以下命令验证：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">C:\Users\chenzixin&gt; java -version</span><br><span class="line">java version <span class="string">&quot;1.8.0_161&quot;</span></span><br><span class="line">Java(TM) SE Runtime Environment (build 1.8.0_161-b12)</span><br><span class="line">Java HotSpot(TM) 64-Bit Server VM (build 25.161-b12, mixed mode)</span><br><span class="line"></span><br><span class="line">C:\Users\chenzixin&gt; javac -version</span><br><span class="line">javac 1.8.0_161</span><br></pre></td></tr></table></figure>

<p>看到这个输出时心里踏实了不少，说明 JDK 安装和环境变量都生效了。</p>
<h3 id="第四步：编写第一个-Java-程序"><a href="#第四步：编写第一个-Java-程序" class="headerlink" title="第四步：编写第一个 Java 程序"></a>第四步：编写第一个 Java 程序</h3><p>在 <code>D:\JavaWorkspace</code> 目录下新建 <code>Test.java</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Test</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Hello Java! 环境搭建成功！&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;JDK 版本: &quot;</span> + System.getProperty(<span class="string">&quot;java.version&quot;</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在命令行中编译和运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">D:\JavaWorkspace&gt; javac Test.java</span><br><span class="line"></span><br><span class="line">D:\JavaWorkspace&gt; java Test</span><br><span class="line">Hello Java! 环境搭建成功！</span><br><span class="line">JDK 版本: 1.8.0_161</span><br></pre></td></tr></table></figure>

<p>第一个 Java 程序跑起来的那一刻，感觉之前折腾的这几天的辛苦都值了。</p>
<h3 id="第五步：安装-Eclipse-并调整-JVM-参数"><a href="#第五步：安装-Eclipse-并调整-JVM-参数" class="headerlink" title="第五步：安装 Eclipse 并调整 JVM 参数"></a>第五步：安装 Eclipse 并调整 JVM 参数</h3><p>下载 Eclipse IDE for Java Developers（Oxygen 版本），解压到 <code>D:\eclipse</code>。</p>
<p>打开 <code>D:\eclipse\eclipse.ini</code>，找到 JVM 参数相关配置，修改前：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-Xms256m</span><br><span class="line">-Xmx1024m</span><br></pre></td></tr></table></figure>

<p>修改后：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-Xms256m</span><br><span class="line">-Xmx512m</span><br></pre></td></tr></table></figure>

<p>只改了一处——把 <code>-Xmx</code> 从 <code>1024m</code> 降到了 <code>512m</code>。改完后重启 Eclipse，顺利启动成功。</p>
<p><em>Eclipse Oxygen 启动后的欢迎界面</em></p>
<h3 id="第六步：配置-Eclipse-的-Workspace"><a href="#第六步：配置-Eclipse-的-Workspace" class="headerlink" title="第六步：配置 Eclipse 的 Workspace"></a>第六步：配置 Eclipse 的 Workspace</h3><p>我选择了 <code>D:\JavaWorkspace</code> 作为工作空间目录，然后在 Eclipse 中配置已安装的 JDK：</p>
<ol>
<li>点击菜单 <code>Window</code> -&gt; <code>Preferences</code></li>
<li>展开 <code>Java</code> -&gt; <code>Installed JREs</code></li>
<li>确保指向的是 <code>C:\Program Files\Java\jdk1.8.0_161</code></li>
</ol>
<p>这样配置后，Eclipse 就自动识别了 JDK 的类库和编译选项，可以在 IDE 里直接创建和运行 Java 项目了。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次搭建环境的经历让我这个 Java 新手学到了不少东西：</p>
<ol>
<li><p><strong>JDK 选择 LTS 版本最稳妥</strong>。JDK 9 虽然新，但 JDK 8 作为 LTS 版本在企业中广泛使用，遇到问题更容易找到解决方案。对于初学者来说，稳定压倒一切。</p>
</li>
<li><p><strong>环境变量配置要仔细检查路径</strong>。多一个斜杠、少一个分号都可能导致配置不生效。配置完一定要重新打开命令行窗口再测试，这个坑后来再也没踩过。</p>
</li>
<li><p><strong>IDE 的 JVM 参数要根据机器配置调整</strong>。Eclipse 默认的 JVM 参数是为配置较高的机器准备的，2018 年主流笔记本还是 4GB~8GB 内存，默认的 1024m 对低配机器来说偏大。<code>eclipse.ini</code> 里的 <code>-Xmx</code> 参数控制最大堆内存，根据自己电脑的内存大小酌情调整就好。后来的 IntelliJ IDEA 也有类似问题，<code>idea64.exe.vmoptions</code> 里面的参数同样需要根据机器配置手动调整。</p>
</li>
<li><p><strong>建议新手先熟悉命令行编译运行再依赖 IDE</strong>。虽然 Eclipse 很方便，但用命令行编译运行能让人更好理解 Java 的编译原理——Java 源代码通过 <code>javac</code> 编译成字节码（<code>.class</code> 文件），再由 JVM 解释执行。理解了这套流程，后面学 Maven、Gradle 这些构建工具也会轻松很多。</p>
</li>
</ol>
<p>最后的最后，记录一下当时的主要软件版本，方便以后回顾：</p>
<table>
<thead>
<tr>
<th>软件</th>
<th>版本</th>
</tr>
</thead>
<tbody><tr>
<td>JDK</td>
<td>1.8.0_161 (build 1.8.0_161-b12)</td>
</tr>
<tr>
<td>Eclipse</td>
<td>Oxygen (4.7)</td>
</tr>
<tr>
<td>操作系统</td>
<td>Windows 10 专业版 64位</td>
</tr>
<tr>
<td>笔记本内存</td>
<td>4GB</td>
</tr>
</tbody></table>
<p>到现在我写这篇博客时，已经能用 Java 写一些简单的 Web 应用了。回头看看搭建环境这件事，其实没那么难，只是对一个新领域的未知让人感到畏惧而已。动手去做，遇到问题就查、就问、就试，总能解决。</p>
]]></content>
      <categories>
        <category>Java基础</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>JDK</tag>
        <tag>环境配置</tag>
        <tag>Eclipse</tag>
      </tags>
  </entry>
  <entry>
    <title>第一个Hello World程序详解</title>
    <url>/2018/02/25/2018-02-25-%E7%AC%AC%E4%B8%80%E4%B8%AAHello-World%E7%A8%8B%E5%BA%8F%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>今天把 JDK 和开发工具都装好以后，终于开始写第一个 Java 程序了。虽然只是最简单的 <code>Hello World</code>，但是实际动手以后发现，这里面并不是只有一行输出语句那么简单。</p>
<p>我之前学 C 语言的时候，也是从 <code>Hello World</code> 开始的。当时大概知道写完 <code>.c</code> 文件以后，用编译器编译，再运行生成的可执行文件就可以了。所以刚开始学 Java 的时候，我也下意识以为流程差不多：写代码、编译、运行。</p>
<p>但是 Java 这里多了一个 <code>.class</code> 文件，还要用 <code>javac</code> 和 <code>java</code> 两个命令分别处理，一开始确实有点懵。于是我想把 Java 程序从编写到运行的完整过程搞清楚，至少以后再遇到编译错误、运行错误时，知道问题大概出在哪里。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>第一次写 Java 的 <code>Hello World</code>，我主要遇到了下面几个问题。</p>
<h2 id="文件名和类名不一致导致编译报错"><a href="#文件名和类名不一致导致编译报错" class="headerlink" title="文件名和类名不一致导致编译报错"></a>文件名和类名不一致导致编译报错</h2><p>我一开始把文件保存成了 <code>hello.java</code>，里面写的是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Hello World&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">javac hello.java</span><br></pre></td></tr></table></figure>

<p>结果编译器报错，大概意思是公共类 <code>HelloWorld</code> 应该放在 <code>HelloWorld.java</code> 文件中。这个错误很典型，因为 Java 对 <code>public class</code> 的类名和文件名要求非常严格。</p>
<h2 id="缺少main方法不知道程序入口在哪"><a href="#缺少main方法不知道程序入口在哪" class="headerlink" title="缺少main方法不知道程序入口在哪"></a>缺少main方法不知道程序入口在哪</h2><p>我还尝试过只写一个普通方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sayHello</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Hello World&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码能编译通过，但是运行时会提示找不到 <code>main</code> 方法。也就是说，Java 程序不是只要有类就能直接运行，JVM 需要知道从哪里开始执行。</p>
<h2 id="不理解为什么javac编译后生成-class文件"><a href="#不理解为什么javac编译后生成-class文件" class="headerlink" title="不理解为什么javac编译后生成.class文件"></a>不理解为什么javac编译后生成.class文件</h2><p>执行 <code>javac HelloWorld.java</code> 后，目录里生成了 <code>HelloWorld.class</code>。我一开始以为编译后应该生成类似 Windows 下的 <code>.exe</code> 文件，但是 Java 生成的是字节码文件，这和 C 语言直接编译成机器码不同。</p>
<h2 id="中文输出乱码问题"><a href="#中文输出乱码问题" class="headerlink" title="中文输出乱码问题"></a>中文输出乱码问题</h2><p>我在程序里输出中文时，也遇到过乱码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">System.out.println(<span class="string">&quot;你好，Java&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>有时编译没问题，但是运行后控制台显示的中文不正常。后来才知道，这和源文件编码、命令行窗口编码、<code>javac</code> 读取源码时使用的编码都有关系。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><h2 id="public类的类名必须和文件名完全一致"><a href="#public类的类名必须和文件名完全一致" class="headerlink" title="public类的类名必须和文件名完全一致"></a>public类的类名必须和文件名完全一致</h2><p>Java 要求一个 <code>.java</code> 文件中，如果存在 <code>public</code> 修饰的类，那么这个 <code>public</code> 类的类名必须和文件名完全一致，包括大小写。</p>
<p>例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>文件名就必须是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">HelloWorld.java</span><br></pre></td></tr></table></figure>

<p>不能写成 <code>helloworld.java</code>、<code>hello.java</code> 或 <code>Hello.java</code>。虽然 Windows 文件名大小写不敏感，但 Java 编译器对类名是区分大小写的，初学的时候最好严格保持一致。</p>
<h2 id="JVM执行时寻找main方法作为程序入口"><a href="#JVM执行时寻找main方法作为程序入口" class="headerlink" title="JVM执行时寻找main方法作为程序入口"></a>JVM执行时寻找main方法作为程序入口</h2><p>Java 程序运行时，JVM 会寻找固定格式的 <code>main</code> 方法作为入口：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span></span><br></pre></td></tr></table></figure>

<p>这里的几个关键点是：</p>
<ul>
<li><code>public</code>：表示 JVM 可以从类外部访问这个方法</li>
<li><code>static</code>：表示不需要创建对象，就可以直接调用这个方法</li>
<li><code>void</code>：表示这个方法没有返回值</li>
<li><code>main</code>：固定的方法名，是程序入口</li>
<li><code>String[] args</code>：命令行参数，运行程序时可以从外部传入字符串数组</li>
</ul>
<p>如果少写了 <code>static</code>，或者把 <code>main</code> 写成 <code>Main</code>，都可能导致 JVM 找不到程序入口。</p>
<h2 id="Java先编译成字节码再由JVM执行"><a href="#Java先编译成字节码再由JVM执行" class="headerlink" title="Java先编译成字节码再由JVM执行"></a>Java先编译成字节码再由JVM执行</h2><p>Java 的执行流程大致是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">HelloWorld.java  --javac编译--&gt;  HelloWorld.class  --JVM执行--&gt;  程序输出结果</span><br></pre></td></tr></table></figure>

<p>这里的 <code>.class</code> 文件不是普通文本文件，而是 Java 字节码文件。JVM 可以读取字节码，然后在不同操作系统上解释执行或即时编译执行。</p>
<p>这也是 Java 经常说的“一次编译，到处运行”的基础：只要目标机器安装了合适的 JVM，同一份 <code>.class</code> 字节码理论上就可以在不同平台运行。</p>
<h2 id="Windows默认GBK编码可能导致中文乱码"><a href="#Windows默认GBK编码可能导致中文乱码" class="headerlink" title="Windows默认GBK编码可能导致中文乱码"></a>Windows默认GBK编码可能导致中文乱码</h2><p>我现在主要在 Windows 环境下学习 Java。Windows 命令行默认编码经常是 GBK，而很多编辑器保存源码时默认是 UTF-8。</p>
<p>如果源文件实际是 UTF-8，但 <code>javac</code> 按系统默认编码去读取，或者控制台用另一种编码显示，就可能出现中文乱码。最稳妥的方式是明确告诉 <code>javac</code> 源文件编码：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">javac -encoding UTF-8 HelloWorld.java</span><br></pre></td></tr></table></figure>

<p>这样至少编译阶段不会因为编码识别错误导致中文出问题。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="完整的HelloWorld-java代码及逐行解释"><a href="#完整的HelloWorld-java代码及逐行解释" class="headerlink" title="完整的HelloWorld.java代码及逐行解释"></a>完整的HelloWorld.java代码及逐行解释</h2><p>下面是我整理后的第一个 Java 程序，文件名必须保存为 <code>HelloWorld.java</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123; <span class="comment">// 定义一个公共类，类名必须和文件名 HelloWorld.java 完全一致</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123; <span class="comment">// Java程序入口，JVM运行程序时会从这个方法开始执行</span></span><br><span class="line">        System.out.println(<span class="string">&quot;Hello World&quot;</span>); <span class="comment">// 向控制台输出一行文本，输出结束后自动换行</span></span><br><span class="line">        System.out.println(<span class="string">&quot;你好，Java&quot;</span>); <span class="comment">// 输出中文内容，用来测试当前编码是否正常</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>逐行理解：</p>
<ol>
<li><code>public class HelloWorld</code>：声明一个公共类，类名是 <code>HelloWorld</code>。</li>
<li><code>&#123;</code> 和 <code>&#125;</code>：表示代码块的开始和结束，类和方法都需要用大括号包起来。</li>
<li><code>public static void main(String[] args)</code>：固定格式的主方法，是 Java 程序入口。</li>
<li><code>System.out.println(&quot;Hello World&quot;)</code>：在控制台打印 <code>Hello World</code>。</li>
<li>每条语句末尾的分号不能省略，这是 Java 的语法要求。</li>
</ol>
<h2 id="javac编译和java运行的完整命令"><a href="#javac编译和java运行的完整命令" class="headerlink" title="javac编译和java运行的完整命令"></a>javac编译和java运行的完整命令</h2><p>假设当前目录下有 <code>HelloWorld.java</code> 文件，先打开命令行进入这个目录。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> D:\java-study</span><br></pre></td></tr></table></figure>

<p>编译 Java 源文件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">javac HelloWorld.java</span><br></pre></td></tr></table></figure>

<p>如果编译成功，命令行通常不会输出任何内容，但是目录里会多出一个文件：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">HelloWorld.class</span><br></pre></td></tr></table></figure>

<p>然后运行程序：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java HelloWorld</span><br></pre></td></tr></table></figure>

<p>注意这里运行时写的是类名 <code>HelloWorld</code>，不要写成 <code>HelloWorld.class</code>，也不要写成 <code>HelloWorld.java</code>。</p>
<p>正常输出如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Hello World</span><br><span class="line">你好，Java</span><br></pre></td></tr></table></figure>

<p>如果担心中文编码问题，可以这样编译：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">javac -encoding UTF-8 HelloWorld.java</span><br><span class="line">java HelloWorld</span><br></pre></td></tr></table></figure>

<p>对应输出：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Hello World</span><br><span class="line">你好，Java</span><br></pre></td></tr></table></figure>


<h2 id="带命令行参数的版本"><a href="#带命令行参数的版本" class="headerlink" title="带命令行参数的版本"></a>带命令行参数的版本</h2><p><code>main</code> 方法里的 <code>String[] args</code> 可以接收命令行参数。为了理解它，我又写了一个版本：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123; <span class="comment">// 定义HelloWorld类，文件名仍然必须是HelloWorld.java</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123; <span class="comment">// args用于接收运行程序时传入的命令行参数</span></span><br><span class="line">        <span class="keyword">if</span> (args.length == <span class="number">0</span>) &#123; <span class="comment">// 判断是否没有传入任何参数，避免直接访问数组导致异常</span></span><br><span class="line">            System.out.println(<span class="string">&quot;Hello World&quot;</span>); <span class="comment">// 没有参数时输出默认内容</span></span><br><span class="line">            <span class="keyword">return</span>; <span class="comment">// 提前结束main方法，让后面的参数输出逻辑不再执行</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;收到的第一个参数是：&quot;</span> + args[<span class="number">0</span>]); <span class="comment">// 输出命令行传入的第一个参数</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>编译：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">javac -encoding UTF-8 HelloWorld.java</span><br></pre></td></tr></table></figure>

<p>不带参数运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java HelloWorld</span><br></pre></td></tr></table></figure>

<p>输出：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Hello World</span><br></pre></td></tr></table></figure>

<p>带参数运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java HelloWorld Java入门</span><br></pre></td></tr></table></figure>

<p>输出：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">收到的第一个参数是：Java入门</span><br></pre></td></tr></table></figure>

<p>如果传入多个参数，<code>args[0]</code> 是第一个，<code>args[1]</code> 是第二个。这个地方让我想到 C 语言里的 <code>argc</code> 和 <code>argv</code>，只是 Java 把参数封装成了字符串数组。</p>
<h2 id="使用Eclipse创建Java项目的步骤"><a href="#使用Eclipse创建Java项目的步骤" class="headerlink" title="使用Eclipse创建Java项目的步骤"></a>使用Eclipse创建Java项目的步骤</h2><p>手动编译运行理解清楚以后，也可以用 Eclipse 创建项目。Eclipse 会自动帮我们调用编译器，很多目录结构和 classpath 细节也会帮忙处理。</p>
<p>大致步骤如下：</p>
<ol>
<li>打开 Eclipse。</li>
<li>选择 <code>File</code> -&gt; <code>New</code> -&gt; <code>Java Project</code>。</li>
<li>在 <code>Project name</code> 中输入项目名，例如 <code>HelloWorldDemo</code>。</li>
<li>选择已经安装好的 JDK，初学阶段默认配置即可。</li>
<li>点击 <code>Finish</code> 创建项目。</li>
<li>在 <code>src</code> 目录上右键，选择 <code>New</code> -&gt; <code>Class</code>。</li>
<li>在 <code>Name</code> 中输入 <code>HelloWorld</code>。</li>
<li>勾选 <code>public static void main(String[] args)</code>，让 Eclipse 自动生成主方法。</li>
<li>在 <code>main</code> 方法中写入输出语句。</li>
<li>右键代码区域，选择 <code>Run As</code> -&gt; <code>Java Application</code> 运行程序。</li>
</ol>
<p>Eclipse 里生成的代码大概是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloWorld</span> &#123; <span class="comment">// Eclipse创建的公共类，类名和文件名保持一致</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123; <span class="comment">// 勾选主方法选项后，Eclipse会自动生成程序入口</span></span><br><span class="line">        System.out.println(<span class="string">&quot;Hello World&quot;</span>); <span class="comment">// 在控制台输出Hello World</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h2 id="classpath的概念"><a href="#classpath的概念" class="headerlink" title="classpath的概念"></a>classpath的概念</h2><p><code>classpath</code> 可以简单理解为 JVM 查找 <code>.class</code> 文件的路径。</p>
<p>我们执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java HelloWorld</span><br></pre></td></tr></table></figure>

<p>JVM 会根据 classpath 去找 <code>HelloWorld.class</code>。如果当前目录不在 classpath 中，就可能出现找不到类的错误。</p>
<p>在简单练习时，可以在 <code>.class</code> 文件所在目录执行命令，或者显式指定 classpath：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java -classpath . HelloWorld</span><br></pre></td></tr></table></figure>

<p>这里的 <code>.</code> 表示当前目录。也可以写成简写形式：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java -<span class="built_in">cp</span> . HelloWorld</span><br></pre></td></tr></table></figure>

<p>如果 class 文件放在 <code>bin</code> 目录下，可以这样运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java -<span class="built_in">cp</span> bin HelloWorld</span><br></pre></td></tr></table></figure>

<p>初学阶段我觉得没必要一开始就背很多 classpath 的复杂规则，但至少要知道：运行 Java 程序时，JVM 必须能在 classpath 指定的位置找到对应的 <code>.class</code> 文件。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>今天这个 <code>Hello World</code> 程序虽然很小，但实际把 Java 的几个基础概念都串起来了。</p>
<p>首先，Java 的编译运行机制和 C 语言有本质区别。C 语言通常直接编译成本机平台的机器码，而 Java 是先通过 <code>javac</code> 编译成 <code>.class</code> 字节码，再交给 JVM 运行。</p>
<p>其次，写 Java 程序时要注意文件名、类名、编码的一致性。尤其是 <code>public class HelloWorld</code> 必须放在 <code>HelloWorld.java</code> 中，中文源码最好统一使用 UTF-8，并在命令行编译时加上：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">javac -encoding UTF-8 HelloWorld.java</span><br></pre></td></tr></table></figure>

<p>再次，<code>main</code> 方法是 Java 程序入口，签名最好先按固定格式记住：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span></span><br></pre></td></tr></table></figure>

<p>最后，我对“一次编译，到处运行”有了更具体的理解。它不是说 Java 源文件可以直接在任何地方运行，而是说 Java 源文件编译成字节码后，可以交给不同平台上的 JVM 执行。</p>
<p>IDE 确实能帮我们屏蔽很多细节，比如自动编译、自动设置 classpath、自动生成 main 方法。但是我觉得初学者还是应该先用命令行手动编译运行几次。这样以后 Eclipse 或其他工具报错时，不至于完全不知道底层发生了什么。</p>
]]></content>
      <categories>
        <category>Java基础</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>HelloWorld</tag>
        <tag>入门</tag>
        <tag>编译运行</tag>
      </tags>
  </entry>
  <entry>
    <title>Java数据类型学习笔记</title>
    <url>/2018/04/08/2018-04-08-Java%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>学完第一个 <code>Hello World</code> 之后，我开始系统学习 Java 的基础语法。刚开始觉得数据类型应该不难，因为之前学 C 语言的时候也接触过 <code>int</code>、<code>char</code>、<code>float</code> 这些类型。</p>
<p>但是真正写 Java 代码时，我发现 Java 的类型系统比 C 语言更严格。变量必须先声明类型，类型之间不能随便混用，很多转换都需要明确写出来。</p>
<p>这几天练习的时候，我还遇到了两个很容易忽略的问题：一个是 <code>int</code> 运算溢出，另一个是 <code>float</code> 和 <code>double</code> 做小数计算时出现精度丢失。看起来只是基础知识，但如果不弄清楚，以后写业务代码肯定会踩坑。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>我目前遇到的问题主要有这几个：</p>
<ul>
<li>整数运算结果和预期不符（int溢出）</li>
<li>float和double计算金额时精度丢失</li>
<li>int和Integer傻傻分不清</li>
<li>自动类型转换和强制类型转换的规则记不住</li>
</ul>
<p>其中最让我困惑的是：明明代码没有报错，运行结果却是错的。比如两个很大的 <code>int</code> 相加，结果可能变成负数；两个小数相加，结果也可能不是我以为的 <code>0.3</code>。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>Java 里常用的整数类型是 <code>int</code>，但是 <code>int</code> 的范围并不是无限大，它的范围大约是 ±21 亿，也就是 <code>-2147483648</code> 到 <code>2147483647</code>。如果超过这个范围，就会发生溢出。大数运算应该使用 <code>long</code>，如果数字特别大，还需要使用 <code>BigInteger</code>。</p>
<p>浮点数的问题也比较典型。<code>float</code> 和 <code>double</code> 采用 IEEE 754 标准，它们在底层是用二进制表示小数的，所以无法精确表示某些十进制小数，比如 <code>0.1</code>、<code>0.2</code>。因此金额计算不能使用 <code>float</code> 或 <code>double</code>，应该使用 <code>BigDecimal</code>。</p>
<p>另外，<code>int</code> 是基本类型，而 <code>Integer</code> 是包装类。基本类型保存的是具体的数值，包装类是对象。Java 5 以后支持自动装箱和自动拆箱，所以平时写代码时它们看起来可以互相转换，但本质上不是一回事。</p>
<p>类型转换的规则可以简单理解为：小类型自动转大类型，大类型转小类型需要强制转换，而且可能丢失精度。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="8种基本类型的完整表格"><a href="#8种基本类型的完整表格" class="headerlink" title="8种基本类型的完整表格"></a>8种基本类型的完整表格</h2><p>Java 一共有 8 种基本类型，可以分成整数、浮点数、字符和布尔四类。</p>
<p><img src="/images/posts/2018/java-types-1.svg" alt="Java数据类型分类"></p>
<table>
<thead>
<tr>
<th>类型</th>
<th align="right">字节数</th>
<th>范围</th>
<th>默认值</th>
</tr>
</thead>
<tbody><tr>
<td>byte</td>
<td align="right">1</td>
<td>-128 ~ 127</td>
<td>0</td>
</tr>
<tr>
<td>short</td>
<td align="right">2</td>
<td>-32768 ~ 32767</td>
<td>0</td>
</tr>
<tr>
<td>int</td>
<td align="right">4</td>
<td>-2147483648 ~ 2147483647</td>
<td>0</td>
</tr>
<tr>
<td>long</td>
<td align="right">8</td>
<td>-9223372036854775808 ~ 9223372036854775807</td>
<td>0L</td>
</tr>
<tr>
<td>float</td>
<td align="right">4</td>
<td>约 ±3.4E38，有效位约 6~7 位</td>
<td>0.0f</td>
</tr>
<tr>
<td>double</td>
<td align="right">8</td>
<td>约 ±1.7E308，有效位约 15 位</td>
<td>0.0d</td>
</tr>
<tr>
<td>char</td>
<td align="right">2</td>
<td>0 ~ 65535</td>
<td>‘\u0000’</td>
</tr>
<tr>
<td>boolean</td>
<td align="right">1位或不确定，JVM未明确规定</td>
<td>true &#x2F; false</td>
<td>false</td>
</tr>
</tbody></table>
<p>下面是 8 种基本类型的声明方式和取值范围演示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">PrimitiveTypeDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">byte</span> <span class="variable">byteValue</span> <span class="operator">=</span> <span class="number">127</span>;</span><br><span class="line">        <span class="type">short</span> <span class="variable">shortValue</span> <span class="operator">=</span> <span class="number">32767</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">intValue</span> <span class="operator">=</span> <span class="number">2147483647</span>;</span><br><span class="line">        <span class="type">long</span> <span class="variable">longValue</span> <span class="operator">=</span> <span class="number">9223372036854775807L</span>;</span><br><span class="line">        <span class="type">float</span> <span class="variable">floatValue</span> <span class="operator">=</span> <span class="number">3.14F</span>;</span><br><span class="line">        <span class="type">double</span> <span class="variable">doubleValue</span> <span class="operator">=</span> <span class="number">3.141592653589793</span>;</span><br><span class="line">        <span class="type">char</span> <span class="variable">charValue</span> <span class="operator">=</span> <span class="string">&#x27;A&#x27;</span>;</span><br><span class="line">        <span class="type">boolean</span> <span class="variable">booleanValue</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;byte范围：&quot;</span> + Byte.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Byte.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;short范围：&quot;</span> + Short.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Short.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;int范围：&quot;</span> + Integer.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Integer.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;long范围：&quot;</span> + Long.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Long.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;float范围：&quot;</span> + Float.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Float.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;double范围：&quot;</span> + Double.MIN_VALUE + <span class="string">&quot; ~ &quot;</span> + Double.MAX_VALUE);</span><br><span class="line">        System.out.println(<span class="string">&quot;char示例：&quot;</span> + charValue);</span><br><span class="line">        System.out.println(<span class="string">&quot;boolean示例：&quot;</span> + booleanValue);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里要注意，<code>long</code> 类型的字面量后面最好加 <code>L</code>，<code>float</code> 类型的字面量后面要加 <code>F</code>，否则 Java 默认会把小数当成 <code>double</code>。</p>
<h2 id="int溢出演示代码"><a href="#int溢出演示代码" class="headerlink" title="int溢出演示代码"></a>int溢出演示代码</h2><p><code>int</code> 超过最大值后不会自动报错，而是会绕回负数，这一点刚开始看很反直觉。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IntOverflowDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">max</span> <span class="operator">=</span> Integer.MAX_VALUE;</span><br><span class="line">        <span class="type">int</span> <span class="variable">overflow</span> <span class="operator">=</span> max + <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;int最大值：&quot;</span> + max);</span><br><span class="line">        System.out.println(<span class="string">&quot;int最大值 + 1：&quot;</span> + overflow);</span><br><span class="line"></span><br><span class="line">        <span class="type">long</span> <span class="variable">correctResult</span> <span class="operator">=</span> (<span class="type">long</span>) max + <span class="number">1</span>;</span><br><span class="line">        System.out.println(<span class="string">&quot;转成long后再计算：&quot;</span> + correctResult);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出结果中，<code>Integer.MAX_VALUE + 1</code> 会变成 <code>Integer.MIN_VALUE</code>。所以如果参与运算的数字可能超过 <code>int</code> 范围，就要提前使用 <code>long</code>。</p>
<p>如果数字比 <code>long</code> 还大，就要考虑 <code>BigInteger</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.math.BigInteger;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BigIntegerDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">BigInteger</span> <span class="variable">a</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BigInteger</span>(<span class="string">&quot;999999999999999999999999999999&quot;</span>);</span><br><span class="line">        <span class="type">BigInteger</span> <span class="variable">b</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BigInteger</span>(<span class="string">&quot;888888888888888888888888888888&quot;</span>);</span><br><span class="line">        <span class="type">BigInteger</span> <span class="variable">sum</span> <span class="operator">=</span> a.add(b);</span><br><span class="line"></span><br><span class="line">        System.out.println(sum);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="float精度丢失代码"><a href="#float精度丢失代码" class="headerlink" title="float精度丢失代码"></a>float精度丢失代码</h2><p>下面这个例子是我印象很深的一个坑：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">FloatPrecisionDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">float</span> <span class="variable">a</span> <span class="operator">=</span> <span class="number">0.1F</span>;</span><br><span class="line">        <span class="type">float</span> <span class="variable">b</span> <span class="operator">=</span> <span class="number">0.2F</span>;</span><br><span class="line">        <span class="type">double</span> <span class="variable">c</span> <span class="operator">=</span> <span class="number">0.1</span>;</span><br><span class="line">        <span class="type">double</span> <span class="variable">d</span> <span class="operator">=</span> <span class="number">0.2</span>;</span><br><span class="line"></span><br><span class="line">        System.out.println(a + b);</span><br><span class="line">        System.out.println(c + d);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果不一定是准确的 <code>0.3</code>，因为二进制浮点数不能精确表示某些十进制小数。</p>
<h2 id="BigDecimal解决金额计算的完整示例"><a href="#BigDecimal解决金额计算的完整示例" class="headerlink" title="BigDecimal解决金额计算的完整示例"></a>BigDecimal解决金额计算的完整示例</h2><p>金额计算应该使用 <code>BigDecimal</code>，而且创建 <code>BigDecimal</code> 时尽量使用字符串，不要直接传入 <code>double</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.math.BigDecimal;</span><br><span class="line"><span class="keyword">import</span> java.math.RoundingMode;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BigDecimalMoneyDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">BigDecimal</span> <span class="variable">price</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BigDecimal</span>(<span class="string">&quot;19.90&quot;</span>);</span><br><span class="line">        <span class="type">BigDecimal</span> <span class="variable">count</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BigDecimal</span>(<span class="string">&quot;3&quot;</span>);</span><br><span class="line">        <span class="type">BigDecimal</span> <span class="variable">discount</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BigDecimal</span>(<span class="string">&quot;5.00&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="type">BigDecimal</span> <span class="variable">total</span> <span class="operator">=</span> price.multiply(count).subtract(discount);</span><br><span class="line">        <span class="type">BigDecimal</span> <span class="variable">average</span> <span class="operator">=</span> total.divide(count, <span class="number">2</span>, RoundingMode.HALF_UP);</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;总金额：&quot;</span> + total);</span><br><span class="line">        System.out.println(<span class="string">&quot;平均金额：&quot;</span> + average);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个例子里，<code>multiply</code> 是乘法，<code>subtract</code> 是减法，<code>divide</code> 是除法。除法一定要注意指定保留位数和舍入方式，否则遇到除不尽的情况可能会抛异常。</p>
<h2 id="引用类型和基本类型的区别对比"><a href="#引用类型和基本类型的区别对比" class="headerlink" title="引用类型和基本类型的区别对比"></a>引用类型和基本类型的区别对比</h2><p>Java 的数据类型可以分成基本类型和引用类型。</p>
<table>
<thead>
<tr>
<th>对比项</th>
<th>基本类型</th>
<th>引用类型</th>
</tr>
</thead>
<tbody><tr>
<td>保存内容</td>
<td>直接保存值</td>
<td>保存对象的引用地址</td>
</tr>
<tr>
<td>是否为对象</td>
<td>不是对象</td>
<td>是对象</td>
</tr>
<tr>
<td>默认值</td>
<td>例如 int 是 0，boolean 是 false</td>
<td>null</td>
</tr>
<tr>
<td>常见类型</td>
<td>byte、short、int、long、float、double、char、boolean</td>
<td>类、接口、数组、枚举、注解</td>
</tr>
<tr>
<td>方法调用</td>
<td>不能直接调用方法</td>
<td>可以调用对象方法</td>
</tr>
<tr>
<td>泛型使用</td>
<td>不能直接作为泛型类型</td>
<td>可以作为泛型类型</td>
</tr>
</tbody></table>
<p>比如 <code>int</code> 和 <code>Integer</code> 就是最常见的一组对比：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IntIntegerDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">primitiveValue</span> <span class="operator">=</span> <span class="number">10</span>;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">wrapperValue</span> <span class="operator">=</span> Integer.valueOf(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">        System.out.println(primitiveValue);</span><br><span class="line">        System.out.println(wrapperValue.toString());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>int</code> 只是一个数值，<code>Integer</code> 是一个对象，所以 <code>Integer</code> 可以调用方法，也可以放进集合里。</p>
<h2 id="自动类型转换规则图示"><a href="#自动类型转换规则图示" class="headerlink" title="自动类型转换规则图示"></a>自动类型转换规则图示</h2><p>Java 中常见的自动类型转换顺序可以记成下面这样：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">byte -&gt; short -&gt; int -&gt; long -&gt; float -&gt; double</span><br><span class="line">char -&gt; int -&gt; long -&gt; float -&gt; double</span><br></pre></td></tr></table></figure>


<p>小范围类型赋值给大范围类型时，可以自动转换：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AutoConvertDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">byte</span> <span class="variable">byteValue</span> <span class="operator">=</span> <span class="number">10</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">intValue</span> <span class="operator">=</span> byteValue;</span><br><span class="line">        <span class="type">long</span> <span class="variable">longValue</span> <span class="operator">=</span> intValue;</span><br><span class="line">        <span class="type">double</span> <span class="variable">doubleValue</span> <span class="operator">=</span> longValue;</span><br><span class="line"></span><br><span class="line">        <span class="type">char</span> <span class="variable">charValue</span> <span class="operator">=</span> <span class="string">&#x27;A&#x27;</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">charCode</span> <span class="operator">=</span> charValue;</span><br><span class="line"></span><br><span class="line">        System.out.println(doubleValue);</span><br><span class="line">        System.out.println(charCode);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里 <code>byte</code> 可以自动转成 <code>int</code>，<code>int</code> 可以自动转成 <code>long</code>，<code>long</code> 也可以自动转成 <code>double</code>。</p>
<h2 id="强制类型转换示例及注意事项"><a href="#强制类型转换示例及注意事项" class="headerlink" title="强制类型转换示例及注意事项"></a>强制类型转换示例及注意事项</h2><p>大范围类型转成小范围类型时，需要强制转换：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ForceConvertDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">double</span> <span class="variable">doubleValue</span> <span class="operator">=</span> <span class="number">3.99</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">intValue</span> <span class="operator">=</span> (<span class="type">int</span>) doubleValue;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">largeValue</span> <span class="operator">=</span> <span class="number">130</span>;</span><br><span class="line">        <span class="type">byte</span> <span class="variable">byteValue</span> <span class="operator">=</span> (<span class="type">byte</span>) largeValue;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;double转int：&quot;</span> + intValue);</span><br><span class="line">        System.out.println(<span class="string">&quot;int转byte：&quot;</span> + byteValue);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>double</code> 转成 <code>int</code> 后，小数部分会被直接截断，不会四舍五入。<code>130</code> 转成 <code>byte</code> 后也不是 <code>130</code>，因为 <code>byte</code> 的范围只有 <code>-128 ~ 127</code>，超出范围会发生溢出。</p>
<h2 id="自动装箱拆箱的示例及IntegerCache陷阱"><a href="#自动装箱拆箱的示例及IntegerCache陷阱" class="headerlink" title="自动装箱拆箱的示例及IntegerCache陷阱"></a>自动装箱拆箱的示例及IntegerCache陷阱</h2><p>自动装箱就是基本类型自动转成包装类，自动拆箱就是包装类自动转成基本类型。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BoxingDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">primitiveValue</span> <span class="operator">=</span> <span class="number">100</span>;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">boxedValue</span> <span class="operator">=</span> primitiveValue;</span><br><span class="line">        <span class="type">int</span> <span class="variable">unboxedValue</span> <span class="operator">=</span> boxedValue;</span><br><span class="line"></span><br><span class="line">        System.out.println(boxedValue);</span><br><span class="line">        System.out.println(unboxedValue);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是 <code>Integer</code> 有一个缓存范围，默认是 <code>-128 ~ 127</code>。这个地方很容易让人误以为 <code>==</code> 比较的是数值，其实对象之间的 <code>==</code> 比较的是引用地址。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IntegerCacheDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">a</span> <span class="operator">=</span> <span class="number">127</span>;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">b</span> <span class="operator">=</span> <span class="number">127</span>;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">c</span> <span class="operator">=</span> <span class="number">128</span>;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">d</span> <span class="operator">=</span> <span class="number">128</span>;</span><br><span class="line"></span><br><span class="line">        System.out.println(a == b);</span><br><span class="line">        System.out.println(c == d);</span><br><span class="line">        System.out.println(c.equals(d));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>a == b</code> 是 <code>true</code>，因为 <code>127</code> 在缓存范围内。<code>c == d</code> 是 <code>false</code>，因为 <code>128</code> 默认不在缓存范围内，会创建不同的对象。比较包装类的数值时，应该使用 <code>equals</code>，不要使用 <code>==</code>。</p>
<p>另外，自动拆箱时如果包装类是 <code>null</code>，会抛出空指针异常：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UnboxingNullDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Integer</span> <span class="variable">value</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> value;</span><br><span class="line"></span><br><span class="line">        System.out.println(result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码编译能通过，但运行时会报 <code>NullPointerException</code>。所以包装类拆箱前要特别注意是否为 <code>null</code>。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>学完这部分之后，我感觉 Java 的数据类型虽然看起来基础，但细节很多。尤其是类型范围、精度问题、包装类和类型转换，如果只靠直觉写代码，很容易出现隐藏 bug。</p>
<p>我的阶段性总结是：</p>
<ul>
<li>Java是强类型语言，类型安全比C更严格</li>
<li>金额计算永远不要用float&#x2F;double</li>
<li>注意基本类型和包装类的区别</li>
<li>类型转换要小心精度丢失</li>
</ul>
<p>以后写代码时，我会尽量先想清楚变量的范围和用途，再决定使用哪种类型。对于金额这种对精度要求很高的数据，一开始就应该使用 <code>BigDecimal</code>，不要等出问题后再改。</p>
]]></content>
      <categories>
        <category>Java基础</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>数据类型</tag>
        <tag>基本类型</tag>
        <tag>引用类型</tag>
        <tag>类型转换</tag>
      </tags>
  </entry>
  <entry>
    <title>面向对象编程理解</title>
    <url>/2018/05/14/2018-05-14-%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%E7%BC%96%E7%A8%8B%E7%90%86%E8%A7%A3/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>学完了Java的基础语法，终于进入了面向对象编程这块硬骨头。在此之前我一直用的是C语言写一些小项目，习惯了面向过程的思维方式——写一个函数，调用它，再写一个函数，再调用它，整个程序就是一系列函数的调用链。突然切换到面向对象，感觉脑子有点转不过弯来。</p>
<p>我记得《Head First Java》里面有一个很形象的类比：说类就像是饼干的模具，而对象就是用这个模具做出来的饼干。模具本身是一个概念上的模板，真正的饼干才是实在的东西。这个类比让我开始慢慢理解”对象”到底是个什么东西。</p>
<p>还特意去图书馆借了本《Java编程思想》，翻了几章感觉有点深，还是先老老实实把基础搞懂再说。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始学的时候，遇到了好多困惑：</p>
<p><strong>第一，类和对象的关系总是搞不清楚。</strong> 我知道类是用class关键字定义的，对象是用new创建出来的，但实际写代码的时候，完全不知道什么时候该定义类，一个类应该包含哪些东西。经常写着写着就把所有代码塞到一个类里面，跟写面向过程没什么区别。</p>
<p><strong>第二，封装的意义不理解。</strong> 刚接触private、public这些访问修饰符的时候，我就想：为什么要把字段设为private，然后还要写getter和setter去访问？这不脱裤子放屁吗？直接public不是更省事？</p>
<p><strong>第三，继承和组合不知道怎么选。</strong> 看了书上的例子说”猫是一种动物”可以用继承，”车有轮子”可以用组合。但实际写代码的时候，总是倾向于用继承，觉得extends一下什么都有了，方便。</p>
<p><strong>第四，多态太抽象了。</strong> 书上说”父类引用指向子类对象”，这句话我背都能背下来，但就是不知道这玩意儿到底有什么用，写代码的时候也完全想不起来用。</p>
<p><strong>第五，接口和抽象类看着差不多，不知道怎么选。</strong> 两个都不能实例化，都包含抽象方法，到底有啥区别？</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>随着写了一些小例子，慢慢对这些概念有了自己的理解。</p>
<p><strong>类和对象：</strong> 类就是一个模板&#x2F;蓝图，比如说”学生”这个概念，它有一些属性（姓名、学号、年龄）和行为（学习、考试）。而对象就是根据这个模板创建出来的具体实例，比如”张三”这个具体的学生。写代码的时候，应该先分析业务需求中有哪些实体，每个实体有什么属性和行为，再来定义类。</p>
<p><strong>封装：</strong> 现在理解了，封装的意义在于保护内部状态。如果不封装，外部代码可以随意修改对象的内部数据，可能会导致数据不一致。比如一个Student的age字段，如果直接暴露为public，外部可以给它赋一个-1这种不合理的值。通过setter方法，可以在赋值前做校验。</p>
<p><strong>继承与组合：</strong> “IS-A”关系适合用继承，”HAS-A”关系适合用组合。但继承的问题在于，父类的任何变化都会影响到所有子类，耦合度太高。现在很多书都推荐优先使用组合，组合更灵活，耦合度更低。</p>
<p><strong>多态：</strong> 多态的本质就在于父类引用指向子类对象，调用的方法却是子类重写后的版本。这样做的最大好处是：可以写通用的代码来处理不同的具体类型。比如定义一个统一的pay()方法，不管是学生交学费，还是员工发工资，都调用同一个方法名，具体怎么实现由子类自己决定。</p>
<p><strong>接口与抽象类：</strong> 接口定义的是行为契约——“你能做什么”；抽象类提供的部分实现——“你是什么”。接口强调的是能力，抽象类强调的是血缘关系。接口弥补了Java单继承的不足，一个类可以实现多个接口。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>光说不练假把式，写几个例子来巩固一下。</p>
<h3 id="封装示例：Student类"><a href="#封装示例：Student类" class="headerlink" title="封装示例：Student类"></a>封装示例：Student类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Student</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> String studentId;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> age;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> score;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Student</span><span class="params">(String name, String studentId, <span class="type">int</span> age)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        <span class="built_in">this</span>.studentId = studentId;</span><br><span class="line">        setAge(age); <span class="comment">// 通过setter方法赋值，享受校验逻辑</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getName</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getStudentId</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> studentId;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getAge</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setAge</span><span class="params">(<span class="type">int</span> age)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (age &lt; <span class="number">0</span> || age &gt; <span class="number">150</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;年龄不合法：&quot;</span> + age);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getScore</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> score;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setScore</span><span class="params">(<span class="type">double</span> score)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (score &lt; <span class="number">0</span> || score &gt; <span class="number">100</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;分数不合法：&quot;</span> + score);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">this</span>.score = score;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">study</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot;正在学习&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">takeExam</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot;正在考试&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>把字段设为private，通过public的getter&#x2F;setter访问，在setter里做合法性校验，这就是封装最直观的用法。</p>
<h3 id="继承示例：Person-Student-Teacher"><a href="#继承示例：Person-Student-Teacher" class="headerlink" title="继承示例：Person -&gt; Student &#x2F; Teacher"></a>继承示例：Person -&gt; Student &#x2F; Teacher</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="keyword">protected</span> String name;</span><br><span class="line">    <span class="keyword">protected</span> <span class="type">int</span> age;</span><br><span class="line">    <span class="keyword">protected</span> String gender;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Person</span><span class="params">(String name, <span class="type">int</span> age, String gender)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">        <span class="built_in">this</span>.gender = gender;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">introduce</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;我叫&quot;</span> + name + <span class="string">&quot;，今年&quot;</span> + age + <span class="string">&quot;岁&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 统一的行为接口，子类可以重写</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">pay</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot;支付了费用&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Student</span> <span class="keyword">extends</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String studentId;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> score;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Student</span><span class="params">(String name, <span class="type">int</span> age, String gender, String studentId)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(name, age, gender);</span><br><span class="line">        <span class="built_in">this</span>.studentId = studentId;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">pay</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;学生&quot;</span> + name + <span class="string">&quot;支付了学费5000元&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">study</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot;正在学习&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Teacher</span> <span class="keyword">extends</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String employeeId;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> salary;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Teacher</span><span class="params">(String name, <span class="type">int</span> age, String gender, String employeeId, <span class="type">double</span> salary)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(name, age, gender);</span><br><span class="line">        <span class="built_in">this</span>.employeeId = employeeId;</span><br><span class="line">        <span class="built_in">this</span>.salary = salary;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">pay</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;教师&quot;</span> + name + <span class="string">&quot;支付了工会费200元&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">teach</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot;正在上课&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Student和Teacher都是Person，所以用继承。它们都重写了pay()方法，这就是多态的基础。</p>
<h3 id="多态示例：用统一的pay-方法"><a href="#多态示例：用统一的pay-方法" class="headerlink" title="多态示例：用统一的pay()方法"></a>多态示例：用统一的pay()方法</h3><p>多态的好处，看下面这段代码就明白了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 创建一个Student和一个Teacher</span></span><br><span class="line">        <span class="type">Person</span> <span class="variable">stu</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Student</span>(<span class="string">&quot;张三&quot;</span>, <span class="number">18</span>, <span class="string">&quot;男&quot;</span>, <span class="string">&quot;S001&quot;</span>);</span><br><span class="line">        <span class="type">Person</span> <span class="variable">tch</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Teacher</span>(<span class="string">&quot;李老师&quot;</span>, <span class="number">35</span>, <span class="string">&quot;女&quot;</span>, <span class="string">&quot;T001&quot;</span>, <span class="number">8000</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 把它们放到同一个数组中</span></span><br><span class="line">        Person[] people = &#123;stu, tch&#125;;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 统一调用pay()方法，实际执行的是各自子类的版本</span></span><br><span class="line">        <span class="keyword">for</span> (Person p : people) &#123;</span><br><span class="line">            p.pay(); <span class="comment">// 多态！不需要判断具体类型</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">学生张三支付了学费5000元</span><br><span class="line">教师李老师支付了工会费200元</span><br></pre></td></tr></table></figure>

<p>看到没有？循环里写的是<code>p.pay()</code>，但实际执行的效果取决于p到底指向的是Student还是Teacher。如果不用多态，就得写一堆if-else来判断类型，扩展性差多了。</p>
<h3 id="接口示例：实现Comparable接口"><a href="#接口示例：实现Comparable接口" class="headerlink" title="接口示例：实现Comparable接口"></a>接口示例：实现Comparable接口</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Student</span> <span class="keyword">extends</span> <span class="title class_">Person</span> <span class="keyword">implements</span> <span class="title class_">Comparable</span>&lt;Student&gt; &#123;</span><br><span class="line">    <span class="comment">// ...其他代码同上...</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">compareTo</span><span class="params">(Student other)</span> &#123;</span><br><span class="line">        <span class="comment">// 按分数排序，分数高的排前面</span></span><br><span class="line">        <span class="keyword">return</span> Double.compare(other.getScore(), <span class="built_in">this</span>.getScore());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 排序用法</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;Student&gt; students = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        students.add(<span class="keyword">new</span> <span class="title class_">Student</span>(<span class="string">&quot;张三&quot;</span>, <span class="number">18</span>, <span class="string">&quot;男&quot;</span>, <span class="string">&quot;S001&quot;</span>));</span><br><span class="line">        students.add(<span class="keyword">new</span> <span class="title class_">Student</span>(<span class="string">&quot;李四&quot;</span>, <span class="number">19</span>, <span class="string">&quot;男&quot;</span>, <span class="string">&quot;S002&quot;</span>));</span><br><span class="line">        students.add(<span class="keyword">new</span> <span class="title class_">Student</span>(<span class="string">&quot;王五&quot;</span>, <span class="number">18</span>, <span class="string">&quot;女&quot;</span>, <span class="string">&quot;S003&quot;</span>));</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 给学生们设置分数</span></span><br><span class="line">        students.get(<span class="number">0</span>).setScore(<span class="number">85</span>);</span><br><span class="line">        students.get(<span class="number">1</span>).setScore(<span class="number">92</span>);</span><br><span class="line">        students.get(<span class="number">2</span>).setScore(<span class="number">78</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 调用Collections.sort()排序，底层会调用compareTo()方法</span></span><br><span class="line">        Collections.sort(students);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (Student s : students) &#123;</span><br><span class="line">            System.out.println(s.getName() + <span class="string">&quot;：&quot;</span> + s.getScore() + <span class="string">&quot;分&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>实现Comparable接口，就是告诉别人：我这个类的对象是可以比较大小的。这意味着你可以把这个类的对象放进TreeSet、用Collections.sort()排序等等。这就是面向接口编程的思想——依赖接口而不是具体实现。</p>
<h3 id="抽象类示例：Shape-Circle-Rectangle"><a href="#抽象类示例：Shape-Circle-Rectangle" class="headerlink" title="抽象类示例：Shape -&gt; Circle &#x2F; Rectangle"></a>抽象类示例：Shape -&gt; Circle &#x2F; Rectangle</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="keyword">protected</span> String color;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Shape</span><span class="params">(String color)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.color = color;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getColor</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> color;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 抽象方法：子类必须实现</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="type">double</span> <span class="title function_">getArea</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 抽象方法：子类必须实现</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="type">double</span> <span class="title function_">getPerimeter</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 具体方法：所有子类共享</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">display</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;颜色：&quot;</span> + color + <span class="string">&quot;，面积：&quot;</span> + getArea() + <span class="string">&quot;，周长：&quot;</span> + getPerimeter());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Circle</span> <span class="keyword">extends</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> radius;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Circle</span><span class="params">(String color, <span class="type">double</span> radius)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(color);</span><br><span class="line">        <span class="built_in">this</span>.radius = radius;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getArea</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> Math.PI * radius * radius;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getPerimeter</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">2</span> * Math.PI * radius;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Rectangle</span> <span class="keyword">extends</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> width;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">double</span> height;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Rectangle</span><span class="params">(String color, <span class="type">double</span> width, <span class="type">double</span> height)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(color);</span><br><span class="line">        <span class="built_in">this</span>.width = width;</span><br><span class="line">        <span class="built_in">this</span>.height = height;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getArea</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> width * height;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getPerimeter</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">2</span> * (width + height);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>抽象类Shape定义了形状该有的行为（求面积、求周长），但具体的计算方式交给子类去实现。display()方法是通用的，在抽象类里直接实现就好，子类不用重复写。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>折腾了两周，对面向对象总算有了个整体的认识。总结一下这几个星期的收获：</p>
<ol>
<li><p><strong>三大特性</strong>：封装、继承、多态。封装保护内部数据，继承实现代码复用，多态让代码更灵活。这三个东西是面向对象的基石，缺一不可。</p>
</li>
<li><p><strong>设计思路要转变</strong>：从面向过程的”先想步骤”变成面向对象的”先找对象”。写代码之前先分析业务中有哪些实体（类），它们有什么属性和行为，它们之间有什么关系。这个思维转换确实需要时间，但慢慢练习就会习惯。</p>
</li>
<li><p><strong>优先用组合</strong>：虽然继承用起来方便，但继承会让父类和子类耦合太紧。如果只是想要复用代码，组合往往是更好的选择。只有当确实是”IS-A”关系，并且行为需要多态时，才考虑继承。</p>
</li>
<li><p><strong>面向接口编程</strong>：接口定义契约，实现类去遵守契约。这样写出来的代码耦合度低，扩展性好。比如排序功能依赖Comparable接口而不是具体的类，以后想换排序规则，只要改compareTo()的实现就好了。</p>
</li>
</ol>
<p>面向对象的路才刚刚开始，后面还有设计模式等着我。不过有了这些基础，至少看代码的时候不会再一头雾水了。继续加油！</p>
]]></content>
      <categories>
        <category>Java基础</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>面向对象</tag>
        <tag>OOP</tag>
        <tag>封装</tag>
        <tag>继承</tag>
        <tag>多态</tag>
      </tags>
  </entry>
  <entry>
    <title>ArrayList与LinkedList区别</title>
    <url>/2018/06/10/2018-06-10-ArrayList%E4%B8%8ELinkedList%E5%8C%BA%E5%88%AB/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>学完 Java 面向对象之后，终于进入了集合框架这一章。说实话，刚接触 <code>List</code> 接口的时候我是懵的——<code>ArrayList</code> 和 <code>LinkedList</code> 都实现了 <code>List</code>，方法几乎一模一样，那到底该用哪个？</p>
<p>当时做课程作业，随手就 <code>new ArrayList&lt;&gt;()</code>，也没多想。直到有一次写一个模拟队列的功能，频繁在头部插入元素，程序跑得巨慢，我才意识到：<strong>选错 List 实现，性能差距可以差到几十倍</strong>。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>踩坑过程中，我遇到了三个核心问题：</p>
<p><strong>1. 随机访问和头插性能差异巨大</strong></p>
<p>同样的 10 万次操作，<code>ArrayList</code> 随机访问几乎瞬间完成，但头部插入慢到怀疑人生；而 <code>LinkedList</code> 头部插入飞快，随机访问却慢得离谱。我当时完全不理解为什么。</p>
<p><strong>2. 不知道什么场景该用哪个</strong></p>
<p>网上文章都说“随机访问多用 ArrayList，增删多用 LinkedList”，但这个“增删”到底指哪个位置的增删？尾部增删两者差不多啊！这种笼统的说法反而让我更困惑。</p>
<p><strong>3. 遍历删除元素时 ConcurrentModificationException</strong></p>
<p>这是最坑的。我想遍历 <code>List</code> 删除符合条件的元素，写了下面这种代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (String item : list) &#123;</span><br><span class="line">    <span class="keyword">if</span> (item.equals(<span class="string">&quot;deleteMe&quot;</span>)) &#123;</span><br><span class="line">        list.remove(item);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>结果直接抛了 <code>ConcurrentModificationException</code>，当时完全不知道为什么，也不知道怎么改。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>要真正搞懂这两个类的区别，必须从底层源码入手。</p>
<h2 id="ArrayList：底层是数组"><a href="#ArrayList：底层是数组" class="headerlink" title="ArrayList：底层是数组"></a>ArrayList：底层是数组</h2><p>翻开 <code>ArrayList</code> 的 JDK 源码，核心就是一个数组：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">transient</span> Object[] elementData;</span><br><span class="line"><span class="keyword">private</span> <span class="type">int</span> size;</span><br></pre></td></tr></table></figure>

<p><strong>随机访问为什么是 O(1)？</strong></p>
<p>因为数组在内存中是连续存储的，通过下标可以直接定位：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">get</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">    rangeCheck(index);</span><br><span class="line">    <span class="keyword">return</span> elementData(index);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">E <span class="title function_">elementData</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> (E) elementData[index];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段源码说明，<code>get(index)</code> 的核心操作就是 <code>elementData[index]</code>，所以随机访问时间复杂度是 O(1)。</p>
<p><strong>头部插入为什么是 O(n)？</strong></p>
<p>因为要在头部插入元素，需要把后面所有元素都往后挪一位：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">add</span><span class="params">(<span class="type">int</span> index, E element)</span> &#123;</span><br><span class="line">    rangeCheckForAdd(index);</span><br><span class="line">    ensureCapacityInternal(size + <span class="number">1</span>);</span><br><span class="line">    System.arraycopy(elementData, index, elementData, index + <span class="number">1</span>, size - index);</span><br><span class="line">    elementData[index] = element;</span><br><span class="line">    size++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关键就在 <code>System.arraycopy</code>。如果在索引 0 的位置插入，原来所有元素都要向后移动，元素越多越慢，所以头插是 O(n)。</p>
<p><strong>扩容机制也会带来额外成本</strong></p>
<p><code>ArrayList</code> 默认初始容量是 10，容量不够时会扩容为原来的 1.5 倍：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">grow</span><span class="params">(<span class="type">int</span> minCapacity)</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">oldCapacity</span> <span class="operator">=</span> elementData.length;</span><br><span class="line">    <span class="type">int</span> <span class="variable">newCapacity</span> <span class="operator">=</span> oldCapacity + (oldCapacity &gt;&gt; <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (newCapacity - minCapacity &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        newCapacity = minCapacity;</span><br><span class="line">    &#125;</span><br><span class="line">    elementData = Arrays.copyOf(elementData, newCapacity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>扩容时会复制旧数组，因此如果一开始就知道大概容量，最好使用 <code>new ArrayList&lt;&gt;(capacity)</code>，避免多次扩容。</p>
<h2 id="LinkedList：底层是双向链表"><a href="#LinkedList：底层是双向链表" class="headerlink" title="LinkedList：底层是双向链表"></a>LinkedList：底层是双向链表</h2><p><code>LinkedList</code> 的核心是 <code>Node</code> 节点，每个节点保存当前元素、前驱节点和后继节点：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Node</span>&lt;E&gt; &#123;</span><br><span class="line">    E item;</span><br><span class="line">    Node&lt;E&gt; next;</span><br><span class="line">    Node&lt;E&gt; prev;</span><br><span class="line"></span><br><span class="line">    Node(Node&lt;E&gt; prev, E element, Node&lt;E&gt; next) &#123;</span><br><span class="line">        <span class="built_in">this</span>.item = element;</span><br><span class="line">        <span class="built_in">this</span>.next = next;</span><br><span class="line">        <span class="built_in">this</span>.prev = prev;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>头部插入为什么是 O(1)？</strong></p>
<p>因为链表头插只需要修改几个引用，不需要移动已有元素：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addFirst</span><span class="params">(E e)</span> &#123;</span><br><span class="line">    linkFirst(e);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">linkFirst</span><span class="params">(E e)</span> &#123;</span><br><span class="line">    <span class="keyword">final</span> Node&lt;E&gt; f = first;</span><br><span class="line">    <span class="keyword">final</span> Node&lt;E&gt; newNode = <span class="keyword">new</span> <span class="title class_">Node</span>&lt;&gt;(<span class="literal">null</span>, e, f);</span><br><span class="line">    first = newNode;</span><br><span class="line">    <span class="keyword">if</span> (f == <span class="literal">null</span>) &#123;</span><br><span class="line">        last = newNode;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        f.prev = newNode;</span><br><span class="line">    &#125;</span><br><span class="line">    size++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个过程只调整 <code>first</code>、<code>prev</code> 等引用，和集合大小没有直接关系，所以是 O(1)。</p>
<p><strong>随机访问为什么是 O(n)？</strong></p>
<p>链表不像数组那样可以通过下标直接定位。即使知道 index，也必须从头或从尾一步一步走过去：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> E <span class="title function_">get</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">    checkElementIndex(index);</span><br><span class="line">    <span class="keyword">return</span> node(index).item;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Node&lt;E&gt; <span class="title function_">node</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (index &lt; (size &gt;&gt; <span class="number">1</span>)) &#123;</span><br><span class="line">        Node&lt;E&gt; x = first;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; index; i++) &#123;</span><br><span class="line">            x = x.next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        Node&lt;E&gt; x = last;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> size - <span class="number">1</span>; i &gt; index; i--) &#123;</span><br><span class="line">            x = x.prev;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>JDK 做了一个优化：如果 index 在前半部分，就从头开始找；如果在后半部分，就从尾开始找。但本质仍然是遍历，复杂度还是 O(n)。</p>
<h2 id="fail-fast-机制与-ConcurrentModificationException"><a href="#fail-fast-机制与-ConcurrentModificationException" class="headerlink" title="fail-fast 机制与 ConcurrentModificationException"></a>fail-fast 机制与 ConcurrentModificationException</h2><p>这个问题和 <code>ArrayList</code>、<code>LinkedList</code> 都有关，因为它们都使用了 <code>AbstractList</code> 中的 <code>modCount</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> <span class="keyword">transient</span> <span class="type">int</span> <span class="variable">modCount</span> <span class="operator">=</span> <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p>每次对集合做结构性修改，比如 <code>add</code>、<code>remove</code>，<code>modCount</code> 都会变化。迭代器创建时会记录一个 <code>expectedModCount</code>，遍历过程中会检查两者是否一致：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">checkForComodification</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (modCount != expectedModCount) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ConcurrentModificationException</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>所以用 for-each 遍历时调用 <code>list.remove()</code>，集合自己的 <code>modCount</code> 变了，但迭代器里的 <code>expectedModCount</code> 没有同步更新，就会触发 fail-fast 机制，抛出 <code>ConcurrentModificationException</code>。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="完整对比表格"><a href="#完整对比表格" class="headerlink" title="完整对比表格"></a>完整对比表格</h2><table>
<thead>
<tr>
<th>对比项</th>
<th>ArrayList</th>
<th>LinkedList</th>
</tr>
</thead>
<tbody><tr>
<td>底层数据结构</td>
<td>Object[] 数组</td>
<td>双向链表</td>
</tr>
<tr>
<td>随机访问 get(i)</td>
<td><strong>O(1)</strong></td>
<td>O(n)</td>
</tr>
<tr>
<td>头部插入 add(0, e)</td>
<td>O(n)</td>
<td><strong>O(1)</strong></td>
</tr>
<tr>
<td>尾部插入 add(e)</td>
<td>均摊 O(1)</td>
<td>O(1)</td>
</tr>
<tr>
<td>中间插入 add(i, e)</td>
<td>O(n)</td>
<td>O(n)</td>
</tr>
<tr>
<td>删除 remove(i)</td>
<td>O(n)</td>
<td>O(n)</td>
</tr>
<tr>
<td>内存占用</td>
<td>比较紧凑，但可能有预留空间</td>
<td>每个节点额外保存 prev、next 引用</td>
</tr>
<tr>
<td>是否支持 RandomAccess</td>
<td>支持</td>
<td>不支持</td>
</tr>
<tr>
<td>额外能力</td>
<td>主要作为 List 使用</td>
<td>同时可作为 Deque、Queue 使用</td>
</tr>
<tr>
<td>适用场景</td>
<td>读多写少、随机访问多、尾部追加多</td>
<td>频繁头部插入删除、队列或双端队列场景</td>
</tr>
</tbody></table>
<p>注意：<code>LinkedList</code> 的中间插入和删除，修改节点引用本身是 O(1)，但定位到目标节点需要 O(n)，所以整体仍然是 O(n)。</p>
<h2 id="ArrayList-LinkedList-基本操作"><a href="#ArrayList-LinkedList-基本操作" class="headerlink" title="ArrayList&#x2F;LinkedList 基本操作"></a>ArrayList&#x2F;LinkedList 基本操作</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ListBasicDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;String&gt; arrayList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        arrayList.add(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">        arrayList.add(<span class="string">&quot;Python&quot;</span>);</span><br><span class="line">        arrayList.add(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line">        arrayList.add(<span class="number">1</span>, <span class="string">&quot;Go&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;ArrayList: &quot;</span> + arrayList);</span><br><span class="line">        System.out.println(<span class="string">&quot;获取索引2: &quot;</span> + arrayList.get(<span class="number">2</span>));</span><br><span class="line">        arrayList.remove(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;删除C++后: &quot;</span> + arrayList);</span><br><span class="line"></span><br><span class="line">        LinkedList&lt;String&gt; linkedList = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line">        linkedList.add(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">        linkedList.addFirst(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line">        linkedList.addLast(<span class="string">&quot;Python&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;LinkedList: &quot;</span> + linkedList);</span><br><span class="line">        System.out.println(<span class="string">&quot;第一个: &quot;</span> + linkedList.getFirst());</span><br><span class="line">        System.out.println(<span class="string">&quot;最后一个: &quot;</span> + linkedList.getLast());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="性能对比测试代码"><a href="#性能对比测试代码" class="headerlink" title="性能对比测试代码"></a>性能对比测试代码</h2><p>这是我当时写的测试代码，能够比较直观地感受到两者差距：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ListPerformanceTest</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">COUNT</span> <span class="operator">=</span> <span class="number">100000</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        testHeadInsert();</span><br><span class="line">        testRandomAccess();</span><br><span class="line">        testTailInsert();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">testHeadInsert</span><span class="params">()</span> &#123;</span><br><span class="line">        List&lt;String&gt; arrayList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        LinkedList&lt;String&gt; linkedList = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">        <span class="type">long</span> <span class="variable">start</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            arrayList.add(<span class="number">0</span>, <span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">long</span> <span class="variable">end</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;ArrayList 头部插入 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line"></span><br><span class="line">        start = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            linkedList.addFirst(<span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line">        end = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;LinkedList 头部插入 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">testRandomAccess</span><span class="params">()</span> &#123;</span><br><span class="line">        List&lt;String&gt; arrayList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        List&lt;String&gt; linkedList = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            arrayList.add(<span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">            linkedList.add(<span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">long</span> <span class="variable">start</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            arrayList.get(i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">long</span> <span class="variable">end</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;ArrayList 随机访问 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line"></span><br><span class="line">        start = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            linkedList.get(i);</span><br><span class="line">        &#125;</span><br><span class="line">        end = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;LinkedList 随机访问 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">testTailInsert</span><span class="params">()</span> &#123;</span><br><span class="line">        List&lt;String&gt; arrayList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        List&lt;String&gt; linkedList = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">        <span class="type">long</span> <span class="variable">start</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            arrayList.add(<span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">long</span> <span class="variable">end</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;ArrayList 尾部插入 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line"></span><br><span class="line">        start = System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; COUNT; i++) &#123;</span><br><span class="line">            linkedList.add(<span class="string">&quot;item&quot;</span> + i);</span><br><span class="line">        &#125;</span><br><span class="line">        end = System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;LinkedList 尾部插入 &quot;</span> + COUNT + <span class="string">&quot; 次: &quot;</span> + (end - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我本机跑出来的结果大致如下，不同机器会有差异：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">ArrayList 头部插入 100000 次: 2835ms</span><br><span class="line">LinkedList 头部插入 100000 次: 8ms</span><br><span class="line">ArrayList 随机访问 100000 次: 3ms</span><br><span class="line">LinkedList 随机访问 100000 次: 5421ms</span><br><span class="line">ArrayList 尾部插入 100000 次: 5ms</span><br><span class="line">LinkedList 尾部插入 100000 次: 9ms</span><br></pre></td></tr></table></figure>

<p>从结果看，头部插入 <code>ArrayList</code> 明显慢，随机访问则是 <code>LinkedList</code> 明显慢，尾部插入两者差距不大。</p>
<h2 id="正确遍历删除的方式"><a href="#正确遍历删除的方式" class="headerlink" title="正确遍历删除的方式"></a>正确遍历删除的方式</h2><p><strong>方式一：使用 Iterator.remove()，推荐这种写法</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.Iterator;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IteratorRemoveDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        list.add(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;Python&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line"></span><br><span class="line">        Iterator&lt;String&gt; iterator = list.iterator();</span><br><span class="line">        <span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">            <span class="type">String</span> <span class="variable">item</span> <span class="operator">=</span> iterator.next();</span><br><span class="line">            <span class="keyword">if</span> (<span class="string">&quot;deleteMe&quot;</span>.equals(item)) &#123;</span><br><span class="line">                iterator.remove();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;删除后: &quot;</span> + list);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>Iterator.remove()</code> 安全的原因是：它删除元素后会同步更新迭代器内部的 <code>expectedModCount</code>，因此不会触发 fail-fast。</p>
<p><strong>方式二：倒序遍历删除，适合 ArrayList</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ReverseRemoveDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        list.add(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;Python&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> list.size() - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="string">&quot;deleteMe&quot;</span>.equals(list.get(i))) &#123;</span><br><span class="line">                list.remove(i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;删除后: &quot;</span> + list);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>倒序删除不会影响还没有遍历到的索引，所以也能避免漏删问题。不过如果已经使用迭代器遍历，还是优先使用 <code>Iterator.remove()</code>。</p>
<p><strong>方式三：JDK 8 的 removeIf，最简洁</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RemoveIfDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        list.add(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;Python&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;deleteMe&quot;</span>);</span><br><span class="line">        list.add(<span class="string">&quot;C++&quot;</span>);</span><br><span class="line"></span><br><span class="line">        list.removeIf(<span class="string">&quot;deleteMe&quot;</span>::equals);</span><br><span class="line">        System.out.println(<span class="string">&quot;删除后: &quot;</span> + list);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>经过这次踩坑，我总结出三条实用原则：</p>
<p><strong>1. 90% 的场景用 ArrayList 就对了</strong></p>
<p>日常开发里，大部分场景都是读多写少、随机访问、尾部追加。<code>ArrayList</code> 底层数组内存连续，访问速度快，CPU 缓存命中率也更友好，所以它应该作为默认选择。</p>
<p><strong>2. 频繁头插再考虑 LinkedList</strong></p>
<p>如果确实需要频繁在头部插入或删除，比如实现队列、双端队列，<code>LinkedList</code> 的 O(1) 头部操作才有优势。但如果还需要大量随机访问，就不要选它。</p>
<p><strong>3. 遍历删除用 Iterator</strong></p>
<p>遍历过程中不要直接 <code>list.remove()</code>，否则很容易遇到 <code>ConcurrentModificationException</code>。稳妥做法是使用 <code>Iterator.remove()</code>，JDK 8 以后也可以使用 <code>removeIf</code>。</p>
<p>最后的感受是：不要只背“ArrayList 查询快，LinkedList 增删快”这种结论。真正重要的是理解数组和链表的底层结构：数组访问快但插入可能搬移元素，链表头插快但定位元素慢。理解了这些，再遇到 List 选择问题就不会慌了。<br>EOF; __tr_native_ec&#x3D;$?; pwd -P &gt;| ‘&#x2F;var&#x2F;folders&#x2F;sb&#x2F;zyyfx17n2q13ky7hfwtl_8dc0000gn&#x2F;T&#x2F;agent-toolhost&#x2F;jobs&#x2F;job-9de6a9094cdf4f9787cdf8a27b5814ac&#x2F;cwd.txt’; exit “$__tr_native_ec”</p>
]]></content>
      <categories>
        <category>Java集合</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>集合框架</tag>
        <tag>ArrayList</tag>
        <tag>LinkedList</tag>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>HashMap基础学习</title>
    <url>/2018/07/22/2018-07-22-HashMap%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>2018 年 7 月，最近把 <code>List</code> 相关的内容过了一遍，接下来开始学习 <code>Map</code>。和 <code>List</code> 按顺序存储元素不同，<code>Map</code> 更像是一张“键值对表”，通过 <code>key</code> 可以快速找到对应的 <code>value</code>。</p>
<p>在 Java 集合框架里，<code>HashMap</code> 应该是工作中最常用的数据结构之一。比如根据用户 id 查用户信息、根据配置名查配置值、统计某个字段出现次数等，都很容易想到用 <code>HashMap</code>。</p>
<p>刚开始看 <code>HashMap</code> 的时候，我觉得它只是“根据 key 存取 value”的工具类。但继续往下学才发现，里面涉及哈希算法、数组、链表、扩容、线程安全、<code>hashCode</code> 和 <code>equals</code> 等一系列知识点。也难怪它经常出现在面试题里。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>学习过程中，我主要遇到了下面几个疑问：</p>
<ol>
<li><code>key</code> 为 <code>null</code> 时，<code>HashMap</code> 到底怎么处理？</li>
<li>以前听说 <code>HashMap</code> 不是线程安全的，实际会出什么问题？</li>
<li>如果两个不同的 <code>key</code> 算出来的 <code>hashCode</code> 一样，数据会不会被覆盖？</li>
<li>遍历 <code>HashMap</code> 的方式太多，到底该用哪一种？</li>
<li><code>HashMap</code> 和 <code>Hashtable</code> 有什么区别？</li>
</ol>
<p>这些问题如果只停留在“会用 API”的层面，其实很容易混淆。所以我打算先从常用操作入手，再慢慢分析它的底层原理。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><h2 id="1-HashMap-可以存-null-吗？"><a href="#1-HashMap-可以存-null-吗？" class="headerlink" title="1. HashMap 可以存 null 吗？"></a>1. HashMap 可以存 null 吗？</h2><p><code>HashMap</code> 允许一个 <code>null key</code>，也允许多个 <code>null value</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, String&gt; map = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;String, String&gt;();</span><br><span class="line">map.put(<span class="literal">null</span>, <span class="string">&quot;空key&quot;</span>);</span><br><span class="line">map.put(<span class="string">&quot;name&quot;</span>, <span class="literal">null</span>);</span><br><span class="line">map.put(<span class="string">&quot;address&quot;</span>, <span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line">System.out.println(map.get(<span class="literal">null</span>));</span><br></pre></td></tr></table></figure>

<p>需要注意的是，<code>key</code> 是唯一的，所以 <code>null key</code> 只能有一个。如果重复放入 <code>null key</code>，后面的值会覆盖前面的值。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, String&gt; map = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;String, String&gt;();</span><br><span class="line">map.put(<span class="literal">null</span>, <span class="string">&quot;第一次放入&quot;</span>);</span><br><span class="line">map.put(<span class="literal">null</span>, <span class="string">&quot;第二次放入&quot;</span>);</span><br><span class="line"></span><br><span class="line">System.out.println(map.get(<span class="literal">null</span>));</span><br></pre></td></tr></table></figure>

<p>输出结果是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">第二次放入</span><br></pre></td></tr></table></figure>

<h2 id="2-HashMap-为什么不是线程安全的？"><a href="#2-HashMap-为什么不是线程安全的？" class="headerlink" title="2. HashMap 为什么不是线程安全的？"></a>2. HashMap 为什么不是线程安全的？</h2><p><code>HashMap</code> 的大多数操作都没有加锁。如果多个线程同时修改同一个 <code>HashMap</code>，就可能出现问题。</p>
<p>在 JDK7 中，<code>HashMap</code> 扩容时采用头插法迁移链表节点，在并发 <code>put</code> 的情况下，链表可能形成环，最终导致查询时死循环。</p>
<p>在 JDK8 中，虽然扩容迁移逻辑有所调整，降低了死循环风险，但 <code>HashMap</code> 依然不是线程安全的。多个线程同时 <code>put</code> 时，仍然可能出现数据覆盖、数据丢失、扩容状态不一致等问题。</p>
<p>所以结论很明确：如果是多线程写入场景，不要直接使用普通 <code>HashMap</code>。</p>
<h2 id="3-hashCode-相同怎么办？"><a href="#3-hashCode-相同怎么办？" class="headerlink" title="3. hashCode 相同怎么办？"></a>3. hashCode 相同怎么办？</h2><p><code>HashMap</code> 根据 key 的哈希值计算数组下标。不同的 key 可能被计算到同一个数组位置，这种情况叫哈希冲突。</p>
<p>JDK7 中，哈希冲突主要通过“数组 + 链表”解决：多个元素落到同一个桶时，会挂在同一条链表上。</p>
<p>JDK8 中，底层结构变成“数组 + 链表 + 红黑树”。当链表长度达到一定阈值，并且数组容量也达到要求后，链表会转成红黑树，减少极端情况下的查询时间。</p>
<p>因此，两个 key 的 <code>hashCode</code> 相同，并不代表一定会互相覆盖。<code>HashMap</code> 还会继续通过 <code>equals</code> 判断 key 是否真正相等。</p>
<h2 id="4-遍历方式应该选哪个？"><a href="#4-遍历方式应该选哪个？" class="headerlink" title="4. 遍历方式应该选哪个？"></a>4. 遍历方式应该选哪个？</h2><p><code>HashMap</code> 的遍历方式很多，比如遍历 <code>keySet</code>、遍历 <code>entrySet</code>、只遍历 value，或者使用 <code>Iterator</code>。</p>
<p>日常开发里，如果同时需要 key 和 value，我更推荐使用 <code>entrySet</code>。因为 <code>entrySet</code> 可以一次拿到 key 和 value，避免通过 key 再执行一次 <code>get</code> 查询。</p>
<h2 id="5-HashMap-和-Hashtable-区别"><a href="#5-HashMap-和-Hashtable-区别" class="headerlink" title="5. HashMap 和 Hashtable 区别"></a>5. HashMap 和 Hashtable 区别</h2><p><code>Hashtable</code> 是比较早期的集合类，<code>HashMap</code> 是后来集合框架中的常用实现。两者主要区别如下：</p>
<table>
<thead>
<tr>
<th>对比项</th>
<th>HashMap</th>
<th>Hashtable</th>
</tr>
</thead>
<tbody><tr>
<td>线程安全</td>
<td>非线程安全</td>
<td>方法使用 synchronized，线程安全</td>
</tr>
<tr>
<td>null 支持</td>
<td>允许一个 null key 和多个 null value</td>
<td>不允许 null key 和 null value</td>
</tr>
<tr>
<td>性能</td>
<td>单线程下通常更好</td>
<td>加锁开销较大</td>
</tr>
<tr>
<td>推荐程度</td>
<td>日常开发常用</td>
<td>现在较少使用</td>
</tr>
<tr>
<td>替代方案</td>
<td>多线程场景用 ConcurrentHashMap</td>
<td>通常不推荐新代码使用</td>
</tr>
</tbody></table>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="1-HashMap-基本-CRUD-操作"><a href="#1-HashMap-基本-CRUD-操作" class="headerlink" title="1. HashMap 基本 CRUD 操作"></a>1. HashMap 基本 CRUD 操作</h2><p>先从最基础的增删改查开始。这里我用 <code>String</code> 作为 key，<code>Integer</code> 作为 value，模拟记录学生成绩。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, Integer&gt; scoreMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;String, Integer&gt;();</span><br><span class="line"></span><br><span class="line">scoreMap.put(<span class="string">&quot;张三&quot;</span>, <span class="number">90</span>);</span><br><span class="line">scoreMap.put(<span class="string">&quot;李四&quot;</span>, <span class="number">85</span>);</span><br><span class="line">scoreMap.put(<span class="string">&quot;王五&quot;</span>, <span class="number">92</span>);</span><br><span class="line"></span><br><span class="line"><span class="type">Integer</span> <span class="variable">zhangSanScore</span> <span class="operator">=</span> scoreMap.get(<span class="string">&quot;张三&quot;</span>);</span><br><span class="line">System.out.println(<span class="string">&quot;张三成绩：&quot;</span> + zhangSanScore);</span><br></pre></td></tr></table></figure>

<p>修改和删除也很简单：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">scoreMap.put(<span class="string">&quot;李四&quot;</span>, <span class="number">88</span>);</span><br><span class="line">scoreMap.remove(<span class="string">&quot;王五&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (scoreMap.containsKey(<span class="string">&quot;李四&quot;</span>)) &#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;李四成绩：&quot;</span> + scoreMap.get(<span class="string">&quot;李四&quot;</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>常见判断操作：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">boolean</span> <span class="variable">hasZhangSan</span> <span class="operator">=</span> scoreMap.containsKey(<span class="string">&quot;张三&quot;</span>);</span><br><span class="line"><span class="type">boolean</span> <span class="variable">hasFullScore</span> <span class="operator">=</span> scoreMap.containsValue(<span class="number">100</span>);</span><br><span class="line"><span class="type">int</span> <span class="variable">size</span> <span class="operator">=</span> scoreMap.size();</span><br><span class="line"><span class="type">boolean</span> <span class="variable">empty</span> <span class="operator">=</span> scoreMap.isEmpty();</span><br><span class="line"></span><br><span class="line">System.out.println(hasZhangSan + <span class="string">&quot;, &quot;</span> + hasFullScore);</span><br><span class="line">System.out.println(<span class="string">&quot;数量：&quot;</span> + size + <span class="string">&quot;, 是否为空：&quot;</span> + empty);</span><br></pre></td></tr></table></figure>

<h2 id="2-用-Student-作为-key-时重写-hashCode-和-equals"><a href="#2-用-Student-作为-key-时重写-hashCode-和-equals" class="headerlink" title="2. 用 Student 作为 key 时重写 hashCode 和 equals"></a>2. 用 Student 作为 key 时重写 hashCode 和 equals</h2><p>如果自定义对象要作为 <code>HashMap</code> 的 key，必须特别注意 <code>hashCode</code> 和 <code>equals</code>。</p>
<p>比如下面这个 <code>Student</code> 类，我认为只要 <code>id</code> 相同，就是同一个学生。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Student</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Integer id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Student</span><span class="params">(Integer id, String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.id = id;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这时应该重写 <code>equals</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">equals</span><span class="params">(Object obj)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">this</span> == obj) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!(obj <span class="keyword">instanceof</span> Student)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">Student</span> <span class="variable">other</span> <span class="operator">=</span> (Student) obj;</span><br><span class="line">    <span class="keyword">return</span> id != <span class="literal">null</span> ? id.equals(other.id) : other.id == <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>同时必须重写 <code>hashCode</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">hashCode</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> id != <span class="literal">null</span> ? id.hashCode() : <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>完整使用示例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;Student, String&gt; studentMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;Student, String&gt;();</span><br><span class="line"></span><br><span class="line"><span class="type">Student</span> <span class="variable">s1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Student</span>(<span class="number">1</span>, <span class="string">&quot;张三&quot;</span>);</span><br><span class="line"><span class="type">Student</span> <span class="variable">s2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Student</span>(<span class="number">1</span>, <span class="string">&quot;张三同学&quot;</span>);</span><br><span class="line"></span><br><span class="line">studentMap.put(s1, <span class="string">&quot;第一次放入&quot;</span>);</span><br><span class="line">studentMap.put(s2, <span class="string">&quot;第二次放入&quot;</span>);</span><br><span class="line"></span><br><span class="line">System.out.println(studentMap.size());</span><br></pre></td></tr></table></figure>

<p>如果 <code>equals</code> 和 <code>hashCode</code> 都按 <code>id</code> 判断，那么 <code>s1</code> 和 <code>s2</code> 会被认为是同一个 key，最后 <code>size</code> 是 <code>1</code>。</p>
<p>这里最重要的规则是：重写 <code>equals</code> 必须重写 <code>hashCode</code>。否则两个对象即使 <code>equals</code> 返回 <code>true</code>，也可能因为哈希值不同而被放到不同桶里，导致 <code>HashMap</code> 行为异常。</p>
<h2 id="3-HashMap-的-4-种遍历方式"><a href="#3-HashMap-的-4-种遍历方式" class="headerlink" title="3. HashMap 的 4 种遍历方式"></a>3. HashMap 的 4 种遍历方式</h2><h3 id="方式一：遍历-keySet，再通过-key-获取-value"><a href="#方式一：遍历-keySet，再通过-key-获取-value" class="headerlink" title="方式一：遍历 keySet，再通过 key 获取 value"></a>方式一：遍历 keySet，再通过 key 获取 value</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (String key : scoreMap.keySet()) &#123;</span><br><span class="line">    <span class="type">Integer</span> <span class="variable">value</span> <span class="operator">=</span> scoreMap.get(key);</span><br><span class="line">    System.out.println(key + <span class="string">&quot; = &quot;</span> + value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种写法直观，但如果同时需要 key 和 value，会多一次 <code>get</code> 查询，不是最推荐的方式。</p>
<h3 id="方式二：遍历-entrySet"><a href="#方式二：遍历-entrySet" class="headerlink" title="方式二：遍历 entrySet"></a>方式二：遍历 entrySet</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (Map.Entry&lt;String, Integer&gt; entry : scoreMap.entrySet()) &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> entry.getKey();</span><br><span class="line">    <span class="type">Integer</span> <span class="variable">value</span> <span class="operator">=</span> entry.getValue();</span><br><span class="line">    System.out.println(key + <span class="string">&quot; = &quot;</span> + value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种方式性能更好，也更适合需要同时访问 key 和 value 的场景。</p>
<h3 id="方式三：只遍历-values"><a href="#方式三：只遍历-values" class="headerlink" title="方式三：只遍历 values"></a>方式三：只遍历 values</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (Integer value : scoreMap.values()) &#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;成绩：&quot;</span> + value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果只关心 value，不关心 key，这种方式最直接。</p>
<h3 id="方式四：使用-Iterator-遍历"><a href="#方式四：使用-Iterator-遍历" class="headerlink" title="方式四：使用 Iterator 遍历"></a>方式四：使用 Iterator 遍历</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Iterator&lt;Map.Entry&lt;String, Integer&gt;&gt; iterator = scoreMap.entrySet().iterator();</span><br><span class="line"><span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">    Map.Entry&lt;String, Integer&gt; entry = iterator.next();</span><br><span class="line">    System.out.println(entry.getKey() + <span class="string">&quot; = &quot;</span> + entry.getValue());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果遍历过程中需要删除元素，推荐使用 <code>Iterator</code> 的 <code>remove</code> 方法，避免 <code>ConcurrentModificationException</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Iterator&lt;Map.Entry&lt;String, Integer&gt;&gt; iterator = scoreMap.entrySet().iterator();</span><br><span class="line"><span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">    Map.Entry&lt;String, Integer&gt; entry = iterator.next();</span><br><span class="line">    <span class="keyword">if</span> (entry.getValue() &lt; <span class="number">60</span>) &#123;</span><br><span class="line">        iterator.remove();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>简单对比一下：</p>
<table>
<thead>
<tr>
<th>遍历方式</th>
<th>适用场景</th>
<th>推荐程度</th>
</tr>
</thead>
<tbody><tr>
<td>keySet + get</td>
<td>只想看 key，或代码很简单</td>
<td>一般</td>
</tr>
<tr>
<td>entrySet</td>
<td>同时需要 key 和 value</td>
<td>推荐</td>
</tr>
<tr>
<td>values</td>
<td>只需要 value</td>
<td>推荐</td>
</tr>
<tr>
<td>Iterator</td>
<td>遍历时需要删除元素</td>
<td>推荐</td>
</tr>
</tbody></table>
<h2 id="4-初始容量和负载因子怎么设置？"><a href="#4-初始容量和负载因子怎么设置？" class="headerlink" title="4. 初始容量和负载因子怎么设置？"></a>4. 初始容量和负载因子怎么设置？</h2><p><code>HashMap</code> 有两个很重要的参数：初始容量和负载因子。</p>
<p>默认情况下，初始容量是 <code>16</code>，负载因子是 <code>0.75</code>。当元素数量超过 <code>容量 * 负载因子</code> 时，<code>HashMap</code> 会触发扩容。</p>
<p>如果提前知道大概要存多少数据，可以合理设置初始容量，减少扩容次数。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">expectedSize</span> <span class="operator">=</span> <span class="number">1000</span>;</span><br><span class="line"><span class="type">float</span> <span class="variable">loadFactor</span> <span class="operator">=</span> <span class="number">0.75f</span>;</span><br><span class="line"><span class="type">int</span> <span class="variable">initialCapacity</span> <span class="operator">=</span> (<span class="type">int</span>) (expectedSize / loadFactor) + <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">Map&lt;String, String&gt; map = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;String, String&gt;(initialCapacity);</span><br><span class="line">System.out.println(<span class="string">&quot;初始容量建议值：&quot;</span> + initialCapacity);</span><br></pre></td></tr></table></figure>

<p>不过，<code>HashMap</code> 内部容量会调整为 2 的幂。实际开发里可以用一个简单方法计算建议容量：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">calculateHashMapCapacity</span><span class="params">(<span class="type">int</span> expectedSize)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (expectedSize &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;expectedSize不能小于0&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> (<span class="type">int</span>) (expectedSize / <span class="number">0.75f</span>) + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用示例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">capacity</span> <span class="operator">=</span> calculateHashMapCapacity(<span class="number">500</span>);</span><br><span class="line">Map&lt;Integer, String&gt; userMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;Integer, String&gt;(capacity);</span><br><span class="line"></span><br><span class="line">userMap.put(<span class="number">1</span>, <span class="string">&quot;用户A&quot;</span>);</span><br><span class="line">userMap.put(<span class="number">2</span>, <span class="string">&quot;用户B&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>这里的思路不是追求绝对精确，而是尽量避免明知道要放很多数据，却还使用默认容量导致频繁扩容。</p>
<h2 id="5-多线程场景使用-ConcurrentHashMap"><a href="#5-多线程场景使用-ConcurrentHashMap" class="headerlink" title="5. 多线程场景使用 ConcurrentHashMap"></a>5. 多线程场景使用 ConcurrentHashMap</h2><p>如果多个线程会同时读写 Map，可以考虑使用 <code>ConcurrentHashMap</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, Integer&gt; countMap = <span class="keyword">new</span> <span class="title class_">ConcurrentHashMap</span>&lt;String, Integer&gt;();</span><br><span class="line"></span><br><span class="line">countMap.put(<span class="string">&quot;Java&quot;</span>, <span class="number">1</span>);</span><br><span class="line">countMap.put(<span class="string">&quot;HashMap&quot;</span>, <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line"><span class="type">Integer</span> <span class="variable">count</span> <span class="operator">=</span> countMap.get(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">System.out.println(<span class="string">&quot;Java出现次数：&quot;</span> + count);</span><br></pre></td></tr></table></figure>

<p>需要注意的是，<code>ConcurrentHashMap</code> 不是简单地给所有方法都加一把大锁。它内部做了更细粒度的并发控制，所以在并发场景下通常比 <code>Hashtable</code> 更合适。</p>
<p>不过，复合操作仍然要小心。例如“先判断再放入”不是天然原子的，可以优先使用 <code>putIfAbsent</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ConcurrentHashMap&lt;String, Integer&gt; map = <span class="keyword">new</span> <span class="title class_">ConcurrentHashMap</span>&lt;String, Integer&gt;();</span><br><span class="line"></span><br><span class="line"><span class="type">Integer</span> <span class="variable">oldValue</span> <span class="operator">=</span> map.putIfAbsent(<span class="string">&quot;Java&quot;</span>, <span class="number">1</span>);</span><br><span class="line"><span class="keyword">if</span> (oldValue == <span class="literal">null</span>) &#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;第一次放入Java&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习 <code>HashMap</code>，我最大的感受是：它看起来简单，但底层并不简单。</p>
<p><code>HashMap</code> 是面试高频题，也是实际开发中非常常用的集合类。只会 <code>put</code> 和 <code>get</code> 当然可以完成很多需求，但如果不了解底层原理，就容易在哈希冲突、扩容、线程安全、自定义 key 等问题上踩坑。</p>
<p>我给自己总结了几条使用原则：</p>
<ol>
<li><code>HashMap</code> 允许一个 <code>null key</code> 和多个 <code>null value</code>。</li>
<li>两个 key 的 <code>hashCode</code> 相同不会直接覆盖，还要继续比较 <code>equals</code>。</li>
<li>同时需要 key 和 value 时，优先使用 <code>entrySet</code> 遍历。</li>
<li>能预估数据量时，可以设置合适的初始容量，减少扩容。</li>
<li>多线程写入场景不要直接使用 <code>HashMap</code>，可以考虑 <code>ConcurrentHashMap</code>。</li>
<li>自定义对象作为 key 时，重写 <code>equals</code> 必须重写 <code>hashCode</code>。</li>
</ol>
<p>学完这些内容后，我再看 <code>HashMap</code> 就不只是一个“键值对容器”了，而是能联想到数组、链表、红黑树、哈希冲突和并发安全等知识点。理解底层原理，才能真正把这个常用工具用好。</p>
]]></content>
      <categories>
        <category>Java集合</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>集合框架</tag>
        <tag>HashMap</tag>
        <tag>哈希表</tag>
        <tag>Map</tag>
      </tags>
  </entry>
  <entry>
    <title>Java异常处理机制</title>
    <url>/2018/08/19/2018-08-19-Java%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>最近写 Java 小项目的时候，我经常会遇到各种报错。有些错误是编译阶段就提示的，比如文件找不到、方法必须抛出异常；有些错误是程序运行到一半才出现的，比如空指针、数组越界、类型转换失败。</p>
<p>一开始我对异常的理解比较简单：报错了就 <code>try-catch</code> 一下，能运行就行。但是写得多了之后发现，异常处理并不是把代码包起来这么简单。如果处理方式不对，程序表面上不报错了，真正的问题反而被藏起来了。</p>
<p>尤其是这段时间，我经常看到代码里有 <code>catch (Exception e)</code>，但 catch 里面只打印一句话，甚至什么都不写。这样虽然编译能通过，但以后排查问题会非常痛苦。</p>
<p>所以我打算在 2018 年 8 月这个阶段，把 Java 异常处理机制系统梳理一遍，弄清楚异常体系、<code>try-catch-finally</code> 执行顺序、什么时候应该抛异常、什么时候应该捕获异常。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>我目前遇到的困惑主要有这几个：</p>
<ul>
<li>Checked异常和Unchecked异常分不清</li>
<li><code>finally</code> 中的 <code>return</code> 会覆盖 <code>try</code> 中的返回值</li>
<li>习惯性 <code>catch (Exception e)</code>，但 catch 之后不知道怎么处理</li>
<li>空的 catch 块会吞掉异常，导致排查问题很困难</li>
<li>不知道什么时候应该向上抛异常，什么时候应该自己 catch</li>
</ul>
<p>其中最让我印象深的是 <code>finally</code> 里的 <code>return</code>。以前我以为 <code>try</code> 里面已经 <code>return</code> 了，方法就结束了。后来才发现，只要有 <code>finally</code>，它仍然会执行；如果 <code>finally</code> 里面也写了 <code>return</code>，还会把 <code>try</code> 或 <code>catch</code> 里的返回值覆盖掉。</p>
<p>另外，空 catch 块也很危险。异常发生了，但是代码没有任何日志、没有任何提示，就像什么都没发生一样。这种写法短期看起来“安静”，长期看就是在给自己埋雷。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>Java 的异常可以先从 <code>Throwable</code> 这个顶层父类看起。它下面主要有两个分支：<code>Error</code> 和 <code>Exception</code>。</p>
<p><code>Error</code> 通常表示比较严重的问题，比如 JVM 内存不足、栈溢出等，一般不是业务代码应该捕获处理的重点。我们平时写业务代码更多关注的是 <code>Exception</code>。</p>
<p><code>Exception</code> 又可以分成两类：Checked异常和Unchecked异常。</p>
<p>Checked异常是编译期检查异常。也就是说，如果方法可能抛出这类异常，编译器会要求我们必须处理：要么使用 <code>try-catch</code> 捕获，要么在方法上使用 <code>throws</code> 声明继续向上抛。</p>
<p>Unchecked异常主要指 <code>RuntimeException</code> 以及它的子类，比如 <code>NullPointerException</code>、<code>IllegalArgumentException</code>、<code>IndexOutOfBoundsException</code> 等。这类异常编译器不会强制要求处理，但它们仍然会在运行时导致程序出错。</p>
<p><img src="/images/posts/2018/exceptions-1.svg" alt="Java异常体系结构图"></p>
<p>简单理解可以这样记：</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>是否编译期强制处理</th>
<th>常见例子</th>
<th>处理思路</th>
</tr>
</thead>
<tbody><tr>
<td>Checked异常</td>
<td>是</td>
<td>IOException、SQLException</td>
<td>捕获后处理，或向上抛出</td>
</tr>
<tr>
<td>Unchecked异常</td>
<td>否</td>
<td>NullPointerException、IllegalArgumentException</td>
<td>优先通过代码逻辑避免</td>
</tr>
<tr>
<td>Error</td>
<td>否</td>
<td>OutOfMemoryError、StackOverflowError</td>
<td>通常不在业务代码中捕获</td>
</tr>
</tbody></table>
<p><code>finally</code> 的规则也要特别注意：无论 <code>try</code> 中是否发生异常，只要 JVM 没有直接退出，<code>finally</code> 通常都会执行。它适合用来释放资源，比如关闭文件流、数据库连接等。</p>
<p>但是不要在 <code>finally</code> 中写 <code>return</code>。因为 <code>finally</code> 的 <code>return</code> 会覆盖 <code>try</code> 或 <code>catch</code> 中已经准备返回的结果，也可能覆盖正在抛出的异常，让问题变得非常隐蔽。</p>
<p>从代码结构上看，比较合理的异常处理思路是：底层代码只负责发现问题并抛出带上下文的异常，上层代码统一捕获并处理，比如返回错误提示、记录日志、回滚事务等。这样职责会更清晰，也更容易排查问题。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="Java异常体系结构图"><a href="#Java异常体系结构图" class="headerlink" title="Java异常体系结构图"></a>Java异常体系结构图</h2><p>先把异常体系记清楚，对后面理解 Checked 和 Unchecked 很有帮助。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Throwable</span><br><span class="line">├── Error</span><br><span class="line">│   ├── OutOfMemoryError</span><br><span class="line">│   └── StackOverflowError</span><br><span class="line">└── Exception</span><br><span class="line">    ├── IOException</span><br><span class="line">    ├── SQLException</span><br><span class="line">    └── RuntimeException</span><br><span class="line">        ├── NullPointerException</span><br><span class="line">        ├── IllegalArgumentException</span><br><span class="line">        └── IndexOutOfBoundsException</span><br></pre></td></tr></table></figure>


<p>Checked异常的典型例子是读取文件：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.FileNotFoundException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CheckedExceptionDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">FileInputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">FileInputStream</span>(<span class="string">&quot;test.txt&quot;</span>);</span><br><span class="line">            System.out.println(inputStream);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (FileNotFoundException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;文件不存在：&quot;</span> + e.getMessage());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>FileNotFoundException</code> 是 Checked异常，如果不处理，代码不能通过编译。</p>
<p>Unchecked异常的典型例子是空指针：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UncheckedExceptionDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">text</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        System.out.println(text.length());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码编译可以通过，但运行时会抛出 <code>NullPointerException</code>。所以 Unchecked异常不是“不重要”，而是编译器不强制我们写 <code>try-catch</code>。</p>
<h2 id="try-catch-finally执行顺序详解"><a href="#try-catch-finally执行顺序详解" class="headerlink" title="try-catch-finally执行顺序详解"></a>try-catch-finally执行顺序详解</h2><p>先看最普通的情况：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TryCatchFinallyDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;1. 执行try代码块&quot;</span>);</span><br><span class="line">            <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> <span class="number">10</span> / <span class="number">0</span>;</span><br><span class="line">            System.out.println(result);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ArithmeticException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;2. 执行catch代码块：&quot;</span> + e.getMessage());</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;3. 执行finally代码块&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;4. 程序继续向下执行&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>执行顺序是：先进入 <code>try</code>，遇到异常后进入匹配的 <code>catch</code>，最后一定执行 <code>finally</code>，然后继续执行后面的代码。</p>
<p>再看有 <code>return</code> 的情况：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">FinallyReturnDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(getNumber());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">getNumber</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">3</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个方法最终返回的是 <code>3</code>，不是 <code>1</code>。原因是 <code>try</code> 中虽然已经准备返回 <code>1</code>，但是在方法真正返回之前，<code>finally</code> 仍然会执行，而 <code>finally</code> 里的 <code>return 3</code> 覆盖了原来的返回值。</p>
<p>更推荐的写法是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BetterFinallyDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(getNumber());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">getNumber</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;释放资源或记录必要信息&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>finally</code> 只做资源释放，不做返回值控制，这样逻辑更清楚。</p>
<h2 id="自定义业务异常类"><a href="#自定义业务异常类" class="headerlink" title="自定义业务异常类"></a>自定义业务异常类</h2><p>实际项目里，很多异常不是 Java 自带异常能准确表达的。比如用户余额不足、订单状态不正确、参数不合法等，都可以定义成业务异常。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BusinessException</span> <span class="keyword">extends</span> <span class="title class_">RuntimeException</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String errorCode;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">BusinessException</span><span class="params">(String errorCode, String message)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(message);</span><br><span class="line">        <span class="built_in">this</span>.errorCode = errorCode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">BusinessException</span><span class="params">(String errorCode, String message, Throwable cause)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(message, cause);</span><br><span class="line">        <span class="built_in">this</span>.errorCode = errorCode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getErrorCode</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> errorCode;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用业务异常时，异常信息要写清楚，最好带上业务上下文：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderService</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">pay</span><span class="params">(String orderId, <span class="type">int</span> balance, <span class="type">int</span> amount)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (balance &lt; amount) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BusinessException</span>(<span class="string">&quot;BALANCE_NOT_ENOUGH&quot;</span>, <span class="string">&quot;订单&quot;</span> + orderId + <span class="string">&quot;支付失败，余额不足&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;订单支付成功：&quot;</span> + orderId);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里我选择让 <code>BusinessException</code> 继承 <code>RuntimeException</code>，是因为业务异常很多时候希望由上层统一处理，而不是在每一层都被迫 <code>try-catch</code>。</p>
<h2 id="异常链cause的使用"><a href="#异常链cause的使用" class="headerlink" title="异常链cause的使用"></a>异常链cause的使用</h2><p>有时候底层异常不能直接暴露给上层，但又不能丢失原始原因。这时就应该使用异常链，也就是把原始异常作为 <code>cause</code> 传进去。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.FileInputStream;</span><br><span class="line"><span class="keyword">import</span> java.io.FileNotFoundException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ExceptionCauseDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            loadConfig(<span class="string">&quot;app.properties&quot;</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (BusinessException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;错误码：&quot;</span> + e.getErrorCode());</span><br><span class="line">            System.out.println(<span class="string">&quot;错误信息：&quot;</span> + e.getMessage());</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">loadConfig</span><span class="params">(String fileName)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">FileInputStream</span>(fileName);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (FileNotFoundException e) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BusinessException</span>(<span class="string">&quot;CONFIG_NOT_FOUND&quot;</span>, <span class="string">&quot;配置文件不存在：&quot;</span> + fileName, e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样做的好处是：上层看到的是业务含义明确的异常，排查问题时又可以通过 <code>cause</code> 找到最原始的 <code>FileNotFoundException</code>。</p>
<h2 id="多catch的使用"><a href="#多catch的使用" class="headerlink" title="多catch的使用"></a>多catch的使用</h2><p>Java 7 之后可以在一个 catch 中捕获多种异常，适合处理方式相同的场景。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MultiCatchDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">String</span> <span class="variable">numberText</span> <span class="operator">=</span> args[<span class="number">0</span>];</span><br><span class="line">            <span class="type">int</span> <span class="variable">number</span> <span class="operator">=</span> Integer.parseInt(numberText);</span><br><span class="line">            System.out.println(<span class="string">&quot;数字是：&quot;</span> + number);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ArrayIndexOutOfBoundsException | NumberFormatException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;请输入一个合法的整数参数：&quot;</span> + e.getMessage());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果不同异常的处理方式不同，就不要强行合并，应该分开 catch。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SeparateCatchDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">String</span> <span class="variable">numberText</span> <span class="operator">=</span> args[<span class="number">0</span>];</span><br><span class="line">            <span class="type">int</span> <span class="variable">number</span> <span class="operator">=</span> Integer.parseInt(numberText);</span><br><span class="line">            System.out.println(<span class="string">&quot;数字是：&quot;</span> + number);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ArrayIndexOutOfBoundsException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;缺少启动参数&quot;</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (NumberFormatException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;参数不是合法整数：&quot;</span> + args[<span class="number">0</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>需要注意的是，catch 的顺序要从具体异常到宽泛异常。如果先 catch <code>Exception</code>，后面的具体异常就没有机会被捕获了。</p>
<h2 id="try-with-resources释放资源"><a href="#try-with-resources释放资源" class="headerlink" title="try-with-resources释放资源"></a>try-with-resources释放资源</h2><p>以前释放资源通常写在 <code>finally</code> 里：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.BufferedReader;</span><br><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OldResourceCloseDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">        <span class="type">BufferedReader</span> <span class="variable">reader</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            reader = <span class="keyword">new</span> <span class="title class_">BufferedReader</span>(<span class="keyword">new</span> <span class="title class_">FileReader</span>(<span class="string">&quot;test.txt&quot;</span>));</span><br><span class="line">            System.out.println(reader.readLine());</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (reader != <span class="literal">null</span>) &#123;</span><br><span class="line">                reader.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Java 7 之后更推荐使用 <code>try-with-resources</code>，它会自动关闭实现了 <code>AutoCloseable</code> 接口的资源。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.BufferedReader;</span><br><span class="line"><span class="keyword">import</span> java.io.FileReader;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TryWithResourcesDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> (<span class="type">BufferedReader</span> <span class="variable">reader</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BufferedReader</span>(<span class="keyword">new</span> <span class="title class_">FileReader</span>(<span class="string">&quot;test.txt&quot;</span>))) &#123;</span><br><span class="line">            System.out.println(reader.readLine());</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;读取文件失败：&quot;</span> + e.getMessage());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<p>相比手写 <code>finally</code>，<code>try-with-resources</code> 更简洁，也更不容易忘记关闭资源。</p>
<h2 id="异常处理最佳实践清单"><a href="#异常处理最佳实践清单" class="headerlink" title="异常处理最佳实践清单"></a>异常处理最佳实践清单</h2><p>经过这次整理，我觉得写异常处理时可以按下面这份清单检查：</p>
<ul>
<li>不要写空 catch 块，至少要记录日志或给出明确处理</li>
<li>不要只写 <code>catch (Exception e)</code>，除非这一层确实是统一异常处理入口</li>
<li>不要在 <code>finally</code> 中写 <code>return</code></li>
<li>异常信息要带上下文，比如订单号、用户ID、文件名</li>
<li>能用代码判断避免的 RuntimeException，就不要依赖 try-catch</li>
<li>底层异常如果无法处理，就向上抛出，不要假装处理了</li>
<li>上层统一处理异常，负责日志、提示、事务回滚等</li>
<li>包装异常时保留原始 cause，避免丢失根因</li>
<li>关闭资源优先使用 try-with-resources</li>
<li>自定义异常命名要清晰，比如 <code>BusinessException</code>、<code>OrderStatusException</code></li>
</ul>
<p>例如下面这种空 catch 就是反模式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> <span class="number">10</span> / <span class="number">0</span>;</span><br><span class="line">    System.out.println(result);</span><br><span class="line">&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>更好的方式是根据当前层的职责处理异常：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> <span class="number">10</span> / <span class="number">0</span>;</span><br><span class="line">    System.out.println(result);</span><br><span class="line">&#125; <span class="keyword">catch</span> (ArithmeticException e) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">BusinessException</span>(<span class="string">&quot;CALCULATE_ERROR&quot;</span>, <span class="string">&quot;计算失败，除数不能为0&quot;</span>, e);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里没有把异常吞掉，而是把底层异常包装成更有业务意义的异常，并保留原始原因。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次系统梳理 Java 异常处理机制之后，我最大的感受是：异常处理不是为了“让程序不报错”，而是为了让错误在合适的位置被发现、被记录、被处理。</p>
<p>我的阶段性总结是：</p>
<ul>
<li>不要吞异常，空 catch 块是非常危险的写法</li>
<li>异常信息要带上下文，不然排查问题会很困难</li>
<li>底层无法处理的异常应该向上抛，上层再统一 catch</li>
<li><code>finally</code> 适合释放资源，但不要在里面写 <code>return</code></li>
<li>关闭文件流、网络连接等资源时，优先使用 <code>try-with-resources</code></li>
</ul>
<p>以后写代码时，我会尽量避免为了“消除红线”而随便 <code>try-catch</code>。真正好的异常处理，应该能帮助我更快定位问题，而不是把问题藏起来。</p>
]]></content>
      <categories>
        <category>Java基础</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>异常处理</tag>
        <tag>try-catch</tag>
        <tag>Exception</tag>
        <tag>最佳实践</tag>
      </tags>
  </entry>
  <entry>
    <title>JDBC连接MySQL实践</title>
    <url>/2018/09/15/2018-09-15-JDBC%E8%BF%9E%E6%8E%A5MySQL%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>2018 年 9 月，我刚把 Java 基础和 MySQL 的常用语法系统学完。前面写 Java 时，大多是在控制台里打印结果；学 MySQL 时，也主要是在命令行或者图形化工具里执行 SQL。到了这个阶段，一个很自然的问题出现了：能不能用 Java 代码直接操作 MySQL 数据库？</p>
<p>答案就是 JDBC。</p>
<p>JDBC，全称是 Java Database Connectivity，它是 Java 提供的一套数据库访问规范。简单理解，JDBC 就像 Java 程序和数据库之间的一座桥：Java 代码通过 JDBC API 发送 SQL，数据库执行后再把结果返回给 Java 程序。</p>
<p><img src="/images/posts/2018/jdbc-mysql-1.svg" alt="Java 程序通过 JDBC 连接 MySQL 的流程图"></p>
<p>我当时的目标很明确：</p>
<ul>
<li>用 Java 连接 MySQL；</li>
<li>用 Java 执行增删改查；</li>
<li>理解 <code>Connection</code>、<code>PreparedStatement</code>、<code>ResultSet</code> 这些核心对象；</li>
<li>避免连接泄漏、SQL 注入、中文乱码这些常见坑。</li>
</ul>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>第一次真正写 JDBC 代码时，问题比想象中多。很多错误不是 SQL 写错，而是连接、驱动、资源释放这些基础细节没有处理好。</p>
<h2 id="1-ClassNotFoundException-找不到驱动"><a href="#1-ClassNotFoundException-找不到驱动" class="headerlink" title="1. ClassNotFoundException 找不到驱动"></a>1. ClassNotFoundException 找不到驱动</h2><p>最常见的报错是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">java.lang.ClassNotFoundException: com.mysql.jdbc.Driver</span><br></pre></td></tr></table></figure>

<p>这个异常的意思很直接：Java 程序找不到 MySQL 的 JDBC 驱动类。</p>
<p>当时我以为 JDK 自带了所有数据库驱动，后来才知道 JDBC 只是规范，真正连接 MySQL 还需要 MySQL 官方提供的 Connector&#x2F;J 驱动包。如果没有把 <code>mysql-connector-java</code> 加入 classpath，<code>Class.forName(&quot;com.mysql.jdbc.Driver&quot;)</code> 就会失败。</p>
<h2 id="2-连接字符串写错"><a href="#2-连接字符串写错" class="headerlink" title="2. 连接字符串写错"></a>2. 连接字符串写错</h2><p>第二类问题是 JDBC URL 写错，比如数据库名漏写、端口写错、参数拼错。</p>
<p>JDBC 连接 MySQL 的基本格式是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">jdbc:mysql://host:port/database</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">jdbc:mysql://localhost:3306/test</span><br></pre></td></tr></table></figure>

<p>如果还要处理中文编码，通常需要加上参数：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">jdbc:mysql://localhost:3306/test?useUnicode=true&amp;characterEncoding=UTF-8</span><br></pre></td></tr></table></figure>

<h2 id="3-SQL-注入风险"><a href="#3-SQL-注入风险" class="headerlink" title="3. SQL 注入风险"></a>3. SQL 注入风险</h2><p>一开始我会用字符串拼接 SQL：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;select * from user where username = &#x27;&quot;</span> + username + <span class="string">&quot;&#x27; and password = &#x27;&quot;</span> + password + <span class="string">&quot;&#x27;&quot;</span>;</span><br></pre></td></tr></table></figure>

<p>这种写法看起来简单，但非常危险。如果用户输入中带有特殊 SQL 片段，就可能绕过登录校验，甚至破坏数据。</p>
<h2 id="4-ResultSet-取值顺序混乱"><a href="#4-ResultSet-取值顺序混乱" class="headerlink" title="4. ResultSet 取值顺序混乱"></a>4. ResultSet 取值顺序混乱</h2><p><code>ResultSet</code> 不是一次性把所有数据都变成 Java 对象，而是一个游标。刚拿到 <code>ResultSet</code> 时，游标在第一行之前，必须先调用 <code>next()</code>，再读取当前行的数据。</p>
<p>如果忘记 <code>next()</code>，或者列名、列下标混着用，很容易出现取值混乱。</p>
<h2 id="5-资源忘记关闭导致连接泄漏"><a href="#5-资源忘记关闭导致连接泄漏" class="headerlink" title="5. 资源忘记关闭导致连接泄漏"></a>5. 资源忘记关闭导致连接泄漏</h2><p>JDBC 里最容易忽略的一点是资源释放。<code>Connection</code>、<code>Statement</code>、<code>ResultSet</code> 都持有数据库或网络资源，用完不关闭，就可能导致连接越来越多，最后数据库拒绝新的连接。</p>
<h2 id="6-中文数据存入乱码"><a href="#6-中文数据存入乱码" class="headerlink" title="6. 中文数据存入乱码"></a>6. 中文数据存入乱码</h2><p>如果数据库、表、连接 URL 的编码不一致，中文数据就可能变成问号或者乱码。尤其是早期 MySQL 项目里，连接参数里没有指定 <code>characterEncoding=UTF-8</code> 时很容易遇到。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>把这些问题拆开看，其实它们都对应 JDBC 的几个基础机制。</p>
<h2 id="1-驱动必须加入-classpath"><a href="#1-驱动必须加入-classpath" class="headerlink" title="1. 驱动必须加入 classpath"></a>1. 驱动必须加入 classpath</h2><p>JDBC API 在 JDK 中提供，但 MySQL 的具体驱动实现不在 JDK 里。要连接 MySQL，必须引入 Connector&#x2F;J。</p>
<p>如果是普通 Java 工程，需要把 jar 包加入 classpath；如果是 Maven 工程，可以加入依赖：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-java<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.1.47<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>2018 年我使用较多的是 MySQL Connector&#x2F;J 5.x，对应驱动类通常写：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">com.mysql.jdbc.Driver</span><br></pre></td></tr></table></figure>

<h2 id="2-JDBC-URL-要写完整"><a href="#2-JDBC-URL-要写完整" class="headerlink" title="2. JDBC URL 要写完整"></a>2. JDBC URL 要写完整</h2><p>JDBC URL 不是普通网址，它会被驱动解析。MySQL 的基础格式是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">jdbc:mysql://主机名:端口号/数据库名?参数1=值1&amp;参数2=值2</span><br></pre></td></tr></table></figure>

<p>常用示例：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">jdbc:mysql://localhost:3306/jdbc_demo?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false</span><br></pre></td></tr></table></figure>

<p>其中：</p>
<ul>
<li><code>localhost</code> 表示本机数据库；</li>
<li><code>3306</code> 是 MySQL 默认端口；</li>
<li><code>jdbc_demo</code> 是数据库名；</li>
<li><code>useUnicode=true</code> 和 <code>characterEncoding=UTF-8</code> 用于处理中文；</li>
<li><code>useSSL=false</code> 可以避免部分版本驱动的 SSL 警告。</li>
</ul>
<h2 id="3-PreparedStatement-比-Statement-更安全"><a href="#3-PreparedStatement-比-Statement-更安全" class="headerlink" title="3. PreparedStatement 比 Statement 更安全"></a>3. PreparedStatement 比 Statement 更安全</h2><p><code>Statement</code> 通常需要拼接 SQL 字符串，这会带来 SQL 注入风险。<code>PreparedStatement</code> 使用占位符 <code>?</code>，再通过 <code>setXxx</code> 方法传参，参数会被驱动正确处理。</p>
<p>例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;select * from user where username = ? and password = ?&quot;</span>;</span><br><span class="line"><span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> conn.prepareStatement(sql);</span><br><span class="line">ps.setString(<span class="number">1</span>, username);</span><br><span class="line">ps.setString(<span class="number">2</span>, password);</span><br></pre></td></tr></table></figure>

<p>这样 SQL 结构和参数值是分离的，可读性和安全性都更好。</p>
<h2 id="4-ResultSet-是游标模式"><a href="#4-ResultSet-是游标模式" class="headerlink" title="4. ResultSet 是游标模式"></a>4. ResultSet 是游标模式</h2><p><code>ResultSet</code> 可以理解为结果集游标。读取数据时通常这样写：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (rs.next()) &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">id</span> <span class="operator">=</span> rs.getInt(<span class="string">&quot;id&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">username</span> <span class="operator">=</span> rs.getString(<span class="string">&quot;username&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我更推荐使用列名取值，而不是列下标。列名更直观，SQL 字段顺序调整时也不容易出错。</p>
<h2 id="5-资源必须关闭"><a href="#5-资源必须关闭" class="headerlink" title="5. 资源必须关闭"></a>5. 资源必须关闭</h2><p>JDBC 资源关闭顺序一般和创建顺序相反：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">ResultSet -&gt; Statement/PreparedStatement -&gt; Connection</span><br></pre></td></tr></table></figure>

<p>JDK 1.7 以后可以使用 try-with-resources 自动关闭资源。不过为了理解原理，先掌握 <code>finally</code> 中手动关闭也很重要。</p>
<h2 id="6-编码要统一"><a href="#6-编码要统一" class="headerlink" title="6. 编码要统一"></a>6. 编码要统一</h2><p>中文乱码通常不是单点问题，要同时检查：</p>
<ul>
<li>数据库编码；</li>
<li>表编码；</li>
<li>字段编码；</li>
<li>JDBC URL 编码参数；</li>
<li>Java 字符串本身。</li>
</ul>
<p>JDBC URL 中至少应明确指定：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">useUnicode=true&amp;characterEncoding=UTF-8</span><br></pre></td></tr></table></figure>

<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>下面记录一套完整的 JDBC 连接 MySQL 实践代码。示例基于 JDK 1.8、MySQL Connector&#x2F;J 5.x，表结构使用一个简单的 <code>user</code> 表。</p>
<h2 id="准备表结构"><a href="#准备表结构" class="headerlink" title="准备表结构"></a>准备表结构</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> database if <span class="keyword">not</span> <span class="keyword">exists</span> jdbc_demo <span class="keyword">default</span> <span class="keyword">character set</span> utf8;</span><br><span class="line"></span><br><span class="line">use jdbc_demo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">create table</span> if <span class="keyword">not</span> <span class="keyword">exists</span> <span class="keyword">user</span> (</span><br><span class="line">    id <span class="type">int</span> <span class="keyword">primary key</span> auto_increment,</span><br><span class="line">    username <span class="type">varchar</span>(<span class="number">50</span>) <span class="keyword">not null</span>,</span><br><span class="line">    password <span class="type">varchar</span>(<span class="number">50</span>) <span class="keyword">not null</span>,</span><br><span class="line">    age <span class="type">int</span>,</span><br><span class="line">    create_time datetime</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h2 id="1-JDBC-连接-MySQL-的-6-步代码"><a href="#1-JDBC-连接-MySQL-的-6-步代码" class="headerlink" title="1. JDBC 连接 MySQL 的 6 步代码"></a>1. JDBC 连接 MySQL 的 6 步代码</h2><p>JDBC 最基础的流程可以概括为 6 步：</p>
<ol>
<li>加载数据库驱动；</li>
<li>获取数据库连接；</li>
<li>编写 SQL；</li>
<li>创建执行 SQL 的对象；</li>
<li>执行 SQL 并处理结果；</li>
<li>关闭资源。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.DriverManager;</span><br><span class="line"><span class="keyword">import</span> java.sql.PreparedStatement;</span><br><span class="line"><span class="keyword">import</span> java.sql.ResultSet;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">JdbcSixStepDemo</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">url</span> <span class="operator">=</span> <span class="string">&quot;jdbc:mysql://localhost:3306/jdbc_demo?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false&quot;</span>;</span><br><span class="line">        <span class="type">String</span> <span class="variable">username</span> <span class="operator">=</span> <span class="string">&quot;root&quot;</span>;</span><br><span class="line">        <span class="type">String</span> <span class="variable">password</span> <span class="operator">=</span> <span class="string">&quot;root&quot;</span>;</span><br><span class="line"></span><br><span class="line">        <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="type">ResultSet</span> <span class="variable">rs</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class.forName(<span class="string">&quot;com.mysql.jdbc.Driver&quot;</span>);</span><br><span class="line">            conn = DriverManager.getConnection(url, username, password);</span><br><span class="line"></span><br><span class="line">            <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;select id, username, age from user where age &gt; ?&quot;</span>;</span><br><span class="line">            ps = conn.prepareStatement(sql);</span><br><span class="line">            ps.setInt(<span class="number">1</span>, <span class="number">18</span>);</span><br><span class="line"></span><br><span class="line">            rs = ps.executeQuery();</span><br><span class="line">            <span class="keyword">while</span> (rs.next()) &#123;</span><br><span class="line">                <span class="type">int</span> <span class="variable">id</span> <span class="operator">=</span> rs.getInt(<span class="string">&quot;id&quot;</span>);</span><br><span class="line">                <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> rs.getString(<span class="string">&quot;username&quot;</span>);</span><br><span class="line">                <span class="type">int</span> <span class="variable">age</span> <span class="operator">=</span> rs.getInt(<span class="string">&quot;age&quot;</span>);</span><br><span class="line">                System.out.println(id + <span class="string">&quot; - &quot;</span> + name + <span class="string">&quot; - &quot;</span> + age);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            closeQuietly(rs, ps, conn);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">closeQuietly</span><span class="params">(ResultSet rs, PreparedStatement ps, Connection conn)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (rs != <span class="literal">null</span>) &#123;</span><br><span class="line">                rs.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (ps != <span class="literal">null</span>) &#123;</span><br><span class="line">                ps.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (conn != <span class="literal">null</span>) &#123;</span><br><span class="line">                conn.close();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码看起来比较长，但它把 JDBC 的基本动作都展示出来了。实际开发中，关闭资源、获取连接这些重复代码应该封装到工具类里。</p>
<h2 id="2-PreparedStatement-的-CRUD-完整示例"><a href="#2-PreparedStatement-的-CRUD-完整示例" class="headerlink" title="2. PreparedStatement 的 CRUD 完整示例"></a>2. PreparedStatement 的 CRUD 完整示例</h2><p>下面用 <code>PreparedStatement</code> 完成增删改查。为了让代码更清晰，我先定义一个简单的实体类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Integer id;</span><br><span class="line">    <span class="keyword">private</span> String username;</span><br><span class="line">    <span class="keyword">private</span> String password;</span><br><span class="line">    <span class="keyword">private</span> Integer age;</span><br><span class="line">    <span class="keyword">private</span> Date createTime;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Integer <span class="title function_">getId</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> id;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setId</span><span class="params">(Integer id)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.id = id;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getUsername</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> username;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setUsername</span><span class="params">(String username)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.username = username;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getPassword</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> password;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setPassword</span><span class="params">(String password)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.password = password;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Integer <span class="title function_">getAge</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setAge</span><span class="params">(Integer age)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Date <span class="title function_">getCreateTime</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> createTime;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setCreateTime</span><span class="params">(Date createTime)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.createTime = createTime;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="新增数据"><a href="#新增数据" class="headerlink" title="新增数据"></a>新增数据</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">insertUser</span><span class="params">(User user)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;insert into user(username, password, age, create_time) values(?, ?, ?, ?)&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        ps = conn.prepareStatement(sql);</span><br><span class="line">        ps.setString(<span class="number">1</span>, user.getUsername());</span><br><span class="line">        ps.setString(<span class="number">2</span>, user.getPassword());</span><br><span class="line">        ps.setInt(<span class="number">3</span>, user.getAge());</span><br><span class="line">        ps.setTimestamp(<span class="number">4</span>, <span class="keyword">new</span> <span class="title class_">java</span>.sql.Timestamp(user.getCreateTime().getTime()));</span><br><span class="line">        <span class="keyword">return</span> ps.executeUpdate();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, ps, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="查询数据"><a href="#查询数据" class="headerlink" title="查询数据"></a>查询数据</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> User <span class="title function_">findUserById</span><span class="params">(Integer id)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;select id, username, password, age, create_time from user where id = ?&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">ResultSet</span> <span class="variable">rs</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        ps = conn.prepareStatement(sql);</span><br><span class="line">        ps.setInt(<span class="number">1</span>, id);</span><br><span class="line">        rs = ps.executeQuery();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!rs.next()) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user.setId(rs.getInt(<span class="string">&quot;id&quot;</span>));</span><br><span class="line">        user.setUsername(rs.getString(<span class="string">&quot;username&quot;</span>));</span><br><span class="line">        user.setPassword(rs.getString(<span class="string">&quot;password&quot;</span>));</span><br><span class="line">        user.setAge(rs.getInt(<span class="string">&quot;age&quot;</span>));</span><br><span class="line">        user.setCreateTime(rs.getTimestamp(<span class="string">&quot;create_time&quot;</span>));</span><br><span class="line">        <span class="keyword">return</span> user;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(rs, ps, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="修改数据"><a href="#修改数据" class="headerlink" title="修改数据"></a>修改数据</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">updateUserAge</span><span class="params">(Integer id, Integer age)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;update user set age = ? where id = ?&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        ps = conn.prepareStatement(sql);</span><br><span class="line">        ps.setInt(<span class="number">1</span>, age);</span><br><span class="line">        ps.setInt(<span class="number">2</span>, id);</span><br><span class="line">        <span class="keyword">return</span> ps.executeUpdate();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, ps, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">deleteUserById</span><span class="params">(Integer id)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;delete from user where id = ?&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        ps = conn.prepareStatement(sql);</span><br><span class="line">        ps.setInt(<span class="number">1</span>, id);</span><br><span class="line">        <span class="keyword">return</span> ps.executeUpdate();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, ps, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里有一个小经验：查询使用 <code>executeQuery()</code>，增删改使用 <code>executeUpdate()</code>。后者返回受影响的行数，可以用来判断操作是否成功。</p>
<h2 id="3-事务管理代码"><a href="#3-事务管理代码" class="headerlink" title="3. 事务管理代码"></a>3. 事务管理代码</h2><p>事务适合处理一组必须同时成功或同时失败的操作，比如转账：A 扣钱成功，B 加钱也必须成功。</p>
<p>JDBC 默认是自动提交事务的，也就是每执行一条 SQL 就提交一次。要手动控制事务，需要关闭自动提交：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">transfer</span><span class="params">(Integer fromId, Integer toId, Integer amount)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">decreaseSql</span> <span class="operator">=</span> <span class="string">&quot;update account set balance = balance - ? where id = ?&quot;</span>;</span><br><span class="line">    <span class="type">String</span> <span class="variable">increaseSql</span> <span class="operator">=</span> <span class="string">&quot;update account set balance = balance + ? where id = ?&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">decreasePs</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">increasePs</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        conn.setAutoCommit(<span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">        decreasePs = conn.prepareStatement(decreaseSql);</span><br><span class="line">        decreasePs.setInt(<span class="number">1</span>, amount);</span><br><span class="line">        decreasePs.setInt(<span class="number">2</span>, fromId);</span><br><span class="line">        decreasePs.executeUpdate();</span><br><span class="line"></span><br><span class="line">        increasePs = conn.prepareStatement(increaseSql);</span><br><span class="line">        increasePs.setInt(<span class="number">1</span>, amount);</span><br><span class="line">        increasePs.setInt(<span class="number">2</span>, toId);</span><br><span class="line">        increasePs.executeUpdate();</span><br><span class="line"></span><br><span class="line">        conn.commit();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        rollback(conn);</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, increasePs, <span class="literal">null</span>);</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, decreasePs, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">rollback</span><span class="params">(Connection conn)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (conn == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn.rollback();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码的关键点是：</p>
<ul>
<li><code>conn.setAutoCommit(false)</code>：关闭自动提交；</li>
<li><code>conn.commit()</code>：所有 SQL 成功后提交；</li>
<li><code>conn.rollback()</code>：出现异常时回滚；</li>
<li>同一个事务中的多条 SQL 必须使用同一个 <code>Connection</code>。</li>
</ul>
<h2 id="4-JDBC-工具类封装"><a href="#4-JDBC-工具类封装" class="headerlink" title="4. JDBC 工具类封装"></a>4. JDBC 工具类封装</h2><p>如果每个方法都写加载驱动、创建连接、关闭资源，代码会非常重复。所以我把这些逻辑封装成一个工具类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.sql.DriverManager;</span><br><span class="line"><span class="keyword">import</span> java.sql.ResultSet;</span><br><span class="line"><span class="keyword">import</span> java.sql.Statement;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">JdbcUtils</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DRIVER_CLASS</span> <span class="operator">=</span> <span class="string">&quot;com.mysql.jdbc.Driver&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">URL</span> <span class="operator">=</span> <span class="string">&quot;jdbc:mysql://localhost:3306/jdbc_demo?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">USERNAME</span> <span class="operator">=</span> <span class="string">&quot;root&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">PASSWORD</span> <span class="operator">=</span> <span class="string">&quot;root&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class.forName(DRIVER_CLASS);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ExceptionInInitializerError</span>(e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">JdbcUtils</span><span class="params">()</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Connection <span class="title function_">getConnection</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="keyword">return</span> DriverManager.getConnection(URL, USERNAME, PASSWORD);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">close</span><span class="params">(ResultSet rs, Statement stmt, Connection conn)</span> &#123;</span><br><span class="line">        closeResultSet(rs);</span><br><span class="line">        closeStatement(stmt);</span><br><span class="line">        closeConnection(conn);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">closeResultSet</span><span class="params">(ResultSet rs)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (rs == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            rs.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">closeStatement</span><span class="params">(Statement stmt)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (stmt == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            stmt.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">closeConnection</span><span class="params">(Connection conn)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (conn == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            conn.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个工具类做了三件事：</p>
<ul>
<li>静态代码块加载驱动；</li>
<li><code>getConnection()</code> 统一获取连接；</li>
<li><code>close()</code> 统一关闭资源。</li>
</ul>
<p>学习阶段这样写足够清晰，但生产环境不建议每次都通过 <code>DriverManager</code> 新建物理连接，因为创建连接成本比较高。</p>
<h2 id="5-try-with-resources-写法"><a href="#5-try-with-resources-写法" class="headerlink" title="5. try-with-resources 写法"></a>5. try-with-resources 写法</h2><p>JDK 1.7 之后可以用 try-with-resources 自动关闭资源。JDK 1.8 中也可以使用这种写法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> User <span class="title function_">findUserByUsername</span><span class="params">(String username)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;select id, username, password, age, create_time from user where username = ?&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> (<span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> JdbcUtils.getConnection();</span><br><span class="line">         <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> conn.prepareStatement(sql)) &#123;</span><br><span class="line"></span><br><span class="line">        ps.setString(<span class="number">1</span>, username);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> (<span class="type">ResultSet</span> <span class="variable">rs</span> <span class="operator">=</span> ps.executeQuery()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!rs.next()) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">            user.setId(rs.getInt(<span class="string">&quot;id&quot;</span>));</span><br><span class="line">            user.setUsername(rs.getString(<span class="string">&quot;username&quot;</span>));</span><br><span class="line">            user.setPassword(rs.getString(<span class="string">&quot;password&quot;</span>));</span><br><span class="line">            user.setAge(rs.getInt(<span class="string">&quot;age&quot;</span>));</span><br><span class="line">            user.setCreateTime(rs.getTimestamp(<span class="string">&quot;create_time&quot;</span>));</span><br><span class="line">            <span class="keyword">return</span> user;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种写法的好处是资源关闭更可靠，也减少了大量 <code>finally</code> 模板代码。</p>
<h2 id="6-Druid-连接池简介"><a href="#6-Druid-连接池简介" class="headerlink" title="6. Druid 连接池简介"></a>6. Druid 连接池简介</h2><p>实际项目中，不应该每次操作数据库都创建一个新的物理连接。连接池会提前维护一批连接，程序需要时从池中取，用完后归还给连接池。</p>
<p>Druid 是阿里开源的数据库连接池，在国内 Java 项目里很常见。Maven 依赖示例：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>druid<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.1.10<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><code>druid.properties</code> 配置示例：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">driverClassName</span>=<span class="string">com.mysql.jdbc.Driver</span></span><br><span class="line"><span class="attr">url</span>=<span class="string">jdbc:mysql://localhost:3306/jdbc_demo?useUnicode=true&amp;characterEncoding=UTF-8&amp;useSSL=false</span></span><br><span class="line"><span class="attr">username</span>=<span class="string">root</span></span><br><span class="line"><span class="attr">password</span>=<span class="string">root</span></span><br><span class="line"><span class="attr">initialSize</span>=<span class="string">5</span></span><br><span class="line"><span class="attr">maxActive</span>=<span class="string">20</span></span><br><span class="line"><span class="attr">minIdle</span>=<span class="string">5</span></span><br><span class="line"><span class="attr">maxWait</span>=<span class="string">3000</span></span><br><span class="line"><span class="attr">validationQuery</span>=<span class="string">select 1</span></span><br><span class="line"><span class="attr">testWhileIdle</span>=<span class="string">true</span></span><br><span class="line"><span class="attr">testOnBorrow</span>=<span class="string">false</span></span><br><span class="line"><span class="attr">testOnReturn</span>=<span class="string">false</span></span><br></pre></td></tr></table></figure>

<p>使用 Druid 获取连接：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.alibaba.druid.pool.DruidDataSourceFactory;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.sql.DataSource;</span><br><span class="line"><span class="keyword">import</span> java.io.InputStream;</span><br><span class="line"><span class="keyword">import</span> java.sql.Connection;</span><br><span class="line"><span class="keyword">import</span> java.util.Properties;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DruidUtils</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> DataSource dataSource;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">Properties</span> <span class="variable">properties</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Properties</span>();</span><br><span class="line">            <span class="type">InputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> DruidUtils.class.getClassLoader().getResourceAsStream(<span class="string">&quot;druid.properties&quot;</span>);</span><br><span class="line">            properties.load(inputStream);</span><br><span class="line">            dataSource = DruidDataSourceFactory.createDataSource(properties);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">ExceptionInInitializerError</span>(e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">DruidUtils</span><span class="params">()</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Connection <span class="title function_">getConnection</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="keyword">return</span> dataSource.getConnection();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> DataSource <span class="title function_">getDataSource</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> dataSource;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用连接池后，调用 <code>conn.close()</code> 并不是真的关闭物理连接，而是把连接归还给连接池。这一点很重要，所以即使用了连接池，也仍然必须关闭连接。</p>
<h2 id="7-批处理操作"><a href="#7-批处理操作" class="headerlink" title="7. 批处理操作"></a>7. 批处理操作</h2><p>当需要一次插入很多条数据时，如果一条一条执行 SQL，效率会比较低。JDBC 提供了批处理能力，可以把多条操作合并提交。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">batchInsertUsers</span><span class="params">(List&lt;User&gt; users)</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;insert into user(username, password, age, create_time) values(?, ?, ?, ?)&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">Connection</span> <span class="variable">conn</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="type">PreparedStatement</span> <span class="variable">ps</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        conn = JdbcUtils.getConnection();</span><br><span class="line">        ps = conn.prepareStatement(sql);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; users.size(); i++) &#123;</span><br><span class="line">            <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> users.get(i);</span><br><span class="line">            ps.setString(<span class="number">1</span>, user.getUsername());</span><br><span class="line">            ps.setString(<span class="number">2</span>, user.getPassword());</span><br><span class="line">            ps.setInt(<span class="number">3</span>, user.getAge());</span><br><span class="line">            ps.setTimestamp(<span class="number">4</span>, <span class="keyword">new</span> <span class="title class_">java</span>.sql.Timestamp(user.getCreateTime().getTime()));</span><br><span class="line">            ps.addBatch();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> ((i + <span class="number">1</span>) % <span class="number">500</span> == <span class="number">0</span>) &#123;</span><br><span class="line">                ps.executeBatch();</span><br><span class="line">                ps.clearBatch();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ps.executeBatch();</span><br><span class="line">        ps.clearBatch();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        JdbcUtils.close(<span class="literal">null</span>, ps, conn);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>批处理适合大量新增或更新数据。这里每 500 条执行一次，是为了避免一次性积累太多 SQL 占用内存。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次 JDBC 连接 MySQL 的实践，让我把 Java 基础、SQL 语法和数据库连接机制串了起来。以前只是在数据库工具里执行 SQL，现在可以通过 Java 代码完成完整的 CRUD 操作，感觉 Java 程序终于可以真正和数据打交道了。</p>
<p>我给自己总结了几条规则：</p>
<ol>
<li>永远优先使用 <code>PreparedStatement</code>，不要用字符串拼接 SQL；</li>
<li><code>ResultSet</code> 是游标模式，读取前先 <code>next()</code>；</li>
<li><code>Connection</code>、<code>Statement</code>、<code>ResultSet</code> 用完必须关闭；</li>
<li>资源关闭要放在 <code>finally</code> 中，或者使用 try-with-resources；</li>
<li>中文数据要注意数据库编码和 JDBC URL 中的 <code>characterEncoding=UTF-8</code>；</li>
<li>涉及多条 SQL 的一致性操作时，要明确事务边界；</li>
<li>生产环境不要直接频繁创建连接，应该使用连接池，比如 Druid。</li>
</ol>
<p>JDBC 的代码确实比较模板化，但它非常适合用来理解数据库访问的底层过程。后面再学习 DBUtils、JdbcTemplate、MyBatis 这些框架时，就会更容易明白它们到底帮我们封装了什么。</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>JDBC</tag>
        <tag>MySQL</tag>
        <tag>数据库连接</tag>
        <tag>CRUD</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL基础增删改查</title>
    <url>/2018/10/28/2018-10-28-MySQL%E5%9F%BA%E7%A1%80%E5%A2%9E%E5%88%A0%E6%94%B9%E6%9F%A5/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>学完JDBC之后，我意识到一个很严重的问题——我一直在“照着写”SQL，却从来没有真正理解过每条SQL背后的含义。JDBC教会了我怎么用Java连接数据库、怎么执行SQL、怎么处理ResultSet，但SQL本身呢？我只会照猫画虎地写几句SELECT和INSERT，稍微复杂一点的查询就抓瞎。</p>
<p>这段时间我决定从头系统地学习SQL，从最基础的建表开始，到增删改查，再到多表关联、聚合函数、索引优化，一步一步把每个知识点搞清楚。这篇文章就是我在2018年10月学习MySQL基础CRUD的完整记录。</p>
<p><img src="/images/posts/2018/mysql-crud-1.svg" alt="MySQL基础增删改查"></p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>在实际写SQL的过程中，我遇到了好几个一直没搞明白的问题：</p>
<ol>
<li><strong>DELETE和TRUNCATE到底有什么区别？</strong> 之前一直以为都是删数据，随便用哪个都行，结果有一次用TRUNCATE删完数据发现回不去了。</li>
<li><strong>GROUP BY和HAVING总是写错</strong>，经常把过滤条件写在WHERE里，结果报错，搞不清什么时候该用WHERE、什么时候该用HAVING。</li>
<li><strong>多表JOIN搞不清关系</strong>，INNER JOIN、LEFT JOIN、RIGHT JOIN到底返回什么数据，每次都要画图才能想明白。</li>
<li><strong>索引加了但查询还是很慢</strong>，以为建了索引就万事大吉，结果有些查询根本没走索引。</li>
<li><strong>不知道什么时候该建索引</strong>，哪些列该建、哪些不该建，完全没有概念。</li>
</ol>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><h2 id="DELETE和TRUNCATE的区别"><a href="#DELETE和TRUNCATE的区别" class="headerlink" title="DELETE和TRUNCATE的区别"></a>DELETE和TRUNCATE的区别</h2><p>这两个看起来都是“删数据”，但底层机制完全不同：</p>
<table>
<thead>
<tr>
<th>对比点</th>
<th>DELETE</th>
<th>TRUNCATE</th>
</tr>
</thead>
<tbody><tr>
<td>删除方式</td>
<td>逐行删除</td>
<td>清空并重建表结构</td>
</tr>
<tr>
<td>SQL类型</td>
<td>DML</td>
<td>DDL</td>
</tr>
<tr>
<td>是否可回滚</td>
<td>在事务中可回滚</td>
<td>通常不可回滚</td>
</tr>
<tr>
<td>是否支持WHERE</td>
<td>支持</td>
<td>不支持</td>
</tr>
<tr>
<td>自增ID</td>
<td>一般不重置</td>
<td>通常会重置</td>
</tr>
<tr>
<td>执行速度</td>
<td>数据量大时较慢</td>
<td>通常更快</td>
</tr>
</tbody></table>
<p>简单理解：DELETE是一行一行删，可以配合WHERE精确删除，也可以在事务中回滚；TRUNCATE更像是把表清空后重新整理，速度快，但风险也更高。</p>
<h2 id="GROUP-BY和HAVING"><a href="#GROUP-BY和HAVING" class="headerlink" title="GROUP BY和HAVING"></a>GROUP BY和HAVING</h2><p>我之前经常把聚合条件写到WHERE里，例如想查平均分大于80的学生，却写成：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> student_id, <span class="built_in">AVG</span>(score)</span><br><span class="line"><span class="keyword">FROM</span> score</span><br><span class="line"><span class="keyword">WHERE</span> <span class="built_in">AVG</span>(score) <span class="operator">&gt;</span> <span class="number">80</span></span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> student_id;</span><br></pre></td></tr></table></figure>

<p>这肯定是不对的，因为WHERE执行时还没有分组，自然也没有AVG(score)这个结果。正确写法应该是：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> student_id, <span class="built_in">AVG</span>(score)</span><br><span class="line"><span class="keyword">FROM</span> score</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> student_id</span><br><span class="line"><span class="keyword">HAVING</span> <span class="built_in">AVG</span>(score) <span class="operator">&gt;</span> <span class="number">80</span>;</span><br></pre></td></tr></table></figure>

<p>WHERE是在分组前过滤原始行，HAVING是在分组后过滤聚合结果。</p>
<h2 id="JOIN类型的区别"><a href="#JOIN类型的区别" class="headerlink" title="JOIN类型的区别"></a>JOIN类型的区别</h2><p>JOIN最核心的不是语法，而是先搞清楚两张表之间的关系：</p>
<ul>
<li><strong>INNER JOIN</strong>：只返回两边都匹配的数据。</li>
<li><strong>LEFT JOIN</strong>：左表全部保留，右表匹配不到的地方用NULL补齐。</li>
<li><strong>RIGHT JOIN</strong>：右表全部保留，左表匹配不到的地方用NULL补齐。</li>
</ul>
<p>实际开发中我感觉LEFT JOIN用得更多，因为很多时候我们会以一张主表为中心，再去关联其它明细表。</p>
<h2 id="索引不是万能的"><a href="#索引不是万能的" class="headerlink" title="索引不是万能的"></a>索引不是万能的</h2><p>索引像书的目录，可以帮助数据库快速定位数据。但它不是万能药，加了索引并不代表一定快：</p>
<ul>
<li>表数据量很小时，全表扫描可能更快。</li>
<li>查询返回的数据太多时，走索引反而不划算。</li>
<li>在索引列上使用函数，可能导致索引失效。</li>
<li>LIKE以<code>%</code>开头时，通常无法利用普通索引。</li>
<li>字段发生隐式类型转换时，也可能导致索引失效。</li>
</ul>
<h2 id="索引应该建在什么列上"><a href="#索引应该建在什么列上" class="headerlink" title="索引应该建在什么列上"></a>索引应该建在什么列上</h2><p>比较适合建索引的列：</p>
<ul>
<li>区分度高的列，比如学生ID、手机号、身份证号。</li>
<li>经常出现在WHERE条件里的列。</li>
<li>经常用于JOIN关联的外键列。</li>
<li>经常用于ORDER BY或GROUP BY的列。</li>
</ul>
<p>不太适合建索引的列：</p>
<ul>
<li>区分度很低的列，比如性别。</li>
<li>经常更新的列，因为维护索引也有成本。</li>
<li>很少参与查询条件的列。</li>
</ul>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><h2 id="准备三张表"><a href="#准备三张表" class="headerlink" title="准备三张表"></a>准备三张表</h2><p>我用学生、课程、成绩三张表来练习，关系比较清晰：一个学生可以选择多门课程，一门课程也可以被多个学生选择，成绩表负责连接学生和课程。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 学生表：保存学生基本信息</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> student (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT COMMENT <span class="string">&#x27;学生ID&#x27;</span>,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;姓名&#x27;</span>,</span><br><span class="line">    age <span class="type">INT</span> COMMENT <span class="string">&#x27;年龄&#x27;</span>,</span><br><span class="line">    gender <span class="type">VARCHAR</span>(<span class="number">10</span>) COMMENT <span class="string">&#x27;性别&#x27;</span>,</span><br><span class="line">    major <span class="type">VARCHAR</span>(<span class="number">50</span>) COMMENT <span class="string">&#x27;专业&#x27;</span>,</span><br><span class="line">    create_time DATETIME <span class="keyword">DEFAULT</span> <span class="built_in">CURRENT_TIMESTAMP</span> COMMENT <span class="string">&#x27;创建时间&#x27;</span></span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8 COMMENT<span class="operator">=</span><span class="string">&#x27;学生表&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 课程表：保存课程基本信息</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> course (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT COMMENT <span class="string">&#x27;课程ID&#x27;</span>,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;课程名称&#x27;</span>,</span><br><span class="line">    credit <span class="type">INT</span> <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;学分&#x27;</span></span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8 COMMENT<span class="operator">=</span><span class="string">&#x27;课程表&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 成绩表：保存学生和课程的对应关系以及成绩</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> score (</span><br><span class="line">    id <span class="type">INT</span> <span class="keyword">PRIMARY KEY</span> AUTO_INCREMENT COMMENT <span class="string">&#x27;成绩ID&#x27;</span>,</span><br><span class="line">    student_id <span class="type">INT</span> <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;学生ID&#x27;</span>,</span><br><span class="line">    course_id <span class="type">INT</span> <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;课程ID&#x27;</span>,</span><br><span class="line">    score <span class="type">DECIMAL</span>(<span class="number">5</span>,<span class="number">2</span>) COMMENT <span class="string">&#x27;成绩&#x27;</span>,</span><br><span class="line">    exam_time <span class="type">DATE</span> COMMENT <span class="string">&#x27;考试日期&#x27;</span>,</span><br><span class="line">    <span class="keyword">CONSTRAINT</span> fk_score_student <span class="keyword">FOREIGN KEY</span> (student_id) <span class="keyword">REFERENCES</span> student(id),</span><br><span class="line">    <span class="keyword">CONSTRAINT</span> fk_score_course <span class="keyword">FOREIGN KEY</span> (course_id) <span class="keyword">REFERENCES</span> course(id)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8 COMMENT<span class="operator">=</span><span class="string">&#x27;成绩表&#x27;</span>;</span><br></pre></td></tr></table></figure>

<h2 id="INSERT：新增数据"><a href="#INSERT：新增数据" class="headerlink" title="INSERT：新增数据"></a>INSERT：新增数据</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 新增学生数据</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> student (name, age, gender, major) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="string">&#x27;张三&#x27;</span>, <span class="number">20</span>, <span class="string">&#x27;男&#x27;</span>, <span class="string">&#x27;计算机科学&#x27;</span>),</span><br><span class="line">(<span class="string">&#x27;李四&#x27;</span>, <span class="number">21</span>, <span class="string">&#x27;女&#x27;</span>, <span class="string">&#x27;软件工程&#x27;</span>),</span><br><span class="line">(<span class="string">&#x27;王五&#x27;</span>, <span class="number">19</span>, <span class="string">&#x27;男&#x27;</span>, <span class="string">&#x27;数据科学&#x27;</span>),</span><br><span class="line">(<span class="string">&#x27;赵六&#x27;</span>, <span class="number">22</span>, <span class="string">&#x27;女&#x27;</span>, <span class="string">&#x27;计算机科学&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 新增课程数据</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> course (name, credit) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="string">&#x27;数据库原理&#x27;</span>, <span class="number">4</span>),</span><br><span class="line">(<span class="string">&#x27;数据结构&#x27;</span>, <span class="number">3</span>),</span><br><span class="line">(<span class="string">&#x27;操作系统&#x27;</span>, <span class="number">4</span>),</span><br><span class="line">(<span class="string">&#x27;计算机网络&#x27;</span>, <span class="number">3</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 新增成绩数据</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> score (student_id, course_id, score, exam_time) <span class="keyword">VALUES</span></span><br><span class="line">(<span class="number">1</span>, <span class="number">1</span>, <span class="number">92.50</span>, <span class="string">&#x27;2018-10-20&#x27;</span>),</span><br><span class="line">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">88.00</span>, <span class="string">&#x27;2018-10-21&#x27;</span>),</span><br><span class="line">(<span class="number">2</span>, <span class="number">1</span>, <span class="number">78.50</span>, <span class="string">&#x27;2018-10-20&#x27;</span>),</span><br><span class="line">(<span class="number">2</span>, <span class="number">3</span>, <span class="number">95.00</span>, <span class="string">&#x27;2018-10-22&#x27;</span>),</span><br><span class="line">(<span class="number">3</span>, <span class="number">2</span>, <span class="number">66.00</span>, <span class="string">&#x27;2018-10-21&#x27;</span>),</span><br><span class="line">(<span class="number">3</span>, <span class="number">4</span>, <span class="number">72.50</span>, <span class="string">&#x27;2018-10-23&#x27;</span>),</span><br><span class="line">(<span class="number">4</span>, <span class="number">1</span>, <span class="number">85.00</span>, <span class="string">&#x27;2018-10-20&#x27;</span>),</span><br><span class="line">(<span class="number">4</span>, <span class="number">4</span>, <span class="number">90.00</span>, <span class="string">&#x27;2018-10-23&#x27;</span>);</span><br></pre></td></tr></table></figure>

<h2 id="UPDATE：修改数据"><a href="#UPDATE：修改数据" class="headerlink" title="UPDATE：修改数据"></a>UPDATE：修改数据</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 修改张三的年龄</span></span><br><span class="line"><span class="keyword">UPDATE</span> student</span><br><span class="line"><span class="keyword">SET</span> age <span class="operator">=</span> <span class="number">21</span></span><br><span class="line"><span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;张三&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 修改数据库原理课程的学分</span></span><br><span class="line"><span class="keyword">UPDATE</span> course</span><br><span class="line"><span class="keyword">SET</span> credit <span class="operator">=</span> <span class="number">5</span></span><br><span class="line"><span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;数据库原理&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 给王五的所有成绩加5分</span></span><br><span class="line"><span class="keyword">UPDATE</span> score</span><br><span class="line"><span class="keyword">SET</span> score <span class="operator">=</span> score <span class="operator">+</span> <span class="number">5</span></span><br><span class="line"><span class="keyword">WHERE</span> student_id <span class="operator">=</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> id <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;王五&#x27;</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h2 id="DELETE：删除数据"><a href="#DELETE：删除数据" class="headerlink" title="DELETE：删除数据"></a>DELETE：删除数据</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 删除低于60分的成绩记录</span></span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> score</span><br><span class="line"><span class="keyword">WHERE</span> score <span class="operator">&lt;</span> <span class="number">60</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除某个学生的成绩记录</span></span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> score</span><br><span class="line"><span class="keyword">WHERE</span> student_id <span class="operator">=</span> <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除成绩表全部数据，可配合事务回滚</span></span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> score;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 清空成绩表，不支持WHERE，风险更高</span></span><br><span class="line"><span class="keyword">TRUNCATE</span> <span class="keyword">TABLE</span> score;</span><br></pre></td></tr></table></figure>

<h2 id="SELECT：基础查询"><a href="#SELECT：基础查询" class="headerlink" title="SELECT：基础查询"></a>SELECT：基础查询</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询所有学生</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 只查询指定列</span></span><br><span class="line"><span class="keyword">SELECT</span> name, age, major <span class="keyword">FROM</span> student;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 条件查询</span></span><br><span class="line"><span class="keyword">SELECT</span> name, age</span><br><span class="line"><span class="keyword">FROM</span> student</span><br><span class="line"><span class="keyword">WHERE</span> major <span class="operator">=</span> <span class="string">&#x27;计算机科学&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 模糊查询</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student</span><br><span class="line"><span class="keyword">WHERE</span> name <span class="keyword">LIKE</span> <span class="string">&#x27;张%&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 排序查询</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> age <span class="keyword">DESC</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 分页查询：从第0条开始取10条</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student</span><br><span class="line">LIMIT <span class="number">0</span>, <span class="number">10</span>;</span><br></pre></td></tr></table></figure>


<h2 id="多表JOIN查询"><a href="#多表JOIN查询" class="headerlink" title="多表JOIN查询"></a>多表JOIN查询</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- INNER JOIN：只查询有成绩的学生和课程</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    c.name <span class="keyword">AS</span> course_name,</span><br><span class="line">    sc.score <span class="keyword">AS</span> exam_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> course c <span class="keyword">ON</span> sc.course_id <span class="operator">=</span> c.id;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- LEFT JOIN：查询所有学生，即使学生还没有成绩也会显示</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    c.name <span class="keyword">AS</span> course_name,</span><br><span class="line">    sc.score <span class="keyword">AS</span> exam_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> course c <span class="keyword">ON</span> sc.course_id <span class="operator">=</span> c.id;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- RIGHT JOIN：以课程表为主，查询所有课程的选课成绩</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    c.name <span class="keyword">AS</span> course_name,</span><br><span class="line">    sc.score <span class="keyword">AS</span> exam_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">RIGHT</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">RIGHT</span> <span class="keyword">JOIN</span> course c <span class="keyword">ON</span> sc.course_id <span class="operator">=</span> c.id;</span><br></pre></td></tr></table></figure>

<h2 id="聚合函数和GROUP-BY"><a href="#聚合函数和GROUP-BY" class="headerlink" title="聚合函数和GROUP BY"></a>聚合函数和GROUP BY</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询每个学生的平均成绩</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    <span class="built_in">AVG</span>(sc.score) <span class="keyword">AS</span> avg_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> s.id, s.name;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询平均成绩大于80分的学生</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    <span class="built_in">AVG</span>(sc.score) <span class="keyword">AS</span> avg_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> s.id, s.name</span><br><span class="line"><span class="keyword">HAVING</span> <span class="built_in">AVG</span>(sc.score) <span class="operator">&gt;</span> <span class="number">80</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询每门课程的选课人数、最高分和最低分</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    c.name <span class="keyword">AS</span> course_name,</span><br><span class="line">    <span class="built_in">COUNT</span>(sc.id) <span class="keyword">AS</span> student_count,</span><br><span class="line">    <span class="built_in">MAX</span>(sc.score) <span class="keyword">AS</span> max_score,</span><br><span class="line">    <span class="built_in">MIN</span>(sc.score) <span class="keyword">AS</span> min_score</span><br><span class="line"><span class="keyword">FROM</span> course c</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> c.id <span class="operator">=</span> sc.course_id</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> c.id, c.name;</span><br></pre></td></tr></table></figure>

<h2 id="子查询"><a href="#子查询" class="headerlink" title="子查询"></a>子查询</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查询成绩高于全班平均分的记录</span></span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    sc.score <span class="keyword">AS</span> exam_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">WHERE</span> sc.score <span class="operator">&gt;</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> <span class="built_in">AVG</span>(score) <span class="keyword">FROM</span> score</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询选择了数据库原理课程的学生</span></span><br><span class="line"><span class="keyword">SELECT</span> name</span><br><span class="line"><span class="keyword">FROM</span> student</span><br><span class="line"><span class="keyword">WHERE</span> id <span class="keyword">IN</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> sc.student_id</span><br><span class="line">    <span class="keyword">FROM</span> score sc</span><br><span class="line">    <span class="keyword">INNER</span> <span class="keyword">JOIN</span> course c <span class="keyword">ON</span> sc.course_id <span class="operator">=</span> c.id</span><br><span class="line">    <span class="keyword">WHERE</span> c.name <span class="operator">=</span> <span class="string">&#x27;数据库原理&#x27;</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查询至少有一门成绩大于90分的学生</span></span><br><span class="line"><span class="keyword">SELECT</span> s.name</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">EXISTS</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> <span class="number">1</span></span><br><span class="line">    <span class="keyword">FROM</span> score sc</span><br><span class="line">    <span class="keyword">WHERE</span> sc.student_id <span class="operator">=</span> s.id</span><br><span class="line">      <span class="keyword">AND</span> sc.score <span class="operator">&gt;</span> <span class="number">90</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h2 id="索引创建和使用建议"><a href="#索引创建和使用建议" class="headerlink" title="索引创建和使用建议"></a>索引创建和使用建议</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 给成绩表的学生ID建立索引，提升按学生查询成绩的速度</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_score_student_id <span class="keyword">ON</span> score(student_id);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 给成绩表的课程ID建立索引，提升按课程查询成绩的速度</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_score_course_id <span class="keyword">ON</span> score(course_id);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 给学生姓名建立索引，提升按姓名精确查询的速度</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_student_name <span class="keyword">ON</span> student(name);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 联合索引：适合同时按学生和课程查询成绩的场景</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_score_student_course <span class="keyword">ON</span> score(student_id, course_id);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 删除索引</span></span><br><span class="line"><span class="keyword">DROP</span> INDEX idx_student_name <span class="keyword">ON</span> student;</span><br></pre></td></tr></table></figure>

<p>联合索引要注意最左前缀原则。假设建立了<code>(student_id, course_id)</code>这个联合索引：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 可以使用联合索引</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> score <span class="keyword">WHERE</span> student_id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 可以使用联合索引</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> score <span class="keyword">WHERE</span> student_id <span class="operator">=</span> <span class="number">1</span> <span class="keyword">AND</span> course_id <span class="operator">=</span> <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 通常不能完整使用这个联合索引，因为跳过了最左边的student_id</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> score <span class="keyword">WHERE</span> course_id <span class="operator">=</span> <span class="number">2</span>;</span><br></pre></td></tr></table></figure>

<h2 id="EXPLAIN查看执行计划"><a href="#EXPLAIN查看执行计划" class="headerlink" title="EXPLAIN查看执行计划"></a>EXPLAIN查看执行计划</h2><p>EXPLAIN可以帮助我判断SQL到底有没有走索引，不能只靠感觉猜。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查看按学生ID查询成绩的执行计划</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> score <span class="keyword">WHERE</span> student_id <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看JOIN查询的执行计划</span></span><br><span class="line">EXPLAIN</span><br><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    s.name <span class="keyword">AS</span> student_name,</span><br><span class="line">    c.name <span class="keyword">AS</span> course_name,</span><br><span class="line">    sc.score <span class="keyword">AS</span> exam_score</span><br><span class="line"><span class="keyword">FROM</span> student s</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> score sc <span class="keyword">ON</span> s.id <span class="operator">=</span> sc.student_id</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> course c <span class="keyword">ON</span> sc.course_id <span class="operator">=</span> c.id;</span><br></pre></td></tr></table></figure>

<p>EXPLAIN里我现在重点看这几个字段：</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>type</td>
<td>访问类型，ALL通常表示全表扫描，ref、range一般比ALL好</td>
</tr>
<tr>
<td>possible_keys</td>
<td>可能用到的索引</td>
</tr>
<tr>
<td>key</td>
<td>实际使用的索引</td>
</tr>
<tr>
<td>rows</td>
<td>MySQL预估要扫描的行数</td>
</tr>
<tr>
<td>Extra</td>
<td>额外信息，比如Using index、Using where、Using filesort</td>
</tr>
</tbody></table>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 如果name没有索引，可能会全表扫描</span></span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;张三&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 建立索引后，再观察key和rows的变化</span></span><br><span class="line"><span class="keyword">CREATE</span> INDEX idx_student_name <span class="keyword">ON</span> student(name);</span><br><span class="line">EXPLAIN <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> student <span class="keyword">WHERE</span> name <span class="operator">=</span> <span class="string">&#x27;张三&#x27;</span>;</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次系统整理MySQL基础增删改查之后，我最大的感受是：SQL真的是后端开发的基本功。以前写JDBC时，我只关心Java代码能不能跑通，现在才发现SQL本身才是和数据库沟通的核心。</p>
<p>我需要牢记的几个点：</p>
<ol>
<li><strong>理解SQL执行顺序</strong>：FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY。只有知道SQL先做什么、后做什么，才能写出正确的分组查询。</li>
<li><strong>DELETE和TRUNCATE不能混用</strong>：DELETE适合按条件删除，也更容易配合事务；TRUNCATE适合清空整张表，但风险更高。</li>
<li><strong>JOIN要先想清楚主表和关系</strong>：INNER JOIN看匹配，LEFT JOIN保留左表，RIGHT JOIN保留右表。</li>
<li><strong>索引要合理使用</strong>：索引应该建在区分度高、经常查询、经常关联的列上，不是越多越好。</li>
<li><strong>用EXPLAIN验证SQL</strong>：查询慢的时候，不要靠猜，要看执行计划。</li>
</ol>
<p>2018年10月的这个阶段，我觉得自己终于从“会照着写SQL”，开始慢慢变成“知道为什么这样写SQL”了。下一步准备继续学习事务、锁和隔离级别，这些应该会更接近真实项目里的数据库问题。</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>MySQL</tag>
        <tag>CRUD</tag>
        <tag>SQL</tag>
        <tag>数据库</tag>
        <tag>索引</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux常用命令整理</title>
    <url>/2018/11/18/2018-11-18-Linux%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4%E6%95%B4%E7%90%86/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>2018年双十一刚过，项目终于要部署上线了。公司买了两台阿里云ECS服务器，操作系统是CentOS 7.4。作为一个从大学到现在一直在Windows环境下写代码的.NET程序员，听到”部署到Linux服务器”这几个字时，内心是拒绝的。</p>
<p>之前对Linux的印象停留在”黑乎乎的命令行窗口，一堆看不懂的英文字母”。但项目不等人，硬着头皮上吧。花了大概两周时间边查边练，把日常运维常用的Linux命令过了一遍，这篇文章就是这段时间的学习笔记。</p>
<p><img src="/images/posts/2018/linux-cli-1.svg" alt="Linux终端界面"></p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚接手Linux服务器时，遇到了一堆”小白问题”，说来有点丢人但确实很真实：</p>
<p><strong>1. 文件权限看不懂</strong></p>
<p><code>ls -l</code> 输出了一堆 <code>drwxr-xr-x</code> 这样的东西，完全不知道什么意思。想给文件加执行权限，百度了半天才找到 <code>chmod 777</code>，但到底什么是”777”也说不清楚。</p>
<p><strong>2. vim编辑器不会用</strong></p>
<p>用 <code>vi</code> 打开文件后，发现打不了字，想退出却退不出来，最后直接关了终端。后来才知道 vim 有三种模式，退出要用 <code>:q</code>。</p>
<p><strong>3. 进程查不到</strong></p>
<p>程序跑起来后不知道在哪看，Windows 有任务管理器，Linux 呢？程序挂了也不知道为什么挂的。</p>
<p><strong>4. 磁盘满了不知道清哪里</strong></p>
<p>有一天数据库写不进去了，排查半天发现是磁盘满了。但日志文件、临时文件散落在各个目录，不知道从哪里开始清理。</p>
<p><strong>5. 日志文件太大查看困难</strong></p>
<p>Java 应用的日志文件动辄几百兆甚至几个G，用记事本打开直接卡死。想查最新的报错信息，总不能把整个文件拖下来吧。</p>
<p><strong>6. 端口被占用不知道怎么查</strong></p>
<p>Tomcat 启动报错 <code>Address already in use</code>，知道是端口被占了，但不知道被哪个程序占了，也不知道怎么杀掉它。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>遇到这些问题后，我花了一些时间理解了 Linux 的几个核心概念，这些概念是后续学习命令的基础。</p>
<h3 id="Linux-一切皆文件"><a href="#Linux-一切皆文件" class="headerlink" title="Linux 一切皆文件"></a>Linux 一切皆文件</h3><p>这是 Linux 最核心的设计哲学。普通文件是文件，目录是文件，设备是文件，甚至是进程信息、网络连接都被抽象成了文件。理解了这一点，就能理解为什么 Linux 有那么多文件操作命令了。</p>
<h3 id="权限的-rwx-表示法"><a href="#权限的-rwx-表示法" class="headerlink" title="权限的 rwx 表示法"></a>权限的 rwx 表示法</h3><p><code>rwx</code> 分别代表：</p>
<ul>
<li><code>r</code>（read）：读取权限，值为 4</li>
<li><code>w</code>（write）：写入权限，值为 2</li>
<li><code>x</code>（execute）：执行权限，值为 1</li>
</ul>
<p>权限分三组，每组三个字符：<strong>所有者&#x2F;所属组&#x2F;其他人</strong>。所以 <code>rwxr-xr-x</code> 表示：所有者可读写执行，所属组可读执行，其他人可读执行。</p>
<p>而 <code>chmod 755</code> 中的 755，就是用数字表示的权限：</p>
<ul>
<li>7 &#x3D; 4+2+1 &#x3D; rwx（所有者）</li>
<li>5 &#x3D; 4+0+1 &#x3D; r-x（所属组）</li>
<li>5 &#x3D; 4+0+1 &#x3D; r-x（其他人）</li>
</ul>
<h3 id="vim-的三种模式"><a href="#vim-的三种模式" class="headerlink" title="vim 的三种模式"></a>vim 的三种模式</h3><p>刚接触 vim 时觉得这编辑器反人类，理解了三种模式后就豁然开朗了：</p>
<ul>
<li><strong>命令模式</strong>：刚进入 vim 时的模式，可以复制粘贴删除，但<strong>不能输入文字</strong></li>
<li><strong>插入模式</strong>：按 <code>i</code> 进入，这个模式才能正常打字</li>
<li><strong>末行模式</strong>：按 <code>:</code> 进入，用来保存退出、搜索替换</li>
</ul>
<p>三种模式切换搞明白后，vim 就好用多了。</p>
<h3 id="进程管理思路"><a href="#进程管理思路" class="headerlink" title="进程管理思路"></a>进程管理思路</h3><p>Linux 的进程管理不像 Windows 那样可视化，但命令行的方式其实更灵活：</p>
<ul>
<li><code>ps</code> 查看静态进程快照</li>
<li><code>top</code> 查看动态进程信息（相当于任务管理器）</li>
<li><code>kill</code> 发送信号给进程</li>
</ul>
<h3 id="磁盘查看思路"><a href="#磁盘查看思路" class="headerlink" title="磁盘查看思路"></a>磁盘查看思路</h3><ul>
<li><code>df</code> 从宏观角度查看磁盘整体使用情况</li>
<li><code>du</code> 从微观角度查看具体目录&#x2F;文件占用空间</li>
</ul>
<h3 id="日志查看三板斧"><a href="#日志查看三板斧" class="headerlink" title="日志查看三板斧"></a>日志查看三板斧</h3><p>大文件用编辑器打开是不现实的，Linux 提供了三个利器：</p>
<ul>
<li><code>tail</code>：从尾部看，适合看最新的日志</li>
<li><code>grep</code>：按关键字过滤，适合搜索特定错误</li>
<li><code>less</code>：分页查看，适合在文件中上下翻阅</li>
</ul>
<h3 id="端口查看思路"><a href="#端口查看思路" class="headerlink" title="端口查看思路"></a>端口查看思路</h3><ul>
<li><code>netstat</code>：查看网络连接状态</li>
<li><code>lsof</code>：查看打开的文件（一切皆文件，网络连接也是文件）</li>
</ul>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>下面按场景分类整理常用命令，每个分类给出常用命令和实际运维示例。</p>
<h3 id="一、文件目录操作"><a href="#一、文件目录操作" class="headerlink" title="一、文件目录操作"></a>一、文件目录操作</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看目录内容</span></span><br><span class="line"><span class="built_in">ls</span>                    <span class="comment"># 列出当前目录文件</span></span><br><span class="line"><span class="built_in">ls</span> -l                 <span class="comment"># 详细列表（显示权限、大小、修改时间）</span></span><br><span class="line"><span class="built_in">ls</span> -a                 <span class="comment"># 显示隐藏文件（以.开头的文件）</span></span><br><span class="line"><span class="built_in">ls</span> -lh                <span class="comment"># 以人类可读方式显示大小（K、M、G）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 目录切换</span></span><br><span class="line"><span class="built_in">cd</span> /usr/local         <span class="comment"># 进入指定目录</span></span><br><span class="line"><span class="built_in">cd</span> ~                  <span class="comment"># 进入home目录</span></span><br><span class="line"><span class="built_in">cd</span> -                  <span class="comment"># 返回上一个目录</span></span><br><span class="line"><span class="built_in">cd</span> ..                 <span class="comment"># 返回上级目录</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建和删除</span></span><br><span class="line"><span class="built_in">mkdir</span> myapp           <span class="comment"># 创建目录</span></span><br><span class="line"><span class="built_in">mkdir</span> -p a/b/c        <span class="comment"># 递归创建多级目录</span></span><br><span class="line"><span class="built_in">touch</span> README.md       <span class="comment"># 创建空文件</span></span><br><span class="line"><span class="built_in">cp</span> app.conf app.conf.bak    <span class="comment"># 复制文件</span></span><br><span class="line"><span class="built_in">cp</span> -r webapp/ webapp_bak/   <span class="comment"># 递归复制目录</span></span><br><span class="line"><span class="built_in">mv</span> app.conf /tmp/     <span class="comment"># 移动文件</span></span><br><span class="line"><span class="built_in">mv</span> oldname newname    <span class="comment"># 重命名文件</span></span><br><span class="line"><span class="built_in">rm</span> temp.txt           <span class="comment"># 删除文件（不会进回收站，直接删）</span></span><br><span class="line"><span class="built_in">rm</span> -rf logs/          <span class="comment"># 递归强制删除目录（慎用！）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查找文件</span></span><br><span class="line">find / -name <span class="string">&quot;*.log&quot;</span>                   <span class="comment"># 从根目录找所有.log文件</span></span><br><span class="line">find /var/log -mtime -7                <span class="comment"># 查找7天内修改过的文件</span></span><br><span class="line">find . -size +100M                     <span class="comment"># 查找当前目录下大于100M的文件</span></span><br><span class="line">find . -name <span class="string">&quot;*.tmp&quot;</span> -<span class="built_in">exec</span> <span class="built_in">rm</span> &#123;&#125; \;    <span class="comment"># 查找并删除临时文件</span></span><br></pre></td></tr></table></figure>

<p><strong>运维场景：查找并清理大文件</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查找根目录下大于500M的文件，按大小排序</span></span><br><span class="line">find / -<span class="built_in">type</span> f -size +500M -<span class="built_in">exec</span> <span class="built_in">ls</span> -lh &#123;&#125; \; 2&gt;/dev/null | awk <span class="string">&#x27;&#123;print $5, $NF&#125;&#x27;</span> | <span class="built_in">sort</span> -rh</span><br><span class="line"></span><br><span class="line"><span class="comment"># 清理7天前的日志文件</span></span><br><span class="line">find /var/log/myapp/ -name <span class="string">&quot;*.log&quot;</span> -mtime +7 -<span class="built_in">exec</span> <span class="built_in">rm</span> &#123;&#125; \;</span><br></pre></td></tr></table></figure>

<h3 id="二、文件查看与搜索"><a href="#二、文件查看与搜索" class="headerlink" title="二、文件查看与搜索"></a>二、文件查看与搜索</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看文件内容</span></span><br><span class="line"><span class="built_in">cat</span> README.md               <span class="comment"># 显示整个文件（适合小文件）</span></span><br><span class="line"><span class="built_in">cat</span> -n app.log              <span class="comment"># 显示行号</span></span><br><span class="line">less app.log                <span class="comment"># 分页查看（按空格翻页，按q退出）</span></span><br><span class="line"><span class="built_in">head</span> -20 app.log            <span class="comment"># 查看前20行</span></span><br><span class="line"><span class="built_in">tail</span> -100 app.log           <span class="comment"># 查看后100行</span></span><br><span class="line"><span class="built_in">tail</span> -f app.log             <span class="comment"># 实时跟踪文件新增内容（看日志神器！）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 搜索文件内容</span></span><br><span class="line">grep <span class="string">&quot;ERROR&quot;</span> app.log                      <span class="comment"># 在文件中搜索关键字</span></span><br><span class="line">grep -i <span class="string">&quot;error&quot;</span> app.log                   <span class="comment"># 忽略大小写搜索</span></span><br><span class="line">grep -r <span class="string">&quot;NullPointerException&quot;</span> /logs/     <span class="comment"># 递归搜索目录下所有文件</span></span><br><span class="line">grep -n <span class="string">&quot;ERROR&quot;</span> app.log                   <span class="comment"># 显示匹配行及其行号</span></span><br><span class="line">grep -c <span class="string">&quot;ERROR&quot;</span> app.log                   <span class="comment"># 统计匹配行数</span></span><br><span class="line">grep -A5 <span class="string">&quot;ERROR&quot;</span> app.log                  <span class="comment"># 显示匹配行及其后5行</span></span><br><span class="line">grep -B5 <span class="string">&quot;ERROR&quot;</span> app.log                  <span class="comment"># 显示匹配行及其前5行</span></span><br><span class="line">grep -C5 <span class="string">&quot;ERROR&quot;</span> app.log                  <span class="comment"># 显示匹配行及其前后各5行</span></span><br></pre></td></tr></table></figure>

<p><strong>运维场景：查看日志中的错误信息</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看今天出现的所有ERROR及上下文</span></span><br><span class="line"><span class="built_in">tail</span> -10000 app.log | grep -C5 <span class="string">&quot;ERROR&quot;</span> | less</span><br><span class="line"></span><br><span class="line"><span class="comment"># 统计各错误类型出现次数</span></span><br><span class="line">grep -o <span class="string">&quot;ERROR \[.*\]&quot;</span> app.log | <span class="built_in">sort</span> | <span class="built_in">uniq</span> -c | <span class="built_in">sort</span> -rn</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看某个时间段内的日志</span></span><br><span class="line">grep <span class="string">&quot;2018-11-18 10:&quot;</span> app.log | less</span><br></pre></td></tr></table></figure>

<h3 id="三、权限管理"><a href="#三、权限管理" class="headerlink" title="三、权限管理"></a>三、权限管理</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 修改权限</span></span><br><span class="line"><span class="built_in">chmod</span> 755 script.sh          <span class="comment"># 设置权限为rwxr-xr-x</span></span><br><span class="line"><span class="built_in">chmod</span> +x script.sh           <span class="comment"># 给文件加执行权限</span></span><br><span class="line"><span class="built_in">chmod</span> -R 644 html/           <span class="comment"># 递归设置目录下所有文件权限</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 修改所有者</span></span><br><span class="line"><span class="built_in">chown</span> admin app.log          <span class="comment"># 修改文件所有者为admin</span></span><br><span class="line"><span class="built_in">chown</span> admin:admin app.log    <span class="comment"># 同时修改所有者和所属组</span></span><br><span class="line"><span class="built_in">chown</span> -R admin:admin /app/   <span class="comment"># 递归修改目录下所有文件所有者</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看文件权限</span></span><br><span class="line"><span class="built_in">ls</span> -l                        <span class="comment"># 查看文件的权限信息</span></span><br><span class="line"><span class="built_in">stat</span> app.conf                <span class="comment"># 查看文件的详细状态（包括权限）</span></span><br></pre></td></tr></table></figure>

<p><strong>运维场景：部署项目后修复权限</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 项目部署后的权限设置</span></span><br><span class="line"><span class="built_in">chown</span> -R www:www /var/www/myapp/          <span class="comment"># 设置为www用户</span></span><br><span class="line">find /var/www/myapp/ -<span class="built_in">type</span> f -<span class="built_in">exec</span> <span class="built_in">chmod</span> 644 &#123;&#125; \;   <span class="comment"># 文件设置为644</span></span><br><span class="line">find /var/www/myapp/ -<span class="built_in">type</span> d -<span class="built_in">exec</span> <span class="built_in">chmod</span> 755 &#123;&#125; \;   <span class="comment"># 目录设置为755</span></span><br></pre></td></tr></table></figure>

<h3 id="四、用户管理"><a href="#四、用户管理" class="headerlink" title="四、用户管理"></a>四、用户管理</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建用户</span></span><br><span class="line">useradd tom                    <span class="comment"># 创建用户tom</span></span><br><span class="line">passwd tom                     <span class="comment"># 设置用户tom的密码</span></span><br><span class="line">useradd -m -g www dev          <span class="comment"># 创建用户dev，加入www组并创建home目录</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看用户</span></span><br><span class="line"><span class="built_in">who</span>                            <span class="comment"># 查看当前登录用户</span></span><br><span class="line">w                              <span class="comment"># 查看更详细的登录信息</span></span><br><span class="line"><span class="built_in">id</span> tom                         <span class="comment"># 查看用户tom的UID和GID</span></span><br><span class="line"><span class="built_in">groups</span>                         <span class="comment"># 查看当前用户所属组</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 切换用户</span></span><br><span class="line">su - tom                       <span class="comment"># 切换到tom用户</span></span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">command</span>                   <span class="comment"># 以root身份执行命令</span></span><br></pre></td></tr></table></figure>

<h3 id="五、进程管理"><a href="#五、进程管理" class="headerlink" title="五、进程管理"></a>五、进程管理</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看进程</span></span><br><span class="line">ps -ef                         <span class="comment"># 查看所有进程的详细信息</span></span><br><span class="line">ps aux                         <span class="comment"># 查看所有进程（CPU/内存占用）</span></span><br><span class="line">ps -ef | grep java             <span class="comment"># 查看Java相关进程</span></span><br><span class="line">top                            <span class="comment"># 实时查看进程状态（按q退出）</span></span><br><span class="line">top -bn1                       <span class="comment"># 一次性输出top结果（适合脚本采集）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 终止进程</span></span><br><span class="line"><span class="built_in">kill</span> 12345                     <span class="comment"># 优雅终止PID为12345的进程</span></span><br><span class="line"><span class="built_in">kill</span> -9 12345                  <span class="comment"># 强制杀掉PID为12345的进程</span></span><br><span class="line">killall java                   <span class="comment"># 杀掉所有Java进程</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 后台运行</span></span><br><span class="line"><span class="built_in">nohup</span> java -jar app.jar &amp;      <span class="comment"># 后台运行Java应用，退出终端不停止</span></span><br><span class="line"><span class="built_in">jobs</span>                           <span class="comment"># 查看后台运行的任务列表</span></span><br><span class="line"><span class="built_in">fg</span> %1                          <span class="comment"># 将后台任务1调回前台</span></span><br></pre></td></tr></table></figure>

<p><strong>运维场景：重启 Java 应用</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 完整的应用重启流程</span></span><br><span class="line">ps -ef | grep app.jar | grep -v grep  <span class="comment"># 先找到进程</span></span><br><span class="line"><span class="built_in">kill</span> 12345                             <span class="comment"># 优雅停止</span></span><br><span class="line"><span class="built_in">sleep</span> 3                                <span class="comment"># 等待3秒</span></span><br><span class="line"><span class="built_in">nohup</span> java -jar app.jar &gt; app.log 2&gt;&amp;1 &amp;   <span class="comment"># 后台启动，日志重定向</span></span><br><span class="line"><span class="built_in">tail</span> -f app.log                       <span class="comment"># 实时查看启动日志</span></span><br></pre></td></tr></table></figure>

<h3 id="六、磁盘管理"><a href="#六、磁盘管理" class="headerlink" title="六、磁盘管理"></a>六、磁盘管理</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看磁盘整体情况</span></span><br><span class="line"><span class="built_in">df</span> -h                          <span class="comment"># 以人类可读格式查看磁盘使用情况</span></span><br><span class="line"><span class="built_in">df</span> -h /dev/sda1                <span class="comment"># 查看指定分区</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看目录大小</span></span><br><span class="line"><span class="built_in">du</span> -sh /var/log/               <span class="comment"># 查看目录总大小</span></span><br><span class="line"><span class="built_in">du</span> -sh *                       <span class="comment"># 查看当前目录下每个子目录的大小</span></span><br><span class="line"><span class="built_in">du</span> -h --max-depth=1 /home/     <span class="comment"># 只看一层目录的大小</span></span><br><span class="line"><span class="built_in">du</span> -sh /* | <span class="built_in">sort</span> -rh           <span class="comment"># 查看根目录下各目录大小并排序</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 磁盘空间排查</span></span><br><span class="line"><span class="built_in">du</span> -sh /var/log/               <span class="comment"># 检查日志目录大小</span></span><br><span class="line"><span class="built_in">du</span> -sh /tmp/                   <span class="comment"># 检查临时目录大小</span></span><br><span class="line"><span class="built_in">du</span> -sh /opt/                   <span class="comment"># 检查应用目录大小</span></span><br></pre></td></tr></table></figure>

<p><strong>运维场景：磁盘告警排查</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 第一步：查看整体使用情况</span></span><br><span class="line"><span class="built_in">df</span> -h</span><br><span class="line"></span><br><span class="line"><span class="comment"># 第二步：找到最大的目录</span></span><br><span class="line"><span class="built_in">du</span> -sh /* | <span class="built_in">sort</span> -rh | <span class="built_in">head</span> -10</span><br><span class="line"></span><br><span class="line"><span class="comment"># 第三步：逐层深入找到大文件</span></span><br><span class="line"><span class="built_in">du</span> -sh /var/* | <span class="built_in">sort</span> -rh | <span class="built_in">head</span> -5</span><br><span class="line"><span class="built_in">du</span> -sh /var/log/* | <span class="built_in">sort</span> -rh | <span class="built_in">head</span> -5</span><br><span class="line"></span><br><span class="line"><span class="comment"># 第四步：清理日志</span></span><br><span class="line">find /var/log/ -name <span class="string">&quot;*.log&quot;</span> -mtime +30 -<span class="built_in">exec</span> <span class="built_in">rm</span> &#123;&#125; \;</span><br></pre></td></tr></table></figure>

<h3 id="七、网络管理"><a href="#七、网络管理" class="headerlink" title="七、网络管理"></a>七、网络管理</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看端口</span></span><br><span class="line">netstat -tlnp                  <span class="comment"># 查看所有TCP监听端口及对应进程</span></span><br><span class="line">netstat -tlnp | grep 8080      <span class="comment"># 查看8080端口被谁占用</span></span><br><span class="line">netstat -an | grep ESTABLISHED <span class="comment"># 查看已建立的连接</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># lsof 查看端口（更直观）</span></span><br><span class="line">lsof -i :8080                  <span class="comment"># 查看8080端口被哪个进程占用</span></span><br><span class="line">lsof -i tcp                    <span class="comment"># 查看所有TCP连接</span></span><br><span class="line">lsof -p 12345                  <span class="comment"># 查看进程12345打开了哪些文件</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 网络连通性</span></span><br><span class="line">ping -c 4 baidu.com            <span class="comment"># 测试网络连通性，发4个包</span></span><br><span class="line">curl -I http://localhost:8080   <span class="comment"># 查看HTTP响应头</span></span><br><span class="line">telnet 192.168.1.1 3306        <span class="comment"># 测试端口是否可达</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># DNS相关</span></span><br><span class="line">nslookup www.baidu.com         <span class="comment"># 域名解析查询</span></span><br><span class="line">host www.baidu.com             <span class="comment"># 简化的域名查询</span></span><br></pre></td></tr></table></figure>

<p><strong>运维场景：端口被占用了怎么办</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 场景：Tomcat启动报错 Address already in use</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 步骤1：查看端口被谁占了</span></span><br><span class="line">netstat -tlnp | grep 8080</span><br><span class="line"><span class="comment"># 输出：tcp6  0  0 :::8080  :::*  LISTEN  12345/java</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 步骤2：确认是这个进程，可以选择：</span></span><br><span class="line"><span class="comment"># 方案A：杀掉占用进程</span></span><br><span class="line"><span class="built_in">kill</span> 12345</span><br><span class="line"></span><br><span class="line"><span class="comment"># 方案B：或者换个端口启动，如果杀不掉的话</span></span><br></pre></td></tr></table></figure>

<h3 id="八、压缩解压"><a href="#八、压缩解压" class="headerlink" title="八、压缩解压"></a>八、压缩解压</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># tar 打包/解包</span></span><br><span class="line">tar -czf myapp.tar.gz /var/www/myapp/      <span class="comment"># 打包并压缩为.tar.gz</span></span><br><span class="line">tar -xzf myapp.tar.gz                      <span class="comment"># 解压.tar.gz包</span></span><br><span class="line">tar -tzf myapp.tar.gz                      <span class="comment"># 查看包内容列表（不实际解压）</span></span><br><span class="line">tar -xjf myapp.tar.bz2                     <span class="comment"># 解压.tar.bz2包</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># gzip压缩</span></span><br><span class="line">gzip app.log                               <span class="comment"># 压缩文件（生成app.log.gz）</span></span><br><span class="line">gzip -d app.log.gz                         <span class="comment"># 解压缩</span></span><br><span class="line">gunzip app.log.gz                          <span class="comment"># 同上，解压缩</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># zip（跨平台常用）</span></span><br><span class="line">zip -r myapp.zip /var/www/myapp/           <span class="comment"># 压缩目录</span></span><br><span class="line">unzip myapp.zip -d /tmp/                   <span class="comment"># 解压到指定目录</span></span><br></pre></td></tr></table></figure>

<p><strong>运维场景：备份项目并压缩</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 备份项目目录，按日期命名</span></span><br><span class="line">tar -czf myapp_$(<span class="built_in">date</span> +%Y%m%d_%H%M%S).tar.gz /var/www/myapp/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 解压到指定目录</span></span><br><span class="line">tar -xzf myapp_20181118.tar.gz -C /tmp/restore/</span><br></pre></td></tr></table></figure>

<h3 id="九、vim-基本操作"><a href="#九、vim-基本操作" class="headerlink" title="九、vim 基本操作"></a>九、vim 基本操作</h3><p>vim 刚接触时确实让人抓狂，记住下面这些就够日常用了：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 打开文件</span></span><br><span class="line">vim app.conf               <span class="comment"># 打开文件</span></span><br><span class="line">vim +100 app.conf          <span class="comment"># 打开文件并定位到第100行</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 命令模式（刚进入vim时的状态）</span></span><br><span class="line">i                          <span class="comment"># 在光标前插入（进入插入模式）</span></span><br><span class="line">a                          <span class="comment"># 在光标后插入</span></span><br><span class="line"><span class="built_in">dd</span>                         <span class="comment"># 删除当前行</span></span><br><span class="line">5dd                        <span class="comment"># 删除从当前行开始的5行</span></span><br><span class="line">yy                         <span class="comment"># 复制当前行</span></span><br><span class="line">p                          <span class="comment"># 粘贴</span></span><br><span class="line">u                          <span class="comment"># 撤销</span></span><br><span class="line">Ctrl + r                   <span class="comment"># 重做（取消撤销）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 末行模式（按:进入）</span></span><br><span class="line">:w                         <span class="comment"># 保存</span></span><br><span class="line">:q                         <span class="comment"># 退出</span></span><br><span class="line">:wq                        <span class="comment"># 保存并退出</span></span><br><span class="line">:q!                        <span class="comment"># 不保存强制退出</span></span><br><span class="line">:%s/旧文本/新文本/g        <span class="comment"># 全文替换</span></span><br><span class="line">:<span class="built_in">set</span> nu                    <span class="comment"># 显示行号</span></span><br></pre></td></tr></table></figure>

<h3 id="十、组合命令与管道"><a href="#十、组合命令与管道" class="headerlink" title="十、组合命令与管道"></a>十、组合命令与管道</h3><p>管道 <code>|</code> 是 Linux 命令的精髓，它把前一个命令的输出作为后一个命令的输入，像流水线一样：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 管道的基本使用</span></span><br><span class="line">ps -ef | grep java              <span class="comment"># 在进程列表中搜索Java进程</span></span><br><span class="line">netstat -tlnp | grep 80         <span class="comment"># 在网络连接中搜索80端口</span></span><br><span class="line"><span class="built_in">cat</span> app.log | grep ERROR | <span class="built_in">head</span> -20    <span class="comment"># 在日志中找错误，只看前20条</span></span><br><span class="line"><span class="built_in">ls</span> -l | grep <span class="string">&quot;^d&quot;</span>               <span class="comment"># 只列出目录（行首为d的是目录）</span></span><br><span class="line"><span class="built_in">ls</span> -l | awk <span class="string">&#x27;&#123;print $5, $NF&#125;&#x27;</span>   <span class="comment"># 只显示文件大小和名称</span></span><br><span class="line"><span class="built_in">ls</span> -l | <span class="built_in">sort</span> -k5 -rn | <span class="built_in">head</span> -5  <span class="comment"># 列出当前目录最大的5个文件</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 输出重定向</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;Hello&quot;</span> &gt; README.md        <span class="comment"># 覆盖写入文件</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;World&quot;</span> &gt;&gt; README.md       <span class="comment"># 追加写入文件</span></span><br><span class="line"><span class="built_in">ls</span> /nonexist 2&gt; error.log       <span class="comment"># 将错误信息重定向到文件</span></span><br><span class="line">java -jar app.jar &gt; app.log 2&gt;&amp;1 &amp;    <span class="comment"># 标准输出和错误都输出到日志文件</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 实际运维组合命令示例</span></span><br><span class="line"><span class="comment"># 找出占用CPU最高的进程</span></span><br><span class="line">top -bn1 | <span class="built_in">head</span> -20</span><br><span class="line"></span><br><span class="line"><span class="comment"># 统计日志中各IP的访问次数</span></span><br><span class="line"><span class="built_in">cat</span> access.log | awk <span class="string">&#x27;&#123;print $1&#125;&#x27;</span> | <span class="built_in">sort</span> | <span class="built_in">uniq</span> -c | <span class="built_in">sort</span> -rn | <span class="built_in">head</span> -10</span><br><span class="line"></span><br><span class="line"><span class="comment"># 批量替换配置文件中的IP</span></span><br><span class="line">find /app/config/ -name <span class="string">&quot;*.properties&quot;</span> | xargs sed -i <span class="string">&#x27;s/192.168.1.100/192.168.2.100/g&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 压缩30天前的日志</span></span><br><span class="line">find /var/log/myapp/ -name <span class="string">&quot;*.log&quot;</span> -mtime +30 | xargs gzip</span><br></pre></td></tr></table></figure>


<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>两周的密集使用下来，从最初在终端面前手足无措，到能比较顺畅地完成日常运维操作，有一些心得想分享给和我一样从 Windows 转 Linux 的同学：</p>
<p><strong>1. Linux 命令不需要死记硬背</strong></p>
<p>命令太多了，没有人能记住所有命令的所有参数。关键是要知道<strong>解决某个问题可以用什么命令</strong>，具体的参数用法用 <code>man</code> 或 <code>--help</code> 现查就行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">man <span class="built_in">ls</span>                 <span class="comment"># 查看ls的完整手册（按q退出）</span></span><br><span class="line"><span class="built_in">ls</span> --<span class="built_in">help</span>              <span class="comment"># 查看ls的快速帮助（推荐，更直观）</span></span><br></pre></td></tr></table></figure>

<p>我基本上每天都要查几次 <code>tar</code> 的参数——“压缩是 <code>-czf</code> 还是 <code>-xzf</code> 来着”——这很正常，查多了自然就记住了。</p>
<p><strong>2. 管道是 Linux 的精髓</strong></p>
<p>管道 <code>|</code> 让我真正感受到了 Linux 命令行的威力。单个命令功能有限，但组合起来能做很复杂的事情。就像搭积木一样，把一个个小命令拼起来解决问题。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 学会了管道之后，就能写出这样的命令来解决复杂问题</span></span><br><span class="line">ps -ef | grep java | grep -v grep | awk <span class="string">&#x27;&#123;print $2&#125;&#x27;</span> | xargs <span class="built_in">kill</span> -9</span><br></pre></td></tr></table></figure>

<p><strong>3. 多用 alias 简化常用操作</strong></p>
<p>把常用的长命令设成别名，能省不少事：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">alias</span> ll=<span class="string">&#x27;ls -alh&#x27;</span></span><br><span class="line"><span class="built_in">alias</span> g=<span class="string">&#x27;grep --color=auto&#x27;</span></span><br><span class="line"><span class="built_in">alias</span> ..=<span class="string">&#x27;cd ..&#x27;</span></span><br><span class="line"><span class="comment"># 写入 .bashrc 后永久生效</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;alias ll=&#x27;ls -alh&#x27;&quot;</span> &gt;&gt; ~/.bashrc</span><br><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br></pre></td></tr></table></figure>

<p><strong>4. 小心 rm -rf</strong></p>
<p><code>rm -rf</code> 是 Linux 上最危险的命令之一。没有回收站，没有二次确认，文件直接就没了。使用前一定要确认路径是否正确，尤其是在 root 用户下。建议养成习惯：</p>
<ul>
<li>执行 <code>rm</code> 前先 <code>ls</code> 确认一下</li>
<li>重要的文件先备份</li>
<li>用 <code>mv</code> 代替 <code>rm</code>，把文件移到 <code>/tmp/</code> 目录（系统定期会清理）</li>
</ul>
<p><strong>5. 多敲多练是最好的学习方式</strong></p>
<p>读十遍这篇文章不如自己在服务器上敲一遍。我在自己的电脑上装了虚拟机，把上面的命令都练了一遍。遇到实际运维问题时，先百度&#x2F;Google，解决了就记下来，慢慢就形成了自己的命令速查手册。</p>
<p>现在回头看，Linux 命令行没有想象的那么可怕。它只是和 Windows 不同的另一种工作方式，适应了之后甚至会喜欢上这种高效的操作。希望这篇文章对同样刚接触 Linux 的你有所帮助。</p>
<hr>
<p><em>这篇文章发布于 2018 年 11 月，是我刚接触 Linux 时的学习笔记。命令在不同发行版上可能略有差异，文中以 CentOS 7.4 为准。</em></p>
]]></content>
      <categories>
        <category>Linux运维</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Shell</tag>
        <tag>运维</tag>
        <tag>命令行</tag>
        <tag>服务器</tag>
      </tags>
  </entry>
  <entry>
    <title>2018年度学习总结</title>
    <url>/2018/12/30/2018-12-30-2018%E5%B9%B4%E5%BA%A6%E5%AD%A6%E4%B9%A0%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p>现在是 2018 年 12 月 30 日深夜，窗外很安静，桌面上摊着几本书、一杯凉掉的咖啡、开着 IDE 的屏幕。刚刚跑通了一个 Spring 的小 Demo，不算什么了不起的东西，但我盯着控制台里那几行日志，愣是坐了好一会儿。</p>
<p>总觉得该写点什么，为这一年。</p>
<p>回头看，2018 年是我真正踏进编程世界的第一年。从连编辑器都不会配，到能用 Java 写点能跑的东西，这三百多天走得七拐八拐，中间有好几次差点放弃。趁着跨年前这个安静的夜晚，把这一年的路好好捋一捋。</p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>2018 年 1 月，我坐在电脑前打开了一本 C 语言的电子书，那是我和编程的第一次正式见面。</p>
<p>在此之前，我对编程的全部认知大概就是——很酷、很复杂、不适合我。高考报志愿时阴差阳错进了计算机相关专业，大一上学期懵懵懂懂地过了，真正开始认真对待，就是从 2018 年开始的。</p>
<p>最开始学的是 C 语言，指针、内存管理、结构体——说实话，学得挺痛苦的。尤其是指针，绕来绕去，debug 的时候经常对着一个段错误发呆半小时。但也正因为 C 的硬核，让我对内存、对程序的底层运行有了一点感觉。</p>
<p>到了三四月份，我开始纠结一个问题：C 语言当然好，但它适合作为长期深耕的方向吗？当时打听了不少学长，也刷了很多帖子，结论是——Java 在后端领域生态成熟、岗位多、学习路径清晰。于是我做了一个重要的决定：转学 Java。</p>
<p>这个决定，基本定义了我整个 2018 年剩下的路。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>目标定下来之后，真正走起来才发现，问题一个接一个。</p>
<h2 id="1-学习路线不清晰，走了不少弯路"><a href="#1-学习路线不清晰，走了不少弯路" class="headerlink" title="1. 学习路线不清晰，走了不少弯路"></a>1. 学习路线不清晰，走了不少弯路</h2><p>网上关于 Java 学习的资料和路线图很多，但信息多到让人不知道该信谁。今天看到一个博客说先学 Java SE，明天又有人说直接上手 Spring Boot。我是那种喜欢把”所有前置知识”都学完再做事情的人，结果就是在基础语法阶段兜兜转转了太久。</p>
<h2 id="2-光看不练，眼高手低"><a href="#2-光看不练，眼高手低" class="headerlink" title="2. 光看不练，眼高手低"></a>2. 光看不练，眼高手低</h2><p>这是我这一年最大的毛病。</p>
<p>看书的时候觉得自己都懂了——不就是面向对象吗，不就是集合框架吗，有什么难的。但是合上书，打开 IDE，写一个简单的增删改查都要查半天 API。印象最深的是学完 HashMap 原理之后，自己试着实现一个简化版本，写到一半发现根本不知道从哪里下手。这才意识到，”看懂”和”会写”之间隔着十万行代码的距离。</p>
<h2 id="3-遇到问题不敢问"><a href="#3-遇到问题不敢问" class="headerlink" title="3. 遇到问题不敢问"></a>3. 遇到问题不敢问</h2><p>当时加了一些技术群，也在论坛上潜水。遇到问题第一反应不是去问，而是反复怀疑自己是不是太笨了——这么基础的东西都不懂。于是经常一个人卡在一个小 bug 上几个小时，浪费大量时间。后来才明白，每个经历过自学的人都是从这些坑里爬出来的，问问题不是丢人的事。</p>
<h2 id="4-博客看得多，写得少"><a href="#4-博客看得多，写得少" class="headerlink" title="4. 博客看得多，写得少"></a>4. 博客看得多，写得少</h2><p>我的浏览器书签里收藏了上百篇文章，从 HashMap 源码分析到 JVM 内存模型，应有尽有。每天刷好几篇，刷完觉得充实。但一个月后回头看，脑子里能留下的印象很模糊。因为光看不写，知识的留存率太低了。</p>
<h2 id="5-时间管理不好，经常拖延"><a href="#5-时间管理不好，经常拖延" class="headerlink" title="5. 时间管理不好，经常拖延"></a>5. 时间管理不好，经常拖延</h2><p>“今天太累了，明天再看吧”——这句话几乎是我上半年的口头禅。没有固定的学习时间，没有计划，全凭热情驱动。热情高的时候一天学八个小时，没状态的时候一周都不碰代码。这种节奏非常低效。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>后来我慢慢想明白了这些问题的根因，也在下半年做了一些调整。</p>
<h2 id="学习规划的重要性"><a href="#学习规划的重要性" class="headerlink" title="学习规划的重要性"></a>学习规划的重要性</h2><p>没有规划的学习就像没有地图的旅行。知识海洋是无限的，但个人的时间和精力是有限的。后来我给自己定了一条原则：选定一个方向，找到一条被验证过的路线，先踏踏实实走完，不要中途反复切换赛道。</p>
<h2 id="编程是手艺活"><a href="#编程是手艺活" class="headerlink" title="编程是手艺活"></a>编程是手艺活</h2><p>我越来越理解一个道理：编程像学游泳，你看再多教学视频，不下水就永远学不会。代码必须亲手敲，bug 必须亲自查，项目必须亲手做。任何一个你”觉得”懂了但写不出来的知识点，都说明你还没真正掌握它。</p>
<p>后来我强迫自己每学一个新知识点，就写一段代码验证它，哪怕只是一个十行的小 demo。</p>
<h2 id="社区和交流的价值"><a href="#社区和交流的价值" class="headerlink" title="社区和交流的价值"></a>社区和交流的价值</h2><p>下半年我开始尝试在群里发言，偶尔回答一些别人提的问题。我发现教是最好的学——为了回答一个问题，你往往需要把那个知识点重新梳理一遍，这个过程本身就是一次高质量复习。而且当你发现别人也会犯和你一样的错误时，那种”只有我不行”的焦虑感会少很多。</p>
<h2 id="写作是深度学习的加速器"><a href="#写作是深度学习的加速器" class="headerlink" title="写作是深度学习的加速器"></a>写作是深度学习的加速器</h2><p>十月份开始写博客之后，我明显感觉到自己对知识的理解深了一层。因为要把一个东西讲清楚，你必须先把它真正搞懂。写作强迫你梳理逻辑、查漏补缺、用自己的语言重新表达。这个过程比单纯地读十篇文章都有效。</p>
<h2 id="固定学习节奏"><a href="#固定学习节奏" class="headerlink" title="固定学习节奏"></a>固定学习节奏</h2><p>下半年我开始用番茄工作法，每 25 分钟专注学习，然后休息 5 分钟。同时给自己定了规矩：每天晚上 8 点到 10 点是固定学习时间，雷打不动。效果比”等我有时间了再学”好太多。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>说是解决方案，其实没有那么多方法论。就是一个普通人在跌跌撞撞中，按季度一步步走过来的真实记录。</p>
<h2 id="Q1（1-3-月）：C-语言入门，决定转-Java"><a href="#Q1（1-3-月）：C-语言入门，决定转-Java" class="headerlink" title="Q1（1-3 月）：C 语言入门，决定转 Java"></a>Q1（1-3 月）：C 语言入门，决定转 Java</h2><p>年初从 C 语言开始，把指针、数组、结构体这些基础概念过了一遍。过程中有过一点小小的成就感——当第一个指针程序运行正确的时候，那种感觉确实很奇妙。但也逐渐意识到，C 语言在应用层面的天花板比较低，对我来说更适合作为理解计算机底层的基础，而不是长期主攻方向。</p>
<p>三月份做了全年最重要的决定：转向 Java。</p>
<p>这个决定不是一时冲动。当时花了一周时间调研，看了很多自学路线帖和招聘要求，确认 Java 在服务端开发中的地位和前景。确定了方向之后，整个人的心态反而安定下来了——不再东张西望，就朝这一个方向走。</p>
<p>接着就是搭环境。JDK 安装、环境变量配置、第一个 IDE（Eclipse）的安装和使用。这些现在看来基础得不能再基础的操作，当时每一步都可能卡住。JDK 版本选哪个？环境变量配不对怎么办？Eclipse 为什么打不开？每一个问题都要查好久。</p>
<p>当 Hello World 终于出现在控制台的时候，我截图发了个朋友圈。虽然现在想来有点好笑，但那一刻真的觉得——这个大门，我算是挤进来了。</p>
<h2 id="Q2（4-6-月）：Java-语法基础"><a href="#Q2（4-6-月）：Java-语法基础" class="headerlink" title="Q2（4-6 月）：Java 语法基础"></a>Q2（4-6 月）：Java 语法基础</h2><p>第二季度是大量输入基础知识的时候。</p>
<p>从基本数据类型、运算符、流程控制开始，然后是面向对象三大特性——封装、继承、多态。这些东西第一次接触时确实觉得抽象，尤其是多态，学完之后做题会做，但问自己”多态在实际项目中有什么用”就答不上来。后来才明白，有些东西的理解是分阶段的——先记住，然后在使用中慢慢感受。</p>
<p>五月份学到集合框架。ArrayList、LinkedList、HashSet、HashMap——每个实现类都过了一遍，大概理解了接口和实现分离的设计思想。</p>
<p>这三个月基本是靠看书 + 看视频 + 敲 demo 过来的。节奏不算快，但比较扎实。每学完一个章节，就把书上的代码亲手敲一遍，改一改，看输出有什么变化。</p>
<h2 id="Q3（7-9-月）：深入-Java，完成第一个小项目"><a href="#Q3（7-9-月）：深入-Java，完成第一个小项目" class="headerlink" title="Q3（7-9 月）：深入 Java，完成第一个小项目"></a>Q3（7-9 月）：深入 Java，完成第一个小项目</h2><p>暑假是全年学习效率最高的一段时间。</p>
<p>七月啃 HashMap。那时候看了好几篇源码分析的文章，自己也把源码拉下来断点跟踪。虽然当时对红黑树的理解还很浅，但至少搞清楚了哈希冲突怎么解决、扩容机制是怎么回事。也在这个月第一次写博客——<a href="https://chenzixin.github.io/2018/07/22/HashMap%E5%9F%BA%E7%A1%80%E5%AD%A6%E4%B9%A0/">HashMap 基础学习</a>，写得很浅，但迈出了第一步。</p>
<p>八月学异常处理机制，写了 <a href="https://chenzixin.github.io/2018/08/19/Java%E5%BC%82%E5%B8%B8%E5%A4%84%E7%90%86%E6%9C%BA%E5%88%B6/">Java 异常处理机制</a>。然后开始学 JDBC，这是第一次把 Java 和数据库联系起来，写了篇文章 <a href="https://chenzixin.github.io/2018/09/15/JDBC%E8%BF%9E%E6%8E%A5MySQL%E5%AE%9E%E8%B7%B5/">JDBC 连接 MySQL 实践</a>。</p>
<p>九月最重要的事情是完成了第一个完整的 Java 小项目——学生管理系统。</p>
<p>说是系统，其实就是一个控制台程序：从 MySQL 里读取学生数据，展示在控制台上，支持增删改查。技术上没什么难度，但对我来说意义很大——我第一次经历了”从零开始分析需求、设计数据结构、写代码、调试、最终跑通”的完整流程。也正是在这个项目里，我初次感受到了写代码的快感。</p>
<h2 id="Q4（10-12-月）：数据库、Linux、Spring-入门"><a href="#Q4（10-12-月）：数据库、Linux、Spring-入门" class="headerlink" title="Q4（10-12 月）：数据库、Linux、Spring 入门"></a>Q4（10-12 月）：数据库、Linux、Spring 入门</h2><p>第四季度开始进一步拓展广度。</p>
<p>MySQL 部分不再满足于基本的 CRUD，开始深入学习表设计、索引、事务隔离级别。也正是在这个过程中，我才理解了为什么三范式、为什么加索引、什么时候用 InnoDB 什么时候用 MyISAM。</p>
<p>十一月开始接触 Linux。装了一个虚拟机，装 CentOS，学着用命令行操作文件、配置网络、装软件。一开始觉得没有图形界面很不习惯，但后来慢慢体会到了命令行的高效。这期间也学会了搭 Nginx 和部署简单项目。</p>
<p>十二月的关键词是 Spring。终于在年底正式接触到了这个如雷贯耳的框架。从 IoC（控制反转）和 DI（依赖注入）的概念开始，慢慢理解为什么 Spring 能让代码变得松耦合。十二月中旬跑通了第一个 Spring 项目，虽然只是配置了几个 Bean，做了最简单的依赖注入，但那种”原来框架是这样工作的”的感觉让我兴奋了好几天。</p>
<h2 id="2019-年计划"><a href="#2019-年计划" class="headerlink" title="2019 年计划"></a>2019 年计划</h2><p>坐在 2018 年的尾巴上，对明年有一些想法：</p>
<ul>
<li><strong>框架学习</strong>：把 SSM（Spring + SpringMVC + MyBatis）这个组合啃下来，搭建一个完整的 Web 项目</li>
<li><strong>Redis</strong>：学习缓存，理解 NoSQL 的适用场景</li>
<li><strong>项目实战</strong>：不再满足于 demo 和小工具，至少做一个拿得出手的完整项目</li>
<li><strong>坚持写博客</strong>：把学到的东西沉淀下来，不管写得好不好，先写</li>
<li><strong>更多交流</strong>：尝试去回答别人提出的问题，在社区中参与讨论</li>
</ul>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>2018 年结束了。</p>
<p>这一年走过的路，用代码来表达大概是这样的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Year2018 &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        int totalDays = 365;</span><br><span class="line">        int learningDays = 250;     // 翻书 + 看视频</span><br><span class="line">        int codingDays = 150;       // 亲手写代码</span><br><span class="line">        int projectCount = 1;       // 第一个完整项目</span><br><span class="line">        int blogCount = 5;          // 这一年写的博客</span><br><span class="line">        int debugErrors = 999;      // 记不清的错误次数，上不封顶</span><br><span class="line"></span><br><span class="line">        String result = (codingDays &gt; 0 &amp;&amp; projectCount &gt; 0)</span><br><span class="line">            ? &quot;入门成功&quot; : &quot;仍需努力&quot;;</span><br><span class="line">        System.out.println(&quot;2018年结果：&quot; + result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>代码不会骗人。这一年的输入和输出清清楚楚地写在上面。</p>
<p>关于心态的变化可能比知识的增长更重要。年初的时候我总觉得编程是一件需要天赋的事情，那些写框架的人、做开源项目的人，一定天生就比普通人聪明。现在我不这么认为了。我看到的所有”厉害的人”，不过是在你看不到的地方，花了你想象不到的时间。</p>
<p>编程是一种技能，技能是可以靠持续练习获得的。这一年教会我最大的道理就是：<strong>不要高估一天能学到的东西，不要低估一年能积累的东西。</strong></p>
<p>当然也有遗憾。有些知识点学得不够深入，很多 bug 挡了一下就绕过去了，没有彻底搞清楚根源。有些计划中的书没看完，有些想写的博客没来得及写。</p>
<p>但没关系。2019 年还会继续。</p>
<p>最后的最后，用一句话总结这个深夜的感慨吧：</p>
<blockquote>
<p>这一年，从 Hello World 写到了 Spring 容器；从对着控制台发呆，到享受 Debug 通过那一刻的畅快。路还很长，但至少，我开始享受写代码这件事了。</p>
</blockquote>
<p>2019，继续写下去。</p>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>年度总结</tag>
        <tag>学习路线</tag>
        <tag>回顾</tag>
        <tag>展望</tag>
      </tags>
  </entry>
  <entry>
    <title>Git版本控制入门</title>
    <url>/2019/01/12/2019-01-12-Git%E7%89%88%E6%9C%AC%E6%8E%A7%E5%88%B6%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>从手动复制代码到使用 Git 管理版本，是我在 2019 年开始项目实践时迈出的第一步。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-01 前后学习和实践 <strong>Git版本控制入门</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>不知道 add、commit、push 分别做了什么</li>
<li>误把 target 和 .class 文件提交到了仓库</li>
<li>合并代码时出现冲突，不敢随便改</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>Git 本质上是在本地维护一个对象数据库，工作区、暂存区、本地仓库、远程仓库是四个不同层次。理解这几层之后，很多命令就不再是死记硬背。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 初始化仓库</span></span><br><span class="line">git init</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看状态</span></span><br><span class="line">git status</span><br><span class="line"></span><br><span class="line"><span class="comment"># 添加到暂存区</span></span><br><span class="line">git add src/main/java</span><br><span class="line"></span><br><span class="line"><span class="comment"># 提交到本地仓库</span></span><br><span class="line">git commit -m <span class="string">&quot;feat: add user module&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 关联远程仓库并推送</span></span><br><span class="line">git remote add origin https://github.com/example/demo.git</span><br><span class="line">git push -u origin master</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">target/</span><br><span class="line">*.class</span><br><span class="line">*.log</span><br><span class="line">.idea/</span><br><span class="line">*.iml</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 解决冲突后的常见流程</span></span><br><span class="line">git pull origin master</span><br><span class="line"><span class="comment"># 手动编辑冲突文件</span></span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;fix: resolve merge conflict&quot;</span></span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 Git版本控制入门 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>开发工具</category>
      </categories>
      <tags>
        <tag>Git</tag>
        <tag>版本控制</tag>
        <tag>GitHub</tag>
        <tag>项目实践</tag>
      </tags>
  </entry>
  <entry>
    <title>Maven项目管理实践</title>
    <url>/2019/02/17/2019-02-17-Maven%E9%A1%B9%E7%9B%AE%E7%AE%A1%E7%90%86%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>Maven 让我第一次感受到 Java 项目不只是写代码，还包括依赖、构建和目录约定。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-02 前后学习和实践 <strong>Maven项目管理实践</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>jar 包版本冲突导致项目启动失败</li>
<li>pom.xml 里 dependency 和 plugin 分不清</li>
<li>不知道 clean、package、install 的区别</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>Maven 的核心价值不是下载 jar 包，而是用约定统一项目结构、依赖关系和构建流程。pom.xml 是项目构建说明书。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">project</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.demo<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>student-system<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-java<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.1.47<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn clean</span><br><span class="line">mvn compile</span><br><span class="line">mvn <span class="built_in">test</span></span><br><span class="line">mvn package</span><br><span class="line">mvn install</span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 Maven项目管理实践 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>项目实践</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Maven</tag>
        <tag>依赖管理</tag>
        <tag>项目构建</tag>
      </tags>
  </entry>
  <entry>
    <title>Servlet工作原理</title>
    <url>/2019/03/09/2019-03-09-Servlet%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[<p><img src="/images/posts/2019/servlet-principle-cover.svg" alt="Servlet工作原理封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>Servlet 是我理解 Java Web 的第一个入口，也是后面学习 Spring MVC 的基础。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-03 前后学习和实践 <strong>Servlet工作原理</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>不知道请求是怎么进入 doGet&#x2F;doPost 的</li>
<li>Servlet 生命周期理解不清</li>
<li>request 和 response 经常混着用</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>Tomcat 接收到 HTTP 请求后，会根据 URL 映射找到对应 Servlet，再调用 service 方法，由 service 分发到 doGet 或 doPost。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HelloServlet</span> <span class="keyword">extends</span> <span class="title class_">HttpServlet</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">doGet</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">        response.setContentType(<span class="string">&quot;text/html;charset=UTF-8&quot;</span>);</span><br><span class="line">        response.getWriter().write(<span class="string">&quot;Hello Servlet&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">doPost</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">        doGet(request, response);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>helloServlet<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span>com.demo.web.HelloServlet<span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>helloServlet<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/hello<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">servlet-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 Servlet工作原理 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>Java Web</category>
      </categories>
      <tags>
        <tag>Servlet</tag>
        <tag>Java Web</tag>
        <tag>Tomcat</tag>
        <tag>请求响应</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP请求与响应学习笔记</title>
    <url>/2019/03/27/2019-03-27-HTTP%E8%AF%B7%E6%B1%82%E4%B8%8E%E5%93%8D%E5%BA%94%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p><img src="/images/posts/2019/http-notes-cover.svg" alt="HTTP请求与响应学习笔记封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>真正写接口之前，我发现自己必须先补上 HTTP 请求与响应这门基础课。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-03 前后学习和实践 <strong>HTTP请求与响应学习笔记</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>GET 和 POST 只会从表面区分</li>
<li>状态码只认识 404 和 500</li>
<li>请求头、响应头看不懂</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>HTTP 是浏览器和服务器之间的文本协议，方法、URL、Header、Body、状态码共同描述了一次完整交互。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="keyword">GET</span> <span class="string">/users/1</span> <span class="meta">HTTP/1.1</span></span><br><span class="line"><span class="attribute">Host</span><span class="punctuation">: </span>localhost:8080</span><br><span class="line"><span class="attribute">Accept</span><span class="punctuation">: </span>application/json</span><br></pre></td></tr></table></figure>

<figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="meta">HTTP/1.1</span> <span class="number">200</span> OK</span><br><span class="line"><span class="attribute">Content-Type</span><span class="punctuation">: </span>application/json;charset=UTF-8</span><br><span class="line"></span><br><span class="line"><span class="language-json"><span class="punctuation">&#123;</span><span class="attr">&quot;id&quot;</span><span class="punctuation">:</span><span class="number">1</span><span class="punctuation">,</span><span class="attr">&quot;name&quot;</span><span class="punctuation">:</span><span class="string">&quot;Tom&quot;</span><span class="punctuation">&#125;</span></span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -X POST http://localhost:8080/users   -H <span class="string">&quot;Content-Type: application/json&quot;</span>   -d <span class="string">&#x27;&#123;&quot;name&quot;:&quot;Tom&quot;,&quot;age&quot;:18&#125;&#x27;</span></span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 HTTP请求与响应学习笔记 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>Java Web</category>
      </categories>
      <tags>
        <tag>请求响应</tag>
        <tag>HTTP</tag>
        <tag>Web基础</tag>
        <tag>状态码</tag>
      </tags>
  </entry>
  <entry>
    <title>JSP开发笔记</title>
    <url>/2019/04/14/2019-04-14-JSP%E5%BC%80%E5%8F%91%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p><img src="/images/posts/2019/jsp-notes-cover.svg" alt="JSP开发笔记封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>JSP 让我第一次把 Java 代码和页面展示联系起来，但也暴露了前后端混写的问题。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-04 前后学习和实践 <strong>JSP开发笔记</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>JSP 里 Java 代码越写越乱</li>
<li>九大内置对象记不住</li>
<li>页面跳转 forward 和 redirect 混淆</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>JSP 最终会被翻译成 Servlet，所以它本质上不是纯页面技术，而是服务端动态页面模板。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight jsp"><table><tr><td class="code"><pre><span class="line">&lt;%@ page contentType=<span class="string">&quot;text/html;charset=UTF-8&quot;</span> language=<span class="string">&quot;java&quot;</span> %&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">  &lt;h2&gt;学生列表&lt;/h2&gt;</span><br><span class="line">  &lt;p&gt;当前用户：$&#123;sessionScope.user.username&#125;&lt;/p&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">request.setAttribute(<span class="string">&quot;studentList&quot;</span>, studentService.list());</span><br><span class="line">request.getRequestDispatcher(<span class="string">&quot;/WEB-INF/views/student.jsp&quot;</span>).forward(request, response);</span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 JSP开发笔记 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>Java Web</category>
      </categories>
      <tags>
        <tag>Servlet</tag>
        <tag>Java Web</tag>
        <tag>JSP</tag>
        <tag>页面渲染</tag>
      </tags>
  </entry>
  <entry>
    <title>Tomcat部署项目</title>
    <url>/2019/05/19/2019-05-19-Tomcat%E9%83%A8%E7%BD%B2%E9%A1%B9%E7%9B%AE/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>把项目从本地 IDE 跑到 Tomcat 服务器上，是我第一次真正感受到部署这件事。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-05 前后学习和实践 <strong>Tomcat部署项目</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>IDE 能跑，部署到 Tomcat 就 404</li>
<li>war 包路径和 context path 搞混</li>
<li>端口 8080 被占用</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>部署失败多数不是代码问题，而是路径、端口、依赖、配置和运行环境不一致。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn clean package</span><br><span class="line"><span class="built_in">cp</span> target/student-system.war /usr/local/tomcat/webapps/</span><br><span class="line"><span class="built_in">cd</span> /usr/local/tomcat/bin</span><br><span class="line">./startup.sh</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看端口占用</span></span><br><span class="line">lsof -i:8080</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看启动日志</span></span><br><span class="line"><span class="built_in">tail</span> -f /usr/local/tomcat/logs/catalina.out</span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 Tomcat部署项目 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>项目实践</category>
      </categories>
      <tags>
        <tag>服务器</tag>
        <tag>Java Web</tag>
        <tag>Tomcat</tag>
        <tag>部署</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring IOC理解</title>
    <url>/2019/06/08/2019-06-08-Spring%20IOC%E7%90%86%E8%A7%A3/</url>
    <content><![CDATA[<p><img src="/images/posts/2019/spring-ioc-cover.svg" alt="Spring IOC理解封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>学习 Spring 的第一道坎就是 IOC，刚开始觉得抽象，后来才明白它是在帮我们管理对象。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-06 前后学习和实践 <strong>Spring IOC理解</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>为什么对象不自己 new 了</li>
<li>XML 配置 Bean 很容易写错</li>
<li>依赖注入和控制反转概念抽象</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>IOC 的重点是对象创建权从业务代码转移到了 Spring 容器，DI 是容器把依赖关系装配进去的过程。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;userRepository&quot;</span> <span class="attr">class</span>=<span class="string">&quot;com.demo.repository.UserRepository&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;userService&quot;</span> <span class="attr">class</span>=<span class="string">&quot;com.demo.service.UserService&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;userRepository&quot;</span> <span class="attr">ref</span>=<span class="string">&quot;userRepository&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserService</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> UserRepository userRepository;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setUserRepository</span><span class="params">(UserRepository userRepository)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.userRepository = userRepository;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 Spring IOC理解 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>IOC</tag>
        <tag>DI</tag>
        <tag>Bean</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring AOP初探</title>
    <url>/2019/07/21/2019-07-21-Spring%20AOP%E5%88%9D%E6%8E%A2/</url>
    <content><![CDATA[<p><img src="/images/posts/2019/spring-aop-cover.svg" alt="Spring AOP初探封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>AOP 是我接触到的第一个比较“绕”的框架思想，尤其是切点和通知，一开始很容易混。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-07 前后学习和实践 <strong>Spring AOP初探</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>切点表达式不会写</li>
<li>前置通知和环绕通知执行顺序不清楚</li>
<li>JDK 动态代理和 CGLIB 分不清</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>AOP 适合处理横切关注点，例如日志、权限、事务。它不是替代 OOP，而是补充 OOP 在横向逻辑复用上的不足。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Aspect</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LogAspect</span> &#123;</span><br><span class="line">    <span class="meta">@Pointcut(&quot;execution(* com.demo.service..*(..))&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">serviceMethods</span><span class="params">()</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Around(&quot;serviceMethods()&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">logCost</span><span class="params">(ProceedingJoinPoint joinPoint)</span> <span class="keyword">throws</span> Throwable &#123;</span><br><span class="line">        <span class="type">long</span> <span class="variable">start</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        <span class="type">Object</span> <span class="variable">result</span> <span class="operator">=</span> joinPoint.proceed();</span><br><span class="line">        System.out.println(joinPoint.getSignature() + <span class="string">&quot; cost &quot;</span> + (System.currentTimeMillis() - start) + <span class="string">&quot;ms&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 Spring AOP初探 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>AOP</tag>
        <tag>动态代理</tag>
        <tag>切面编程</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatis使用总结</title>
    <url>/2019/08/18/2019-08-18-MyBatis%E4%BD%BF%E7%94%A8%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>从 JDBC 到 MyBatis，代码量少了很多，但也需要理解 Mapper、SqlSession 和动态 SQL。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-08 前后学习和实践 <strong>MyBatis使用总结</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>Mapper XML 路径写错导致绑定失败</li>
<li>#{} 和 ${} 分不清</li>
<li>一对多查询结果映射混乱</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>MyBatis 保留 SQL 的可控性，同时帮我们完成参数绑定、结果映射和会话管理。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;findById&quot;</span> <span class="attr">resultType</span>=<span class="string">&quot;com.demo.entity.User&quot;</span>&gt;</span></span><br><span class="line">  select id, username, password, create_time</span><br><span class="line">  from t_user</span><br><span class="line">  where id = #&#123;id&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">insert</span> <span class="attr">id</span>=<span class="string">&quot;insert&quot;</span> <span class="attr">parameterType</span>=<span class="string">&quot;com.demo.entity.User&quot;</span>&gt;</span></span><br><span class="line">  insert into t_user(username, password)</span><br><span class="line">  values(#&#123;username&#125;, #&#123;password&#125;)</span><br><span class="line"><span class="tag">&lt;/<span class="name">insert</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> userMapper.findById(<span class="number">1L</span>);</span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 MyBatis使用总结 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>SQL</tag>
        <tag>数据库</tag>
        <tag>MyBatis</tag>
        <tag>ORM</tag>
      </tags>
  </entry>
  <entry>
    <title>RESTful接口设计</title>
    <url>/2019/09/08/2019-09-08-RESTful%E6%8E%A5%E5%8F%A3%E8%AE%BE%E8%AE%A1/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>写接口不只是能返回 JSON，还要考虑 URL、方法、状态码和语义。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-09 前后学习和实践 <strong>RESTful接口设计</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>URL 里到底该不该出现动词</li>
<li>状态码是否统一返回 200 很纠结</li>
<li>接口返回结构不统一</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>RESTful 更强调资源语义，URL 表示资源，HTTP 方法表示动作，状态码表达结果。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">GET    /api/users        查询用户列表</span><br><span class="line">GET    /api/users/&#123;id&#125;   查询用户详情</span><br><span class="line">POST   /api/users        新增用户</span><br><span class="line">PUT    /api/users/&#123;id&#125;   修改用户</span><br><span class="line">DELETE /api/users/&#123;id&#125;   删除用户</span><br></pre></td></tr></table></figure>

<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;code&quot;</span><span class="punctuation">:</span> <span class="number">0</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;message&quot;</span><span class="punctuation">:</span> <span class="string">&quot;success&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;data&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;username&quot;</span><span class="punctuation">:</span> <span class="string">&quot;tom&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 RESTful接口设计 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>接口设计</category>
      </categories>
      <tags>
        <tag>HTTP</tag>
        <tag>RESTful</tag>
        <tag>接口设计</tag>
        <tag>后端开发</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring MVC请求流程理解</title>
    <url>/2019/06/29/2019-06-29-Spring%20MVC%E8%AF%B7%E6%B1%82%E6%B5%81%E7%A8%8B%E7%90%86%E8%A7%A3/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>在能写 Controller 之后，我开始追问一个问题：请求到底是怎么一步步进入方法里的。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-06 前后学习和实践 <strong>Spring MVC请求流程理解</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>Controller 方法为什么能接到参数</li>
<li>DispatcherServlet 到底做了什么</li>
<li>视图解析器和 JSON 返回分不清</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>Spring MVC 是围绕 DispatcherServlet 展开的请求分发体系，它负责协调映射、适配、调用和响应。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/users&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserController</span> &#123;</span><br><span class="line">    <span class="meta">@GetMapping(&quot;/&#123;id&#125;&quot;)</span></span><br><span class="line">    <span class="meta">@ResponseBody</span></span><br><span class="line">    <span class="keyword">public</span> UserVO <span class="title function_">detail</span><span class="params">(<span class="meta">@PathVariable</span> Long id)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> userService.findById(id);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>dispatcher<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span>org.springframework.web.servlet.DispatcherServlet<span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>dispatcher<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">servlet-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 Spring MVC请求流程理解 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>Spring MVC</tag>
        <tag>DispatcherServlet</tag>
        <tag>Controller</tag>
        <tag>请求流程</tag>
      </tags>
  </entry>
  <entry>
    <title>Postman接口调试记录</title>
    <url>/2019/09/24/2019-09-24-Postman%E6%8E%A5%E5%8F%A3%E8%B0%83%E8%AF%95%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>Postman 帮我把接口调试从浏览器地址栏里解放出来，也让我开始重视请求参数和响应结构。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-09 前后学习和实践 <strong>Postman接口调试记录</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>浏览器无法方便测试 POST&#x2F;PUT&#x2F;DELETE</li>
<li>请求体格式写错导致后端接不到参数</li>
<li>Token 和 Cookie 不知道怎么带</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>Postman 的价值在于把请求方法、Header、Body、环境变量和测试脚本统一管理起来，让接口调试可重复。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -X POST http://localhost:8080/api/login   -H <span class="string">&quot;Content-Type: application/json&quot;</span>   -d <span class="string">&#x27;&#123;&quot;username&quot;:&quot;admin&quot;,&quot;password&quot;:&quot;123456&quot;&#125;&#x27;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">pm.<span class="title function_">test</span>(<span class="string">&quot;响应状态为 200&quot;</span>, <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    pm.<span class="property">response</span>.<span class="property">to</span>.<span class="property">have</span>.<span class="title function_">status</span>(<span class="number">200</span>);</span><br><span class="line">&#125;);</span><br><span class="line">pm.<span class="property">environment</span>.<span class="title function_">set</span>(<span class="string">&quot;token&quot;</span>, pm.<span class="property">response</span>.<span class="title function_">json</span>().<span class="property">data</span>.<span class="property">token</span>);</span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 Postman接口调试记录 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>接口设计</category>
      </categories>
      <tags>
        <tag>RESTful</tag>
        <tag>Postman</tag>
        <tag>接口调试</tag>
        <tag>测试</tag>
      </tags>
  </entry>
  <entry>
    <title>权限管理模块设计</title>
    <url>/2019/11/16/2019-11-16-%E6%9D%83%E9%99%90%E7%AE%A1%E7%90%86%E6%A8%A1%E5%9D%97%E8%AE%BE%E8%AE%A1/</url>
    <content><![CDATA[<p><img src="/images/posts/2019/permission-design-cover.svg" alt="权限管理模块设计封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>权限管理是第一个让我明显感受到“业务建模”重要性的模块。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-11 前后学习和实践 <strong>权限管理模块设计</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>用户、角色、权限三者关系不清楚</li>
<li>菜单权限和按钮权限混在一起</li>
<li>SQL 查询权限列表效率不高</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>RBAC 把权限直接分配给角色，再把角色分配给用户，可以降低用户和权限之间的复杂度。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create table</span> sys_user(id <span class="type">bigint</span> <span class="keyword">primary key</span>, username <span class="type">varchar</span>(<span class="number">64</span>));</span><br><span class="line"><span class="keyword">create table</span> sys_role(id <span class="type">bigint</span> <span class="keyword">primary key</span>, role_name <span class="type">varchar</span>(<span class="number">64</span>));</span><br><span class="line"><span class="keyword">create table</span> sys_permission(id <span class="type">bigint</span> <span class="keyword">primary key</span>, permission_code <span class="type">varchar</span>(<span class="number">128</span>));</span><br><span class="line"><span class="keyword">create table</span> sys_user_role(user_id <span class="type">bigint</span>, role_id <span class="type">bigint</span>);</span><br><span class="line"><span class="keyword">create table</span> sys_role_permission(role_id <span class="type">bigint</span>, permission_id <span class="type">bigint</span>);</span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> p.permission_code</span><br><span class="line"><span class="keyword">from</span> sys_user u</span><br><span class="line"><span class="keyword">join</span> sys_user_role ur <span class="keyword">on</span> u.id <span class="operator">=</span> ur.user_id</span><br><span class="line"><span class="keyword">join</span> sys_role_permission rp <span class="keyword">on</span> ur.role_id <span class="operator">=</span> rp.role_id</span><br><span class="line"><span class="keyword">join</span> sys_permission p <span class="keyword">on</span> rp.permission_id <span class="operator">=</span> p.id</span><br><span class="line"><span class="keyword">where</span> u.id <span class="operator">=</span> ?;</span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 权限管理模块设计 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>项目实践</category>
      </categories>
      <tags>
        <tag>权限管理</tag>
        <tag>RBAC</tag>
        <tag>角色权限</tag>
        <tag>系统设计</tag>
      </tags>
  </entry>
  <entry>
    <title>登录功能实现案例</title>
    <url>/2019/10/20/2019-10-20-%E7%99%BB%E5%BD%95%E5%8A%9F%E8%83%BD%E5%AE%9E%E7%8E%B0%E6%A1%88%E4%BE%8B/</url>
    <content><![CDATA[<p><img src="/images/posts/2019/login-case-cover.svg" alt="登录功能实现案例封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>登录功能看起来简单，但真正实现时会遇到密码校验、Session、拦截器和安全问题。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-10 前后学习和实践 <strong>登录功能实现案例</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>密码明文存储不安全</li>
<li>Session 什么时候创建什么时候失效不清楚</li>
<li>未登录用户也能访问后台页面</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>登录不是简单查一次数据库，而是身份认证、会话维护、访问控制和安全策略的组合。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">login</span><span class="params">(String username, String password, HttpSession session)</span> &#123;</span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> userMapper.findByUsername(username);</span><br><span class="line">    <span class="keyword">if</span> (user == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!DigestUtils.md5DigestAsHex(password.getBytes()).equals(user.getPassword())) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    session.setAttribute(<span class="string">&quot;loginUser&quot;</span>, user);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">    <span class="keyword">if</span> (request.getSession().getAttribute(<span class="string">&quot;loginUser&quot;</span>) == <span class="literal">null</span>) &#123;</span><br><span class="line">        response.sendRedirect(<span class="string">&quot;/login&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 登录功能实现案例 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>项目实践</category>
      </categories>
      <tags>
        <tag>Java Web</tag>
        <tag>登录功能</tag>
        <tag>Session</tag>
        <tag>Cookie</tag>
      </tags>
  </entry>
  <entry>
    <title>项目上线前检查清单</title>
    <url>/2019/12/08/2019-12-08-%E9%A1%B9%E7%9B%AE%E4%B8%8A%E7%BA%BF%E5%89%8D%E6%A3%80%E6%9F%A5%E6%B8%85%E5%8D%95/</url>
    <content><![CDATA[<p><img src="/images/posts/2019/release-checklist-cover.svg" alt="项目上线前检查清单封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>第一次准备上线项目时，我才发现代码写完并不等于项目完成。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-12 前后学习和实践 <strong>项目上线前检查清单</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>本地能跑不代表线上能跑</li>
<li>配置文件里还有本地路径和测试密码</li>
<li>日志、备份、回滚方案没有提前准备</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>上线是一组工程动作，包括配置检查、构建检查、数据库检查、日志监控、备份和回滚。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 构建检查</span></span><br><span class="line">mvn clean package -DskipTests=<span class="literal">false</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置检查</span></span><br><span class="line">grep -R <span class="string">&quot;localhost\|127.0.0.1\|root&quot;</span> src/main/resources</span><br><span class="line"></span><br><span class="line"><span class="comment"># 日志检查</span></span><br><span class="line"><span class="built_in">tail</span> -f logs/app.log</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">上线前检查：</span><br><span class="line">1. 数据库脚本是否备份</span><br><span class="line">2. 配置是否切换到生产环境</span><br><span class="line">3. 日志目录是否存在</span><br><span class="line">4. 端口是否开放</span><br><span class="line">5. 是否有回滚包</span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 项目上线前检查清单 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>项目实践</category>
      </categories>
      <tags>
        <tag>运维</tag>
        <tag>测试</tag>
        <tag>项目上线</tag>
        <tag>部署检查</tag>
      </tags>
  </entry>
  <entry>
    <title>代码规范的重要性</title>
    <url>/2020/02/16/2020-02-16-%E4%BB%A3%E7%A0%81%E8%A7%84%E8%8C%83%E7%9A%84%E9%87%8D%E8%A6%81%E6%80%A7/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/code-style-cover.svg" alt="代码规范的重要性封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>代码规范不是为了追求形式统一，而是为了降低团队沟通成本和长期维护成本。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>同一类逻辑在不同人手里写法差异很大</li>
<li>命名随意导致 Review 成本上升</li>
<li>没有统一异常、日志、返回值规范</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>规范的本质是降低差异。代码会被多人阅读和长期维护，统一风格能让团队把注意力放在业务逻辑，而不是表达方式。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 推荐：命名表达业务含义</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">freezeUserAccount</span><span class="params">(Long userId, String reason)</span> &#123;</span><br><span class="line">    validateFreezeReason(reason);</span><br><span class="line">    userRepository.updateStatus(userId, UserStatus.FROZEN);</span><br><span class="line">    log.info(<span class="string">&quot;user account frozen, userId=&#123;&#125;, reason=&#123;&#125;&quot;</span>, userId, reason);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Review 关注点：</span><br><span class="line">- 命名是否清晰</span><br><span class="line">- 异常是否有上下文</span><br><span class="line">- 日志是否能支撑排查</span><br><span class="line">- 方法是否过长</span><br><span class="line">- 是否存在重复逻辑</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>代码规范的重要性</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>工程规范</category>
      </categories>
      <tags>
        <tag>代码规范</tag>
        <tag>Code Review</tag>
        <tag>可维护性</tag>
        <tag>团队协作</tag>
      </tags>
  </entry>
  <entry>
    <title>数据库设计经验总结</title>
    <url>/2020/03/14/2020-03-14-%E6%95%B0%E6%8D%AE%E5%BA%93%E8%AE%BE%E8%AE%A1%E7%BB%8F%E9%AA%8C%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/db-design-cover.svg" alt="数据库设计经验总结封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>数据库设计一旦进入线上环境，修改成本会远高于代码，因此前期设计需要更谨慎。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>字段先随手加，后续统计和查询非常痛苦</li>
<li>索引建得过早或过多，反而增加维护成本</li>
<li>表关系没有边界，业务变更时牵一发动全身</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>数据库结构承载的是业务事实，表、字段、索引和约束都应该围绕业务长期演进来设计。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create table</span> order_info (</span><br><span class="line">  id <span class="type">bigint</span> <span class="keyword">primary key</span>,</span><br><span class="line">  order_no <span class="type">varchar</span>(<span class="number">64</span>) <span class="keyword">not null</span>,</span><br><span class="line">  user_id <span class="type">bigint</span> <span class="keyword">not null</span>,</span><br><span class="line">  order_status tinyint <span class="keyword">not null</span>,</span><br><span class="line">  total_amount <span class="type">decimal</span>(<span class="number">12</span>,<span class="number">2</span>) <span class="keyword">not null</span>,</span><br><span class="line">  created_at datetime <span class="keyword">not null</span>,</span><br><span class="line">  updated_at datetime <span class="keyword">not null</span>,</span><br><span class="line">  <span class="keyword">unique</span> key uk_order_no(order_no),</span><br><span class="line">  key idx_user_status(user_id, order_status)</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">explain <span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> order_info <span class="keyword">where</span> user_id <span class="operator">=</span> <span class="number">1001</span> <span class="keyword">and</span> order_status <span class="operator">=</span> <span class="number">2</span>;</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>数据库设计经验总结</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>MySQL</tag>
        <tag>索引</tag>
        <tag>数据库设计</tag>
        <tag>表结构</tag>
      </tags>
  </entry>
  <entry>
    <title>新人如何快速熟悉项目</title>
    <url>/2020/01/11/2020-01-11-%E6%96%B0%E4%BA%BA%E5%A6%82%E4%BD%95%E5%BF%AB%E9%80%9F%E7%86%9F%E6%82%89%E9%A1%B9%E7%9B%AE/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/onboarding-project-cover.svg" alt="新人如何快速熟悉项目封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>从“能把项目跑起来”到“能独立改需求”，中间隔着对业务、代码结构和协作方式的系统理解。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>只关注代码入口，忽略业务流程和数据流</li>
<li>本地环境搭建耗时过长，影响进入真实需求</li>
<li>遇到问题只问结论，没有沉淀排查路径</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>熟悉项目的关键不是把每一行代码都看懂，而是先建立业务地图、系统边界、核心链路和本地调试能力。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">项目熟悉清单：</span><br><span class="line">1. 本地启动方式</span><br><span class="line">2. 核心业务流程</span><br><span class="line">3. 数据库核心表</span><br><span class="line">4. 主要外部依赖</span><br><span class="line">5. 最近三个月变更记录</span><br><span class="line">6. 常见问题排查文档</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 熟悉项目时常用命令</span></span><br><span class="line">git <span class="built_in">log</span> --oneline --decorate -20</span><br><span class="line">grep -R <span class="string">&quot;@RequestMapping\|@GetMapping\|@PostMapping&quot;</span> src/main/java</span><br><span class="line">mvn clean <span class="built_in">test</span></span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>新人如何快速熟悉项目</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>经验沉淀</category>
      </categories>
      <tags>
        <tag>团队协作</tag>
        <tag>新人上手</tag>
        <tag>项目熟悉</tag>
        <tag>工程实践</tag>
      </tags>
  </entry>
  <entry>
    <title>需求评审中的常见问题</title>
    <url>/2020/05/10/2020-05-10-%E9%9C%80%E6%B1%82%E8%AF%84%E5%AE%A1%E4%B8%AD%E7%9A%84%E5%B8%B8%E8%A7%81%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>很多开发问题并不是写代码时产生的，而是在需求评审阶段就已经埋下了。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>只评估开发量，没有识别边界条件</li>
<li>异常流程和权限规则讨论不足</li>
<li>需求变更没有留下版本记录</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>需求评审不是确认“做不做”，而是确认“做什么、不做什么、异常情况怎么办、验收标准是什么”。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求评审问题清单：</span><br><span class="line">1. 目标用户是谁？</span><br><span class="line">2. 正常流程是什么？</span><br><span class="line">3. 异常流程有哪些？</span><br><span class="line">4. 权限边界是什么？</span><br><span class="line">5. 数据是否需要迁移？</span><br><span class="line">6. 验收标准如何定义？</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">结论记录格式：</span><br><span class="line">- 已确认：</span><br><span class="line">- 待产品补充：</span><br><span class="line">- 技术风险：</span><br><span class="line">- 不在本期范围：</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>需求评审中的常见问题</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>项目协作</category>
      </categories>
      <tags>
        <tag>需求评审</tag>
        <tag>产品协作</tag>
        <tag>项目管理</tag>
        <tag>沟通</tag>
      </tags>
  </entry>
  <entry>
    <title>第一个完整项目复盘</title>
    <url>/2019/12/29/2019-12-29-%E7%AC%AC%E4%B8%80%E4%B8%AA%E5%AE%8C%E6%95%B4%E9%A1%B9%E7%9B%AE%E5%A4%8D%E7%9B%98/</url>
    <content><![CDATA[<p><img src="/images/posts/2019/first-project-review-cover.svg" alt="第一个完整项目复盘封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>2019 年的最后，我把第一个完整项目从需求、开发、部署到复盘重新梳理了一遍。</p>
<p>2019 年对我来说和 2018 年明显不一样。2018 年更多是在补 Java 基础，到了 2019 年，我开始把零散知识往项目里放。很多以前看起来懂了的概念，只要一放进真实项目流程里，马上就会暴露问题。</p>
<p>这篇文章记录的是我在 2019-12 前后学习和实践 <strong>第一个完整项目复盘</strong> 时的理解。内容不会写得像官方文档，更像是一份踩坑记录：当时为什么学、遇到了哪些问题、怎么排查，最后沉淀出哪些可以复用的经验。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>刚开始实践时，我主要卡在下面几个点：</p>
<ol>
<li>需求边写边改导致返工</li>
<li>代码分层不清晰</li>
<li>部署和文档总是拖到最后</li>
</ol>
<p>这些问题单独看都不算复杂，但放在项目里就很容易连锁反应。比如配置错了，表面上看到的是页面打不开，实际原因可能是路径、依赖、运行环境或者请求流程中的某个环节出了问题。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>项目复盘的价值是把“能跑”背后的过程问题暴露出来，包括需求、设计、开发、测试和部署。</p>
<p>我后来发现，学项目实践类技术时，不能只背 API。更重要的是把它放到一条完整链路里看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">需求 -&gt; 编码 -&gt; 配置 -&gt; 构建 -&gt; 运行 -&gt; 调试 -&gt; 部署 -&gt; 复盘</span><br></pre></td></tr></table></figure>

<p>只要能说清楚它在这条链路里的位置，就不会完全靠记忆去使用。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式是先把最小可运行例子跑通，再逐步放到项目中验证。下面是当时整理下来的关键操作和代码片段。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">项目名称：学生管理系统</span><br><span class="line">技术栈：Servlet + JSP + Spring + MyBatis + MySQL</span><br><span class="line">模块：登录、学生管理、课程管理、成绩管理、权限控制</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘维度：</span><br><span class="line">1. 需求是否清晰</span><br><span class="line">2. 数据库设计是否合理</span><br><span class="line">3. 代码分层是否清楚</span><br><span class="line">4. 异常处理是否完善</span><br><span class="line">5. 部署文档是否完整</span><br></pre></td></tr></table></figure>

<p>实践过程中我还给自己定了一个小规则：每次遇到问题，不只记录最终命令，还要记录“为什么这么做”。否则下次换一个环境，很容易又从头查一遍。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>这次学习给我的最大感受是：<strong>项目实践里的知识点，真正难的不是单个语法，而是上下文。</strong></p>
<p>对 第一个完整项目复盘 的理解，我最后沉淀成几条经验：</p>
<ul>
<li>先理解它解决什么问题，再记具体用法；</li>
<li>先跑通最小 Demo，再接入完整项目；</li>
<li>配置类问题要从路径、依赖、版本、环境四个方向排查；</li>
<li>代码示例要自己敲一遍，复制能跑不代表真的理解；</li>
<li>每次踩坑最好留下记录，后面会节省大量时间。</li>
</ul>
<p>2019 年的学习明显比 2018 年更贴近真实开发。很多概念，比如 Servlet、Spring、MyBatis、接口设计、权限管理，都是在一次次“跑不起来”和“终于跑通了”的过程中慢慢理解的。</p>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>Java Web</tag>
        <tag>Spring</tag>
        <tag>项目复盘</tag>
        <tag>成长总结</tag>
      </tags>
  </entry>
  <entry>
    <title>如何阅读遗留代码</title>
    <url>/2020/06/20/2020-06-20-%E5%A6%82%E4%BD%95%E9%98%85%E8%AF%BB%E9%81%97%E7%95%99%E4%BB%A3%E7%A0%81/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/legacy-code-cover.svg" alt="如何阅读遗留代码封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>读遗留代码不能一上来就想重写，第一目标应该是建立事实，而不是表达审美。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>一上来就想重构，忽略现有逻辑背后的业务原因</li>
<li>没有调用链和数据链路视角</li>
<li>改动范围评估不充分</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>遗留代码通常是历史约束下的结果。先尊重事实，再谈优化，是降低维护风险的前提。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 找入口</span></span><br><span class="line">grep -R <span class="string">&quot;OrderController&quot;</span> src/main/java</span><br><span class="line"></span><br><span class="line"><span class="comment"># 看最近修改</span></span><br><span class="line">git <span class="built_in">log</span> --oneline -- src/main/java/com/demo/order</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查调用关系</span></span><br><span class="line"> grep -R <span class="string">&quot;createOrder&quot;</span> src/main/java</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">阅读顺序：</span><br><span class="line">入口接口 -&gt; Service 主流程 -&gt; 数据库表 -&gt; 外部依赖 -&gt; 异常分支 -&gt; 历史提交</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>如何阅读遗留代码</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>经验沉淀</category>
      </categories>
      <tags>
        <tag>遗留代码</tag>
        <tag>代码阅读</tag>
        <tag>重构</tag>
        <tag>维护</tag>
      </tags>
  </entry>
  <entry>
    <title>接口文档管理实践</title>
    <url>/2020/04/19/2020-04-19-%E6%8E%A5%E5%8F%A3%E6%96%87%E6%A1%A3%E7%AE%A1%E7%90%86%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/api-docs-cover.svg" alt="接口文档管理实践封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>接口文档不是附属品，而是前后端协作、测试验证和后续维护的共同契约。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>接口实现和文档长期不一致</li>
<li>错误码、字段含义、示例数据缺失</li>
<li>前后端联调时大量时间浪费在口头确认</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>接口文档是协作契约，只有和代码、测试用例、版本变更绑定在一起，才不会沦为过期说明。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="string">GET</span> <span class="string">/api/orders/&#123;orderNo&#125;</span></span><br><span class="line"><span class="attr">response:</span></span><br><span class="line">  <span class="attr">code:</span> <span class="number">0</span></span><br><span class="line">  <span class="attr">message:</span> <span class="string">success</span></span><br><span class="line">  <span class="attr">data:</span></span><br><span class="line">    <span class="attr">orderNo:</span> <span class="string">&quot;202004190001&quot;</span></span><br><span class="line">    <span class="attr">status:</span> <span class="number">2</span></span><br><span class="line">    <span class="attr">totalAmount:</span> <span class="number">199.00</span></span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">接口文档必须包含：</span><br><span class="line">- 请求方式和路径</span><br><span class="line">- 参数说明</span><br><span class="line">- 响应字段</span><br><span class="line">- 错误码</span><br><span class="line">- 示例数据</span><br><span class="line">- 变更记录</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>接口文档管理实践</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>接口设计</category>
      </categories>
      <tags>
        <tag>接口文档</tag>
        <tag>API</tag>
        <tag>协作</tag>
        <tag>Swagger</tag>
      </tags>
  </entry>
  <entry>
    <title>Git协作开发流程</title>
    <url>/2020/07/12/2020-07-12-Git%E5%8D%8F%E4%BD%9C%E5%BC%80%E5%8F%91%E6%B5%81%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>个人使用 Git 和团队协作使用 Git 是两回事，真正的难点在分支策略和冲突管理。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>多人同时改同一模块导致冲突频繁</li>
<li>分支长期不合并，最终集成风险很高</li>
<li>提交信息无法说明改动目的</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>协作流程的重点不在命令本身，而在分支边界、提交粒度、合并节奏和问题回滚。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git checkout develop</span><br><span class="line">git pull origin develop</span><br><span class="line">git checkout -b feature/order-export</span><br><span class="line"></span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;feat: add order export&quot;</span></span><br><span class="line">git push origin feature/order-export</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">分支规则：</span><br><span class="line">feature/*  功能开发</span><br><span class="line">bugfix/*   缺陷修复</span><br><span class="line">release/*  发布准备</span><br><span class="line">hotfix/*   线上修复</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>Git协作开发流程</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>工程规范</category>
      </categories>
      <tags>
        <tag>Git</tag>
        <tag>分支管理</tag>
        <tag>协作流程</tag>
        <tag>代码合并</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot项目搭建</title>
    <url>/2020/09/13/2020-09-13-Spring%20Boot%E9%A1%B9%E7%9B%AE%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/spring-boot-setup-cover.svg" alt="Spring Boot项目搭建封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>Spring Boot 降低了项目启动门槛，但也要求我们理解默认配置背后的约定。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>依赖引入很快，但默认配置理解不足</li>
<li>配置文件拆分不清晰</li>
<li>项目结构没有统一约定</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>Spring Boot 把大量工程默认值封装起来，成熟使用方式不是只会启动，而是知道默认值如何被覆盖。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DemoApplication</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        SpringApplication.run(DemoApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">8080</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">profiles:</span></span><br><span class="line">    <span class="attr">active:</span> <span class="string">dev</span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn spring-boot:run</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>Spring Boot项目搭建</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>Spring Boot</category>
      </categories>
      <tags>
        <tag>后端开发</tag>
        <tag>Spring Boot</tag>
        <tag>项目搭建</tag>
        <tag>自动配置</tag>
      </tags>
  </entry>
  <entry>
    <title>配置管理经验</title>
    <url>/2020/09/29/2020-09-29-%E9%85%8D%E7%BD%AE%E7%AE%A1%E7%90%86%E7%BB%8F%E9%AA%8C/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/config-management-cover.svg" alt="配置管理经验封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>配置管理的目标不是把配置集中放起来，而是让不同环境的行为可预期、可追踪、可回滚。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>测试环境和生产环境配置混用</li>
<li>配置变更没有记录，问题难以回溯</li>
<li>敏感配置散落在代码仓库中</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>配置管理关乎环境一致性。配置应该可区分、可审计、可回滚，并避免把敏感信息写入代码。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">profiles:</span></span><br><span class="line">    <span class="attr">active:</span> <span class="string">dev</span></span><br><span class="line"></span><br><span class="line"><span class="attr">app:</span></span><br><span class="line">  <span class="attr">upload-dir:</span> <span class="string">/data/app/uploads</span></span><br><span class="line">  <span class="attr">timeout-ms:</span> <span class="number">3000</span></span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">配置原则：</span><br><span class="line">- 环境配置分离</span><br><span class="line">- 密码不进仓库</span><br><span class="line">- 修改要有记录</span><br><span class="line">- 默认值要保守</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>配置管理经验</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>工程实践</category>
      </categories>
      <tags>
        <tag>部署</tag>
        <tag>Spring Boot</tag>
        <tag>配置管理</tag>
        <tag>环境隔离</tag>
      </tags>
  </entry>
  <entry>
    <title>日志设计实践</title>
    <url>/2020/08/23/2020-08-23-%E6%97%A5%E5%BF%97%E8%AE%BE%E8%AE%A1%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/logging-practice-cover.svg" alt="日志设计实践封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>好的日志不是越多越好，而是在出问题时能准确回答“发生了什么、影响了谁、下一步查哪里”。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>出问题时日志里只有“失败了”，没有上下文</li>
<li>日志级别混乱，INFO 和 ERROR 随意使用</li>
<li>敏感信息被直接打印到日志</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>日志设计要面向排查场景，能串起请求、用户、业务对象、异常堆栈和关键状态变化。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">log.info(<span class="string">&quot;create order start, requestId=&#123;&#125;, userId=&#123;&#125;, skuId=&#123;&#125;&quot;</span>, requestId, userId, skuId);</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    orderService.create(command);</span><br><span class="line">&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">    log.error(<span class="string">&quot;create order failed, requestId=&#123;&#125;, userId=&#123;&#125;, command=&#123;&#125;&quot;</span>, requestId, userId, command, e);</span><br><span class="line">    <span class="keyword">throw</span> e;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">日志字段建议：requestId、userId、bizId、action、result、cost、errorCode</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>日志设计实践</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>工程实践</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>日志设计</tag>
        <tag>排查问题</tag>
        <tag>可观测性</tag>
      </tags>
  </entry>
  <entry>
    <title>开发与测试协作经验</title>
    <url>/2020/11/15/2020-11-15-%E5%BC%80%E5%8F%91%E4%B8%8E%E6%B5%8B%E8%AF%95%E5%8D%8F%E4%BD%9C%E7%BB%8F%E9%AA%8C/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/dev-test-collaboration-cover.svg" alt="开发与测试协作经验封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>开发和测试不是对立关系，真正的目标是用更低成本把问题挡在上线之前。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>Bug 描述缺少复现步骤</li>
<li>开发自测不足，低级问题进入测试阶段</li>
<li>测试关注结果，开发关注实现，沟通语言不一致</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>开发和测试对质量负责的角度不同，但目标一致。好的协作应该让问题更早暴露、更清晰流转。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Bug 单必要信息：</span><br><span class="line">- 环境</span><br><span class="line">- 账号</span><br><span class="line">- 操作步骤</span><br><span class="line">- 期望结果</span><br><span class="line">- 实际结果</span><br><span class="line">- 截图/日志</span><br><span class="line">- 接口请求参数</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">提测前自检：主流程、异常参数、权限边界、兼容旧数据、日志是否充分</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>开发与测试协作经验</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>项目协作</category>
      </categories>
      <tags>
        <tag>测试协作</tag>
        <tag>Bug管理</tag>
        <tag>质量保障</tag>
        <tag>项目协作</tag>
      </tags>
  </entry>
  <entry>
    <title>项目部署标准化流程</title>
    <url>/2020/10/18/2020-10-18-%E9%A1%B9%E7%9B%AE%E9%83%A8%E7%BD%B2%E6%A0%87%E5%87%86%E5%8C%96%E6%B5%81%E7%A8%8B/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/deploy-standard-cover.svg" alt="项目部署标准化流程封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>部署标准化的价值，是让上线从一次“手工冒险”变成一套可重复执行的工程动作。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>上线步骤依赖个人经验，缺少清单</li>
<li>部署后没有验证项</li>
<li>出现问题时没有回滚方案</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>标准化部署把个人经验转化为团队流程，让每次上线都能被复盘、复用和持续改进。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn clean package -DskipTests=<span class="literal">false</span></span><br><span class="line">scp target/app.jar deploy@server:/data/app/</span><br><span class="line">ssh deploy@server <span class="string">&quot;systemctl restart app&quot;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">部署步骤：构建 -&gt; 备份 -&gt; 上传 -&gt; 停服 -&gt; 替换 -&gt; 启动 -&gt; 验证 -&gt; 观察日志</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>项目部署标准化流程</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>项目部署</category>
      </categories>
      <tags>
        <tag>运维</tag>
        <tag>项目部署</tag>
        <tag>标准化流程</tag>
        <tag>发布</tag>
      </tags>
  </entry>
  <entry>
    <title>技术债治理思考</title>
    <url>/2020/11/28/2020-11-28-%E6%8A%80%E6%9C%AF%E5%80%BA%E6%B2%BB%E7%90%86%E6%80%9D%E8%80%83/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>技术债不是脏代码的代名词，它更多是业务速度、资源投入和长期维护之间的取舍结果。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>所有历史问题都想一次性解决</li>
<li>重构没有业务收益说明，很难获得排期</li>
<li>技术债长期无人维护，最终变成系统风险</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>技术债治理不是情绪化重写，而是识别风险、评估收益、拆分阶段并结合业务节奏逐步偿还。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">技术债评估维度：</span><br><span class="line">1. 是否频繁影响需求交付</span><br><span class="line">2. 是否增加线上风险</span><br><span class="line">3. 是否阻碍新人理解</span><br><span class="line">4. 是否有明确替代方案</span><br><span class="line">5. 是否能分阶段治理</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">治理方式：局部重构、补测试、补文档、统一规范、灰度替换</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>技术债治理思考</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>经验沉淀</category>
      </categories>
      <tags>
        <tag>可维护性</tag>
        <tag>重构</tag>
        <tag>技术债</tag>
        <tag>架构治理</tag>
      </tags>
  </entry>
  <entry>
    <title>工作第一年总结</title>
    <url>/2020/12/27/2020-12-27-%E5%B7%A5%E4%BD%9C%E7%AC%AC%E4%B8%80%E5%B9%B4%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/first-year-review-cover.svg" alt="工作第一年总结封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>工作第一年的成长，不只是学会更多框架，而是开始理解工程、业务和协作之间的关系。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>只关注完成需求，没有主动理解业务价值</li>
<li>遇到问题时容易陷入局部技术细节</li>
<li>对协作、沟通、质量的理解不够系统</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>第一年的成长是从“把功能做完”转向“把事情做稳”，开始关注可维护性、协作效率和交付质量。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">第一年关键词：</span><br><span class="line">- 从学习框架到理解工程</span><br><span class="line">- 从完成任务到关注质量</span><br><span class="line">- 从单点开发到团队协作</span><br><span class="line">- 从解决问题到沉淀方法</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">下一年目标：系统设计、性能优化、稳定性建设、持续输出</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>工作第一年总结</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>年度总结</tag>
        <tag>成长总结</tag>
        <tag>工作复盘</tag>
        <tag>工程经验</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis缓存穿透解决方案</title>
    <url>/2021/03/14/2021-03-14-Redis%E7%BC%93%E5%AD%98%E7%A9%BF%E9%80%8F%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88/</url>
    <content><![CDATA[<p><img src="/images/posts/2021/redis-penetration-cover.svg" alt="Redis缓存穿透解决方案技术图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>缓存穿透的典型特征是缓存未命中后大量请求直接打到数据库。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">记录类型：故障排查</span><br><span class="line">记录年份：2021</span><br><span class="line">记录方式：现象、命令、日志、代码、配置、结论</span><br></pre></td></tr></table></figure>

<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Redis miss rate: 78%</span><br><span class="line">DB QPS: 2400 -&gt; 9600</span><br><span class="line">hot key pattern: product:not-exist:*</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli --bigkeys</span><br><span class="line">redis-cli monitor | grep product</span><br></pre></td></tr></table></figure>

<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">Product</span> <span class="variable">product</span> <span class="operator">=</span> redis.get(key);</span><br><span class="line"><span class="keyword">if</span> (product == <span class="literal">null</span>) &#123;</span><br><span class="line">    product = productMapper.selectById(id);</span><br><span class="line">    <span class="keyword">if</span> (product != <span class="literal">null</span>) &#123;</span><br><span class="line">        redis.set(key, product, <span class="number">30</span>, TimeUnit.MINUTES);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> <span class="string">&quot;product:&quot;</span> + id;</span><br><span class="line"><span class="type">Product</span> <span class="variable">cached</span> <span class="operator">=</span> redis.get(key);</span><br><span class="line"><span class="keyword">if</span> (cached != <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> cached == EMPTY_PRODUCT ? <span class="literal">null</span> : cached;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">Product</span> <span class="variable">db</span> <span class="operator">=</span> productMapper.selectById(id);</span><br><span class="line">redis.set(key, db == <span class="literal">null</span> ? EMPTY_PRODUCT : db, db == <span class="literal">null</span> ? <span class="number">60</span> : <span class="number">1800</span>, TimeUnit.SECONDS);</span><br><span class="line"><span class="keyword">return</span> db;</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">方案：缓存空值 + 参数校验 + 布隆过滤器</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘要点：</span><br><span class="line">1. 先保留现场，再尝试恢复</span><br><span class="line">2. 先确认影响范围，再判断根因</span><br><span class="line">3. 日志、监控、配置、代码需要串起来看</span><br><span class="line">4. 修复动作必须可验证</span><br><span class="line">5. 故障结束后补充检查项和自动化规则</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>缓存穿透</tag>
        <tag>布隆过滤器</tag>
        <tag>缓存设计</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis缓存雪崩实践</title>
    <url>/2021/04/18/2021-04-18-Redis%E7%BC%93%E5%AD%98%E9%9B%AA%E5%B4%A9%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>缓存雪崩通常不是单个 key 的问题，而是一批 key 在同一时间失效。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">记录类型：故障排查</span><br><span class="line">记录年份：2021</span><br><span class="line">记录方式：现象、命令、日志、代码、配置、结论</span><br></pre></td></tr></table></figure>

<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">10:00:00 Redis key expire batch</span><br><span class="line">10:00:03 DB connection pool exhausted</span><br><span class="line">10:00:05 order-api error rate 23%</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli info keyspace</span><br><span class="line">redis-cli --scan --pattern <span class="string">&#x27;home:*&#x27;</span> | <span class="built_in">head</span></span><br></pre></td></tr></table></figure>

<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">风险点：</span><br><span class="line">1. 大量 key 使用同一过期时间</span><br><span class="line">2. 热点数据没有预热</span><br><span class="line">3. Redis 故障时没有降级策略</span><br><span class="line">4. 后端 DB 无保护</span><br></pre></td></tr></table></figure>

<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">baseSeconds</span> <span class="operator">=</span> <span class="number">30</span> * <span class="number">60</span>;</span><br><span class="line"><span class="type">int</span> <span class="variable">randomSeconds</span> <span class="operator">=</span> ThreadLocalRandom.current().nextInt(<span class="number">0</span>, <span class="number">10</span> * <span class="number">60</span>);</span><br><span class="line">redisTemplate.opsForValue().set(key, value, baseSeconds + randomSeconds, TimeUnit.SECONDS);</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">处理：过期时间随机化、热点预热、限流降级、多级缓存</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘要点：</span><br><span class="line">1. 先保留现场，再尝试恢复</span><br><span class="line">2. 先确认影响范围，再判断根因</span><br><span class="line">3. 日志、监控、配置、代码需要串起来看</span><br><span class="line">4. 修复动作必须可验证</span><br><span class="line">5. 故障结束后补充检查项和自动化规则</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>缓存设计</tag>
        <tag>缓存雪崩</tag>
        <tag>高可用</tag>
      </tags>
  </entry>
  <entry>
    <title>Nginx反向代理问题</title>
    <url>/2021/05/23/2021-05-23-Nginx%E5%8F%8D%E5%90%91%E4%BB%A3%E7%90%86%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<p><img src="/images/posts/2021/nginx-proxy-cover.svg" alt="Nginx反向代理问题技术图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>反向代理问题需要同时看 Nginx、上游服务和网络连通性。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">记录类型：故障排查</span><br><span class="line">记录年份：2021</span><br><span class="line">记录方式：现象、命令、日志、代码、配置、结论</span><br></pre></td></tr></table></figure>

<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">upstream timed out (110: Connection timed out) while reading response header from upstream</span><br><span class="line">client: 10.0.1.24, server: api.demo.com, request: &quot;POST /api/upload HTTP/1.1&quot;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">tail</span> -f /var/log/nginx/error.log</span><br><span class="line">curl -I https://api.demo.com/api/health</span><br></pre></td></tr></table></figure>

<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">location</span> /api/ &#123;</span><br><span class="line">    <span class="attribute">proxy_pass</span> http://backend;</span><br><span class="line">    <span class="attribute">proxy_connect_timeout</span> <span class="number">3s</span>;</span><br><span class="line">    <span class="attribute">proxy_read_timeout</span> <span class="number">3s</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nginx -t</span><br><span class="line">curl http://127.0.0.1:8080/api/health</span><br></pre></td></tr></table></figure>

<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="section">upstream</span> backend &#123;</span><br><span class="line">    <span class="attribute">server</span> <span class="number">127.0.0.1:8080</span> max_fails=<span class="number">3</span> fail_timeout=<span class="number">30s</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="section">location</span> /api/ &#123;</span><br><span class="line">    <span class="attribute">proxy_pass</span> http://backend;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> Host <span class="variable">$host</span>;</span><br><span class="line">    <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">    <span class="attribute">proxy_connect_timeout</span> <span class="number">2s</span>;</span><br><span class="line">    <span class="attribute">proxy_read_timeout</span> <span class="number">30s</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘要点：</span><br><span class="line">1. 先保留现场，再尝试恢复</span><br><span class="line">2. 先确认影响范围，再判断根因</span><br><span class="line">3. 日志、监控、配置、代码需要串起来看</span><br><span class="line">4. 修复动作必须可验证</span><br><span class="line">5. 故障结束后补充检查项和自动化规则</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>部署</tag>
        <tag>Nginx</tag>
        <tag>反向代理</tag>
        <tag>网关</tag>
      </tags>
  </entry>
  <entry>
    <title>一次接口超时问题排查</title>
    <url>/2021/01/17/2021-01-17-%E4%B8%80%E6%AC%A1%E6%8E%A5%E5%8F%A3%E8%B6%85%E6%97%B6%E9%97%AE%E9%A2%98%E6%8E%92%E6%9F%A5/</url>
    <content><![CDATA[<p><img src="/images/posts/2021/api-timeout-cover.svg" alt="一次接口超时问题排查技术图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>接口超时的排查重点是确认耗时发生在入口、数据库、缓存还是下游服务。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">记录类型：故障排查</span><br><span class="line">记录年份：2021</span><br><span class="line">记录方式：现象、命令、日志、代码、配置、结论</span><br></pre></td></tr></table></figure>

<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">2021-01-17 20:18:42 WARN  order-api request timeout</span><br><span class="line">path=/api/orders/submit</span><br><span class="line">cost=8123ms</span><br><span class="line">traceId=adf9c0a2b1</span><br><span class="line">upstream=payment-service</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -w <span class="string">&quot;dns:%&#123;time_namelookup&#125; connect:%&#123;time_connect&#125; start:%&#123;time_starttransfer&#125; total:%&#123;time_total&#125;</span></span><br><span class="line"><span class="string">&quot;</span>   -o /dev/null -s http://localhost:8080/api/orders/submit</span><br></pre></td></tr></table></figure>

<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep <span class="string">&quot;adf9c0a2b1&quot;</span> app.log</span><br><span class="line"><span class="built_in">cat</span> app.log | grep <span class="string">&quot;payment-service&quot;</span> | <span class="built_in">tail</span> -100</span><br><span class="line">jstack $(pgrep java) &gt; /tmp/order-api.jstack</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">排查顺序：</span><br><span class="line">1. 确认是单接口还是全站超时</span><br><span class="line">2. 对比接口入口耗时和下游调用耗时</span><br><span class="line">3. 查看线程是否阻塞在外部 HTTP 调用</span><br><span class="line">4. 检查连接池、超时参数、重试次数</span><br></pre></td></tr></table></figure>

<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">payment:</span></span><br><span class="line">  <span class="attr">connect-timeout-ms:</span> <span class="number">1000</span></span><br><span class="line">  <span class="attr">read-timeout-ms:</span> <span class="number">2000</span></span><br><span class="line">  <span class="attr">max-retry:</span> <span class="number">1</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">RequestConfig</span> <span class="variable">config</span> <span class="operator">=</span> RequestConfig.custom()</span><br><span class="line">    .setConnectTimeout(<span class="number">1000</span>)</span><br><span class="line">    .setSocketTimeout(<span class="number">2000</span>)</span><br><span class="line">    .setConnectionRequestTimeout(<span class="number">500</span>)</span><br><span class="line">    .build();</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘要点：</span><br><span class="line">1. 先保留现场，再尝试恢复</span><br><span class="line">2. 先确认影响范围，再判断根因</span><br><span class="line">3. 日志、监控、配置、代码需要串起来看</span><br><span class="line">4. 修复动作必须可验证</span><br><span class="line">5. 故障结束后补充检查项和自动化规则</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>Spring Boot</tag>
        <tag>接口超时</tag>
        <tag>故障排查</tag>
        <tag>性能分析</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux磁盘爆满处理</title>
    <url>/2021/06/20/2021-06-20-Linux%E7%A3%81%E7%9B%98%E7%88%86%E6%BB%A1%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>磁盘爆满处理要先恢复服务写入能力，再定位增长来源。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">记录类型：故障排查</span><br><span class="line">记录年份：2021</span><br><span class="line">记录方式：现象、命令、日志、代码、配置、结论</span><br></pre></td></tr></table></figure>

<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">df</span> -h</span><br><span class="line">Filesystem      Size  Used Avail Use% Mounted on</span><br><span class="line">/dev/vda1        40G   40G     0 100% /</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">应用报错：No space left on device</span><br><span class="line">MySQL 写入失败</span><br><span class="line">日志无法继续滚动</span><br></pre></td></tr></table></figure>

<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">du</span> -h --max-depth=1 / | <span class="built_in">sort</span> -hr | <span class="built_in">head</span></span><br><span class="line"><span class="built_in">du</span> -h --max-depth=1 /var/log | <span class="built_in">sort</span> -hr | <span class="built_in">head</span></span><br><span class="line">lsof | grep deleted | <span class="built_in">head</span></span><br></pre></td></tr></table></figure>

<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">find /var/log -name <span class="string">&quot;*.log&quot;</span> -mtime +7 -<span class="built_in">type</span> f -delete</span><br><span class="line">journalctl --vacuum-time=7d</span><br><span class="line"></span><br><span class="line"><span class="comment"># 处理已删除但仍被进程占用的文件</span></span><br><span class="line">lsof | grep deleted</span><br><span class="line">systemctl restart app</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘要点：</span><br><span class="line">1. 先保留现场，再尝试恢复</span><br><span class="line">2. 先确认影响范围，再判断根因</span><br><span class="line">3. 日志、监控、配置、代码需要串起来看</span><br><span class="line">4. 修复动作必须可验证</span><br><span class="line">5. 故障结束后补充检查项和自动化规则</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>运维</tag>
        <tag>磁盘空间</tag>
        <tag>日志清理</tag>
      </tags>
  </entry>
  <entry>
    <title>死锁问题定位过程</title>
    <url>/2021/09/19/2021-09-19-%E6%AD%BB%E9%94%81%E9%97%AE%E9%A2%98%E5%AE%9A%E4%BD%8D%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<p><img src="/images/posts/2021/deadlock-debug-cover.svg" alt="死锁问题定位过程技术图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>死锁定位需要保存现场，优先抓取线程 dump，而不是盲目重启。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">记录类型：故障排查</span><br><span class="line">记录年份：2021</span><br><span class="line">记录方式：现象、命令、日志、代码、配置、结论</span><br></pre></td></tr></table></figure>

<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">接口无响应，CPU 正常，部分请求一直 pending</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jps -l</span><br><span class="line">jstack $(pgrep java) &gt; /tmp/deadlock.jstack</span><br><span class="line">grep -n <span class="string">&quot;deadlock&quot;</span> /tmp/deadlock.jstack</span><br></pre></td></tr></table></figure>

<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Found one Java-level deadlock:</span><br><span class="line">&quot;thread-a&quot; waiting to lock monitor 0x001</span><br><span class="line">&quot;thread-b&quot; waiting to lock monitor 0x002</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">synchronized</span> (orderLock) &#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (stockLock) &#123;</span><br><span class="line">        <span class="comment">// update order and stock</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">lockKey</span> <span class="operator">=</span> orderId.compareTo(stockId) &lt; <span class="number">0</span></span><br><span class="line">    ? orderId + <span class="string">&quot;:&quot;</span> + stockId</span><br><span class="line">    : stockId + <span class="string">&quot;:&quot;</span> + orderId;</span><br><span class="line"></span><br><span class="line"><span class="keyword">synchronized</span> (lockKey.intern()) &#123;</span><br><span class="line">    updateOrder();</span><br><span class="line">    updateStock();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">原则：统一加锁顺序、缩小锁范围、避免锁内远程调用</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘要点：</span><br><span class="line">1. 先保留现场，再尝试恢复</span><br><span class="line">2. 先确认影响范围，再判断根因</span><br><span class="line">3. 日志、监控、配置、代码需要串起来看</span><br><span class="line">4. 修复动作必须可验证</span><br><span class="line">5. 故障结束后补充检查项和自动化规则</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>死锁</tag>
        <tag>线程 dump</tag>
        <tag>并发问题</tag>
      </tags>
  </entry>
  <entry>
    <title>线程池使用踩坑记录</title>
    <url>/2021/08/15/2021-08-15-%E7%BA%BF%E7%A8%8B%E6%B1%A0%E4%BD%BF%E7%94%A8%E8%B8%A9%E5%9D%91%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>线程池问题往往不是线程不够，而是队列、任务耗时和拒绝策略设计不合理。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">记录类型：故障排查</span><br><span class="line">记录年份：2021</span><br><span class="line">记录方式：现象、命令、日志、代码、配置、结论</span><br></pre></td></tr></table></figure>

<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">active=32 poolSize=32 queueSize=10000 completed=1200</span><br><span class="line">接口响应时间从 200ms 上升到 6s</span><br><span class="line">CPU 不高，任务堆积明显</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Executors.newFixedThreadPool(<span class="number">32</span>);</span><br></pre></td></tr></table></figure>

<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">问题：</span><br><span class="line">1. 队列过大，流量尖峰时延迟被隐藏</span><br><span class="line">2. 无拒绝策略监控</span><br><span class="line">3. 任务内部还有远程调用</span><br><span class="line">4. 线程池参数没有按业务隔离</span><br></pre></td></tr></table></figure>

<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">ThreadPoolExecutor</span> <span class="variable">executor</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>(</span><br><span class="line">    <span class="number">16</span>,</span><br><span class="line">    <span class="number">32</span>,</span><br><span class="line">    <span class="number">60</span>,</span><br><span class="line">    TimeUnit.SECONDS,</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">ArrayBlockingQueue</span>&lt;&gt;(<span class="number">500</span>),</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>.CallerRunsPolicy()</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">监控指标：activeCount、queueSize、completedTaskCount、rejectCount</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘要点：</span><br><span class="line">1. 先保留现场，再尝试恢复</span><br><span class="line">2. 先确认影响范围，再判断根因</span><br><span class="line">3. 日志、监控、配置、代码需要串起来看</span><br><span class="line">4. 修复动作必须可验证</span><br><span class="line">5. 故障结束后补充检查项和自动化规则</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>线程池</tag>
        <tag>并发</tag>
        <tag>性能问题</tag>
      </tags>
  </entry>
  <entry>
    <title>RabbitMQ消息丢失排查</title>
    <url>/2021/10/24/2021-10-24-RabbitMQ%E6%B6%88%E6%81%AF%E4%B8%A2%E5%A4%B1%E6%8E%92%E6%9F%A5/</url>
    <content><![CDATA[<p><img src="/images/posts/2021/rabbitmq-message-loss-cover.svg" alt="RabbitMQ消息丢失排查技术图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>消息丢失要按生产、交换机、队列、消费确认四段链路排查。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">记录类型：故障排查</span><br><span class="line">记录年份：2021</span><br><span class="line">记录方式：现象、命令、日志、代码、配置、结论</span><br></pre></td></tr></table></figure>

<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">订单已支付，但积分未到账</span><br><span class="line">producer log: message sent</span><br><span class="line">consumer log: no message received</span><br><span class="line">queue ready=0 unacked=0</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rabbitmqctl list_queues name durable messages_ready messages_unacknowledged</span><br><span class="line">rabbitmqctl list_exchanges name <span class="built_in">type</span> durable</span><br></pre></td></tr></table></figure>

<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">排查链路：</span><br><span class="line">producer confirm -&gt; exchange durable -&gt; queue durable -&gt; binding key -&gt; consumer ack -&gt; retry/dlx</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">rabbitTemplate.convertAndSend(<span class="string">&quot;point.exchange&quot;</span>, <span class="string">&quot;point.add&quot;</span>, message);</span><br></pre></td></tr></table></figure>

<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">rabbitTemplate.setConfirmCallback((correlationData, ack, cause) -&gt; &#123;</span><br><span class="line">    <span class="keyword">if</span> (!ack) &#123;</span><br><span class="line">        log.error(<span class="string">&quot;message publish failed, id=&#123;&#125;, cause=&#123;&#125;&quot;</span>, correlationData.getId(), cause);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">rabbitmq:</span></span><br><span class="line">    <span class="attr">publisher-confirm-type:</span> <span class="string">correlated</span></span><br><span class="line">    <span class="attr">publisher-returns:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">listener:</span></span><br><span class="line">      <span class="attr">simple:</span></span><br><span class="line">        <span class="attr">acknowledge-mode:</span> <span class="string">manual</span></span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘要点：</span><br><span class="line">1. 先保留现场，再尝试恢复</span><br><span class="line">2. 先确认影响范围，再判断根因</span><br><span class="line">3. 日志、监控、配置、代码需要串起来看</span><br><span class="line">4. 修复动作必须可验证</span><br><span class="line">5. 故障结束后补充检查项和自动化规则</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>RabbitMQ</tag>
        <tag>消息队列</tag>
        <tag>消息丢失</tag>
        <tag>可靠性</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Cloud学习笔记</title>
    <url>/2022/02/20/2022-02-20-Spring%20Cloud%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>为什么会研究这个问题</p>
<p>在理解了微服务的基本拆分思路之后，我开始系统学习 Spring Cloud。相比单个 Spring Boot 应用，Spring Cloud 更像是一组微服务治理工具箱，覆盖注册发现、配置管理、网关路由、服务调用、熔断限流等能力。研究它的原因也很明确：当服务数量超过两三个之后，手写服务地址、手动维护配置、靠人肉检查调用链会变得非常不可靠。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>遇到了什么情况</p>
<p>刚开始学习时，最大的问题是概念很多但边界不清。注册中心、配置中心、网关、Feign、负载均衡这些组件看起来都能跑 Demo，但真正放到项目里，会发现不知道每个组件负责什么。比如服务调用失败到底是注册中心没发现服务，还是网关路由错了，还是 Feign 超时时间太短。组件之间的关系如果没有先画清楚，排查问题时会非常被动。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>排查过程</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Spring Cloud 常见组件关系：</span><br><span class="line"></span><br><span class="line">client</span><br><span class="line">  ↓</span><br><span class="line">Gateway</span><br><span class="line">  ↓</span><br><span class="line">service-a  --Feign--&gt; service-b</span><br><span class="line">  ↓              ↓</span><br><span class="line">Nacos Registry / Config</span><br></pre></td></tr></table></figure>

<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">order-service</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">nacos:</span></span><br><span class="line">      <span class="attr">discovery:</span></span><br><span class="line">        <span class="attr">server-addr:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:8848</span></span><br><span class="line">      <span class="attr">config:</span></span><br><span class="line">        <span class="attr">server-addr:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:8848</span></span><br></pre></td></tr></table></figure>

<p>排查时我会先确认服务是否注册，再确认配置是否加载，最后确认调用链是否正常。顺序不能乱，否则容易被表面异常误导。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>具体操作</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">学习顺序：</span><br><span class="line">1. Spring Boot 单服务启动</span><br><span class="line">2. 接入 Nacos 注册中心</span><br><span class="line">3. 通过 OpenFeign 完成服务调用</span><br><span class="line">4. 增加 Gateway 统一入口</span><br><span class="line">5. 再考虑配置中心、熔断限流、链路追踪</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@FeignClient(name = &quot;stock-service&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">StockClient</span> &#123;</span><br><span class="line">    <span class="meta">@GetMapping(&quot;/stock/&#123;skuId&#125;&quot;)</span></span><br><span class="line">    Integer <span class="title function_">getStock</span><span class="params">(<span class="meta">@PathVariable</span> Long skuId)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我把每个组件都拆成最小可运行样例，再逐步组合。这样做虽然慢一点，但能清楚知道问题出现在哪一层。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>经验沉淀</p>
<p>Spring Cloud 的学习重点不是背组件名，而是理解服务治理的分层。注册中心解决服务在哪里，配置中心解决配置怎么统一，网关解决入口怎么收口，Feign 解决服务之间怎么调用。只有把这些问题放回真实架构场景里，组件才有意义。</p>
]]></content>
      <categories>
        <category>架构成长</category>
      </categories>
      <tags>
        <tag>配置管理</tag>
        <tag>Spring Cloud</tag>
        <tag>微服务</tag>
        <tag>注册中心</tag>
      </tags>
  </entry>
  <entry>
    <title>年度故障案例总结</title>
    <url>/2021/12/26/2021-12-26-%E5%B9%B4%E5%BA%A6%E6%95%85%E9%9A%9C%E6%A1%88%E4%BE%8B%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p><img src="/images/posts/2021/incident-review-2021-cover.svg" alt="年度故障案例总结技术图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>年度故障总结的重点不是罗列事故，而是把重复问题转成规范和工具。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">记录类型：故障排查</span><br><span class="line">记录年份：2021</span><br><span class="line">记录方式：现象、命令、日志、代码、配置、结论</span><br></pre></td></tr></table></figure>

<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">2021 故障分类：</span><br><span class="line">- 慢 SQL：3 起</span><br><span class="line">- 缓存问题：2 起</span><br><span class="line">- 部署配置：2 起</span><br><span class="line">- 并发问题：2 起</span><br><span class="line">- 消息可靠性：1 起</span><br></pre></td></tr></table></figure>

<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘模板：</span><br><span class="line">1. 故障时间线</span><br><span class="line">2. 影响范围</span><br><span class="line">3. 直接原因</span><br><span class="line">4. 根本原因</span><br><span class="line">5. 恢复动作</span><br><span class="line">6. 后续改进</span><br><span class="line">7. 责任人和截止时间</span><br></pre></td></tr></table></figure>

<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">沉淀项：</span><br><span class="line">- 慢查询巡检脚本</span><br><span class="line">- Redis key 过期时间规范</span><br><span class="line">- 线程池监控指标</span><br><span class="line">- 发布前配置检查清单</span><br><span class="line">- MQ 可靠投递模板</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘要点：</span><br><span class="line">1. 先保留现场，再尝试恢复</span><br><span class="line">2. 先确认影响范围，再判断根因</span><br><span class="line">3. 日志、监控、配置、代码需要串起来看</span><br><span class="line">4. 修复动作必须可验证</span><br><span class="line">5. 故障结束后补充检查项和自动化规则</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>年度总结</tag>
        <tag>故障复盘</tag>
        <tag>稳定性</tag>
        <tag>经验沉淀</tag>
      </tags>
  </entry>
  <entry>
    <title>微服务架构初体验</title>
    <url>/2022/01/16/2022-01-16-%E5%BE%AE%E6%9C%8D%E5%8A%A1%E6%9E%B6%E6%9E%84%E5%88%9D%E4%BD%93%E9%AA%8C/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>为什么会研究这个问题</p>
<p>2022 年开始接触微服务时，我最直接的感受是：它不是把一个系统拆成多个服务那么简单。过去做单体应用，所有模块在一个进程里，调用方式直接、事务边界清楚、部署也简单。但当业务模块逐渐增多，订单、用户、库存、支付之间的代码耦合越来越明显，每次改一个功能都要回归一大片逻辑，发布窗口也越来越长。正是在这种背景下，我开始研究微服务架构。最初的目标并不是追求架构先进，而是想弄清楚系统拆分之后，团队协作、独立部署、故障隔离到底能解决哪些真实问题。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>遇到了什么情况</p>
<p>第一次拆服务时遇到的情况比较典型：模块拆出去了，但调用关系反而更复杂；原来一次本地方法调用，现在变成了网络请求；原来一个数据库事务能覆盖的流程，现在要考虑分布式事务或最终一致性。尤其是订单创建这个流程，涉及用户校验、库存扣减、优惠计算和支付记录，拆开之后每一步都可能失败。如果只按代码包结构拆服务，很容易变成“物理拆分、逻辑仍然耦合”。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>排查过程</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">单体结构：</span><br><span class="line">app</span><br><span class="line">├── user</span><br><span class="line">├── order</span><br><span class="line">├── stock</span><br><span class="line">└── payment</span><br><span class="line"></span><br><span class="line">初步拆分：</span><br><span class="line">user-service  -&gt; user_db</span><br><span class="line">order-service -&gt; order_db</span><br><span class="line">stock-service -&gt; stock_db</span><br><span class="line">pay-service   -&gt; pay_db</span><br></pre></td></tr></table></figure>

<p>排查拆分边界时，我主要看三个维度：数据归属、业务闭环、变更频率。用户资料和订单生命周期明显不是同一类数据；库存服务变更频率不高，但对一致性要求高；支付服务外部依赖多，需要单独隔离。由此可以判断，服务拆分应该围绕业务能力，而不是围绕代码目录。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>具体操作</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">拆分原则：</span><br><span class="line">1. 先按业务能力拆，而不是按技术层拆</span><br><span class="line">2. 每个服务尽量拥有自己的数据表</span><br><span class="line">3. 服务之间通过接口通信，不共享数据库</span><br><span class="line">4. 核心链路先少拆，边界稳定后再细化</span><br><span class="line">5. 引入服务治理前先把日志和调用链补齐</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">StockClient</span> &#123;</span><br><span class="line">    <span class="type">boolean</span> <span class="title function_">deduct</span><span class="params">(Long skuId, Integer count)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>实际落地时，我没有一次性把所有模块拆开，而是先从边界相对清晰的库存查询、用户信息查询开始。这样即使出现问题，也不会直接影响订单主流程。等到接口、日志、异常处理逐渐稳定后，再考虑更核心的链路。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>经验沉淀</p>
<p>微服务带来的不是天然简单，而是把原来隐藏在进程内部的问题显性化。网络延迟、接口契约、数据一致性、服务监控都会成为必须面对的工程问题。我的经验是：如果团队还没有稳定的部署流程、日志规范和接口文档，过早拆微服务只会放大混乱。微服务不是目标，降低复杂度、提升交付效率和故障隔离能力才是目标。</p>
]]></content>
      <categories>
        <category>架构成长</category>
      </categories>
      <tags>
        <tag>微服务</tag>
        <tag>架构设计</tag>
        <tag>系统拆分</tag>
        <tag>后端架构</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL慢查询分析</title>
    <url>/2021/02/21/2021-02-21-MySQL%E6%85%A2%E6%9F%A5%E8%AF%A2%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<p><img src="/images/posts/2021/mysql-slow-query-cover.svg" alt="MySQL慢查询分析技术图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>慢查询不能只看 SQL 文本，需要结合执行计划、索引、扫描行数和返回字段。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">记录类型：故障排查</span><br><span class="line">记录年份：2021</span><br><span class="line">记录方式：现象、命令、日志、代码、配置、结论</span><br></pre></td></tr></table></figure>

<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">set</span> <span class="keyword">global</span> slow_query_log <span class="operator">=</span> <span class="string">&#x27;ON&#x27;</span>;</span><br><span class="line"><span class="keyword">set</span> <span class="keyword">global</span> long_query_time <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line"># Time: 2021-02-21T14:22:19</span><br><span class="line"># Query_time: 6.834 Lock_time: 0.001 Rows_sent: 20 Rows_examined: 581239</span><br><span class="line">select * from order_info where user_id = 1001 order by created_at desc limit 20;</span><br></pre></td></tr></table></figure>

<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">explain <span class="keyword">select</span> <span class="operator">*</span></span><br><span class="line"><span class="keyword">from</span> order_info</span><br><span class="line"><span class="keyword">where</span> user_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> created_at <span class="keyword">desc</span></span><br><span class="line">limit <span class="number">20</span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">type: ALL</span><br><span class="line">possible_keys: idx_user_id</span><br><span class="line">key: NULL</span><br><span class="line">rows: 581239</span><br><span class="line">Extra: Using where; Using filesort</span><br></pre></td></tr></table></figure>

<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">alter table</span> order_info</span><br><span class="line"><span class="keyword">add</span> index idx_user_created(user_id, created_at);</span><br><span class="line"></span><br><span class="line">explain <span class="keyword">select</span> id, order_no, order_status, created_at</span><br><span class="line"><span class="keyword">from</span> order_info</span><br><span class="line"><span class="keyword">where</span> user_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line"><span class="keyword">order</span> <span class="keyword">by</span> created_at <span class="keyword">desc</span></span><br><span class="line">limit <span class="number">20</span>;</span><br></pre></td></tr></table></figure>

<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">复盘要点：</span><br><span class="line">1. 先保留现场，再尝试恢复</span><br><span class="line">2. 先确认影响范围，再判断根因</span><br><span class="line">3. 日志、监控、配置、代码需要串起来看</span><br><span class="line">4. 修复动作必须可验证</span><br><span class="line">5. 故障结束后补充检查项和自动化规则</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>MySQL</tag>
        <tag>索引</tag>
        <tag>慢查询</tag>
        <tag>SQL优化</tag>
      </tags>
  </entry>
  <entry>
    <title>Nacos,Gateway,OpenFeign实操</title>
    <url>/2022/03/27/2022-03-27-Nacos,Gateway,OpenFeign%E5%AE%9E%E6%93%8D/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>为什么会研究这个问题</p>
<p>在 Spring Cloud 的基础概念跑通之后，我把重点放到 Nacos、Gateway 和 OpenFeign 三个组件上。原因很简单：它们覆盖了微服务最基础的一条链路——服务注册、统一入口和服务间调用。只要这三块稳定，至少能搭出一个可运行的微服务骨架。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>遇到了什么情况</p>
<p>实操中遇到的问题主要集中在配置和路径上。服务已经启动，但 Nacos 控制台看不到实例；Gateway 能访问，但路由匹配不到目标服务；OpenFeign 调用时出现 404 或超时。很多问题不是代码逻辑错，而是服务名、路径前缀、配置层级和依赖版本不一致导致的。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>排查过程</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">请求链路：</span><br><span class="line">Browser/Postman</span><br><span class="line">  ↓ /api/order/create</span><br><span class="line">Gateway</span><br><span class="line">  ↓ lb://order-service/order/create</span><br><span class="line">order-service</span><br><span class="line">  ↓ Feign: stock-service</span><br><span class="line">stock-service</span><br></pre></td></tr></table></figure>

<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">gateway:</span></span><br><span class="line">      <span class="attr">routes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">id:</span> <span class="string">order-route</span></span><br><span class="line">          <span class="attr">uri:</span> <span class="string">lb://order-service</span></span><br><span class="line">          <span class="attr">predicates:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">Path=/api/order/**</span></span><br><span class="line">          <span class="attr">filters:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="string">StripPrefix=1</span></span><br></pre></td></tr></table></figure>

<p>如果 <code>StripPrefix</code> 配错，请求到后端服务的路径就会变化。排查时要同时看 Gateway 日志和后端 Controller 的映射。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>具体操作</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/order&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderController</span> &#123;</span><br><span class="line">    <span class="meta">@PostMapping(&quot;/create&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">create</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;ok&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@FeignClient(&quot;stock-service&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">StockFeignClient</span> &#123;</span><br><span class="line">    <span class="meta">@GetMapping(&quot;/stock/check&quot;)</span></span><br><span class="line">    Boolean <span class="title function_">checkStock</span><span class="params">(<span class="meta">@RequestParam</span> Long skuId)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">检查清单：</span><br><span class="line">1. spring.application.name 是否和 Feign 名称一致</span><br><span class="line">2. Nacos 控制台是否有实例</span><br><span class="line">3. Gateway 路由 Path 是否匹配</span><br><span class="line">4. StripPrefix 是否符合后端路径</span><br><span class="line">5. Feign 超时时间是否合理</span><br></pre></td></tr></table></figure>

<p>通过清单逐项排查，比直接看异常堆栈更稳定。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>经验沉淀</p>
<p>Nacos、Gateway、OpenFeign 组合起来并不复杂，复杂的是路径、服务名和配置之间的细节一致性。我的经验是：每新增一个服务，先验证注册，再验证网关，再验证 Feign，不要一次性把所有配置都写完再排查。</p>
]]></content>
      <categories>
        <category>架构成长</category>
      </categories>
      <tags>
        <tag>Spring Cloud</tag>
        <tag>Nacos</tag>
        <tag>Gateway</tag>
        <tag>OpenFeign</tag>
      </tags>
  </entry>
  <entry>
    <title>RabbitMQ可靠性设计</title>
    <url>/2022/06/19/2022-06-19-RabbitMQ%E5%8F%AF%E9%9D%A0%E6%80%A7%E8%AE%BE%E8%AE%A1/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>为什么会研究这个问题</p>
<p>随着系统拆分，很多原本同步完成的操作开始改成异步。比如订单创建后发积分、发通知、写操作日志，这些逻辑不应该阻塞主流程，于是 RabbitMQ 就进入了架构设计范围。研究可靠性设计，是因为消息队列一旦使用不当，很容易出现“主流程成功了，但后续动作丢了”的问题。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>遇到了什么情况</p>
<p>最典型的问题是消息丢失和重复消费。生产者以为消息发出去了，但交换机没有正确接收；队列没有持久化，服务重启后消息消失；消费者处理成功但 ack 失败，导致消息重复投递。如果没有可靠性设计，问题往往发生在线上，而且不容易第一时间发现。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>排查过程</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">可靠投递链路：</span><br><span class="line">Producer</span><br><span class="line">  -&gt; Exchange</span><br><span class="line">  -&gt; Queue</span><br><span class="line">  -&gt; Consumer</span><br><span class="line">  -&gt; Ack</span><br></pre></td></tr></table></figure>

<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">rabbitmq:</span></span><br><span class="line">    <span class="attr">publisher-confirm-type:</span> <span class="string">correlated</span></span><br><span class="line">    <span class="attr">publisher-returns:</span> <span class="literal">true</span></span><br><span class="line">    <span class="attr">listener:</span></span><br><span class="line">      <span class="attr">simple:</span></span><br><span class="line">        <span class="attr">acknowledge-mode:</span> <span class="string">manual</span></span><br></pre></td></tr></table></figure>

<p>排查消息可靠性时，我会把链路拆成四段：生产确认、路由确认、队列持久化、消费确认。任何一段缺失，都可能导致数据不一致。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>具体操作</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">rabbitTemplate.setConfirmCallback((correlationData, ack, cause) -&gt; &#123;</span><br><span class="line">    <span class="keyword">if</span> (!ack) &#123;</span><br><span class="line">        log.error(<span class="string">&quot;message send failed, id=&#123;&#125;, cause=&#123;&#125;&quot;</span>, correlationData.getId(), cause);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RabbitListener(queues = &quot;order.point.queue&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">consume</span><span class="params">(Message message, Channel channel)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">    <span class="type">long</span> <span class="variable">tag</span> <span class="operator">=</span> message.getMessageProperties().getDeliveryTag();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        handle(message);</span><br><span class="line">        channel.basicAck(tag, <span class="literal">false</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        channel.basicNack(tag, <span class="literal">false</span>, <span class="literal">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">设计补充：</span><br><span class="line">- 业务表增加 message_id 去重</span><br><span class="line">- 失败消息进入死信队列</span><br><span class="line">- 消费失败次数需要监控</span><br><span class="line">- 关键消息保留发送记录</span><br></pre></td></tr></table></figure>


<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>经验沉淀</p>
<p>RabbitMQ 可靠性不是一个配置能解决的，而是一组机制共同保证。生产确认保证消息发出去，持久化保证消息不因重启丢失，手动 ack 保证消费结果可控，幂等设计保证重复投递不会破坏数据。</p>
]]></content>
      <categories>
        <category>架构成长</category>
      </categories>
      <tags>
        <tag>RabbitMQ</tag>
        <tag>架构设计</tag>
        <tag>消息可靠性</tag>
        <tag>异步解耦</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis高可用架构</title>
    <url>/2022/07/24/2022-07-24-Redis%E9%AB%98%E5%8F%AF%E7%94%A8%E6%9E%B6%E6%9E%84/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>为什么会研究这个问题</p>
<p>Redis 从单机缓存进入核心链路后，高可用就必须认真考虑。最初使用 Redis 只是为了缓存热点数据，服务挂了最多回源数据库。但随着登录态、限流、库存预扣等场景进入 Redis，单点故障的影响范围明显扩大，所以需要研究 Redis 高可用架构。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>遇到了什么情况</p>
<p>单机 Redis 最大的问题是故障恢复依赖人工。Redis 进程挂掉、机器重启、内存打满都会导致业务抖动。如果没有主从、哨兵或集群方案，应用只能等待人工处理。另一个问题是，很多人以为主从复制就等于高可用，但主节点故障后如果不能自动切换，仍然无法支撑线上稳定性。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>排查过程</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Sentinel 架构：</span><br><span class="line"></span><br><span class="line">app</span><br><span class="line"> ↓</span><br><span class="line">sentinel-1 sentinel-2 sentinel-3</span><br><span class="line"> ↓</span><br><span class="line">redis-master</span><br><span class="line"> ↓</span><br><span class="line">redis-slave-1</span><br><span class="line">redis-slave-2</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sentinel monitor mymaster 127.0.0.1 6379 2</span><br><span class="line">sentinel down-after-milliseconds mymaster 5000</span><br><span class="line">sentinel failover-timeout mymaster 60000</span><br><span class="line">sentinel parallel-syncs mymaster 1</span><br></pre></td></tr></table></figure>

<p>排查高可用时，重点看三个问题：主从复制是否正常，Sentinel 是否能判断主观下线，故障转移后客户端是否能感知新主节点。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>具体操作</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">sentinel:</span></span><br><span class="line">      <span class="attr">master:</span> <span class="string">mymaster</span></span><br><span class="line">      <span class="attr">nodes:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:26379</span></span><br><span class="line">        <span class="bullet">-</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:26380</span></span><br><span class="line">        <span class="bullet">-</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:26381</span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli -p 26379 sentinel masters</span><br><span class="line">redis-cli -p 26379 sentinel slaves mymaster</span><br><span class="line">redis-cli -p 26379 sentinel failover mymaster</span><br></pre></td></tr></table></figure>

<p>对于缓存场景，我会同时配合过期时间随机化、本地缓存和降级策略，避免 Redis 故障时所有流量瞬间压到数据库。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>经验沉淀</p>
<p>Redis 高可用不能只看 Redis 本身，还要看客户端、业务降级和数据库保护。Sentinel 解决主从切换问题，但不解决缓存击穿、雪崩和热点 key 问题。真正可靠的缓存架构，需要高可用、限流、降级和监控一起设计。</p>
]]></content>
      <categories>
        <category>架构成长</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>高可用</tag>
        <tag>缓存架构</tag>
        <tag>哨兵模式</tag>
      </tags>
  </entry>
  <entry>
    <title>线上问题排查记录</title>
    <url>/2020/10/31/2020-10-31-%E7%BA%BF%E4%B8%8A%E9%97%AE%E9%A2%98%E6%8E%92%E6%9F%A5%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<p><img src="/images/posts/2020/prod-debug-cover.svg" alt="线上问题排查记录封面图"></p>
<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>线上问题最考验的不是技术点本身，而是排查顺序、信息完整度和沟通节奏。</p>
<p>如果说 2019 年的重点是把项目做出来，那么 2020 年更像是开始理解“项目为什么要这样做”。很多问题表面上是技术问题，往深处看其实是规范、协作、边界和长期维护的问题。</p>
<p>这一年我逐渐意识到，成熟的开发不是写出更复杂的代码，而是在合适的约束下，把问题拆清楚，把风险讲明白，把结果稳定交付出去。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>这类问题在工作中并不少见，当时我主要遇到下面几个情况：</p>
<ol>
<li>报警出现后先猜原因，缺少证据链</li>
<li>日志、监控、数据库信息没有按顺序收集</li>
<li>修复后没有复盘，问题容易重复出现</li>
</ol>
<p>这些问题的共同点是：短期看只是效率问题，长期看会逐渐变成质量问题。它们不会立刻让系统崩掉，但会在需求迭代、人员交接、线上排查时持续放大成本。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>线上排查要避免凭经验跳结论，应该按现象、影响范围、变更记录、日志证据和恢复手段逐步推进。</p>
<p>我后来总结出一个判断标准：凡是需要多人长期参与的事情，都不能只依赖个人经验。经验如果不能被记录、复用和检查，就很难成为团队能力。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>我的处理方式不是追求一步到位，而是先把最容易失控的部分标准化，再逐步优化细节。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看错误日志</span></span><br><span class="line">grep -n <span class="string">&quot;ERROR&quot;</span> app.log | <span class="built_in">tail</span> -50</span><br><span class="line"></span><br><span class="line"><span class="comment"># 按 requestId 追踪</span></span><br><span class="line">grep <span class="string">&quot;req-20201031001&quot;</span> app.log</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看进程资源</span></span><br><span class="line">top -Hp $(pgrep java)</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">排查顺序：现象 -&gt; 影响范围 -&gt; 最近变更 -&gt; 日志证据 -&gt; 临时恢复 -&gt; 根因复盘</span><br></pre></td></tr></table></figure>

<p>具体落地时，我更倾向于采用“小步治理”的方式：先让流程可见，再让责任明确，最后再谈自动化或平台化。这样推进阻力会小很多，也更容易让团队接受。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>关于 <strong>线上问题排查记录</strong>，我这一阶段最大的收获是：成熟的工程经验往往不是某个技巧，而是一套稳定处理问题的方式。</p>
<p>这篇文章可以沉淀为几条原则：</p>
<ul>
<li>先明确问题边界，再讨论技术方案；</li>
<li>能形成清单的事情，不要只靠记忆；</li>
<li>能通过规范减少沟通的地方，就不要反复口头确认；</li>
<li>重要结论要留痕，方便后续追溯；</li>
<li>技术方案要服务于交付质量，而不是只追求形式上的先进。</li>
</ul>
<p>2020 年以后，我对“经验”的理解发生了变化：经验不是踩过坑之后说一句“下次注意”，而是把坑转化为规范、流程、工具或文档，让后面的人少踩一次。</p>
]]></content>
      <categories>
        <category>问题排查</category>
      </categories>
      <tags>
        <tag>故障排查</tag>
        <tag>线上问题</tag>
        <tag>日志</tag>
        <tag>复盘</tag>
      </tags>
  </entry>
  <entry>
    <title>ELK日志平台搭建</title>
    <url>/2022/10/23/2022-10-23-ELK%E6%97%A5%E5%BF%97%E5%B9%B3%E5%8F%B0%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>为什么会研究这个问题</p>
<p>服务数量增加后，登录服务器看日志的方式开始变得低效。一次请求可能经过网关、订单服务、库存服务和消息消费者，如果每个服务都登录机器 grep 日志，排查效率会很低。因此我开始搭建 ELK 日志平台，把分散日志集中采集、存储和检索。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>遇到了什么情况</p>
<p>搭建过程中主要遇到三个问题：日志格式不统一，导致字段无法检索；Logstash 解析规则写得太随意，数据进 Elasticsearch 后结构混乱；日志量上来后磁盘增长很快，如果不设置索引生命周期，很容易把机器打满。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>排查过程</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">日志链路：</span><br><span class="line">app.log</span><br><span class="line">  -&gt; Filebeat</span><br><span class="line">  -&gt; Logstash</span><br><span class="line">  -&gt; Elasticsearch</span><br><span class="line">  -&gt; Kibana</span><br></pre></td></tr></table></figure>

<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;timestamp&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2022-10-23T21:00:00&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;level&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ERROR&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;service&quot;</span><span class="punctuation">:</span> <span class="string">&quot;order-service&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;traceId&quot;</span><span class="punctuation">:</span> <span class="string">&quot;abc123&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;message&quot;</span><span class="punctuation">:</span> <span class="string">&quot;create order failed&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>日志平台不是简单收集文本，而是要让日志结构化。只有字段固定，才能按 traceId、service、level、bizId 快速过滤。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>具体操作</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">filebeat.inputs:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">type:</span> <span class="string">log</span></span><br><span class="line">    <span class="attr">paths:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/data/logs/*.log</span></span><br><span class="line">    <span class="attr">fields:</span></span><br><span class="line">      <span class="attr">service:</span> <span class="string">order-service</span></span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">filter &#123;</span><br><span class="line">  json &#123;</span><br><span class="line">    source =&gt; &quot;message&quot;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">output &#123;</span><br><span class="line">  elasticsearch &#123;</span><br><span class="line">    hosts =&gt; [&quot;http://elasticsearch:9200&quot;]</span><br><span class="line">    index =&gt; &quot;app-log-%&#123;+YYYY.MM.dd&#125;&quot;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">落地规则：</span><br><span class="line">1. 应用统一 JSON 日志格式</span><br><span class="line">2. 必须打印 traceId</span><br><span class="line">3. ERROR 日志必须有异常栈</span><br><span class="line">4. 索引按天切分</span><br><span class="line">5. 保留周期按业务价值设置</span><br></pre></td></tr></table></figure>


<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>经验沉淀</p>
<p>ELK 的核心价值是让日志可检索、可关联、可追溯。平台搭起来只是第一步，更重要的是日志规范。没有 traceId、业务 id 和统一字段，ELK 也只能变成一个更大的文本搜索框。</p>
]]></content>
      <categories>
        <category>架构成长</category>
      </categories>
      <tags>
        <tag>ELK</tag>
        <tag>日志平台</tag>
        <tag>Elasticsearch</tag>
        <tag>Logstash</tag>
      </tags>
  </entry>
  <entry>
    <title>Prometheus监控部署</title>
    <url>/2022/11/20/2022-11-20-Prometheus%E7%9B%91%E6%8E%A7%E9%83%A8%E7%BD%B2/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>为什么会研究这个问题</p>
<p>日志能回答“发生了什么”，但不能很好回答“系统现在是否健康”。为了补齐监控能力，我开始研究 Prometheus。相比传统监控，Prometheus 更适合以指标为中心观察服务状态，比如 QPS、响应耗时、错误率、线程池队列、JVM 内存等。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>遇到了什么情况</p>
<p>刚开始部署时，最容易忽略的是指标设计。Prometheus 能抓到很多默认指标，但如果没有业务指标，排查时仍然不够。比如接口错误率升高，只知道 HTTP 500 变多，却不知道是哪个业务动作失败。另一个问题是标签设计不当，label 维度过多会导致时间序列爆炸。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>排查过程</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">监控链路：</span><br><span class="line">Spring Boot Actuator</span><br><span class="line">  -&gt; Prometheus scrape</span><br><span class="line">  -&gt; PromQL query</span><br><span class="line">  -&gt; Grafana dashboard</span><br><span class="line">  -&gt; Alertmanager</span><br></pre></td></tr></table></figure>

<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">management:</span></span><br><span class="line">  <span class="attr">endpoints:</span></span><br><span class="line">    <span class="attr">web:</span></span><br><span class="line">      <span class="attr">exposure:</span></span><br><span class="line">        <span class="attr">include:</span> <span class="string">prometheus,health,metrics</span></span><br><span class="line">  <span class="attr">metrics:</span></span><br><span class="line">    <span class="attr">tags:</span></span><br><span class="line">      <span class="attr">application:</span> <span class="string">order-service</span></span><br></pre></td></tr></table></figure>

<p>监控指标需要分层：基础资源、JVM、接口、数据库连接池、业务指标。只看 CPU 和内存，无法支撑复杂业务排查。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>具体操作</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">scrape_configs:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">job_name:</span> <span class="string">&quot;order-service&quot;</span></span><br><span class="line">    <span class="attr">metrics_path:</span> <span class="string">&quot;/actuator/prometheus&quot;</span></span><br><span class="line">    <span class="attr">static_configs:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">targets:</span> [<span class="string">&quot;order-service:8080&quot;</span>]</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">rate(http_server_requests_seconds_count[1m])</span><br><span class="line"></span><br><span class="line">histogram_quantile(</span><br><span class="line">  0.95,</span><br><span class="line">  rate(http_server_requests_seconds_bucket[5m])</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">sum(rate(http_server_requests_seconds_count&#123;status=~&quot;5..&quot;&#125;[1m]))</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">告警建议：</span><br><span class="line">- 5xx 错误率持续升高</span><br><span class="line">- P95 响应时间超过阈值</span><br><span class="line">- JVM Old 区使用率过高</span><br><span class="line">- 线程池队列持续堆积</span><br><span class="line">- 数据库连接池耗尽</span><br></pre></td></tr></table></figure>


<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>经验沉淀</p>
<p>Prometheus 的价值不只是画图，而是建立系统健康度指标。好的监控应该能提前暴露趋势，而不是等用户反馈后才看。日志、指标、告警和复盘结合起来，才算完整的可观测性建设。</p>
]]></content>
      <categories>
        <category>架构成长</category>
      </categories>
      <tags>
        <tag>可观测性</tag>
        <tag>Prometheus</tag>
        <tag>监控</tag>
        <tag>Grafana</tag>
      </tags>
  </entry>
  <entry>
    <title>从单体到微服务总结</title>
    <url>/2022/12/25/2022-12-25-%E4%BB%8E%E5%8D%95%E4%BD%93%E5%88%B0%E5%BE%AE%E6%9C%8D%E5%8A%A1%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>为什么会研究这个问题</p>
<p>回看 2022 年，从单体应用到微服务架构，我最大的变化不是学会了更多组件，而是开始理解架构演进的代价。单体并不落后，微服务也不天然先进。架构选择必须服务于团队规模、业务复杂度、交付频率和稳定性要求。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>遇到了什么情况</p>
<p>这一年遇到的典型问题包括：服务拆分后调用链变长，排查问题更困难；配置项分散，环境差异变大；消息队列、缓存、注册中心都引入了新的故障点；原来一个事务能保证的逻辑，现在要考虑最终一致性。微服务解决了一部分单体问题，同时也引入了新的复杂度。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>排查过程</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">单体优势：</span><br><span class="line">- 部署简单</span><br><span class="line">- 本地调试方便</span><br><span class="line">- 事务边界清晰</span><br><span class="line">- 小团队效率高</span><br><span class="line"></span><br><span class="line">微服务优势：</span><br><span class="line">- 服务边界清晰</span><br><span class="line">- 可独立部署</span><br><span class="line">- 故障隔离更好</span><br><span class="line">- 团队协作更灵活</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">架构演进路径：</span><br><span class="line">单体应用</span><br><span class="line">  -&gt; 模块化单体</span><br><span class="line">  -&gt; 拆分边界服务</span><br><span class="line">  -&gt; 引入注册发现</span><br><span class="line">  -&gt; 引入网关和配置中心</span><br><span class="line">  -&gt; 补齐日志、监控、链路追踪</span><br></pre></td></tr></table></figure>

<p>如果基础设施没有跟上，微服务会把复杂度直接暴露给业务开发。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>具体操作</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">落地建议：</span><br><span class="line">1. 先做模块化治理，再拆服务</span><br><span class="line">2. 优先拆边界清晰、风险较低的模块</span><br><span class="line">3. 服务之间禁止共享数据库</span><br><span class="line">4. 所有接口必须有文档和错误码</span><br><span class="line">5. 日志必须带 traceId</span><br><span class="line">6. 核心链路必须有监控和告警</span><br><span class="line">7. 异步消息必须设计幂等和补偿</span><br></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">基础设施清单：</span><br><span class="line">- 注册中心</span><br><span class="line">- 配置中心</span><br><span class="line">- API 网关</span><br><span class="line">- 统一日志</span><br><span class="line">- 指标监控</span><br><span class="line">- 链路追踪</span><br><span class="line">- 自动化部署</span><br></pre></td></tr></table></figure>


<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>经验沉淀</p>
<p>从单体到微服务，本质上是从代码架构走向工程体系。服务拆分只是表象，真正重要的是边界治理、稳定性建设、自动化部署和可观测性。我的经验是：不要为了微服务而微服务，当单体还能支撑业务时，先做好模块化和工程规范；当协作、发布和稳定性成为瓶颈时，再逐步演进。</p>
]]></content>
      <categories>
        <category>架构成长</category>
      </categories>
      <tags>
        <tag>年度总结</tag>
        <tag>系统设计</tag>
        <tag>微服务</tag>
        <tag>架构演进</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker基础部署，Docker Compose实践</title>
    <url>/2022/08/14/2022-08-14-Docker%E5%9F%BA%E7%A1%80%E9%83%A8%E7%BD%B2%EF%BC%8CDocker%20Compose%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>为什么会研究这个问题</p>
<p>在多服务项目越来越多之后，本地环境和测试环境的差异变得明显。以前部署一个 Java 应用只需要 JDK、配置文件和启动脚本，现在还要依赖 MySQL、Redis、RabbitMQ、Nginx 等组件。为了减少环境搭建成本，我开始研究 Docker 和 Docker Compose。</p>
<h1 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h1><p>遇到了什么情况</p>
<p>最开始遇到的问题是容器能启动，但服务之间连不上。比如应用容器访问 localhost:3306，结果连接的是容器内部而不是宿主机；数据卷没有挂载，容器删除后数据也没了；端口映射冲突，导致服务无法启动。这些问题说明，Docker 部署不能只记命令，要理解镜像、容器、网络、数据卷之间的关系。</p>
<h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><p>排查过程</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Docker 关键概念：</span><br><span class="line">image     静态模板</span><br><span class="line">container 运行实例</span><br><span class="line">volume    数据持久化</span><br><span class="line">network   容器通信</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker ps</span><br><span class="line">docker logs app</span><br><span class="line">docker <span class="built_in">exec</span> -it app sh</span><br><span class="line">docker network <span class="built_in">ls</span></span><br><span class="line">docker volume <span class="built_in">ls</span></span><br></pre></td></tr></table></figure>

<p>Docker Compose 的价值在于把多个容器的启动方式写成声明式配置，避免每次手写一堆 docker run 命令。</p>
<h1 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h1><p>具体操作</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3.8&quot;</span></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">mysql:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">mysql:8.0</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">MYSQL_ROOT_PASSWORD:</span> <span class="string">root</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;3306:3306&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">mysql-data:/var/lib/mysql</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">redis:6.2</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;6379:6379&quot;</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">app:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">demo-app:1.0</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">mysql</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">redis</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;8080:8080&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">mysql-data:</span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker compose up -d</span><br><span class="line">docker compose logs -f app</span><br><span class="line">docker compose down</span><br></pre></td></tr></table></figure>


<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>经验沉淀</p>
<p>Docker 带来的最大收益是环境一致性。Docker Compose 则进一步把多组件依赖固化下来。我的经验是：开发环境可以用 Compose 提效，但生产环境还需要考虑镜像版本、日志收集、健康检查、资源限制和安全配置。</p>
]]></content>
      <categories>
        <category>架构成长</category>
      </categories>
      <tags>
        <tag>部署</tag>
        <tag>Docker</tag>
        <tag>Docker Compose</tag>
        <tag>容器化</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux服务器CPU飙高问题定位</title>
    <url>/2023/06/18/Linux%E6%9C%8D%E5%8A%A1%E5%99%A8CPU%E9%A3%99%E9%AB%98%E9%97%AE%E9%A2%98%E5%AE%9A%E4%BD%8D/</url>
    <content><![CDATA[<blockquote>
<p>写在前面：看到 CPU 100% 以后，最直接的反应往往是重启服务。重启确实可能恢复业务，但也会把最有价值的线程现场一起清掉。只要业务还扛得住，最好先用几分钟把证据留下来。</p>
</blockquote>
<p>服务器 CPU 飙高的排查路径并不复杂：系统、进程、线程、调用栈，一层一层往下缩小范围。真正容易踩坑的是把 <code>load average</code> 当成 CPU 使用率，或者找到高 CPU 进程以后就直接下结论。</p>
<h2 id="1-先确认是不是-CPU-问题"><a href="#1-先确认是不是-CPU-问题" class="headerlink" title="1. 先确认是不是 CPU 问题"></a>1. 先确认是不是 CPU 问题</h2><p>第一步先看负载、CPU 分类和运行队列：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">uptime</span></span><br><span class="line">top -c</span><br><span class="line">vmstat 1 5</span><br><span class="line">mpstat -P ALL 1 5</span><br></pre></td></tr></table></figure>

<p>重点关注这些指标：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">us：用户态 CPU，Java 业务代码通常体现在这里</span><br><span class="line">sy：内核态 CPU，频繁系统调用、网络或驱动问题可能升高</span><br><span class="line">wa：等待磁盘 IO 的时间，不等于 CPU 真正在计算</span><br><span class="line">st：虚拟机被宿主机抢走的 CPU 时间</span><br><span class="line">r ：正在运行或等待 CPU 的任务数量</span><br><span class="line">b ：不可中断睡眠的任务数量，常见于 IO 等待</span><br></pre></td></tr></table></figure>

<p><code>load average</code> 表示一段时间内可运行和不可中断任务的平均数量，不是 CPU 百分比。8 核机器负载为 8 和 2 核机器负载为 8，严重程度完全不同。</p>
<p>例如下面这种情况，CPU 真正忙在用户态：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">%Cpu(s): 92.1 us, 5.3 sy, 0.0 ni, 1.8 id, 0.3 wa, 0.0 hi, 0.5 si, 0.0 st</span><br></pre></td></tr></table></figure>

<p>如果 <code>wa</code> 很高而 <code>us</code> 不高，方向应该切到磁盘 IO，不要继续只盯着 Java 线程。</p>
<h2 id="2-找到最消耗-CPU-的进程"><a href="#2-找到最消耗-CPU-的进程" class="headerlink" title="2. 找到最消耗 CPU 的进程"></a>2. 找到最消耗 CPU 的进程</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ps -eo pid,ppid,user,%cpu,%mem,<span class="built_in">stat</span>,lstart,cmd --<span class="built_in">sort</span>=-%cpu | <span class="built_in">head</span> -20</span><br></pre></td></tr></table></figure>

<p>需要持续观察时可以使用 <code>pidstat</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pidstat -u -p ALL 1 5</span><br></pre></td></tr></table></figure>

<p>假设最终定位到 Java 进程 PID 为 <code>21847</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PID=21847</span><br><span class="line">ps -p <span class="variable">$PID</span> -o pid,ppid,user,%cpu,%mem,etime,cmd</span><br></pre></td></tr></table></figure>

<p>这时先记录进程启动时间、CPU、内存和完整启动命令，确认是不是刚发布的新版本，或者 JVM 参数是否发生变化。</p>
<h2 id="3-从进程继续定位到线程"><a href="#3-从进程继续定位到线程" class="headerlink" title="3. 从进程继续定位到线程"></a>3. 从进程继续定位到线程</h2><p>Java 进程内部有很多线程，需要找到具体是哪一个线程在消耗 CPU：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">top -H -p <span class="variable">$PID</span></span><br></pre></td></tr></table></figure>

<p>也可以直接输出线程列表：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ps -Lp <span class="variable">$PID</span> -o pid,tid,psr,pcpu,<span class="built_in">stat</span>,<span class="built_in">comm</span> --<span class="built_in">sort</span>=-pcpu | <span class="built_in">head</span> -20</span><br></pre></td></tr></table></figure>

<p>假设高 CPU 线程的十进制 TID 是 <code>21936</code>。<code>jstack</code> 中的线程编号 <code>nid</code> 使用十六进制，因此先转换：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">TID=21936</span><br><span class="line"><span class="built_in">printf</span> <span class="string">&#x27;0x%x\n&#x27;</span> <span class="variable">$TID</span></span><br></pre></td></tr></table></figure>

<p>输出：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">0x55b0</span><br></pre></td></tr></table></figure>

<h2 id="4-使用-jstack-对应-Java-调用栈"><a href="#4-使用-jstack-对应-Java-调用栈" class="headerlink" title="4. 使用 jstack 对应 Java 调用栈"></a>4. 使用 jstack 对应 Java 调用栈</h2><p>连续抓三次线程快照，比只看一次更可靠：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jstack <span class="variable">$PID</span> &gt; /tmp/java-<span class="variable">$PID</span>-1.jstack</span><br><span class="line"><span class="built_in">sleep</span> 5</span><br><span class="line">jstack <span class="variable">$PID</span> &gt; /tmp/java-<span class="variable">$PID</span>-2.jstack</span><br><span class="line"><span class="built_in">sleep</span> 5</span><br><span class="line">jstack <span class="variable">$PID</span> &gt; /tmp/java-<span class="variable">$PID</span>-3.jstack</span><br></pre></td></tr></table></figure>

<p>再根据十六进制线程号查找：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep -n <span class="string">&#x27;nid=0x55b0&#x27;</span> -A 40 /tmp/java-<span class="variable">$PID</span>-1.jstack</span><br></pre></td></tr></table></figure>

<p>执行 <code>jstack</code> 的用户最好与 Java 进程启动用户一致，并确认使用兼容的 JDK 工具。容器环境还要注意 PID 命名空间，宿主机看到的 PID 和容器内部可能不同。</p>
<p>一个典型的高 CPU 调用栈可能是这样：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">&quot;retry-worker-3&quot; #86 prio=5 os_prio=0 tid=0x... nid=0x55b0 runnable</span><br><span class="line">   java.lang.Thread.State: RUNNABLE</span><br><span class="line">        at com.example.order.RetryService.retry(RetryService.java:127)</span><br><span class="line">        at com.example.order.RetryService.run(RetryService.java:91)</span><br></pre></td></tr></table></figure>

<p>如果连续三次都停在同一段业务代码，基本可以确定热点位置。示例中的问题是失败重试没有退避时间，在下游不可用时进入了快速循环：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 错误示例：失败后立即进入下一次循环，会持续占用 CPU</span></span><br><span class="line"><span class="keyword">while</span> (!success) &#123;</span><br><span class="line">    success = callDownstream();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>修复时要增加最大次数、退避时间和可观测日志：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">maxAttempts</span> <span class="operator">=</span> <span class="number">3</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> <span class="variable">attempt</span> <span class="operator">=</span> <span class="number">1</span>; attempt &lt;= maxAttempts; attempt++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (callDownstream()) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 退避时间需要结合业务超时预算设置</span></span><br><span class="line">    TimeUnit.MILLISECONDS.sleep(attempt * <span class="number">200L</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalStateException</span>(<span class="string">&quot;下游调用连续失败&quot;</span>);</span><br></pre></td></tr></table></figure>

<h2 id="5-判断是不是-GC-导致-CPU-升高"><a href="#5-判断是不是-GC-导致-CPU-升高" class="headerlink" title="5. 判断是不是 GC 导致 CPU 升高"></a>5. 判断是不是 GC 导致 CPU 升高</h2><p>如果高 CPU 线程主要是 GC 线程，继续看垃圾回收情况：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jstat -gcutil <span class="variable">$PID</span> 1000 10</span><br></pre></td></tr></table></figure>

<p>重点观察：</p>
<ol>
<li><code>YGC</code>、<code>FGC</code> 是否快速增长。</li>
<li><code>FGCT</code> 是否持续增加。</li>
<li>老年代使用率是否长期接近 100%。</li>
<li>每次 GC 后内存是否能明显下降。</li>
</ol>
<p>频繁 Full GC 只是现象，后面还要分析对象分配、堆大小、缓存和内存泄漏。不要看到 GC 高就先改大堆内存，堆变大以后单次 Full GC 可能更慢。</p>
<h2 id="6-非-Java-进程怎么排查"><a href="#6-非-Java-进程怎么排查" class="headerlink" title="6. 非 Java 进程怎么排查"></a>6. 非 Java 进程怎么排查</h2><p>如果热点不是 Java，可以继续使用下面的工具：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看进程的 CPU、上下文切换和调度情况</span></span><br><span class="line">pidstat -u -w -p <span class="variable">$PID</span> 1 5</span><br><span class="line"></span><br><span class="line"><span class="comment"># 有权限且服务器安装了 perf 时，查看热点函数</span></span><br><span class="line">perf top -p <span class="variable">$PID</span></span><br></pre></td></tr></table></figure>

<p><code>perf</code> 可能需要 root 权限和内核符号支持，生产环境使用前要确认安全规范。对于 Nginx、压缩程序、日志采集器等进程，热点函数通常能帮助判断是加密、压缩、正则还是网络处理造成的消耗。</p>
<h2 id="7-几种容易误判的情况"><a href="#7-几种容易误判的情况" class="headerlink" title="7. 几种容易误判的情况"></a>7. 几种容易误判的情况</h2><h3 id="7-1-单核打满"><a href="#7-1-单核打满" class="headerlink" title="7.1 单核打满"></a>7.1 单核打满</h3><p>在多核服务器上，一个死循环可能只打满一个核心。整机 CPU 看起来只有十几个百分点，但对应接口已经异常，因此要用 <code>mpstat -P ALL</code> 查看每个核心。</p>
<h3 id="7-2-iowait-很高"><a href="#7-2-iowait-很高" class="headerlink" title="7.2 iowait 很高"></a>7.2 iowait 很高</h3><p><code>top</code> 里负载很高，并不代表 CPU 计算繁忙。如果大量线程处于 <code>D</code> 状态且 <code>wa</code> 很高，应该检查磁盘、网络存储和文件系统。</p>
<h3 id="7-3-steal-很高"><a href="#7-3-steal-很高" class="headerlink" title="7.3 steal 很高"></a>7.3 steal 很高</h3><p>虚拟机的 <code>st</code> 持续升高，说明宿主机资源竞争严重。应用代码可能没有变化，这时需要结合云平台监控确认宿主机和实例规格。</p>
<h3 id="7-4-BLOCKED-线程很多"><a href="#7-4-BLOCKED-线程很多" class="headerlink" title="7.4 BLOCKED 线程很多"></a>7.4 BLOCKED 线程很多</h3><p>大量线程等待锁会造成接口慢，但等待中的线程通常不消耗大量 CPU。真正的 CPU 热点可能是持锁线程，或者另一个不断自旋的线程。</p>
<h2 id="8-应急处理顺序"><a href="#8-应急处理顺序" class="headerlink" title="8. 应急处理顺序"></a>8. 应急处理顺序</h2><ol>
<li>确认影响范围，必要时先限流、摘流量或扩容。</li>
<li>保存 <code>top</code>、<code>pidstat</code>、线程列表和至少三份 <code>jstack</code>。</li>
<li>判断是否与刚发布版本、流量突增或下游异常有关。</li>
<li>有明确回退版本时优先回滚，不要在线上临时改复杂代码。</li>
<li>只有业务已经无法承受时才直接重启或终止进程。</li>
<li>恢复后补齐监控、告警和压测用例。</li>
</ol>
<h2 id="9-最后的排查清单"><a href="#9-最后的排查清单" class="headerlink" title="9. 最后的排查清单"></a>9. 最后的排查清单</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">系统层：uptime、top、vmstat、mpstat</span><br><span class="line">进程层：ps、pidstat</span><br><span class="line">线程层：top -H、ps -L</span><br><span class="line">Java 栈：jstack，连续抓取三次</span><br><span class="line">GC 情况：jstat -gcutil</span><br><span class="line">非 Java 热点：perf top</span><br></pre></td></tr></table></figure>

<p>CPU 问题定位的关键不是记住多少命令，而是始终保留“系统到进程、进程到线程、线程到代码”的证据链。这样即使重启恢复了服务，后面也还有东西可以复盘。</p>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Linux</tag>
        <tag>故障排查</tag>
        <tag>CPU</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL联合索引与索引失效排查</title>
    <url>/2023/04/23/MySQL%E8%81%94%E5%90%88%E7%B4%A2%E5%BC%95%E4%B8%8E%E7%B4%A2%E5%BC%95%E5%A4%B1%E6%95%88%E6%8E%92%E6%9F%A5/</url>
    <content><![CDATA[<blockquote>
<p>写在前面：索引建了不等于一定会用，执行计划里出现了索引也不等于查询就足够快。很多所谓的“索引失效”，其实是索引顺序和查询方式根本对不上。</p>
</blockquote>
<p>这篇把工作中比较容易遇到的联合索引问题放在一起，重点不是背口诀，而是知道看到 <code>key=NULL</code> 或扫描行数异常时应该怎么往下查。</p>
<h2 id="1-先看一个联合索引"><a href="#1-先看一个联合索引" class="headerlink" title="1. 先看一个联合索引"></a>1. 先看一个联合索引</h2><p>订单表有下面这个索引：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_tenant_status_created</span><br><span class="line"><span class="keyword">ON</span> order_info (tenant_id, status, created_at);</span><br></pre></td></tr></table></figure>

<p>可以把联合索引简单理解成按照下面的顺序排列：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">tenant_id -&gt; status -&gt; created_at</span><br></pre></td></tr></table></figure>

<p>因此下面几类条件比较容易利用这个索引：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 使用 tenant_id</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 使用 tenant_id、status</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 等值过滤以后，再按 created_at 做范围查询</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> created_at <span class="operator">&gt;=</span> <span class="string">&#x27;2023-04-01&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>而只使用中间字段时，通常无法走出理想的索引范围：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>这就是常说的最左前缀原则。它描述的是索引有序性的利用方式，不是要求 SQL 条件必须按照索引字段顺序书写。下面两种写法对优化器来说基本没有区别：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span> <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line"><span class="keyword">WHERE</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span> <span class="keyword">AND</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br></pre></td></tr></table></figure>

<h2 id="2-常见的索引失效场景"><a href="#2-常见的索引失效场景" class="headerlink" title="2. 常见的索引失效场景"></a>2. 常见的索引失效场景</h2><h3 id="2-1-在索引字段上做函数计算"><a href="#2-1-在索引字段上做函数计算" class="headerlink" title="2.1 在索引字段上做函数计算"></a>2.1 在索引字段上做函数计算</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 不推荐：每一行都要计算 DATE(created_at)</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> <span class="type">DATE</span>(created_at) <span class="operator">=</span> <span class="string">&#x27;2023-04-23&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>可以改成明确的时间范围：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> created_at <span class="operator">&gt;=</span> <span class="string">&#x27;2023-04-23 00:00:00&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> created_at <span class="operator">&lt;</span>  <span class="string">&#x27;2023-04-24 00:00:00&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>MySQL 8.0 支持函数索引，但也不代表所有函数条件都应该依赖函数索引解决。时间范围写法更直观，也更容易复用普通索引。</p>
<h3 id="2-2-字段类型不一致导致隐式转换"><a href="#2-2-字段类型不一致导致隐式转换" class="headerlink" title="2.2 字段类型不一致导致隐式转换"></a>2.2 字段类型不一致导致隐式转换</h3><p>假设 <code>user_phone</code> 是 <code>VARCHAR</code>：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 数字常量可能触发隐式类型转换</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> user_info</span><br><span class="line"><span class="keyword">WHERE</span> user_phone <span class="operator">=</span> <span class="number">13800138000</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 参数类型与字段保持一致</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> user_info</span><br><span class="line"><span class="keyword">WHERE</span> user_phone <span class="operator">=</span> <span class="string">&#x27;13800138000&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>Java 代码里也要检查 MyBatis 参数类型。数据库字段是字符串，代码却传了 <code>Long</code>，SQL 文本看起来差别不大，执行计划可能完全不同。</p>
<h3 id="2-3-前置通配符"><a href="#2-3-前置通配符" class="headerlink" title="2.3 前置通配符"></a>2.3 前置通配符</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 普通 B+Tree 索引无法定位字符串起点</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> article</span><br><span class="line"><span class="keyword">WHERE</span> title <span class="keyword">LIKE</span> <span class="string">&#x27;%MySQL%&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 固定前缀可以利用索引范围</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> article</span><br><span class="line"><span class="keyword">WHERE</span> title <span class="keyword">LIKE</span> <span class="string">&#x27;MySQL%&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>真正的任意关键词检索更适合全文索引或专门的搜索引擎，不建议为了模糊搜索不断堆普通索引。</p>
<h3 id="2-4-范围条件后面的字段"><a href="#2-4-范围条件后面的字段" class="headerlink" title="2.4 范围条件后面的字段"></a>2.4 范围条件后面的字段</h3><p>如果索引改成下面的顺序：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_tenant_created_status</span><br><span class="line"><span class="keyword">ON</span> order_info (tenant_id, created_at, status);</span><br></pre></td></tr></table></figure>

<p>查询条件为：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line">  <span class="keyword">AND</span> created_at <span class="operator">&gt;=</span> <span class="string">&#x27;2023-04-01&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p><code>created_at</code> 进入范围扫描以后，后面的 <code>status</code> 通常不能继续缩小索引扫描区间。部分版本可能通过索引条件下推减少回表，但扫描范围仍然可能很大。因此对这个查询而言，<code>(tenant_id, status, created_at)</code> 往往更合适。</p>
<h3 id="2-5-OR-两边没有完整索引"><a href="#2-5-OR-两边没有完整索引" class="headerlink" title="2.5 OR 两边没有完整索引"></a>2.5 OR 两边没有完整索引</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> order_no <span class="operator">=</span> <span class="string">&#x27;A20230423001&#x27;</span></span><br><span class="line">   <span class="keyword">OR</span> third_party_no <span class="operator">=</span> <span class="string">&#x27;WX20230423001&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>如果只有 <code>order_no</code> 有索引，优化器可能选择全表扫描。可以根据业务语义评估给两个字段分别建索引，或者拆成两个查询后合并结果。不要为了让执行计划好看直接改成 <code>UNION</code>，需要确认去重规则和实际耗时。</p>
<h2 id="3-为什么索引符合规则，优化器还是不用"><a href="#3-为什么索引符合规则，优化器还是不用" class="headerlink" title="3. 为什么索引符合规则，优化器还是不用"></a>3. 为什么索引符合规则，优化器还是不用</h2><p>索引不是越多越好，优化器会估算“走索引再回表”和“直接扫描表”的成本。下面几种情况，即使语法上可以使用索引，也可能选择全表扫描：</p>
<ol>
<li>表非常小，全表扫描成本更低。</li>
<li>条件返回了表中大部分数据，回表次数过多。</li>
<li>字段区分度很低，例如只有两个值的状态字段。</li>
<li>统计信息过旧，优化器对数据分布估算错误。</li>
<li>查询返回字段过多，无法形成覆盖索引。</li>
</ol>
<p>先用执行计划确认，不要凭感觉判断：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">EXPLAIN FORMAT<span class="operator">=</span>JSON</span><br><span class="line"><span class="keyword">SELECT</span> id, order_no, amount, created_at</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">50</span>;</span><br></pre></td></tr></table></figure>

<p>普通 <code>EXPLAIN</code> 适合快速看结论，JSON 格式会给出更详细的成本和过滤信息。MySQL 8.0 还可以在确认查询安全后使用 <code>EXPLAIN ANALYZE</code> 查看实际行数。</p>
<h2 id="4-联合索引怎么设计"><a href="#4-联合索引怎么设计" class="headerlink" title="4. 联合索引怎么设计"></a>4. 联合索引怎么设计</h2><p>我一般按照下面的顺序判断：</p>
<ol>
<li>先收集高频且重要的真实 SQL，不根据单个字段想象索引。</li>
<li>稳定的等值条件通常放在前面，例如租户、用户、业务状态。</li>
<li>范围和排序字段通常放在等值字段之后。</li>
<li>比较查询返回字段，判断是否值得做覆盖索引。</li>
<li>合并可以互相复用的索引，避免重复索引增加写放大。</li>
</ol>
<p>例如这个列表查询：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, order_no, amount, created_at</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">50</span>;</span><br></pre></td></tr></table></figure>

<p>可以先评估：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_tenant_status_created</span><br><span class="line"><span class="keyword">ON</span> order_info (tenant_id, status, created_at);</span><br></pre></td></tr></table></figure>

<p>网上经常说“区分度高的字段放最前面”，这个说法不够完整。联合索引首先要匹配查询路径和排序需求，区分度只是成本判断的一部分。把高区分度字段放到前面，却破坏了多个高频查询可以复用的前缀，整体效果可能更差。</p>
<h2 id="5-检查重复和长期不用的索引"><a href="#5-检查重复和长期不用的索引" class="headerlink" title="5. 检查重复和长期不用的索引"></a>5. 检查重复和长期不用的索引</h2><p>先查看表上的索引：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> INDEX <span class="keyword">FROM</span> order_info;</span><br></pre></td></tr></table></figure>

<p>如果已经存在 <code>(tenant_id, status, created_at)</code>，通常不再需要单独的 <code>(tenant_id)</code> 索引，因为前者可以覆盖它的访问前缀。但是否删除仍然要结合索引大小、查询统计和执行计划验证。</p>
<p>MySQL 的 <code>sys</code> 库可以提供未使用索引线索：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> object_schema, object_name, index_name</span><br><span class="line"><span class="keyword">FROM</span> sys.schema_unused_indexes</span><br><span class="line"><span class="keyword">WHERE</span> object_schema <span class="operator">=</span> <span class="string">&#x27;order_db&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>这里的数据通常来自实例启动后的统计，重启会影响观察窗口。<strong>不能看到“未使用”就立即删除索引。</strong> 应该覆盖完整业务周期，并提前准备恢复索引的 DDL。</p>
<h2 id="6-不建议直接使用-FORCE-INDEX"><a href="#6-不建议直接使用-FORCE-INDEX" class="headerlink" title="6. 不建议直接使用 FORCE INDEX"></a>6. 不建议直接使用 FORCE INDEX</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> order_info FORCE INDEX (idx_tenant_status_created)</span><br><span class="line"><span class="keyword">WHERE</span> tenant_id <span class="operator">=</span> <span class="number">1001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p><code>FORCE INDEX</code> 可以用于验证猜想，但不应该成为默认修复方式。数据分布会变化，今天合适的索引路径以后未必合适。强制索引会限制优化器选择，最好先解决统计信息、SQL 写法或索引设计问题。</p>
<h2 id="7-排查清单"><a href="#7-排查清单" class="headerlink" title="7. 排查清单"></a>7. 排查清单</h2><ol>
<li>确认字段类型与传入参数类型是否一致。</li>
<li>检查索引字段上是否存在函数、计算或隐式转换。</li>
<li>检查联合索引是否满足真实查询的最左前缀。</li>
<li>注意范围条件、排序和后续字段之间的关系。</li>
<li>比较扫描行数、返回行数和回表成本。</li>
<li>检查重复索引和长期不用的索引，但删除前必须持续观察。</li>
<li>修改后重新查看执行计划，并通过真实流量指标验证。</li>
</ol>
<p>索引问题最后还是要回到一句话：<strong>用数据证明优化有效。</strong> 能走索引只是过程，稳定降低响应时间和数据库负载才是结果。</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>MySQL</tag>
        <tag>SQL优化</tag>
        <tag>联合索引</tag>
      </tags>
  </entry>
  <entry>
    <title>一次MySQL慢SQL调优记录</title>
    <url>/2023/02/19/%E4%B8%80%E6%AC%A1MySQL%E6%85%A2SQL%E8%B0%83%E4%BC%98%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<blockquote>
<p>写在前面：SQL 调优最怕一上来就加索引。索引可能暂时把耗时压下去，也可能只是把问题从查询端转移到了写入端。比较稳妥的方式还是先保留现场，再根据慢查询日志和执行计划判断到底慢在哪里。</p>
</blockquote>
<p>这次整理一个比较常见的订单列表查询。数据量上来以后，接口从两三百毫秒涨到了四五秒，数据库 CPU 没有打满，但是扫描行数非常夸张。</p>
<h2 id="1-现场现象"><a href="#1-现场现象" class="headerlink" title="1. 现场现象"></a>1. 现场现象</h2><p>接口查询的是某个用户已支付的订单，按创建时间倒序取最近 20 条：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, order_no, user_id, status, amount, created_at</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">10001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure>

<p>慢查询日志里最值得关注的不是 <code>Query_time</code>，而是 <code>Rows_examined</code> 和最终返回行数的差距：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line"># Query_time: 4.782  Lock_time: 0.000</span><br><span class="line"># Rows_sent: 20  Rows_examined: 2864317</span><br></pre></td></tr></table></figure>

<p>只返回 20 行，却扫描了 280 多万行，方向基本已经明确：先检查执行计划和现有索引。</p>
<h2 id="2-先确认慢-SQL-从哪里来"><a href="#2-先确认慢-SQL-从哪里来" class="headerlink" title="2. 先确认慢 SQL 从哪里来"></a>2. 先确认慢 SQL 从哪里来</h2><p>生产环境不要为了排查随手修改全局参数，先查看当前配置：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> VARIABLES <span class="keyword">LIKE</span> <span class="string">&#x27;slow_query_log&#x27;</span>;</span><br><span class="line"><span class="keyword">SHOW</span> VARIABLES <span class="keyword">LIKE</span> <span class="string">&#x27;long_query_time&#x27;</span>;</span><br><span class="line"><span class="keyword">SHOW</span> VARIABLES <span class="keyword">LIKE</span> <span class="string">&#x27;slow_query_log_file&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>如果已经启用了 <code>performance_schema</code>，也可以从聚合结果里找总耗时最高的 SQL：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> DIGEST_TEXT,</span><br><span class="line">       COUNT_STAR,</span><br><span class="line">       ROUND(SUM_TIMER_WAIT <span class="operator">/</span> <span class="number">1000000000000</span>, <span class="number">2</span>) <span class="keyword">AS</span> total_seconds,</span><br><span class="line">       ROUND(AVG_TIMER_WAIT <span class="operator">/</span> <span class="number">1000000000</span>, <span class="number">2</span>) <span class="keyword">AS</span> avg_ms</span><br><span class="line"><span class="keyword">FROM</span> performance_schema.events_statements_summary_by_digest</span><br><span class="line"><span class="keyword">WHERE</span> DIGEST_TEXT <span class="keyword">IS</span> <span class="keyword">NOT NULL</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> SUM_TIMER_WAIT <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">10</span>;</span><br></pre></td></tr></table></figure>

<p>这里要同时看三个维度：单次很慢、调用次数很多、累计耗时很高。只盯着最慢的一条 SQL，可能会漏掉高频的小慢查询。</p>
<h2 id="3-看懂执行计划"><a href="#3-看懂执行计划" class="headerlink" title="3. 看懂执行计划"></a>3. 看懂执行计划</h2><p>先执行普通 <code>EXPLAIN</code>，这个命令不会真正跑完整查询，在线上相对安全：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">EXPLAIN</span><br><span class="line"><span class="keyword">SELECT</span> id, order_no, user_id, status, amount, created_at</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">10001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure>

<p>优化前的核心结果如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">type: ALL</span><br><span class="line">possible_keys: idx_user_id, idx_status</span><br><span class="line">key: NULL</span><br><span class="line">rows: 2864317</span><br><span class="line">filtered: 1.00</span><br><span class="line">Extra: Using where; Using filesort</span><br></pre></td></tr></table></figure>

<p>几个字段可以这样理解：</p>
<ol>
<li><code>type=ALL</code>：全表扫描，这次问题的重点。</li>
<li><code>key=NULL</code>：优化器最终没有选择索引。</li>
<li><code>rows</code>：预计需要检查的行数，不是精确值，但可以判断量级。</li>
<li><code>filtered</code>：经过条件过滤后预计保留的比例。</li>
<li><code>Using filesort</code>：排序无法直接利用索引完成，并不一定真的写磁盘，但会产生额外排序成本。</li>
</ol>
<p>表里原来只有 <code>user_id</code> 和 <code>status</code> 两个单列索引。MySQL 通常不会把多个单列索引自动组合成最理想的访问路径，即使发生了索引合并，也解决不了后面的时间排序问题。</p>
<h2 id="4-根据查询模式设计索引"><a href="#4-根据查询模式设计索引" class="headerlink" title="4. 根据查询模式设计索引"></a>4. 根据查询模式设计索引</h2><p>这个查询有两个等值条件，后面跟一个排序字段，因此新增联合索引：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER TABLE</span> order_info</span><br><span class="line"><span class="keyword">ADD</span> INDEX idx_user_status_created (user_id, status, created_at);</span><br></pre></td></tr></table></figure>

<p>再次查看执行计划：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">type: ref</span><br><span class="line">key: idx_user_status_created</span><br><span class="line">rows: 37</span><br><span class="line">filtered: 100.00</span><br><span class="line">Extra: Using index condition</span><br></pre></td></tr></table></figure>

<p>查询耗时从秒级降到了几十毫秒，扫描行数也从百万级下降到几十行。索引顺序不是固定公式，而是由真实查询决定：这里 <code>user_id</code> 和 <code>status</code> 都是等值条件，<code>created_at</code> 用于排序，因此这个顺序比较合适。</p>
<p><strong>不要为了覆盖查询把所有返回字段都塞进联合索引。</strong> <code>order_no</code>、<code>amount</code> 等字段加入索引后虽然可能减少回表，但会明显增加索引体积和写入成本。是否做覆盖索引，需要结合查询频率、字段长度和写入压力再决定。</p>
<h2 id="5-MySQL-8-0-可以进一步验证"><a href="#5-MySQL-8-0-可以进一步验证" class="headerlink" title="5. MySQL 8.0 可以进一步验证"></a>5. MySQL 8.0 可以进一步验证</h2><p>MySQL 8.0.18 以后可以使用 <code>EXPLAIN ANALYZE</code> 查看实际执行时间和真实行数：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">EXPLAIN ANALYZE</span><br><span class="line"><span class="keyword">SELECT</span> id, order_no, user_id, status, amount, created_at</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">10001</span></span><br><span class="line">  <span class="keyword">AND</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure>

<p>这个命令会真正执行 SQL。对于更新、删除语句或者可能返回大量数据的查询，不能当作普通 <code>EXPLAIN</code> 随便在线上执行。MySQL 5.7 没有这个能力，只能结合慢查询日志、执行计划和监控数据验证。</p>
<h2 id="6-还有几个容易忽略的问题"><a href="#6-还有几个容易忽略的问题" class="headerlink" title="6. 还有几个容易忽略的问题"></a>6. 还有几个容易忽略的问题</h2><h3 id="6-1-深分页"><a href="#6-1-深分页" class="headerlink" title="6.1 深分页"></a>6.1 深分页</h3><p>下面这种分页到了后面仍然会扫描并丢弃大量数据：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, order_no, created_at</span><br><span class="line"><span class="keyword">FROM</span> order_info</span><br><span class="line"><span class="keyword">WHERE</span> user_id <span class="operator">=</span> <span class="number">10001</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">200000</span>, <span class="number">20</span>;</span><br></pre></td></tr></table></figure>

<p>业务允许时，优先使用上一页最后一条记录作为游标，避免不断增大的 <code>OFFSET</code>。如果时间可能重复，需要同时携带 <code>created_at</code> 和唯一主键，保证翻页稳定。</p>
<h3 id="6-2-统计信息过旧"><a href="#6-2-统计信息过旧" class="headerlink" title="6.2 统计信息过旧"></a>6.2 统计信息过旧</h3><p>表数据变化很大时，优化器估算可能失真。可以先比较执行计划和真实数据分布，再在低峰期评估是否执行：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">ANALYZE <span class="keyword">TABLE</span> order_info;</span><br></pre></td></tr></table></figure>

<p>这不是固定的“优化命令”，执行前要确认 MySQL 版本、表大小和锁影响。</p>
<h3 id="6-3-只看一次压测结果"><a href="#6-3-只看一次压测结果" class="headerlink" title="6.3 只看一次压测结果"></a>6.3 只看一次压测结果</h3><p>首次查询可能涉及磁盘读取，后续查询可能命中 Buffer Pool。调优前后要使用相同数据范围，多执行几轮，并观察数据库 CPU、磁盘 IO、扫描行数和接口分位耗时，不能只拿一次最快结果做结论。</p>
<h2 id="7-复盘清单"><a href="#7-复盘清单" class="headerlink" title="7. 复盘清单"></a>7. 复盘清单</h2><ol>
<li>从慢查询日志或聚合指标确认真实高耗时 SQL。</li>
<li>对比扫描行数与返回行数，先判断是否存在无效扫描。</li>
<li>使用 <code>EXPLAIN</code> 检查 <code>type</code>、<code>key</code>、<code>rows</code> 和 <code>Extra</code>。</li>
<li>按实际查询条件设计联合索引，不机械套用字段选择性公式。</li>
<li>同时评估索引对新增、更新和磁盘空间的影响。</li>
<li>调优后用同一组数据和完整监控验证，保留回退方案。</li>
</ol>
<p>SQL 调优本质上不是让某一条 SQL 看起来更快，而是在查询性能、写入成本和维护复杂度之间找到一个能长期运行的平衡点。</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>MySQL</tag>
        <tag>慢查询</tag>
        <tag>SQL优化</tag>
      </tags>
  </entry>
  <entry>
    <title>从零开始学习设计模式-第一篇</title>
    <url>/2024/04/24/%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E5%AD%A6%E4%B9%A0%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%AC%AC%E4%B8%80%E7%AF%87/</url>
    <content><![CDATA[<h1 id="设计模式"><a href="#设计模式" class="headerlink" title="设计模式"></a>设计模式</h1><blockquote>
<p>写在前面：</p>
</blockquote>
<p>找工作下来感觉自己基础知识确实很不牢靠，成了无情的接口调用工具人，所以恶补一下相关的知识。</p>


<h2 id="1-单例模式"><a href="#1-单例模式" class="headerlink" title="1. 单例模式"></a>1. 单例模式</h2><p>概念就是<strong>一个类负责创建自己的对象，同时确保只有单个对象被创建</strong>。</p>
<h3 id="1-1分类"><a href="#1-1分类" class="headerlink" title="1.1分类"></a>1.1分类</h3><h4 id="恶汉：加载就会导致该实例对象被创建"><a href="#恶汉：加载就会导致该实例对象被创建" class="headerlink" title="恶汉：加载就会导致该实例对象被创建"></a>恶汉：加载就会导致该实例对象被创建</h4><ol>
<li>创建方式<ol>
<li>静态变量方式 直接new Singleton（）；</li>
<li>静态代码块的方式 写一个 static{}</li>
</ol>
</li>
</ol>
<h4 id="懒汉：首次使用该对象时才会创建对象"><a href="#懒汉：首次使用该对象时才会创建对象" class="headerlink" title="懒汉：首次使用该对象时才会创建对象"></a>懒汉：首次使用该对象时才会创建对象</h4><blockquote>
<p>懒汉式会出现线程安全问题，需要优化判断条件</p>
</blockquote>
<p>可以通过对外提供静态方法的方式来获取，但是效率特别低。一般使用双重检查锁的方式进行处理，但是JVM可能会出现优化和指令重排的问题，所以可以使用volatile关键字保证代码的有序性。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Singleton</span>{</span><br><span class="line">  </span><br><span class="line">  	<span class="keyword">private</span> <span class="title function_">Singleton</span><span class="params">()</span>{}</span><br><span class="line">  </span><br><span class="line">  	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> Singleton instance;</span><br><span class="line">  </span><br><span class="line">  <span class="comment">//对外提供静态方法获取该对象</span></span><br><span class="line">  	<span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title function_">getInstance</span><span class="params">()</span>{</span><br><span class="line">      	<span class="keyword">if</span>(instance == <span class="literal">null</span>){</span><br><span class="line">          <span class="keyword">synchronized</span>(Singleton.class){</span><br><span class="line">            <span class="keyword">if</span>(instance == <span class="literal">null</span>){</span><br><span class="line">              instance = <span class="keyword">new</span> <span class="title class_">Singleton</span>();</span><br><span class="line">            }</span><br><span class="line">          }</span><br><span class="line">        }</span><br><span class="line">      <span class="keyword">return</span> instance;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>

<h3 id="1-2-破解单例"><a href="#1-2-破解单例" class="headerlink" title="1.2 破解单例"></a>1.2 破解单例</h3><blockquote>
<p>可以通过序列化和反射的方式破解单例模式</p>
</blockquote>
<p><strong>解决办法</strong></p>
<ol>
<li>序列化：添加readResolve()方法，返回SingletonHolder.INSTANCE;</li>
<li>反射：在构造方法中，增加判断，如果私有化对象不为空，直接抛出异常</li>
</ol>
]]></content>
      <categories>
        <category>Java基础</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>博客重新出发</title>
    <url>/2026/06/09/%E5%8D%9A%E5%AE%A2%E9%87%8D%E6%96%B0%E5%87%BA%E5%8F%91/</url>
    <content><![CDATA[<p>博客源码重新搭建完成，后续会继续记录技术学习、项目实践和问题排查过程。</p>
<span id="more"></span>

<h2 id="为什么重新搭建"><a href="#为什么重新搭建" class="headerlink" title="为什么重新搭建"></a>为什么重新搭建</h2><p>之前只保留了静态站点产物，源码目录已经丢失。为了后续能继续维护文章、主题和部署流程，这次重新生成了一个完整的 Hexo 源码项目。</p>
<h2 id="当前技术栈"><a href="#当前技术栈" class="headerlink" title="当前技术栈"></a>当前技术栈</h2><ul>
<li>Hexo：静态博客生成器</li>
<li>NexT：Hexo 主题</li>
<li>GitHub Pages：静态站点托管</li>
<li>GitHub Actions：后续可用于自动构建和部署</li>
</ul>
<h2 id="后续计划"><a href="#后续计划" class="headerlink" title="后续计划"></a>后续计划</h2><ol>
<li>整理 Java 后端开发笔记</li>
<li>梳理并发编程和 JVM 相关知识</li>
<li>记录 Linux、Nginx、部署排查经验</li>
<li>逐步完善博客的搜索、订阅和 SEO 配置</li>
</ol>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>GitHub Pages</tag>
      </tags>
  </entry>
  <entry>
    <title>无外网安装nginx</title>
    <url>/2024/05/22/%E6%97%A0%E5%A4%96%E7%BD%91%E5%AE%89%E8%A3%85nginx/</url>
    <content><![CDATA[<blockquote>
<p>写在前面：客户给的服务器是银河麒麟，然后不给外网权限，要求安装nginx，网上教程基本没有这种情况的。也是踩了很多的坑，所以记录一下方便后面的bro。PS：重点在于如何安装上nginx，比较基础的操作可以同时参考其他教程一起看！！</p>
</blockquote>


<h2 id="1-服务器情况："><a href="#1-服务器情况：" class="headerlink" title="1. 服务器情况："></a>1. 服务器情况：</h2><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> /proc/version</span><br><span class="line">Linux version 4.19.90-23.8.v2101.ky10.x86_64</span><br></pre></td></tr></tbody></table></figure>

<h2 id="2-准备工作"><a href="#2-准备工作" class="headerlink" title="2.准备工作"></a>2.准备工作</h2><h3 id="2-1-首先判断是否安装了nginx的三个依赖"><a href="#2-1-首先判断是否安装了nginx的三个依赖" class="headerlink" title="2.1.首先判断是否安装了nginx的三个依赖"></a>2.1.首先判断是否安装了nginx的三个依赖</h3><ol>
<li>gcc</li>
<li>pcre</li>
<li>zlib</li>
</ol>
<p>可以访问外网的情况下可以直接命令行一键安装</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">yum -y install gcc zlib zlib-devel pcre-devel openssl </span><br></pre></td></tr></tbody></table></figure>

<p>本篇就不涉及有外网了，重点在无外网。</p>
<p>没外网的情况下，需要先手动下载上面三个依赖包，方便大家我就把整合好的地址放下面，大家有需要就自取</p>
<p>链接：<a target="_blank" rel="noopener" href="https://pan.baidu.com/s/1r0c0o6ORbOdB9vM8RoWUrA?pwd=bpmy">https://pan.baidu.com/s/1r0c0o6ORbOdB9vM8RoWUrA?pwd=bpmy</a><br>提取码：bpmy </p>
<p>下载好了以后，分别把三个依赖包，和nginx，指定到自己指定的位置<br>我是把依赖包解压到了/usr/local/src下</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">这是解压命令</span><br><span class="line">tar -xvf openssl-3.0.13.tar.gz</span><br><span class="line"></span><br><span class="line">三个包手动替换的名称就好</span><br></pre></td></tr></tbody></table></figure>

<blockquote>
<p>重点！！！</p>
</blockquote>
<p>回到nginx的安装目录，执行下面的命令</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">./configure --prefix=/usr/local/nginx   --with-pcre=/usr/local/src/pcre-7.3 --with-zlib=/usr/local/src/zlib  --with-openssl=/usr/local/src/openssl</span><br><span class="line"></span><br><span class="line">解释</span><br><span class="line">--prefix 是指定nginx的安装路径，后续的我们操作的nginx都会在这里，类似于windows的选择安装路径</span><br><span class="line">--with-xxx 指定依赖的解压路径，用来找到我们刚刚解压缩的那三个依赖。</span><br></pre></td></tr></tbody></table></figure>

<p>没报错的情况下，继续执行</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line">make</span><br><span class="line">make install</span><br><span class="line"></span><br><span class="line">就可以正常安装了</span><br></pre></td></tr></tbody></table></figure>

<p>到这里nginx应该就安装成功了，后续就是一些优化操作</p>
<ol>
<li>可以删除nginx压缩包和解压目录<br>2.设置软链，后续就不用在安装nginx的目录下操作nginx</li>
</ol>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"><span class="built_in">ln</span> -s /usr/local/webserver/nginx/sbin/nginx /usr/bin/</span><br><span class="line"></span><br><span class="line">在其他目录使用</span><br><span class="line">nginx -v</span><br><span class="line">查看是否设置成功</span><br></pre></td></tr></tbody></table></figure>

<p>3.nginx常用命令</p>
<figure class="highlight bash"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment"># 检查配置</span></span><br><span class="line">$ nginx -t（检查nginx.conf配置是否正确）</span><br><span class="line">$ nginx -s reload（重新载入配置文件，通常配合-t使用，在修改了nginx.conf且检查无误之后）</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动</span></span><br><span class="line">$ nginx</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看进程</span></span><br><span class="line">$ ps -ef | grep nginx</span><br></pre></td></tr></tbody></table></figure>

<blockquote>
<p>之前部署项目的时候没遇到过国产系统+无内网两种情况叠加，导致踩坑了两天才调通，希望这篇文章能对后面遇到这种情况的兄弟们有所帮助！</p>
</blockquote>
]]></content>
      <categories>
        <category>Linux运维</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>运维</tag>
      </tags>
  </entry>
  <entry>
    <title>服务器内存不足与OOM排查</title>
    <url>/2023/09/10/%E6%9C%8D%E5%8A%A1%E5%99%A8%E5%86%85%E5%AD%98%E4%B8%8D%E8%B6%B3%E4%B8%8EOOM%E6%8E%92%E6%9F%A5/</url>
    <content><![CDATA[<blockquote>
<p>写在前面：服务器内存“看起来快满了”和真正发生 OOM 不是一回事。Linux 会尽量利用空闲内存做缓存，<code>free</code> 很低并不一定有问题；反过来，容器被 OOM Kill 时，宿主机可能还有很多空闲内存。</p>
</blockquote>
<p>内存问题最重要的是先分清边界：整机内存不足、容器达到限制、JVM 堆溢出、堆外内存增长，处理方式完全不同。</p>
<h2 id="1-先看整机内存状态"><a href="#1-先看整机内存状态" class="headerlink" title="1. 先看整机内存状态"></a>1. 先看整机内存状态</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">free -h</span><br><span class="line">vmstat 1 5</span><br><span class="line">ps -eo pid,user,%mem,rss,vsz,etime,cmd --<span class="built_in">sort</span>=-rss | <span class="built_in">head</span> -20</span><br></pre></td></tr></table></figure>

<p><code>free -h</code> 可能得到类似结果：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">              total        used        free      shared  buff/cache   available</span><br><span class="line">Mem:            16G         11G        420M        210M         4.6G        4.2G</span><br><span class="line">Swap:            2G        128M        1.9G</span><br></pre></td></tr></table></figure>

<p>这里优先看 <code>available</code>，它估算了不发生交换时还可以提供给新进程的内存。<code>free</code> 很少但 <code>available</code> 仍然充足，通常只是 Linux 使用了页缓存，不需要立即清理。</p>
<p><code>vmstat</code> 里重点关注：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">si/so：是否持续发生 swap 换入和换出</span><br><span class="line">r    ：等待 CPU 的任务</span><br><span class="line">b    ：不可中断等待任务</span><br><span class="line">free ：空闲内存趋势</span><br></pre></td></tr></table></figure>

<p>持续的 <code>si/so</code> 往往会明显拖慢接口，即使进程还没有被杀掉，也需要尽快处理。</p>
<h2 id="2-确认有没有触发-Linux-OOM-Killer"><a href="#2-确认有没有触发-Linux-OOM-Killer" class="headerlink" title="2. 确认有没有触发 Linux OOM Killer"></a>2. 确认有没有触发 Linux OOM Killer</h2><p>Linux 在内存无法满足申请时，可能选择一个进程杀掉。先检查内核日志：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dmesg -T | grep -Ei <span class="string">&#x27;out of memory|oom-killer|killed process&#x27;</span></span><br><span class="line">journalctl -k --since <span class="string">&#x27;2 hours ago&#x27;</span> | grep -Ei <span class="string">&#x27;out of memory|oom|killed process&#x27;</span></span><br></pre></td></tr></table></figure>

<p>典型日志：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Out of memory: Killed process 18472 (java) total-vm:12582912kB,</span><br><span class="line">anon-rss:7340032kB, file-rss:1024kB, shmem-rss:0kB</span><br></pre></td></tr></table></figure>

<p>看到这条日志以后，至少可以确认进程不是正常退出，也不是 Java 主动抛出一个普通异常，而是被操作系统直接终止。接下来要对比进程 RSS、容器限制和当时的系统内存趋势。</p>
<h2 id="3-JVM-堆内存怎么检查"><a href="#3-JVM-堆内存怎么检查" class="headerlink" title="3. JVM 堆内存怎么检查"></a>3. JVM 堆内存怎么检查</h2><p>假设 Java 进程 PID 为 <code>18472</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PID=18472</span><br><span class="line">jcmd <span class="variable">$PID</span> VM.flags</span><br><span class="line">jcmd <span class="variable">$PID</span> GC.heap_info</span><br><span class="line">jstat -gcutil <span class="variable">$PID</span> 1000 10</span><br></pre></td></tr></table></figure>

<p>JDK 8 下主要看：</p>
<ol>
<li>JVM 最大堆 <code>-Xmx</code> 是否超过机器或容器可用内存。</li>
<li>老年代使用率是否持续增长。</li>
<li>Full GC 后占用是否明显下降。</li>
<li>Full GC 次数和耗时是否不断增加。</li>
</ol>
<p>查看类实例数量可以使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jcmd <span class="variable">$PID</span> GC.class_histogram &gt; /tmp/class-histogram.txt</span><br></pre></td></tr></table></figure>

<p>在线上大堆进程执行直方图或 <code>jmap -histo:live</code> 可能触发停顿，不能看到命令就直接运行。业务高峰期应先评估影响，必要时在摘流量后的实例或复现环境执行。</p>
<h2 id="4-保留-Heap-Dump"><a href="#4-保留-Heap-Dump" class="headerlink" title="4. 保留 Heap Dump"></a>4. 保留 Heap Dump</h2><p>最理想的方式是在 JVM 启动时提前配置 OOM 自动导出：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">-XX:+HeapDumpOnOutOfMemoryError</span><br><span class="line">-XX:HeapDumpPath=/data/heapdump</span><br><span class="line">-XX:+ExitOnOutOfMemoryError</span><br></pre></td></tr></table></figure>

<p><code>-XX:+ExitOnOutOfMemoryError</code> 需要较新的 JDK 8 update 版本，使用前应通过 <code>java -version</code> 和启动测试确认。<code>HeapDumpPath</code> 所在磁盘必须预留足够空间。堆最大为 8 GB 时，Dump 文件可能同样非常大；如果磁盘本来就快满了，导出失败还会带来额外 IO 压力。</p>
<p>进程仍然存活且确认可以承受停顿时，也可以手动导出：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jcmd <span class="variable">$PID</span> GC.heap_dump /data/heapdump/java-<span class="variable">$PID</span>.hprof</span><br></pre></td></tr></table></figure>

<p>JDK 8 环境也常见下面的命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jmap -dump:format=b,file=/data/heapdump/java-<span class="variable">$PID</span>.hprof <span class="variable">$PID</span></span><br></pre></td></tr></table></figure>

<p>Dump 文件可能包含业务数据和用户信息，上传或转移前要遵守数据安全规范。分析时可以使用 MAT、VisualVM 等工具重点查看大对象、引用链和支配树。</p>
<h2 id="5-堆没有满，内存为什么还在涨"><a href="#5-堆没有满，内存为什么还在涨" class="headerlink" title="5. 堆没有满，内存为什么还在涨"></a>5. 堆没有满，内存为什么还在涨</h2><p>Java 进程的 RSS 不只包含 JVM 堆，还包括：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">JVM Heap</span><br><span class="line">Metaspace</span><br><span class="line">Direct Buffer</span><br><span class="line">线程栈</span><br><span class="line">JIT Code Cache</span><br><span class="line">JNI 和本地库</span><br><span class="line">内存映射文件</span><br></pre></td></tr></table></figure>

<p>先看进程级数据：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> /proc/<span class="variable">$PID</span>/status | grep -E <span class="string">&#x27;VmPeak|VmSize|VmRSS|VmSwap|Threads&#x27;</span></span><br><span class="line">pmap -x <span class="variable">$PID</span> | <span class="built_in">tail</span> -1</span><br></pre></td></tr></table></figure>

<p>如果启动时增加了下面的参数：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">-XX:NativeMemoryTracking=summary</span><br></pre></td></tr></table></figure>

<p>可以使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jcmd <span class="variable">$PID</span> VM.native_memory summary</span><br></pre></td></tr></table></figure>

<p>Native Memory Tracking 必须在 JVM 启动时开启，也会有一定性能开销。没有提前开启时，不能临时靠这条命令还原历史现场。</p>
<p>常见堆外问题包括：</p>
<ol>
<li>Netty Direct Buffer 未及时释放。</li>
<li>线程数不断增长，每个线程都需要栈空间。</li>
<li>动态生成大量类导致 Metaspace 增长。</li>
<li>JNI 或本地库发生内存泄漏。</li>
<li>大量内存映射文件没有解除映射。</li>
</ol>
<h2 id="6-容器环境要单独检查限制"><a href="#6-容器环境要单独检查限制" class="headerlink" title="6. 容器环境要单独检查限制"></a>6. 容器环境要单独检查限制</h2><p>容器只看宿主机 <code>free -h</code> 很容易误判。先查看容器实际使用量：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker stats --no-stream</span><br><span class="line">docker inspect --format <span class="string">&#x27;&#123;&#123;.HostConfig.Memory&#125;&#125;&#x27;</span> &lt;container_name&gt;</span><br></pre></td></tr></table></figure>

<p>cgroup v2 可以查看：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> /sys/fs/cgroup/memory.current</span><br><span class="line"><span class="built_in">cat</span> /sys/fs/cgroup/memory.max</span><br><span class="line"><span class="built_in">cat</span> /sys/fs/cgroup/memory.events</span><br></pre></td></tr></table></figure>

<p>如果 <code>memory.events</code> 里的 <code>oom_kill</code> 增加，就说明容器触发过内存限制。cgroup v1 的文件路径不同，需要在对应的 <code>memory</code> 控制组目录下查看 <code>memory.usage_in_bytes</code> 和 <code>memory.limit_in_bytes</code>。</p>
<p>容器限制是 2 GB 时，不能简单设置 <code>-Xmx2g</code>。除了堆以外，线程栈、Metaspace 和堆外内存都要留空间。JDK 8 不同更新版本对容器感知能力也不同，必须核对实际 JVM 参数，而不是默认它会自动识别限制。</p>
<h2 id="7-几种典型根因"><a href="#7-几种典型根因" class="headerlink" title="7. 几种典型根因"></a>7. 几种典型根因</h2><h3 id="7-1-无界缓存"><a href="#7-1-无界缓存" class="headerlink" title="7.1 无界缓存"></a>7.1 无界缓存</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 错误示例：缓存没有数量限制和过期策略</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Map&lt;String, Object&gt; CACHE = <span class="keyword">new</span> <span class="title class_">ConcurrentHashMap</span>&lt;&gt;();</span><br></pre></td></tr></table></figure>

<p>修复时应该使用有最大容量、过期时间和命中率监控的缓存实现，同时确认缓存键是否真的会复用。</p>
<h3 id="7-2-线程池队列无限增长"><a href="#7-2-线程池队列无限增长" class="headerlink" title="7.2 线程池队列无限增长"></a>7.2 线程池队列无限增长</h3><p><code>LinkedBlockingQueue</code> 默认容量接近无限。下游变慢时，任务可能不断堆积，占用大量对象。线程池必须设置有界队列、拒绝策略和队列长度告警。</p>
<h3 id="7-3-一次性加载大结果集"><a href="#7-3-一次性加载大结果集" class="headerlink" title="7.3 一次性加载大结果集"></a>7.3 一次性加载大结果集</h3><p>导出、报表和批处理如果一次查询几十万行并全部放入 List，很容易制造瞬时大对象。可以改为游标、分页或流式处理，并限制单次任务的数据范围。</p>
<h3 id="7-4-线程泄漏"><a href="#7-4-线程泄漏" class="headerlink" title="7.4 线程泄漏"></a>7.4 线程泄漏</h3><p>定时任务反复创建线程池却不关闭，或者每个请求都创建线程，RSS 会随着线程栈增长。除了堆分析，还要长期监控进程线程数。</p>
<h2 id="8-应急处理不要踩这些坑"><a href="#8-应急处理不要踩这些坑" class="headerlink" title="8. 应急处理不要踩这些坑"></a>8. 应急处理不要踩这些坑</h2><ol>
<li>不要看到 <code>free</code> 很低就执行 <code>drop_caches</code>，它可能导致后续大量磁盘读取。</li>
<li>不要在磁盘快满时直接导出大 Heap Dump。</li>
<li>不要在业务高峰对大堆执行高停顿风险的 <code>jmap -histo:live</code>。</li>
<li>不要只增加 <code>-Xmx</code>，否则可能把 JVM OOM 变成整机或容器 OOM Kill。</li>
<li>不要重启以后就结束排查，至少保留内核日志、GC 日志和监控时间点。</li>
</ol>
<h2 id="9-排查清单"><a href="#9-排查清单" class="headerlink" title="9. 排查清单"></a>9. 排查清单</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">整机：free、vmstat、ps、内核 OOM 日志</span><br><span class="line">容器：docker stats、cgroup memory.current/memory.max/memory.events</span><br><span class="line">JVM 堆：VM.flags、GC.heap_info、jstat、Heap Dump</span><br><span class="line">堆外：/proc/PID/status、线程数、NMT、Direct Buffer</span><br><span class="line">业务：缓存容量、线程池队列、批处理数据量、对象生命周期</span><br></pre></td></tr></table></figure>

<p>内存排查不能只看一个瞬时数值，最有价值的是趋势：什么时间开始增长、发布前后是否变化、GC 后能不能回落、增长速度是否与流量一致。把这些时间线对齐，根因通常会清晰很多。</p>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>OOM</tag>
        <tag>JVM</tag>
        <tag>内存排查</tag>
      </tags>
  </entry>
  <entry>
    <title>JUC并发编程知识点梳理</title>
    <url>/2024/05/02/juc%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E4%B8%80%E4%BA%9B%E7%90%86%E8%A7%A3/</url>
    <content><![CDATA[<h1 id="JUC并发编程"><a href="#JUC并发编程" class="headerlink" title="JUC并发编程"></a>JUC并发编程</h1><blockquote>
<p>写在前面</p>
</blockquote>
<p>基础的查缺补漏问题，感觉工作上用的并发编程不是很多，都快忘得差不多了，通过看书+视频的方式，补充一些相关知识。</p>


<h2 id="1-什么是JUC"><a href="#1-什么是JUC" class="headerlink" title="1.什么是JUC"></a>1.什么是JUC</h2><p>JUC（Java Util Concurrency）是Java工具类库中的一部分，提供了在多线程环境下编写并发程序所需的工具和框架。JUC包含了一些并发编程的核心概念和工具，使得开发人员能够更轻松地编写高效、线程安全的并发程序。</p>
<h2 id="2-进程和线程"><a href="#2-进程和线程" class="headerlink" title="2.进程和线程"></a>2.进程和线程</h2><ol>
<li><p>进程：一个程序的集合。称为进程。</p>
</li>
<li><p>一个程序可以包含多个线程，至少包含一个线程！</p>
</li>
<li><p>Java默认包含两个线程，main ，GC</p>
</li>
<li><p>Java开启线程的方式，Thread，Runnable，Callable</p>
</li>
<li><p><strong>Java是无法自己开启线程的</strong>，本质上Java是通过操作c来开启线程</p>
<blockquote>
<p>​	并发，并行</p>
</blockquote>
<p>并发：多个线程操作同一个资源</p>
<ul>
<li>cpu单核，模拟出多个线程，不停切换</li>
</ul>
<p>并行： 多个线程同时操作多个资源</p>
<ul>
<li>​	多核cpu，多个线程同时执行</li>
</ul>
<p>并发编程的本质：充分利用cpu的资源</p>
<blockquote>
<p>线程状态</p>
</blockquote>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">State</span> {</span><br><span class="line"><span class="comment">//新生</span></span><br><span class="line">NEW,</span><br><span class="line">  <span class="comment">// 运行</span></span><br><span class="line">RUNNABLE,</span><br><span class="line"><span class="comment">// 阻塞</span></span><br><span class="line">BLOCKED,</span><br><span class="line"><span class="comment">// 等待，死死地等</span></span><br><span class="line">WAITING,</span><br><span class="line"><span class="comment">// 超时等待</span></span><br><span class="line">TIMED_WAITING,</span><br><span class="line"><span class="comment">// 终止</span></span><br><span class="line">TERMINATED;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>

<blockquote>
<p>​	wait/sleep的区别</p>
</blockquote>
<ol>
<li><p>来自不同的类</p>
<p>Wait =&gt;object,sleep =&gt;Thread</p>
</li>
<li><p>锁的释放</p>
<p>wait会释放锁，sleep 不会释放锁！</p>
</li>
<li><p>使用的范围不同</p>
<p>wait：必须在同步代码块中。sleep没有限制</p>
</li>
<li><p>唤醒方式</p>
<p>wait：需要调用notify或者notifyAll唤醒，sleep自动唤醒，或者调用interrupt（）方法唤醒</p>
</li>
</ol>
</li>
</ol>
<h2 id="3-Lock锁（重点）"><a href="#3-Lock锁（重点）" class="headerlink" title="3.Lock锁（重点）"></a>3.Lock锁（重点）</h2><blockquote>
<p>传统Synchronized</p>
</blockquote>
<p>在代码块或者方法上加synchronized</p>
<blockquote>
<p>Lock接口</p>
</blockquote>
<ol>
<li>加锁，解锁方法</li>
<li>可重入锁，读写锁方法</li>
<li>公平锁，非公平锁</li>
</ol>
<blockquote>
<p>Synchronized 和 Lock 区别</p>
</blockquote>
<ol>
<li><p><code>synchronized</code>关键字和<code>Lock</code>接口是Java中用于实现线程同步的两种不同方式，它们之间有以下几点区别：</p>
<ol>
<li><p><strong>使用方式：</strong></p>
<ul>
<li><code>synchronized</code>是Java语言提供的关键字，可以直接用于方法或代码块中，不需要显式地创建锁对象。</li>
<li><code>Lock</code>是一个接口，需要显式地创建锁对象，通常使用<code>ReentrantLock</code>类来实现。</li>
</ul>
</li>
<li><p><strong>灵活性：</strong></p>
<ul>
<li><code>synchronized</code>是一种内置的锁机制，比较简单，但缺乏一些高级功能。</li>
<li><code>Lock</code>接口提供了更多的功能，如可中断锁、超时获取锁、尝试非阻塞获取锁等。</li>
</ul>
</li>
<li><p><strong>等待锁的方式：</strong></p>
<ul>
<li><code>synchronized</code>在获取不到锁时，线程会进入阻塞状态，直到获取到锁为止。</li>
<li><code>Lock</code>在获取不到锁时，可以选择等待、放弃等待或者做其他处理。</li>
</ul>
</li>
<li><p><strong>释放锁的方式：</strong></p>
<ul>
<li><code>synchronized</code>在方法执行完毕或代码块结束时会自动释放锁。</li>
<li><code>Lock</code>需要手动调用<code>unlock()</code>方法来释放锁。</li>
</ul>
</li>
<li><p><strong>性能：</strong></p>
<ul>
<li><code>synchronized</code>在JVM层面上对锁进行优化，因此在某些情况下性能可能比<code>Lock</code>更好。</li>
<li><code>Lock</code>提供了更多的灵活性和控制，但由于是通过Java代码实现的，性能上可能会稍差。</li>
</ul>
</li>
</ol>
<p>总的来说，<code>synchronized</code>适用于简单的线程同步需求，而<code>Lock</code>适用于对锁需要更精细控制和灵活性的场景。在选择使用时，需要根据具体情况来决定哪种方式更合适。</p>
</li>
</ol>
<h2 id="4-生产者消费者问题"><a href="#4-生产者消费者问题" class="headerlink" title="4.生产者消费者问题"></a>4.生产者消费者问题</h2><p>本质：等待唤醒，通知唤醒。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">* 生产者消费者问题</span></span><br><span class="line"><span class="comment">* */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="type">data1</span> <span class="variable">d</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">data1</span>();</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt;{</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) {</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    d.inc();</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        },<span class="string">"T1"</span>).start();</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt;{</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) {</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    d.dec();</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        },<span class="string">"T2"</span>).start();</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">data1</span>{</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">number</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title function_">inc</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException {</span><br><span class="line">        <span class="keyword">if</span> (number != <span class="number">0</span>) {</span><br><span class="line">            <span class="built_in">this</span>.wait();</span><br><span class="line">        }</span><br><span class="line">        number++;</span><br><span class="line">        System.out.println(Thread.currentThread().getName()+<span class="string">"=&gt;"</span>+number);</span><br><span class="line">        <span class="built_in">this</span>.notifyAll();</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title function_">dec</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException {</span><br><span class="line">        <span class="keyword">if</span> (number == <span class="number">0</span>) {</span><br><span class="line">            <span class="built_in">this</span>.wait();</span><br><span class="line">        }</span><br><span class="line">        number--;</span><br><span class="line">        System.out.println(Thread.currentThread().getName()+<span class="string">"=&gt;"</span>+number);</span><br><span class="line">        <span class="built_in">this</span>.notifyAll();</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>

<blockquote>
<p>虚假唤醒问题</p>
</blockquote>
<p>虚假唤醒（Spurious Wakeup）是指在多线程编程中，一个线程在没有被通知、没有收到信号的情况下，从等待状态（通常是wait()函数）中被唤醒，但此时并没有符合其等待条件的事件发生。这种情况可能会在某些多线程环境中发生，尽管并没有其他线程调用notify()或notifyAll()来通知等待线程，等待线程仍然被唤醒。</p>
<p>虚假唤醒可能会导致程序逻辑错误，因为等待线程被唤醒后可能会继续执行，但实际上并没有真正的事件发生。为了防止虚假唤醒，通常在使用wait()方法时，应该始终在循环中检查条件，并在条件不满足时继续等待，直到条件满足为止。例如：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">synchronized</span> (obj) {</span><br><span class="line">    <span class="keyword">while</span> (!condition) {</span><br><span class="line">        obj.wait();</span><br><span class="line">    }</span><br><span class="line">    <span class="comment">// 执行其他操作</span></span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>

<p>这样可以确保即使发生虚假唤醒，等待线程也会重新检查条件并在条件满足时继续执行，避免了逻辑错误。</p>
<blockquote>
<p>使用juc实现</p>
</blockquote>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.jucstudy.customer;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Condition;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Lock;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.ReentrantLock;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">* 生产者消费者问题</span></span><br><span class="line"><span class="comment">* */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="type">data1</span> <span class="variable">d</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">data1</span>();</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt;{</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) {</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    d.inc();</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        },<span class="string">"T1"</span>).start();</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt;{</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) {</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    d.dec();</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        },<span class="string">"T2"</span>).start();</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">data1</span>{</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">number</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="type">Lock</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ReentrantLock</span>();</span><br><span class="line">    <span class="type">Condition</span> <span class="variable">condition</span> <span class="operator">=</span> lock.newCondition();</span><br><span class="line">    <span class="keyword">public</span>  <span class="keyword">void</span> <span class="title function_">inc</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException {</span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> {</span><br><span class="line">            <span class="keyword">if</span> (number != <span class="number">0</span>) {</span><br><span class="line"><span class="comment">//                等待</span></span><br><span class="line">                condition.await();</span><br><span class="line">            }</span><br><span class="line">            number++;</span><br><span class="line">            System.out.println(Thread.currentThread().getName()+<span class="string">"=&gt;"</span>+number);</span><br><span class="line"><span class="comment">//            通知</span></span><br><span class="line">            condition.signalAll();</span><br><span class="line">        } <span class="keyword">finally</span> {</span><br><span class="line">            lock.unlock();</span><br><span class="line">        }</span><br><span class="line"></span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">public</span>  <span class="keyword">void</span> <span class="title function_">dec</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException {</span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> {</span><br><span class="line">            <span class="keyword">if</span> (number == <span class="number">0</span>) {</span><br><span class="line">                condition.await();</span><br><span class="line">            }</span><br><span class="line">            number--;</span><br><span class="line">            System.out.println(Thread.currentThread().getName()+<span class="string">"=&gt;"</span>+number);</span><br><span class="line">            condition.signalAll();</span><br><span class="line">        } <span class="keyword">finally</span> {</span><br><span class="line">            lock.unlock();</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>



<blockquote>
<p>使用JUC的优点，Condition可以精准的通知和唤醒线程   </p>
</blockquote>
<h2 id="5-集合类不安全"><a href="#5-集合类不安全" class="headerlink" title="5.集合类不安全"></a>5.集合类不安全</h2><blockquote>
<p>LIst，Set，Map，在并发情况下修改数据，都会产生 java.util.ConcurrentModificationException 并发修改异常！</p>
</blockquote>
<h4 id="List下的解决方案"><a href="#List下的解决方案" class="headerlink" title="List下的解决方案"></a>List下的解决方案</h4><ol>
<li>List list = new Vector&lt;&gt;();</li>
<li>List list = Collections.synchronizedList(new ArrayList&lt;&gt; ());</li>
<li><strong>List list = new CopyOnWriteArrayList&lt;&gt;()；</strong></li>
</ol>
<blockquote>
<p>Cow,读写分离</p>
</blockquote>
<p>Copy-on-write（写时复制）是一种用于处理共享数据的技术，它可以解决写入时覆盖的问题。当多个进程或线程要访问同一个资源时，如果每个进程都复制一份资源的副本，会占用大量内存。相反，可以让所有进程共享同一个资源的副本，但只有在有进程要修改资源时才会复制资源，这就是写时复制的概念。</p>
<p>具体来说，当一个进程要修改资源时，系统会首先检查资源的引用计数。如果这个资源只被一个进程引用，那么该进程可以直接修改资源而无需复制。但如果资源被多个进程引用，系统就会将资源复制一份，然后让要修改资源的进程修改新复制的资源，而其他进程继续共享原始资源。这样一来，每个进程都可以独立地修改自己的资源副本，而不会影响其他进程。</p>
<p>通过写时复制，可以在多个进程之间共享资源的同时，避免了写入时的覆盖问题，提高了系统的性能和效率。</p>
<h4 id="Set的解决方案"><a href="#Set的解决方案" class="headerlink" title="Set的解决方案"></a>Set的解决方案</h4><ol>
<li>Set set = Collections.synchronizedSet(new HashSet&lt;&gt;());</li>
<li>Set set = new CopyOnWriteArraySet&lt;&gt;();</li>
</ol>
<blockquote>
<p>HashSet的底层</p>
</blockquote>
<p>本质上就是HashMap，用set的值作为key，所以保证了唯一性。</p>
<h4 id="Map的解决方案"><a href="#Map的解决方案" class="headerlink" title="Map的解决方案"></a>Map的解决方案</h4><ol>
<li>Map map = new ConcurrentHashMap&lt;&gt;();</li>
<li>Collections.synchronizedMap；</li>
</ol>
<p>原理：</p>
<p>ConcurrentHashMap 是 Java 中线程安全的哈希表实现，它采用了一种不同于传统 HashMap 的机制来实现并发访问。主要原理如下：</p>
<ol>
<li><p><strong>分段锁（Segment）</strong>：ConcurrentHashMap 内部维护了一个 Segment 数组，每个 Segment 就是一个独立的哈希表，类似于将一个大的哈希表分成多个小的哈希表。每个 Segment 都相当于一个小的 HashMap，它们之间相互独立，可以独立进行并发操作。</p>
</li>
<li><p><strong>Hash 表的扩容</strong>：与 HashMap 类似，ConcurrentHashMap 也会在需要时对哈希表进行扩容。但不同的是，它只会对某个 Segment 进行扩容，而不是对整个哈希表进行扩容。这样可以减少扩容时的锁竞争，提高并发性能。</p>
</li>
<li><p><strong>put 操作的实现</strong>：当执行 put 操作时，ConcurrentHashMap 首先根据 key 的哈希值确定要放入哪个 Segment 中，然后在该 Segment 上加锁，进行插入操作。这样可以保证在放入元素时，其他线程可以继续访问其他 Segment 中的元素，不会受到阻塞。</p>
</li>
<li><p><strong>get 操作的实现</strong>：与 put 操作类似，get 操作也会根据 key 的哈希值确定要查找哪个 Segment，然后在该 Segment 上加锁，进行查找操作。这样可以保证在查找元素时，其他线程可以继续访问其他 Segment 中的元素，不会受到阻塞。</p>
</li>
<li><p><strong>性能优化</strong>：ConcurrentHashMap 在实现上进行了多项性能优化，如使用了 volatile 修饰的数组元素来保证可见性，使用了 CAS（Compare and Swap）操作来保证原子性，以及使用了分段锁来减少锁竞争等。</p>
<h2 id="6-Callable"><a href="#6-Callable" class="headerlink" title="6.Callable"></a>6.Callable</h2></li>
</ol>
<p>优势（对比Runnable）</p>
<ol>
<li>有返回值</li>
<li>可以抛出异常</li>
<li>方法不同run/call</li>
</ol>
<blockquote>
<p> 使用：需要借助FutureTask适配类来放入线程中</p>
</blockquote>
<h2 id="7-常用的辅助类"><a href="#7-常用的辅助类" class="headerlink" title="7.常用的辅助类"></a>7.常用的辅助类</h2><h3 id="8-1-CountDownLatch-减法计数器"><a href="#8-1-CountDownLatch-减法计数器" class="headerlink" title="8.1 CountDownLatch-减法计数器"></a>8.1 CountDownLatch-减法计数器</h3><figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException {</span><br><span class="line">        <span class="type">CountDownLatch</span> <span class="variable">countDownLatch</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CountDownLatch</span>(<span class="number">5</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) {</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt;{</span><br><span class="line">                System.out.println(Thread.currentThread().getName()+<span class="string">"离开"</span>);</span><br><span class="line">            countDownLatch.countDown();<span class="comment">//数量-1</span></span><br><span class="line">                 },String.valueOf(i)).start();</span><br><span class="line">        }</span><br><span class="line"></span><br><span class="line">        countDownLatch.await();</span><br><span class="line">        System.out.println(<span class="string">"结束！！！"</span>);</span><br><span class="line">        }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>

<p>原理：</p>
<p><strong>countDownLatch.countDown();</strong> 数量-1</p>
<p><strong>countDownLatch.await();</strong> 等待计数器归零，再向下执行。</p>
<h3 id="8-2-CyclicBarrier-加法计数器"><a href="#8-2-CyclicBarrier-加法计数器" class="headerlink" title="8.2 CyclicBarrier-加法计数器"></a>8.2 CyclicBarrier-加法计数器</h3><figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException, BrokenBarrierException {</span><br><span class="line">        <span class="type">CyclicBarrier</span> <span class="variable">cyclicBarrier</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CyclicBarrier</span>(<span class="number">7</span>,()-&gt;{</span><br><span class="line">            System.out.println(<span class="string">"总数到七了"</span>);</span><br><span class="line">        });</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">7</span>; i++) {</span><br><span class="line">            <span class="type">int</span> <span class="variable">finalI</span> <span class="operator">=</span> i;</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Thread</span>(()-&gt;{</span><br><span class="line">                System.out.println(<span class="string">"现在共有"</span>+ finalI +<span class="string">"个线程"</span>);</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    cyclicBarrier.await();</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException | BrokenBarrierException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                }</span><br><span class="line">            },i+<span class="string">""</span>).start();</span><br><span class="line">        }</span><br><span class="line">        }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>

<h3 id="8-3-Semaphore信号量"><a href="#8-3-Semaphore信号量" class="headerlink" title="8.3 Semaphore信号量"></a>8.3 Semaphore信号量</h3><figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException, BrokenBarrierException {</span><br><span class="line">        <span class="type">Semaphore</span> <span class="variable">semaphore</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Semaphore</span>(<span class="number">3</span>);<span class="comment">//线程数量</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">6</span>; i++) {</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; {</span><br><span class="line">                <span class="keyword">try</span> {</span><br><span class="line">                    semaphore.acquire();<span class="comment">//获得</span></span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">"创建了"</span>);</span><br><span class="line">                    TimeUnit.SECONDS.sleep(<span class="number">2</span>);</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">"销毁了"</span>);</span><br><span class="line">                } <span class="keyword">catch</span> (InterruptedException e) {</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">                } <span class="keyword">finally</span> {</span><br><span class="line">                    semaphore.release();<span class="comment">//释放</span></span><br><span class="line">                }</span><br><span class="line">            }, String.valueOf(i)).start();</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>



<p> <strong>semaphore.acquire()</strong>;//获得，如果已经到了设置的上限，则会等待，到释放为止</p>
<p><strong>semaphore.release();//释放</strong> 将当前的信号量释放，然后唤醒等待的线程。</p>
<blockquote>
<p>作用：多个共享资源互斥的使用，并发限流，控制最大的线程数！</p>
</blockquote>
<h2 id="8-读写锁"><a href="#8-读写锁" class="headerlink" title="8. 读写锁"></a>8. 读写锁</h2><p>ReadWriteLock</p>
<p>读可以多个线程读，写只能有一个线程</p>
<h2 id="9-阻塞队列"><a href="#9-阻塞队列" class="headerlink" title="9.阻塞队列"></a>9.阻塞队列</h2><p>BlockingQueue:Collection包的子类</p>
<p>用处：</p>
<ol>
<li>多线程并发处理</li>
<li>线程池</li>
<li>使用： ArrayBlockingQueue blockingQueue = new ArrayBlockingQueue&lt;&gt;(3);</li>
</ol>
<p>四组APi</p>
<table>
<thead>
<tr>
<th>方式</th>
<th>抛出异常</th>
<th>有返回值，不抛出</th>
<th>阻塞等待</th>
<th>超时等待</th>
</tr>
</thead>
<tbody><tr>
<td>添加</td>
<td>Add</td>
<td>Offer()</td>
<td>put</td>
<td>offer(,,,)</td>
</tr>
<tr>
<td>移除</td>
<td>Remove</td>
<td>Poll</td>
<td>Take</td>
<td>Poll(,)</td>
</tr>
<tr>
<td>检测队首元素</td>
<td>Element</td>
<td>Peek</td>
<td>-</td>
<td>-</td>
</tr>
</tbody></table>
<blockquote>
<p>SynchoronousQueue 同步队列</p>
</blockquote>
<p>没有容量：进去一个元素必须取出来以后，才能放入其他元素。put take来使用</p>
<h2 id="10-线程池（三大方法，七大参数，四种拒绝策略）"><a href="#10-线程池（三大方法，七大参数，四种拒绝策略）" class="headerlink" title="10.线程池（三大方法，七大参数，四种拒绝策略）"></a>10.线程池（三大方法，七大参数，四种拒绝策略）</h2><blockquote>
<p>池化技术</p>
</blockquote>
<p>本质：优化资源的使用，实现准备好一些资源，要用的时候直接来拿，使用完再返回给池</p>
<p>好处</p>
<ol>
<li>降低资源的消耗</li>
<li>提高响应速度</li>
<li>方便管理</li>
<li>线程复用，可以控制最大并发的数量，管理线程</li>
</ol>
<h3 id="三大方法"><a href="#三大方法" class="headerlink" title="三大方法"></a>三大方法</h3><figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">demo01</span> {</span><br><span class="line"></span><br><span class="line"><span class="comment">//    线程池三大方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line"><span class="comment">//        ExecutorService threadPool = Executors.newSingleThreadExecutor();//单例模式</span></span><br><span class="line"><span class="comment">//        ExecutorService threadPool = Executors.newFixedThreadPool(5);//创建指定数量的线程池</span></span><br><span class="line">        <span class="type">ExecutorService</span> <span class="variable">threadPool</span> <span class="operator">=</span> Executors.newCachedThreadPool();<span class="comment">//可伸缩的线程池</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> {</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) {</span><br><span class="line"><span class="comment">//                使用线程池来创建线程</span></span><br><span class="line">                threadPool.execute(()-&gt;{</span><br><span class="line">                    System.out.println(Thread.currentThread().getName()+<span class="string">"okkk"</span>);</span><br><span class="line">                });</span><br><span class="line">            }</span><br><span class="line">        } <span class="keyword">finally</span> {</span><br><span class="line">            threadPool.shutdown();</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>



<h3 id="七大参数"><a href="#七大参数" class="headerlink" title="七大参数"></a>七大参数</h3><figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">ThreadPoolExecutor</span><span class="params">(<span class="type">int</span> corePoolSize,//核心线程数</span></span><br><span class="line"><span class="params">                          <span class="type">int</span> maximumPoolSize,//最大线程数</span></span><br><span class="line"><span class="params">                          <span class="type">long</span> keepAliveTime,//超时释放时间</span></span><br><span class="line"><span class="params">                          TimeUnit unit,//超时单位</span></span><br><span class="line"><span class="params">                          BlockingQueue&lt;Runnable&gt; workQueue//阻塞队列</span></span><br><span class="line"><span class="params">                          ThreadFactory threadFactory, // 线程工厂：创建线程的，一般不用动</span></span><br><span class="line"><span class="params">											RejectedExecutionHandler handle // 拒绝策略</span></span><br><span class="line"><span class="params">                         )</span> {</span><br><span class="line"> </span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>



<blockquote>
<p>为什么阿里巴巴编程规范说，不要使用Executors创建线程池</p>
</blockquote>
<ol>
<li>FixedThreadPool和SingleThreadPool：最大线程数设置为Interger.MAX_VALUE.可能会堆积大量的请求，导致OOM内存溢出。</li>
</ol>
<blockquote>
<p>线程池不拒绝的最大线程数</p>
</blockquote>
<p>最大线程数+阻塞队列长度</p>
<h3 id="四种拒绝策略"><a href="#四种拒绝策略" class="headerlink" title="四种拒绝策略"></a>四种拒绝策略</h3><p>在 Java 中，线程池中的任务队列满了，无法继续接受新任务时，线程池会根据配置的拒绝策略来处理这种情况。常见的四种拒绝策略如下：</p>
<ol>
<li><p><strong>AbortPolicy（默认策略）</strong>：直接抛出 RejectedExecutionException 异常，表示拒绝执行新任务。</p>
</li>
<li><p><strong>CallerRunsPolicy</strong>：将任务交给调用线程来执行，如果线程池已满，新任务将在调用线程中执行。这样可以降低新任务的提交速度，避免任务堆积，但可能会影响调用线程的性能。</p>
</li>
<li><p><strong>DiscardPolicy</strong>：直接丢弃无法处理的任务，不做任何处理。这种策略可能会导致任务丢失，慎用。</p>
</li>
<li><p><strong>DiscardOldestPolicy</strong>：丢弃最早加入任务队列的任务，然后尝试再次提交新任务。这种策略可以保证新任务能够得到处理，但会丢失一部分任务。</p>
</li>
</ol>
<h3 id="如何去设置线程池的大小"><a href="#如何去设置线程池的大小" class="headerlink" title="如何去设置线程池的大小"></a>如何去设置线程池的大小</h3><p>判断当前系统属于IO密集型还是CPU密集型</p>
<ol>
<li><strong>CPU 密集型任务</strong>：对于 CPU 密集型任务，线程池的大小应该设置为接近或等于处理器核心数的数量。这样可以充分利用 CPU 资源，避免线程过多导致频繁的上下文切换，提高任务执行效率。过多的线程数量可能会导致竞争，降低性能。</li>
<li><strong>I/O 密集型任务</strong>：对于 I/O 密集型任务，线程池的大小通常可以设置得更大一些，因为线程在等待 I/O 操作完成时并不会占用 CPU 资源，可以允许更多的线程同时执行。一般建议将线程池大小设置为 2 * CPU 核心数 或者更大，以确保能够充分利用 CPU 和 I/O 资源。</li>
</ol>
<h2 id="11-四大函数式接口"><a href="#11-四大函数式接口" class="headerlink" title="11.四大函数式接口"></a>11.四大函数式接口</h2><blockquote>
<p>函数式接口：只有一个方法的接口</p>
</blockquote>
<h3 id="1-Function函数式接口-有输入，有返回"><a href="#1-Function函数式接口-有输入，有返回" class="headerlink" title="1. Function函数式接口:有输入，有返回"></a>1. Function函数式接口:有输入，有返回</h3><figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line">Function&lt;String,String&gt; function = (str)-&gt;{<span class="keyword">return</span> str;};</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>

<h3 id="2-断定型接口：只有一个输入参数，返回值必须是布尔值"><a href="#2-断定型接口：只有一个输入参数，返回值必须是布尔值" class="headerlink" title="2. 断定型接口：只有一个输入参数，返回值必须是布尔值"></a>2. 断定型接口：只有一个输入参数，返回值必须是布尔值</h3><figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line">Predicate&lt;String&gt; predicate = (str)-&gt;{<span class="keyword">return</span> str.isEmpty(); };</span><br></pre></td></tr></tbody></table></figure>

<h3 id="3-消费型接口：有输入，有返回"><a href="#3-消费型接口：有输入，有返回" class="headerlink" title="3. 消费型接口：有输入，有返回"></a>3. 消费型接口：有输入，有返回</h3><figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line">Consumer&lt;String&gt; consumer = (str)-&gt;{System.out.println(str);};</span><br></pre></td></tr></tbody></table></figure>

<h3 id="4-Supper：供给型接口-没有输入，只有返回"><a href="#4-Supper：供给型接口-没有输入，只有返回" class="headerlink" title="4.Supper：供给型接口:没有输入，只有返回"></a>4.Supper：供给型接口:没有输入，只有返回</h3><figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="type">Supplier</span> <span class="variable">supplier</span> <span class="operator">=</span> ()-&gt;{ <span class="keyword">return</span> <span class="number">1024</span>; };</span><br></pre></td></tr></tbody></table></figure>



<h2 id="12-Stream流式运算"><a href="#12-Stream流式运算" class="headerlink" title="12.Stream流式运算"></a>12.Stream流式运算</h2><figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Test</span> {</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line"><span class="type">User</span> <span class="variable">u1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="number">1</span>,<span class="string">"a"</span>,<span class="number">21</span>);</span><br><span class="line"><span class="type">User</span> <span class="variable">u2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="number">2</span>,<span class="string">"b"</span>,<span class="number">22</span>);</span><br><span class="line"><span class="type">User</span> <span class="variable">u3</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="number">3</span>,<span class="string">"c"</span>,<span class="number">23</span>);</span><br><span class="line"><span class="type">User</span> <span class="variable">u4</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="number">4</span>,<span class="string">"d"</span>,<span class="number">24</span>);</span><br><span class="line"><span class="type">User</span> <span class="variable">u5</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="number">6</span>,<span class="string">"e"</span>,<span class="number">25</span>);</span><br><span class="line"><span class="comment">// 集合就是存储</span></span><br><span class="line">List&lt;User&gt; list = Arrays.asList(u1, u2, u3, u4, u5);</span><br><span class="line"><span class="comment">// 计算交给Stream流</span></span><br><span class="line"><span class="comment">// lambda表达式、链式编程、函数式接口、Stream流式计算</span></span><br><span class="line">list.stream()</span><br><span class="line">.filter(u-&gt;{<span class="keyword">return</span> u.getId()%<span class="number">2</span>==<span class="number">0</span>;})</span><br><span class="line">.filter(u-&gt;{<span class="keyword">return</span> u.getAge()&gt;<span class="number">23</span>;})</span><br><span class="line">.map(u-&gt;{<span class="keyword">return</span> u.getName().toUpperCase();})</span><br><span class="line">.sorted((uu1,uu2)-&gt;{<span class="keyword">return</span> uu2.compareTo(uu1);})</span><br><span class="line">.limit(<span class="number">1</span>)</span><br><span class="line">.forEach(System.out::println);</span><br><span class="line">}</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>

<p><strong>运用到的就是四大函数式接口的概念</strong></p>
<h2 id="13-ForkJoin"><a href="#13-ForkJoin" class="headerlink" title="13.ForkJoin"></a>13.ForkJoin</h2><p>ForkJoin 是 Java 中用于并行计算的框架，主要用于处理分治任务。它基于工作窃取（work-stealing）算法，可以将一个大任务拆分成多个小任务，并且在多个线程之间动态地调度这些任务，以实现并行计算的效果。</p>
<p>ForkJoin 框架的核心是 ForkJoinPool、ForkJoinTask 和 RecursiveTask（或 RecursiveAction）：</p>
<ol>
<li><p><strong>ForkJoinPool</strong>：ForkJoinPool 是一个特殊的线程池，它使用工作窃取算法来提高并行任务的执行效率。ForkJoinPool 中的线程通过工作队列来存放任务，每个线程都有自己的工作队列，当自己的队列中没有任务时，会去其他线程的队列中窃取任务执行。</p>
</li>
<li><p><strong>ForkJoinTask</strong>：ForkJoinTask 是一个抽象类，用于表示可以并行执行的任务。它有两个重要的子类：RecursiveTask 和 RecursiveAction。RecursiveTask 用于有返回值的任务，而 RecursiveAction 用于没有返回值的任务。</p>
</li>
<li><p><strong>RecursiveTask</strong>：RecursiveTask 是一个抽象类，表示可以返回结果的任务。当一个任务需要拆分成多个子任务进行并行计算，并且最终需要合并子任务的结果时，可以使用 RecursiveTask。</p>
</li>
<li><p><strong>RecursiveAction</strong>：RecursiveAction 是一个抽象类，表示不需要返回结果的任务。当一个任务只需要执行某些操作，而不需要返回结果时，可以使用 RecursiveAction。</p>
</li>
</ol>
<p>ForkJoin 框架适用于需要并行处理的分治任务，可以有效地利用多核处理器的优势，提高程序的性能。在使用 ForkJoin 框架时，需要注意合理设计任务的拆分和合并策略，以及避免出现死锁等并发问题。</p>
<h2 id="14-异步调用"><a href="#14-异步调用" class="headerlink" title="14.异步调用"></a>14.异步调用</h2><p>异步调用是一种编程模型，用于处理需要等待较长时间的操作，例如网络请求、文件读写、数据库查询等。在传统的同步调用中，调用者会等待操作完成后才能继续执行后续代码，而在异步调用中，调用者可以继续执行后续代码，而不必等待操作完成。</p>
<p>在异步调用中，通常会通过回调函数、Future/Promise 对象或者异步方法来处理操作完成后的结果。这样可以提高程序的响应性和并发性，避免阻塞主线程。异步调用常用于需要处理大量并发请求或者需要等待外部资源响应的场景，能够更有效地利用系统资源。</p>
<p>在 Java 中，可以使用 CompletableFuture、FutureTask、Callback 等机制来实现异步调用。在 JavaScript 中，可以使用 Promise、async/await 等机制来实现异步调用。异步调用在现代编程中非常常见，能够提高程序的性能和用户体验。</p>
<h2 id="15-JMM"><a href="#15-JMM" class="headerlink" title="15.JMM"></a>15.JMM</h2><blockquote>
<p>什么是JMM</p>
</blockquote>
<p>JMM 是 Java 内存模型（Java Memory Model）的缩写，是 Java 虚拟机规范中定义的一种抽象的计算机内存模型，用于屏蔽不同硬件和操作系统的内存访问差异，确保在不同平台上对 Java 程序的并发访问都能得到一致的结果。</p>
<p>Java 内存模型规定了所有线程之间共享的主内存以及每个线程私有的工作内存之间的关系，主要包括以下几个方面的内容：</p>
<ol>
<li><p><strong>原子性（Atomicity）</strong>：对基本数据类型的读取和赋值操作具有原子性，即这些操作不会被中断。</p>
</li>
<li><p><strong>可见性（Visibility）</strong>：一个线程修改的变量对其他线程是可见的，即一个线程修改的共享变量的值在其他线程中是可以看到的。</p>
</li>
<li><p><strong>有序性（Ordering）</strong>：在单个线程内，程序的执行顺序按照代码的顺序执行。在多线程情况下，由于指令重排序的存在，可能会导致不同线程观察到的执行顺序不同。Java 内存模型通过 happens-before 原则来规定多线程之间操作的执行顺序。</p>
</li>
</ol>
<p><strong>关于JMM的一些同步约定</strong></p>
<ol>
<li>线程解锁前，必须将共享变量立刻刷新回主存。</li>
<li>进程加锁前，必须读取主存的最新值到工作内存中</li>
<li>加锁解锁都必须是同一把锁。</li>
</ol>
<h2 id="16-Volatile"><a href="#16-Volatile" class="headerlink" title="16. Volatile"></a>16. Volatile</h2><ol>
<li><strong>可见性</strong>：被 <code>volatile</code> 修饰的变量在一个线程中被修改后，会立即更新到主内存中，其他线程读取该变量时会直接从主内存中获取最新值，保证了变量的可见性。</li>
<li><strong>禁止重排序</strong>：<code>volatile</code> 修饰的变量禁止了指令重排序，保证了变量的修改顺序与代码中的顺序一致。</li>
<li><strong>不保证原子性</strong>：<code>volatile</code> 修饰的变量对于单次读/写操作是原子的，但是对于多次操作（比如 i++）并不能保证原子性，如果需要保证原子性，可以使用 <code>synchronized</code> 关键字或者 <code>java.util.concurrent.atomic</code> 包中的原子类</li>
</ol>
<blockquote>
<p>如果不使用lock和synchronized，如何保证原子性？</p>
</blockquote>
<p>使用原子类 Atomic，底层直接根操作系统挂钩，在内存中修改值。</p>
<blockquote>
<p>指令重排</p>
</blockquote>
<p>代处理器为了提高执行效率而进行的一种优化手段。处理器在执行指令时，并不一定会按照代码中的顺序依次执行，而是可能会重新排列指令的执行顺序，以尽量减少指令之间的相关性，提高并行度和性能。</p>
<blockquote>
<p>volatile如何避免指令重排</p>
</blockquote>
<p><code>volatile</code>关键字实现禁止指令重排的机制是通过在编译器层面和运行时层面进行限制的。在编译器层面，编译器会禁止对<code>volatile</code>变量进行重排序优化，保证在执行顺序上不会将后面的操作排在前面。在运行时层面，JVM会在每次对<code>volatile</code>变量进行读写操作时，插入内存屏障指令（Memory Barrier），确保变量的写操作先于后续的读操作，从而保证了操作的顺序性。</p>
<h2 id="17-单例模式"><a href="#17-单例模式" class="headerlink" title="17.单例模式"></a>17.单例模式</h2><h3 id="饿汉式"><a href="#饿汉式" class="headerlink" title="饿汉式"></a>饿汉式</h3><p>直接创建对象，会浪费内存空间</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Test1</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">Test1</span> <span class="variable">instance</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Test1</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">Test1</span><span class="params">()</span> {}</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Test1 <span class="title function_">getInstance</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    }</span><br><span class="line">    </span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>

<h3 id="懒汉式"><a href="#懒汉式" class="headerlink" title="懒汉式"></a>懒汉式</h3><p>在使用时才会创建，会出现并发问题</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Singleton</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> Singleton instance;<span class="comment">//使用volatile，保证可见性</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">Singleton</span><span class="params">()</span> {</span><br><span class="line">        <span class="comment">// 私有化构造函数，避免外部实例化</span></span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title function_">getInstance</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">if</span> (instance == <span class="literal">null</span>) {</span><br><span class="line">            <span class="keyword">synchronized</span> (Singleton.class) {</span><br><span class="line">                <span class="keyword">if</span> (instance == <span class="literal">null</span>) {</span><br><span class="line">                    instance = <span class="keyword">new</span> <span class="title class_">Singleton</span>();</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">doSomething</span><span class="params">()</span> {</span><br><span class="line">        System.out.println(<span class="string">"Singleton is doing something."</span>);</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure>

<blockquote>
<p>反射可以破坏单例模式</p>
</blockquote>
<p><strong>最好的单例模式，是枚举，无法被破坏</strong></p>
<h2 id="18-CAS相关"><a href="#18-CAS相关" class="headerlink" title="18.CAS相关"></a>18.CAS相关</h2><blockquote>
<p>什么是CAS</p>
</blockquote>
<p>CAS（Compare and Swap，比较并交换）是一种用于实现多线程同步的原子操作。在CAS操作中，系统会先比较内存中某个位置的值与预期值是否相等，如果相等，则将该位置的值更新为新的值；如果不相等，则操作失败。整个操作是原子的，即在同一时刻只有一个线程能够成功执行该操作。</p>
<p>CAS操作通常用于实现非阻塞算法和并发数据结构，例如Java中的<code>AtomicInteger</code>、<code>AtomicLong</code>等类就是基于CAS操作实现的。CAS操作的优势在于它避免了传统的加锁操作所带来的性能损耗，因为它是基于硬件级别的原子操作，不需要像锁一样在用户态和内核态之间切换。</p>
<p>在Java中，CAS操作由<code>java.util.concurrent.atomic</code>包提供支持，通常使用<code>compareAndSet()</code>方法来实现。例如：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><span class="line"><span class="type">AtomicInteger</span> <span class="variable">atomicInteger</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AtomicInteger</span>(<span class="number">0</span>);</span><br><span class="line"><span class="type">int</span> <span class="variable">expectedValue</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> <span class="variable">newValue</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"><span class="keyword">while</span> (!atomicInteger.compareAndSet(expectedValue, newValue)) {</span><br><span class="line">    expectedValue = atomicInteger.get();</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure>

<p>上面的代码中，<code>compareAndSet()</code>方法会比较<code>atomicInteger</code>的当前值是否等于<code>expectedValue</code>，如果相等，则将其更新为<code>newValue</code>，否则操作失败。通过循环不断尝试，直到操作成功为止。</p>
<blockquote>
<p>unsafe类</p>
</blockquote>
<p>Java是无法直接操作内存的，c++可以，CAS就是通过直接操作内存来实现的。</p>
<p>CAS ： 比较当前工作内存中的值和主内存中的值，如果这个值是期望的，那么则执行操作！如果不是就 一直循环！ </p>
<p>缺点</p>
<p> 1、 循环会耗时</p>
<p> 2、一次性只能保证一个共享变量的原子性 </p>
<p>3、ABA问题</p>
<h3 id="ABA问题（狸猫换太子）"><a href="#ABA问题（狸猫换太子）" class="headerlink" title="ABA问题（狸猫换太子）"></a>ABA问题（狸猫换太子）</h3><p>一个线程读取了值之后，另外一个线程修改了这个值两次，最终改为了原来的值，第一个线程发现值没有变化，则不会发现其他线程修改过。</p>
<h2 id="19-原子引用"><a href="#19-原子引用" class="headerlink" title="19.原子引用"></a>19.原子引用</h2><blockquote>
<p>解决ABA问题，引入原子引用，对应的是乐观锁思想</p>
</blockquote>
<p>解决ABA问题的常见方法是使用带有版本号的原子引用，例如<code>AtomicStampedReference</code>类。这个类在比较时除了比较值外，还会比较一个版本号（stamp），只有当值和版本号都匹配时才进行修改。这样一来，即使值在过程中发生了变化，只要版本号不同，就能够避免误判。</p>
<h2 id="20-各种锁的理解"><a href="#20-各种锁的理解" class="headerlink" title="20.各种锁的理解"></a>20.各种锁的理解</h2><h3 id="1-公平锁，非公平锁"><a href="#1-公平锁，非公平锁" class="headerlink" title="1.公平锁，非公平锁"></a>1.公平锁，非公平锁</h3><p>公平锁：先来后到。</p>
<p>非公平锁：可以插队（默认）</p>
<h3 id="2-可重入锁"><a href="#2-可重入锁" class="headerlink" title="2. 可重入锁"></a>2. 可重入锁</h3><p>可重入锁（Reentrant Lock）是一种支持在同一个线程中多次获取同一把锁的锁机制。这意味着线程可以在持有锁的情况下再次获取该锁，而不会造成死锁。可重入锁是一种比较常见的锁实现方式，在Java中通过<code>synchronized</code>关键字和<code>ReentrantLock</code>类来实现。</p>
<p>在使用可重入锁时，同一个线程可以多次获取锁而不会被阻塞。线程每次成功获取锁后，需要在最后释放相同次数的锁。这样可以确保锁的所有权不会被其他线程所获取，从而避免了死锁的发生。</p>
<p>可重入锁的一个典型应用场景是递归函数。如果一个函数是递归的，并且这个函数需要获取锁来访问临界区资源，那么使用可重入锁可以确保同一线程在递归调用过程中可以多次获取锁而不会产生死锁。</p>
<p>总的来说，可重入锁是一种非常实用的锁机制，能够简化并发编程中的锁管理，并提高程序的可靠性和性能。</p>
<h3 id="3-自旋锁（底层是CAS）"><a href="#3-自旋锁（底层是CAS）" class="headerlink" title="3.自旋锁（底层是CAS）"></a>3.自旋锁（底层是CAS）</h3><p>自旋锁是一种基于忙等待的锁机制，在尝试获取锁时，线程不会立即进入阻塞状态，而是循环检测锁的状态，直到获取到锁为止。自旋锁适用于锁被持有时间较短的情况，因为长时间的自旋会消耗CPU资源。</p>
<p>在Java中，<code>AtomicInteger</code>等原子类的<code>compareAndSet()</code>方法就是使用自旋锁实现的。当多个线程同时尝试修改同一个<code>AtomicInteger</code>的值时，如果没有竞争，<code>compareAndSet()</code>会立即成功；如果有竞争，线程会不断尝试，直到成功为止。</p>
<p>自旋锁的优点是避免了线程切换的开销，缺点是会消耗CPU资源，并且在高并发情况下可能会导致性能下降。因此，使用自旋锁时需要根据实际情况权衡。</p>
<h3 id="4-死锁"><a href="#4-死锁" class="headerlink" title="4.死锁"></a>4.死锁</h3><p>死锁是指两个或多个线程（或进程）在执行过程中，因争夺资源而造成的一种互相等待的现象，导致它们都无法继续执行下去。简单来说，就是多个线程在互相等待对方释放资源，导致它们都无法继续执行下去。</p>
<p>死锁发生的必要条件包括：</p>
<ol>
<li>互斥条件（资源只能被一个线程占用）、</li>
<li>请求与保持条件（一个线程持有了至少一个资源，并且正在请求另一个资源，但该资源被其他线程持有）、</li>
<li>不剥夺条件（线程不能强制占有其他线程的资源，只能自己释放）</li>
<li>循环等待条件（若干线程之间形成一种头尾相接的循环等待资源关系）。</li>
</ol>
<p>死锁在多线程编程中是一个比较常见的问题，因为线程在获取资源时可能会发生竞争，如果不合理地使用锁或资源分配策略，就容易造成死锁。预防死锁的方法包括使用适当的锁策略、避免循环等待、按照固定的顺序获取锁等。</p>
<blockquote>
<p>如何排查死锁</p>
</blockquote>
<ol>
<li>使用jps -l定位进程号</li>
<li>使用jstack进程号，找死锁问题</li>
</ol>
]]></content>
      <categories>
        <category>Java基础</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>并发编程</tag>
      </tags>
  </entry>
  <entry>
    <title>服务器磁盘空间与IO异常定位</title>
    <url>/2023/11/26/%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%A3%81%E7%9B%98%E7%A9%BA%E9%97%B4%E4%B8%8EIO%E5%BC%82%E5%B8%B8%E5%AE%9A%E4%BD%8D/</url>
    <content><![CDATA[<blockquote>
<p>写在前面：磁盘问题不只有“空间满了”。空间还有很多但文件创建失败，可能是 inode 用完；接口突然变慢但 CPU 不高，可能是 IO 等待；文件已经删除但空间不释放，可能是进程仍然占着文件句柄。</p>
</blockquote>
<p>这篇把磁盘空间、inode 和 IO 延迟放在一条排查链路里。遇到告警时先判断是哪一类问题，再决定能不能清理文件，不要上来就对 <code>/var/log</code> 执行删除命令。</p>
<h2 id="1-先判断空间还是-inode"><a href="#1-先判断空间还是-inode" class="headerlink" title="1. 先判断空间还是 inode"></a>1. 先判断空间还是 inode</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">df</span> -hT</span><br><span class="line"><span class="built_in">df</span> -ih</span><br><span class="line">lsblk -f</span><br></pre></td></tr></table></figure>

<p>三个命令分别回答：</p>
<ol>
<li>哪个文件系统的容量用完了。</li>
<li>哪个文件系统的 inode 用完了。</li>
<li>分区、文件系统类型和挂载点是什么。</li>
</ol>
<p>典型空间告警：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Filesystem     Type  Size  Used Avail Use% Mounted on</span><br><span class="line">/dev/vda1      xfs    80G   77G  3.0G  97% /</span><br></pre></td></tr></table></figure>

<p>典型 inode 告警：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Filesystem     Inodes IUsed IFree IUse% Mounted on</span><br><span class="line">/dev/vda1        40M   40M     0  100% /</span><br></pre></td></tr></table></figure>

<p>inode 用完通常是产生了海量小文件，例如临时文件、会话文件、消息落盘或日志分片。此时 <code>du</code> 看到的总容量可能不大，但系统已经无法创建新文件。</p>
<h2 id="2-从挂载点向下找大目录"><a href="#2-从挂载点向下找大目录" class="headerlink" title="2. 从挂载点向下找大目录"></a>2. 从挂载点向下找大目录</h2><p>确认是根分区以后，使用 <code>-x</code> 限制在当前文件系统，避免把其他挂载盘也统计进去：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">du</span> -xhd1 / 2&gt;/dev/null | <span class="built_in">sort</span> -h</span><br><span class="line"><span class="built_in">du</span> -xhd1 /var 2&gt;/dev/null | <span class="built_in">sort</span> -h</span><br><span class="line"><span class="built_in">du</span> -xhd1 /var/log 2&gt;/dev/null | <span class="built_in">sort</span> -h</span><br></pre></td></tr></table></figure>

<p>查找大文件时可以使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">find /var -xdev -<span class="built_in">type</span> f -size +1G -<span class="built_in">printf</span> <span class="string">&#x27;%s %p\n&#x27;</span> 2&gt;/dev/null \</span><br><span class="line">  | <span class="built_in">sort</span> -nr \</span><br><span class="line">  | <span class="built_in">head</span> -20</span><br></pre></td></tr></table></figure>

<p><code>-printf</code> 是 GNU <code>find</code> 的能力，部分精简系统不支持。命令只用于定位，找到文件以后还要确认文件归属、写入进程和保留策略，不能根据大小直接删除。</p>
<p>如果 inode 用完，可以按目录统计文件数量：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">find /var -xdev -<span class="built_in">type</span> f -<span class="built_in">printf</span> <span class="string">&#x27;%h\n&#x27;</span> 2&gt;/dev/null \</span><br><span class="line">  | <span class="built_in">sort</span> \</span><br><span class="line">  | <span class="built_in">uniq</span> -c \</span><br><span class="line">  | <span class="built_in">sort</span> -nr \</span><br><span class="line">  | <span class="built_in">head</span> -20</span><br></pre></td></tr></table></figure>

<h2 id="3-df-和-du-对不上怎么办"><a href="#3-df-和-du-对不上怎么办" class="headerlink" title="3. df 和 du 对不上怎么办"></a>3. df 和 du 对不上怎么办</h2><p>常见现象是 <code>df</code> 显示磁盘使用了 70 GB，<code>du</code> 加起来却只有 40 GB。一个高频原因是文件已经被删除，但进程仍然打开着文件描述符。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsof +L1</span><br></pre></td></tr></table></figure>

<p>输出中 <code>NLINK</code> 为 0 的文件已经从目录结构删除，但只要进程没有关闭句柄，空间就不会真正释放：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">COMMAND   PID USER   FD   TYPE DEVICE  SIZE/OFF NLINK NAME</span><br><span class="line">java    18342 app   12w   REG  253,1 21474836480     0 /var/log/app.log (deleted)</span><br></pre></td></tr></table></figure>

<p>最稳妥的处理方式是让应用正常重新打开日志文件，或者在可控窗口重启对应进程。直接清空 <code>/proc/&lt;PID&gt;/fd/&lt;FD&gt;</code> 指向的内容风险很高，可能破坏正在写入的数据，只能在确认文件用途并获得操作许可后作为应急方案。</p>
<p>这也是为什么删除正在写入的大日志以后，磁盘空间有时完全没有下降。日志轮转应该通知进程重新打开文件，而不是只执行 <code>rm</code>。</p>
<h2 id="4-空间没满，但接口还是很慢"><a href="#4-空间没满，但接口还是很慢" class="headerlink" title="4. 空间没满，但接口还是很慢"></a>4. 空间没满，但接口还是很慢</h2><p>先看系统是不是在等待 IO：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vmstat 1 5</span><br><span class="line">iostat -xz 1 5</span><br></pre></td></tr></table></figure>

<p><code>iostat</code> 重点关注：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">r/s、w/s：每秒读写请求数</span><br><span class="line">rkB/s、wkB/s：每秒读写数据量</span><br><span class="line">await：IO 请求平均等待时间</span><br><span class="line">aqu-sz：平均队列长度</span><br><span class="line">%util：设备忙碌时间比例</span><br></pre></td></tr></table></figure>

<p>机械盘上 <code>%util</code> 长期接近 100% 往往表示设备饱和，但在 SSD、NVMe 和云盘环境里，不能只靠 <code>%util</code> 判断性能上限。还要结合 <code>await</code>、队列长度、吞吐、IOPS 和云平台磁盘配额。</p>
<p>继续定位到进程：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pidstat -d -p ALL 1 5</span><br></pre></td></tr></table></figure>

<p>服务器安装了 <code>iotop</code> 且具备权限时，可以查看实时 IO：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">iotop -oPa</span><br></pre></td></tr></table></figure>

<p><code>-o</code> 只显示正在产生 IO 的进程，<code>-P</code> 按进程聚合，<code>-a</code> 显示累计值。短时间突发任务可能一闪而过，因此最好和监控上的告警时间点对齐。</p>
<h2 id="5-常见的磁盘占用来源"><a href="#5-常见的磁盘占用来源" class="headerlink" title="5. 常见的磁盘占用来源"></a>5. 常见的磁盘占用来源</h2><h3 id="5-1-应用日志"><a href="#5-1-应用日志" class="headerlink" title="5.1 应用日志"></a>5.1 应用日志</h3><p>检查日志目录和 systemd journal：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">du</span> -sh /var/log/* 2&gt;/dev/null | <span class="built_in">sort</span> -h</span><br><span class="line">journalctl --disk-usage</span><br></pre></td></tr></table></figure>

<p>应用日志需要同时限制单文件大小、保留天数和总文件数。只配置按天切割，如果某天流量异常，单个文件仍然可能把磁盘写满。</p>
<h3 id="5-2-Docker-日志与镜像"><a href="#5-2-Docker-日志与镜像" class="headerlink" title="5.2 Docker 日志与镜像"></a>5.2 Docker 日志与镜像</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker system <span class="built_in">df</span></span><br><span class="line">find /var/lib/docker/containers -name <span class="string">&#x27;*-json.log&#x27;</span> -size +500M -<span class="built_in">print</span></span><br></pre></td></tr></table></figure>

<p>不要直接删除 Docker 存储目录里的未知文件。镜像和构建缓存可以先通过 <code>docker system df</code> 确认，再使用明确的清理命令；正在运行容器的 JSON 日志应该通过日志驱动设置轮转。</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;log-driver&quot;</span><span class="punctuation">:</span> <span class="string">&quot;json-file&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;log-opts&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;max-size&quot;</span><span class="punctuation">:</span> <span class="string">&quot;100m&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;max-file&quot;</span><span class="punctuation">:</span> <span class="string">&quot;5&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>修改 Docker daemon 配置会影响后续创建的容器，生产操作前要验证配置并安排维护窗口。</p>
<h3 id="5-3-MySQL-Binlog-和临时文件"><a href="#5-3-MySQL-Binlog-和临时文件" class="headerlink" title="5.3 MySQL Binlog 和临时文件"></a>5.3 MySQL Binlog 和临时文件</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> <span class="type">BINARY</span> LOGS;</span><br><span class="line"><span class="keyword">SHOW</span> VARIABLES <span class="keyword">LIKE</span> <span class="string">&#x27;binlog_expire_logs_seconds&#x27;</span>;</span><br><span class="line"><span class="keyword">SHOW</span> VARIABLES <span class="keyword">LIKE</span> <span class="string">&#x27;expire_logs_days&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>MySQL 8.0 优先使用 <code>binlog_expire_logs_seconds</code>，MySQL 5.7 常见 <code>expire_logs_days</code>。清理 Binlog 要结合主从复制、备份和时间点恢复要求，不能直接从文件系统删除。应使用 MySQL 提供的过期策略或 <code>PURGE BINARY LOGS</code>，并确认从库已经消费。</p>
<h3 id="5-4-临时文件和-Core-Dump"><a href="#5-4-临时文件和-Core-Dump" class="headerlink" title="5.4 临时文件和 Core Dump"></a>5.4 临时文件和 Core Dump</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">du</span> -xhd1 /tmp /var/tmp 2&gt;/dev/null | <span class="built_in">sort</span> -h</span><br><span class="line">find / -xdev -<span class="built_in">type</span> f -name <span class="string">&#x27;core.*&#x27;</span> -size +100M -<span class="built_in">print</span> 2&gt;/dev/null</span><br></pre></td></tr></table></figure>

<p>临时目录也不能按文件年龄盲删，需要确认是否仍被运行中的任务使用。Core Dump 可能是定位崩溃的唯一证据，转移或分析后再按保留策略清理。</p>
<h2 id="6-使用-logrotate-控制日志"><a href="#6-使用-logrotate-控制日志" class="headerlink" title="6. 使用 logrotate 控制日志"></a>6. 使用 logrotate 控制日志</h2><p>一个基础配置示例：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">/var/log/myapp/*.log &#123;</span><br><span class="line">    daily</span><br><span class="line">    rotate 14</span><br><span class="line">    maxsize 200M</span><br><span class="line">    compress</span><br><span class="line">    delaycompress</span><br><span class="line">    missingok</span><br><span class="line">    notifempty</span><br><span class="line">    copytruncate</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>copytruncate</code> 不需要应用重新打开文件，但复制和截断之间可能丢少量日志，大文件复制时也会产生额外 IO。更推荐应用支持滚动日志，或者在 <code>postrotate</code> 中发送信号让进程重新打开文件。具体方式要根据应用日志框架选择。</p>
<p>配置后先做调试检查：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">logrotate -d /etc/logrotate.d/myapp</span><br></pre></td></tr></table></figure>

<p><code>-d</code> 只输出调试信息，不真正轮转。确认结果以后再交给系统定时任务执行。</p>
<h2 id="7-应急清理的顺序"><a href="#7-应急清理的顺序" class="headerlink" title="7. 应急清理的顺序"></a>7. 应急清理的顺序</h2><ol>
<li>确认是容量、inode 还是 IO 性能问题。</li>
<li>找出增长最快的目录和对应写入进程。</li>
<li>优先清理明确可再生、已过期且没有被占用的文件。</li>
<li>需要删除业务文件时，先确认备份和恢复要求。</li>
<li>清理后再次检查 <code>df -h</code>、<code>df -i</code> 和应用状态。</li>
<li>如果空间仍未释放，使用 <code>lsof +L1</code> 检查已删除文件句柄。</li>
<li>恢复后补上轮转、容量告警和增长趋势监控。</li>
</ol>
<h2 id="8-排查清单"><a href="#8-排查清单" class="headerlink" title="8. 排查清单"></a>8. 排查清单</h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">容量：df -hT、du -xhd1、find 大文件</span><br><span class="line">inode：df -ih、按目录统计文件数量</span><br><span class="line">删除未释放：lsof +L1</span><br><span class="line">IO 延迟：vmstat、iostat -xz</span><br><span class="line">进程 IO：pidstat -d、iotop</span><br><span class="line">常见来源：应用日志、Docker、Binlog、临时文件、Core Dump</span><br></pre></td></tr></table></figure>

<p>磁盘告警处理完以后，最好不要只把阈值从 80% 改到 90%。真正有效的复盘是找到增长来源，为它设置保留上限，并保证告警能在磁盘还有足够处理空间时提前触发。</p>
]]></content>
      <categories>
        <category>故障排查</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>故障排查</tag>
        <tag>磁盘IO</tag>
        <tag>服务器运维</tag>
      </tags>
  </entry>
</search>
