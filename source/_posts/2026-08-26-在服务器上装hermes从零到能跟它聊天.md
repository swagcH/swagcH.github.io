---
title: 在服务器上装 Hermes：从零到能跟它聊天，我踩了哪些坑
date: 2026-08-26 12:30:00
updated: 2026-08-26 12:30:00
tags:
  - Hermes
  - AI
  - 服务器
  - 教程
categories: 折腾记录
description: 把 Hermes Agent 装到 Linux 服务器上，配成开机自启，能在飞书里跟它聊天。一篇大白话教程，含我实际踩的坑。
keywords:
  - Hermes Agent
  - Nous Research
  - 服务器部署
  - systemd
  - 飞书机器人
cover:
---

# 在服务器上装 Hermes：从零到能跟它聊天，我踩了哪些坑

说真的，AI Agent 这东西装在本地电脑上其实挺尴尬的——你总不能把笔记本一直开着，让它 24 小时待命吧。

所以我就想：能不能把它装到我的云服务器上？让它一直跑着，微信/飞书/邮件随时能叫起来干活。

折腾了一晚上，还真搞定了。这篇文章就把我做的每一步都写下来，包括中间踩的坑，给也想这么干的人一个参考。

<!-- more -->

## 先说我要达成什么

我想要的是这样：

- Hermes 跑在云服务器后台，关掉 SSH 它也照常工作
- 在飞书里发消息能直接调它（不是网页版那种）
- 服务器重启了不用我手动起来

听起来简单对吧。实际上每一步都有坑。

## 准备工作：你的服务器得有这些

先看下我用的环境，后面所有命令都是基于这个：

```
系统:  OpenCloudOS 9.4（CentOS 系，基本一样）
架构:  x86_64
Node:  v22.15.0
Python: 3.11.6
```

注意 Node 版本。Hermes 官方说需要 `>= 20`，我装的是 22，没问题。

如果你的系统是 Ubuntu/Debian，绝大部分命令也一样，只有 `yum/dnf` 换成 `apt` 的事。

## 第一步：装 Node.js

这个不是装 Hermes 必须的（它其实有 Python 版本），但官方文档和大部分教程都按 Node 走，而且后面如果你想自己写点工具脚本也会用到。装上不亏。

```bash
# 看下有没有
node --version

# 没有的话，OpenCloudOS / CentOS 用 dnf
dnf install -y nodejs npm

# Ubuntu / Debian 用 apt
# apt install -y nodejs npm
```

装完验证一下：

```bash
$ node --version
v22.15.0
$ npm --version
10.9.2
```

如果版本太低（Node < 20），就别用系统源了，去 Node 官网下 LTS。

## 第二步：装 Hermes

这是正文。打开 [Hermes Agent 的 GitHub](https://github.com/NousResearch/hermes-agent)，README 写得很清楚。

我用的是官方推荐的一键脚本：

```bash
curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash
```

跑完会让你选几个东西。**这步我栽过跟头**，所以我一个个说。

### 第一个选择：装哪里

```
Install directory [/usr/local/lib/hermes-agent]:
```

直接回车用默认 `/usr/local/lib/hermes-agent` 就行。改路径后面配 systemd 会麻烦。

### 第二个选择：要不要启 gateway

```
Start the gateway now? [Y/n]:
```

**选 Y**。gateway 就是 Hermes 跟外部世界（飞书、微信）通信的桥梁。不启它就是个命令行工具，不能在飞书里调它。

### 第三个选择：要不要开机自启

```
Enable systemd service? [Y/n]:
```

**选 Y**。你不会想每次重启服务器都手动 `hermes gateway start` 吧。

装完最后会给你一个表格，类似这样：

```
┌─────────────────────────────────────────┐
│  Installation complete                   │
│  Install dir:    /usr/local/lib/hermes  │
│  Python venv:    .../venv                │
│  Gateway:        running                 │
│  Systemd:        enabled                 │
└─────────────────────────────────────────┘
```

## 第三步：验证装好了

不管安装脚本怎么说，自己跑一遍命令确认：

```bash
$ hermes --version
Hermes Agent v0.20.5 (2026.8.19) · upstream 1bbb6e5b
Install directory: /usr/local/lib/hermes-agent
Install method: git
```

看到版本号就算装好了。

再查下 gateway 是否在跑：

```bash
$ hermes gateway status
● hermes-gateway.service - Hermes Agent Gateway
     Loaded: loaded
     Active: active (running) since Tue 2026-08-25 23:30:03 CST
   Main PID: 1998472 (hermes)
      Memory: 596.5M
```

`active (running)` 就对了。

如果显示 `inactive (dead)`，手动起一下：

```bash
systemctl --user start hermes-gateway.service
```

## 第四步：配飞书（或者你用的 IM）

这一步是**真正能聊天**的环节。Hermes 支持飞书、Slack、WhatsApp、邮件……我自己用飞书，所以只讲飞书。

### 4.1 先在飞书开放平台建个应用

打开 [飞书开放平台](https://open.feishu.cn/app)，登录后：

1. 点「创建企业自建应用」
2. 名字随便起，比如「我的 Hermes 助手」
3. 头像、描述随便填
4. 进应用详情页，记下 **App ID** 和 **App Secret**（后面要用）

### 4.2 配权限

在应用的「权限管理」里，至少要勾这几个：

- `im:message` — 收发消息
- `im:message:send_as_bot` — 以机器人身份发消息
- `im:message.p2p_msg` — 私聊（如果你想跟它 1v1 聊）
- `im:message.group_at_msg` — 群聊 @ 机器人

申请完要点「发布版本」才生效。

### 4.3 配事件订阅

在「事件订阅」里：

- 请求网址 URL：填 `https://你的服务器公网IP:端口/hermes/feishu/webhook`
  - 注意要先用 nginx 或别的反代把 HTTPS 配上，**飞书只接受 HTTPS**
  - 或者用 [ngrok](https://ngrok.com) 这种内网穿透
- 加密策略：选「加密」或「明文」都行

### 4.4 把凭证告诉 Hermes

回到服务器，跑：

```bash
hermes setup feishu
```

它会问你 App ID 和 App Secret，照着填。配置文件会写到 `~/.config/hermes/feishu.json`，权限 600。

填完重启 gateway：

```bash
systemctl --user restart hermes-gateway.service
```

### 4.5 验证

在飞书里找你的机器人发一条「你好」。**这里我翻车过一次**——消息发过去完全没反应。

排查思路：

```bash
# 1. 看 gateway 日志
journalctl --user -u hermes-gateway -f

# 2. 看有没有收到回调
grep "feishu" ~/.hermes/logs/*.log

# 3. 检查端口监听
ss -tlnp | grep hermes
```

我那次的问题是**事件订阅的 URL 没配对**。配的是 `http://...`，飞书非要 `https://...`。改成反代之后就好了。

## 第五步：跑个真任务试试

别光发"你好"测试。**用任务验证它**才是真的。

我让它干的第一件事：

> 「帮我看下当前服务器都跑了哪些服务，按 CPU 占用排序」

它的回复（精简版）：

```
跑了 21 个 systemd 服务 + 2 个 Docker 容器。
按 CPU 排：
1. kswapd0 (内核) - 17.2%
2. hermes-gateway (我自己) - 8.7%
3. mysqld - 11.5%
...
```

OK 它确实在跑、确实能看到系统。**装到这里就算完整跑通了**。

## 我踩的坑（建议你提前避）

### 坑 1：systemd 用错了 user 还是 system

Hermes 装的是 `~/.config/systemd/user/hermes-gateway.service`，是**用户级**的。

所以**所有命令都要加 `--user`**：

```bash
systemctl --user status hermes-gateway   # ✅
systemctl status hermes-gateway          # ❌ 找不到
```

如果你服务器开了多用户，gateway 跟用户绑定。用户退了 gateway 也停。要全局用就改成 system 级，但官方不推荐。

### 坑 2：Node 版本 < 20 装不上

```
ERROR: Hermes Agent requires Node.js 20+ (found 18.17.0)
```

老老实实装新版。

### 坑 3：飞书事件订阅 URL 必须 HTTPS

这个上面提了。如果你懒得搞反代，用 `ngrok` 一行命令解决：

```bash
ngrok http 8765
```

把 ngrok 给你的 https 地址填进飞书就行。**仅限测试用**，生产环境别这么干。

### 坑 4：防火墙没开

```bash
# 看端口监听
ss -tlnp | grep -E '8765|hermes'

# 没监听 = gateway 没起来
# 起了但外面连不上 = 防火墙
firewall-cmd --add-port=8765/tcp --permanent
firewall-cmd --reload
```

云服务器（阿里云/腾讯云）**还要去控制台的安全组里开端口**，不然白搭。

### 坑 5：内存不够被 OOM kill

Hermes gateway 启动后内存大概 400-600MB。如果你的服务器只有 1GB 内存，跑它一个就吃满了，再加别的服务就被 OOM。

我建议**至少 2GB 内存**起步。

## 日常怎么用

装好之后基本不用管它了。常用命令：

```bash
# 查 gateway 状态
hermes gateway status

# 看最近日志
hermes logs --tail 50

# 重启 gateway
systemctl --user restart hermes-gateway

# 完全卸载（如果你哪天不想要了）
hermes uninstall
```

如果你想让它**主动**做点什么（比如每天早上 7 点给你发新闻），那就要用 `hermes cron`：

```bash
hermes cron list                    # 列出所有定时任务
hermes cron run <job_id>            # 手动跑一次
hermes cron pause <job_id>          # 暂停
hermes cron resume <job_id>         # 恢复
```

这个我另一篇文章会写。

## 最后说两句

装完 Hermes 在服务器上跑了快两周了，最大的感受是：**这玩意真的能当个私人助手用**。

不是那种"打开网页问一句、答完就走"的玩具，而是真的在你服务器上待命、知道你的环境、能直接动你文件的助手。

比如我最近写代码遇到一个 Node 的报错，直接在飞书里描述给它，它让我贴 log、让我跑几条命令诊断，最后定位到是某个 npm 包的版本冲突——整个过程我手都没离开手机。

当然它不是万能的。复杂决策、需要物理操作的事它干不了。**但作为一个 7x24 待命的程序，它比大部分同类都靠谱**。

如果你们也想装，按我上面写的顺序走一遍，应该 1-2 小时搞定。中间卡住了欢迎来问我——别一个人闷头搞，那是最慢的方式。

---

**附录：完整命令清单（复制就能跑）**

```bash
# 1. 装 Node（如果没装）
dnf install -y nodejs npm  # 或 apt install -y nodejs npm

# 2. 一键装 Hermes
curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash

# 3. 验证
hermes --version
hermes gateway status

# 4. 配飞书
hermes setup feishu
# （按提示填 App ID / App Secret）

# 5. 重启
systemctl --user restart hermes-gateway

# 6. 去飞书给机器人发消息测试
```
