---
title: Nginx反向代理问题
date: 2021-05-23 15:55:12
tags: [Nginx, 反向代理, 网关, 部署]
categories: 故障排查
keywords: Nginx反向代理, upstream timeout, proxy_pass, Nginx配置, 网关排查
cover: /images/posts/2021/nginx-proxy-cover.svg
---

![Nginx反向代理问题技术图](/images/posts/2021/nginx-proxy-cover.svg)

# 背景

反向代理问题需要同时看 Nginx、上游服务和网络连通性。

```text
记录类型：故障排查
记录年份：2021
记录方式：现象、命令、日志、代码、配置、结论
```

# 问题

```text
upstream timed out (110: Connection timed out) while reading response header from upstream
client: 10.0.1.24, server: api.demo.com, request: "POST /api/upload HTTP/1.1"
```

```bash
tail -f /var/log/nginx/error.log
curl -I https://api.demo.com/api/health
```

# 分析

```nginx
location /api/ {
    proxy_pass http://backend;
    proxy_connect_timeout 3s;
    proxy_read_timeout 3s;
}
```

```bash
nginx -t
curl http://127.0.0.1:8080/api/health
```

# 解决方案

```nginx
upstream backend {
    server 127.0.0.1:8080 max_fails=3 fail_timeout=30s;
}

location /api/ {
    proxy_pass http://backend;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_connect_timeout 2s;
    proxy_read_timeout 30s;
}
```

# 总结

```text
复盘要点：
1. 先保留现场，再尝试恢复
2. 先确认影响范围，再判断根因
3. 日志、监控、配置、代码需要串起来看
4. 修复动作必须可验证
5. 故障结束后补充检查项和自动化规则
```
